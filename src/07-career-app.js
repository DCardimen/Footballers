(function () {
  "use strict";
  class RngCore {
    state;
    constructor(t) {
      this.state = t >>> 0 || 1831565813;
    }
    next() {
      let t = (this.state += 1831565813);
      return (
        (t = Math.imul(t ^ (t >>> 15), t | 1)),
        (t ^= t + Math.imul(t ^ (t >>> 7), t | 61)),
        ((t ^ (t >>> 14)) >>> 0) / 4294967296
      );
    }
    between(t, a) {
      return t + (a - t) * this.next();
    }
    integer(t, a) {
      return Math.floor(this.between(t, a + 1));
    }
  }
  const PLAN_IDS = ["disciplined", "feature", "explosive", "team", "recovery"],
    SPECIALIZATIONS = [
      {
        id: "tactician",
        icon: "🧠",
        name: "Tactician",
        description: "Better scouting clarity and stronger matchup counters."
      },
      {
        id: "ironman",
        icon: "🦾",
        name: "Ironman",
        description: "Faster recovery and lower recurrence, not injury immunity."
      },
      {
        id: "competitor",
        icon: "⚔️",
        name: "Competitor",
        description: "Role battles move faster when you outperform a rival."
      },
      { id: "showtime", icon: "🎬", name: "Showtime", description: "More high-leverage upside with more pressure." },
      {
        id: "architect",
        icon: "🏗️",
        name: "Architect",
        description: "Story choices and chemistry effects become more reliable."
      }
    ],
    LEGACY_UNLOCKS = [
      {
        id: "origin-draft",
        icon: "🎭",
        name: "Expanded Origin Draft",
        cost: 4,
        description: "See four origin choices instead of three at the start of each career."
      },
      {
        id: "scout-notebook",
        icon: "📓",
        name: "Scouting Notebook",
        cost: 6,
        description: "Reveal one additional opponent detail in every scouting report."
      },
      {
        id: "rival-dossier",
        icon: "🗂️",
        name: "Rival Dossier",
        cost: 8,
        description: "Reveal rival potential and the trait shaping their role battle."
      },
      {
        id: "story-insight",
        icon: "🧭",
        name: "Story Insight",
        cost: 8,
        description: "Show a tighter estimate of story-choice probability."
      },
      {
        id: "fourth-objective",
        icon: "🏅",
        name: "Fourth Legacy Goal",
        cost: 10,
        description: "Every new run receives one additional optional legacy objective."
      }
    ],
    ORIGINS = [
      {
        id: "late-bloomer",
        icon: "🌱",
        name: "Late Bloomer",
        description: "Starts behind physically, then develops faster after age 16.",
        strength: "Late development and ceiling",
        weakness: "Weaker youth athleticism",
        effects: { startPhysical: -3, potential: 10 },
        objectiveIds: ["varsity-starter", "college-starter", "late-nfl"]
      },
      {
        id: "undersized",
        icon: "🐺",
        name: "Undersized Competitor",
        description: "Smaller frame, sharper technique, permanent chip on the shoulder.",
        strength: "Grit and role battles",
        weakness: "Strength and body fit",
        effects: { mental: 4, grit: 6, strength: -4 },
        objectiveIds: ["beat-rival", "all-state", "undrafted"]
      },
      {
        id: "prodigy",
        icon: "⭐",
        name: "Five-Star Prodigy",
        description: "Obvious talent with enormous expectations.",
        strength: "Starting ability and recruiting",
        weakness: "Pressure and harsher evaluations",
        effects: { startAll: 3, stars: 2, pressure: 14 },
        objectiveIds: ["championship", "award", "first-contract"]
      },
      {
        id: "iron-will",
        icon: "🦾",
        name: "Iron-Will Grinder",
        description: "Durability, discipline, and relentless preparation.",
        strength: "Recovery and availability",
        weakness: "Lower explosive ceiling",
        effects: { durability: 6, recovery: 12, explosive: -3 },
        objectiveIds: ["iron-season", "long-career", "starter"]
      },
      {
        id: "position-convert",
        icon: "🔄",
        name: "Position Convert",
        description: "Raw tools support several possible roles.",
        strength: "Versatility",
        weakness: "Slower early role mastery",
        effects: { versatility: 1, awareness: 3 },
        objectiveIds: ["two-positions", "rotation", "nfl-roster"]
      },
      {
        id: "small-town",
        icon: "🏘️",
        name: "Small-Town Prospect",
        description: "Limited exposure makes every big game an audition.",
        strength: "Loyalty and clutch opportunity",
        weakness: "Recruiting visibility",
        effects: { rep: 4, clutch: 0.12 },
        objectiveIds: ["all-state", "scholarship", "hometown-title"]
      },
      {
        id: "injury-prone",
        icon: "🩹",
        name: "Fragile Phenom",
        description: "Rare talent with a body requiring careful management.",
        strength: "High performance ceiling",
        weakness: "Injury and recurrence risk",
        effects: { startAll: 2, susceptibility: 18, recurrence: 0.16 },
        objectiveIds: ["comeback", "healthy-season", "second-contract"]
      },
      {
        id: "walk-on",
        icon: "🚪",
        name: "Walk-On Mentality",
        description: "No promises; every snap must be earned.",
        strength: "Underdog trust gains",
        weakness: "Low role security",
        effects: { coachTrust: -10, snap: -0.05, grit: 4 },
        objectiveIds: ["starter", "scholarship", "second-contract"]
      }
    ],
    LEGACY_OBJECTIVES = [
      {
        id: "varsity-starter",
        icon: "💡",
        title: "Late Arrival",
        description: "Become a Varsity starter.",
        reward: 2,
        check: e => e.level >= 4 && roleRank(e.depthRole) >= 3
      },
      {
        id: "college-starter",
        icon: "🎓",
        title: "Belong Here",
        description: "Become a College starter.",
        reward: 3,
        check: e => e.level >= 5 && roleRank(e.depthRole) >= 3
      },
      {
        id: "late-nfl",
        icon: "⌛",
        title: "Never Too Late",
        description: "Reach the UFF at age 24 or older.",
        reward: 4,
        check: e => e.level >= 7 && e.age >= 24
      },
      {
        id: "beat-rival",
        icon: "⚔️",
        title: "Win the Job",
        description: "Defeat a named role rival.",
        reward: 2,
        check: e => (e.rivalsDefeatedV11 || 0) >= 1
      },
      {
        id: "all-state",
        icon: "🗺️",
        title: "State Recognition",
        description: "Earn a major high-school award.",
        reward: 2,
        check: e => e.level >= 4 && (e.awards || []).length > 0
      },
      {
        id: "undrafted",
        icon: "📞",
        title: "Undrafted Route",
        description: "Reach an UFF roster without a five-star start.",
        reward: 5,
        check: e => e.level >= 7 && (e.stars || 0) < 5
      },
      {
        id: "championship",
        icon: "🏆",
        title: "Meet Expectations",
        description: "Win a championship.",
        reward: 3,
        check: e => (e.titles || 0) >= 1
      },
      {
        id: "award",
        icon: "🥇",
        title: "Best in the Country",
        description: "Win a major award.",
        reward: 3,
        check: e => (e.awards || []).length >= 1
      },
      {
        id: "first-contract",
        icon: "✍️",
        title: "Franchise Investment",
        description: "Earn an UFF extension.",
        reward: 4,
        check: e => (e.nflStateV11?.contractsEarned || 0) >= 1
      },
      {
        id: "iron-season",
        icon: "🦾",
        title: "Available Every Week",
        description: "Complete a 10-game season without injury.",
        reward: 2,
        check: e => (e.healthySeasonsV11 || 0) >= 1
      },
      {
        id: "long-career",
        icon: "🕰️",
        title: "Built to Last",
        description: "Play beyond age 34.",
        reward: 4,
        check: e => (e.age || 0) >= 35
      },
      {
        id: "starter",
        icon: "📈",
        title: "Earn the First Team",
        description: "Become a starter.",
        reward: 2,
        check: e => roleRank(e.depthRole) >= 3
      },
      {
        id: "two-positions",
        icon: "🔁",
        title: "Positionless",
        description: "Record meaningful time at two positions.",
        reward: 3,
        check: e => (e.positionsPlayedV11 || []).length >= 2
      },
      {
        id: "rotation",
        icon: "🔧",
        title: "Useful Everywhere",
        description: "Earn a rotational role.",
        reward: 1,
        check: e => roleRank(e.depthRole) >= 2
      },
      {
        id: "nfl-roster",
        icon: "🏈",
        title: "Make the 53",
        description: "Earn active UFF roster status.",
        reward: 4,
        check: e => ["active-backup", "rotation", "starter", "franchise"].includes(e.nflStateV11?.status)
      },
      {
        id: "hometown-title",
        icon: "🏘️",
        title: "Hometown Hero",
        description: "Win a title before College.",
        reward: 3,
        check: e => e.level <= 4 && (e.titles || 0) >= 1
      },
      {
        id: "scholarship",
        icon: "🎓",
        title: "Earn the Offer",
        description: "Reach College.",
        reward: 3,
        check: e => e.level >= 5
      },
      {
        id: "comeback",
        icon: "❤️‍🩹",
        title: "Comeback Season",
        description: "Return from a severe injury and post an 80+ game.",
        reward: 4,
        check: e => (e.comebackGamesV11 || 0) >= 1
      },
      {
        id: "healthy-season",
        icon: "✅",
        title: "Managed the Body",
        description: "Complete a healthy full season.",
        reward: 3,
        check: e => (e.healthySeasonsV11 || 0) >= 1
      },
      {
        id: "second-contract",
        icon: "💵",
        title: "Proven Professional",
        description: "Earn a second UFF contract.",
        reward: 5,
        check: e => (e.nflStateV11?.contractsEarned || 0) >= 2
      }
    ],
    MILESTONES = [
      { id: "first-start", icon: "🏈", title: "First Start", tokens: 1, test: e => roleRank(e.depthRole) >= 3 },
      { id: "varsity-roster", icon: "🌃", title: "Friday Night Roster", tokens: 1, test: e => e.level >= 4 },
      { id: "college-offer", icon: "🎓", title: "College Opportunity", tokens: 2, test: e => e.level >= 5 },
      {
        id: "college-starter",
        icon: "📣",
        title: "College Starter",
        tokens: 2,
        test: e => e.level >= 5 && roleRank(e.depthRole) >= 3
      },
      { id: "combine", icon: "⏱️", title: "Combine Invitation", tokens: 3, test: e => e.level >= 6 },
      { id: "nfl-camp", icon: "📋", title: "UFF Training Camp", tokens: 3, test: e => e.level >= 7 },
      {
        id: "active-roster",
        icon: "53",
        title: "Made the Active Roster",
        tokens: 3,
        test: e => ["active-backup", "rotation", "starter", "franchise"].includes(e.nflStateV11?.status)
      },
      {
        id: "second-contract",
        icon: "✍️",
        title: "Second UFF Contract",
        tokens: 4,
        test: e => (e.nflStateV11?.contractsEarned || 0) >= 2
      },
      { id: "champion", icon: "🏆", title: "Champion", tokens: 3, test: e => (e.titles || 0) >= 1 },
      { id: "comeback", icon: "❤️‍🩹", title: "Comeback Complete", tokens: 2, test: e => (e.comebackGamesV11 || 0) >= 1 }
    ];
  function roleRank(e) {
    const t = String(e || "").toLowerCase();
    return t.includes("franchise") || t.includes("team star") || t.includes("first string")
      ? 5
      : t.includes("starter")
        ? 4
        : t.includes("rotation")
          ? 3
          : t.includes("second") || t.includes("active backup")
            ? 2
            : t.includes("practice")
              ? 1
              : 0;
  }
  const clamp = (e, t, a) => Math.max(t, Math.min(a, e)),
    sample = (e, t = Math.random) => e[Math.floor(t() * e.length)];
  function shuffled(e, t = Math.random) {
    const a = [...e];
    for (let s = a.length - 1; s > 0; s--) {
      const n = Math.floor(t() * (s + 1)),
        i = a[s];
      ((a[s] = a[n]), (a[n] = i));
    }
    return a;
  }
  function hashFnv1a(e) {
    let t = 2166136261;
    for (let a = 0; a < e.length; a++) ((t ^= e.charCodeAt(a)), (t = Math.imul(t, 16777619)));
    return t >>> 0;
  }
  function seededRng(...e) {
    const t = new RngCore(hashFnv1a(e.map(a => String(a ?? "")).join("|")));
    return () => t.next();
  }
  function selectOriginOptions(e, t, a = 3) {
    a = Math.max(a, e.legacyUnlocksV11?.["origin-draft"] ? 4 : a);
    const s = seededRng(e.careers || 0, t.name, t.seasonSeed, "origins"),
      n = [...ORIGINS],
      i = [];
    for (; n.length && i.length < a;) i.push(n.splice(Math.floor(s() * n.length), 1)[0]);
    return i;
  }
  function getOrigin(e) {
    return ORIGINS.find(t => t.id === e.originV11);
  }
  /* ===== v131 THE PRICE OF STARTING OVER, IN NUMBERS =====
   * When a career ends and you roll another one, two screens decide the next man and neither of
   * them gave you a number. The ORIGIN DRAFT — three cards, or four with the Expanded Origin Draft
   * — described itself in prose: "Starts behind physically, then develops faster after 16", "Better
   * scouting clarity and stronger matchup counters". Every one of those is a real, exact edit to the
   * sheet (`As` applies it, `gt` applies the body half) and not one of the numbers was shown. And the
   * REROLL PENALTY — the -5% on every attribute you take for abandoning a man unfinished — was on the
   * position screen and the hub card and nowhere near the pregame, which is the screen you read
   * before you play the season it is charging you for.
   *
   * `originSayV131` builds each card's effect list from its own `effects`, in the same order `As`
   * applies them, so a card cannot promise something the game does not do. Which is also how three
   * dead keys turned up: `pressure` (the Prodigy's "harsher evaluations"), `clutch` (the Small-Town
   * Prospect's whole identity) and `versatility` were in the data and read by nothing at all. Two of
   * them are real now — `pressNeedV131` raises the advancement bar the Prodigy is judged against,
   * `clutchV131` is a genuine rating bump in the games that decide a season — and the third always
   * was real, just delivered by a different name (the position-change button is gated on that
   * origin). `window.__V131`; `origincheck.mjs` is the gate. ===== */
  const ORIGIN_GROUP_V131 = {
    startAll: ["every starting attribute", null],
    startPhysical: ["Speed, Strength, Quickness, Acceleration, Agility, Jumping &amp; Stamina", null],
    mental: ["Awareness, Vision, Discipline &amp; Grit", null],
    explosive: ["Speed, Acceleration &amp; Quickness", null],
    durability: ["Durability", null],
    grit: ["Grit", null],
    strength: ["Strength", null],
    awareness: ["Awareness", null]
  };
  function originEffV131(o2) {
    const n = (o2 && o2.effects) || {},
      out = [];
    const pt2 = (k, label) => {
      if (n[k]) out.push({ good: n[k] > 0, t: `<b>${n[k] > 0 ? "+" : "−"}${Math.abs(n[k])}</b> to ${label}` });
    };
    for (const k in ORIGIN_GROUP_V131) pt2(k, ORIGIN_GROUP_V131[k][0]);
    if (n.stars)
      out.push({ good: n.stars > 0, t: `<b>${n.stars > 0 ? "+" : "−"}${Math.abs(n.stars)}</b> recruit ★ at creation` });
    if (n.potential)
      out.push({ good: n.potential > 0, t: `<b>+${n.potential}</b> potential ceiling — a higher wall to grow into` });
    if (n.coachTrust)
      out.push({
        good: n.coachTrust > 0,
        t: `<b>${n.coachTrust > 0 ? "+" : "−"}${Math.abs(n.coachTrust)}</b> coach trust on day one`
      });
    if (n.snap)
      out.push({
        good: n.snap > 0,
        t: `<b>${n.snap > 0 ? "+" : "−"}${Math.abs(Math.round(n.snap * 100))}%</b> of the snaps to start with`
      });
    if (n.rep) out.push({ good: true, t: `<b>+${n.rep}</b> school reputation — more eyes on you` });
    if (n.susceptibility)
      out.push({
        good: n.susceptibility < 0,
        t: `<b>+${n.susceptibility}</b> injury susceptibility, for the whole career`
      });
    if (n.recovery)
      out.push({ good: n.recovery > 0, t: `<b>+${n.recovery}</b> recovery — you heal faster than anyone` });
    if (n.recurrence)
      out.push({ good: false, t: `<b>+${Math.round(n.recurrence * 100)}%</b> chance an old injury comes back` });
    if (n.clutch)
      out.push({
        good: true,
        t: `<b>+${Math.round(n.clutch * TU("clutchK", 60))}</b> rating in playoff and rivalry games`
      });
    if (n.pressure)
      out.push({
        good: false,
        t: `<b>+${(n.pressure * TU("pressNeedK", 0.12)).toFixed(1)}</b> to the OVR you are judged against for promotion`
      });
    if (n.versatility)
      out.push({ good: true, t: `<b>${n.versatility}</b> free position change per level \u2014 nobody else gets one` });
    return out;
  }
  function originSayV131(o2) {
    const rows = originEffV131(o2);
    return rows.length
      ? `<ul class="orig-say-v131">${rows.map(r => `<li class="${r.good ? "up" : "dn"}">${r.t}</li>`).join("")}</ul>`
      : "";
  }
  /* the two keys that were in the data and read by nothing */
  const pressNeedV131 = e => {
    try {
      const a = getOrigin(e);
      return ((a && a.effects && a.effects.pressure) || 0) * TU("pressNeedK", 0.12);
    } catch (_) {
      return 0;
    }
  };
  const clutchV131 = (e, opts) => {
    try {
      if (!opts || !(opts.playoff || opts.rivalV128)) return 0;
      const a = getOrigin(e);
      return (
        ((a && a.effects && a.effects.clutch) || 0) * TU("clutchK", 60) + (opts.playoff ? gearV147("clutch") : 0)
      ); /* v147 C */
    } catch (_) {
      return 0;
    }
  };
  window.__V131 = {
    eff: originEffV131,
    say: originSayV131,
    origins: () => ORIGINS,
    press: e => pressNeedV131(e || (state && state.player)),
    clutch: (e, op) => clutchV131(e || (state && state.player), op || { playoff: !0 }),
    reroll: e => rerollNoteV112(e || (state && state.player))
  };
  function applyOrigin(e, t, a) {
    const s = ORIGINS.find(d => d.id === t);
    if (!s || e.originV11) return s || null;
    ((e.originV11 = s.id),
      (e.originNameV11 = s.name),
      (e.legacyObjectivesV11 = [...s.objectiveIds]),
      (e.completedObjectivesV11 = e.completedObjectivesV11 || {}));
    const n = s.effects,
      i = ["speed", "strength", "quickness", "acceleration", "agility", "jumping", "stamina"],
      r = ["awareness", "vision", "discipline", "grit"],
      l = (d, c) => {
        e.attrs?.[d] != null && (e.attrs[d] = clamp(e.attrs[d] + c, 1, 250));
      };
    return (
      n.startAll && a.forEach(d => l(d, n.startAll)),
      n.startPhysical && i.forEach(d => l(d, n.startPhysical)),
      n.mental && r.forEach(d => l(d, n.mental)),
      ["grit", "strength", "awareness"].forEach(d => n[d] && l(d, n[d])),
      n.durability && l("injuryResist", n.durability),
      n.explosive && ["speed", "acceleration", "quickness"].forEach(d => l(d, n.explosive)),
      n.stars && (e.stars = clamp((e.stars || 1) + n.stars, 1, 5)),
      n.potential && (e.potentialCeil = (e.potentialCeil || 60) + n.potential),
      n.coachTrust && (e.coachTrust = clamp((e.coachTrust || 50) + n.coachTrust, 0, 100)),
      n.snap && (e.snapShare = clamp((e.snapShare || 0.1) + n.snap, 0.04, 0.98)),
      n.rep && (e.schoolRep = (e.schoolRep || 0) + n.rep),
      s
    );
  }
  function ensureCondition(e) {
    e.conditionV11 ||
      (e.conditionV11 = {
        fatigue: 14,
        susceptibility: 18,
        mentalLoad: 18,
        recovery: 55,
        injury: null,
        consecutiveGames: 0,
        lastUpdatedWeek: -1
      });
    const t = e.conditionV11,
      a = getOrigin(e);
    return (
      !e._originConditionAppliedV11 &&
        a &&
        ((t.susceptibility = clamp(t.susceptibility + (a.effects.susceptibility || 0), 0, 100)),
        (t.recovery = clamp(t.recovery + (a.effects.recovery || 0), 0, 100)),
        (e._originConditionAppliedV11 = !0)),
      t
    );
  }
  function conditionModifiers(e) {
    const t = ensureCondition(e),
      a = t.injury ? t.injury.severity * 5 + Math.min(8, t.injury.weeksRemaining) : 0,
      s = Math.max(0, t.fatigue - 45) * 0.16,
      n = Math.max(0, t.mentalLoad - 62) * 0.08,
      i = Math.max(0, (e.age || 18) - 29) * 0.004,
      r = 0.004 + t.susceptibility / 2200 + Math.max(0, t.fatigue - 48) / 3e3 + i + (t.injury?.recurrence || 0) * 0.18;
    return {
      perf: -(a + s + n),
      injuryRisk: clamp(r, 0.003, 0.22),
      snapMultiplier: clamp(1 - t.fatigue / 230 - (t.injury?.severity || 0) * 0.11, 0.35, 1),
      recovery: t.recovery
    };
  }
  const INJURIES = [
    ["Ankle sprain", 1, 2, 0.18],
    ["Hamstring strain", 1, 3, 0.24],
    ["Shoulder bruise", 1, 2, 0.15],
    ["Knee sprain", 2, 4, 0.28],
    ["High ankle sprain", 2, 5, 0.32],
    ["Concussion protocol", 2, 2, 0.12],
    ["Torn muscle", 3, 7, 0.36],
    ["Major knee injury", 3, 10, 0.42]
  ];
  function rollInjury(e, t = Math.random) {
    const a = ensureCondition(e),
      s = clamp((a.fatigue - 50) / 45 + Math.max(0, (e.age || 18) - 31) / 20, 0, 1.3);
    let n = INJURIES.filter(l => l[1] <= (s > 0.72 ? 3 : s > 0.28 ? 2 : 1));
    n.length || (n = [...INJURIES]);
    const i = sample(n, t),
      r = getOrigin(e);
    return {
      id: `inj-${e.seasonSeed || 0}-${e.level || 0}-${a.consecutiveGames}-${Math.floor(t() * 9999)}`,
      name: i[0],
      severity: i[1],
      weeksRemaining: Math.max(1, i[2] + Math.round(s)),
      recurrence: clamp(i[3] + (r?.effects.recurrence || 0), 0.04, 0.7)
    };
  }
  function updateConditionAfterGame(e, t, a, s) {
    const n = ensureCondition(e),
      i = { disciplined: 5, feature: 11, explosive: 16, team: 9, recovery: -10 },
      r = Math.max(0, (e.age || 18) - 28) * 0.7,
      l = t.snaps || Math.round((e.snapShare || 0.2) * 65),
      d = (i[a] ?? 7) + l * 0.12 + r;
    ((n.fatigue = clamp(n.fatigue + d - n.recovery * 0.08, 0, 100)),
      (n.mentalLoad = clamp(n.mentalLoad + Math.abs((t.perf || 60) - 60) * 0.08 + (t.playoff ? 8 : 3) - 4, 0, 100)),
      n.consecutiveGames++);
    const c = getOrigin(e),
      u = Math.max(
        0,
        (n.recovery + (s === "ironman" ? 10 : 0) + (c?.id === "iron-will" ? 7 : 0)) / 28 -
          Math.max(0, (e.age || 18) - 30) * 0.12
      );
    return (
      n.injury &&
        ((n.injury.weeksRemaining = Math.max(
          0,
          n.injury.weeksRemaining - (a === "recovery" || a === "recover" ? 2 : 1)
        )),
        n.injury.weeksRemaining <= 0 && ((e.recentlyRecoveredV11 = n.injury.severity), (n.injury = null))),
      (n.fatigue = clamp(n.fatigue - u * (1 + gearV147("recovery")), 0, 100)),
      t.injured &&
        !n.injury &&
        (n.injury = rollInjury(e, seededRng(e.seasonSeed, e.level, t.week, t.opp, t.perf, "injury-roll"))),
      { fatigueDelta: d - u, condition: n }
    );
  }
  function restBetweenSeasons(e) {
    const t = ensureCondition(e),
      a = clamp(42 - Math.max(0, (e.age || 18) - 29) * 1.4, 12, 45);
    ((t.fatigue = clamp(t.fatigue - a, 0, 100)),
      (t.mentalLoad = clamp(t.mentalLoad - 28, 0, 100)),
      (t.consecutiveGames = 0),
      t.injury &&
        ((t.injury.weeksRemaining = Math.max(0, t.injury.weeksRemaining - 3)),
        t.injury.weeksRemaining <= 0 && (t.injury = null)));
  }
  const OPP_STRENGTHS = [
      ["Relentless pass rush", "disciplined", "explosive"],
      ["Fast, physical secondary", "team", "explosive"],
      ["Elite run fits", "feature", "team"],
      ["Aggressive blitz package", "disciplined", "feature"],
      ["Ball-hawking defense", "disciplined", "explosive"],
      ["High-tempo offense", "recovery", "feature"],
      ["Power rushing attack", "team", "explosive"],
      ["Disciplined coverage unit", "feature", "explosive"]
    ],
    OPP_WEAKNESSES = [
      ["Slow linebackers in space", "explosive"],
      ["Thin defensive rotation", "team"],
      ["Undersized secondary", "feature"],
      ["Overaggressive pursuit", "disciplined"],
      ["Poor tackling after contact", "team"],
      ["Communication breakdowns", "disciplined"],
      ["Vulnerable to explosive plays", "explosive"],
      ["Weak interior protection", "team"]
    ],
    OPP_TENDENCIES = [
      "Disguises pressure until the snap",
      "Rotates safeties late",
      "Overloads the strong side",
      "Attacks inexperienced players",
      "Fades late when tempo rises",
      "Adjusts quickly to repeated concepts",
      "Forces long drives",
      "Gambles on early-down aggression"
    ];
  function createOpponentProfile(e, t, a, s, n) {
    const i = seededRng(s, t, a, e, n, "opponent"),
      r = sample(OPP_STRENGTHS, i);
    let l = sample(OPP_WEAKNESSES, i);
    l[1] === r[1] && (l = OPP_WEAKNESSES[(OPP_WEAKNESSES.indexOf(l) + 1) % OPP_WEAKNESSES.length]);
    const d = n != null,
      c = d && (n || 0) >= 2,
      u = !d && (i() < 0.12 || a === Math.max(2, Math.min(8, t + 3))),
      h = c ? "championship" : d ? "playoff" : u ? "rivalry" : t >= 7 && i() < 0.28 ? "evaluation" : "routine",
      p = 42 + t * 5.7 + i() * 13 + (d ? 7 + (n || 0) * 4 : 0) + tierOppShiftV139(t);
    return {
      id: `${s}-${t}-${a}-${n ?? "r"}`,
      name: e,
      rating: Math.round(p),
      offense: Math.round(p + (i() - 0.5) * 14),
      defense: Math.round(p + (i() - 0.5) * 14),
      tempo: Math.round(35 + i() * 55),
      physicality: Math.round(35 + i() * 55),
      pressure: Math.round(35 + i() * 55),
      strength: r[0],
      weakness: l[0],
      tendency: sample(OPP_TENDENCIES, i),
      counterPlan: l[1],
      trapPlan: r[2],
      rivalry: u,
      importance: h
    };
  }
  function scoutOpponent(e, t, a) {
    const s = t.attrs?.awareness || 10,
      n = t.training === "film" || t.training === "mental" ? 12 : 0,
      i = a === "tactician" ? 18 : 0,
      r = t.legacyUnlocksV11?.["scout-notebook"] ? 10 : 0,
      l = clamp(24 + s * 0.48 + n + i + r, 20, 98),
      d = seededRng(e.id, t.name, l, "scout"),
      c = [
        `Strength: ${e.strength}`,
        `Weakness: ${e.weakness}`,
        `Tendency: ${e.tendency}`,
        `Defense rating: ${e.defense}`,
        `Tempo: ${e.tempo}`
      ],
      u = l >= 82 ? 5 : l >= 66 ? 4 : l >= 48 ? 3 : 2,
      h = shuffled(c, d).slice(0, u);
    let p = e.counterPlan;
    l < 45 &&
      d() < 0.32 &&
      (p = sample(
        PLAN_IDS.filter(C => C !== e.counterPlan),
        d
      ));
    const m = e.rating > t.level * 6 + 58 ? "Difficult" : e.rating < t.level * 5 + 43 ? "Favorable" : "Competitive",
      y = { confidence: Math.round(l), reveals: h, hiddenCount: c.length - u, recommendedPlan: p, matchupLabel: m };
    return ((e.scouted = y), y);
  }
  function matchupPlanModifier(e, t, a, s) {
    let n = 0,
      i = 0,
      r = 0,
      l = "Neutral matchup";
    return (
      t === e.counterPlan && ((n += s === "tactician" ? 7 : 4), (i += 2), (l = `Countered ${e.weakness}`)),
      t === e.trapPlan && ((n -= 5), (r += 0.025), (l = `${e.strength} punished the plan`)),
      a >= 2 && ((n -= Math.min(7, 2 + a * 1.5)), (l += " · opponent adapted")),
      t === "recovery" && ((n -= 3), (r -= 0.08)),
      { perf: n, trust: i, injuryRisk: r, note: l }
    );
  }
  const Ao = [
      "Mentor",
      "Coach Favorite",
      "Quiet Professional",
      "Entitled Star",
      "Friendly Competitor",
      "Dirty Rival",
      "Highly Drafted Rookie",
      "Injury-Prone Veteran"
    ],
    SCOUT_NOTES = [
      "Excellent practice player",
      "Clutch in important games",
      "Limited athletic ceiling",
      "Strong scheme fit",
      "Special-teams value",
      "Media favorite",
      "Fast developer",
      "Recurring injury concern"
    ];
  function ensureRival(e, t, a = Math.random) {
    if (e.roleRivalV11 && e.roleRivalV11.level === e.level) return e.roleRivalV11;
    const s = sample(
        ["Jaylen", "Trey", "DeAndre", "Cooper", "Zane", "Marcus", "Kai", "Bryce", "Tank", "Julian", "Rome", "Deacon"],
        a
      ),
      n = sample(
        ["Washington", "Kingsley", "Duval", "Steele", "Rhodes", "Beckett", "Okoro", "Silva", "Cross", "Vega", "Dunham"],
        a
      ),
      i = sample(Ao, a),
      r = e._displayOvrV11 || t;
    return (
      (e.roleRivalV11 = {
        id: `rival-${e.seasonSeed || 0}-${e.level || 0}-${s}-${n}`,
        name: `${s} ${n}`,
        position: e.pos || "ATH",
        age: e.age || 18,
        ovr: Math.round(clamp(r + (a() - 0.45) * 12, t - 7, t + 12)),
        potential: Math.round(45 + a() * 50),
        personality: i,
        trait: sample(SCOUT_NOTES, a),
        coachTrust: Math.round(46 + a() * 32),
        snapShare: clamp(0.55 + (a() - 0.5) * 0.28, 0.25, 0.88),
        health: Math.round(68 + a() * 31),
        weeklyPerf: 60,
        relationship: ["Friendly Competitor", "Mentor"].includes(i) ? 18 : 0,
        history: [],
        level: e.level
      }),
      e.roleRivalV11
    );
  }
  function resolveRivalWeek(e, t, a, s) {
    const n = ensureRival(e, a, seededRng(e.seasonSeed, t.week, "rival")),
      i = seededRng(e.seasonSeed, t.week, n.id, t.perf, "rival-week"),
      r = n.personality === "Coach Favorite" ? 4 : n.personality === "Quiet Professional" ? 2 : 0;
    ((n.weeklyPerf = Math.round(
      clamp(54 + (n.ovr - a) * 0.65 + r - (100 - n.health) * 0.08 + (i() - 0.5) * 25, 20, 98)
    )),
      (n.health = clamp(n.health - 3 + i() * 6, 25, 100)),
      i() < 0.04 + (100 - n.health) / 1200 && (n.health = clamp(n.health - 28, 10, 100)));
    const l = ((e.coachTrust || 50) - n.coachTrust) * 0.04,
      d = n.relationship > 10 ? 1 : n.relationship < -10 ? -1 : 0,
      c = getOrigin(e),
      u = (s === "competitor" ? 1.14 : 1) * (c?.id === "undersized" ? 1.08 : 1),
      h = clamp((((t.perf || 50) - n.weeklyPerf) * 0.22 + l + d) * u, -12, 15),
      p = clamp((e.snapShare || 0.15) + n.snapShare, 0.65, 1.15);
    ((e.snapShare = clamp((e.snapShare || 0.15) + h / 100, 0.04, 0.94)),
      (n.snapShare = clamp(p - e.snapShare, 0.06, 0.92)),
      (n.coachTrust = clamp(n.coachTrust + (n.weeklyPerf - 60) * 0.08, 0, 100)),
      (n.relationship = clamp(n.relationship + ((t.perf || 50) > n.weeklyPerf ? -1 : 1), -50, 50)),
      c?.id === "walk-on" && (t.perf || 50) > n.weeklyPerf && (e.coachTrust = clamp((e.coachTrust || 50) + 2, 0, 100)),
      n.history.unshift({ week: t.week || 0, playerPerf: t.perf || 50, rivalPerf: n.weeklyPerf, swing: Math.round(h) }),
      (n.history = n.history.slice(0, 8)));
    const m = e.snapShare >= 0.66 && n.snapShare <= 0.3 && (t.perf || 0) >= n.weeklyPerf;
    return (
      m && !n.defeated && ((n.defeated = !0), (e.rivalsDefeatedV11 = (e.rivalsDefeatedV11 || 0) + 1)),
      { rival: n, rivalPerf: n.weeklyPerf, swing: Math.round(h), defeated: m }
    );
  }
  function makeHighLeverageMoments(e, t) {
    const a = t.opponentV11?.importance || (t.playoff ? "playoff" : "routine"),
      s = a === "championship" ? 3 : a === "playoff" ? 2 : ["rivalry", "evaluation"].includes(a) ? 1 : 0;
    if (!s) return [];
    const n = seededRng(e.seasonSeed, t.week, t.opp, e.pos, "moments"),
      i = [
        ["Third and seven", "The defense shows pressure before the snap."],
        ["Red-zone possession", "One mistake ends the drive; one clean rep changes the game."],
        ["Two-minute drill", "The stadium is loud and the clock is moving."],
        ["Fourth-quarter response", "Momentum has shifted and the staff puts the game in your hands."],
        ["Goal-line stand", "Every player knows exactly where the ball is going."]
      ];
    return Array.from({ length: s }, (r, l) => {
      const d = sample(i, n);
      return {
        id: `${t.week}-${l}-${Math.floor(n() * 9999)}`,
        title: d[0],
        copy: d[1],
        index: l,
        choices: Oo(e.pos || "RB")
      };
    });
  }
  function Oo(e) {
    return e === "QB"
      ? [
          {
            id: "safe",
            label: "Take the safe answer",
            description: "Protect the ball and move the chains.",
            chance: 0.78,
            perf: 3,
            fail: -3,
            score: 1,
            risk: "Low"
          },
          {
            id: "deep",
            label: "Attack the deep matchup",
            description: "Trust the read for a game-changing play.",
            chance: 0.46,
            perf: 11,
            fail: -10,
            score: 5,
            risk: "High"
          },
          {
            id: "legs",
            label: "Create with your legs",
            description: "Escape pressure and accept contact.",
            chance: 0.62,
            perf: 7,
            fail: -5,
            score: 3,
            injury: 0.08,
            risk: "Moderate"
          }
        ]
      : e === "RB"
        ? [
            {
              id: "block",
              label: "Follow the blocking",
              description: "Take the efficient gain.",
              chance: 0.8,
              perf: 3,
              fail: -2,
              score: 1,
              risk: "Low"
            },
            {
              id: "bounce",
              label: "Bounce outside",
              description: "Trade certainty for open-field upside.",
              chance: 0.52,
              perf: 9,
              fail: -7,
              score: 4,
              risk: "High"
            },
            {
              id: "contact",
              label: "Lower the shoulder",
              description: "Fight through contact.",
              chance: 0.65,
              perf: 7,
              fail: -4,
              score: 2,
              injury: 0.07,
              risk: "Moderate"
            }
          ]
        : ["WR", "TE"].includes(e)
          ? [
              {
                id: "option",
                label: "Win the option route",
                description: "Read leverage and secure the conversion.",
                chance: 0.74,
                perf: 4,
                fail: -3,
                score: 1,
                risk: "Low"
              },
              {
                id: "vertical",
                label: "Demand the vertical shot",
                description: "Attack the matchup for a signature play.",
                chance: 0.48,
                perf: 11,
                fail: -8,
                score: 5,
                risk: "High"
              },
              {
                id: "traffic",
                label: "Work through traffic",
                description: "Accept contact to find the soft spot.",
                chance: 0.64,
                perf: 7,
                fail: -4,
                score: 2,
                injury: 0.05,
                risk: "Moderate"
              }
            ]
          : e === "OL"
            ? [
                {
                  id: "inside",
                  label: "Protect inside first",
                  description: "Take away the shortest path.",
                  chance: 0.8,
                  perf: 4,
                  fail: -3,
                  score: 1,
                  risk: "Low"
                },
                {
                  id: "aggressive",
                  label: "Jump the protection",
                  description: "Attack the rusher early.",
                  chance: 0.56,
                  perf: 9,
                  fail: -7,
                  score: 3,
                  injury: 0.04,
                  risk: "Moderate"
                },
                {
                  id: "help",
                  label: "Call for help",
                  description: "Reduce exposure at the cost of a route.",
                  chance: 0.86,
                  perf: 2,
                  fail: -2,
                  score: 0,
                  risk: "Very low"
                }
              ]
            : ["DL", "LB", "CB", "S"].includes(e)
              ? [
                  {
                    id: "assignment",
                    label: "Protect the assignment",
                    description: "Keep leverage and force execution.",
                    chance: 0.78,
                    perf: 4,
                    fail: -3,
                    score: 1,
                    risk: "Low"
                  },
                  {
                    id: "jump",
                    label: "Jump the tendency",
                    description: "Gamble on the scouting report.",
                    chance: 0.44,
                    perf: 12,
                    fail: -9,
                    score: 5,
                    risk: "High"
                  },
                  {
                    id: "pressure",
                    label: "Attack downhill",
                    description: "Create disruption with contact exposure.",
                    chance: 0.62,
                    perf: 8,
                    fail: -5,
                    score: 3,
                    injury: 0.05,
                    risk: "Moderate"
                  }
                ]
              : [
                  {
                    id: "steady",
                    label: "Execute the assignment",
                    description: "Choose the reliable football play.",
                    chance: 0.78,
                    perf: 4,
                    fail: -3,
                    score: 1,
                    risk: "Low"
                  },
                  {
                    id: "swing",
                    label: "Chase the big play",
                    description: "Accept volatility for a defining moment.",
                    chance: 0.48,
                    perf: 10,
                    fail: -8,
                    score: 4,
                    risk: "High"
                  },
                  {
                    id: "physical",
                    label: "Win with physicality",
                    description: "Take on contact to create an edge.",
                    chance: 0.64,
                    perf: 7,
                    fail: -4,
                    score: 2,
                    injury: 0.06,
                    risk: "Moderate"
                  }
                ];
  }
  function resolveHighLeverageMoment(e, t, a, s, n) {
    const i = a.choices.find(U => U.id === s) || a.choices[0],
      r = e.attrs?.awareness || 10,
      l = e.composure103 || 50,
      d = conditionModifiers(e),
      c = t.opponentV11?.scouted?.confidence || 40,
      u = getOrigin(e),
      h = u?.id === "small-town" && t.opponentV11?.importance !== "routine" ? 0.045 : 0,
      p = u?.id === "prodigy" ? -0.025 : 0,
      m = (r - 30) / 850 + (l - 50) / 760 + (c - 50) / 1500 + (n === "showtime" ? 0.055 : 0) + h + p + d.perf / 650,
      y = String(i.risk || "").toLowerCase(),
      C = y.includes("high") ? 0.64 : y.includes("moderate") ? 0.76 : 0.86,
      V = clamp(i.chance + m, 0.12, C),
      P = seededRng(e.seasonSeed, t.week, a.id, i.id, t.momentResolutionsV11?.length || 0),
      v = P() < V,
      j = v ? i.perf : i.fail;
    return (
      (t.perf = Math.round(clamp((t.perf || 60) + j, 1, 100))),
      v
        ? (t.us = Math.max(0, (t.us || 0) + i.score))
        : (t.them = Math.max(0, (t.them || 0) + Math.max(0, Math.ceil(i.score / 2)))),
      i.injury && P() < i.injury + d.injuryRisk * 0.18 && (t.injured = !0),
      (t.won = (t.us || 0) > (t.them || 0)),
      (e.composure103 = clamp(l + (v ? 4 : -4), 0, 100)),
      (e.coachTrust = clamp((e.coachTrust || 50) + (v ? 2 : -2), 0, 100)),
      (t.momentResolutionsV11 = t.momentResolutionsV11 || []),
      t.momentResolutionsV11.push({
        momentId: a.id,
        choiceId: s,
        success: v,
        chance: Math.round(V * 100),
        perfDelta: j
      }),
      { success: v, chance: V, perfDelta: j, choice: i }
    );
  }
  const choiceDef = (e, t, a, s, n, i, r) => ({
      id: e,
      label: t,
      description: a,
      risk: s,
      baseChance: n,
      success: i,
      failure: r
    }),
    STORY_ARCS = [
      {
        id: "coach-confidence",
        icon: "📋",
        title: "Coach Loses Confidence",
        minLevel: 3,
        maxLevel: 8,
        cooldown: 5,
        trigger: (e, t) => (t.perf ?? 60) < 48 || (e.coachTrust ?? 50) < 34,
        stages: [
          {
            title: "The Meeting",
            copy: "The staff says your role will shrink unless preparation changes.",
            choices: [
              choiceDef(
                "own",
                "Own the mistakes",
                "Accept responsibility and request a correction plan.",
                "Low risk",
                0.84,
                { trust: 7, awareness: 1 },
                { trust: -2 }
              ),
              choiceDef(
                "push",
                "Challenge the evaluation",
                "Defend your film and demand another opportunity.",
                "High social risk",
                0.44,
                { trust: 5, snap: 0.08, pressure: 4 },
                { trust: -10, snap: -0.08, pressure: 10 }
              ),
              choiceDef(
                "role",
                "Offer to change roles",
                "Trade comfort for immediate usefulness.",
                "Moderate",
                0.66,
                { trust: 8, versatility: 1, snap: 0.05 },
                { roleProgress: -7, fatigue: 5 }
              )
            ]
          },
          {
            title: "Three-Week Evaluation",
            copy: "The staff has watched every snap since the meeting.",
            choices: [
              choiceDef(
                "safe",
                "Execute the safe script",
                "Prioritize assignments and ball security.",
                "Controlled",
                0.78,
                { nextPerf: 5, trust: 5 },
                { nextPerf: -3, trust: -4 }
              ),
              choiceDef(
                "hero",
                "Try to seize the job back",
                "Take the largest possible role.",
                "Boom or bust",
                0.48,
                { nextPerf: 12, trust: 9, roleProgress: 10 },
                { nextPerf: -10, trust: -9, pressure: 8 }
              )
            ]
          }
        ]
      },
      {
        id: "rivalry",
        icon: "⚔️",
        title: "The Rivalry Turns Personal",
        minLevel: 2,
        maxLevel: 8,
        cooldown: 6,
        trigger: e => !!e.roleRivalV11 && Math.abs((e.snapShare ?? 0.1) - (e.roleRivalV11.snapShare ?? 0.5)) < 0.25,
        stages: [
          {
            title: "Practice Collision",
            copy: "Your rival turns an ordinary rep into a public challenge.",
            choices: [
              choiceDef(
                "field",
                "Answer on the field",
                "Keep it football-only and win practice.",
                "Skill roll",
                0.62,
                { roleProgress: 9, relationship: 3, trust: 3 },
                { roleProgress: -5, fatigue: 6 }
              ),
              choiceDef(
                "together",
                "Train together",
                "Turn hostility into useful competition.",
                "Relationship roll",
                0.72,
                { relationship: 14, awareness: 1, roleProgress: 3 },
                { relationship: -7, pressure: 5 }
              ),
              choiceDef(
                "public",
                "Fire back publicly",
                "Create pressure that forces a winner.",
                "Volatile",
                0.43,
                { hype: 10, roleProgress: 12 },
                { trust: -7, relationship: -14, pressure: 10 }
              )
            ]
          },
          {
            title: "The Snap Split",
            copy: "The coaches divide first-team reps evenly.",
            choices: [
              choiceDef(
                "details",
                "Win with details",
                "Choose consistency and assignments.",
                "Reliable",
                0.76,
                { nextPerf: 6, trust: 7, roleProgress: 7 },
                { nextPerf: -3 }
              ),
              choiceDef(
                "highlight",
                "Create the undeniable highlight",
                "Chase a play the staff cannot ignore.",
                "High variance",
                0.46,
                { nextPerf: 14, roleProgress: 15, hype: 8 },
                { nextPerf: -11, roleProgress: -7, injuryRisk: 0.08 }
              )
            ]
          }
        ]
      },
      {
        id: "injury",
        icon: "🩹",
        title: "The Injury Will Not Go Away",
        minLevel: 2,
        maxLevel: 8,
        cooldown: 8,
        trigger: e => !!e.conditionV11?.injury || (e.conditionV11?.fatigue ?? 0) > 84,
        stages: [
          {
            title: "Medical Crossroads",
            copy: "The medical staff offers three paths.",
            choices: [
              choiceDef(
                "rehab",
                "Accept a reduced role",
                "Prioritize healing.",
                "Safe",
                0.93,
                { recovery: 18, snap: -0.1, recurrence: -0.08 },
                { nextPerf: -3 }
              ),
              choiceDef(
                "through",
                "Play through it",
                "Keep the job and accept escalation risk.",
                "Dangerous",
                0.58,
                { nextPerf: 8, trust: 4 },
                { injuryRisk: 0.24, severity: 1, fatigue: 14 }
              ),
              choiceDef(
                "specialist",
                "Seek a specialist",
                "Pursue better information and treatment.",
                "Moderate",
                0.72,
                { recovery: 12, recurrence: -0.12 },
                { pressure: 4, recovery: -4 }
              )
            ]
          },
          {
            title: "Return-to-Play Test",
            copy: "Game speed will reveal whether recovery held.",
            choices: [
              choiceDef(
                "limited",
                "Accept a snap limit",
                "Build back gradually.",
                "Controlled",
                0.88,
                { snap: -0.08, injuryRisk: -0.14, nextPerf: 2 },
                { nextPerf: -2 }
              ),
              choiceDef(
                "full",
                "Request full workload",
                "Prove you are fully back.",
                "High exposure",
                0.61,
                { snap: 0.1, nextPerf: 9, comeback: 1 },
                { injuryRisk: 0.18, fatigue: 10 }
              )
            ]
          }
        ]
      },
      {
        id: "media",
        icon: "📺",
        title: "The Story Becomes Bigger Than Football",
        minLevel: 4,
        maxLevel: 8,
        cooldown: 6,
        trigger: (e, t) => (t.perf ?? 0) >= 88 || (e.worldState?.mediaHeat ?? 0) > 62,
        stages: [
          {
            title: "National Attention",
            copy: "Praise, scrutiny, and next-level questions arrive together.",
            choices: [
              choiceDef(
                "team",
                "Redirect praise to the team",
                "Build chemistry and reduce pressure.",
                "Safe",
                0.9,
                { chemistry: 9, trust: 4, pressure: -7 },
                { hype: -2 }
              ),
              choiceDef(
                "embrace",
                "Embrace the spotlight",
                "Use attention to build opportunity.",
                "Moderate",
                0.67,
                { hype: 12, rep: 5, pressure: 6 },
                { pressure: 12, trust: -3 }
              ),
              choiceDef(
                "guarantee",
                "Make a guarantee",
                "Create a career-defining expectation.",
                "Boom or bust",
                0.45,
                { nextPerf: 12, hype: 14, legacy: 1 },
                { nextPerf: -9, pressure: 15, trust: -6 }
              )
            ]
          },
          {
            title: "Prime-Time Follow-Up",
            copy: "The audience expects another statement.",
            choices: [
              choiceDef(
                "routine",
                "Protect the routine",
                "Ignore the noise.",
                "Reliable",
                0.81,
                { composure: 8, nextPerf: 4 },
                { pressure: 3 }
              ),
              choiceDef(
                "showtime",
                "Build around the moment",
                "Accept volatility for a signature performance.",
                "Volatile",
                0.52,
                { nextPerf: 13, hype: 8 },
                { nextPerf: -10, pressure: 9 }
              )
            ]
          }
        ]
      },
      {
        id: "contract",
        icon: "💼",
        title: "Contract-Year Pressure",
        minLevel: 7,
        maxLevel: 8,
        cooldown: 7,
        trigger: e => e.level >= 7 && (e.nflStateV11?.contract?.yearsRemaining ?? 1) <= 1,
        stages: [
          {
            title: "The Business Meeting",
            copy: "The next month may decide role, security, and guaranteed money.",
            choices: [
              choiceDef(
                "security",
                "Prioritize guaranteed security",
                "Accept a smaller ceiling.",
                "Safe",
                0.85,
                { security: 10, expectation: -4 },
                { security: -2 }
              ),
              choiceDef(
                "bet",
                "Bet on yourself",
                "Decline safety and chase a larger role.",
                "High variance",
                0.52,
                { snap: 0.1, expectation: 8, hype: 8 },
                { security: -10, pressure: 10 }
              ),
              choiceDef(
                "fit",
                "Target scheme fit",
                "Trade salary leverage for opportunity.",
                "Moderate",
                0.71,
                { schemeFit: 12, nextPerf: 5, security: 5 },
                { expectation: 4 }
              )
            ]
          },
          {
            title: "Final Evaluation",
            copy: "The front office has one final performance window.",
            choices: [
              choiceDef(
                "pro",
                "Play the assignment",
                "Show reliability and roster value.",
                "Controlled",
                0.8,
                { nextPerf: 5, security: 8 },
                { security: -5 }
              ),
              choiceDef(
                "star",
                "Make the star case",
                "Demand high-value touches.",
                "Boom or bust",
                0.47,
                { nextPerf: 14, security: 12, hype: 8 },
                { nextPerf: -10, security: -14 }
              )
            ]
          }
        ]
      },
      {
        id: "locker-room",
        icon: "🗣️",
        title: "Locker Room Fracture",
        minLevel: 3,
        maxLevel: 8,
        cooldown: 6,
        trigger: e => (e.worldState?.teamChemistry ?? 50) < 38 || (e.worldState?.mediaHeat ?? 20) > 75,
        stages: [
          {
            title: "The Team Meeting",
            copy: "Veterans and younger players disagree about accountability.",
            choices: [
              choiceDef(
                "mediate",
                "Mediate the room",
                "Use leadership to rebuild alignment.",
                "Leadership roll",
                0.67,
                { chemistry: 14, trust: 5 },
                { chemistry: -7, pressure: 4 }
              ),
              choiceDef(
                "vets",
                "Back the veterans",
                "Gain established support.",
                "Political",
                0.64,
                { trust: 6, chemistry: 4, relationship: -3 },
                { chemistry: -10, hype: -4 }
              ),
              choiceDef(
                "young",
                "Back younger players",
                "Increase energy and opportunity pressure.",
                "Political",
                0.58,
                { snap: 0.06, hype: 7, chemistry: 2 },
                { trust: -8, chemistry: -6 }
              )
            ]
          },
          {
            title: "Leadership Test",
            copy: "A difficult week gives the team one chance to prove the meeting mattered.",
            choices: [
              choiceDef(
                "steady",
                "Set the standard quietly",
                "Lead through preparation.",
                "Reliable",
                0.82,
                { nextPerf: 5, chemistry: 6, trust: 4 },
                { chemistry: -3 }
              ),
              choiceDef(
                "speech",
                "Deliver the emotional speech",
                "Attempt a major momentum swing.",
                "High variance",
                0.53,
                { momentum: 14, chemistry: 10 },
                { momentum: -8, pressure: 5 }
              )
            ]
          }
        ]
      }
    ];
  STORY_ARCS.push(
    {
      id: "academics",
      icon: "📚",
      title: "Eligibility Is Slipping",
      minLevel: 2,
      maxLevel: 5,
      cooldown: 7,
      trigger: (e, t) => (e.attrs?.discipline ?? 50) < 48 && (t.week || 0) % 3 === 0,
      stages: [
        {
          title: "The Warning",
          copy: "A teacher says football disappears if the work does not improve.",
          choices: [
            choiceDef(
              "tutor",
              "Accept tutoring",
              "Give up free time for stability.",
              "Safe",
              0.86,
              { awareness: 1, trust: 3, fatigue: 4 },
              { pressure: 3 }
            ),
            choiceDef(
              "cram",
              "Cram alone",
              "Protect practice time and gamble on the exam.",
              "Moderate",
              0.62,
              { discipline: 1, nextPerf: 2 },
              { trust: -4, pressure: 7 }
            )
          ]
        },
        {
          title: "Eligibility Check",
          copy: "One final week decides whether you stay on the field.",
          choices: [
            choiceDef(
              "finish",
              "Finish the work first",
              "Sacrifice preparation to secure eligibility.",
              "Reliable",
              0.82,
              { trust: 5, nextPerf: -2 },
              { snap: -0.08 }
            ),
            choiceDef(
              "balance",
              "Try to do everything",
              "Keep all upside with more stress.",
              "High variance",
              0.54,
              { nextPerf: 6, trust: 3 },
              { fatigue: 10, pressure: 8 }
            )
          ]
        }
      ]
    },
    {
      id: "recruiting",
      icon: "📬",
      title: "Recruiting Week",
      minLevel: 4,
      maxLevel: 5,
      cooldown: 7,
      trigger: (e, t) => (t.perf ?? 0) >= 72 && (t.week || 0) % 3 === 1,
      stages: [
        {
          title: "The First Serious Offer",
          copy: "A program offers opportunity, but not the role you imagined.",
          choices: [
            choiceDef(
              "fit",
              "Choose development fit",
              "Prioritize coaching and patience.",
              "Reliable",
              0.8,
              { rep: 5, awareness: 1, trust: 3 },
              { hype: -2 }
            ),
            choiceDef(
              "stage",
              "Choose the biggest stage",
              "Bet on exposure and competition.",
              "Moderate",
              0.61,
              { hype: 8, momentum: 6 },
              { roleProgress: -6, pressure: 7 }
            )
          ]
        },
        {
          title: "Signing Pressure",
          copy: "Coaches want an answer before the season is over.",
          choices: [
            choiceDef(
              "commit",
              "Commit early",
              "Bank certainty and reduce noise.",
              "Safe",
              0.87,
              { pressure: -8, trust: 4 },
              { hype: -2 }
            ),
            choiceDef(
              "wait",
              "Wait for the dream offer",
              "Keep the ceiling open.",
              "High risk",
              0.49,
              { rep: 10, hype: 8 },
              { pressure: 11, trust: -4 }
            )
          ]
        }
      ]
    },
    {
      id: "transfer",
      icon: "🚪",
      title: "A Transfer Door Opens",
      minLevel: 4,
      maxLevel: 6,
      cooldown: 8,
      trigger: (e, t) => (e.snapShare ?? 0.1) < 0.32 && (t.week || 0) >= 5,
      stages: [
        {
          title: "The Call",
          copy: "Another program promises a cleaner path to playing time.",
          choices: [
            choiceDef(
              "stay",
              "Stay and compete",
              "Keep relationships and win the job.",
              "Moderate",
              0.69,
              { trust: 7, roleProgress: 7 },
              { roleProgress: -3 }
            ),
            choiceDef(
              "leave",
              "Take the new opportunity",
              "Reset the room for greater upside.",
              "High variance",
              0.56,
              { snap: 0.12, momentum: 8 },
              { trust: -7, pressure: 7 }
            )
          ]
        },
        {
          title: "The Consequence",
          copy: "Your choice changes how coaches and teammates view the season.",
          choices: [
            choiceDef(
              "earn",
              "Keep your head down",
              "Let work repair the relationships.",
              "Reliable",
              0.81,
              { chemistry: 7, trust: 5 },
              { pressure: 3 }
            ),
            choiceDef(
              "speak",
              "Explain the decision publicly",
              "Try to control the story.",
              "Volatile",
              0.51,
              { hype: 8, chemistry: 4 },
              { media: 10, trust: -5 }
            )
          ]
        }
      ]
    },
    {
      id: "nil",
      icon: "💵",
      title: "Money Arrives Before the UFF",
      minLevel: 5,
      maxLevel: 5,
      cooldown: 7,
      trigger: (e, t) => (t.perf ?? 0) >= 78,
      stages: [
        {
          title: "The NIL Deal",
          copy: "A local sponsor offers real money and real obligations.",
          choices: [
            choiceDef(
              "small",
              "Take the smaller clean deal",
              "Limit distraction and build security.",
              "Safe",
              0.88,
              { hype: 4, pressure: 2 },
              { nextPerf: -1 }
            ),
            choiceDef(
              "large",
              "Take the national campaign",
              "Gain attention with a crowded schedule.",
              "Moderate",
              0.63,
              { hype: 13, rep: 7 },
              { fatigue: 9, pressure: 10 }
            ),
            choiceDef(
              "decline",
              "Decline and focus",
              "Keep football simple.",
              "Reliable",
              0.9,
              { nextPerf: 4, trust: 3 },
              { hype: -5 }
            )
          ]
        },
        {
          title: "Delivery Day",
          copy: "The sponsor expects appearances during a critical football week.",
          choices: [
            choiceDef(
              "show",
              "Honor every appearance",
              "Protect the relationship.",
              "Controlled",
              0.78,
              { hype: 7, rep: 3 },
              { fatigue: 7 }
            ),
            choiceDef(
              "football",
              "Cancel for football",
              "Protect preparation and risk backlash.",
              "Political",
              0.57,
              { nextPerf: 7 },
              { hype: -8, pressure: 5 }
            )
          ]
        }
      ]
    },
    {
      id: "draft-stock",
      icon: "📊",
      title: "Draft Stock Is Moving",
      minLevel: 5,
      maxLevel: 6,
      cooldown: 6,
      trigger: (e, t) => (t.perf ?? 0) >= 82 || e.level === 6,
      stages: [
        {
          title: "The Evaluation",
          copy: "Scouts identify one trait that may decide your future.",
          choices: [
            choiceDef(
              "repair",
              "Attack the weakness",
              "Trade comfort for measurable growth.",
              "Reliable",
              0.79,
              { awareness: 1, nextPerf: 4, fatigue: 5 },
              { nextPerf: -2 }
            ),
            choiceDef(
              "strength",
              "Showcase the strength",
              "Double down on what makes you special.",
              "Moderate",
              0.62,
              { hype: 8, nextPerf: 7 },
              { pressure: 6 }
            )
          ]
        },
        {
          title: "The Final Impression",
          copy: "One workout or game can move you several rounds.",
          choices: [
            choiceDef(
              "clean",
              "Post the clean result",
              "Avoid catastrophe and confirm the floor.",
              "Controlled",
              0.82,
              { trust: 4, rep: 5 },
              { rep: -2 }
            ),
            choiceDef(
              "record",
              "Chase the headline number",
              "Risk a mistake for a historic result.",
              "Boom or bust",
              0.46,
              { hype: 15, rep: 12 },
              { injuryRisk: 0.12, nextPerf: -8 }
            )
          ]
        }
      ]
    },
    {
      id: "rookie-wall",
      icon: "🧱",
      title: "The Rookie Wall",
      minLevel: 7,
      maxLevel: 7,
      cooldown: 7,
      trigger: (e, t) => (e.age ?? 23) <= 25 && ((e.conditionV11?.fatigue ?? 0) > 58 || (t.week || 0) >= 9),
      stages: [
        {
          title: "The Body Changes",
          copy: "The UFF calendar is longer and faster than anything before it.",
          choices: [
            choiceDef(
              "routine",
              "Rebuild the weekly routine",
              "Spend time on sleep, food, and treatment.",
              "Reliable",
              0.84,
              { recovery: 9, fatigue: -8 },
              { nextPerf: -2 }
            ),
            choiceDef(
              "push",
              "Keep the rookie pace",
              "Refuse to give away reps.",
              "Dangerous",
              0.56,
              { nextPerf: 8, trust: 4 },
              { fatigue: 14, injuryRisk: 0.12 }
            )
          ]
        },
        {
          title: "December Test",
          copy: "The staff needs to know whether your body can finish the season.",
          choices: [
            choiceDef(
              "manage",
              "Accept managed snaps",
              "Protect availability.",
              "Controlled",
              0.86,
              { snap: -0.06, recovery: 7, security: 5 },
              { nextPerf: -2 }
            ),
            choiceDef(
              "full",
              "Demand full work",
              "Make the durability statement.",
              "High exposure",
              0.57,
              { nextPerf: 10, security: 8 },
              { injuryRisk: 0.14, fatigue: 10 }
            )
          ]
        }
      ]
    },
    {
      id: "trade-rumors",
      icon: "🔄",
      title: "Trade Rumors Spread",
      minLevel: 7,
      maxLevel: 8,
      cooldown: 8,
      trigger: (e, t) => (e.nflStateV11?.security ?? 60) < 46 && (t.week || 0) % 3 === 0,
      stages: [
        {
          title: "The Rumor",
          copy: "Your name appears in calls around the league.",
          choices: [
            choiceDef(
              "professional",
              "Stay professional",
              "Protect reputation and focus.",
              "Reliable",
              0.84,
              { security: 5, trust: 4 },
              { pressure: 3 }
            ),
            choiceDef(
              "request",
              "Request a better fit",
              "Push for a cleaner opportunity.",
              "Political",
              0.55,
              { schemeFit: 10, snap: 0.06 },
              { security: -8, trust: -6 }
            )
          ]
        },
        {
          title: "Deadline Week",
          copy: "The team either recommits or moves on.",
          choices: [
            choiceDef(
              "prove",
              "Make them keep you",
              "Take the largest available role.",
              "High variance",
              0.55,
              { nextPerf: 12, security: 10 },
              { nextPerf: -9, security: -12 }
            ),
            choiceDef(
              "accept",
              "Accept the business",
              "Protect body and reputation.",
              "Controlled",
              0.8,
              { recovery: 5, security: 4 },
              { hype: -3 }
            )
          ]
        }
      ]
    },
    {
      id: "leadership",
      icon: "🧭",
      title: "The Team Needs a Leader",
      minLevel: 5,
      maxLevel: 8,
      cooldown: 8,
      trigger: (e, t) => (e.age ?? 18) >= 24 && (e.coachTrust ?? 0) >= 62,
      stages: [
        {
          title: "The Younger Players Ask",
          copy: "Teammates begin following your preparation habits.",
          choices: [
            choiceDef(
              "mentor",
              "Mentor them directly",
              "Spend time building the room.",
              "Reliable",
              0.86,
              { chemistry: 10, trust: 5, fatigue: 3 },
              { nextPerf: -1 }
            ),
            choiceDef(
              "standard",
              "Set the standard silently",
              "Lead by performance.",
              "Controlled",
              0.8,
              { nextPerf: 5, trust: 4 },
              { chemistry: -2 }
            )
          ]
        },
        {
          title: "Leadership Costs Something",
          copy: "A teammate mistake puts your reputation on the line.",
          choices: [
            choiceDef(
              "protect",
              "Protect the teammate",
              "Take responsibility and preserve trust.",
              "Political",
              0.67,
              { chemistry: 12, trust: 3 },
              { hype: -5 }
            ),
            choiceDef(
              "account",
              "Demand accountability",
              "Risk conflict to protect standards.",
              "Moderate",
              0.61,
              { trust: 6, momentum: 7 },
              { chemistry: -7 }
            )
          ]
        }
      ]
    },
    {
      id: "veteran-role",
      icon: "🕰️",
      title: "The Veteran Role Changes",
      minLevel: 7,
      maxLevel: 8,
      cooldown: 9,
      trigger: (e, t) => (e.age ?? 0) >= 31 && (t.week || 0) >= 6,
      stages: [
        {
          title: "The Rookie Is Coming",
          copy: "A younger player begins taking developmental reps.",
          choices: [
            choiceDef(
              "mentor",
              "Mentor and compete",
              "Increase team value beyond the box score.",
              "Reliable",
              0.83,
              { security: 7, chemistry: 8 },
              { snap: -0.03 }
            ),
            choiceDef(
              "fight",
              "Fight every rep",
              "Protect the role through performance.",
              "High variance",
              0.55,
              { snap: 0.09, nextPerf: 8 },
              { fatigue: 10, security: -7 }
            )
          ]
        },
        {
          title: "The New Reality",
          copy: "The staff asks what role you are willing to accept.",
          choices: [
            choiceDef(
              "adapt",
              "Accept a flexible role",
              "Extend the career through versatility.",
              "Controlled",
              0.84,
              { security: 10, schemeFit: 7 },
              { hype: -3 }
            ),
            choiceDef(
              "start",
              "Demand to start",
              "Bet the career on one standard.",
              "Boom or bust",
              0.46,
              { snap: 0.13, security: 8 },
              { security: -16, trust: -8 }
            )
          ]
        }
      ]
    },
    {
      id: "family-pressure",
      icon: "❤️",
      title: "Football Is Straining Home",
      minLevel: 7,
      maxLevel: 8,
      cooldown: 8,
      trigger: (e, t) => (e.lifeV12?.fulfillment ?? 50) < 40,
      stages: [
        {
          title: "The Missed Moment",
          copy: "Someone important asks whether football always has to come first.",
          choices: [
            choiceDef(
              "home",
              "Protect family time",
              "Give up preparation for a stronger home life.",
              "Reliable",
              0.88,
              { pressure: -8, recovery: 4, nextPerf: -2 },
              { pressure: 3 }
            ),
            choiceDef(
              "season",
              "Ask them to wait",
              "Keep football first through the season.",
              "Moderate",
              0.63,
              { nextPerf: 6 },
              { pressure: 9, chemistry: -3 }
            )
          ]
        },
        {
          title: "The Promise",
          copy: "Your answer will shape the rest of the year.",
          choices: [
            choiceDef(
              "plan",
              "Create a real plan",
              "Turn the promise into scheduled action.",
              "Controlled",
              0.82,
              { pressure: -8, composure: 5 },
              { fatigue: 3 }
            ),
            choiceDef(
              "gift",
              "Try to solve it with money",
              "Buy relief without changing time.",
              "Political",
              0.58,
              { hype: 3 },
              { pressure: 6 }
            )
          ]
        }
      ]
    },
    {
      id: "retirement-question",
      icon: "🌅",
      title: "The Retirement Question",
      minLevel: 7,
      maxLevel: 8,
      cooldown: 10,
      trigger: (e, t) => (e.age ?? 0) >= 34 && (t.week || 0) >= 8,
      stages: [
        {
          title: "How Much Is Enough?",
          copy: "The body, money, and family all point in different directions.",
          choices: [
            choiceDef(
              "plan",
              "Build the retirement plan",
              "Reduce uncertainty and protect the future.",
              "Reliable",
              0.87,
              { recovery: 4, pressure: -6 },
              { nextPerf: -1 }
            ),
            choiceDef(
              "ring",
              "Chase one more ring",
              "Accept one more year of risk.",
              "High variance",
              0.55,
              { nextPerf: 9, hype: 7 },
              { injuryRisk: 0.1, fatigue: 8 }
            )
          ]
        },
        {
          title: "The Final Standard",
          copy: "You decide what would make walking away feel complete.",
          choices: [
            choiceDef(
              "health",
              "Leave healthy",
              "Protect the body and family.",
              "Controlled",
              0.85,
              { recovery: 8, security: 3 },
              { hype: -2 }
            ),
            choiceDef(
              "legacy",
              "Leave legendary",
              "Chase the defining finish.",
              "Boom or bust",
              0.48,
              { nextPerf: 13, hype: 12 },
              { injuryRisk: 0.14, pressure: 10 }
            )
          ]
        }
      ]
    }
  );
  function ensureStoryState(e) {
    ((e.storyDecisionQueueV11 = e.storyDecisionQueueV11 || []),
      (e.storyArcHistoryV11 = e.storyArcHistoryV11 || []),
      (e.activeStoryArcV11 = e.activeStoryArcV11 || null),
      (e.storyArcsThisSeasonV11 = e.storyArcsThisSeasonV11 || 0));
  }
  function $n(e, t, a) {
    const s = STORY_ARCS.find(r => r.id === t);
    if (!s) return null;
    if (a >= s.stages.length) {
      const r = e.storyArcHistoryV11.find(l => l.id === t && l.status === "active");
      return (
        r && (r.status = "resolved"),
        (e.completedStoryArcsV11 = (e.completedStoryArcsV11 || 0) + 1),
        (e.activeStoryArcV11 = null),
        null
      );
    }
    const n = s.stages[a];
    ((e.activeStoryArcV11.stage = a), (e.activeStoryArcV11.weeksSinceStage = 0));
    const i = {
      id: `arc-${t}-${a}-${e.seasonSeed || 0}-${e.totalSeasons || 0}-${e.decisionCount || 0}`,
      templateId: t,
      stageIndex: a,
      icon: s.icon,
      arcTitle: s.title,
      title: n.title,
      copy: n.copy,
      choices: n.choices
    };
    return (e.storyDecisionQueueV11.push(i), i);
  }
  /* ===== v18 CHOICE EXPANSION — every story-arc stage offers 5 options. The extra
   * options draw from a wide pool of buffs and negatives (form, health, locker room,
   * media, coach trust, injury risk...). Labels/risk words are written so the
   * personality wheel reads them: bold personas gravitate to the loud options,
   * disciplined/even-keeled personas to the safe and healing ones. ===== */
  const V18_POOL = [
    (a, st) =>
      choiceDef(
        "v18-film",
        "Live in the film room",
        "Out-prepare everyone this week.",
        "Disciplined",
        0.8,
        { nextPerf: 5, awareness: 1 },
        { fatigue: 6 }
      ),
    (a, st) =>
      choiceDef(
        "v18-media",
        "Feed the media a headline",
        "Talk loud, then back it up.",
        "Brash gamble",
        0.5,
        { hype: 9, momentum: 6, nextPerf: 6 },
        { pressure: 9, trust: -4 }
      ),
    (a, st) =>
      choiceDef(
        "v18-rest",
        "Rest and let the body heal",
        "Skip extras, sleep, treatment. Nature does the work.",
        "Safe recovery",
        0.9,
        { fatigue: -16, recovery: 12, injuryRisk: -0.1 },
        { roleProgress: -3 }
      ),
    (a, st) =>
      choiceDef(
        "v18-rally",
        "Rally the locker room",
        "Team dinner, hard talks, tighter huddle.",
        "Steady",
        0.72,
        { chemistry: 9, trust: 4 },
        { composure: -3 }
      ),
    (a, st) =>
      choiceDef(
        "v18-redline",
        "Red-line the whole week",
        "Max reps, max speed, no brakes.",
        "Reckless",
        0.45,
        { nextPerf: 13, momentum: 8 },
        { fatigue: 16, injuryRisk: 0.15 }
      ),
    (a, st) =>
      choiceDef(
        "v18-mentor",
        "Pour into a young teammate",
        "Give away your secrets, earn the room.",
        "Selfless",
        0.78,
        { trust: 6, chemistry: 6, legacy: 1 },
        { snap: -0.04 }
      ),
    (a, st) =>
      choiceDef(
        "v18-guru",
        "Hire a private specialist",
        "Pay for elite outside coaching.",
        "Calculated",
        0.68,
        { nextPerf: 8, awareness: 2 },
        { pressure: 5 }
      ),
    (a, st) =>
      choiceDef(
        "v18-tubs",
        "Cold tubs and bodywork",
        "A full week of recovery protocol.",
        "Careful healing",
        0.85,
        { recovery: 14, fatigue: -10, recurrence: -0.06 },
        { momentum: -3 }
      ),
    (a, st) =>
      choiceDef(
        "v18-hero",
        "Freelance for the highlight",
        "Break the play call and chase glory.",
        "Wild",
        0.4,
        { nextPerf: 15, hype: 10 },
        { trust: -8, roleProgress: -6 }
      ),
    (a, st) =>
      choiceDef(
        "v18-quiet",
        "Keep your head down",
        "No noise. Just stack quiet, clean days.",
        "Humble",
        0.82,
        { roleProgress: 5, trust: 5 },
        { hype: -4 }
      ),
    (a, st) =>
      choiceDef(
        "v18-callout",
        "Call the group out",
        "Demand a higher standard, loudly.",
        "Fiery",
        0.5,
        { momentum: 10, roleProgress: 8 },
        { chemistry: -8, trust: -6 }
      ),
    (a, st) =>
      choiceDef(
        "v18-zen",
        "Breathwork and visualization",
        "Slow everything down. Heal the mind.",
        "Composed",
        0.75,
        { composure: 9, recovery: 6, fatigue: -6 },
        { nextPerf: -2 }
      ),
    (a, st) =>
      choiceDef(
        "v18-iron",
        "Attack the weight room",
        "Armor the body against the season.",
        "Grind",
        0.6,
        { nextPerf: 6, injuryRisk: -0.06 },
        { fatigue: 12 }
      ),
    (a, st) =>
      choiceDef(
        "v18-scout",
        "Obsess over your matchup",
        "Know the man across from you cold.",
        "Prepared",
        0.7,
        { nextPerf: 7, awareness: 1 },
        { composure: -2 }
      )
  ];
  const V18_HEAL = [2, 7, 11]; // pool indexes that naturally heal — surfaced first on injury/fatigue arcs
  const v18h = w => {
    let h = 0;
    for (let i = 0; i < w.length; i++) h = (h * 31 + w.charCodeAt(i)) >>> 0;
    return h;
  };
  window.__NtV18 = STORY_ARCS;
  try {
    STORY_ARCS.forEach(arc =>
      arc.stages.forEach((st, si) => {
        if (!st || !Array.isArray(st.choices)) return;
        const healy = arc.id === "injury" || /injur|fatigue|health|body/i.test(arc.title || "");
        let gi = 0,
          guard = 0;
        const order = healy ? V18_HEAL.concat(V18_POOL.map((_, i) => i)) : V18_POOL.map((_, i) => i);
        while (st.choices.length < 5 && guard++ < 40) {
          const idx = order[(v18h(arc.id + ":" + si) + gi++) % order.length];
          const op = V18_POOL[idx](arc, st);
          if (st.choices.some(c => c && c.id === op.id)) continue;
          st.choices.push(op);
        }
      })
    );
  } catch (e) {}
  /* v150 A: retired long ago — everything after its leading return null was unreachable, and is gone */
  function maybeStartStoryArc() {
    return null;
  }
  function addClamped(e, t, a, s = 0, n = 100) {
    a && (e[t] = clamp((e[t] || 0) + a, s, n));
  }
  function Bo(e, t, a) {
    if (!a) return;
    ((e.worldState = e.worldState || { teamChemistry: 50, programMomentum: 50, mediaHeat: 20 }),
      addClamped(e, "coachTrust", a.trust || 0),
      addClamped(e, "snapShare", a.snap || 0, 0.04, 0.98),
      addClamped(e, "roleBattle103", a.roleProgress || 0, 0, 125),
      addClamped(e, "composure103", a.composure || 0),
      addClamped(e, "momentum103", a.momentum || 0),
      addClamped(e.worldState, "teamChemistry", a.chemistry || 0),
      addClamped(e.worldState, "mediaHeat", a.pressure || a.hype || 0),
      addClamped(e.worldState, "programMomentum", a.momentum || 0),
      a.rep && (e.schoolRep = (e.schoolRep || 0) + a.rep),
      a.security && e.nflStateV11 && addClamped(e.nflStateV11, "security", a.security),
      a.expectation && e.nflStateV11 && addClamped(e.nflStateV11, "expectation", a.expectation, 20, 95),
      a.schemeFit && e.nflStateV11 && addClamped(e.nflStateV11, "schemeFit", a.schemeFit));
    const s = ensureCondition(e);
    (addClamped(s, "fatigue", a.fatigue || 0),
      addClamped(s, "recovery", a.recovery || 0),
      s.injury &&
        (a.severity && (s.injury.severity = clamp(s.injury.severity + a.severity, 1, 3)),
        a.recurrence && (s.injury.recurrence = clamp(s.injury.recurrence + a.recurrence, 0.02, 0.75))),
      (a.injuryRisk || a.nextPerf) &&
        ((e.tempEffects = e.tempEffects || {}),
        (e.tempEffects[
          `arc-${e.seasonSeed || 0}-${e.totalSeasons || 0}-${e.decisionCount || 0}-${Object.keys(e.tempEffects).length}`
        ] = { games: 2, fx: { injuryRisk: a.injuryRisk || 0, nextPerf: a.nextPerf || 0 } })),
      a.awareness && e.attrs?.awareness != null && (e.attrs.awareness = clamp(e.attrs.awareness + a.awareness, 1, 250)),
      a.versatility &&
        ((e.positionsPlayedV11 = e.positionsPlayedV11 || [e.pos]),
        (e.versatilityV11 = (e.versatilityV11 || 0) + a.versatility)),
      a.relationship && e.roleRivalV11 && addClamped(e.roleRivalV11, "relationship", a.relationship, -50, 50),
      a.comeback && (e.comebackPendingV11 = !0),
      a.legacy && (t.legacyTokensV11 = (t.legacyTokensV11 || 0) + a.legacy));
  }
  function resolveStoryChoice(e, t, a, s) {
    ensureStoryState(e);
    const n = e.storyDecisionQueueV11[0];
    if (!n) return null;
    const i = n.choices.find(C => C.id === a) || n.choices[0],
      r = e.worldState?.teamChemistry || 50,
      l = e.coachTrust || 50,
      d = e.attrs?.awareness || 10,
      c = (r - 50) / 1e3 + (l - 50) / 1200 + (d - 20) / 1500 + (s === "architect" ? 0.045 : 0),
      u = String(i.risk || "").toLowerCase(),
      h = u.includes("danger")
        ? 0.55
        : u.includes("high") || u.includes("volatile") || u.includes("boom")
          ? 0.62
          : u.includes("moderate") || u.includes("political")
            ? 0.74
            : 0.82,
      p = clamp(i.baseChance + c, 0.12, h),
      m = seededRng(e.seasonSeed, n.id, i.id, e.decisionCount || 0),
      y = m() < p;
    if (
      (Bo(e, t, y ? i.success : i.failure),
      e.storyDecisionQueueV11.shift(),
      (e.decisionCount = (e.decisionCount || 0) + 1),
      e.activeStoryArcV11)
    ) {
      e.activeStoryArcV11.choices.push({ stage: n.stageIndex, choiceId: a, success: y });
      const C = STORY_ARCS.find(V => V.id === n.templateId)?.stages.length || 1;
      if (n.stageIndex >= C - 1) {
        const V = e.storyArcHistoryV11.find(P => P.id === n.templateId && P.status === "active");
        (V && (V.status = "resolved"),
          (e.completedStoryArcsV11 = (e.completedStoryArcsV11 || 0) + 1),
          (e.activeStoryArcV11 = null));
      }
    }
    return { success: y, chance: p, choice: i, decision: n };
  }
  function nflSalaryForStatus(e, t, a = 50) {
    const s = Math.max(0, t - 68),
      n = Math.max(0, a - 40),
      i = {
        camp: 9e5,
        "practice-squad": 31e4,
        "active-backup": 145e4,
        rotation: 34e5,
        starter: 85e5,
        franchise: 235e5,
        "free-agent": 11e5,
        waivers: 78e4
      },
      r = {
        camp: 35e3,
        "practice-squad": 9e3,
        "active-backup": 65e3,
        rotation: 135e3,
        starter: 31e4,
        franchise: 76e4,
        "free-agent": 45e3,
        waivers: 25e3
      };
    return Math.round(
      (i[e] || 1e6) + s * (r[e] || 45e3) + n * (e === "franchise" ? 115e3 : e === "starter" ? 55e3 : 18e3)
    );
  }
  function ensureNflState(e, t = 70) {
    if (e.level < 7) return null;
    if (!e.nflStateV11) {
      const a = (e.stars || 1) < 4,
        s = "camp",
        n = clamp(34 + (t - 74) * 1.5 + (e.coachTrust || 50) * 0.18, 18, 72),
        i = nflSalaryForStatus(s, t, n);
      e.nflStateV11 = {
        status: s,
        security: n,
        expectation: (a ? 42 : 60) + (getOrigin(e)?.id === "prodigy" ? 8 : getOrigin(e)?.id === "walk-on" ? -4 : 0),
        schemeFit: 52,
        evaluationWeeks: 4,
        contractsEarned: 0,
        careerEarnings: 0,
        healthyScratches: 0,
        cuts: 0,
        contract: {
          yearsRemaining: a ? 1 : 4,
          salary: i,
          guaranteed: a ? 0.08 : 0.34,
          expectedRole: s,
          signingBonus: a ? 15e3 : Math.round(i * 0.22)
        },
        offers: []
      };
    }
    return e.nflStateV11;
  }
  function generateNflOffers(e, t, a = 3) {
    const s = seededRng(e.name, e.age, e.totalSeasons, t, "offers"),
      n = [
        "Austin Outlaws",
        "Portland Pioneers",
        "Orlando Orbit",
        "Columbus Captains",
        "San Antonio Scorpions",
        "St. Louis Archers",
        "Salt Lake Summit",
        "Oklahoma City Bison"
      ],
      i = ["Vertical Spread", "West Coast", "Power Run", "Multiple Front", "Pressure Defense", "Zone Match"];
    return shuffled(n, s)
      .slice(0, a)
      .map((r, l) => {
        const d = clamp(30 + (t - 70) * 1.1 + s() * 28 - l * 4, 16, 88),
          c = d >= 81 ? "starter" : d >= 62 ? "rotation" : d >= 44 ? "active-backup" : "practice-squad",
          u = c === "starter" ? 3 : c === "rotation" ? 2 : 1,
          h = Math.round(35 + s() * 55),
          p = Math.round(nflSalaryForStatus(c, t, d) * (0.9 + s() * 0.22));
        return {
          id: `offer-${e.seasonSeed || 0}-${e.totalSeasons || 0}-${e.age || 0}-${l}-${r.replace(/\s+/g, "-").toLowerCase()}`,
          team: r,
          scheme: sample(i, s),
          schemeFit: h,
          role: c,
          security: Math.round(d),
          years: u,
          salary: p,
          signingBonus: Math.round(
            p * (c === "starter" ? 0.28 : c === "rotation" ? 0.16 : c === "active-backup" ? 0.08 : 0.02)
          ),
          guaranteed: c === "practice-squad" ? 0.03 : c === "active-backup" ? 0.16 : c === "rotation" ? 0.31 : 0.52
        };
      });
  }
  function evaluateNflWeek(e, t, a) {
    const s = ensureNflState(e, a);
    if (!s) return null;
    const n = s.expectation || 55,
      i = s.status === "starter" ? 4 : s.status === "franchise" ? 7 : 0,
      r = Math.max(0, (e.age || 23) - 30) * 0.7,
      l = (t.perf - n) * 0.22 + (e.coachTrust - 50) * 0.035 + (s.schemeFit - 50) * 0.025 - i - r - (t.injured ? 8 : 0);
    ((s.security = clamp(s.security + l, 0, 100)), (s.evaluationWeeks = Math.max(0, s.evaluationWeeks - 1)));
    const d = s.status;
    s.security >= 82
      ? (s.status = "franchise")
      : s.security >= 67
        ? (s.status = "starter")
        : s.security >= 51
          ? (s.status = "rotation")
          : s.security >= 35
            ? (s.status = "active-backup")
            : s.security >= 18
              ? (s.status = "practice-squad")
              : (s.status = "waivers");
    const c = {
      "practice-squad": [0.04, 0.16],
      "active-backup": [0.16, 0.38],
      rotation: [0.38, 0.66],
      starter: [0.66, 0.9],
      franchise: [0.82, 0.98]
    };
    return (
      c[s.status] && (e.snapShare = clamp(Math.max(e.snapShare || 0, c[s.status][0]), c[s.status][0], c[s.status][1])),
      s.status === "waivers" && (s.cuts++, (s.offers = generateNflOffers(e, a, 3))),
      {
        previous: d,
        status: s.status,
        securityDelta: Math.round(l),
        security: Math.round(s.security),
        offers: s.offers
      }
    );
  }
  function evaluateNflSeason(e, t, a) {
    const s = ensureNflState(e, a);
    if (!s) return null;
    s.contract.yearsRemaining = Math.max(0, (s.contract.yearsRemaining || 0) - 1);
    const n = t.avg || 50;
    if (s.contract.yearsRemaining <= 0) {
      if (n >= s.expectation - 3 && s.security >= 42) {
        const i = s.status === "franchise" ? 5 : s.status === "starter" ? 4 : s.status === "rotation" ? 3 : 2,
          r = Math.round(nflSalaryForStatus(s.status, a, s.security) * (1 + Math.max(0, n - 68) / 115));
        return (
          (s.contract = {
            yearsRemaining: i,
            salary: r,
            signingBonus: Math.round(r * (s.status === "franchise" ? 0.65 : s.status === "starter" ? 0.38 : 0.18)),
            guaranteed: clamp(0.16 + s.security / 150, 0.16, 0.82),
            expectedRole: s.status
          }),
          s.contractsEarned++,
          (s.expectation = clamp(n - 2 + (s.status === "franchise" ? 7 : 0), 45, 88)),
          (s.offers = []),
          { extended: !0, contract: s.contract }
        );
      }
      return (
        (s.offers = generateNflOffers(e, a, n >= 58 ? 3 : n >= 48 ? 2 : 1)),
        (s.status = "free-agent"),
        { freeAgent: !0, offers: s.offers }
      );
    }
    return s.security < 16
      ? ((s.offers = generateNflOffers(e, a, n >= 52 ? 2 : 1)), (s.status = "waivers"), { cut: !0, offers: s.offers })
      : { retained: !0 };
  }
  function acceptNflOffer(e, t) {
    const a = e.nflStateV11;
    if (!a) return null;
    const s = (a.offers || []).find(n => n.id === t);
    return s
      ? ((a.status = s.role),
        (a.security = s.security),
        (a.schemeFit = s.schemeFit),
        (a.contract = {
          yearsRemaining: s.years,
          salary: s.salary,
          signingBonus: s.signingBonus || 0,
          guaranteed: s.guaranteed,
          expectedRole: s.role
        }),
        (a.offers = []),
        (e.nflCutPending = !1),
        (e.coachTrust = clamp(40 + s.schemeFit * 0.18, 30, 70)),
        s)
      : null;
  }
  function buyLegacyUnlock(e, t) {
    ensureAccountState(e);
    const a = LEGACY_UNLOCKS.find(s => s.id === t);
    return !a || e.legacyUnlocksV11[t]
      ? null
      : (e.legacyTokensV11 || 0) < a.cost
        ? !1
        : ((e.legacyTokensV11 -= a.cost), (e.legacyUnlocksV11[t] = !0), a);
  }
  function evaluateMilestones(e, t) {
    ((t.legacyMilestonesV11 = t.legacyMilestonesV11 || {}), (t.legacyTokensV11 = t.legacyTokensV11 || 0));
    const a = [];
    return (
      MILESTONES.forEach(s => {
        !t.legacyMilestonesV11[s.id] &&
          s.test(e) &&
          ((t.legacyMilestonesV11[s.id] = !0), (t.legacyTokensV11 += s.tokens), a.push(s));
      }),
      a
    );
  }
  function evaluateLegacyObjectives(e, t) {
    ((e.completedObjectivesV11 = e.completedObjectivesV11 || {}), (t.legacyTokensV11 = t.legacyTokensV11 || 0));
    const a = [];
    return (
      (e.legacyObjectivesV11 || []).forEach(s => {
        const n = LEGACY_OBJECTIVES.find(i => i.id === s);
        n &&
          !e.completedObjectivesV11[s] &&
          n.check(e) &&
          ((e.completedObjectivesV11[s] = !0),
          ((e.objectiveStampsV89 = e.objectiveStampsV89 || {})[s] = {
            t: Date.now(),
            y: (e.totalSeasons || 0) + 1,
            lv: e.level || 0
          }),
          (t.legacyTokensV11 += n.reward),
          a.push(n));
      }),
      a
    );
  }
  function careerLegacyGrade(e) {
    const t = [4, 10, 20, 34, 50, 68, 86, 104, 132][Math.max(0, Math.min(8, e.level || 0))] || 4,
      a = Math.min(24, (e.awards || []).length * 2.5),
      s = Math.min(30, (e.titles || 0) * 8),
      n = Math.min(18, Object.keys(e.completedObjectivesV11 || {}).length * 5),
      i = Math.min(14, Math.sqrt(Math.max(0, e.depthStarts || 0)) * 2.1),
      r =
        (e.level || 0) >= 7
          ? Math.min(14, Math.max(0, (e.age || 23) - 26) * 1.15)
          : Math.min(6, Math.max(0, (e.age || 8) - 18) * 0.45),
      l = Math.min(14, Object.keys(e.lifeV12?.completedGoals || {}).length * 2.5 + (e.lifeV12?.retired ? 4 : 0)),
      d = Math.round(t + a + s + n + i + r + l),
      c =
        e.level >= 8
          ? "Interstellar Icon"
          : e.level >= 7
            ? "UFF Professional"
            : e.level >= 6
              ? "Draft Prospect"
              : e.level >= 5
                ? "College Legacy"
                : e.level >= 4
                  ? "Friday Night Star"
                  : e.level >= 3
                    ? "Developmental Success"
                    : "Early Foundation",
      u = d >= 205 ? "S" : d >= 168 ? "A" : d >= 128 ? "B" : d >= 94 ? "C" : d >= 58 ? "D" : "E";
    return { score: d, ending: c, grade: u };
  }
  function originGrowthMultiplier(e) {
    const t = getOrigin(e)?.id;
    return t === "late-bloomer"
      ? (e.age || 8) >= 16
        ? 1.16
        : 0.9
      : t === "iron-will"
        ? 1.04
        : t === "position-convert"
          ? 0.96
          : 1;
  }
  function originPerformanceModifier(e, t) {
    const a = getOrigin(e)?.id;
    return a === "small-town" && t?.opponentV11?.importance !== "routine"
      ? 3
      : a === "injury-prone"
        ? 2
        : a === "prodigy" && (e.worldState?.mediaHeat || 0) > 65
          ? -2
          : 0;
  }
  function prestigeCap(e, t) {
    const s = {
      startAll: 4,
      perfFlat: 3,
      growth: 0.22,
      advFlat: 5,
      injDown: 0.45,
      teamQual: 0.12,
      coachStart: 8,
      snapFloor: 0.08,
      cutSave: 0.45,
      pointsFlat: 3,
      goodLuck: 0.06,
      depthPower: 0.18,
      youthPerf: 5,
      nflPerf: 4,
      physicalStart: 5,
      mentalStart: 5
    }[e];
    if (s == null) return t;
    const n = t < 0 ? -1 : 1,
      i = Math.abs(t);
    return i <= s ? t : n * (s + Math.log1p(i - s) * s * 0.18);
  }
  function pathCap(e, t) {
    return e === "startAttr"
      ? Math.min(t, 6)
      : e === "growthMult"
        ? Math.min(t, 1.18)
        : e === "advBonus"
          ? Math.min(t, 5)
          : e === "extraSeasons"
            ? Math.min(t, 2)
            : e === "injuryMult"
              ? Math.max(t, 0.55)
              : t;
  }
  function resetSeasonSystems(e) {
    ((e.storyArcsThisSeasonV11 = 0),
      (e.storyDecisionQueueV11 = []),
      (e.activeStoryArcV11 = null),
      (e.planHistoryV11 = []),
      (e.highLeverageStatsV11 = { attempts: 0, successes: 0 }),
      restBetweenSeasons(e));
  }
  function ensureAccountState(e) {
    ((e.schemaVersion = Math.max(e.schemaVersion || 1, 11)),
      (e.legacyTokensV11 = e.legacyTokensV11 || 0),
      (e.legacyMilestonesV11 = e.legacyMilestonesV11 || {}),
      (e.legacyUnlocksV11 = e.legacyUnlocksV11 || {}),
      (e.specializationV11 = e.specializationV11 || "tactician"),
      (e.featureFlagsV11 = {
        sequentialWeeks: !0,
        storyArcs: !0,
        namedRivals: !0,
        highLeverageMoments: !0,
        prestigeSpecialization: !0,
        partialVictories: !0,
        scouting: !0,
        conditionManagement: !0,
        nflSurvival: !0,
        runIdentity: !0,
        ...(e.featureFlagsV11 || {})
      }));
  }
  function ensurePlayerState(e, t, a, s, n) {
    if (
      (ensureAccountState(e),
      (t._displayOvrV11 = n),
      (t.legacyUnlocksV11 = { ...(e.legacyUnlocksV11 || {}) }),
      (t.originOptionsV11 = t.originOptionsV11 || selectOriginOptions(e, t).map(i => i.id)),
      (t.completedObjectivesV11 = t.completedObjectivesV11 || {}),
      (t.positionsPlayedV11 = t.positionsPlayedV11 || (t.pos ? [t.pos] : [])),
      (t.planHistoryV11 = t.planHistoryV11 || []),
      (t.weekModifiersV11 = t.weekModifiersV11 || []),
      (t.worldState = t.worldState || { teamChemistry: 50, programMomentum: 50, mediaHeat: 20 }),
      ensureStoryState(t),
      ensureCondition(t),
      t.pos && ensureRival(t, s, seededRng(t.seasonSeed, t.level, t.pos, "ensure-rival")),
      t.level >= 7 && ensureNflState(t, n),
      t.originV11 && !t._originAppliedV11)
    ) {
      const i = t.originV11;
      ((t.originV11 = null), applyOrigin(t, i, a), (t._originAppliedV11 = !0));
    }
    if (t.originV11 && e.legacyUnlocksV11?.["fourth-objective"] && (t.legacyObjectivesV11 || []).length < 4) {
      const i = LEGACY_OBJECTIVES.filter(r => !(t.legacyObjectivesV11 || []).includes(r.id));
      i.length && t.legacyObjectivesV11.push(sample(i, seededRng(t.seasonSeed, t.originV11, "fourth-objective")).id);
    }
  }
  const V11_MODULE = Object.freeze(
      Object.defineProperty(
        {
          __proto__: null,
          LEGACY_OBJECTIVES: LEGACY_OBJECTIVES,
          LEGACY_UNLOCKS: LEGACY_UNLOCKS,
          MILESTONES: MILESTONES,
          ORIGINS: ORIGINS,
          PLAN_IDS: PLAN_IDS,
          SPECIALIZATIONS: SPECIALIZATIONS,
          STORY_ARCS: STORY_ARCS,
          acceptNflOffer: acceptNflOffer,
          applyOrigin: applyOrigin,
          bounded: clamp,
          buyLegacyUnlock: buyLegacyUnlock,
          careerLegacyGrade: careerLegacyGrade,
          conditionModifiers: conditionModifiers,
          createOpponentProfile: createOpponentProfile,
          ensureAccountState: ensureAccountState,
          ensureCondition: ensureCondition,
          ensureNflState: ensureNflState,
          ensurePlayerState: ensurePlayerState,
          ensureRival: ensureRival,
          evaluateLegacyObjectives: evaluateLegacyObjectives,
          evaluateMilestones: evaluateMilestones,
          evaluateNflSeason: evaluateNflSeason,
          evaluateNflWeek: evaluateNflWeek,
          generateNflOffers: generateNflOffers,
          getOrigin: getOrigin,
          makeHighLeverageMoments: makeHighLeverageMoments,
          matchupPlanModifier: matchupPlanModifier,
          maybeStartStoryArc: maybeStartStoryArc,
          nflSalaryForStatus: nflSalaryForStatus,
          originGrowthMultiplier: originGrowthMultiplier,
          originPerformanceModifier: originPerformanceModifier,
          pathCap: pathCap,
          prestigeCap: prestigeCap,
          resetSeasonSystems: resetSeasonSystems,
          resolveHighLeverageMoment: resolveHighLeverageMoment,
          resolveRivalWeek: resolveRivalWeek,
          resolveStoryChoice: resolveStoryChoice,
          restBetweenSeasons: restBetweenSeasons,
          roleRank: roleRank,
          rollInjury: rollInjury,
          sample: sample,
          scoutOpponent: scoutOpponent,
          seededRng: seededRng,
          selectOriginOptions: selectOriginOptions,
          shuffled: shuffled,
          updateConditionAfterGame: updateConditionAfterGame
        },
        Symbol.toStringTag,
        { value: "Module" }
      )
    ),
    LIFE_VERSION = 12,
    RETIREMENT_PLANS = [
      {
        id: "comfortable",
        icon: "🛋️",
        name: "Comfortable Exit",
        target: 4e6,
        family: 40,
        description: "A paid-for home, disciplined spending, and roughly $160K of sustainable annual income."
      },
      {
        id: "secure",
        icon: "🌅",
        name: "Secure for Life",
        target: 8e6,
        family: 55,
        description: "The default plan: flexibility, family security, and roughly $320K of sustainable annual income."
      },
      {
        id: "wealthy",
        icon: "🏦",
        name: "Never Worry Again",
        target: 15e6,
        family: 65,
        description: "A larger safety margin, room for generosity, and roughly $600K of sustainable annual income."
      },
      {
        id: "dynasty",
        icon: "♾️",
        name: "Family Dynasty",
        target: 3e7,
        family: 75,
        description: "Build wealth intended to outlive the player and materially change the family tree."
      }
    ],
    LIFESTYLES = {
      meals: [
        {
          id: "team-cafeteria",
          icon: "🥣",
          name: "Team Cafeteria",
          weekly: 0,
          description: "Free team meals. Functional, not optimized.",
          recovery: 0,
          perf: 0,
          injuryRisk: 0,
          fulfillment: 0
        },
        {
          id: "performance-meals",
          icon: "🥗",
          name: "Performance Meals",
          weekly: 1800,
          description: "Prepared macros, hydration, and travel meals.",
          recovery: 3,
          perf: 1,
          injuryRisk: -0.008,
          fulfillment: 1
        },
        {
          id: "personal-chef",
          icon: "👨‍🍳",
          name: "Personal Chef",
          weekly: 5500,
          description: "Elite nutrition and constant meal preparation.",
          recovery: 6,
          perf: 1.5,
          injuryRisk: -0.015,
          fulfillment: 2
        }
      ],
      recovery: [
        {
          id: "team-only",
          icon: "🧊",
          name: "Team Treatment Only",
          weekly: 0,
          description: "Use only the club medical staff.",
          recovery: 0,
          perf: 0,
          injuryRisk: 0,
          recurrence: 0
        },
        {
          id: "private-trainer",
          icon: "🏋️",
          name: "Private Trainer",
          weekly: 6500,
          description: "Technique, mobility, and offseason development.",
          recovery: 4,
          perf: 1,
          injuryRisk: -0.01,
          recurrence: -0.015
        },
        {
          id: "elite-body-team",
          icon: "🩺",
          name: "Elite Body Team",
          weekly: 12500,
          description: "Trainer, therapist, massage, and recovery technology.",
          recovery: 9,
          perf: 2,
          injuryRisk: -0.024,
          recurrence: -0.045
        }
      ],
      luxury: [
        {
          id: "quiet-life",
          icon: "🧢",
          name: "Quiet Life",
          weekly: 1200,
          description: "Low-profile spending and a small inner circle.",
          fulfillment: 0,
          media: 0,
          fatigue: -1
        },
        {
          id: "comfortable-pro",
          icon: "🚙",
          name: "Comfortable Pro",
          weekly: 4800,
          description: "Nice travel, a reliable vehicle, and room to breathe.",
          fulfillment: 3,
          media: 1,
          fatigue: -2
        },
        {
          id: "celebrity-life",
          icon: "✨",
          name: "Celebrity Life",
          weekly: 16e3,
          description: "Luxury travel, nightlife, and a public-facing lifestyle.",
          fulfillment: 7,
          media: 6,
          fatigue: 3
        }
      ]
    },
    HOUSES = [
      {
        id: "rental",
        icon: "🏢",
        name: "Rookie Rental",
        price: 0,
        weekly: 2800,
        value: 0,
        description: "A furnished rental near the facility.",
        recovery: 0,
        family: 0,
        fulfillment: 0
      },
      {
        id: "starter-home",
        icon: "🏠",
        name: "Starter Home",
        price: 45e4,
        weekly: 650,
        value: 45e4,
        description: "A stable first home and a real foothold.",
        recovery: 1,
        family: 8,
        fulfillment: 4
      },
      {
        id: "family-home",
        icon: "🏡",
        name: "Family Home",
        price: 12e5,
        weekly: 1650,
        value: 12e5,
        description: "Space for family, privacy, and long-term stability.",
        recovery: 2,
        family: 18,
        fulfillment: 8
      },
      {
        id: "mansion",
        icon: "🏛️",
        name: "Luxury Mansion",
        price: 45e5,
        weekly: 5900,
        value: 45e5,
        description: "A major-status home with major upkeep.",
        recovery: 2,
        family: 22,
        fulfillment: 14,
        media: 4
      },
      {
        id: "legacy-estate",
        icon: "🌳",
        name: "Legacy Estate",
        price: 12e6,
        weekly: 15500,
        value: 12e6,
        description: "A multigenerational property built to outlast the career.",
        recovery: 3,
        family: 34,
        fulfillment: 20,
        media: 3
      }
    ],
    PORTFOLIOS = [
      {
        id: "bonds",
        icon: "🛡️",
        name: "Treasury & Bonds",
        annualReturn: 0.038,
        annualVol: 0.025,
        description: "Slow, stable compounding."
      },
      {
        id: "index",
        icon: "📈",
        name: "Index Funds",
        annualReturn: 0.075,
        annualVol: 0.16,
        description: "Diversified long-term growth."
      },
      {
        id: "real-estate",
        icon: "🏘️",
        name: "Real Estate Fund",
        annualReturn: 0.064,
        annualVol: 0.095,
        description: "Property income and moderate appreciation."
      },
      {
        id: "venture",
        icon: "🚀",
        name: "Venture Portfolio",
        annualReturn: 0.13,
        annualVol: 0.38,
        description: "High upside with real loss risk."
      }
    ],
    LIFE_GOALS = [
      {
        id: "parents-retirement",
        icon: "❤️",
        name: "Fund Parents' Retirement",
        target: 15e5,
        description: "Set aside enough to remove the financial burden from your parents.",
        family: 25,
        fulfillment: 18
      },
      {
        id: "kids-college",
        icon: "🎓",
        name: "Build Kids' College Fund",
        target: 5e5,
        description: "Fully fund education for the next generation.",
        family: 20,
        fulfillment: 14
      },
      {
        id: "family-home",
        icon: "🏡",
        name: "Buy the Family Home",
        target: 12e5,
        description: "Own a stable long-term home for the family.",
        house: "family-home",
        family: 18,
        fulfillment: 12
      },
      {
        id: "charity-foundation",
        icon: "🤝",
        name: "Launch a Foundation",
        target: 2e6,
        description: "Create a permanent community and youth-sports fund.",
        family: 6,
        fulfillment: 24
      },
      {
        id: "business-owner",
        icon: "🏢",
        name: "Own a Business Portfolio",
        target: 3e6,
        description: "Build wealth that does not depend on football.",
        portfolio: "venture",
        family: 8,
        fulfillment: 16
      },
      {
        id: "generational-wealth",
        icon: "♾️",
        name: "Create Generational Wealth",
        target: 25e6,
        description: "Finish with enough invested wealth to change the family tree.",
        netWorth: !0,
        family: 30,
        fulfillment: 25
      },
      {
        id: "retire-secure",
        icon: "🌅",
        name: "Retire on Your Terms",
        target: 8e6,
        description: "Reach a sustainable retirement target before leaving the league.",
        netWorth: !0,
        family: 20,
        fulfillment: 20
      }
    ],
    LIFE_EVENTS = [
      {
        id: "parents-call",
        icon: "📞",
        title: "A Call From Home",
        copy: "Your parents have delayed repairs and retirement plans for years.",
        minWeek: 2,
        choices: [
          {
            id: "help-now",
            label: "Send $50,000 now",
            description: "Immediate family security and fulfillment.",
            cash: -5e4,
            family: 8,
            fulfillment: 5
          },
          {
            id: "fund-plan",
            label: "Start a formal retirement fund",
            description: "Move $100,000 into the parents goal.",
            goal: "parents-retirement",
            amount: 1e5,
            family: 12,
            fulfillment: 7
          },
          {
            id: "wait",
            label: "Promise to help after the season",
            description: "Keep liquidity, but carry the pressure.",
            family: -3,
            fulfillment: -2,
            mental: 5
          }
        ]
      },
      {
        id: "teammate-pitch",
        icon: "💼",
        title: "The Teammate Investment Pitch",
        copy: "A veteran brings you a private business opportunity.",
        minWeek: 3,
        choices: [
          { id: "decline", label: "Decline politely", description: "Keep the portfolio simple.", fulfillment: 0 },
          {
            id: "small",
            label: "Invest $75,000",
            description: "A measured venture bet.",
            portfolio: "venture",
            amount: 75e3,
            fulfillment: 2
          },
          {
            id: "big",
            label: "Invest $250,000",
            description: "A major upside bet with real downside.",
            portfolio: "venture",
            amount: 25e4,
            media: 3,
            fulfillment: 4
          }
        ]
      },
      {
        id: "endorsement",
        icon: "📸",
        title: "A Brand Wants Your Name",
        copy: "The deal pays well, but the appearances consume recovery time.",
        minWeek: 2,
        choices: [
          {
            id: "take",
            label: "Take the full campaign",
            description: "Large cash payment, more media pressure.",
            cash: 175e3,
            media: 10,
            fatigue: 8
          },
          {
            id: "limited",
            label: "Negotiate a limited campaign",
            description: "Smaller payment with manageable workload.",
            cash: 8e4,
            media: 4,
            fatigue: 3
          },
          {
            id: "decline",
            label: "Protect football",
            description: "No money; small trust and recovery gain.",
            trust: 2,
            recovery: 3
          }
        ]
      },
      {
        id: "family-time",
        icon: "👨‍👩‍👧",
        title: "The Season Is Taking Everything",
        copy: "Your family asks for one protected week with fewer appearances and workouts.",
        minWeek: 4,
        choices: [
          {
            id: "protect",
            label: "Protect the family week",
            description: "Spend on travel and reduce workload.",
            cash: -22e3,
            family: 10,
            fulfillment: 8,
            recovery: 5,
            nextPerf: -2
          },
          {
            id: "balance",
            label: "Split the week",
            description: "Moderate family and preparation benefits.",
            cash: -8e3,
            family: 5,
            fulfillment: 4,
            recovery: 2
          },
          {
            id: "football",
            label: "Stay fully locked in",
            description: "Short-term football edge, long-term strain.",
            family: -7,
            fulfillment: -4,
            nextPerf: 4
          }
        ]
      },
      {
        id: "charity",
        icon: "🤲",
        title: "The Community Asks You to Lead",
        copy: "A youth program needs funding and a public champion.",
        minWeek: 5,
        choices: [
          {
            id: "lead",
            label: "Donate $100,000 and lead it",
            description: "Large fulfillment and community legacy.",
            goal: "charity-foundation",
            amount: 1e5,
            fulfillment: 10,
            media: 4
          },
          {
            id: "support",
            label: "Donate $25,000 quietly",
            description: "Help without creating another obligation.",
            goal: "charity-foundation",
            amount: 25e3,
            fulfillment: 5
          },
          { id: "later", label: "Wait until the offseason", description: "Keep cash and focus.", fulfillment: -1 }
        ]
      },
      {
        id: "advisor",
        icon: "🧾",
        title: "Who Handles the Money?",
        copy: "Three advisors pitch very different visions for your career earnings.",
        minWeek: 2,
        choices: [
          {
            id: "fiduciary",
            label: "Hire the boring fiduciary",
            description: "Pay a modest fee for discipline, diversification, and fewer bad decisions.",
            cash: -18e3,
            fulfillment: 1,
            advisor: "fiduciary"
          },
          {
            id: "celebrity",
            label: "Hire the celebrity money manager",
            description: "Expensive access and aggressive opportunities with more volatility.",
            cash: -75e3,
            media: 6,
            fulfillment: 4,
            advisor: "celebrity"
          },
          {
            id: "self",
            label: "Manage it yourself",
            description: "Keep control and fees low, but mistakes remain entirely yours.",
            advisor: "self"
          }
        ]
      },
      {
        id: "baby-fund",
        icon: "🍼",
        title: "The Family Is Growing",
        copy: "A new child changes what every contract and investment is supposed to accomplish.",
        minWeek: 6,
        choices: [
          {
            id: "fund",
            label: "Seed the College fund with $75,000",
            description: "A major early contribution and lasting family security.",
            goal: "kids-college",
            amount: 75e3,
            family: 12,
            fulfillment: 8
          },
          {
            id: "home",
            label: "Save toward a family home",
            description: "Keep the cash liquid and formalize the home goal.",
            family: 5,
            fulfillment: 5,
            goalIntent: "family-home"
          },
          {
            id: "delay",
            label: "Handle it after the season",
            description: "Protect liquidity now, but increase mental pressure.",
            family: -4,
            fulfillment: -3,
            fatigue: 3
          }
        ]
      },
      {
        id: "entourage",
        icon: "🎉",
        title: "Everybody Wants to Come Along",
        copy: "Friends and extended family expect access, travel, tickets, and financial help.",
        minWeek: 4,
        choices: [
          {
            id: "boundary",
            label: "Set hard boundaries",
            description: "Protect the money and recovery, but take a short-term relationship hit.",
            family: -2,
            fulfillment: -1,
            recovery: 3
          },
          {
            id: "budget",
            label: "Create a $40,000 support budget",
            description: "Help people without creating an unlimited obligation.",
            cash: -4e4,
            family: 6,
            fulfillment: 4
          },
          {
            id: "carry",
            label: "Pay for everybody",
            description: "Big social reward, major spending, and constant distraction.",
            cash: -14e4,
            family: 8,
            fulfillment: 8,
            media: 5,
            fatigue: 6
          }
        ]
      },
      {
        id: "market-drop",
        icon: "📉",
        title: "The Market Drops Hard",
        copy: "Your portfolio falls during the season and every headline says to get out.",
        minWeek: 7,
        choices: [
          {
            id: "hold",
            label: "Hold the plan",
            description: "Accept the drawdown and keep the long-term allocation.",
            portfolioShock: -0.035,
            fulfillment: -1
          },
          {
            id: "buy",
            label: "Invest another 10% of cash",
            description: "Buy into fear while preserving an emergency reserve.",
            investCashPct: 0.1,
            portfolio: "index",
            portfolioShock: -0.015,
            fulfillment: 2
          },
          {
            id: "sell",
            label: "Sell half the risky assets",
            description: "Reduce future volatility but lock in part of the loss.",
            sellRiskyPct: 0.5,
            portfolioShock: -0.055,
            fulfillment: -3
          }
        ]
      },
      {
        id: "parents-house",
        icon: "🏠",
        title: "A House for the People Who Raised You",
        copy: "The opportunity appears to buy your parents a safe home near family.",
        minWeek: 8,
        choices: [
          {
            id: "buy",
            label: "Buy the $350,000 house",
            description: "A direct, permanent family legacy with a major cash cost.",
            cash: -35e4,
            family: 20,
            fulfillment: 14,
            goalCredit: "parents-retirement",
            creditAmount: 15e4
          },
          {
            id: "down",
            label: "Cover a $100,000 down payment",
            description: "Meaningful help while keeping more liquidity.",
            cash: -1e5,
            family: 11,
            fulfillment: 8,
            goalCredit: "parents-retirement",
            creditAmount: 5e4
          },
          {
            id: "wait",
            label: "Wait for the next contract",
            description: "Protect the current balance sheet, but the promise remains unfinished.",
            family: -4,
            fulfillment: -4
          }
        ]
      },
      {
        id: "injury-insurance",
        icon: "🛡️",
        title: "Protect the Next Contract",
        copy: "An insurance policy can protect the family if one injury ends the career.",
        minWeek: 3,
        choices: [
          {
            id: "full",
            label: "Buy full protection",
            description: "Large premium, lower financial downside, and better peace of mind.",
            cash: -6e4,
            family: 7,
            fulfillment: 4,
            insurance: 1,
            recovery: 1
          },
          {
            id: "basic",
            label: "Buy basic coverage",
            description: "Moderate premium and partial protection.",
            cash: -22e3,
            family: 3,
            insurance: 0.45
          },
          {
            id: "skip",
            label: "Bet on the body",
            description: "Keep the cash but carry more pressure into every hit.",
            fatigue: 2,
            fulfillment: -1
          }
        ]
      }
    ]; /* v97: prestige is rare (a fifth of a star per career-end, TU("prestigeGainMult")) and its upgrades are quiet — every bonus that scales on it (starting attributes, the potential ceiling, the ratings floor, the soft-cap share) reads a fifth of what it did (TU("prestigeEffectMult")) */
  window.__htV97 = e => effectivePrestige(e);
  function effectivePrestige(e) {
    const t = Math.max(0, e || 0),
      v =
        t <= 6
          ? t
          : t <= 20
            ? 6 + (t - 6) * 0.5
            : t <= 60
              ? 13 + Math.sqrt(t - 20) * 1.4
              : 21.85 + Math.log1p(t - 60) * 2;
    return v * TU("prestigeEffectMult", 0.2);
  }
  function rawPrestigePowerRatio(e) {
    return e <= 0 ? 0 : effectivePrestige(e) / e;
  } /* ===== v139 THE NATIONAL STANDING IS A FLOOR ON THE SEASON GRADE =====
   *
   * `Ns` grades you against your LEVEL'S EXPECTATION — perf average minus what a man of your
   * rating is supposed to post — and hands out an F for anything more than ten points under
   * it. The national rank is a different measurement entirely: production against the elite
   * line for the country. The two can disagree completely, and when they do the report card
   * is the one that reads wrong: a top-ten player in the country can finish more than ten
   * points under what his own rating was supposed to deliver and be shown an F for a season
   * almost nobody in the game had.
   *
   * The advancement chance is already computed on this very screen from that national
   * standing, so the grade is floored against it. A man the game is about to tell you is a
   * lock to move up is not having an F season, and the card no longer says two opposite
   * things at once. It is a FLOOR and never a cap: a great season against expectation still
   * grades on its own merits, and TU("gradeRankFloorV139", 1) turns it off. */
  function gradeFloorV139(g, chance) {
    /* `Oa` is Ns(...).grade — a bare letter — but the season card and the debrief both read
     * grades in either shape, so take both and hand back the shape we were given. */
    const obj = g && typeof g === "object",
      cur = String((obj ? g.grade : g) || "");
    if (!cur || !isFinite(chance) || !TU("gradeRankFloorV139", 1)) return g;
    const L = ["F", "D", "C", "B", "B+", "A", "A+"];
    const min = chance >= 90 ? "A" : chance >= 75 ? "B+" : chance >= 55 ? "B" : chance >= 35 ? "C" : null;
    gradeFloorV139.last = { cur, chance: Math.round(chance), min };
    if (!min || L.indexOf(cur) < 0 || L.indexOf(cur) >= L.indexOf(min)) return g;
    return obj ? Object.assign({}, g, { grade: min, floorV139: { from: cur, chance: Math.round(chance) } }) : min;
  }
  window.__gradeFloorV139 = gradeFloorV139;
  function expectedGrade(e, t, a, s = 60) {
    const n = (1 - clamp(a, 0.04, 0.98)) * 8,
      i = (s - 60) * 0.12,
      r = e - t + n + i;
    return {
      grade: r >= 12 ? "A+" : r >= 7 ? "A" : r >= 3 ? "B+" : r >= -1 ? "B" : r >= -5 ? "C" : r >= -10 ? "D" : "F",
      delta: Math.round(r),
      score: r
    };
  }
  function capDecisionChance(e, t, a = 0) {
    const s = String(t || "").toLowerCase();
    let n = 0.84;
    return (
      (s.includes("very low") || s.includes("safe")) && (n = 0.88),
      (s.includes("low") || s.includes("controlled") || s.includes("reliable")) && (n = 0.86),
      (s.includes("moderate") || s.includes("political")) && (n = 0.78),
      (s.includes("high") || s.includes("volatile") || s.includes("boom")) && (n = 0.66),
      s.includes("danger") && (n = 0.58),
      clamp(e + a, 0.12, n)
    );
  }
  function tunedInjuryRisk(e, t) {
    const a = t.conditionV11?.injury?.severity || 0,
      s = t.conditionV11?.fatigue || 0,
      n = Math.max(0, (t.age || 22) - 31);
    return clamp(e * 0.42 + Math.max(0, s - 72) * 0.0025 + n * 0.002 + a * 0.01, 0.003, 0.19);
  }
  function recoveryProjection(e, t) {
    const a = e.conditionV11 || { fatigue: 20, recovery: 55, susceptibility: 18 },
      n =
        ({ disciplined: 5, feature: 11, explosive: 16, team: 9, recovery: -14 }[t] ?? 7) +
        Math.max(0, (e.age || 22) - 28) * 0.7,
      i = clamp(a.fatigue + n - (a.recovery || 55) * 0.11, 0, 100),
      r = tunedInjuryRisk(0.008 + (a.susceptibility || 18) / 900 + i / 1150, e);
    return {
      fatigueAfter: Math.round(i),
      injuryRisk: r,
      label: i < 35 ? "Fresh" : i < 58 ? "Manageable" : i < 76 ? "Heavy" : "Danger"
    };
  }
  function regretScenario(e, t) {
    const a = [
        [
          {
            reached: "Pee Wee",
            next: "Youth League",
            dream: "make the next team",
            reason: "be the kid everyone wanted on their side at recess"
          },
          {
            reached: "Pee Wee",
            next: "the real school leagues",
            dream: "prove this was more than a backyard phase",
            reason: "show my friends I was actually good at something"
          },
          {
            reached: "Pee Wee",
            next: "Youth League",
            dream: "wear a real uniform",
            reason: "make my family cheer for my name from the stands"
          }
        ],
        [
          {
            reached: "Youth League",
            next: "Middle School",
            dream: "wear the school colors",
            reason: "finally feel cool and like I belonged"
          },
          {
            reached: "Youth League",
            next: "the school team",
            dream: "walk through school as one of the players",
            reason: "stop feeling invisible in the hallway"
          },
          {
            reached: "Youth League",
            next: "Middle School football",
            dream: "play with the older kids",
            reason: "prove I was not just good against little kids"
          }
        ],
        [
          {
            reached: "Middle School",
            next: "JV",
            dream: "make the high-school program",
            reason: "prove I was not just another kid in the hallway"
          },
          {
            reached: "Middle School",
            next: "High School football",
            dream: "survive the first real cut",
            reason: "earn a locker, a jersey, and a place with the team"
          },
          {
            reached: "Middle School",
            next: "JV",
            dream: "keep the dream alive through freshman year",
            reason: "show the coaches they were wrong to overlook me"
          }
        ],
        [
          {
            reached: "JV",
            next: "Varsity",
            dream: "play under the Friday-night lights",
            reason: "be somebody at school—and maybe finally get the girl"
          },
          {
            reached: "JV",
            next: "Varsity",
            dream: "hear my name over the stadium speakers",
            reason: "make everyone who doubted me watch from the stands"
          },
          {
            reached: "JV",
            next: "Friday nights",
            dream: "earn a starting spot",
            reason: "make my parents proud when the whole town was watching"
          }
        ],
        [
          {
            reached: "Varsity",
            next: "College",
            dream: "earn a scholarship",
            reason: "take pressure off my family and make everyone proud"
          },
          {
            reached: "Varsity",
            next: "a College campus",
            dream: "turn football into an education",
            reason: "be the first person in my family to get that opportunity"
          },
          {
            reached: "Varsity",
            next: "College football",
            dream: "keep playing after graduation",
            reason: "prove the Friday-night version of me was not the ending"
          }
        ],
        [
          {
            reached: "College",
            next: "the UFF Combine",
            dream: "get one real shot at the league",
            reason: "make enough money to support the people who carried me"
          },
          {
            reached: "College",
            next: "an UFF training camp",
            dream: "turn four years of work into one contract",
            reason: "pay back every sacrifice my family made"
          },
          {
            reached: "College",
            next: "draft weekend",
            dream: "hear my name called",
            reason: "change what money and opportunity look like for my future kids"
          }
        ],
        [
          {
            reached: "UFF Combine",
            next: "an UFF roster",
            dream: "sign that first contract",
            reason: "buy my parents a house and change my family forever"
          },
          {
            reached: "UFF Combine",
            next: "opening day",
            dream: "survive final cuts",
            reason: "prove one bad drill or one bad scout could not define me"
          },
          {
            reached: "UFF Combine",
            next: "the first UFF paycheck",
            dream: "make the active roster",
            reason: "give my family security we had never known"
          }
        ],
        [
          {
            reached: "the UFF",
            next: "the next UFF contract",
            dream: "stay in the league",
            reason: "finish what I started and secure my family for life"
          },
          {
            reached: "the UFF",
            next: "one more season",
            dream: "earn back my roster spot",
            reason: "finish the retirement plan before football decided for me"
          },
          {
            reached: "the UFF",
            next: "a second contract",
            dream: "prove I belonged for more than a moment",
            reason: "fund the house, the College accounts, and my parents’ retirement"
          },
          {
            reached: "the UFF",
            next: "the championship stage",
            dream: "leave with a ring",
            reason: "make the pain, pressure, and years of work mean something permanent"
          }
        ],
        [
          {
            reached: "the Interstellar League",
            next: "immortality",
            dream: "be remembered forever",
            reason: "leave a legacy no one could erase"
          },
          {
            reached: "the Interstellar League",
            next: "the final record book",
            dream: "become the greatest across worlds",
            reason: "build a legacy that survived long after the last snap"
          }
        ]
      ],
      s = a[Math.max(0, Math.min(a.length - 1, e))],
      n = String(t?.seasonSeed || t?.name || e)
        .split("")
        .reduce((l, d) => l + d.charCodeAt(0), 0),
      i = s[n % s.length],
      r = String(t?.name || "I").split(" ")[0] || "I";
    return {
      ...i,
      dialogue: `“Man… I was so close to getting to ${i.next} so I could ${i.reason}. If I could do it over, I would…”`,
      vows: [
        {
          id: "smarter",
          icon: "🧠",
          label: "Train smarter",
          description: "Next career starts with better awareness, planning, and scouting discipline."
        },
        {
          id: "health",
          icon: "🩺",
          label: "Protect my body",
          description: "Next career starts with better recovery and lower early injury exposure."
        },
        {
          id: "courage",
          icon: "🔥",
          label: "Stop playing afraid",
          description: "Next career starts with more composure and clutch confidence."
        },
        {
          id: "family",
          icon: "❤️",
          label: `Remember who ${r} is doing this for`,
          description: "Next career gains steadier motivation and one family-focused legacy objective."
        }
      ]
    };
  }
  function applyPendingVow(e, t) {
    const a = e.pendingVowV12;
    return !a || t._vowAppliedV12
      ? null
      : ((t._vowAppliedV12 = !0),
        (t.vowV12 = a),
        a === "smarter"
          ? (["awareness", "vision", "discipline"].forEach(s => {
              t.attrs?.[s] != null && (t.attrs[s] = clamp(t.attrs[s] + 2, 1, 250));
            }),
            (t.scoutVowV12 = 8))
          : a === "health"
            ? ((t.conditionV11 = t.conditionV11 || {
                fatigue: 12,
                susceptibility: 18,
                mentalLoad: 18,
                recovery: 55,
                injury: null,
                consecutiveGames: 0,
                lastUpdatedWeek: -1
              }),
              (t.conditionV11.recovery = clamp((t.conditionV11.recovery || 55) + 7, 0, 100)),
              (t.conditionV11.susceptibility = clamp((t.conditionV11.susceptibility || 18) - 6, 0, 100)))
            : a === "courage"
              ? ((t.composure103 = clamp((t.composure103 || 50) + 8, 0, 100)),
                (t.momentum103 = clamp((t.momentum103 || 50) + 3, 0, 100)))
              : a === "family" &&
                ((t.gritV12 = (t.gritV12 || 0) + 5),
                t.attrs?.grit != null && (t.attrs.grit = clamp(t.attrs.grit + 3, 1, 250))),
        (e.pendingVowV12 = null),
        a);
  }
  function ensureFinanceState(e) {
    if ((e.level || 0) < 7) return null;
    if (!e.lifeV12) {
      const n = e.nflStateV11?.contract?.salary || 78e4;
      e.lifeV12 = {
        cash: Math.round(Math.max(15e3, n * 0.018)),
        debt: 0,
        investments: { cash: 0, bonds: 0, index: 0, "real-estate": 0, venture: 0 },
        contributions: {},
        completedGoals: {},
        lifestyle: { meals: "team-cafeteria", recovery: "team-only", luxury: "quiet-life" },
        houseId: "rental",
        houseValue: 0,
        careerGross: 0,
        careerTaxes: 0,
        careerExpenses: 0,
        careerInterest: 0,
        investmentGains: 0,
        endorsements: 0,
        donations: 0,
        familySecurity: 10,
        fulfillment: 45,
        weeksPaid: 0,
        retirementTarget: 8e6,
        retirementPlanId: "secure",
        retired: !1,
        missedPayments: 0,
        history: [],
        eventQueue: [],
        resolvedEvents: [],
        lastContractSignature: ""
      };
    }
    const t = e.lifeV12;
    ((t.debt = Math.max(0, Number(t.debt) || 0)),
      (t.careerInterest = Math.max(0, Number(t.careerInterest) || 0)),
      (t.missedPayments = Math.max(0, Number(t.missedPayments) || 0)),
      (t.retirementPlanId = t.retirementPlanId || "secure"));
    const a = RETIREMENT_PLANS.find(n => n.id === t.retirementPlanId) || RETIREMENT_PLANS[1];
    t.retirementTarget = Number(t.retirementTarget) || a.target;
    const s = e.nflStateV11?.contract;
    if (s) {
      const n = [s.salary || 0, s.signingBonus || 0, s.expectedRole || "", e.nflStateV11?.contractsEarned || 0].join(
        "|"
      );
      if (n !== t.lastContractSignature) {
        const i = Math.max(0, Math.round(s.signingBonus || 0));
        if (i) {
          const r = Math.round(i * (taxRate(s.salary || i) + 0.03)),
            l = Math.max(0, i - r);
          ((t.cash += l),
            (t.careerGross += i),
            (t.careerTaxes += r),
            t.history.unshift({
              week: t.weeksPaid,
              label: "Contract signing bonus after tax",
              amount: l,
              netWorth:
                t.cash + Object.values(t.investments).reduce((d, c) => d + (Number(c) || 0), 0) + t.houseValue - t.debt
            }));
        }
        t.lastContractSignature = n;
      }
    }
    return t;
  }
  function lifestyleById(e, t) {
    return LIFESTYLES[e].find(a => a.id === t) || LIFESTYLES[e][0];
  }
  function houseById(e) {
    return HOUSES.find(t => t.id === e) || HOUSES[0];
  }
  function portfolioById(e) {
    return PORTFOLIOS.find(t => t.id === e);
  }
  function lifeGoalById(e) {
    return LIFE_GOALS.find(t => t.id === e);
  }
  function lifestyleEffects(e) {
    const t = ensureFinanceState(e);
    if (!t)
      return {
        weeklyCost: 0,
        recovery: 0,
        perf: 0,
        injuryRisk: 0,
        recurrence: 0,
        fulfillment: 0,
        media: 0,
        fatigue: 0
      };
    const a = lifestyleById("meals", t.lifestyle.meals),
      s = lifestyleById("recovery", t.lifestyle.recovery),
      n = lifestyleById("luxury", t.lifestyle.luxury),
      i = houseById(t.houseId);
    return {
      weeklyCost: a.weekly + s.weekly + n.weekly + i.weekly,
      recovery: (a.recovery || 0) + (s.recovery || 0) + (i.recovery || 0),
      perf: (a.perf || 0) + (s.perf || 0),
      injuryRisk: (a.injuryRisk || 0) + (s.injuryRisk || 0),
      recurrence: s.recurrence || 0,
      fulfillment: (a.fulfillment || 0) + (n.fulfillment || 0) + (i.fulfillment || 0),
      media: (n.media || 0) + (i.media || 0),
      fatigue: n.fatigue || 0
    };
  }
  function netWorth(e) {
    const t = ensureFinanceState(e);
    return t
      ? Math.round(
          t.cash + Object.values(t.investments).reduce((a, s) => a + (Number(s) || 0), 0) + t.houseValue - t.debt
        )
      : 0;
  }
  function liquidInvestments(e) {
    const t = ensureFinanceState(e);
    return t ? Object.values(t.investments).reduce((a, s) => a + (Number(s) || 0), 0) : 0;
  }
  function safeAnnualIncome(e) {
    return Math.round(liquidInvestments(e) * 0.04);
  }
  function retirementAssets(e) {
    const t = ensureFinanceState(e);
    return t ? Math.max(0, Math.round(t.cash + liquidInvestments(e) + t.houseValue * 0.5 - t.debt)) : 0;
  }
  function retirementPlanById(e) {
    return RETIREMENT_PLANS.find(t => t.id === e) || RETIREMENT_PLANS[1];
  }
  function setRetirementPlan(e, t) {
    const a = ensureFinanceState(e),
      s = RETIREMENT_PLANS.find(n => n.id === t);
    return !a || !s ? !1 : ((a.retirementPlanId = s.id), (a.retirementTarget = s.target), !0);
  }
  function taxRate(e) {
    return e <= 1e6 ? 0.36 : e <= 3e6 ? 0.405 : e <= 8e6 ? 0.445 : 0.475;
  }
  function Fo(e, t, a) {
    const s = ensureFinanceState(e);
    if (!s) return 0;
    let n = 0;
    const i = s.advisorId || "self",
      r = i === "celebrity" ? 0.006 : i === "fiduciary" ? -0.001 : 0,
      l = i === "celebrity" ? 1.32 : i === "fiduciary" ? 0.8 : 1,
      d = i === "celebrity" ? 0.022 : i === "fiduciary" ? 0.0065 : 0;
    for (const c of PORTFOLIOS) {
      const u = s.investments[c.id] || 0;
      if (u <= 0) continue;
      const h = (c.annualReturn + r) / 18,
        p = (c.annualVol * l) / Math.sqrt(18),
        m = (a() + a() + a() + a() - 2) * p * 0.72;
      let y = u * (h + m);
      if (c.id === "venture" && a() < 0.0065) {
        const V = u * (0.18 + a() * 0.38);
        ((y -= V),
          s.history.unshift({
            week: t.week || s.weeksPaid,
            label: "Private investment failed",
            amount: -Math.round(V),
            netWorth: netWorth(e)
          }));
      } else if (c.id === "real-estate" && a() < 0.0025) {
        const V = u * (0.08 + a() * 0.14);
        ((y -= V),
          s.history.unshift({
            week: t.week || s.weeksPaid,
            label: "Property fund write-down",
            amount: -Math.round(V),
            netWorth: netWorth(e)
          }));
      }
      const C = (u * d) / 18;
      ((y -= C), (s.careerExpenses += Math.round(C)), (s.investments[c.id] = Math.max(0, Math.round(u + y))), (n += y));
    }
    return (
      (s.investmentGains += Math.round(n)),
      Math.abs(n) >= 1e3 &&
        s.history.unshift({
          week: t.week || s.weeksPaid,
          label: n >= 0 ? "Portfolio gain" : "Portfolio loss",
          amount: Math.round(n),
          netWorth: netWorth(e)
        }),
      Math.round(n)
    );
  }
  function emergencyReserve(e) {
    if (!ensureFinanceState(e)) return 5e3;
    const a = lifestyleEffects(e).weeklyCost * (52 / 18);
    return Math.max(25e3, Math.round(a * 5), Math.round((e.nflStateV11?.contract?.salary || 0) * 0.018));
  }
  function processNflFinancialWeek(e, t) {
    const a = ensureFinanceState(e);
    if (!a) return null;
    const s = e.nflStateV11 || {},
      n = Math.max(0, s.contract?.salary || 0),
      i = String(s.status || "camp"),
      r = i === "practice-squad" ? n / 36 : n / 18,
      l = r * taxRate(n),
      d = r * 0.03,
      c = lifestyleEffects(e);
    let u = 0;
    ((t.perf || 0) >= 85 && (u += i === "franchise" ? 5e4 : i === "starter" ? 25e3 : i === "rotation" ? 1e4 : 3500),
      t.playoff && (u += i === "franchise" ? 4e4 : i === "starter" ? 2e4 : 7500),
      t.opponentV11?.importance === "championship" && t.won && (u += 1e5));
    const h = c.weeklyCost * (52 / 18),
      p = a.debt * (0.16 / 18);
    let m = 0;
    t.injured && (a.insuranceCoverage || 0) > 0 && (m = Math.round(n * 0.012 * (a.insuranceCoverage || 0)));
    const y = r + u + m,
      C = y - l - d - h - p;
    if (((a.cash = Math.round(a.cash + C)), a.cash < 0)) {
      const P = Math.abs(a.cash);
      ((a.cash = 0),
        (a.debt += P),
        a.missedPayments++,
        (a.familySecurity = clamp(a.familySecurity - 3, 0, 100)),
        (a.fulfillment = clamp(a.fulfillment - 5, 0, 100)));
    }
    const V = emergencyReserve(e);
    if (a.debt > 0 && a.cash > V) {
      const P = Math.min(a.debt, Math.round((a.cash - V) * 0.5));
      ((a.cash -= P), (a.debt -= P));
    }
    if (
      (a.debt > Math.max(15e4, n * 0.12) &&
        ((a.lifestyle.luxury = "quiet-life"), a.debt > Math.max(4e5, n * 0.25) && (a.lifestyle.recovery = "team-only")),
      a.houseValue > 0)
    ) {
      const P = seededRng(e.seasonSeed, e.totalSeasons, t.week, "house-value"),
        v = 0.03 / 18 + (P() - 0.5) * 0.012;
      a.houseValue = Math.max(0, Math.round(a.houseValue * (1 + v)));
    }
    return (
      (a.careerGross += Math.round(y)),
      (a.careerTaxes += Math.round(l + d)),
      (a.careerExpenses += Math.round(h)),
      (a.careerInterest += Math.round(p)),
      a.weeksPaid++,
      (a.fulfillment = clamp(a.fulfillment + c.fulfillment * 0.045 - (a.cash < V * 0.5 ? 2 : 0), 0, 100)),
      e.conditionV11 &&
        ((e.conditionV11.recovery = clamp((e.conditionV11.recovery || 55) + c.recovery * 0.08, 0, 100)),
        (e.conditionV11.fatigue = clamp((e.conditionV11.fatigue || 0) + c.fatigue - c.recovery * 0.12, 0, 100)),
        e.conditionV11.injury &&
          (e.conditionV11.injury.recurrence = clamp(e.conditionV11.injury.recurrence + c.recurrence, 0.02, 0.75))),
      e.worldState && (e.worldState.mediaHeat = clamp((e.worldState.mediaHeat || 20) + c.media * 0.08, 0, 100)),
      Fo(e, t, seededRng(e.seasonSeed, e.totalSeasons, t.week, "finance")),
      evaluateLifeGoals(e),
      a.history.unshift({
        week: t.week || a.weeksPaid,
        label: `Game check${u ? ` + ${formatMoney(u)} bonus` : ""}${m ? " + insurance" : ""}`,
        amount: Math.round(C),
        netWorth: netWorth(e)
      }),
      (a.history = a.history.slice(0, 18)),
      {
        gross: Math.round(y),
        taxes: Math.round(l + d),
        expenses: Math.round(h),
        interest: Math.round(p),
        net: Math.round(C),
        cash: a.cash,
        debt: Math.round(a.debt),
        netWorth: netWorth(e),
        bonus: Math.round(u),
        insurancePayout: m
      }
    );
  }
  function chooseLifestyle(e, t, a) {
    const s = ensureFinanceState(e);
    return !s || !LIFESTYLES[t].some(n => n.id === a) ? !1 : ((s.lifestyle[t] = a), !0);
  }
  function buyHouse(e, t) {
    const a = ensureFinanceState(e),
      s = houseById(t);
    if (!a || !s) return { ok: !1, reason: "Unavailable" };
    if (a.houseId === t) return { ok: !1, reason: "Already owned" };
    const n = houseById(a.houseId),
      i = Math.round((a.houseValue || n.value || 0) * 0.86),
      r = Math.max(0, s.price - i);
    return a.cash < r
      ? { ok: !1, reason: `Need ${formatMoney(r - a.cash)} more cash` }
      : ((a.cash -= r),
        (a.houseId = s.id),
        (a.houseValue = s.value),
        (a.familySecurity = clamp(a.familySecurity + (s.family || 0), 0, 100)),
        (a.fulfillment = clamp(a.fulfillment + (s.fulfillment || 0), 0, 100)),
        a.history.unshift({ week: a.weeksPaid, label: `Bought ${s.name}`, amount: -r, netWorth: netWorth(e) }),
        evaluateLifeGoals(e),
        { ok: !0, house: s, refund: i });
  }
  function invest(e, t, a) {
    const s = ensureFinanceState(e);
    if (!s || t === "cash") return { ok: !1, amount: 0, reason: "Invalid investment" };
    const n = emergencyReserve(e),
      i = Math.max(0, Math.min(Math.round(a), Math.max(0, s.cash - n)));
    return i < 1e3
      ? { ok: !1, amount: 0, reason: `Keep at least ${formatMoney(n)} in emergency cash` }
      : ((s.cash -= i),
        (s.investments[t] = (s.investments[t] || 0) + i),
        s.history.unshift({
          week: s.weeksPaid,
          label: `Invested in ${portfolioById(t)?.name || t}`,
          amount: -i,
          netWorth: netWorth(e)
        }),
        evaluateLifeGoals(e),
        { ok: !0, amount: i });
  }
  function withdraw(e, t, a) {
    const s = ensureFinanceState(e);
    if (!s || t === "cash") return { ok: !1, amount: 0, reason: "Invalid investment" };
    const n = s.investments[t] || 0,
      i = Math.max(0, Math.min(Math.round(a), n));
    return i < 1e3
      ? { ok: !1, amount: 0, reason: "Not enough invested" }
      : ((s.investments[t] -= i),
        (s.cash += i),
        s.history.unshift({
          week: s.weeksPaid,
          label: `Sold ${portfolioById(t)?.name || t}`,
          amount: i,
          netWorth: netWorth(e)
        }),
        { ok: !0, amount: i });
  }
  function contributeGoal(e, t, a) {
    const s = ensureFinanceState(e),
      n = lifeGoalById(t);
    if (!s || !n) return { ok: !1, amount: 0, reason: "Goal unavailable" };
    const i = Math.max(0, n.target - (s.contributions[t] || 0)),
      r = Math.max(0, Math.min(Math.round(a), s.cash, i));
    if (r < 1e3) return { ok: !1, amount: 0, reason: "Not enough available cash" };
    ((s.cash -= r),
      (s.contributions[t] = (s.contributions[t] || 0) + r),
      t === "charity-foundation" && (s.donations += r));
    const l = evaluateLifeGoals(e).some(d => d.id === t);
    return (
      s.history.unshift({ week: s.weeksPaid, label: `Funded ${n.name}`, amount: -r, netWorth: netWorth(e) }),
      { ok: !0, amount: r, completed: l }
    );
  }
  function evaluateLifeGoals(e) {
    const t = ensureFinanceState(e);
    if (!t) return [];
    const a = [],
      s = netWorth(e);
    for (const n of LIFE_GOALS) {
      const i = n;
      if (t.completedGoals[i.id]) continue;
      let r = !1;
      (i.netWorth
        ? (r = s >= i.target)
        : i.house
          ? (r = ["family-home", "mansion", "legacy-estate"].includes(t.houseId))
          : i.portfolio
            ? (r = (t.investments[i.portfolio] || 0) >= i.target)
            : (r = (t.contributions[i.id] || 0) >= i.target),
        r &&
          ((t.completedGoals[i.id] = !0),
          (t.familySecurity = clamp(t.familySecurity + (i.family || 0), 0, 100)),
          (t.fulfillment = clamp(t.fulfillment + (i.fulfillment || 0), 0, 100)),
          a.push(i)));
    }
    return a;
  }
  function maybeQueueLifeEvent(e, t) {
    const a = ensureFinanceState(e);
    if (!a || a.eventQueue.length || (t.week || 0) < 2 || (t.week || 0) % 3 !== 0) return null;
    const s = LIFE_EVENTS.filter(
      l => (t.week || 0) >= l.minWeek && !a.resolvedEvents.includes(`${l.id}-${e.totalSeasons || 0}`)
    );
    if (!s.length) return null;
    const n = seededRng(e.seasonSeed, e.totalSeasons, t.week, "life-event");
    if (n() > 0.72) return null;
    const i = s[Math.floor(n() * s.length)],
      r = { ...i, uid: `${i.id}-${e.totalSeasons || 0}-${t.week}` };
    return (a.eventQueue.push(r), r);
  }
  function resolveLifeEvent(e, t) {
    const a = ensureFinanceState(e),
      s = a?.eventQueue?.[0];
    if (!a || !s) return null;
    const n = s.choices.find(r => r.id === t) || s.choices[0];
    if (
      (n.cash &&
        ((a.cash += n.cash),
        n.cash > 0 ? (a.endorsements += n.cash) : (a.careerExpenses += Math.abs(n.cash)),
        a.cash < 0 && ((a.debt += Math.abs(a.cash)), (a.cash = 0), a.missedPayments++)),
      n.goal && n.amount)
    ) {
      const r = Math.min(n.amount, a.cash);
      ((a.cash -= r),
        (a.contributions[n.goal] = (a.contributions[n.goal] || 0) + r),
        n.goal === "charity-foundation" && (a.donations += r));
    }
    if (n.portfolio && n.amount) {
      const r = Math.min(n.amount, Math.max(0, a.cash - 5e3));
      ((a.cash -= r), (a.investments[n.portfolio] = (a.investments[n.portfolio] || 0) + r));
    }
    if (
      (n.goalCredit &&
        n.creditAmount &&
        (a.contributions[n.goalCredit] = (a.contributions[n.goalCredit] || 0) + n.creditAmount),
      n.investCashPct && n.portfolio)
    ) {
      const r = emergencyReserve(e),
        l = Math.max(0, Math.min(a.cash - r, a.cash * n.investCashPct));
      ((a.cash -= l), (a.investments[n.portfolio] = (a.investments[n.portfolio] || 0) + l));
    }
    if (n.sellRiskyPct)
      for (const r of ["venture", "real-estate"]) {
        const l = Math.round((a.investments[r] || 0) * n.sellRiskyPct);
        ((a.investments[r] -= l), (a.cash += l));
      }
    if (n.portfolioShock)
      for (const r of ["bonds", "index", "real-estate", "venture"])
        a.investments[r] = Math.max(0, Math.round((a.investments[r] || 0) * (1 + n.portfolioShock)));
    (n.advisor && (a.advisorId = n.advisor),
      n.insurance != null && (a.insuranceCoverage = clamp(n.insurance, 0, 1)),
      n.goalIntent && (a.goalIntent = n.goalIntent),
      (a.familySecurity = clamp(a.familySecurity + (n.family || 0), 0, 100)),
      (a.fulfillment = clamp(a.fulfillment + (n.fulfillment || 0), 0, 100)),
      e.conditionV11 &&
        ((e.conditionV11.fatigue = clamp((e.conditionV11.fatigue || 0) + (n.fatigue || 0) - (n.recovery || 0), 0, 100)),
        (e.conditionV11.recovery = clamp((e.conditionV11.recovery || 55) + (n.recovery || 0), 0, 100)),
        (e.conditionV11.mentalLoad = clamp((e.conditionV11.mentalLoad || 20) + (n.mental || 0), 0, 100))),
      e.worldState && (e.worldState.mediaHeat = clamp((e.worldState.mediaHeat || 20) + (n.media || 0), 0, 100)),
      (e.coachTrust = clamp((e.coachTrust || 50) + (n.trust || 0), 0, 100)),
      n.nextPerf &&
        ((e.tempEffects = e.tempEffects || {}),
        (e.tempEffects[`life-${s.uid}`] = { games: 1, fx: { nextPerf: n.nextPerf } })),
      a.resolvedEvents.push(`${s.id}-${e.totalSeasons || 0}`),
      a.eventQueue.shift());
    const i = evaluateLifeGoals(e);
    return (
      a.history.unshift({
        week: a.weeksPaid,
        label: s.title,
        amount: n.cash || -(n.amount || 0),
        netWorth: netWorth(e)
      }),
      { event: s, choice: n, goals: i }
    );
  }
  function retirementReadiness(e) {
    const t = ensureFinanceState(e);
    if (!t) return null;
    const a = netWorth(e),
      s = retirementAssets(e),
      n = retirementPlanById(t.retirementPlanId),
      i = t.retirementTarget || n.target,
      r = clamp(s / i, 0, 2.5),
      l = safeAnnualIncome(e),
      d = s >= i && t.familySecurity >= n.family && t.debt <= 0 && t.fulfillment >= 40;
    return {
      netWorth: a,
      retirementAssets: s,
      target: i,
      ratio: r,
      annualIncome: l,
      ready: d,
      goals: Object.keys(t.completedGoals).length,
      totalGoals: LIFE_GOALS.length,
      plan: n,
      debt: t.debt
    };
  }
  function canRetire(e) {
    const t = retirementReadiness(e);
    return !!t && ((e.age || 0) >= 30 || t.ready);
  }
  function retirePlayer(e) {
    const t = ensureFinanceState(e);
    if (!t) return null;
    t.retired = !0;
    const a = retirementReadiness(e);
    return {
      ...a,
      ending: a?.ready
        ? "Retired on Your Terms"
        : a && a.netWorth >= a.target * 0.65
          ? "Comfortable Retirement"
          : "Football Ended Before the Money Was Ready"
    };
  }
  function formatMoney(e) {
    const t = Math.round(e || 0),
      a = Math.abs(t);
    return a >= 1e9
      ? `${t < 0 ? "-" : ""}$${Math.round(a / 1e6).toLocaleString()}M`
      : a >= 1e6
        ? `${t < 0 ? "-" : ""}$${Math.round(a / 1e6)}M`
        : a >= 1e3
          ? `${t < 0 ? "-" : ""}$${Math.round(a / 1e3)}K`
          : `${t < 0 ? "-" : ""}$${a.toLocaleString()}`;
  }
  function ensureAccountV12(e) {
    return (
      (e.schemaVersion = Math.max(e.schemaVersion || 1, LIFE_VERSION)),
      (e.featureFlagsV12 = {
        cutReflection: !0,
        balancePolish: !0,
        nflLife: !0,
        weeklySalary: !0,
        investing: !0,
        housing: !0,
        lifeGoals: !0,
        retirementPlanning: !0,
        ...(e.featureFlagsV12 || {})
      }),
      (e.completedLifeGoalsV12 = e.completedLifeGoalsV12 || {}),
      e
    );
  }
  const V12_MODULE = Object.freeze(
      Object.defineProperty(
        {
          __proto__: null,
          HOUSES: HOUSES,
          LIFESTYLES: LIFESTYLES,
          LIFE_EVENTS: LIFE_EVENTS,
          LIFE_GOALS: LIFE_GOALS,
          LIFE_VERSION: LIFE_VERSION,
          PORTFOLIOS: PORTFOLIOS,
          RETIREMENT_PLANS: RETIREMENT_PLANS,
          applyPendingVow: applyPendingVow,
          buyHouse: buyHouse,
          canRetire: canRetire,
          capDecisionChance: capDecisionChance,
          chooseLifestyle: chooseLifestyle,
          contributeGoal: contributeGoal,
          effectivePrestige: effectivePrestige,
          emergencyReserve: emergencyReserve,
          ensureAccountV12: ensureAccountV12,
          ensureFinanceState: ensureFinanceState,
          evaluateLifeGoals: evaluateLifeGoals,
          expectedGrade: expectedGrade,
          formatMoney: formatMoney,
          houseById: houseById,
          invest: invest,
          lifeGoalById: lifeGoalById,
          lifestyleEffects: lifestyleEffects,
          liquidInvestments: liquidInvestments,
          maybeQueueLifeEvent: maybeQueueLifeEvent,
          netWorth: netWorth,
          portfolioById: portfolioById,
          processNflFinancialWeek: processNflFinancialWeek,
          rawPrestigePowerRatio: rawPrestigePowerRatio,
          recoveryProjection: recoveryProjection,
          regretScenario: regretScenario,
          resolveLifeEvent: resolveLifeEvent,
          retirePlayer: retirePlayer,
          retirementAssets: retirementAssets,
          retirementPlanById: retirementPlanById,
          retirementReadiness: retirementReadiness,
          safeAnnualIncome: safeAnnualIncome,
          setRetirementPlan: setRetirementPlan,
          tunedInjuryRisk: tunedInjuryRisk,
          withdraw: withdraw
        },
        Symbol.toStringTag,
        { value: "Module" }
      )
    ),
    LEVELS = [
      {
        key: "peewee",
        name: "Pee Wee",
        grade: "Ages 8-9",
        age: 8,
        games: 8,
        seasons: 2,
        need: 16,
        prestige: 0.4,
        slots: 18e5,
        desc: "Backyard dreams. Everybody plays."
      },
      {
        key: "youth",
        name: "Youth League",
        grade: "Ages 10-12",
        age: 10,
        games: 8,
        seasons: 3,
        need: 28,
        prestige: 0.7,
        slots: 11e5,
        desc: "First real teams. Coaches start to notice."
      },
      {
        key: "middle",
        name: "Middle School",
        grade: "Grades 6-8",
        age: 12,
        games: 9,
        seasons: 3,
        need: 42,
        prestige: 1,
        slots: 6e5,
        desc: "Puberty picks winners. Cuts begin."
      },
      {
        key: "jv",
        name: "JV",
        grade: "Grades 9-10",
        age: 15,
        games: 9,
        seasons: 2,
        need: 56,
        prestige: 1.3,
        slots: 28e4,
        desc: "Prove you belong on varsity."
      },
      {
        key: "varsity",
        name: "Varsity",
        grade: "Grades 11-12",
        age: 17,
        games: 10,
        seasons: 2,
        need: 66,
        prestige: 1.9,
        slots: 11e4,
        desc: "Friday night lights. Scouts in the stands."
      },
      {
        key: "college",
        name: "College",
        grade: "Freshman-Senior",
        age: 19,
        games: 12,
        seasons: 4,
        need: 74,
        prestige: 2.8,
        slots: 16e3,
        desc: "Full scholarship. The grind is real."
      },
      {
        key: "combine",
        name: "UFF Combine",
        grade: "Draft Year",
        age: 22,
        games: 6,
        seasons: 1,
        need: 78,
        prestige: 3.6,
        slots: 1500,
        desc: "40 times and draft boards. One shot."
      },
      {
        key: "nfl",
        name: "The UFF",
        grade: "The League",
        age: 23,
        games: 17,
        seasons: 99,
        need: 80,
        prestige: 5,
        slots: 1700,
        desc: "You made it. Now stay."
      },
      {
        key: "interstellar",
        name: "Interstellar League",
        grade: "Beyond Earth",
        age: 25,
        games: 14,
        seasons: 99,
        need: 135,
        prestige: 12,
        slots: 96,
        desc: "The IFL. Ninety-six beings in the galaxy get the call. Champions only."
      }
    ],
    TIERS = {
      varsity: [
        {
          key: "power",
          name: "Powerhouse HS",
          icon: "🏆",
          comp: 1.3,
          growth: 1.35,
          ppMult: 1.5,
          stars: 1,
          desc: "Elite recruits everywhere. Brutal competition forges elite growth — and every scout watches."
        },
        {
          key: "strong",
          name: "Contender HS",
          icon: "🔥",
          comp: 1.12,
          growth: 1.15,
          ppMult: 1.2,
          stars: 0,
          desc: "Tough district, good coaching. Solid development, real exposure."
        },
        {
          key: "small",
          name: "Small Program",
          icon: "🌱",
          comp: 0.9,
          growth: 0.92,
          ppMult: 1,
          stars: 0,
          desc: "You're the star, but weak competition means slower growth and fewer eyes."
        }
      ],
      college: [
        {
          key: "blue",
          name: "Blue-Blood Program",
          icon: "👑",
          comp: 1.35,
          growth: 1.4,
          ppMult: 1.6,
          stars: 1,
          desc: "National title contender. UFF pipeline. The competition is savage — and so is the growth."
        },
        {
          key: "p5",
          name: "Power-Conference",
          icon: "⚡",
          comp: 1.15,
          growth: 1.18,
          ppMult: 1.25,
          stars: 0,
          desc: "Big-time college ball. Strong development and a clear path to the draft."
        },
        {
          key: "mid",
          name: "Mid-Major",
          icon: "🎓",
          comp: 0.92,
          growth: 0.95,
          ppMult: 1,
          stars: 0,
          desc: "Get on the field early, but you'll have to dominate to get noticed by the league."
        }
      ]
    };
  function tiersFor(e) {
    return TIERS[e] || null;
  }
  function currentTier(e) {
    const t = LEVELS[e.level].key;
    if (!e.tiers || !e.tiers[t]) return null;
    const a = TIERS[t];
    return (a && a.find(s => s.key === e.tiers[t])) || null;
  }
  function tierComp(e) {
    const t = currentTier(e);
    return t ? t.comp : 1;
  }
  function tierGrowth(e) {
    const t = currentTier(e);
    return t ? t.growth : 1;
  }
  function tierPPMult(e) {
    let t = 1;
    if (e.tiers)
      for (const a in e.tiers) {
        const s = TIERS[a];
        if (!s) continue;
        const n = s.find(i => i.key === e.tiers[a]);
        n && (t = Math.max(t, n.ppMult));
      }
    return t;
  }
  const ATTR_INFO = {
      speed: {
        name: "Speed",
        desc: "Straight-line burst",
        icon: "💨",
        metric: {
          label: "Top speed",
          better: "high",
          unit: "mph",
          fmt: e => Math.round(81.8 / (5.55 - 1.35 * (1 - Math.exp(-e / 85)))) + " mph"
        }
      },
      strength: {
        name: "Strength",
        desc: "Power at the point of contact",
        icon: "💪",
        metric: {
          label: "Bench press",
          better: "high",
          unit: "reps",
          fmt: e => Math.round(40 * (1 - Math.exp(-e / 95))) + " reps"
        }
      },
      quickness: {
        name: "Quickness",
        desc: "First-step explosion",
        icon: "⚡",
        metric: {
          label: "Shuttle speed",
          better: "high",
          unit: "mph",
          fmt: e => Math.round(40.9 / (4.75 - 1.05 * (1 - Math.exp(-e / 85)))) + " mph"
        }
      },
      agility: {
        name: "Agility",
        desc: "Cuts, jukes, change of dir",
        icon: "🌀",
        metric: {
          label: "3-cone speed",
          better: "high",
          unit: "mph",
          fmt: e => Math.round(61.4 / (7.9 - 1.6 * (1 - Math.exp(-e / 85)))) + " mph"
        }
      },
      awareness: {
        name: "Awareness",
        desc: "Football IQ: finds the ball, sees through fakes",
        icon: "🧠",
        metric: {
          label: "Wonderlic",
          better: "high",
          unit: "/50",
          fmt: e => Math.round(8 + 42 * (1 - Math.exp(-e / 95))) + "/50"
        }
      },
      catching: {
        name: "Catching",
        desc: "Hands & ball tracking",
        icon: "🧤",
        metric: {
          label: "Catch rate",
          better: "high",
          unit: "%",
          fmt: e => Math.round(45 + 52 * (1 - Math.exp(-e / 90))) + "%"
        }
      },
      throwing: {
        name: "Throwing",
        desc: "Arm strength & accuracy",
        icon: "🎯",
        metric: {
          label: "Throw velocity",
          better: "high",
          unit: "mph",
          fmt: e => Math.round(40 + 30 * (1 - Math.exp(-e / 95))) + " mph"
        }
      },
      tackling: {
        name: "Tackling",
        desc: "Wrap up & bring down",
        icon: "🛡️",
        metric: {
          label: "Tackle rate",
          better: "high",
          unit: "%",
          fmt: e => Math.round(50 + 47 * (1 - Math.exp(-e / 90))) + "%"
        }
      },
      grit: {
        name: "Grit",
        desc: "Clutch, effort, mental edge",
        icon: "🔥",
        metric: {
          label: "4th-qtr rating",
          better: "high",
          unit: "",
          fmt: e => Math.round(50 + 49 * (1 - Math.exp(-e / 95)))
        }
      },
      stamina: {
        name: "Stamina",
        desc: "Stay strong late in games",
        icon: "🫁",
        metric: {
          label: "Snaps/game",
          better: "high",
          unit: "",
          fmt: e => Math.round(20 + 55 * (1 - Math.exp(-e / 90)))
        }
      },
      injuryResist: {
        name: "Durability",
        desc: "Resist injuries",
        icon: "🩹",
        metric: {
          label: "Games/16 avail",
          better: "high",
          unit: "",
          fmt: e => Math.round(9 + 7 * (1 - Math.exp(-e / 90)))
        }
      },
      vision: {
        name: "Vision",
        desc: "See the field, find lanes & openings",
        icon: "👁️",
        metric: {
          label: "Read radius",
          better: "high",
          unit: "",
          fmt: e => (window.__visionRadiusV96 ? window.__visionRadiusV96(e) : Math.max(0, Math.round(e) - 74)) + " yd"
        }
      },
      acceleration: {
        name: "Acceleration",
        desc: "How fast you reach top speed",
        icon: "🚀",
        metric: {
          label: "10-yd burst",
          better: "high",
          unit: "mph",
          fmt: e => Math.round(20.5 / (1.85 - 0.55 * (1 - Math.exp(-e / 85)))) + " mph"
        }
      },
      jumping: {
        name: "Jumping",
        desc: "Vertical leap & high-point catches",
        icon: "🦘",
        metric: {
          label: "Vertical",
          better: "high",
          unit: "in",
          fmt: e => Math.round(20 + 24 * (1 - Math.exp(-e / 95))) + '"'
        }
      },
      blocking: {
        name: "Blocking",
        desc: "Seal defenders & protect the play",
        icon: "🧱",
        metric: {
          label: "Pass-block win%",
          better: "high",
          unit: "%",
          fmt: e => Math.round(40 + 55 * (1 - Math.exp(-e / 90))) + "%"
        }
      },
      ballControl: {
        name: "Ball Control",
        desc: "Ball security & elusiveness",
        icon: "🎮",
        metric: {
          label: "Fumble rate",
          better: "low",
          unit: "%",
          fmt: e => Math.round(3.2 - 3 * (1 - Math.exp(-e / 90))) + "%"
        }
      },
      discipline: {
        name: "Discipline",
        desc: "Avoid penalties & mental errors",
        icon: "🎖️",
        metric: {
          label: "Penalties/season",
          better: "low",
          unit: "",
          fmt: e => Math.max(0, Math.round(12 - 11 * (1 - Math.exp(-e / 90))))
        }
      }
    },
    ATTR_KEYS = Object.keys(ATTR_INFO),
    ATTR_HARD_CAP = 999;

  /* ===== v142 EVERY STAT SAYS WHAT IT DOES =====
   * An ⓘ beside every attribute, on all four screens that list them (the hub sheet `Vr`, the
   * training sheet `un`, the pregame sheet `pregamePlayerStatsV25` and the offseason board's
   * preview `tpRowV113`). It opens a card with a plain-language line, then what the stat ACTUALLY
   * drives — in the live play sim and outside it — written off the code that reads it (v141 put
   * every one of these on the field, so the detail is the mechanic, not flavour), plus the
   * player's own number, his soft cap and how much it weighs for HIS position.
   * Declared as hoisted `function`s beside `Le` because a boot-time render calls the button helper
   * by bare name — see v140. `window.__V142`; `v142check.mjs`. */
  const STAT_INFO_V142 = {
    speed: {
      short: "How fast you are once you're up to full speed.",
      f: [
        "Sets your top speed on every play — the engine builds a man's flat-out pace straight off this number.",
        "Receivers pull away from coverage with it; defenders run ball carriers down from behind with it.",
        "A quarterback with it beats the edge, so a collapsing pocket becomes a scramble instead of a sack."
      ],
      s: [
        "The top-speed and 40-yard numbers on your sheet, and the combine's speed drill.",
        "One of the heaviest OVR weights at receiver, corner, safety and running back."
      ],
      n: "It is read against everyone else on the field, never as an absolute — the same number is a track star in Pee Wee and ordinary in the UFF."
    },
    acceleration: {
      short: "How quickly you go from standing still to full speed.",
      f: [
        "Your burst out of the stance and off every cut — it runs for about a quarter-second, then top speed takes over.",
        "Feeds the broken-tackle roll and your balance through contact, so a burst runner slips more arm tackles.",
        "Also how fast you rebuild speed after a hard turn, which is most of what separates two men with the same top end."
      ],
      s: ["The 10-yard burst on your sheet and the combine's acceleration drill."],
      n: "Short fields and goal-line work live on this rather than on speed — there is no room to reach top speed."
    },
    quickness: {
      short: "Your first step, and how fast you react to what you see.",
      f: [
        "Sets your reaction time on every snap. Each point takes about 2.4 milliseconds off a base of 295, so a quick man is simply moving before a slow one is.",
        "Decides defensive-line shed contests, and whether a ball carrier picks a spin, a juke or a side step.",
        "Refreshes a defender's pursuit angle more often, so he re-aims at the carrier instead of running to where the play was."
      ],
      s: ["The shuttle number on your sheet and the combine's quickness drill."],
      n: "Position changes what it is worth: a corner's counts about 1.14×, an offensive lineman's about 0.84× — flipping your hips is the job in one and not the other."
    },
    agility: {
      short: "Changing direction without losing speed.",
      f: [
        "How much pace you keep through a cut. Low agility bleeds speed every time you turn; high agility carries it through.",
        "Your elusiveness on jukes, and how hard you are to wrap once a defender commits.",
        "Beating a press corner at the line, and winning separation at the top of a route."
      ],
      s: ["The 3-cone number on your sheet and the combine's agility drill."],
      n: "It pairs with acceleration: agility keeps the speed through the cut, acceleration rebuilds what is lost."
    },
    strength: {
      short: "Winning collisions, both ways.",
      f: [
        "Every block you shed or hold, every truck, stiff-arm and wrap, and who wins when a pile forms.",
        "Ripping the ball out of a carrier's hands, and holding onto it when someone rips at yours.",
        "Jamming a receiver at the line, and boxing a defender off a ball in the air."
      ],
      s: [
        "The bench-press number on your sheet and the combine's strength drill.",
        "The heaviest OVR weight on the offensive and defensive lines."
      ],
      n: "It is checked head-to-head against the man opposite, so it is worth most where you are outmatched."
    },
    awareness: {
      short: "Football IQ — seeing the play before it happens.",
      f: [
        "How fast a defender diagnoses a play. A high-awareness man reads run or pass early; a low one is still watching while the ball is gone.",
        "Resistance to play-action and fakes, and to a disguised coverage rotating late.",
        "For a passer: how well you lead a receiver, how tight your accuracy is, whether you see the blitz and slide the protection, and whether you take the sack instead of forcing it.",
        "Finding the ball in the air as a receiver, which is the difference between a catch and a ball off the helmet."
      ],
      s: ["The Wonderlic number on your sheet.", "Heavy OVR weight at quarterback, safety and linebacker."],
      n: "Your coverage ability is built from this and speed, half each — there is no separate coverage stat on your sheet."
    },
    catching: {
      short: "Whether you hold onto the ball when it arrives.",
      f: [
        "The base chance you complete a catch — worth about a third of a percent per point, on every ball thrown your way.",
        "How highly the quarterback grades you when he picks a target, so good hands also mean more targets.",
        "Winning a contested ball when a defender is in your chest."
      ],
      s: [
        "The catch-rate number on your sheet.",
        "The heaviest OVR weight at receiver, and joint-heaviest at tight end."
      ],
      n: "It decides whether you hold the ball, not whether it arrives well — that is the passer's throwing and awareness."
    },
    throwing: {
      short: "Arm strength and accuracy.",
      f: [
        "How hard and how far the ball goes, and how long it hangs — a weak arm gives defenders time to break on it.",
        "How much lead you put on a moving receiver, so the ball arrives where he will be rather than where he was.",
        "The size of your miss when you are wrong, and a real term in whether the catch is made at all.",
        "Caps how deep you can realistically go — the engine will not let you throw past your arm."
      ],
      s: ["The throw-velocity number on your sheet.", "By far the heaviest OVR weight at quarterback."],
      n: "Before v141 this never reached the field and every passer in the game threw identically — it is live now."
    },
    tackling: {
      short: "Bringing the ball carrier down.",
      f: [
        "Your chance of missing the tackle, and the quality of the wrap when you land it.",
        "Stopping a hurdle, and whether a carrier drags you for extra yards or goes down where he stood.",
        "Punching the ball out, and breaking up a catch as it arrives.",
        "Feeds the pass rush rating your front generates, so it shows up in sacks as well as stops."
      ],
      s: [
        "The tackle-rate number on your sheet and the combine's read.",
        "The heaviest OVR weight at linebacker, and heavy at safety and on the defensive line."
      ],
      n: "It is checked against the carrier's strength, agility and balance, so the same number misses more against a better back."
    },
    blocking: {
      short: "Keeping defenders off your ball carrier and your passer.",
      f: [
        "Every shed contest at the line — a rusher's strength and quickness against your blocking, ticking the whole play.",
        "Putting a man on the ground, which opens the hole the back is aiming at.",
        "Passing off a stunt cleanly when the front twists, instead of letting a free rusher through.",
        "A back or tight end picking up a blitzer who came free."
      ],
      s: [
        "The pass-block win-rate number on your sheet.",
        "The heaviest OVR weight on the offensive line, and joint-heaviest at tight end."
      ],
      n: "It protects the whole offence rather than your own box score, so it rarely shows in your stat line and always shows in the score."
    },
    vision: {
      short: "Seeing lanes and openings before they close.",
      f: [
        "Your read radius — from 75 up, every point lets you see a yard further down the field.",
        "Finding the cutback when the called lane is shut, which is where long runs actually come from.",
        "How far ahead a back projects defenders as he picks his lane, so he runs to space instead of into a filling gap.",
        "Helps a back spot and pick up a blitzer."
      ],
      s: [
        "The read-radius number on your sheet, quoted in yards.",
        "Heavy OVR weight at running back, quarterback, linebacker and safety."
      ],
      n: "Under 75 it reads as it always did; the gain starts above that line."
    },
    jumping: {
      short: "How high you get off the ground to go up for a ball.",
      f: [
        "Hurdling a tackler who goes low, which turns a two-yard gain into a broken play.",
        "High-pointing a ball over a defender on anything thrown deep.",
        "Swatting a pass away as a defender, and contesting a jump ball instead of conceding it."
      ],
      s: ["The vertical number on your sheet and the combine's jumping drill."],
      n: "It only comes into play on deep balls and in traffic — on a short throw nobody leaves the ground."
    },
    stamina: {
      short: "Staying as good in the fourth quarter as in the first.",
      f: [
        "How fast your tank refills between plays — the single biggest thing deciding whether you are still fresh late.",
        "How long your sprint lasts when you kick it in to chase or pull away.",
        "How much you fade over a long play: everyone slows after about two seconds, and stamina decides by how much.",
        "How many plays you are stuck gassed for after emptying the tank completely."
      ],
      s: [
        "The snaps-per-game number on your sheet.",
        "Multiplies your end-of-season performance roll, so it pays even in weeks you sim rather than watch."
      ],
      n: "It pairs with durability: stamina is how well you play tired, durability is whether being tired hurts you."
    },
    grit: {
      short: "Effort, and the will to finish the play.",
      f: [
        "A tackler who misses gets back into the play faster with it, instead of being out of the rep.",
        "Driving the pile forward after contact — the extra yard that moves the chains.",
        "Straining for the sticks while a defender has hold of you, and breaking the grip entirely.",
        "A passer's composure: how slowly panic builds with a rusher bearing down, and how high it can get."
      ],
      s: [
        "The fourth-quarter rating on your sheet.",
        "Widens your game-to-game swings, so a gritty player has bigger nights and rougher ones."
      ],
      n: "Most of its live effects start above the halfway mark — below that it is doing very little for you."
    },
    discipline: {
      short: "Staying sound and not biting on what isn't there.",
      f: [
        "Not falling for play action, a pump fake or a fake handoff — an undisciplined man commits and the play goes past him.",
        "Taking a clean pursuit angle instead of overrunning the ball carrier.",
        "Keeping contain on the edge rather than crashing inside and letting the run outside you.",
        "A passer's composure under pressure, alongside grit and awareness."
      ],
      s: ["Heavy OVR weight at linebacker, and counts at every position."],
      n: "It is the stat that stops you being wrong fast — it does nothing when you are already right."
    },
    ballControl: {
      short: "Ball security, and slipping the first man.",
      f: [
        "Whether a hard hit knocks the ball loose for a bobble or a fumble.",
        "Resisting a defender punching at the ball while you are wrapped up.",
        "Securing a catch that arrives with a defender on you.",
        "Holding on through a long grip as tacklers pile on."
      ],
      s: [
        "The fumble-rate number on your sheet — this one is better when it is lower.",
        "Counts for OVR at running back and quarterback especially."
      ],
      n: "A fumble hands the other team the ball, so this is worth more than its OVR weight suggests."
    },
    injuryResist: {
      short: "How well your body holds up to punishment.",
      f: [
        "How much of your speed you keep through a glancing hit or a stagger — a durable man is knocked off stride less, so he stays on his feet and keeps running.",
        "That is all it does during a play. Everything else it is worth happens between them."
      ],
      s: [
        "The injury roll every single week. It is the biggest single lever on whether you finish a season at all.",
        "The games-available number on your sheet, and it keeps paying past 100."
      ],
      n: "It is the least glamorous stat on the sheet and usually the one that decides a career — a great player who is hurt is a bench player."
    }
  };
  function statGroupV142(k) {
    try {
      for (const g of UP_GROUPS_V97) if (g[1].indexOf(k) >= 0) return g[0];
    } catch (_) {}
    return "ATTRIBUTE";
  }
  function statWeightSayV142(k) {
    const pl = state && state.player;
    if (!pl || !pl.pos || !POSITIONS[pl.pos]) return "";
    const w = POSITIONS[pl.pos].w[k] || 0;
    return w >= 0.14
      ? '<b class="si-key-v142">KEY for a ' + POSITIONS[pl.pos].name + "</b>"
      : w > 0
        ? "counts toward your " + POSITIONS[pl.pos].name + " rating"
        : "not part of your " + POSITIONS[pl.pos].name + " rating — it still works on the field";
  }
  function statInfoBtnV142(k) {
    if (!STAT_INFO_V142[k]) return "";
    const nm = (ATTR_INFO[k] && ATTR_INFO[k].name) || k;
    return (
      '<button type="button" class="si-b-v142" onclick="statInfoV142(\'' +
      k +
      '\');event.stopPropagation()" title="What ' +
      nm +
      ' does" aria-label="What ' +
      nm +
      ' does">i</button>'
    );
  }
  function statInfoV142(k) {
    const d = STAT_INFO_V142[k];
    if (!d) return;
    statInfoCloseV142();
    const pl = state && state.player,
      L = ATTR_INFO[k] || { name: k, icon: "" };
    let chips = "";
    try {
      if (pl && pl.attrs && pl.attrs[k] != null) {
        const cur = Math.round(pl.attrs[k]);
        const cap = typeof drSoftCap === "function" ? Math.round(drSoftCap(pl, k)) : 0;
        chips += '<span class="si-chip-v142">YOURS <b>' + cur + "</b></span>";
        if (cap) chips += '<span class="si-chip-v142">SOFT CAP <b>' + cap + "</b></span>";
        if (L.metric)
          chips +=
            '<span class="si-chip-v142">' + L.metric.label.toUpperCase() + " <b>" + L.metric.fmt(cur) + "</b></span>";
      }
    } catch (_) {}
    const ul = a => '<ul class="si-ul-v142">' + a.map(t => "<li>" + t + "</li>").join("") + "</ul>";
    const w = statWeightSayV142(k);
    const el = document.createElement("div");
    el.className = "si-wrap-v142";
    el.id = "statInfoV142";
    el.setAttribute("onclick", "statInfoCloseV142()");
    el.innerHTML =
      '<div class="si-card-v142" onclick="event.stopPropagation()" role="dialog" aria-label="' +
      L.name +
      '">' +
      '<button type="button" class="si-x-v142" onclick="statInfoCloseV142()" aria-label="Close">✕</button>' +
      '<div class="si-head-v142"><span class="si-ico-v142">' +
      (L.icon || "") +
      "</span>" +
      '<div><div class="si-name-v142">' +
      L.name +
      '</div><div class="si-sub-v142">' +
      statGroupV142(k) +
      (w ? " · " + w : "") +
      "</div></div></div>" +
      '<div class="si-short-v142">' +
      d.short +
      "</div>" +
      (chips ? '<div class="si-chips-v142">' + chips + "</div>" : "") +
      '<div class="si-sec-v142"><h4>On the field</h4>' +
      ul(d.f) +
      "</div>" +
      (d.s && d.s.length ? '<div class="si-sec-v142"><h4>Through the season</h4>' + ul(d.s) + "</div>" : "") +
      (d.n ? '<div class="si-note-v142">' + d.n + "</div>" : "") +
      "</div>";
    document.body.appendChild(el);
    try {
      el.querySelector(".si-x-v142").focus();
    } catch (_) {}
    document.addEventListener("keydown", statInfoKeyV142);
  }
  function statInfoKeyV142(e) {
    if (e && (e.key === "Escape" || e.key === "Esc")) statInfoCloseV142();
  }
  function statInfoCloseV142() {
    const el = document.getElementById("statInfoV142");
    if (el && el.parentNode) el.parentNode.removeChild(el);
    document.removeEventListener("keydown", statInfoKeyV142);
  }
  window.__V142 = {
    info: STAT_INFO_V142,
    btn: statInfoBtnV142,
    open: statInfoV142,
    close: statInfoCloseV142,
    keys: () => Object.keys(STAT_INFO_V142)
  };
  window.statInfoV142 = statInfoV142;
  window.statInfoCloseV142 = statInfoCloseV142;

  function treeFx(e) {
    let t = 0;
    if (!state || !state.tree) return 0;
    for (const a in state.tree) {
      const s = TREE_NODES[a];
      s && s.fx && s.fx[e] && (t += s.fx[e] * (state.tree[a] || 0));
    }
    return t;
  }
  function chaosTotal() {
    if (!state || !state.chaos) return 0;
    let e = 0;
    for (const t in state.chaos) e += state.chaos[t] || 0;
    return e;
  }
  function chaosCap() {
    return (state && state.chaosCap) || 0;
  }
  function chaosMaxed() {
    return chaosTotal() >= chaosCap();
  }
  function chaosPPMult() {
    const e = chaosTotal();
    return e ? 3 * Math.pow(1.16, e) * (1 + treeFx("chaosPP")) : 1;
  }
  function chaosEarnedMult(e) {
    return 1 + (chaosPPMult() - 1) * clamp99(Math.pow((e + 1) / 8, 1.6), 0.06, 1);
  }
  function Yo() {
    const e = chaosTotal();
    return e ? 0.18 + e * 0.012 + Math.floor(e / 30) * 0.08 + treeFx("chaosEnemy") / 100 : 0;
  }
  function chaosOppBoost() {
    const e = chaosTotal();
    return e ? 22 + e * 1.05 + Math.floor(e / 30) * 12 + treeFx("chaosEnemy") : 0;
  }
  function attrCap() {
    return ATTR_HARD_CAP + treeFx("capPlus") + chaosTotal() * 3;
  }
  const MASTERY_MAX = 10;
  function masteryLvl(e) {
    return (state && state.mastery && state.mastery[e]) || 0;
  }
  function masteryCost(e) {
    return 1 + Math.floor(masteryLvl(e) / 3);
  }
  function masteryMult(e) {
    const t = masteryLvl(e.key);
    return t ? (e.lowerBetter ? Math.max(0.4, 1 - 0.06 * t) : e.rate ? 1 + 0.04 * t : 1 + 0.08 * t) : 1;
  }
  function xi(e) {
    return e <= 55
      ? e
      : e >= 170
        ? 100
        : e >= 150
          ? 96 + ((e - 150) / 20) * 4
          : 55 + 45 * (1 - Math.exp(-(e - 55) / 40));
  }
  function perfScale(e) {
    return Math.max(0.1, Math.pow(Math.max(e, 6) / 60, 2.15));
  }
  const POSITIONS = {
      QB: {
        name: "Quarterback",
        desc: "Field general. Reads & throws.",
        w: {
          throwing: 0.24,
          awareness: 0.18,
          vision: 0.12,
          agility: 0.07,
          speed: 0.05,
          grit: 0.09,
          stamina: 0.05,
          quickness: 0.05,
          ballControl: 0.08,
          discipline: 0.07
        }
      },
      RB: {
        name: "Running Back",
        desc: "Speed & vision out of the backfield.",
        w: {
          speed: 0.16,
          agility: 0.15,
          quickness: 0.13,
          vision: 0.13,
          acceleration: 0.1,
          strength: 0.08,
          ballControl: 0.1,
          grit: 0.07,
          stamina: 0.05,
          awareness: 0.03
        }
      },
      WR: {
        name: "Wide Receiver",
        desc: "Burners with hands.",
        w: {
          speed: 0.18,
          catching: 0.2,
          agility: 0.12,
          quickness: 0.1,
          acceleration: 0.1,
          jumping: 0.1,
          vision: 0.06,
          awareness: 0.06,
          stamina: 0.08
        }
      },
      TE: {
        name: "Tight End",
        desc: "Blocker & mismatch weapon.",
        w: {
          catching: 0.16,
          strength: 0.14,
          blocking: 0.16,
          speed: 0.09,
          jumping: 0.08,
          awareness: 0.1,
          tackling: 0.05,
          grit: 0.08,
          stamina: 0.07,
          discipline: 0.07
        }
      },
      OL: {
        name: "Offensive Line",
        desc: "Trench warfare. Protect & maul.",
        w: {
          blocking: 0.28,
          strength: 0.24,
          awareness: 0.14,
          agility: 0.08,
          grit: 0.1,
          stamina: 0.08,
          discipline: 0.08
        }
      },
      DL: {
        name: "Defensive Line",
        desc: "Explosive power off the edge.",
        w: {
          strength: 0.24,
          tackling: 0.18,
          quickness: 0.12,
          acceleration: 0.12,
          grit: 0.12,
          stamina: 0.08,
          awareness: 0.07,
          discipline: 0.07
        }
      },
      LB: {
        name: "Linebacker",
        desc: "Heart of the defense. Sideline to sideline.",
        w: {
          tackling: 0.2,
          strength: 0.14,
          speed: 0.11,
          vision: 0.11,
          awareness: 0.13,
          grit: 0.1,
          stamina: 0.1,
          discipline: 0.11
        }
      },
      CB: {
        name: "Cornerback",
        desc: "Island coverage. Fast & fearless.",
        w: {
          speed: 0.2,
          agility: 0.15,
          quickness: 0.14,
          acceleration: 0.1,
          jumping: 0.09,
          awareness: 0.12,
          catching: 0.08,
          discipline: 0.06,
          stamina: 0.06
        }
      },
      S: {
        name: "Safety",
        desc: "Last line. Range & big hits.",
        w: {
          speed: 0.15,
          tackling: 0.15,
          awareness: 0.16,
          vision: 0.12,
          agility: 0.1,
          jumping: 0.06,
          catching: 0.08,
          grit: 0.1,
          discipline: 0.08
        }
      }
    },
    POS_BODY = {
      QB: { h: [75, 4], w: [225, 25], mus: 0.5, note: "Tall to see the field" },
      RB: { h: [70, 3], w: [215, 20], mus: 0.8, note: "Compact & powerful" },
      WR: { h: [73, 4], w: [195, 20], mus: 0.6, note: "Long & lean speedster" },
      TE: { h: [77, 3], w: [250, 20], mus: 0.7, note: "Big athletic frame" },
      OL: { h: [77, 3], w: [315, 30], mus: 0.6, note: "Massive & immovable" },
      DL: { h: [75, 3], w: [290, 30], mus: 0.8, note: "Powerful & explosive" },
      LB: { h: [74, 3], w: [245, 20], mus: 0.85, note: "Dense muscle, range" },
      CB: { h: [71, 3], w: [190, 18], mus: 0.7, note: "Quick-twitch & lean" },
      S: { h: [72, 3], w: [205, 18], mus: 0.75, note: "Rangy & physical" }
    };
  function bodyFit(e, t) {
    if (!e) return 0.6;
    const a = POS_BODY[t],
      s = Math.max(0, 1 - Math.abs(e.height - a.h[0]) / (a.h[1] * 2.2)),
      n = Math.max(0, 1 - Math.abs(e.weight - a.w[0]) / (a.w[1] * 2.2));
    let i = a.mus;
    typeof state < "u" &&
      state &&
      state.tree &&
      nodeLvl("lean") &&
      ["WR", "CB", "RB", "S"].includes(t) &&
      (i += nodeLvl("lean") * 0.06);
    const r = e.muscle / 100;
    return s * 0.4 + n * 0.4 + r * i * 0.2;
  }
  function physFit(e, t) {
    return Math.round((bodyFit(e, t) - 0.6) * 35);
  }
  function fmtHeight(e) {
    return Math.floor(e / 12) + "'" + (e % 12) + '"';
  }
  function bodyLabel(e) {
    return e
      ? e.weight >= 290
        ? "Massive"
        : e.weight >= 250
          ? "Big"
          : e.height >= 75 && e.weight <= 210
            ? "Tall & lean"
            : e.height <= 70 && e.muscle >= 65
              ? "Compact & powerful"
              : e.muscle >= 70
                ? "Chiseled"
                : e.height >= 75
                  ? "Long"
                  : e.weight <= 195
                    ? "Lean"
                    : "Balanced"
      : "";
  }
  function Pi(e) {
    const t = e || { height: 0, weight: 0, muscle: 0 },
      a = Math.random();
    let s, n, i;
    return (
      a < 0.12
        ? ((s = randRange(73, 80)), (n = randRange(278, 338)), (i = randRange(46, 72)))
        : a < 0.25
          ? ((s = randRange(73, 79)), (n = randRange(225, 282)), (i = randRange(42, 68)))
          : a < 0.55
            ? ((s = randRange(67, 75)), (n = randRange(172, 222)), (i = randRange(32, 60)))
            : ((s = randRange(68, 77)), (n = randRange(195, 265)), (i = randRange(35, 64))),
      (s = clamp99(Math.round(s + t.height * 0.8), 62, 82)),
      (n = clamp99(Math.round(n + t.weight * 8), 150, 355)),
      (i = clamp99(Math.round(i + t.muscle * 6), 20, 95)),
      { height: s, weight: n, muscle: i }
    );
  }
  /* ===== v112 THE FRAME HE HAS, AND THE FRAME HE IS GOING TO GET =====
   * Pi() has always rolled a GROWN MAN's frame and the game then handed it to an eight-year-old
   * at Pee Wee. That frame is not what he IS, it is what he PROJECTS to — so it keeps its name
   * (`player.body`), keeps every gameplay read it already had (pa(), the OVR build bonus, ns()'s
   * position fit: you are scouting the man he becomes, not the boy in front of you) and is
   * LABELLED a projection on every screen that prints it. What he is today is bodyNowV112(body,
   * age): one growth curve, DERIVED at read time — never rolled, never stored, no save migration —
   * so it walks up to the projection on its own as the career ages, and a man at growAdultAge is
   * his projection exactly. Nothing in the sim or on the attribute sheet reads the young frame,
   * which is what keeps the scoreboard still. ===== */
  const HT_FRAC_V112 = {
      8: 0.72,
      9: 0.745,
      10: 0.77,
      11: 0.795,
      12: 0.826,
      13: 0.866,
      14: 0.91,
      15: 0.95,
      16: 0.975,
      17: 0.99,
      18: 0.997
    },
    WT_FRAC_V112 = {
      8: 0.29,
      9: 0.32,
      10: 0.355,
      11: 0.395,
      12: 0.44,
      13: 0.51,
      14: 0.59,
      15: 0.685,
      16: 0.785,
      17: 0.865,
      18: 0.92,
      19: 0.95,
      20: 0.97,
      21: 0.99
    };
  function fracV112(e, t) {
    const a = TU("growYoungAge", 8),
      s = TU("growAdultAge", 22),
      n = Math.round(Number(t));
    return !isFinite(n) || n >= s ? 1 : n <= a ? (e[a] != null ? e[a] : 1) : e[n] != null ? e[n] : 1;
  }
  function bodyNowV112(e, t) {
    if (!e) return e;
    const a = fracV112(HT_FRAC_V112, t),
      s = fracV112(WT_FRAC_V112, t);
    return a >= 1 && s >= 1
      ? { height: e.height, weight: e.weight, muscle: e.muscle, proj: e, frac: 1, grown: !0 }
      : {
          height: clamp99(Math.round(e.height * a), 36, e.height),
          weight: clamp99(Math.round(e.weight * s), 45, e.weight),
          muscle: clamp99(Math.round(e.muscle * Math.pow(s, TU("growMusclePow", 0.55))), 8, e.muscle),
          proj: e,
          frac: +s.toFixed(3),
          grown: !1
        };
  }
  function bodyOfV112(e) {
    return e && e.body ? bodyNowV112(e.body, e.age) : null;
  }
  const TREE = {
    physical: {
      name: "Physical",
      icon: "💪",
      color: "#c9622f",
      nodes: [
        {
          key: "genetics",
          name: "Elite Genetics",
          icon: "🧬",
          desc: "+3 to every starting attribute.",
          cost: 8,
          mult: 1.5,
          max: 10
        },
        {
          key: "fastTwitch",
          name: "Fast-Twitch Fibers",
          icon: "💨",
          desc: "+6 starting Speed, Quickness & Accel.",
          cost: 5,
          mult: 1.4,
          max: 8
        },
        { key: "frame", name: "Big Frame", icon: "🏋️", desc: "+7 starting Strength.", cost: 5, mult: 1.4, max: 8 },
        { key: "iron", name: "Iron Body", icon: "🦾", desc: "+7 starting Durability.", cost: 4, mult: 1.38, max: 8 },
        { key: "nimble", name: "Loose Hips", icon: "🌀", desc: "+6 starting Agility.", cost: 5, mult: 1.4, max: 8 },
        { key: "lungs", name: "Big Lungs", icon: "🫁", desc: "+7 starting Stamina.", cost: 4, mult: 1.38, max: 8 },
        {
          key: "motor",
          name: "High Motor",
          icon: "⚙️",
          desc: "Injuries hurt less; +4 Stamina.",
          cost: 7,
          mult: 1.5,
          max: 6,
          req: { honors: 5 }
        },
        {
          key: "explosive",
          name: "Explosive First Step",
          icon: "⚡",
          desc: "+8 starting Quickness, Agility & Accel.",
          cost: 9,
          mult: 1.55,
          max: 6,
          req: { honors: 6 }
        },
        {
          key: "workhorse",
          name: "Workhorse",
          icon: "🐎",
          desc: "Stamina no longer drops from training.",
          cost: 10,
          mult: 1.6,
          max: 3,
          req: { node: "lungs", lvl: 4 }
        },
        {
          key: "freak",
          name: "Athletic Freak",
          icon: "🦍",
          desc: "Raise all starting attribute caps by +8.",
          cost: 20,
          mult: 1.9,
          max: 5,
          req: { node: "genetics", lvl: 5 }
        },
        {
          key: "primeGenes",
          name: "Prime Bloodline",
          icon: "🧪",
          desc: "+6 MORE to every starting attribute.",
          cost: 30,
          mult: 2,
          max: 4,
          req: { honors: 16 }
        },
        {
          key: "superhuman",
          name: "Superhuman",
          icon: "🦸",
          desc: "Attribute soft-caps each season +15 higher.",
          cost: 45,
          mult: 2.3,
          max: 3,
          req: { honors: 24 }
        },
        {
          key: "springs",
          name: "Pogo Springs",
          icon: "🦘",
          desc: "+5 starting Jumping & Agility.",
          cost: 5,
          mult: 1.4,
          max: 8,
          fx: { start_jumping: 3, start_agility: 2 }
        },
        {
          key: "anchor",
          name: "Low Anchor",
          icon: "⚓",
          desc: "+5 starting Strength & Tackling.",
          cost: 5,
          mult: 1.4,
          max: 8,
          fx: { start_strength: 3, start_tackling: 2 }
        },
        {
          key: "recovery",
          name: "Rapid Recovery",
          icon: "🧊",
          desc: "Injuries 6% rarer per level.",
          cost: 7,
          mult: 1.5,
          max: 6,
          fx: { injDown: 0.06 }
        },
        {
          key: "engine",
          name: "Twin Engines",
          icon: "🔋",
          desc: "+2 to ALL stats every game.",
          cost: 12,
          mult: 1.65,
          max: 6,
          req: { honors: 9 },
          fx: { perfFlat: 2 }
        },
        {
          key: "juggernaut",
          name: "Juggernaut",
          icon: "🛞",
          desc: "+12 potential ceiling per level.",
          cost: 22,
          mult: 1.9,
          max: 5,
          req: { honors: 14 },
          fx: { ceilPlus: 12 }
        },
        {
          key: "evolution",
          name: "Evolution",
          icon: "🧬",
          desc: "Raise the ABSOLUTE stat cap by +10.",
          cost: 34,
          mult: 2.1,
          max: 6,
          req: { node: "superhuman", lvl: 1 },
          fx: { capPlus: 10 }
        }
      ]
    },
    mental: {
      name: "Mental",
      icon: "🧠",
      color: "#3f8f6b",
      nodes: [
        {
          key: "personaMastery",
          name: "Sports Psychologist",
          icon: "🧘",
          desc: "Softens every personality DRAWBACK 15% per level (injury risk, boom/bust swings, stamina burn, slow starts).",
          cost: 9,
          mult: 1.7,
          max: 4
        },
        {
          key: "personaPlus",
          name: "Identity Coach",
          icon: "🪞",
          desc: "+2 personality ADJUSTMENT points per level — bend your rolled personality further at creation (stacks with the +1 you earn per prestige).",
          cost: 12,
          mult: 2,
          max: 2
        },
        {
          key: "talent",
          name: "Natural Talent",
          icon: "⭐",
          desc: "+6% attribute growth.",
          cost: 6,
          mult: 1.4,
          max: 12
        },
        {
          key: "coachable",
          name: "Coachable",
          icon: "📋",
          desc: "+1 upgrade point per season.",
          cost: 8,
          mult: 1.55,
          max: 8
        },
        {
          key: "filmrat",
          name: "Film Rat",
          icon: "🎞️",
          desc: "+7 starting Awareness & +4 Vision.",
          cost: 5,
          mult: 1.38,
          max: 8
        },
        {
          key: "clutch",
          name: "Clutch Gene",
          icon: "🔥",
          desc: "+7 starting Grit; big-game bonuses +20%.",
          cost: 6,
          mult: 1.45,
          max: 8
        },
        { key: "handsy", name: "Soft Hands", icon: "🧤", desc: "+7 starting Catching.", cost: 5, mult: 1.4, max: 6 },
        { key: "cannon", name: "Live Arm", icon: "🎯", desc: "+7 starting Throwing.", cost: 5, mult: 1.4, max: 6 },
        { key: "wrap", name: "Sure Tackler", icon: "🛡️", desc: "+7 starting Tackling.", cost: 5, mult: 1.4, max: 6 },
        {
          key: "quickstudy",
          name: "Quick Study",
          icon: "📚",
          desc: "Training focus effects +15% stronger.",
          cost: 10,
          mult: 1.55,
          max: 6,
          req: { honors: 7 }
        },
        {
          key: "vet",
          name: "Veteran Presence",
          icon: "🎖️",
          desc: "+2 upgrade points per season.",
          cost: 14,
          mult: 1.7,
          max: 4,
          req: { node: "coachable", lvl: 5 }
        },
        {
          key: "prodigy",
          name: "Prodigy",
          icon: "🌟",
          desc: "First 3 seasons of every career grant double growth.",
          cost: 22,
          mult: 1.9,
          max: 4,
          req: { node: "talent", lvl: 6 }
        },
        {
          key: "genius",
          name: "Football Genius",
          icon: "🧩",
          desc: "+14 Awareness; reads never fail events.",
          cost: 28,
          mult: 2,
          max: 3,
          req: { honors: 18 }
        },
        {
          key: "mastermind",
          name: "Mastermind",
          icon: "♟️",
          desc: "+10% growth AND +2 points per season.",
          cost: 40,
          mult: 2.2,
          max: 3,
          req: { honors: 22 }
        },
        {
          key: "iceVeins",
          name: "Ice in the Veins",
          icon: "🥶",
          desc: "Game-to-game swings 8% smaller — fewer bad days.",
          cost: 7,
          mult: 1.5,
          max: 5,
          fx: { varDown: 0.08 }
        },
        {
          key: "bigStage",
          name: "Big Stage DNA",
          icon: "🎭",
          desc: "+4 performance in every playoff game.",
          cost: 9,
          mult: 1.55,
          max: 6,
          req: { honors: 6 },
          fx: { playoffPerf: 4 }
        },
        {
          key: "silverTongue",
          name: "Silver Tongue",
          icon: "🗣️",
          desc: "Scouts love you — +2% advance chance.",
          cost: 8,
          mult: 1.55,
          max: 6,
          req: { honors: 6 },
          fx: { advFlat: 2 }
        },
        {
          key: "chipShoulder",
          name: "Chip on the Shoulder",
          icon: "😤",
          desc: "Failed declares build +3 extra Determination.",
          cost: 6,
          mult: 1.5,
          max: 5,
          fx: { detPlus: 3 }
        },
        {
          key: "visionary",
          name: "Field Visionary",
          icon: "👁️",
          desc: "+6 starting Vision & Awareness.",
          cost: 6,
          mult: 1.45,
          max: 6,
          fx: { start_vision: 3, start_awareness: 3 }
        },
        {
          key: "zen",
          name: "Zen Focus",
          icon: "🧘",
          desc: "+1 to ALL stats every game AND swings 4% smaller.",
          cost: 15,
          mult: 1.7,
          max: 5,
          req: { honors: 12 },
          fx: { perfFlat: 1, varDown: 0.04 }
        }
      ]
    },
    career: {
      name: "Career",
      icon: "📈",
      color: "#c99a2f",
      nodes: [
        {
          key: "recruited",
          name: "Blue-Chip Recruit",
          icon: "💎",
          desc: "Higher starting recruiting stars & rank.",
          cost: 7,
          mult: 1.45,
          max: 6
        },
        {
          key: "medic",
          name: "Team Medical Staff",
          icon: "⚕️",
          desc: "Season-ending injuries 20% less likely.",
          cost: 8,
          mult: 1.5,
          max: 6
        },
        {
          key: "goodProgram",
          name: "Winning Culture",
          icon: "🏆",
          desc: "Your teams are stronger — half a percent of team quality per level, up to +3% (win more games).",
          cost: 9,
          mult: 1.5,
          max: 6
        },
        {
          key: "extra",
          name: "Second Wind",
          icon: "♻️",
          desc: "+1 season allowed at each level before the cut.",
          cost: 14,
          mult: 1.8,
          max: 4
        },
        {
          key: "gym",
          name: "Private Trainer",
          icon: "🏟️",
          desc: "+5% attribute growth every season.",
          cost: 9,
          mult: 1.5,
          max: 6
        },
        {
          key: "headstart",
          name: "Head Start",
          icon: "🚀",
          desc: "Begin career one level higher.",
          cost: 16,
          mult: 2.1,
          max: 3,
          req: { honors: 5 }
        },
        {
          key: "spotlight",
          name: "Spotlight",
          icon: "📸",
          desc: "Ranking climbs 20% faster from good seasons.",
          cost: 11,
          mult: 1.55,
          max: 6,
          req: { honors: 8 }
        },
        {
          key: "combineKing",
          name: "Combine King",
          icon: "🏅",
          desc: "Combine event gains are doubled.",
          cost: 12,
          mult: 1.6,
          max: 3,
          req: { honors: 9 }
        },
        {
          key: "dynastyTeam",
          name: "Dynasty Program",
          icon: "🏰",
          desc: "Recruited by championship teams — +4% team quality per 5 levels & you carry them further.",
          cost: 18,
          mult: 1.75,
          max: 4,
          req: { honors: 10 }
        },
        {
          key: "legacy",
          name: "Family Legacy",
          icon: "🏈",
          desc: "+1 PP income per season played.",
          cost: 18,
          mult: 1.7,
          max: 5,
          req: { node: "recruited", lvl: 4 }
        },
        {
          key: "phenom",
          name: "Generational Phenom",
          icon: "👑",
          desc: "Start 5-star with an OVR head start.",
          cost: 26,
          mult: 1.9,
          max: 3,
          req: { honors: 15 }
        },
        {
          key: "unstoppable",
          name: "Unstoppable",
          icon: "🚂",
          desc: "+2 seasons allowed AND injuries 15% rarer.",
          cost: 38,
          mult: 2.2,
          max: 2,
          req: { honors: 20 }
        },
        {
          key: "thirdTrait",
          name: "Pedigree",
          icon: "🎲",
          desc: "Every new player rolls a THIRD trait — always positive.",
          cost: 24,
          mult: 3,
          max: 1,
          req: { honors: 10 },
          fx: { trait3: 1 }
        },
        {
          key: "boosters",
          name: "Booster Club",
          icon: "📣",
          desc: "+2% team quality per 5 levels — the whole program lifts.",
          cost: 8,
          mult: 1.5,
          max: 6,
          fx: { teamQual: 0.04 }
        },
        {
          key: "january",
          name: "Built for January",
          icon: "❄️",
          desc: "+3 playoff performance AND title PP +15%.",
          cost: 14,
          mult: 1.65,
          max: 5,
          req: { honors: 9 },
          fx: { playoffPerf: 3, titleMult: 0.15 }
        },
        {
          key: "iron_sched",
          name: "Iron Sharpens Iron",
          icon: "⚔️",
          desc: "+5 potential ceiling per level.",
          cost: 9,
          mult: 1.5,
          max: 8,
          fx: { ceilPlus: 5 }
        }
      ]
    },
    economy: {
      name: "Legacy",
      icon: "👑",
      color: "#a05ac9",
      nodes: [
        {
          key: "endorse",
          name: "Endorsements",
          icon: "💰",
          desc: "+20% Prestige Points earned per career.",
          cost: 9,
          mult: 1.55,
          max: 8
        },
        {
          key: "scout",
          name: "Scouting Network",
          icon: "🔭",
          desc: "See rival ratings & your exact percentile.",
          cost: 6,
          mult: 1.7,
          max: 1
        },
        {
          key: "agent",
          name: "Super Agent",
          icon: "🤝",
          desc: "+15% PP AND +1 star recruiting.",
          cost: 12,
          mult: 1.6,
          max: 4
        },
        {
          key: "hof",
          name: "Hall of Fame Path",
          icon: "🏆",
          desc: "+30% PP if you reach the UFF.",
          cost: 14,
          mult: 1.65,
          max: 5,
          req: { honors: 8 }
        },
        {
          key: "brand",
          name: "Global Brand",
          icon: "🌍",
          desc: "+35% PP earned per career.",
          cost: 20,
          mult: 1.8,
          max: 4,
          req: { honors: 12 }
        },
        {
          key: "dynasty",
          name: "Dynasty",
          icon: "♛",
          desc: "Keep 10% of last career's attributes as a bonus.",
          cost: 25,
          mult: 2,
          max: 4,
          req: { honors: 14 }
        },
        {
          key: "goat",
          name: "The G.O.A.T.",
          icon: "🐐",
          desc: "+50% PP AND start with +2 prestige each career.",
          cost: 35,
          mult: 2.2,
          max: 3,
          req: { honors: 18 }
        },
        {
          key: "immortal",
          name: "Immortal",
          icon: "∞",
          desc: "+2 permanent prestige stars per UFF career.",
          cost: 40,
          mult: 2.4,
          max: 3,
          req: { honors: 24 }
        },
        {
          key: "ringChaser",
          name: "Ring Chaser",
          icon: "💍",
          desc: "Championship PP payouts +25% per level.",
          cost: 10,
          mult: 1.6,
          max: 6,
          fx: { titleMult: 0.25 }
        },
        {
          key: "mythmaker",
          name: "Mythmaker",
          icon: "📜",
          desc: "Milestone PP rewards +20% per level.",
          cost: 12,
          mult: 1.6,
          max: 5,
          req: { honors: 8 },
          fx: { mileMult: 0.2 }
        },
        {
          key: "trophyCase",
          name: "Trophy Case",
          icon: "🏛️",
          desc: "Challenge PP rewards +20% per level.",
          cost: 12,
          mult: 1.6,
          max: 5,
          req: { honors: 8 },
          fx: { chalMult: 0.2 }
        }
      ]
    },
    body: {
      name: "Body",
      icon: "🧍",
      color: "#4a90c9",
      nodes: [
        {
          key: "devHeight",
          name: "Growth Spurt",
          icon: "📏",
          desc: "Taller starting frame (+height).",
          cost: 6,
          mult: 1.45,
          max: 6
        },
        {
          key: "devWeight",
          name: "Mass Builder",
          icon: "⚖️",
          desc: "Heavier starting build (+weight).",
          cost: 6,
          mult: 1.45,
          max: 6
        },
        {
          key: "devMuscle",
          name: "Lean Muscle",
          icon: "🥩",
          desc: "Better starting muscle mass.",
          cost: 7,
          mult: 1.45,
          max: 6
        },
        {
          key: "lean",
          name: "Athletic Build",
          icon: "🏃",
          desc: "Muscle boosts fit at speed positions more.",
          cost: 8,
          mult: 1.5,
          max: 4
        },
        {
          key: "sculpt",
          name: "Body Sculptor",
          icon: "🎨",
          desc: "Nudge body toward position ideal each season.",
          cost: 12,
          mult: 1.6,
          max: 4,
          req: { honors: 6 }
        },
        {
          key: "nutrition",
          name: "Elite Nutrition",
          icon: "🥗",
          desc: "Muscle grows +2/season automatically.",
          cost: 10,
          mult: 1.55,
          max: 4,
          req: { honors: 7 }
        },
        {
          key: "perfectFrame",
          name: "Perfect Frame",
          icon: "💠",
          desc: "Physical fit bonus is +40% stronger.",
          cost: 18,
          mult: 1.8,
          max: 4,
          req: { node: "sculpt", lvl: 2 }
        },
        {
          key: "idealBody",
          name: "Ideal Specimen",
          icon: "🗿",
          desc: "Start with a body already matched to your best position.",
          cost: 28,
          mult: 2.1,
          max: 2,
          req: { honors: 16 }
        },
        {
          key: "ligaments",
          name: "Steel Ligaments",
          icon: "🦿",
          desc: "Injuries 8% rarer per level.",
          cost: 9,
          mult: 1.55,
          max: 5,
          req: { honors: 5 },
          fx: { injDown: 0.08 }
        },
        {
          key: "boneDensity",
          name: "Bone Density",
          icon: "🦴",
          desc: "+4 starting Durability & Strength.",
          cost: 6,
          mult: 1.45,
          max: 8,
          fx: { start_injuryResist: 2, start_strength: 2 }
        }
      ]
    },
    camp: {
      name: "Camps",
      icon: "🏕️",
      color: "#5aa0a0",
      nodes: [
        { key: "campSpeed", name: "Speed Camp", icon: "💨", desc: "+3 starting Speed.", cost: 4, mult: 1.35, max: 8 },
        {
          key: "campStr",
          name: "Strength Camp",
          icon: "💪",
          desc: "+3 starting Strength.",
          cost: 4,
          mult: 1.35,
          max: 8
        },
        {
          key: "campHands",
          name: "Receiver Camp",
          icon: "🧤",
          desc: "+3 starting Catching.",
          cost: 4,
          mult: 1.35,
          max: 8
        },
        { key: "campQB", name: "QB Academy", icon: "🎯", desc: "+3 starting Throwing.", cost: 4, mult: 1.35, max: 8 },
        {
          key: "campDef",
          name: "Defense Camp",
          icon: "🛡️",
          desc: "+3 starting Tackling.",
          cost: 4,
          mult: 1.35,
          max: 8
        },
        {
          key: "campIQ",
          name: "Chalk-Talk Camp",
          icon: "🧠",
          desc: "+3 starting Awareness.",
          cost: 4,
          mult: 1.35,
          max: 8
        },
        {
          key: "allStarCamp",
          name: "All-Star Camp",
          icon: "🌟",
          desc: "+2 to ALL starting attributes.",
          cost: 14,
          mult: 1.7,
          max: 5,
          req: { honors: 8 }
        },
        {
          key: "proDay",
          name: "Pro Day Prep",
          icon: "📋",
          desc: "+1 upgrade point per season.",
          cost: 10,
          mult: 1.6,
          max: 5,
          req: { honors: 6 }
        },
        {
          key: "megaCamp",
          name: "Elite Combine Camp",
          icon: "🔱",
          desc: "+4 to ALL starting attributes.",
          cost: 30,
          mult: 2,
          max: 3,
          req: { honors: 20 }
        },
        {
          key: "campFoot",
          name: "Footwork Camp",
          icon: "🦶",
          desc: "+3 starting Agility.",
          cost: 4,
          mult: 1.35,
          max: 8,
          fx: { start_agility: 3 }
        },
        {
          key: "campFilm",
          name: "Film Camp",
          icon: "📽️",
          desc: "+3 starting Vision.",
          cost: 4,
          mult: 1.35,
          max: 8,
          fx: { start_vision: 3 }
        },
        {
          key: "campGrit",
          name: "Grit Camp",
          icon: "🪨",
          desc: "+3 starting Grit.",
          cost: 4,
          mult: 1.35,
          max: 8,
          fx: { start_grit: 3 }
        },
        {
          key: "campEndure",
          name: "Conditioning Camp",
          icon: "🏔️",
          desc: "+3 starting Stamina.",
          cost: 4,
          mult: 1.35,
          max: 8,
          fx: { start_stamina: 3 }
        },
        {
          key: "privateCoach",
          name: "Position Guru",
          icon: "🧑‍🏫",
          desc: "+1 to ALL starting attributes.",
          cost: 8,
          mult: 1.5,
          max: 8,
          req: { honors: 4 },
          fx: { startAll: 1 }
        }
      ]
    },
    swagger: {
      name: "Swagger",
      icon: "🕶️",
      color: "#e05aa0",
      nodes: [
        {
          key: "highlightReel",
          name: "Highlight Reel",
          icon: "📼",
          desc: "+10% Prestige Points per career.",
          cost: 7,
          mult: 1.5,
          max: 8,
          fx: { ppMult: 0.1 }
        },
        {
          key: "mixtape",
          name: "Viral Mixtape",
          icon: "🎬",
          desc: "+1 recruiting star chance & +5% PP.",
          cost: 9,
          mult: 1.55,
          max: 5,
          fx: { ppMult: 0.05 }
        },
        {
          key: "trashTalk",
          name: "Trash Talk",
          icon: "🗯️",
          desc: "+1 to ALL stats every game — get in their heads.",
          cost: 10,
          mult: 1.6,
          max: 5,
          req: { honors: 6 },
          fx: { perfFlat: 1 }
        },
        {
          key: "crowdFavorite",
          name: "Crowd Favorite",
          icon: "📢",
          desc: "+3% team quality per 10 levels — the stadium lifts them.",
          cost: 9,
          mult: 1.55,
          max: 6,
          fx: { teamQual: 0.03 }
        },
        {
          key: "primetime",
          name: "PRIMETIME",
          icon: "🌙",
          desc: "+5 playoff performance — built for the lights.",
          cost: 16,
          mult: 1.7,
          max: 4,
          req: { honors: 10 },
          fx: { playoffPerf: 5 }
        },
        {
          key: "legendAura",
          name: "Aura",
          icon: "🔮",
          desc: "+2 perf, swings 5% smaller, +2% advance.",
          cost: 26,
          mult: 1.95,
          max: 4,
          req: { honors: 15 },
          fx: { perfFlat: 2, varDown: 0.05, advFlat: 2 }
        },
        {
          key: "iconStatus",
          name: "Icon Status",
          icon: "🗽",
          desc: "+25% PP AND championship PP +25%.",
          cost: 36,
          mult: 2.2,
          max: 3,
          req: { honors: 20 },
          fx: { ppMult: 0.25, titleMult: 0.25 }
        }
      ]
    },
    eternal: {
      name: "Eternal",
      icon: "∞",
      color: "#e8e8ff",
      nodes: [
        {
          key: "etGrowth",
          name: "Eternal Growth",
          icon: "🌌",
          desc: "+2% attribute growth. FOREVER repeatable.",
          cost: 12,
          mult: 1.22,
          max: 999,
          fx: { eGrowth: 0.02 }
        },
        {
          key: "etFortune",
          name: "Eternal Fortune",
          icon: "💫",
          desc: "+3% Prestige Points. FOREVER repeatable.",
          cost: 12,
          mult: 1.22,
          max: 999,
          fx: { ppMult: 0.03 }
        },
        {
          key: "etCeiling",
          name: "Eternal Ceiling",
          icon: "🛗",
          desc: "+2 potential ceiling. FOREVER repeatable.",
          cost: 14,
          mult: 1.22,
          max: 999,
          fx: { ceilPlus: 2 }
        },
        {
          key: "etForm",
          name: "Eternal Form",
          icon: "🌠",
          desc: "Half a point to ALL stats every game. FOREVER repeatable.",
          cost: 16,
          mult: 1.24,
          max: 999,
          fx: { perfFlat: 0.5 }
        },
        {
          key: "etWisdom",
          name: "Eternal Wisdom",
          icon: "📿",
          desc: "+1 upgrade point per season. FOREVER repeatable.",
          cost: 18,
          mult: 1.26,
          max: 999,
          fx: { pointsFlat: 1 }
        },
        {
          key: "etAegis",
          name: "Eternal Aegis",
          icon: "🌗",
          desc: "Injuries 2% rarer. FOREVER repeatable (soft cap 85%).",
          cost: 12,
          mult: 1.22,
          max: 999,
          fx: { injDown: 0.02 }
        }
      ]
    },
    apex: {
      name: "Apex",
      icon: "🏔️",
      color: "#ffd66b",
      nodes: [
        {
          key: "longevity",
          name: "Borrowed Time",
          icon: "⏳",
          desc: "+1 season before mandatory retirement, per level.",
          cost: 80,
          mult: 1.8,
          max: 3,
          req: { honors: 12 }
        },
        {
          key: "cleanSlate",
          name: "Clean Slate",
          icon: "🧼",
          desc: "Walking away from a career costs nothing: the reroll penalty never applies.",
          cost: 50,
          mult: 1,
          max: 1,
          req: { honors: 8 }
        },
        {
          key: "throwOpen",
          name: "Throw Him Open",
          icon: "🎯",
          desc: "Your quarterbacks throw receivers open +20% more often per level — the ball in stride, more of the time.",
          cost: 45,
          mult: 1.7,
          max: 4,
          req: { honors: 9 }
        },
        {
          key: "grudge",
          name: "Grudge Match",
          icon: "🔥",
          desc: "The rivalry pays ×2.5 instead of ×2 per level — trust, growth and the injury roll alike.",
          cost: 40,
          mult: 1.7,
          max: 3,
          req: { honors: 8 }
        },
        {
          key: "trainerRoom",
          name: "Trainer's Room",
          icon: "🏥",
          desc: "Once per career, a season-ending injury is downgraded to three games out.",
          cost: 90,
          mult: 1,
          max: 1,
          req: { honors: 14 }
        },
        {
          key: "reputation",
          name: "Reputation",
          icon: "📜",
          desc: "Every Honors requirement on this tree is 1 lower per level.",
          cost: 70,
          mult: 2,
          max: 3,
          req: { honors: 10 }
        },
        {
          key: "compound",
          name: "Compound Interest",
          icon: "🏦",
          desc: "Unspent PP earns 4% per season, per level (up to 50 PP a season per level).",
          cost: 120,
          mult: 2,
          max: 3,
          req: { honors: 16 }
        },
        {
          key: "franchiseTag",
          name: "Franchise Player",
          icon: "🏷️",
          desc: "Every new player starts with the coach's trust at 70 — the full snap share from day one.",
          cost: 150,
          mult: 1,
          max: 1,
          req: { honors: 18 },
          fx: { coachStart: 42 }
        },
        {
          key: "inheritance",
          name: "Bloodline",
          icon: "🧬",
          desc: "A new player inherits 25% of the last career's final attributes per level, on top of everything else.",
          cost: 200,
          mult: 2.2,
          max: 2,
          req: { honors: 20 }
        },
        {
          key: "oracle",
          name: "The Oracle",
          icon: "🔮",
          desc: "Every fate roll — the offseason program AND the weekly game plan — is rolled twice and the better kept.",
          cost: 300,
          mult: 1,
          max: 1,
          req: { honors: 24 }
        },
        /* ===== v139 THE PRICE OF A LONGER PRIME ===== the endgame tier: five figures a level, and every
         * one of them is a hook the game already reads. Second Wind answers ageCutV139, Early Declaration
         * answers ss(), and the four below ride fx keys the sim has honoured since v97. */
        {
          key: "secondWind",
          name: "Second Wind",
          icon: "🌬️",
          desc: "The age curve is read ONE YEAR YOUNGER per level, to eight — his athletic prime lasts that much longer.",
          cost: 5e3,
          mult: 1.35,
          max: 8,
          req: { honors: 15 }
        },
        {
          key: "earlyDeclare",
          name: "Early Declaration",
          icon: "📢",
          desc: "Declare for the next level a full season early, per level (never below one season).",
          cost: 1e4,
          mult: 2,
          max: 2,
          req: { honors: 18 }
        },
        {
          key: "apexOutput",
          name: "Peak Output",
          icon: "📈",
          desc: "+10% stat production across the board, per level.",
          cost: 15e3,
          mult: 1.6,
          max: 5,
          req: { honors: 20 },
          fx: { statProd: 0.1 }
        },
        {
          key: "apexCeiling",
          name: "No Ceiling",
          icon: "🚀",
          desc: "+10 potential ceiling per level — the soft cap moves with him.",
          cost: 25e3,
          mult: 1.8,
          max: 4,
          req: { honors: 22 },
          fx: { ceilPlus: 10 }
        },
        {
          key: "goldenAge",
          name: "Golden Age",
          icon: "🌞",
          desc: "+50% Prestige Points per level. The hoard grows with the legend.",
          cost: 4e4,
          mult: 1.7,
          max: 5,
          req: { honors: 25 },
          fx: { ppMult: 0.5 }
        },
        {
          key: "inevitable",
          name: "Inevitable",
          icon: "⚡",
          desc: "+8 to the chance of being called up, per level. They come looking for him.",
          cost: 6e4,
          mult: 2,
          max: 3,
          req: { honors: 30 },
          fx: { advBonus: 8 }
        }
      ]
    },
    franchise: {
      name: "Franchise",
      icon: "🏟️",
      color: "#5ad0c9",
      nodes: [
        {
          key: "gmEye",
          name: "GM's Eye",
          icon: "🔍",
          desc: "Half a percent of team quality per level — front office finds talent.",
          cost: 8,
          mult: 1.5,
          max: 6,
          fx: { teamQual: 0.05 }
        },
        {
          key: "oline_wall",
          name: "The Wall",
          icon: "🧱",
          desc: "Your line protects: injuries 5% rarer, +1 perf.",
          cost: 10,
          mult: 1.6,
          max: 5,
          req: { honors: 6 },
          fx: { injDown: 0.05, perfFlat: 1 }
        },
        {
          key: "playbook",
          name: "Deep Playbook",
          icon: "📖",
          desc: "+3% stat production across the board.",
          cost: 11,
          mult: 1.6,
          max: 8,
          req: { honors: 5 },
          fx: { statProd: 0.03 }
        },
        {
          key: "homeField",
          name: "Fortress Home Field",
          icon: "🏰",
          desc: "+2% team quality per 5 levels & +2 playoff performance.",
          cost: 14,
          mult: 1.65,
          max: 5,
          req: { honors: 9 },
          fx: { teamQual: 0.04, playoffPerf: 2 }
        },
        {
          key: "warRoom",
          name: "Draft War Room",
          icon: "🎛️",
          desc: "+3% advance chance — the league knows your name.",
          cost: 16,
          mult: 1.7,
          max: 4,
          req: { honors: 11 },
          fx: { advFlat: 3 }
        },
        {
          key: "juggernautTeam",
          name: "Superteam",
          icon: "💫",
          desc: "+7% team quality per 10 levels — a loaded roster every year.",
          cost: 24,
          mult: 1.9,
          max: 4,
          req: { honors: 15 },
          fx: { teamQual: 0.07 }
        },
        {
          key: "foreverFranchise",
          name: "Forever Franchise",
          icon: "♾️",
          desc: "Title PP +40% AND +8% stat production.",
          cost: 38,
          mult: 2.2,
          max: 3,
          req: { honors: 22 },
          fx: { titleMult: 0.4, statProd: 0.08 }
        }
      ]
    }
  };
  TREE.fate = {
    name: "Fate & Pressure",
    icon: "🎲",
    color: "#8f6bd1",
    nodes: [
      {
        key: "coachFavorite",
        name: "Coach's Favorite",
        icon: "📣",
        desc: "+8 starting Coach Trust per level.",
        cost: 9,
        mult: 1.55,
        max: 5,
        fx: { coachStart: 8 }
      },
      {
        key: "practiceAce",
        name: "Practice Ace",
        icon: "🧪",
        desc: "+7% depth-chart strength per level.",
        cost: 10,
        mult: 1.6,
        max: 6,
        fx: { depthPower: 0.07 }
      },
      {
        key: "specialTeams",
        name: "Special Teams Demon",
        icon: "⚡",
        desc: "Minimum snap share +4% per level.",
        cost: 8,
        mult: 1.5,
        max: 6,
        fx: { snapFloor: 0.04 }
      },
      {
        key: "nextMan",
        name: "Next Man Up",
        icon: "🪜",
        desc: "Good limited-snap games build 30% more trust.",
        cost: 11,
        mult: 1.62,
        max: 5,
        fx: { trustGain: 0.3 }
      },
      {
        key: "longLeash",
        name: "Long Leash",
        icon: "🦮",
        desc: "UFF cut threshold −4 performance per level.",
        cost: 15,
        mult: 1.72,
        max: 5,
        req: { honors: 10 },
        fx: { cutRelief: 4 }
      },
      {
        key: "guaranteedDeal",
        name: "Guaranteed Deal",
        icon: "📝",
        desc: "+1 protected UFF season before cuts.",
        cost: 26,
        mult: 2,
        max: 3,
        req: { honors: 18 },
        fx: { protectedYears: 1 }
      },
      {
        key: "comebackKid",
        name: "Comeback Kid",
        icon: "🔁",
        desc: "After a bad season, next season gains +5 performance.",
        cost: 15,
        mult: 1.7,
        max: 4,
        fx: { bounceBack: 5 }
      },
      {
        key: "luckyBreaks",
        name: "Lucky Breaks",
        icon: "🍀",
        desc: "Two and a half percent more chance of a huge positive game event.",
        cost: 12,
        mult: 1.62,
        max: 6,
        fx: { goodLuck: 0.025 }
      },
      {
        key: "survivor",
        name: "Roster Survivor",
        icon: "🛟",
        desc: "Cut rolls are 10% more forgiving.",
        cost: 18,
        mult: 1.8,
        max: 5,
        req: { honors: 14 },
        fx: { cutSave: 0.1 }
      },
      {
        key: "secondChance",
        name: "Second Chances",
        icon: "🔂",
        desc: "Survive one more UFF cut a season per level before the career ends.",
        cost: 30,
        mult: 2.1,
        max: 3,
        req: { honors: 16 },
        fx: { cutLives: 1 }
      },
      {
        key: "chaosScholar",
        name: "Chaos Scholar",
        icon: "🔥",
        desc: "+35% Chaos PP per level.",
        cost: 28,
        mult: 2,
        max: 5,
        req: { honors: 25 },
        fx: { chaosPP: 0.35 }
      },
      {
        key: "boomBust",
        name: "Boom or Bust",
        icon: "💥",
        desc: "+22% game variance; +18% PP.",
        cost: 7,
        mult: 1.55,
        max: 5,
        fx: { varianceUp: 0.22, ppMult: 0.18 },
        trade: "Your bad games become much worse."
      },
      {
        key: "glassCannon",
        name: "Glass Cannon",
        icon: "🔫",
        desc: "+4 to ALL stats every game; injuries 18% more likely.",
        cost: 9,
        mult: 1.6,
        max: 5,
        fx: { perfFlat: 4, injUp: 0.18 },
        trade: "More injuries and shorter careers."
      },
      {
        key: "heroBall",
        name: "Hero Ball",
        icon: "🦸",
        desc: "+7 playoff performance; −7 regular-season consistency.",
        cost: 13,
        mult: 1.7,
        max: 4,
        req: { honors: 9 },
        fx: { playoffPerf: 7, regularPenalty: 7 },
        trade: "Regular-season grades suffer."
      },
      {
        key: "mercenary",
        name: "Mercenary",
        icon: "💼",
        desc: "+30% PP; starting Coach Trust −10.",
        cost: 12,
        mult: 1.65,
        max: 5,
        fx: { ppMult: 0.3, coachStart: -10 },
        trade: "You must earn the locker room."
      },
      {
        key: "smallSchoolStar",
        name: "Small-School Star",
        icon: "🌾",
        desc: "+9 performance through Varsity; College exposure −8%.",
        cost: 10,
        mult: 1.62,
        max: 5,
        fx: { youthPerf: 9, collegeAdvPenalty: 8 },
        trade: "College recruiting is harder."
      },
      {
        key: "rawAthlete",
        name: "Raw Athlete",
        icon: "🧱",
        desc: "+6 all physical starts; −8 Awareness and Vision.",
        cost: 10,
        mult: 1.62,
        max: 5,
        fx: { physicalStart: 6, mentalStartPenalty: 8 },
        trade: "Decision-making develops from behind."
      },
      {
        key: "filmOnly",
        name: "Film-Room Savant",
        icon: "📽️",
        desc: "+8 Awareness/Vision; −5 Speed/Acceleration.",
        cost: 10,
        mult: 1.62,
        max: 5,
        fx: { mentalStart: 8, athleticStartPenalty: 5 },
        trade: "Lower athletic ceiling early."
      },
      {
        key: "undrafted",
        name: "Undrafted Hunger",
        icon: "😡",
        desc: "+12 UFF performance; UFF entry chance −10%.",
        cost: 18,
        mult: 1.78,
        max: 4,
        req: { honors: 16 },
        fx: { nflPerf: 12, nflAdvPenalty: 10 },
        trade: "Getting into the league is even harder."
      },
      {
        key: "chaosGambler",
        name: "Chaos Gambler",
        icon: "🎰",
        desc: "Chaos PP +55%; Chaos opponents +8 power.",
        cost: 24,
        mult: 1.9,
        max: 5,
        req: { honors: 24 },
        fx: { chaosPP: 0.55, chaosEnemy: 8 },
        trade: "Chaos becomes dramatically less stable."
      },
      {
        key: "allIn",
        name: "All In",
        icon: "☠️",
        desc: "+60% all PP; career-ending cut chance rises after poor seasons.",
        cost: 34,
        mult: 2.05,
        max: 3,
        req: { honors: 30 },
        fx: { ppMult: 0.6, cutRisk: 0.16 },
        trade: "One bad year can erase a deep run."
      }
    ]
  };
  const TREE_NODES = {};
  Object.entries(TREE).forEach(([e, t]) =>
    t.nodes.forEach(a => {
      TREE_NODES[a.key] = { ...a, branch: e };
    })
  );
  const PATHS = {
    phenom: {
      name: "The Phenom",
      icon: "🌟",
      color: "#f0bb45",
      tag: "Born great",
      cheapBranch: "physical",
      desc: "Start every career with a massive head start: +18 to all starting attributes and 5★ recruiting — but growth is slightly slower (you were already elite). Physical branch 25% cheaper.",
      startAttr: 18,
      startStars: 5,
      growthMult: 0.92,
      ppMult: 1
    },
    grinder: {
      name: "The Grinder",
      icon: "⚙️",
      color: "#c9622f",
      tag: "Outwork everyone",
      cheapBranch: "camp",
      desc: "Modest start, but +25% attribute growth every season and +2 upgrade points per year. You get better than anyone the longer you play. Camps branch 25% cheaper.",
      startAttr: 0,
      growthMult: 1.25,
      bonusPoints: 2,
      ppMult: 1
    },
    prodigy: {
      name: "The Prodigy",
      icon: "🧠",
      color: "#3f8f6b",
      tag: "Genius of the game",
      cheapBranch: "mental",
      desc: "Never fails an in-season decision, doubles combine gains, and advance odds get a permanent +8% boost. The smartest player on every field. Mental branch 25% cheaper.",
      advBonus: 8,
      growthMult: 1.05,
      ppMult: 1.05
    },
    magnate: {
      name: "The Magnate",
      icon: "👑",
      color: "#a05ac9",
      tag: "Build a dynasty",
      cheapBranch: "economy",
      desc: "+60% Prestige Points from every career and start each run with +3 prestige. Slower on the field, but your legacy compounds fastest. Legacy branch 25% cheaper.",
      growthMult: 0.96,
      ppMult: 1.6,
      startPrestige: 3
    },
    ironman: {
      name: "The Ironman",
      icon: "🦾",
      color: "#4a90c9",
      tag: "Unbreakable",
      cheapBranch: "body",
      desc: "Nearly injury-proof (−70% injury), +2 season attempts at every level, and durability never limits you. Longevity wins championships. Body branch 25% cheaper.",
      injuryMult: 0.3,
      extraSeasons: 2,
      growthMult: 1.08,
      ppMult: 1
    }
  };
  function currentPath() {
    return state.path ? PATHS[state.path] : null;
  }
  function pathVal(e, t) {
    const a = currentPath();
    return a && a[e] != null ? a[e] : t || 0;
  }
  function nodeLvl(e) {
    return (state.tree && state.tree[e]) || 0;
  }
  function nodeCost(e) {
    let t = e.cost * Math.pow(e.mult, nodeLvl(e.key));
    const a = currentPath(),
      s = (TREE_NODES[e.key] && TREE_NODES[e.key].branch) || e.branch;
    return (a && a.cheapBranch && s === a.cheapBranch && (t *= 0.75), Math.max(1, Math.round(t)));
  }
  const PATH_HONORS = 6;
  function Li(e) {
    return Math.max(1, Math.round(e * 1.6));
  }
  /* ===== v146 C THE HOARD BUYS THE IMPOSSIBLE =====
   * The Apex tier tops out at 60,000 PP and a legend with a hoard in the millions had nothing left
   * to want. So a branch of its own, THE IMPOSSIBLE: thirteen nodes from 100,000 PP to 10,000,000,
   * and every one of them moves a number the game ALREADY reads — nothing here is a new system:
   *   drCost()          the over-cap price: CRACK THE WALL (×5 past the wall → ×4, ×3, ×2), MOVE THE
   *                     WALL (250 → +25 a level), LONG BANDS (a band is 10 wide → +3 a level) and THE
   *                     PRICE CEILING (the band price stops climbing: 8, then 6, then 4 at most)
   *   drSoftCap()       RAISE THE BAR (+2% of ceiling a level on every soft cap)
   *   fatigueMulV120()  IRON LUNGS (the slope past 40 loses 20% of its bite a level)
   *   materializeInjuryV18()  MIRACLE HANDS (a 2+ game injury is 25% shorter a level, never under 1)
   *   kt()              BORN FIVE-STAR (+1 recruit ★ at birth a level, still capped at 5)
   *   dc()              ENDLESS REPS (+4 season points a level, beside pointsFlat)
   *   kt() (wrapped)    HAND-PICKED (+10 coach trust at birth a level, after the coachStart sum)
   *   fx keys           DRAGON'S HOARD (ppMult), BOX-SCORE MYTH (statProd), BEYOND POTENTIAL
   *                     (ceilPlus) — the same G() sums the older nodes ride. NOT pointsFlat or
   *                     coachStart: ai() soft-limits those two (3 and 8) so "+4 a level" would lie.
   * Nothing on this branch stops a man ageing: the age curve is v139's and Second Wind's alone.
   * EVERGREEN is retired (at Lv 2 the years never took a point) — a save that bought it is paid back
   * what it spent, once, at boot (`evergreenRefundV146`, called from mc()), and ageCutV139 no longer
   * reads it. `ppFmtV146` is how a price in the millions fits a buy button.
   * window.__V146C; scripts/v146Ccheck.mjs ===== */
  TREE.impossible = {
    name: "Impossible",
    icon: "🌌",
    color: "#b58cff",
    nodes: [
      {
        key: "wallCrack",
        name: "Crack the Wall",
        icon: "🔨",
        desc: "Every point past the wall costs ×4 the band price instead of ×5, per level — ×4, ×3, ×2 at Lv 3.",
        cost: 1e5,
        mult: 2.5,
        max: 3,
        req: { honors: 30 }
      },
      {
        key: "handPicked",
        name: "Hand-Picked",
        icon: "🤝",
        desc: "+10 starting Coach Trust per level, on top of everything else (never past 100).",
        cost: 125e3,
        mult: 2,
        max: 3,
        req: { honors: 30 }
      },
      {
        key: "longBands",
        name: "Long Bands",
        icon: "📏",
        desc: "Every price band past the soft cap is 3 points wider per level — 10 → 13 → 16 → 19 → 22 points before the price climbs again.",
        cost: 15e4,
        mult: 2,
        max: 4,
        req: { honors: 31 }
      },
      {
        key: "megaReps",
        name: "Endless Reps",
        icon: "🔁",
        desc: "+4 upgrade points every season, per level.",
        cost: 2e5,
        mult: 1.9,
        max: 5,
        req: { honors: 31 }
      },
      {
        key: "wallPush",
        name: "Move the Wall",
        icon: "🏗️",
        desc: "The wall moves 25 points higher per level — 250 → 275 → 300 → 325 → 350.",
        cost: 25e4,
        mult: 2,
        max: 4,
        req: { honors: 32 }
      },
      {
        key: "capRaise",
        name: "Raise the Bar",
        icon: "📐",
        desc: "Every soft cap sits 2% of his ceiling higher per level (+10% at Lv 5) — more points at 1 pt apiece.",
        cost: 3e5,
        mult: 1.8,
        max: 5,
        req: { honors: 32 }
      },
      {
        key: "ironLungs",
        name: "Iron Lungs",
        icon: "🫁",
        desc: "Fatigue past 40 costs 20% less per level — at 100 fatigue he plays at −16%, −12%, −8% instead of −20%. Hurt still holds him to −10%.",
        cost: 4e5,
        mult: 2,
        max: 3,
        req: { honors: 33 }
      },
      {
        key: "fastHeal",
        name: "Miracle Hands",
        icon: "🩺",
        desc: "An injury that keeps him out 2+ games heals 25% faster per level (rounded, never under 1 game). Season-enders are untouched.",
        cost: 5e5,
        mult: 2,
        max: 2,
        req: { honors: 33 }
      },
      {
        key: "bornStar",
        name: "Born Five-Star",
        icon: "🌟",
        desc: "Every new player is rated 1 recruit ★ higher at birth, per level (never past 5★).",
        cost: 75e4,
        mult: 2,
        max: 2,
        req: { honors: 34 }
      },
      {
        key: "megaPP",
        name: "Dragon's Hoard",
        icon: "🐉",
        desc: "+100% Prestige Points from every career, per level.",
        cost: 1e6,
        mult: 2.5,
        max: 3,
        req: { honors: 35 },
        fx: { ppMult: 1 }
      },
      {
        key: "statLegend",
        name: "Box-Score Myth",
        icon: "📜",
        desc: "+25% stat production across the board, per level.",
        cost: 25e5,
        mult: 2,
        max: 2,
        req: { honors: 36 },
        fx: { statProd: 0.25 }
      },
      {
        key: "ceilLift",
        name: "Beyond Potential",
        icon: "🌠",
        desc: "+25 potential ceiling per level at every season's roll — and every soft cap moves with it.",
        cost: 5e6,
        mult: 2,
        max: 2,
        req: { honors: 38 },
        fx: { ceilPlus: 25 }
      },
      {
        key: "priceCeiling",
        name: "The Price Ceiling",
        icon: "🛑",
        desc: "The band price past the soft cap stops climbing: at most 8 pts at Lv 1, 6 at Lv 2, 4 at Lv 3 (the wall still multiplies it).",
        cost: 1e7,
        mult: 2.5,
        max: 3,
        req: { honors: 40 }
      }
    ]
  };
  TREE.impossible.nodes.forEach(a => {
    TREE_NODES[a.key] = { ...a, branch: "impossible" };
  });
  function wallMultV146() {
    return Math.max(
      TU("wallMultFloorV146", 2),
      TU("drWallMult", 5) - nodeLvl("wallCrack") * TU("wallCrackStepV146", 1)
    );
  }
  function wallAtV146() {
    return TU("drWallAt", 250) + nodeLvl("wallPush") * TU("wallPushStepV146", 25);
  }
  function bandWV146() {
    return Math.max(1, TU("drBandWidth", 10) + nodeLvl("longBands") * TU("longBandsStepV146", 3));
  }
  function bandTopV146() {
    const l = nodeLvl("priceCeiling");
    return l > 0
      ? Math.max(TU("bandTopFloorV146", 4), TU("bandTopBaseV146", 10) - l * TU("bandTopStepV146", 2))
      : 1 / 0;
  }
  function softPctV146() {
    return nodeLvl("capRaise") * TU("capRaiseStepV146", 0.02);
  }
  function fatKeepV146() {
    return Math.max(0, 1 - nodeLvl("ironLungs") * TU("ironLungsStepV146", 0.2));
  }
  function healWeeksV146(w) {
    const l = nodeLvl("fastHeal"),
      g = gearV147("injDur");
    return (!l && !g) || !(w > 1)
      ? w
      : Math.max(1, Math.round(w * Math.max(0, 1 - l * TU("fastHealStepV146", 0.25)) * (1 - g)));
  } /* v147 C: gear shortens it too */
  function repsV146() {
    return nodeLvl("megaReps") * TU("megaRepsPtsV146", 4);
  }
  function trustPlusV146() {
    return nodeLvl("handPicked") * TU("handPickedStepV146", 10);
  }
  function starsPlusV146() {
    return nodeLvl("bornStar") * TU("bornStarStepV146", 1);
  }
  function ppFmtV146(n) {
    n = Math.round(Number(n) || 0);
    const t = (v, s) => (Math.round(v * 100) / 100).toString() + s;
    return n >= 1e9
      ? t(n / 1e9, "B")
      : n >= 1e6
        ? t(n / 1e6, "M")
        : n >= 1e5
          ? t(n / 1e3, "K")
          : n.toLocaleString("en-US");
  }
  /* EVERGREEN, retired: the PP its levels cost (the tree's own price, cost·mult^i — respec's sum), paid back once */
  function evergreenRefundV146() {
    try {
      if (!state || !state.tree || state.tree.evergreen == null) return 0;
      const l = Math.max(0, state.tree.evergreen | 0);
      delete state.tree.evergreen;
      if (!l || state.evergreenRefundV146) return 0;
      let t = 0;
      for (let i = 0; i < l; i++) t += Math.round(60 * Math.pow(1.9, i));
      state.pp = (state.pp || 0) + t;
      state.evergreenRefundV146 = { lv: l, pp: t, at: Date.now() };
      setTimeout(() => {
        try {
          showToast("🌲 Evergreen is retired — " + t + " PP refunded");
        } catch (_) {}
      }, 1800);
      return t;
    } catch (_) {
      return 0;
    }
  }
  window.__V146C = {
    nodes: () => TREE.impossible.nodes.map(n => n.key),
    wallMult: wallMultV146,
    wallAt: wallAtV146,
    bandW: bandWV146,
    bandTop: bandTopV146,
    softPct: softPctV146,
    fatKeep: fatKeepV146,
    heal: healWeeksV146,
    starsPlus: starsPlusV146,
    reps: repsV146,
    trustPlus: trustPlusV146,
    fmt: ppFmtV146,
    refund: evergreenRefundV146,
    fx: k => treeFx(k),
    drCost: (e, k) => drCost(e, k),
    softCap: (e, k) => drSoftCap(e, k)
  };
  /* ===== v130 HONORS, NOT STARS =====
   * The game had two star ratings and they meant nothing like each other. One is the RECRUIT
   * rating — the 1-5 stars on a player, the thing scouts give him, the thing drSoftCap reads. The
   * other was the account's PRESTIGE, drawn with the same ★ and quoted the same way: the header
   * chip said "★3", the prestige tree said "🔒 Needs ★8 prestige", the path screen said "Reach ★6
   * prestige", and none of those had anything to do with being a four-star recruit. Two currencies
   * wearing one symbol is not a UI problem, it is a rules problem: a player reading "Needs ★8"
   * reasonably concludes he needs an eight-star recruit, which does not exist.
   *
   * So the account's rank has its own name and its own mark: HONORS, 🎖️. Nothing about the model
   * moves — `o.prestige` is still the number, `Li()` is still the threshold curve, and PP is still
   * the currency you spend — but every place that used to draw a ★ for it now draws a medal and
   * says Honors, and the ★ means the recruit rating and only that. The node requirement key is
   * `honors` too, with the old `stars` still read so a save or a patch layer written against it
   * keeps working. `window.__V130`; `honorcheck.mjs` is the gate. ===== */
  const HONOR_ICON_V130 = "🎖️";
  const honorsV130 = () => Math.max(0, Math.round((typeof state < "u" && state && state.prestige) || 0));
  const honorTagV130 = n => HONOR_ICON_V130 + Math.round(n || 0);
  const honorReqV130 = req =>
    Math.max(
      1,
      Li((req && (req.honors != null ? req.honors : req.stars)) || 0) - nodeLvl("reputation")
    ); /* v134 Apex: Reputation */
  const honorHasReqV130 = req => !!(req && (req.honors != null || req.stars != null));
  window.__V130 = {
    icon: HONOR_ICON_V130,
    name: "Honors",
    have: honorsV130,
    tag: honorTagV130,
    req: honorReqV130,
    hasReq: honorHasReqV130,
    curve: n => Li(n)
  };
  function nodeUnlocked(e) {
    return e.req
      ? !(
          (honorHasReqV130(e.req) && state.prestige < honorReqV130(e.req)) ||
          (e.req.node && nodeLvl(e.req.node) < e.req.lvl)
        )
      : !0;
  } /* ===== v139 THE HONORS THE CARD PROMISES ARE THE HONORS YOU GET =====
   * Qs() counts the honors a career earned — level reached, rings, peak OVR, titles — and the
   * UFF card printed that count. Both settles then credited it through TU("prestigeGainMult"),
   * which was .2, so a card reading "+7 HONORS" moved the account by 1.4 and a short career
   * moved it by 0.2. The prestige tree's own gates run to 30 honors, a scale that only makes
   * sense against the number on the card, so the MULTIPLIER is what was wrong, not the card:
   * it is 1 now, one function does the conversion, and the card prints that function's answer. */
  function honorPayV139(raw) {
    return +((raw || 0) * TU("prestigeGainMult", 1)).toFixed(1);
  }
  window.__honorPayV139 = honorPayV139;
  function prestigeStarReward(e, t, a) {
    let s = 0;
    return (
      t >= 4 && (s += 1),
      t >= 5 && (s += 1),
      t >= 6 && (s += 1),
      t >= 7 && (s += 1),
      (e.nflRings || 0) > 0 && (s += 2),
      (e.peakOvr || playerOvr(e)) >= 90 && (s += 1),
      (e.titles || 0) >= 3 && (s += 1),
      a && (s += nodeLvl("immortal")),
      (s += Math.floor(nodeLvl("goat") / 2)),
      Math.max(1, s)
    );
  }
  function objectivesDone() {
    return state.objectivesCompleted || 0;
  }
  function chaosUnlockReady(e) {
    return !!(e && e.level >= 7 && (e.peakOvr || playerOvr(e)) >= 85 && objectivesDone() >= 20);
  }
  function chaosUnlockReq(e) {
    return `UFF title · 85 OVR (${e ? e.peakOvr || playerOvr(e) : 0}) · 20 objectives (${objectivesDone()})`;
  }
  const PROGRAMS = {
    balanced: {
      name: "Balanced Program",
      icon: "⚖️",
      desc: "Mainline development across every attribute, with a small balanced bonus.",
      priorityBonus: 0.04,
      injury: 1,
      focus: null
    },
    speed: {
      name: "Speed Academy",
      icon: "💨",
      desc: "Priority: Speed, Acceleration, Agility & Quickness. Track-first work — you give up a little mass for it.",
      priorityBonus: 0.1,
      injury: 1.25,
      focus: ["speed", "acceleration", "agility", "quickness"],
      cost: { strength: -1 }
    },
    weight: {
      name: "Weight Room",
      icon: "🏋️",
      desc: "Priority: Strength, Blocking & Tackling. Every hour under the bar is an hour out of the film room.",
      priorityBonus: 0.1,
      injury: 1.1,
      focus: ["strength", "tackling", "blocking"],
      cost: { awareness: -2, stamina: -1 }
    },
    film: {
      name: "Film Study",
      icon: "🎞️",
      desc: "Priority: Awareness, Vision & Discipline. +2 bonus points and low injury risk — but the tape room is not the weight room.",
      priorityBonus: 0.08,
      injury: 0.7,
      focus: ["awareness", "vision", "discipline"],
      bonusPoints: 2,
      cost: { strength: -1 }
    },
    skills: {
      name: "Skills Clinic",
      icon: "🎯",
      desc: "Priority: Catching, Throwing & Ball Control. Safe and steady.",
      priorityBonus: 0.09,
      injury: 0.9,
      focus: ["catching", "throwing", "ballControl"]
    },
    hops: {
      name: "Explosion & Hops",
      icon: "🦘",
      desc: "Priority: Jumping & Acceleration. Pure explosion shortens the stride, and top-end speed pays for it.",
      priorityBonus: 0.1,
      injury: 1.12,
      focus: ["jumping", "acceleration"],
      cost: { speed: -2 }
    },
    conditioning: {
      name: "Conditioning",
      icon: "🫁",
      desc: "Priority: Stamina & Durability. Safest program and lowest injury risk.",
      priorityBonus: 0.09,
      injury: 0.55,
      focus: ["stamina", "injuryResist"]
    },
    grind: {
      name: "The Grind",
      icon: "🔥",
      desc: "Everything at once, harder than anyone trains it. Brutal on the body — and on your discipline.",
      priorityBonus: 0.08,
      injury: 1.6,
      focus: null,
      cost: { injuryResist: -2, discipline: -1 }
    },
    yoga: {
      name: "Mobility & Yoga",
      icon: "🧘",
      desc: "Priority: Agility, Ball Control & Discipline. Heals the body — very low injury wear, +1 bonus point.",
      priorityBonus: 0.08,
      injury: 0.5,
      focus: ["agility", "ballControl", "discipline"],
      bonusPoints: 1
    },
    contact: {
      name: "Full-Contact Camp",
      icon: "💥",
      desc: "Priority: Tackling, Strength & Grit. The biggest gains in the game — you come out of camp meaner, and stiffer.",
      priorityBonus: 0.13,
      injury: 1.75,
      focus: ["tackling", "strength", "grit"],
      cost: { agility: -2, stamina: -1 }
    },
    track: {
      name: "Track Club",
      icon: "🏃",
      desc: "Priority: Speed & Acceleration. Elite top-end speed work, real hamstring risk, and mass off the frame.",
      priorityBonus: 0.12,
      injury: 1.35,
      focus: ["speed", "acceleration", "quickness"],
      cost: { strength: -2 }
    },
    lab: {
      name: "Recovery Lab",
      icon: "🧊",
      desc: "Priority: Stamina & Injury Resist. Cryo, sleep science and nutrition: modest growth, bulletproof body.",
      priorityBonus: 0.06,
      injury: 0.4,
      focus: ["stamina", "injuryResist"],
      bonusPoints: 1
    }
  };
  /* ===== v124 THE PROGRAM CUTS BOTH WAYS =====
   * Training was twelve flavours of the same promise: pick a program, get its stats. The only
   * price was a stray -1 Stamina on two of them and an injury multiplier you never saw resolve,
   * so "HIGH RISK" meant nothing you could point at afterwards.
   *
   * Two things now. First, the harder programs TRADE: they take a real, named attribute off you
   * to pay for the one they build (explosion at the cost of top-end speed, the weight room at the
   * cost of the film room, the track at the cost of mass). That price rides the SAME `cost` field
   * the growth roll and the v113 preview sheet have always read, so the board already draws it in
   * red and the season already charges it — it just has weight now.
   *
   * Second, the four volatile programs are a ROLL. PLAN_FATE_V124 gives each one odds and two
   * outcomes: HIT and the trade is waived and the priority growth is multiplied up; MISS and the
   * trade DOUBLES and the growth is cut. The odds are the player's to move — Weighted Coin
   * (`fateOdds`) lifts them, Loaded Dice (`fateReroll`) rolls twice and keeps the better, House
   * Money (`fateHedge`) turns a miss into a partial payout, and Scripted Destiny (`fateDestiny`)
   * rewrites one failure a season. The same four prestige nodes the game-plan roll already reads,
   * so a fate build is a build, not a per-screen gimmick.
   *
   * The roll happens ONCE, inside the season roll, and is stamped on the player
   * (`player.planFateV124`) so the report card and the coach can say which way it went. It is
   * never re-rolled by a re-render, and a program with no fate entry is exactly what it was.
   * `window.__V124` is the hook; `traincheck.mjs` is the gate. ===== */
  const PLAN_FATE_V124 = {
    contact: {
      odds: 0.55,
      hi: 1.55,
      lo: 0.55,
      hiSay: "Camp broke your way — you came out of it violent and whole.",
      loSay: "Camp broke you down instead. You spent the spring sore and stiff."
    },
    grind: {
      odds: 0.4,
      hi: 1.9,
      lo: 0.45,
      hiSay: "The grind paid. Nobody outworked you and the body held.",
      loSay: "The grind won. You trained through what you should have rested."
    },
    track: {
      odds: 0.6,
      hi: 1.45,
      lo: 0.6,
      hiSay: "The track work took — you are flat-out faster.",
      loSay: "You tightened up chasing a time and never found the extra gear."
    },
    hops: {
      odds: 0.65,
      hi: 1.35,
      lo: 0.7,
      hiSay: "The plyo block landed. You are coming off the ground differently.",
      loSay: "The jumps never carried over. All you got was heavy legs."
    }
  };
  function planFateOddsV124(k) {
    const d = PLAN_FATE_V124[k];
    if (!d) return 0;
    return clamp99(
      d.odds +
        nodeLvl("fateOdds") * 0.04 +
        (state && state.path === "prodigy" ? 0.05 : 0) +
        treeFx("fateOdds") +
        gearV147("fateOdds"),
      0.05,
      0.97
    );
  }
  /* the roll, resolved once and written down. hit -> the trade is waived; miss -> it doubles. */
  function rollPlanFateV124(e, k) {
    const d = PLAN_FATE_V124[k];
    if (!d || !e) return null;
    const p = planFateOddsV124(k);
    let hit = Math.random() < p,
      rerolled = !1,
      saved = !1;
    if (!hit && (nodeLvl("fateReroll") > 0 || nodeLvl("oracle") > 0)) {
      rerolled = !0;
      hit = Math.random() < p;
    } // Loaded Dice, or the Oracle (v134)
    if (!hit && nodeLvl("fateDestiny") > 0 && e._destinySeasonV124 !== (e.seasonsPlayed | 0)) {
      e._destinySeasonV124 = e.seasonsPlayed | 0;
      saved = hit = !0;
    } // Scripted Destiny
    const hedge = !hit && nodeLvl("fateHedge") > 0; // House Money
    return {
      key: k,
      hit,
      hedge,
      rerolled,
      saved,
      odds: p,
      growth: hit ? d.hi : hedge ? 1 + (d.hi - 1) * 0.3 : d.lo,
      tradeMul: hit ? 0 : hedge ? 1 : 2,
      say: hit ? d.hiSay : hedge ? d.loSay + " House Money covered part of it." : d.loSay
    };
  }
  /* what the board promises before you commit: the EXPECTED multiplier, so the preview sheet is
   * honest about a program whose season is a coin. No roll, no state, nothing written. */
  function planFateExpectV124(k) {
    const d = PLAN_FATE_V124[k];
    if (!d) return { growth: 1, tradeMul: 1 };
    const p = planFateOddsV124(k);
    return { growth: p * d.hi + (1 - p) * d.lo, tradeMul: (1 - p) * 2, odds: p };
  }
  function planFateSeasonV124(e, k) {
    if (!e) return null;
    const sn = (e.seasonsSinceStart | 0) + "/" + (e.level | 0),
      cur = e.planFateV124;
    if (cur && cur.key === k && cur.stamp === sn) return cur; // the roll happens ONCE a season, not once a render
    const r = rollPlanFateV124(e, k);
    if (!r) {
      e.planFateV124 = null;
      return null;
    }
    r.stamp = sn;
    e.planFateV124 = r;
    return r;
  }
  function planTradeV124(e, k) {
    const n = PROGRAMS[k];
    if (!n || !n.cost) return null;
    const out = {};
    for (const a in n.cost) out[a] = n.cost[a];
    return out;
  }
  /* the one line the tile, the sheet and the coach all say about a program's roll */
  function planFateSayV124(k) {
    const d = PLAN_FATE_V124[k];
    if (!d) return "";
    const p = planFateOddsV124(k);
    return (
      Math.round(p * 100) +
      "% IT LANDS · hits: the cost is waived and priority growth ×" +
      d.hi.toFixed(2).replace(/0$/, "") +
      " · misses: the cost DOUBLES and growth ×" +
      d.lo.toFixed(2).replace(/0$/, "")
    );
  }
  /* ===== v124 THE COACH NAMES A STAT =====
   * recommendTraining opened with `if (injuryResist < 30 + level*20) return "conditioning"` — a line
   * almost nobody clears (70 at level 2), so the coach said CONDITIONING every season of every
   * career whatever position you played or how the year had gone. Gone. The recommendation is a
   * SCORE over the position's own weighted stats now: how much the level wants that stat, how far
   * below its soft cap it still is (a capped stat is worth nothing — the next point costs 4), and
   * how far it lags the rest of the sheet. Durability is one line in that table like everything
   * else, and only wins when the body is genuinely the thing holding the career back.
   * `trainWhyV124` is the sentence: the stat, why it, and the program that trains it. ===== */
  const PROG_FOR_V124 = {
    speed: "speed",
    acceleration: "speed",
    agility: "speed",
    quickness: "speed",
    strength: "weight",
    tackling: "weight",
    blocking: "weight",
    catching: "skills",
    throwing: "skills",
    ballControl: "skills",
    awareness: "film",
    vision: "film",
    discipline: "film",
    jumping: "hops",
    grit: "contact",
    stamina: "conditioning",
    injuryResist: "conditioning"
  };
  function trainScoreV124(e) {
    const W = POSITIONS[e.pos].w,
      top = Math.max(1, ...Object.values(W)),
      cap = k => clamp99(drSoftCap(e, k), 1, attrCap());
    const vals = ATTR_KEYS.map(k => e.attrs[k] || 0),
      mean = vals.reduce((a, b) => a + b, 0) / Math.max(1, vals.length);
    const out = [];
    for (const k of ATTR_KEYS) {
      const v = e.attrs[k] || 0,
        c = cap(k),
        room = clamp99((c - v) / Math.max(8, c * 0.35), 0, 1.6);
      if (room <= 0.02) continue; // already at its cap: the next point costs 4
      const want = k === "injuryResist" || k === "stamina" ? 0.34 : (W[k] || 0) / top; // the body matters, but it is not a position
      const lag = clamp99((mean - v) / 26, -0.5, 1.1);
      let sc = want * 2.1 + room * 0.85 + lag * 0.6;
      if (k === "injuryResist" && v < 14 + e.level * 3) sc += 0.7; // a body that is actually breaking down jumps the queue
      if (!PROG_FOR_V124[k]) continue;
      out.push({ key: k, score: sc, room: Math.round(c - v), cap: Math.round(c), val: Math.round(v) });
    }
    return out.sort((a, b) => b.score - a.score);
  }
  function trainWhyV124(e) {
    const r = trainScoreV124(e)[0];
    if (!r) return null;
    const prog = PROG_FOR_V124[r.key],
      why =
        r.key === "injuryResist"
          ? "your body is the thing ending your seasons"
          : r.key === "stamina"
            ? "you are running out of gas before the fourth quarter"
            : (POSITIONS[e.pos].w[r.key] || 0) >= Math.max(...Object.values(POSITIONS[e.pos].w)) * 0.8
              ? "it is the stat your position is graded on"
              : "it is the furthest behind, and still has " + r.room + " points of room under its cap";
    return {
      stat: r.key,
      name: ATTR_INFO[r.key].name,
      prog,
      progName: (PROGRAMS[prog] || {}).name || prog,
      room: r.room,
      cap: r.cap,
      val: r.val,
      line: "Train " + ATTR_INFO[r.key].name + " — " + why + " (" + r.val + " of a " + r.cap + " cap)."
    };
  }
  function eventReq(e, t) {
    return Math.round(e + t * 22);
  }
  /* ===== v128 RIVALRY WEEK MEANS SOMETHING =====
   * Rivalry Week offered five choices and told you nothing about any of them. "+Big performance,
   * real injury risk" is not a number; "Safe, solid game" is not a number; and "Needs strong Grit or
   * it backfires" named a requirement of 84 Grit at Middle School that essentially nobody clears, so
   * the option was a trap that quietly took a -1.6x rating penalty when you picked it. There was also
   * no rivalry GAME: the stage was a season-wide modifier wearing a fixture's name, and one of its
   * five effects (`rank`) was never read by anything at all.
   *
   * All three are fixed here.
   *
   * WHAT IT DOES. `rivalSayV128` builds each option's effect list FROM its own `eff`, so the card can
   * never drift from the model: the rating it adds to every game, the injury multiplier, the
   * attributes it pays at season's end, and the chance the staff benches you for it. `rivalVarV128`
   * is the missing word — VARIANCE is a real multiplier now (`eff.varMult`), applied to every game's
   * rating around 50 exactly the way the persona sliders already do it, and the card says in plain
   * words what a wide one means: the good games get better AND the bad ones get worse.
   *
   * WHAT IT COSTS TO PICK. Every option but the safe one is LOCKED behind an attribute
   * (`reqAttr`/`reqBase`, scaled by `rivalReqV128` at a reachable +9 a level rather than the old
   * +22). Locked means locked: the card is dimmed, it states the number you need and the number you
   * have, and `chooseEvent` refuses it. A choice you cannot make is better than a choice that
   * silently punishes you for making it.
   *
   * AND THE GAME IS REAL. `rivalWeekV128` marks one mid-season fixture as the rivalry. That game is
   * played against a boosted opponent, and everything it pays lands DOUBLE: the coach-trust swing,
   * the injury roll, and — if you win it — a real bonus to the season's attribute growth. The
   * schedule row says RIVALRY, and so does the pregame. `window.__V128`; `rivalcheck.mjs`. */
  const RIVAL_MULT_V128 = () =>
    TU("rivalMult", 2) + nodeLvl("grudge") * TU("rivalGrudgeK", 0.5); /* v134 Apex: Grudge Match */
  function rivalReqV128(base, level) {
    return Math.round((base || 0) + (level | 0) * 9);
  }
  /* which fixture is the rivalry: a mid-season one, the same every time for a given schedule */
  function rivalWeekV128(games) {
    return clamp99(Math.round((games || 8) * 0.6), 1, Math.max(1, (games || 8) - 1));
  }
  function rivalLockV128(eff, pl) {
    if (!eff || !eff.reqAttr || !pl || !pl.attrs) return null;
    const need = rivalReqV128(eff.reqBase, pl.level),
      have = Math.round(pl.attrs[eff.reqAttr] || 0);
    return {
      attr: eff.reqAttr,
      name: (ATTR_INFO[eff.reqAttr] && ATTR_INFO[eff.reqAttr].name) || eff.reqAttr,
      need,
      have,
      ok: have >= need
    };
  }
  /* the option's effects, read off the option itself so the card and the model cannot disagree */
  function rivalSayV128(eff, pl) {
    const O = 6 + ((pl && pl.level) | 0) * 4,
      out = [],
      g = k => Math.round((eff[k] || 0) * 0.3 * O);
    if (eff.perf)
      out.push(
        `<b style="color:${eff.perf > 0 ? "#9fe6b0" : "#ff9b93"}">${eff.perf > 0 ? "+" : ""}${eff.perf}</b> to your rating in <b>every game</b> this season`
      );
    if (eff.inj)
      out.push(
        `injury risk <b style="color:${eff.inj > 0 ? "#ff9b93" : "#9fe6b0"}">×${(1 + eff.inj * 3).toFixed(2)}</b> all season`
      );
    if (eff.awareGainBase) out.push(`<b style="color:#9fe6b0">+${g("awareGainBase")}</b> Awareness at season’s end`);
    if (eff.gritGainBase) out.push(`<b style="color:#9fe6b0">+${g("gritGainBase")}</b> Grit at season’s end`);
    if (eff.disciplineGainBase)
      out.push(`<b style="color:#9fe6b0">+${g("disciplineGainBase")}</b> Discipline at season’s end`);
    if (eff.growth)
      out.push(`<b style="color:#9fe6b0">+${Math.round(eff.growth * 100)}%</b> attribute growth this season`);
    if (eff.bench)
      out.push(
        `<b style="color:#ff9b93">${Math.round(eff.bench * 100)}%</b> chance the staff sits you for it — <b>−14</b> rating if they do`
      );
    return out;
  }
  /* variance, said in words, because "volatile" is not a number either */
  function rivalVarV128(eff) {
    const v = (eff && eff.varMult) || 1,
      pct = Math.round(Math.abs(v - 1) * 100);
    if (v >= 1.6)
      return {
        band: "EXTREME",
        col: "#ff7a7a",
        line: `Every game's rating is pushed <b>${pct}% further from average</b>. Your best week becomes a monster and your worst becomes a disaster — this is the widest swing on the board.`
      };
    if (v >= 1.3)
      return {
        band: "HIGH",
        col: "#ff9b93",
        line: `Every game's rating is pushed <b>${pct}% further from average</b>: the good games get better AND the bad ones get worse. More ceiling, more floor.`
      };
    if (v > 1.02) return { band: "SOME", col: "var(--gold)", line: `Game to game swings widen by <b>${pct}%</b>.` };
    if (v < 0.98)
      return {
        band: "LOW",
        col: "#9fe6b0",
        line: `Every game's rating is pulled <b>${pct}% closer to average</b>: fewer disasters, fewer monsters. You will be who you are, every week.`
      };
    return { band: "NONE", col: "var(--chalk-dim)", line: "Week to week you play exactly as you would have anyway." };
  }
  function rivalWonV128(pl) {
    try {
      return !!((pl && pl.weekResults) || []).some(w => w && w.rivalV128 && w.played && w.won);
    } catch (_) {
      return !1;
    }
  }
  window.__V128 = {
    mult: RIVAL_MULT_V128,
    req: rivalReqV128,
    week: rivalWeekV128,
    lock: (e, p) => rivalLockV128(e, p || (state && state.player)),
    sched: p => buildSeasonSchedule(p || (state && state.player)),
    say: (e, p) => rivalSayV128(e, p || (state && state.player)),
    vary: rivalVarV128,
    won: p => rivalWonV128(p || (state && state.player)),
    stage: () => SEASON_EVENTS.find(z => z.id === "bigGame")
  };
  const SEASON_EVENTS = [
      {
        id: "bigGame",
        title: "Rivalry Week",
        minLevel: 2,
        weight: 2,
        text: e =>
          `One fixture on the ${LEVELS[e.player.level].name} schedule is the rivalry, and it is not like the others. The side is stronger than anything else you play, and everything the week pays out lands DOUBLE \u2014 the coach's trust in you, what the season adds to your attributes, and the chance you get hurt. Scouts in the stands. How do you approach it?`,
        choices: [
          {
            label: "Go all-out \u2014 leave it all on the field",
            eff: { perf: 16, inj: 0.2, varMult: 1.45, reqAttr: "grit", reqBase: 28, rivalV128: !0 }
          },
          { label: "Play smart and controlled", eff: { perf: 6, inj: 0.02, varMult: 0.8, rivalV128: !0 } },
          {
            label: "Showboat for the cameras",
            eff: { perf: 15, inj: 0.07, varMult: 1.7, bench: 0.3, reqAttr: "grit", reqBase: 38, rivalV128: !0 }
          },
          {
            label: "Study the rival all week",
            eff: {
              perf: 8,
              inj: -0.03,
              varMult: 0.85,
              awareGainBase: 1,
              reqAttr: "awareness",
              reqBase: 30,
              rivalV128: !0
            }
          },
          {
            label: "Feed your teammates the spotlight",
            eff: {
              perf: 2,
              varMult: 1,
              gritGainBase: 1,
              disciplineGainBase: 1,
              reqAttr: "discipline",
              reqBase: 26,
              rivalV128: !0
            }
          }
        ]
      },
      {
        id: "transfer",
        title: "Transfer Portal",
        minLevel: 3,
        maxLevel: 5,
        weight: 1,
        text: e =>
          `A powerhouse program wants ${e.player.name}. More exposure and faster growth — but you restart on the depth chart.`,
        choices: [
          {
            label: "Transfer up — bet on yourself",
            sub: "+20% growth this year, tougher games",
            eff: { perf: -7, growth: 0.2, rank: 6 }
          },
          { label: "Stay and be the franchise", sub: "Dominate where you are", eff: { perf: 7 } },
          { label: "Visit, then decide late", sub: "Keep every option open", eff: { perf: 3, growth: 0.08 } },
          {
            label: "Leverage the offer for a bigger role",
            sub: "Bold — the staff may call the bluff",
            eff: { perf: 6, bench: 0.2, rank: 3 }
          },
          {
            label: "Recruit stars to join YOU",
            sub: "Build the powerhouse at home",
            eff: { perf: 4, growth: 0.05, rank: 2 }
          }
        ]
      },
      {
        id: "coach",
        title: "Scheme Conflict",
        minLevel: 2,
        weight: 1,
        text: e => "The coordinator's scheme doesn't fit your strengths. What do you do?",
        choices: [
          {
            label: "Pitch a smarter scheme",
            sub: "Needs strong Awareness or it backfires",
            eff: { perf: 9, awareReqBase: 35, awareGainBase: 1 }
          },
          { label: "Keep your head down and grind", sub: "Small safe gain, +Grit", eff: { perf: 3, gritGainBase: 1 } },
          { label: "Demand a bigger role", sub: "High upside, risk of benching", eff: { perf: 12, bench: 0.3 } },
          {
            label: "Master the new scheme cold",
            sub: "Slow build, long payoff",
            eff: { perf: 2, awareGainBase: 2, growth: 0.06 }
          },
          { label: "Quietly play it your way", sub: "Production or the bench", eff: { perf: 8, bench: 0.25 } }
        ]
      },
      {
        id: "injury",
        title: "Nagging Injury",
        minLevel: 1,
        weight: 1,
        cond: e => e.midInjury,
        text: e => "You tweaked something. The trainer says you can play through it — your call.",
        choices: [
          { label: "Play through the pain", sub: "Risky, but builds Grit", eff: { inj: 0.35, gritGainBase: 2 } },
          {
            label: "Rest two games to heal",
            sub: "You WILL sit 2 games — body fully resets",
            eff: { perf: -4, restWeeks: 2, healFatigue: 30 }
          },
          {
            label: "Pay for elite treatment (2 PP)",
            sub: "Near-full recovery",
            eff: { perf: -3, inj: 0.05, ppCost: 2 }
          },
          {
            label: "Rest one game, ease back in",
            sub: "Miss 1 game, partial healing",
            eff: { perf: -2, restWeeks: 1, healFatigue: 16 }
          },
          {
            label: "Daily rehab around practice",
            sub: "No games missed, slow natural healing",
            eff: { perf: -5, inj: -0.08, healFatigue: 12 }
          }
        ]
      },
      {
        id: "mentor",
        title: "A Veteran Takes Notice",
        minLevel: 3,
        weight: 1,
        text: e => "An older star offers to mentor you — extra hours, less spotlight.",
        choices: [
          { label: "Soak up everything", sub: "+Awareness, long-term growth", eff: { awareGainBase: 3, growth: 0.08 } },
          { label: "Focus on your own game", sub: "Immediate production", eff: { perf: 5 } },
          {
            label: "Train with him every morning",
            sub: "Grueling but transformative",
            eff: { growth: 0.12, perf: -2 }
          },
          {
            label: "Ask him to attack your weakness",
            sub: "Targeted, humbling work",
            eff: { perf: 2, awareGainBase: 2 }
          },
          {
            label: "Decline politely, grind film alone",
            sub: "Lone-wolf preparation",
            eff: { perf: 3, awareGainBase: 1 }
          }
        ]
      },
      {
        id: "party",
        title: "Off-Field Temptation",
        minLevel: 3,
        weight: 1,
        text: e => "Teammates want you out the night before a big game.",
        choices: [
          {
            label: "Stay in and prepare",
            sub: "Discipline pays off",
            eff: { perf: 5, gritGainBase: 1, disciplineGainBase: 2 }
          },
          { label: "Go out — you earned it", sub: "Fun, but you pay for it", eff: { perf: -7, inj: 0.03 } },
          { label: "Go, but leave by ten", sub: "Balance the room and the body", eff: { perf: -2, gritGainBase: 1 } },
          {
            label: "Host a film night instead",
            sub: "Flip the party into prep",
            eff: { perf: 3, awareGainBase: 1, disciplineGainBase: 1 }
          },
          {
            label: "Skip it for recovery and treatment",
            sub: "Cold tubs while they dance",
            eff: { perf: 1, inj: -0.06, healFatigue: 12 }
          }
        ]
      },
      {
        id: "combine",
        title: "Combine Prep",
        minLevel: 6,
        maxLevel: 6,
        weight: 5,
        text: e => "The Combine defines your draft stock. Where do you focus?",
        choices: [
          {
            label: "Obsess over the 40-yard dash",
            sub: "Speed & Acceleration spike",
            eff: { focusGainBase: { speed: 5, acceleration: 4, quickness: 3 } }
          },
          {
            label: "Position drills & film",
            sub: "Look polished and pro-ready",
            eff: { focusGainBase: { awareness: 4, vision: 3, catching: 2, throwing: 2, tackling: 2 } }
          },
          {
            label: "Strength & explosion",
            sub: "Bench & vertical impress",
            eff: { focusGainBase: { strength: 5, jumping: 4, agility: 2 } }
          },
          {
            label: "Interview & leadership prep",
            sub: "Wow every front office in the room",
            eff: { focusGainBase: { awareness: 3, discipline: 3, vision: 2 }, rank: 4 }
          },
          {
            label: "Balanced program — no weaknesses",
            sub: "Solid everywhere, spectacular nowhere",
            eff: { focusGainBase: { speed: 2, strength: 2, awareness: 2, agility: 2 } }
          }
        ]
      }
    ],
    RIVAL_FIRST = [
      "Jaylen",
      "Trey",
      "DeAndre",
      "Cooper",
      "Zane",
      "Marcus",
      "Kai",
      "Bryce",
      "Tank",
      "Julian",
      "Semaj",
      "Rome",
      "Deacon",
      "Axel"
    ],
    RIVAL_LAST = [
      "Washington",
      "Kingsley",
      "Duval",
      "Steele",
      "Achebe",
      "Fontaine",
      "Rhodes",
      "Beckett",
      "Okoro",
      "Silva",
      "Cross",
      "Vega",
      "Dunham",
      "Frost"
    ],
    randRange = (e, t) => e + Math.random() * (t - e),
    randInt = (e, t) => Math.floor(randRange(e, t + 1)),
    clamp99 = (e, t = 1, a = 99) => Math.max(t, Math.min(a, e)),
    randPick = e => e[Math.floor(Math.random() * e.length)],
    byId = e => document.getElementById(e),
    FIRST_NAMES = [
      "Marcus",
      "Deshawn",
      "Tyler",
      "Cody",
      "Jamal",
      "Bo",
      "Trey",
      "Xavier",
      "Cole",
      "Isaiah",
      "Brady",
      "Kaden",
      "Malik",
      "Chase",
      "Rex",
      "Dez",
      "Nico",
      "Jaylen",
      "Hunter",
      "Amari"
    ],
    LAST_NAMES = [
      "Reyes",
      "Johnson",
      "Colt",
      "Vance",
      "Brooks",
      "Okafor",
      "Hill",
      "Sparks",
      "Mercer",
      "Cruz",
      "Bishop",
      "Stone",
      "Fox",
      "Rivera",
      "Boone",
      "Nash",
      "Wade",
      "Kingston",
      "Ellison",
      "Prince"
    ];
  function mulberry32(e) {
    return function () {
      ((e |= 0), (e = (e + 1831565813) | 0));
      let t = Math.imul(e ^ (e >>> 15), 1 | e);
      return ((t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t), ((t ^ (t >>> 14)) >>> 0) / 4294967296);
    };
  }
  const TRAITS = {
    xfactor: {
      name: "X-Factor",
      icon: "⚡",
      good: 1,
      desc: "Ice in the veins — plays bigger in playoff games (+8 perf)"
    },
    lateBloomer: { name: "Late Bloomer", icon: "🌱", good: 0, desc: "−10% growth before age 16, +20% after" },
    gymRat: { name: "Gym Rat", icon: "🏋️", good: 1, desc: "Training focus gains hit +15% harder" },
    coachsSon: { name: "Coach's Son", icon: "📋", good: 1, desc: "+1 upgrade point every season" },
    showman: { name: "Showman", icon: "🎬", good: 1, desc: "The cameras love you — +15% Prestige Points" },
    ironFrame: { name: "Iron Frame", icon: "🦾", good: 1, desc: "Injury chance −30%" },
    bornLeader: {
      name: "Born Leader",
      icon: "🎖️",
      good: 1,
      desc: "Teammates rally around you — team plays +6% better"
    },
    bigGameHunter: {
      name: "Big-Game Hunter",
      icon: "🎯",
      good: 1,
      desc: "Dominant games grant +1 extra upgrade point"
    },
    streaky: { name: "Streaky", icon: "🎲", good: 0, desc: "Wilder swings — higher highs, lower lows" },
    slowStarter: { name: "Slow Starter", icon: "🐢", good: 0, desc: "−15% growth first 3 seasons, +10% after" },
    glassBones: { name: "Glass Bones", icon: "🩹", good: -1, desc: "Fragile — injury chance +35%" },
    butterFingers: {
      name: "Butter Fingers",
      icon: "🧈",
      good: -1,
      desc: "Occasional costly mistakes (−4 perf floor games)"
    }
  };
  function hasTrait(e, t) {
    return !!(e && e.traits && e.traits.includes(t));
  }
  function rollTraits() {
    const e = Object.keys(TRAITS),
      t = e.filter(l => TRAITS[l].good === 1),
      a = e.filter(l => TRAITS[l].good === 0),
      s = e.filter(l => TRAITS[l].good === -1),
      n = randPick(t),
      i = Math.random();
    let r;
    return (
      i < 0.45 ? (r = randPick(t.filter(l => l !== n))) : i < 0.75 ? (r = randPick(a)) : (r = randPick(s)),
      [n, r]
    );
  }
  /* ===== v112 TWO CARDS FROM A POOL OF TEN =====
   * The second trait used to be a silent roll the player never saw (45% good / 30% neutral /
   * 25% bad). It is a DECISION now: ten traits are OFFERABLE, two of them are drawn at creation
   * and laid out as cards on the position screen, and the one he taps is the one he carries for
   * the career. The first trait is still Ai()'s guaranteed good roll. Every existing trait key,
   * effect and Oe() lookup is untouched — ironFrame still divides the injury roll, glassBones
   * still multiplies it — and the only two members of dt left OUT of the offer are the two pure
   * punishments: a card with nothing but a downside is not a choice. ===== */
  const TRAIT_POOL_V112 = [
    "xfactor",
    "gymRat",
    "coachsSon",
    "showman",
    "ironFrame",
    "bornLeader",
    "bigGameHunter",
    "lateBloomer",
    "slowStarter",
    "streaky"
  ];
  function traitOfferV112(e) {
    const t = TRAIT_POOL_V112.filter(a => TRAITS[a] && (e || []).indexOf(a) < 0),
      a = [];
    for (let s = TU("traitOfferN", 2); a.length < s && t.length;)
      a.push(t.splice(Math.floor(Math.random() * t.length), 1)[0]);
    return a;
  }
  function traitAutoV112(e) {
    if (!e || !Array.isArray(e.traitOfferV112) || !e.traitOfferV112.length) return null;
    const t = randPick(e.traitOfferV112);
    return (
      (e.traits = e.traits || []),
      e.traits.indexOf(t) < 0 && e.traits.push(t),
      (e.traitPickedV112 = t),
      (e.traitOfferV112 = null),
      t
    );
  }
  window.pickTraitV112 = function (e) {
    const t = typeof state < "u" && state && state.player;
    if (!t || !Array.isArray(t.traitOfferV112) || t.traitOfferV112.indexOf(e) < 0) return !1;
    return (
      (t.traits = t.traits || []),
      t.traits.indexOf(e) < 0 && t.traits.push(e),
      (t.traitPickedV112 = e),
      (t.traitOfferV112 = null),
      saveGame(),
      state.view === "choosePos" ? screenChoosePos() : render(),
      !0
    );
  };
  /* ===== v123 THE LEAGUE HAS A MAP =====
   * Fifteen towns and fifteen mascots meant the same handful of names all career, and the mascot was
   * picked with no regard for the crest: a "Buffaloes" side could wear the eagle, because logoForName
   * hashes any name it has no RULE for. Both halves are fixed here.
   *
   * TOWNS_V123 is 120 invented towns — no real city, college or pro club is in it, and the mascots
   * pair with invented towns anyway, so no pairing can collide with a real team. MASCOTS_V123 is 72,
   * and EVERY ONE of them matches a LOGO_RULES pattern, so the crest a side wears is always the animal
   * or figure in its name (`namecheck.mjs` proves it, mascot by mascot). The two old names, `Ga` and
   * `er`, keep their identity so every existing reader — the opponent generator, the leaders board,
   * the team identity — is handed the big pool without knowing anything changed.
   *
   * Above them the name is LEVEL-SHAPED: a youth side is a school, a college is a college, and the
   * UFF is a fixed fifty-club league drawn once per save (DFL_V123 / dflClubV123), so the pro league
   * a career climbs into holds still while the schools below it keep rolling. */
  const TOWNS = [
      "Fairview",
      "Riverton",
      "Oak Hill",
      "Maplewood",
      "Cedar Falls",
      "Bayside",
      "Ironwood",
      "Summit",
      "Clayton",
      "Westbrook",
      "Dry Creek",
      "Palmetto",
      "North Ridge",
      "Silver Lake",
      "Harlan",
      "Ashford",
      "Brookhaven",
      "Calder",
      "Dunmore",
      "Eastvale",
      "Fallbrook",
      "Granite Bay",
      "Hollow Rock",
      "Ingleside",
      "Jessup",
      "Kingsport",
      "Lakemont",
      "Millbrook",
      "Norwood",
      "Oakmont",
      "Pinehurst",
      "Quarry Hill",
      "Redstone",
      "Stonebridge",
      "Thornton",
      "Union Gap",
      "Verdant",
      "Whitfield",
      "Yarrow",
      "Zephyr Hills",
      "Amber Creek",
      "Bellhaven",
      "Cross Plains",
      "Deerfield",
      "Elmridge",
      "Foxglove",
      "Gladstone",
      "Hartwell",
      "Iron Gate",
      "Juniper",
      "Kestrel",
      "Larkspur",
      "Mesa Verde",
      "Northgate",
      "Orchard Park",
      "Prairie View",
      "Quail Run",
      "Rockport",
      "Seabright",
      "Tallgrass",
      "Umber Flats",
      "Vinemont",
      "Willow Bend",
      "Xenia Falls",
      "Yellow Pine",
      "Blackwater",
      "Copperfield",
      "Drift Valley",
      "Emberton",
      "Flint Hollow",
      "Glenmoor",
      "Havenport",
      "Ironbark",
      "Junction City",
      "Kettle Rock",
      "Lone Pine",
      "Marbury",
      "New Vernon",
      "Old Mill",
      "Pellston",
      "Quinn River",
      "Ridgeway",
      "Saltmarsh",
      "Tidewater",
      "Upton",
      "Valemont",
      "Windmere",
      "Yorkfield",
      "Ansley",
      "Braxton",
      "Cobalt Springs",
      "Dover Flats",
      "Edgewater",
      "Fernbank",
      "Goldvale",
      "Hillcrest Falls",
      "Inlet Bay",
      "Jasper Ridge",
      "Kirkwood",
      "Lantern Hill",
      "Moss Landing",
      "Nightingale",
      "Overlook",
      "Pinnacle",
      "Quarrytown",
      "Riverbend",
      "Stillwater Bay",
      "Templeton",
      "Ulmer",
      "Vantage",
      "Waterloo Springs",
      "Yardley Hollow",
      "Amberdale",
      "Bristow",
      "Cliffside",
      "Dunbarton",
      "Emberly Pass",
      "Fort Bellows",
      "Greyrock",
      "Hallowell"
    ],
    MASCOTS = [
      "Bulldogs",
      "Mustangs",
      "Wolverines",
      "Firebirds",
      "Tigers",
      "Hornets",
      "Rebels",
      "Yetis",
      "Pirates",
      "Bearcats",
      "Longhorns",
      "Thunder",
      "Wildcats",
      "Miners",
      "Rockets",
      "Wolves",
      "Grizzlies",
      "Panthers",
      "Eagles",
      "Gators",
      "Sharks",
      "Bulls",
      "Razorbacks",
      "Rams",
      "Bison",
      "Lions",
      "Jaguars",
      "Hawks",
      "Cobras",
      "Dragons",
      "Rhinos",
      "Phoenix",
      "Krakens",
      "Gorillas",
      "Owls",
      "Scorpions",
      "Mantises",
      "Coyotes",
      "Stags",
      "Jackals",
      "Broncos",
      "Spartans",
      "Knights",
      "Vikings",
      "Samurai",
      "Trojans",
      "Monarchs",
      "Barbarians",
      "Cavaliers",
      "Paladins",
      "Buccaneers",
      "Outlaws",
      "Reapers",
      "Golems",
      "Blizzard",
      "Sorcerers",
      "Emperors",
      "Crows",
      "Berserkers",
      "Wyverns",
      "Drakes",
      "Valkyries",
      "Inferno",
      "Bees",
      "Widows",
      "Fireflies",
      "Centipedes",
      "Crabs",
      "Sasquatch",
      "Meteors",
      "Lumberjacks",
      "Anglers",
      "Polar Bears",
      "Phantoms",
      "Demons",
      "Eclipse",
      "Icebergs",
      "Volcanoes",
      "Express",
      "Anvils",
      "Drillers",
      "Summits",
      "Swampers",
      "Beacons",
      "Invaders",
      "Hydras",
      "Voltage",
      "Octopi"
    ];
  /* the two suffixes a college wears, and the fifty clubs of the UFF. A club is a TOWN plus a MASCOT,
   * so its crest is matched the same way every other side's is. */
  const COLLEGE_V123 = ["State", "Tech", "A&M", "University", "College", "Poly", "Union", "Institute"];
  function hashV123(t) {
    t = String(t || "");
    let h = 2166136261 >>> 0;
    for (let i = 0; i < t.length; i++) {
      h ^= t.charCodeAt(i);
      h = Math.imul(h, 16777619) >>> 0;
    }
    return h >>> 0;
  }
  function dflClubV123(i) {
    const t = TOWNS[(i * 7 + 3) % TOWNS.length],
      m = MASCOTS[(i * 11 + 5) % MASCOTS.length];
    return t + " " + m;
  }
  const DFL_V123 = Array.from({ length: 50 }, (_, i) => dflClubV123(i));
  function newTeamIdentity() {
    return { town: randPick(TOWNS), mascot: randPick(MASCOTS), dfl: DFL_V123[(Math.random() * DFL_V123.length) | 0] };
  }
  function teamName(e) {
    e.teamIdentity || (e.teamIdentity = newTeamIdentity());
    const I = e.teamIdentity,
      t = e.level;
    if (t >= 7) return I.dfl || (I.dfl = DFL_V123[hashV123(I.town + I.mascot) % DFL_V123.length]); // the pro club he signed with
    if (t >= 5) return I.town + " " + (I.college || (I.college = COLLEGE_V123[hashV123(I.town) % COLLEGE_V123.length]));
    return I.town + " " + I.mascot;
  }
  window.__NAMES_V123 = {
    towns: () => TOWNS.slice(),
    mascots: () => MASCOTS.slice(),
    colleges: () => COLLEGE_V123.slice(),
    dfl: () => DFL_V123.slice(),
    nameFor: (lvl, town, mascot) =>
      lvl >= 7
        ? dflClubV123(hashV123(town + mascot) % 50)
        : lvl >= 5
          ? town + " " + COLLEGE_V123[hashV123(town) % COLLEGE_V123.length]
          : town + " " + mascot
  };
  function ovrGradeClass(e) {
    return e >= 200 ? "g-elite" : e >= 140 ? "g-hi" : e >= 80 ? "g-mid" : "g-lo";
  }
  let state = null;
  function freshState() {
    return {
      prestige: 0,
      pp: 0,
      tree: {},
      bestLevel: 0,
      careers: 0,
      nflReached: 0,
      objectivesCompleted: 0,
      lastCareerAttrs: null,
      achievements: {},
      milestones: {},
      respecUsed: 0,
      path: null,
      pathResets: 0,
      rerollV112: null,
      settings: { skipOpp: !1, onlyInvolved: !1, fastSim: !1, sound: !0, haptics: !0 },
      tutorialSeen: !1,
      highScore: 0,
      highStreak: 0,
      player: null,
      view: "menu"
    };
  }
  function settingOn(e) {
    return (state.settings && state.settings[e]) || !1;
  }
  function toggleSetting(e) {
    (state.settings || (state.settings = {}),
      (state.settings[e] = !state.settings[e]),
      saveGame(),
      state.view === "settings" && screenSettings());
  }
  const MILESTONE_PP = [0, 2, 4, 8, 14, 24, 40, 70, 220];
  function grantMilestone(e) {
    state.milestones || (state.milestones = {});
    const t = LEVELS[e].key;
    if (state.milestones[t]) return 0;
    state.milestones[t] = !0;
    const a = Math.round((MILESTONE_PP[e] || 0) * (1 + treeFx("mileMult")) * chaosEarnedMult(e));
    return (a > 0 && bankPPV136(a, "milestone"), a);
  } /* ===== v134 THE GOALS ARE WORTH CHASING =====
   * The challenge board paid 6 to 150 PP for feats that take whole careers: a UFF title was 10 PP, the
   * INTERSTELLAR championship 150. The prestige tree's big nodes cost 40-300 a level. So the board
   * that says "prove it" paid pocket change for the two hardest things in the game, and nothing on it
   * distinguished winning the title from being the man who won it. The scale is real now -- tens for
   * a season feat, hundreds for a career one, thousands for the pros -- and the two feats the whole
   * game points at have their own rows: the UFF title as its MVP is 10,000 PP, the Interstellar title
   * as its MVP is 1,000,000. A challenge still pays once, ever, through the same chalMult / chaos
   * multiplier it always did (`es()`). */
  const CHALLENGES = [
    {
      id: "ring",
      icon: "💍",
      name: "Ring Season",
      pp: 25,
      desc: "Win any championship",
      check: (e, t) => t && t.playoffs && t.playoffs.champion
    },
    {
      id: "perfect",
      icon: "💯",
      name: "Perfect Season",
      pp: 60,
      desc: "Go undefeated AND win the title",
      check: (e, t) => t && t.playoffs && t.playoffs.champion && t.teamLosses === 0
    },
    {
      id: "mvp",
      icon: "🏆",
      name: "Most Valuable",
      pp: 40,
      desc: "Win an MVP / Player of the Year award",
      check: (e, t) =>
        t && t.awards && t.awards.some(a => a.name.includes("MVP") || a.name.includes("Player of the Year"))
    },
    {
      id: "wreck",
      icon: "🔥",
      name: "Wrecking Ball",
      pp: 30,
      desc: "Post 5 dominant games (92+) in one season",
      check: (e, t) => t && t.dominantGames >= 5
    },
    {
      id: "ironman",
      icon: "🩹",
      name: "Indestructible",
      pp: 15,
      desc: "A healthy 10+ game season, no injuries",
      check: (e, t) => t && t.injuries === 0 && t.sampleGames && t.sampleGames.length >= 10
    },
    {
      id: "natlNo1",
      icon: "👑",
      name: "Statistical Champion",
      pp: 100,
      desc: "Finish #1 in the nation in your primary stat",
      check: null
    },
    {
      id: "hometown",
      icon: "🏘️",
      name: "Hometown Hero",
      pp: 30,
      desc: "Win a title before high school (level ≤ Middle School)",
      check: (e, t) => t && t.playoffs && t.playoffs.champion && e.player && e.player.level <= 2
    },
    {
      id: "grinder5",
      icon: "⚙️",
      name: "Journeyman",
      pp: 80,
      desc: "Complete 5 careers",
      check: e => (e.careersCompleted || 0) >= 5
    },
    {
      id: "dynasty3",
      icon: "♛",
      name: "Dynasty",
      pp: 200,
      desc: "Win 3 titles in a single career",
      check: e => e.player && (e.player.titles || 0) >= 3
    },
    {
      id: "superstar",
      icon: "⭐",
      name: "Superstar",
      pp: 250,
      desc: "Hit 90 OVR",
      check: e => (e.recordOvr || 0) >= 90
    },
    {
      id: "madeit",
      icon: "🌟",
      name: "One in a Million",
      pp: 400,
      desc: "Reach the UFF",
      check: e => (e.nflReached || 0) > 0
    },
    {
      id: "clearance5",
      icon: "⛓️",
      name: "Chain Breaker",
      pp: 500,
      desc: "Raise chaos capacity 5 times (cap 30+)",
      check: e => (e.chaosCap || 0) >= 30
    },
    {
      id: "legend",
      icon: "🐐",
      name: "Living Legend",
      pp: 600,
      desc: "Hit 95 OVR",
      check: e => (e.recordOvr || 0) >= 95
    },
    {
      id: "ovr100",
      icon: "💫",
      name: "Beyond the Wall",
      pp: 1500,
      desc: "Break the 99 OVR barrier (100+)",
      check: e => (e.recordOvr || 0) >= 100
    },
    {
      id: "starcall",
      icon: "🛸",
      name: "The Star Call",
      pp: 2500,
      desc: "Ascend to the Interstellar League",
      check: e => e.player && e.player.level >= 8
    },
    {
      id: "dflMvpTitle",
      icon: "🏆",
      name: "The Man Who Won It",
      pp: 10000,
      desc: "Win the UFF CHAMPIONSHIP as the League MVP — the title, and the award, in the same season",
      check: (e, t) =>
        t &&
        t.playoffs &&
        t.playoffs.champion &&
        e.player &&
        e.player.level === 7 &&
        t.awards &&
        t.awards.some(a => /MVP|Player of the Year/.test(a.name))
    },
    {
      id: "galaxyring",
      icon: "🌌",
      name: "Galactic Champion",
      pp: 20000,
      desc: "Win the INTERSTELLAR CHAMPIONSHIP",
      check: (e, t) => t && t.playoffs && t.playoffs.champion && e.player && e.player.level >= 8
    },
    {
      id: "galaxyMvpTitle",
      icon: "👽",
      name: "Best in the Galaxy",
      pp: 1e6,
      desc: "Win the INTERSTELLAR CHAMPIONSHIP as its MVP — the last thing there is to do",
      check: (e, t) =>
        t &&
        t.playoffs &&
        t.playoffs.champion &&
        e.player &&
        e.player.level >= 8 &&
        t.awards &&
        t.awards.some(a => /MVP|Player of the Year/.test(a.name))
    }
  ]; /* ===== v136 C THE ESTATE IS SETTLED AT THE END =====
   * Prestige Points used to land the moment they were earned — a goal on Tuesday paid on Tuesday,
   * a title paid the week it was won, a scrapped item paid at the locker — so a career was spending
   * the family's money while it was still being played. PP is only RECOVERED at the end of a career
   * now: every mid-career credit goes through `bankPPV136`, which banks it on the account
   * (`o.ppBankV136`, with a small log) while a player is alive and unsettled, and pays it straight
   * through when there is no one on the field. The two settles (`ms` for a cut career, `no` for a
   * UFF one) call `flushBankV136` first, so the bank lands in the same moment as the career's own
   * payout, and the career-end card says how much of the total was banked. Spending is untouched —
   * the tree, an event's cost and a respec's refund are the account's business, not the player's.
   * `TU("ppBankV136",0)` restores pay-as-you-go. `window.__V136_C`. */
  function bankingV136() {
    const e = typeof state < "u" && state && state.player;
    return !!(e && !e._settled && TU("ppBankV136", 1));
  }
  function bankPPV136(n, why) {
    n = Math.round(n || 0);
    if (!(n > 0)) return 0;
    if (!bankingV136()) {
      state.pp += n;
      return n;
    }
    state.ppBankV136 = (state.ppBankV136 || 0) + n;
    state.ppBankLogV136 = (state.ppBankLogV136 || []).concat({ n, why: why || "", at: Date.now() }).slice(-40);
    return n;
  }
  function bankedV136() {
    return typeof state < "u" && state ? state.ppBankV136 || 0 : 0;
  }
  function flushBankV136() {
    const b = bankedV136();
    state.ppBankV136 = 0;
    state.ppBankLogV136 = [];
    if (b > 0) state.pp += b;
    return b;
  }
  window.__V136_C = { bank: bankPPV136, banked: bankedV136, flush: flushBankV136, banking: bankingV136 };
  function completeChallenges(e) {
    state.challenges || (state.challenges = {});
    let t = 0;
    const a = [];
    if (
      (CHALLENGES.forEach(s => {
        if (state.challenges[s.id] || !s.check) return;
        let n = !1;
        try {
          n = s.check(state, e || (state.player && state.player.seasonStats));
        } catch {}
        if (n) {
          const i = Math.round(s.pp * (1 + treeFx("chalMult")) * chaosPPMult());
          ((state.challenges[s.id] = !0), bankPPV136(i, "goal"), (t += i), a.push(s.icon + " " + s.name));
        }
      }),
      !state.challenges.natlNo1 && state.player && state.player.lastSeasonLine)
    ) {
      const s = state.player,
        n = s.lastSeasonLine;
      if (n.level === s.level && POS_STATS[n.pos]) {
        const i = POS_STATS[n.pos],
          r = i.stats.find(l => l.key === i.primary);
        if (r) {
          const l = genNationalLeaders(s.level, n.pos, s.seasonSeed || 1),
            d = n.statLine[i.primary];
          if (r.lowerBetter ? l.every(u => d <= u.line[i.primary]) : l.every(u => d >= u.line[i.primary])) {
            state.challenges.natlNo1 = !0;
            const u = CHALLENGES.find(p => p.id === "natlNo1"),
              h = Math.round(u.pp * (1 + treeFx("chalMult")) * chaosPPMult());
            (bankPPV136(h, "goal"), (t += h), a.push(u.icon + " " + u.name));
          }
        }
      }
    }
    return (
      t > 0 &&
        typeof document < "u" &&
        byId("toast") &&
        showToast(
          "🎯 Challenge complete: " +
            a.join(", ") +
            " — +" +
            t +
            " PP" +
            (bankingV136() ? ", banked for the end of the career" : "") +
            "!"
        ),
      t
    );
  }
  function allStatDefs() {
    const e = {},
      t = [];
    return (
      Object.entries(POS_STATS).forEach(([a, s]) => {
        s.stats.forEach(n => {
          if (e[n.key]) {
            e[n.key].pos.push(a);
            return;
          }
          ((e[n.key] = { ...n, pos: [a] }), t.push(e[n.key]));
        });
      }),
      t
    );
  }
  function buyMastery(e) {
    if (!allStatDefs().find(n => n.key === e)) return;
    state.mastery || (state.mastery = {});
    const a = masteryLvl(e);
    if (a >= MASTERY_MAX) {
      showToast("Maxed out!");
      return;
    }
    const s = masteryCost(e);
    if ((state.rings || 0) < s) {
      showToast("Not enough Rings — win the UFF Championship! 💍");
      return;
    }
    ((state.rings -= s), (state.mastery[e] = a + 1), saveGame(), screenDynasty());
  }
  function setChaos(e, t) {
    if (!state.chaosUnlocked) {
      showToast("Chaos requires an UFF title, 85 OVR, and 20 completed objectives!");
      return;
    }
    state.chaos || (state.chaos = {});
    const a = state.chaos[e] || 0,
      s = clamp99(a + t, 0, 10);
    if (s !== a) {
      if (t > 0 && chaosTotal() + t > chaosCap()) {
        showToast("⛓️ Chaos capacity " + chaosCap() + " reached — win a championship at FULL chaos to raise it!");
        return;
      }
      ((state.chaos[e] = s),
        saveGame(),
        screenDynasty(),
        s > a && showToast("🔥 Chaos +1 — enemies stronger, PP +" + Math.round((chaosPPMult() - 1) * 100) + "%"));
    }
  }
  function chaosMaxAll() {
    if (!state.chaosUnlocked) return;
    return askV150(
      "Set chaos to your FULL capacity (" + chaosCap() + ")? Every enemy in the world becomes vastly stronger.",
      { title: "Chaos: max all", ok: "Max it", danger: !0 }
    ).then(ok => ok && chaosMaxAllNowV150());
  }
  function chaosMaxAllNowV150() {
    state.chaos = {};
    let e = chaosCap();
    (ATTR_KEYS.forEach(t => {
      const a = Math.min(10, e);
      (a > 0 && (state.chaos[t] = a), (e -= a));
    }),
      saveGame(),
      screenDynasty());
  }
  function screenDynasty() {
    const e = !!state.chaosUnlocked,
      t = chaosTotal(),
      a = chaosPPMult(),
      s = allStatDefs();
    ((byId("screen").innerHTML = `
    <div class="eyebrow" style="color:#ff5a5a">Dynasty · Endgame</div>
    <div class="h1">💍 Rings & Chaos</div>
    <div class="sub">Win UFF Championships to mint Rings. Chaos Mode unlocks after a title at <b>85+ OVR</b> with <b>20 completed season objectives</b>.</div>

    <div class="card" style="margin-top:14px;border-color:var(--gold)">
      <div style="display:flex;align-items:center;gap:14px">
        <div style="font-size:38px">💍</div>
        <div style="flex:1">
          <div style="font-family:'Oswald';font-weight:700;font-size:24px;color:var(--gold)">${state.rings || 0} <span style="font-size:13px;letter-spacing:2px">RINGS</span></div>
          <div class="small">+1 Ring per UFF Championship won. Spend below on Stat Mastery.</div>
        </div>
      </div>
    </div>

    <div class="card chaos-card ${e ? "" : "locked"}">
      <div class="chaos-head">
        <span class="chaos-flame">🔥</span>
        <div style="flex:1">
          <div style="font-family:'Oswald';font-weight:700;font-size:19px;letter-spacing:1px;color:#ff5a5a">CHAOS MODE ${e ? "" : "🔒"}</div>
          <div class="small">${e ? "Boost enemy performance <b>+10% per level, per stat</b>. Harder worlds pay exponentially." : "Locked — win the UFF Championship to open the gates."}</div>
        </div>
      </div>
      ${
        e
          ? `
      <div class="chaos-summary">
        <div class="cs-box"><div class="n">${t}<span style="font-size:12px;color:var(--chalk-dim)">/${chaosCap()}</span></div><div class="l">Chaos / Capacity</div></div>
        <div class="cs-box"><div class="n" style="color:var(--gold)">+${Math.round((a - 1) * 100)}%</div><div class="l">All PP Gains</div></div>
        <div class="cs-box"><div class="n" style="color:#57e07a">${attrCap()}</div><div class="l">Stat Cap</div></div>
      </div>
      <div class="threshold-note" style="margin-top:4px;color:#ff9b93">⛓️ <b>Chaos Clearance:</b> win a championship at <b>FULL capacity</b> to raise it (+6 UFF · +10 Interstellar). Depth must be earned — flaming out early under chaos pays only a fraction.</div>
      <div class="small" style="margin:6px 0 10px;color:var(--chalk-dim)">Every chaos level also raises your <b>potential ceiling</b> — the harder the world, the higher you can climb.</div>
      ${ATTR_KEYS.map(n => {
        const i = (state.chaos && state.chaos[n]) || 0;
        return `<div class="chaos-row">
          <span class="cr-name">${ATTR_INFO[n].name}</span>
          <span class="cr-fill"><i style="width:${i * 10}%"></i></span>
          <button class="step cstep" onclick="setChaos('${n}',-1)" ${i <= 0 ? "disabled" : ""}>−</button>
          <span class="cr-lvl ${i > 0 ? "hot" : ""}">${i}</span>
          <button class="step cstep hotbtn" onclick="setChaos('${n}',1)" ${i >= 10 ? "disabled" : ""}>+</button>
          <span class="cr-pct">+${i * 10}%</span>
        </div>`;
      }).join("")}
      <button class="btn danger" style="margin-top:10px" onclick="chaosMaxAll()">🔥 MAXIMUM CHAOS</button>
      `
          : ""
      }
    </div>

    <div class="h2" style="margin-top:16px">📊 Stat Mastery <span style="color:var(--chalk-dim);font-size:13px">— individual upgrades for every stat</span></div>
    <div class="sub" style="margin-bottom:10px">Each level: <b style="color:#57e07a">+8% production</b> of that stat (rate stats +4%, turnovers −6%). Paid in Rings.</div>
    ${s
      .map(n => {
        const i = masteryLvl(n.key),
          r = masteryCost(n.key),
          l = i >= MASTERY_MAX,
          d = n.lowerBetter ? `−${i * 6}%` : n.rate ? `+${i * 4}%` : `+${i * 8}%`;
        return `<div class="mastery-row">
        <div class="mr-info">
          <div class="mr-name">${n.name} <span class="mr-pos">${n.pos.join(" · ")}</span></div>
          <div class="mr-track">${Array.from({ length: MASTERY_MAX }, (c, u) => `<i class="${u < i ? "on" : ""}"></i>`).join("")}</div>
        </div>
        <div class="mr-eff ${i > 0 ? "hot" : ""}">${i > 0 ? d : "—"}</div>
        <button class="mr-buy ${l ? "maxed" : ""}" onclick="buyMastery('${n.key}')" ${l ? "disabled" : ""}>${l ? "MAX" : `💍 ${r}`}</button>
      </div>`;
      })
      .join("")}
  `),
      (byId("dock").innerHTML = `<button class="btn secondary" onclick="go('shop')">← Back to Prestige</button>`));
  }
  function screenHof() {
    const e = state.hof || [],
      t = state.era || 0,
      a = hofWings();
    ((byId("screen").innerHTML = `
    <div class="eyebrow">${eraName()} · Era ${t + 1}</div>
    <div class="h1">🏛️ Hall of Fame</div>
    <div class="sub">Every career you complete is enshrined forever. Museum wings open at 5 / 10 / 25 / 50 careers — each a permanent <b style="color:var(--gold)">+5% PP</b>.</div>

    <div class="card" style="margin-top:14px">
      <div class="statline">
        <div class="statbox"><div class="n" style="color:var(--gold)">${e.length ? e[0].goat : 0}</div><div class="l">Best GOAT Score</div></div>
        <div class="statbox"><div class="n">${state.careersCompleted || 0}</div><div class="l">Enshrined</div></div>
        <div class="statbox"><div class="n" style="color:#57e07a">+${a * 5}%</div><div class="l">Museum PP</div></div>
      </div>
      ${t > 0 ? `<div class="threshold-note" style="margin-top:8px;color:#c9b8ff">🌌 Era bonus: permanent <b>+${Math.round((eraMult() - 1) * 100)}% PP</b>. Next era: win the UFF title at <b>${nextEraChaos()}+ total chaos</b>.</div>` : '<div class="threshold-note" style="margin-top:8px">🌌 Win the UFF title to begin your first Era.</div>'}
    </div>

    ${lineageHofV136()}
    <div class="h2">🧩 Position Mastery</div>
    <div class="sub" style="margin-bottom:8px">Reach the UFF at a position: <b style="color:#57e07a">+1 all starting attrs</b> forever. Win a ring there: <b style="color:var(--gold)">+3% PP</b> forever.</div>
    <div class="card tight">
      <div class="pm-grid">
        ${Object.keys(POSITIONS)
          .map(s => {
            const n = (state.posMastery && state.posMastery[s]) || {};
            return `<div class="pm-cell ${n.ring ? "ringed" : n.nfl ? "reached" : ""}">
            <div class="pm-pos">${s}</div>
            <div class="pm-marks">${n.nfl ? "🏈" : "·"}${n.ring ? "💍" : ""}</div>
          </div>`;
          })
          .join("")}
      </div>
      <div class="small center" style="margin-top:7px">Mastered: <b>${posMasteryCount("nfl")}/9</b> UFF · <b>${posMasteryCount("ring")}/9</b> ringed</div>
    </div>

    <div class="h2">🗿 The Busts <span style="color:var(--chalk-dim);font-size:12px;font-weight:400">— tap one for the whole career</span></div>
    ${
      e.length
        ? e
            .slice(0, 25)
            .map(
              (s, n) => `
      <div class="hof-row ${n === 0 ? "top" : ""} hof-tap-v134" onclick="hofOpenV134(${n})" role="button" tabindex="0">
        <span class="hof-rank">${n === 0 ? "👑" : "#" + (n + 1)}</span>
        <span class="hof-bust">🗿</span>
        <div class="hof-info">
          <div class="hof-name">${escHtml(s.name)} <small>${s.pos}</small></div>
          <div class="hof-line">${LEVELS[s.reached].name}${s.won ? " 🏆" : ""} · ${s.peak} OVR${s.power > s.peak ? ' <b style="color:#c9b8ff">' + s.power + " PWR</b>" : ""} · ${s.titles}💍 · ${s.seasons} szn${s.box && s.box.totals ? ` · <span style="color:#8ec3ee">${s.box.totals.games} gp</span>` : ""}</div>
        </div>
        <span class="hof-goat">${s.goat}<small>GOAT</small></span>
      </div>
      <div class="hof-box-v134" id="hofBox${n}" hidden>${hofCardV134(s, n)}</div>`
            )
            .join("")
        : '<div class="card tight"><div class="small center">Complete a career to enshrine your first legend.</div></div>'
    }
  `),
      (byId("dock").innerHTML = `
    <div class="btn-row">
      <button class="btn ghost" onclick="go('locker')">🎒 Locker</button>
      <button class="btn secondary" onclick="go('menu')">Back</button>
    </div>`));
  } /* one bust's whole career: totals, the sheet he ended on, every season's line, the hardware */
  function hofCardV134(s, n) {
    const b = s.box;
    if (!b)
      return `<div class="card tight" style="margin:-2px 0 10px;border-color:#3a4a5f"><div class="small">No box score was kept for this career — he was enshrined before the Hall started keeping them. Every career from here on carries its whole record.</div></div>`;
    const T = b.totals || {},
      cfg = POS_STATS[s.pos] || { stats: [] },
      prim = cfg.primary;
    const stat = (k, v) => {
      const d = cfg.stats.find(x => x.key === k);
      return d && d.rate ? (+v).toFixed(1) : Math.round(v);
    };
    const lines = (T.lines || [])
      .map(
        l =>
          `<div class="hof-tot-v134${l.key === prim ? " prim" : ""}"><b>${l.rate ? (+l.total).toFixed(1) : Math.round(l.total).toLocaleString("en-US")}</b><small>${escHtml(l.name)}${l.rate ? "" : ` · ${l.per}/g`}</small></div>`
      )
      .join("");
    const rows = (b.log || [])
      .slice()
      .reverse()
      .map(
        r => `<div class="hof-szn-v134${r.champion ? " ring" : ""}">
      <span class="hs-lv">${escHtml(String(r.levelName || "").replace(/ League| School/, ""))}<small>age ${r.age}</small></span>
      <span class="hs-rec">${r.wins}-${r.losses}${r.champion ? " 💍" : r.roundsWon ? " 🎟️" : ""}</span>
      <span class="hs-line">${cfg.stats
        .slice(0, 3)
        .map(
          st =>
            `<i>${r.line && r.line[st.key] != null ? stat(st.key, r.line[st.key]) : "—"} <em>${escHtml(st.name)}</em></i>`
        )
        .join("")}</span>
      <span class="hs-grade" style="color:${/^A/.test(r.grade || "") ? "var(--good)" : /^[DF]/.test(r.grade || "") ? "var(--blood)" : "var(--gold)"}">${escHtml(r.grade || "")}<small>${r.avg} rtg · ${r.ovr} OVR</small></span>
      ${r.awards && r.awards.length ? `<span class="hs-aw">${r.awards.map(a => escHtml(a)).join(" · ")}</span>` : ""}
    </div>`
      )
      .join("");
    return `<div class="card tight hof-card-v134">
    <div class="eyebrow">${escHtml(b.team || "")}${b.origin ? ` · ${b.origin.icon} ${escHtml(b.origin.name)}` : ""} · ${"★".repeat(clamp99(b.stars || 1, 1, 5))}</div>
    <div class="hof-tots-v134">
      <div class="hof-tot-v134"><b>${T.games || 0}</b><small>GAMES</small></div>
      <div class="hof-tot-v134"><b>${T.wins || 0}-${T.losses || 0}</b><small>RECORD</small></div>
      <div class="hof-tot-v134"><b>${T.titles || 0}</b><small>TITLES</small></div>
      <div class="hof-tot-v134"><b>${T.awards || 0}</b><small>AWARDS</small></div>
      <div class="hof-tot-v134"><b>${T.peakOvr || s.peak}</b><small>PEAK OVR</small></div>
      <div class="hof-tot-v134"><b>${T.bestAvg || 0}</b><small>BEST YEAR</small></div>
    </div>
    ${lines ? `<div class="hof-kick-v134">CAREER ${escHtml(cfg.label || "").toUpperCase()}</div><div class="hof-tots-v134">${lines}</div>` : ""}
    ${b.attrs && b.attrs.length ? `<div class="hof-kick-v134">THE SHEET HE ENDED ON · ${b.endOvr} OVR</div><div class="hof-attrs-v134">${b.attrs.map(a => `<span>${a.icon} ${escHtml(a.name)} <b>${a.v}</b></span>`).join("")}</div>` : ""}
    ${b.awards && b.awards.length ? `<div class="hof-kick-v134">HARDWARE</div><div class="hof-attrs-v134">${b.awards.map(a => `<span>${escHtml(a.n)}${a.c > 1 ? ` <b>×${a.c}</b>` : ""}</span>`).join("")}</div>` : ""}
    ${rows ? `<div class="hof-kick-v134">EVERY SEASON</div><div class="hof-log-v134">${rows}</div>` : ""}
    ${b.traits && b.traits.length ? `<div class="small" style="margin-top:6px;color:var(--chalk-dim)">${b.traits.map(escHtml).join(" · ")}</div>` : ""}
  </div>`;
  }
  window.hofOpenV134 = function (n) {
    const el = document.getElementById("hofBox" + n);
    if (!el) return;
    const open = !el.hidden;
    document.querySelectorAll(".hof-box-v134").forEach(x => (x.hidden = !0));
    document.querySelectorAll(".hof-tap-v134").forEach(x => x.classList.remove("open"));
    if (!open) {
      el.hidden = !1;
      const row = el.previousElementSibling;
      row && row.classList.add("open");
      try {
        el.scrollIntoView({ block: "nearest", behavior: "smooth" });
      } catch (_) {}
    }
  };
  window.__HOF_V134 = {
    snap: e => hofSnapV134(e || (state && state.player), (state && state.player && state.player.level) | 0),
    card: hofCardV134,
    open: n => window.hofOpenV134(n),
    busts: () => (state && state.hof) || []
  };
  /* ===== v147 C THE GEAR HAS A ROLL =====
   * A gear piece used to be ONE number: a slot, a rarity and a single `eff`/`val` (seven kinds, two
   * of them — Conditioning's perfFlat and nothing else — read by nobody). Now every piece also ROLLS
   * up to three modifiers by rarity (Common 0–1, Rare 1, Epic 2, Legendary 3, Mythic 3 and bigger),
   * once, when it drops, off a PRNG seeded by its own id, and the roll is stored on the item
   * (`mods: [{k, v}]`, `modsV147`, `tierV147` = the level the career ended at). A piece saved before
   * v147 has no roll; `gearEnsureV147` gives it one the first time anything reads it — seeded by the
   * same id, so the equipped copy and the inventory copy (they are separate objects after a reload)
   * roll the SAME modifiers, and the roll never moves again.
   *
   * Fifty modifiers, and every one is a hook into code the game already reads (`gearCatV147` names
   * the hook beside each group): seventeen ATTRIBUTES (a flat bonus added to the effective value in
   * the live sim's accessor `_raw` AND in `effAttrsV85`, after `condMultV54` and the focus — so the
   * silent week, the watched game, the projection and the pregame sheet all see the same number),
   * seventeen PRODUCTION lines (a % on that stat of the season line `is()` builds — the same place
   * `statProd` scales it, so the week box scores, i.e. the credited plays, are never touched), and
   * sixteen more: injury chance (`injChanceV54`), injury time (`healWeeksV146`), fatigue gain
   * (`chargeV111`/`forecastV111` through `wearFatKV147`), weekly recovery (the v11 week), coach trust
   * gain (the v10 `et` wrapper), snap share (`trustShareV120`), game-to-game swing (`et`'s variance),
   * program fate odds (`planFateOddsV124`), PP (`ms`/`no`), soft caps (`drSoftCap`), upgrade points
   * (`tt`), training effect (`tt`'s Tt and its copy in `projectSeasonGainsV85`), age decline
   * (`ageCutV139`), clutch (`clutchV131`), call-up chance (`qt`) and team quality (the v76 margin).
   * Totals are capped per modifier (`gearAttrCapV147`, `gearProdCapV147`, `gearCapV147_<key>`), so
   * three Mythics cannot break a soft cap or the UFF. `TU("v147C", 0)` switches every modifier off.
   * `window.__V147C`; `v147Ccheck.mjs`. */
  var _gcV147 = null,
    _gtV147 = null,
    _gearSelV147 = null;
  function gearRsV147(r) {
    return { common: 1, rare: 1.3, epic: 1.65, legendary: 2.1, mythic: 2.7 }[r] || 1;
  } /* rarity → value scale; a function so a boot-time render can never outrun it (v140) */
  function gearCatV147() {
    if (_gcV147) return _gcV147;
    /* hook: _raw (live sim + silent week + projection) and effAttrsV85 (the sheets) */
    const A = k => ({
      k: "a_" + k,
      kind: "attr",
      stat: k,
      name: (ATTR_INFO[k] && ATTR_INFO[k].name) || k,
      base: 3,
      capK: "gearAttrCapV147",
      capD: 20,
      sign: 1,
      int: 1,
      hook: "_raw · effAttrsV85"
    });
    /* hook: is() — the season line, where statProd already scales it */
    const P = (k, n, low) => ({
      k: "p_" + k + (low ? "L" : ""),
      kind: "prod",
      stat: k,
      low: !!low,
      name: n,
      base: 0.03,
      capK: "gearProdCapV147",
      capD: 0.25,
      sign: low ? -1 : 1,
      pct: 1,
      hook: "is() season line"
    });
    const X = (k, n, base, capD, sign, pct, hook, int) => ({
      k,
      kind: "misc",
      name: n,
      base,
      capK: "gearCapV147_" + k,
      capD,
      sign,
      pct,
      int,
      hook
    });
    return (_gcV147 = ATTR_KEYS.map(A).concat(
      [
        P("passYds", "Pass Yards"),
        P("passTD", "Pass TDs"),
        P("int", "INTs Thrown", 1),
        P("rushYds", "Rush Yards"),
        P("rushTD", "Rush TDs"),
        P("ypc", "Yards / Carry"),
        P("rec", "Catches"),
        P("recYds", "Rec Yards"),
        P("recTD", "Rec TDs"),
        P("grade", "Block Grade"),
        P("pancakes", "Pancakes"),
        P("sacksAllowed", "Sacks Allowed", 1),
        P("tackles", "Tackles"),
        P("sacks", "Sacks"),
        P("tfl", "Tackles for Loss"),
        P("pd", "Passes Defended"),
        P("int", "Interceptions")
      ],
      [
        X("injChance", "Injury Chance", 0.04, 0.3, -1, 1, "injChanceV54"),
        X("injDur", "Injury Time Out", 0.05, 0.35, -1, 1, "healWeeksV146"),
        X("fatigueGain", "Fatigue Gain", 0.04, 0.3, -1, 1, "chargeV111 · forecastV111"),
        X("recovery", "Weekly Recovery", 0.06, 0.4, 1, 1, "v11 week recovery"),
        X("trustGain", "Coach Trust Gain", 0.06, 0.4, 1, 1, "et (v10 wrapper)"),
        X("snapShare", "Snap Share", 0.02, 0.1, 1, 1, "trustShareV120"),
        X("varDown", "Game-to-Game Swing", 0.03, 0.2, -1, 1, "et variance"),
        X("fateOdds", "Program Fate Odds", 0.012, 0.08, 1, 1, "planFateOddsV124"),
        X("ppGain", "Prestige Points", 0.04, 0.3, 1, 1, "ms / no (career-end PP)"),
        X("softCap", "Soft Caps", 0.006, 0.04, 1, 1, "drSoftCap"),
        X("points", "Upgrade Pts / Season", 1, 4, 1, 0, "tt points", 1),
        X("training", "Training Effect", 0.04, 0.25, 1, 1, "tt Tt · projectSeasonGainsV85"),
        X("ageDecline", "Age Decline", 0.05, 0.3, -1, 1, "ageCutV139"),
        X("clutch", "Playoff Rating", 1.2, 8, 1, 0, "clutchV131"),
        X("callUp", "Call-Up Chance", 0.6, 4, 1, 0, "qt (advanceChance)"),
        X("teamQual", "Team Quality", 0.012, 0.06, 1, 1, "v76 margin")
      ]
    ));
  }
  function gearDefV147(k) {
    return gearCatV147().find(c => c.k === k) || null;
  }
  function gearCapV147(c) {
    return TU(c.capK, c.capD);
  }
  function gearRngV147(id) {
    let h = 2166136261;
    const s = String(id);
    for (let i = 0; i < s.length; i++) {
      h ^= s.charCodeAt(i);
      h = Math.imul(h, 16777619);
    }
    let a = h >>> 0;
    return () => {
      a = (a + 1831565813) | 0;
      let t = Math.imul(a ^ (a >>> 15), 1 | a);
      t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
      return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
    };
  }
  function gearModCountV147(rar, r) {
    return rar === "common"
      ? r() < TU("gearCommonModOddsV147", 0.5)
        ? 1
        : 0
      : rar === "rare"
        ? 1
        : rar === "epic"
          ? 2
          : 3;
  }
  function gearRollV147(it, tier) {
    if (!it) return it;
    const r = gearRngV147(it.id),
      cat = gearCatV147(),
      n = gearModCountV147(it.rarity, r),
      rs = gearRsV147(it.rarity),
      tm = 1 + clamp99(tier | 0, 0, 8) * TU("gearTierStepV147", 0.1),
      mods = [],
      used = {};
    for (let i = 0; i < n; i++) {
      const kr = r(),
        kind = kr < 0.4 ? "attr" : kr < 0.7 ? "prod" : "misc",
        pool = cat.filter(c => c.kind === kind && !used[c.k]),
        c = pool[Math.floor(r() * pool.length) % pool.length];
      used[c.k] = 1;
      let v = c.base * rs * tm * (0.8 + 0.4 * r()) * TU("gearValueMulV147", 1);
      v = c.int ? Math.max(1, Math.round(v)) : +v.toFixed(c.pct ? 3 : 1);
      mods.push({ k: c.k, v });
    }
    it.mods = mods;
    it.tierV147 = tier | 0;
    it.modsV147 = 1;
    return it;
  }
  function gearEnsureV147(it) {
    if (it && !it.modsV147) gearRollV147(it, it.tierV147 != null ? it.tierV147 : 0);
    return it;
  }
  /* the equipped totals, capped per modifier; memoised on the three equipped objects */
  function gearTotalsV147() {
    const eq = state && state.equipped;
    if (!eq) return {};
    const c = _gtV147;
    if (c && c.eq === eq && c.a === eq.cleats && c.b === eq.gloves && c.c === eq.chain) return c.t;
    const raw = {},
      t = {};
    GEAR_SLOTS.forEach(s => {
      const it = eq[s.key];
      if (!it) return;
      gearEnsureV147(it);
      (it.mods || []).forEach(m => (raw[m.k] = (raw[m.k] || 0) + (+m.v || 0)));
    });
    gearCatV147().forEach(d => {
      if (raw[d.k]) t[d.k] = Math.min(raw[d.k], gearCapV147(d));
    });
    _gtV147 = { eq, a: eq.cleats, b: eq.gloves, c: eq.chain, t, raw };
    return t;
  }
  function gearV147(k) {
    return TU("v147C", 1) ? gearTotalsV147()[k] || 0 : 0;
  }
  /* one attribute's flat gear bonus: its own modifier plus the old Conditioning piece (perfFlat,
   * "+N to ALL stats every game"), which nothing read until now */
  function gearAttrV147(k) {
    return gearV147("a_" + k) + (TU("gearPerfFlatV147", 1) ? Math.round(gearFx("perfFlat")) : 0);
  }
  /* a production modifier for one stat definition of Ne[pos].stats (lower-is-better lines are cut) */
  function gearProdV147(c) {
    return c ? gearV147("p_" + c.key + (c.lowerBetter ? "L" : "")) : 0;
  }
  function wearFatKV147() {
    return TU("wearFatK", 0.5) * (1 - gearV147("fatigueGain"));
  }
  function gearTxtV147(k, v) {
    const c = gearDefV147(k);
    if (!c) return "";
    const sg = c.sign < 0 ? "−" : "+",
      n = Math.abs(+v || 0);
    return (
      sg +
      (c.pct
        ? (Math.round(n * 1000) / 10).toString().replace(/\.0$/, "") + "%"
        : c.int
          ? Math.round(n)
          : Math.round(n * 10) / 10) +
      " " +
      c.name
    );
  }
  /* does this modifier do anything for the man in the career right now (a QB's pass lines, not a DL's) */
  function gearLiveV147(k, pos) {
    const c = gearDefV147(k);
    if (!c || c.kind !== "prod" || !pos || !POS_STATS[pos]) return !0;
    return POS_STATS[pos].stats.some(s => s.key === c.stat && !!s.lowerBetter === c.low);
  }
  /* every effect a piece carries, keyed, as goodness magnitudes (bigger is always better) */
  function gearFxV147(it) {
    const out = {};
    if (!it) return out;
    gearEnsureV147(it);
    const b = GEAR_EFFECTS.find(n => n.key === it.eff);
    b && (out["b_" + it.eff] = { v: +it.val || 0, t: b.fmt(it.val), base: 1 });
    (it.mods || []).forEach(m => {
      out[m.k] = { v: +m.v || 0, t: gearTxtV147(m.k, m.v) };
    });
    return out;
  }
  function gearModsHtmlV147(it, pos) {
    gearEnsureV147(it);
    const m = it.mods || [];
    if (!m.length) return `<div class="gm-none-v147">no modifiers</div>`;
    return `<div class="gm-list-v147">${m.map(x => `<span class="gm-v147${gearLiveV147(x.k, pos) ? "" : " off"}">${escHtml(gearTxtV147(x.k, x.v))}</span>`).join("")}</div>`;
  }
  function gearSumV147(pos) {
    const eq = state.equipped || {},
      parts = [];
    GEAR_EFFECTS.forEach(b => {
      const v = gearFx(b.key);
      v && parts.push(`<span class="gm-v147 base">${escHtml(b.fmt(v))}</span>`);
    });
    const t = gearTotalsV147(),
      raw = (_gtV147 && _gtV147.raw) || {};
    gearCatV147().forEach(c => {
      const v = t[c.k];
      if (!v) return;
      const cap = raw[c.k] > v + 1e-9;
      parts.push(
        `<span class="gm-v147${gearLiveV147(c.k, pos) ? "" : " off"}${cap ? " cap" : ""}" title="${escHtml(c.hook)}">${escHtml(gearTxtV147(c.k, v))}${cap ? " · CAP" : ""}</span>`
      );
    });
    return `<div class="gear-sum-v147"><div class="gs-h-v147">TOTAL GEAR BONUSES${TU("v147C", 1) ? "" : " · OFF"}</div>${parts.length ? `<div class="gm-list-v147">${parts.join("")}</div>` : `<div class="gm-none-v147">Nothing equipped — tap a piece below to compare it, then EQUIP.</div>`}</div>`;
  }
  /* the compare view: the picked piece against whatever is in its slot now */
  function gearCompareV147(it, pos) {
    const eq = state.equipped || {},
      cur = eq[it.slot],
      same = cur && cur.id === it.id,
      A = gearFxV147(it),
      B = same ? {} : gearFxV147(cur),
      keys = [...new Set(Object.keys(A).concat(Object.keys(B)))],
      r = RARITIES.find(i => i.key === it.rarity);
    const rows = keys
      .map(k => {
        const a = A[k],
          b = B[k],
          d = (a ? a.v : 0) - (b ? b.v : 0),
          cls = same ? "" : d > 1e-9 ? "up" : d < -1e-9 ? "dn" : "eq";
        return `<div class="gc-row-v147 ${cls}${a && !a.base && !gearLiveV147(k, pos) ? " off" : ""}"><span>${escHtml((a || b).t)}</span><b>${same ? "" : d > 1e-9 ? "▲ new" : d < -1e-9 ? (a ? "▼ less" : "✕ lost") : "="}</b></div>`;
      })
      .join("");
    return `<div class="gear-cmp-v147"><div class="gc-h-v147">${same ? "EQUIPPED" : cur ? `vs <span style="color:${(RARITIES.find(i => i.key === cur.rarity) || {}).color}">${escHtml(cur.name)}</span>` : "slot is empty"}</div>${rows || '<div class="gm-none-v147">no effects</div>'}
    <div class="gc-btns-v147">${same ? `<button class="mr-buy" onclick="unequipGearV147('${it.slot}')">UNEQUIP</button>` : `<button class="mr-buy" onclick="equipGear('${it.id}')">EQUIP</button><button class="gr-scrap" onclick="scrapGear('${it.id}')">♻️ SCRAP</button>`}</div></div>`;
  }
  function gearPickV147(id) {
    _gearSelV147 = _gearSelV147 === id ? null : id;
    screenLocker();
  }
  /* a redraw keeps the list where it was, and keeps the picked piece in view inside the list's own
   * scroller — never scrollIntoView, which moves the page (v146 E). Run again a beat later, because
   * the shell's fit() sizes the list after the redraw lands. */
  function gearKeepV147(st) {
    const run = () => {
      try {
        const L = document.querySelector(".gear-list-v147");
        if (!L) return;
        st && (L.scrollTop = st);
        const r = L.querySelector(".gear-row.sel");
        if (!r) return;
        const top = r.getBoundingClientRect().top - L.getBoundingClientRect().top + L.scrollTop;
        if (top < L.scrollTop || top + r.offsetHeight > L.scrollTop + L.clientHeight)
          L.scrollTop = Math.max(0, Math.min(top - 6, top + r.offsetHeight - L.clientHeight + 6));
      } catch (_) {}
    };
    run();
    setTimeout(run, 90);
  }
  function unequipGearV147(slot) {
    if (state.equipped && state.equipped[slot]) {
      delete state.equipped[slot];
      saveGame();
    }
    screenLocker();
  }
  /* a save from before v147: every piece gets its roll once, keyed on its id */
  function gearMigrateV147() {
    if (!state) return 0;
    let n = 0;
    (state.inventory || []).forEach(it => {
      it && !it.modsV147 && (gearEnsureV147(it), n++);
    });
    const eq = state.equipped || {};
    GEAR_SLOTS.forEach(s => {
      const it = eq[s.key];
      it && !it.modsV147 && (gearEnsureV147(it), n++);
    });
    return n;
  }
  function screenLocker() {
    gearMigrateV147();
    const _gl = document.querySelector(".gear-list-v147"),
      _gst = _gl ? _gl.scrollTop : 0;
    const e = state.inventory || [],
      t = state.equipped || {},
      pos = state.player && state.player.pos,
      a = n => RARITIES.find(i => i.key === n) || RARITIES[0],
      s = n => {
        const b = GEAR_EFFECTS.find(i => i.key === n.eff);
        return b ? b.fmt(n.val) : "";
      };
    _gearSelV147 && !e.some(n => n.id === _gearSelV147) && (_gearSelV147 = null);
    ((byId("screen").innerHTML = `
    <div class="eyebrow">Drops at every career end · rarer = more modifiers</div>
    <div class="eq-row eq-row-v147">
      ${GEAR_SLOTS.map(n => {
        const i = t[n.key],
          r = i ? a(i.rarity) : null;
        return `<div class="eq-slot ${i ? "r-" + i.rarity : ""}"${i ? ` onclick="gearPickV147('${i.id}')"` : ""}>
          <div class="eq-ic">${n.icon}</div>
          ${i ? `<div class="eq-name" style="color:${r.color}">${escHtml(i.name)}</div><div class="eq-eff">${r.name} · ${(i.mods || []).length} mod${(i.mods || []).length === 1 ? "" : "s"}</div>` : `<div class="eq-name" style="color:var(--chalk-dim)">Empty ${n.name}</div>`}
        </div>`;
      }).join("")}
    </div>
    ${gearSumV147(pos)}
    <div class="h2 gear-h-v147">Inventory <span>(${e.length}/40) · tap to compare</span></div>
    <div class="gear-list-v147">
    ${
      e.length
        ? [...e]
            .sort((n, i) => rarityIndex(i) - rarityIndex(n))
            .map(n => {
              const i = a(n.rarity),
                r = t[n.slot] && t[n.slot].id === n.id,
                sel = _gearSelV147 === n.id;
              return `<div class="gear-row r-${n.rarity}${sel ? " sel" : ""}" data-gear="${escHtml(n.id)}">
        <div class="gr-main-v147">
        <span class="gr-ic">${n.icon}</span>
        <div class="gr-info" onclick="gearPickV147('${n.id}')">
          <div class="gr-name" style="color:${i.color}">${escHtml(n.name)} ${r ? '<span style="color:#57e07a">✓ EQUIPPED</span>' : ""}</div>
          <div class="gr-eff"><b style="color:${i.color}">${i.name}</b> · ${s(n)}</div>
          ${gearModsHtmlV147(n, pos)}
        </div>
        ${r ? "" : `<button class="mr-buy" onclick="equipGear('${n.id}')">EQUIP</button>`}
        </div>
        ${sel ? gearCompareV147(n, pos) : ""}
      </div>`;
            })
            .join("")
        : '<div class="card tight"><div class="small center">No gear yet — finish a career for your first drop.</div></div>'
    }
    </div>
  `),
      (byId("dock").innerHTML = `
    <div class="btn-row">
      <button class="btn ghost" onclick="go('hof')">🏛️ Hall of Fame</button>
      <button class="btn secondary" onclick="go(S.player&&S.player.pos?'hub':'menu')">Back</button>
    </div>`));
    gearKeepV147(_gst);
  }
  window.gearPickV147 = gearPickV147;
  window.unequipGearV147 = unequipGearV147;
  window.__V147C = {
    effAttrs: e => effAttrsV85(e),
    cat: () => gearCatV147(),
    roll: (it, tier) => gearRollV147(it, tier),
    ensure: gearEnsureV147,
    totals: () => gearTotalsV147(),
    get: k => gearV147(k),
    attr: k => gearAttrV147(k),
    migrate: () => gearMigrateV147(),
    fx: gearFxV147,
    txt: gearTxtV147,
    count: gearModCountV147,
    rng: gearRngV147,
    make: (minR, tier) => {
      const it = rollGear(minR);
      return tier != null ? gearRollV147(it, tier) : it;
    },
    /* the hooks, read through the real functions, for the check */
    seasonLine: (pos, perf, lvl) => prodStats(pos, perf, lvl),
    wearFatK: () => wearFatKV147(),
    heal: w => healWeeksV146(w),
    softCap: (e, k) => drSoftCap(e, k),
    fateOdds: k => planFateOddsV124(k),
    ageCut: e => ageCutV139(e),
    trustShare: e => trustShareV120(e),
    clutch: (e, op) => clutchV131(e, op || { playoff: !0 }),
    ppMul: () => gearV147("ppGain"),
    trustMul: () => 1 + gearV147("trustGain"),
    recovMul: () => 1 + gearV147("recovery"),
    trainMul: () => 1 + gearV147("training")
  };
  function rr() {
    state.challenges || (state.challenges = {});
    const e = CHALLENGES.filter(t => state.challenges[t.id]).length;
    ((byId("screen").innerHTML = `
    <div class="eyebrow">Career Challenges · ${e}/${CHALLENGES.length} complete</div>
    <div class="h1">Prove It</div>
    <div class="sub">One-time feats that pay permanent Prestige Points — banked, and paid the day the career ends. They stay done forever — chase them across careers. The two at the bottom are the whole game: the UFF title <b>as its MVP</b> pays <b style="color:var(--gold)">10,000</b>, the Interstellar title as its MVP pays <b style="color:var(--gold)">1,000,000</b>.</div>
    <div class="mt" style="margin-top:14px">
    ${CHALLENGES.map(t => {
      const a = !!state.challenges[t.id];
      return `<div class="shop-item" style="border-color:${a ? "var(--good)" : "var(--line)"};${a ? "" : "opacity:.85"}">
        <div class="ic">${t.icon}</div>
        <div class="si"><div class="st">${t.name} ${a ? '<span style="color:var(--good)">✓ DONE</span>' : ""}</div>
          <div class="sd">${t.desc}</div></div>
        <div style="font-family:'Oswald';font-weight:700;color:${a ? "var(--good)" : "var(--gold)"};white-space:nowrap">${a ? "+" : ""}${t.pp.toLocaleString("en-US")} PP</div>
      </div>`;
    }).join("")}
    </div>
  `),
      (byId("dock").innerHTML =
        `<button class="btn secondary" onclick="go(S.player&&S.player.pos?'hub':'menu')">Back</button>`));
  }
  function newPlayer() {
    const e = Math.min(2, nodeLvl("headstart")),
      t = {},
      a = nodeLvl("genetics") * 3 + nodeLvl("primeGenes") * 6,
      s = effectivePrestige(state.prestige) + pathVal("startPrestige", 0),
      n = 55 + nodeLvl("freak") * 8 + s * 0.5,
      i = nodeLvl("goat") * 2;
    (ATTR_KEYS.forEach(v => {
      t[v] = clamp99(Math.round(8 + randRange(-2, 4) + (s + i) * 0.55 + a), 1, n);
    }),
      (t.speed = clamp99(t.speed + nodeLvl("fastTwitch") * 6 + nodeLvl("campSpeed") * 4, 1, n)),
      (t.acceleration = clamp99((t.acceleration || 10) + nodeLvl("fastTwitch") * 4 + nodeLvl("explosive") * 6, 1, n)),
      (t.quickness = clamp99(t.quickness + nodeLvl("fastTwitch") * 6 + nodeLvl("explosive") * 8, 1, n)),
      (t.agility = clamp99(t.agility + nodeLvl("nimble") * 6 + nodeLvl("explosive") * 8, 1, n)),
      (t.strength = clamp99(t.strength + nodeLvl("frame") * 7 + nodeLvl("campStr") * 4, 1, n)),
      (t.injuryResist = clamp99(t.injuryResist + nodeLvl("iron") * 7, 1, n + 20)),
      (t.stamina = clamp99(t.stamina + nodeLvl("motor") * 4 + nodeLvl("lungs") * 7, 1, n + 15)),
      (t.awareness = clamp99(
        t.awareness + nodeLvl("filmrat") * 7 + nodeLvl("genius") * 14 + nodeLvl("campIQ") * 4,
        1,
        n
      )),
      (t.vision = clamp99((t.vision || 10) + nodeLvl("filmrat") * 4 + nodeLvl("genius") * 8, 1, n)),
      (t.grit = clamp99(t.grit + nodeLvl("clutch") * 7, 1, n + 15)),
      (t.catching = clamp99(t.catching + nodeLvl("handsy") * 7 + nodeLvl("campHands") * 4, 1, n)),
      (t.throwing = clamp99(t.throwing + nodeLvl("cannon") * 7 + nodeLvl("campQB") * 4, 1, n)),
      (t.tackling = clamp99(t.tackling + nodeLvl("wrap") * 7 + nodeLvl("campDef") * 4, 1, n)));
    const r = nodeLvl("allStarCamp") * 3 + nodeLvl("megaCamp") * 6;
    r && ATTR_KEYS.forEach(v => (t[v] = clamp99(t[v] + r, 1, n + 15)));
    const l = pathVal("startAttr", 0);
    l && ATTR_KEYS.forEach(v => (t[v] = clamp99(t[v] + l, 1, attrCap())));
    const d = nodeLvl("dynasty") * 0.1 + nodeLvl("inheritance") * 0.25;
    d &&
      state.lastCareerAttrs &&
      ATTR_KEYS.forEach(v => {
        t[v] = clamp99(Math.round(t[v] + (state.lastCareerAttrs[v] || 0) * d), 1, attrCap());
      });
    /* v134 Apex: Bloodline rides the same line as Dynasty */ const c =
      randRange(0.85, 1.25) + (effectivePrestige(state.prestige) + i) * 0.006 + nodeLvl("talent") * 0.06;
    let u = Pi({ height: nodeLvl("devHeight"), weight: nodeLvl("devWeight"), muscle: nodeLvl("devMuscle") });
    const h = ATTR_KEYS.reduce((v, j) => v + t[j], 0) / ATTR_KEYS.length;
    let p = clamp99(
      Math.round(h / 14) + nodeLvl("recruited") + nodeLvl("agent") + starsPlusV146() + (nodeLvl("phenom") > 0 ? 5 : 0),
      1,
      5
    );
    if ((pathVal("startStars", 0) && (p = Math.max(p, pathVal("startStars"))), nodeLvl("idealBody"))) {
      const v = suggestPositions(t, u)[0].pos,
        j = POS_BODY[v];
      ((u.height = j.h[0]), (u.weight = j.w[0]), (u.muscle = Math.min(90, u.muscle + 15)));
    }
    const _t112 = rollTraits()[0],
      _o112 = traitOfferV112([_t112]);
    const m = {
        name: randPick(FIRST_NAMES) + " " + randPick(LAST_NAMES),
        attrs: t,
        body: u,
        pos: null,
        level: e,
        age: LEVELS[e].age,
        potential: c,
        seasonsAtLevel: 0,
        points: 0,
        totalSeasons: 0,
        stars: p,
        seasonsSinceStart: 0,
        training: null,
        pendingEvent: null,
        eventLog: [],
        rivals: [],
        rivalScope: null,
        lastGains: {},
        career: [],
        seasonStats: null,
        awards: [],
        milestones: [],
        traits: [_t112],
        traitOfferV112: _o112,
        traitPickedV112: null,
        teamIdentity: newTeamIdentity(),
        titles: 0,
        declareBonus: 0,
        seasonSeed: randInt(1, 2e9)
      },
      y = treeFx("startAll");
    if (
      (ATTR_KEYS.forEach(v => {
        const j = y + treeFx("start_" + v);
        j > 0 && (m.attrs[v] = clamp99(m.attrs[v] + j, 1, attrCap()));
      }),
      treeFx("trait3") > 0)
    ) {
      const v = Object.keys(TRAITS).filter(j => TRAITS[j].good === 1 && !m.traits.includes(j));
      v.length && m.traits.push(randPick(v));
    }
    const C = Math.round(gearFx("startAll")),
      V = posMasteryCount("nfl");
    (C + V > 0 &&
      ATTR_KEYS.forEach(v => {
        m.attrs[v] = clamp99(m.attrs[v] + C + V, 1, attrCap());
      }),
      rollNemesis(m),
      (m.peakOvr = 0));
    const P = Object.values(state.tree || {}).reduce((v, j) => v + j, 0);
    return (
      (m.potentialCeil = Math.round(
        clamp99(
          30 +
            effectivePrestige(state.prestige) * 3.4 +
            P * 0.95 +
            nodeLvl("freak") * 12 +
            nodeLvl("superhuman") * 20 +
            nodeLvl("primeGenes") * 9 +
            (pathVal("growthMult", 1) - 1) * 36 +
            (state.path === "phenom" ? 30 : 0),
          30,
          attrCap()
        )
      )),
      nodeLvl("phenom") && ATTR_KEYS.forEach(v => (t[v] = clamp99(t[v] + nodeLvl("phenom") * 3, 1, n + 10))),
      m
    );
  }
  function genRivals(e) {
    const t = playerOvr(e),
      a = [],
      s = 5;
    for (let n = 0; n < s; n++) {
      const i = randInt(-8, 10);
      a.push({
        name: randPick(RIVAL_FIRST) + " " + randPick(RIVAL_LAST),
        pos: e.pos,
        ovr: clamp99(t + i, 5, 999),
        growth: randRange(0.9, 1.3),
        stars: clamp99(Math.round((t + i) / 16) + randInt(0, 1), 1, 5),
        alive: !0
      });
    }
    ((e.rivals = a), (e.rivalScope = e.level + "|" + e.pos));
  }
  function lr(e) {
    e.rivals.forEach(t => {
      if (!t.alive) return;
      const a = LEVELS[e.level];
      ((t.ovr = clamp99(Math.round(t.ovr + randRange(1, 4) * t.growth * (e.level <= 1 ? 1.5 : 1)), 5, 999)),
        t.ovr < a.need - 6 && Math.random() < 0.3 && (t.alive = !1));
    });
  } /* ===== v139 THE COMBINE IS DRILLS, NOT GAMES =====
   * The UFF Combine was four more football games against "COMBINE FIELD", scored like any other
   * week and summed into a season, which is not what a combine is and not what anyone is watching.
   * It is one year now (`Bt`), and what happens in it is the six drills every scout actually writes
   * down — measured off the attributes they measure, on the SAME curves the skills sheet already
   * prints, so the number here and the number there can never disagree. A time is reported as a
   * time, which is what a combine reports; `Le`'s own metric turns those into mph for the sheet.
   *
   * `elite` and `poor` are the ends of the real range for each drill, so the percentile is a
   * position among men rather than a rescaled attribute.
   *
   * The WEEKS are the drills too: `A[6].games` is six, one per drill, and the v11 schedule builder
   * names each week for the drill instead of drawing an opponent out of `Dt` — so the combine year
   * reads like a combine, and `week.combineV139` says which drill a week is. */
  const COMBINE_V139 = [
    {
      k: "forty",
      name: "40-Yard Dash",
      icon: "\u{1F4A8}",
      attr: "speed",
      unit: "s",
      low: 1,
      fmt: a => (5.55 - 1.35 * (1 - Math.exp(-a / 85))).toFixed(2),
      elite: 4.35,
      poor: 5.35
    },
    {
      k: "split",
      name: "10-Yard Split",
      icon: "\u{1F680}",
      attr: "acceleration",
      unit: "s",
      low: 1,
      fmt: a => (1.85 - 0.55 * (1 - Math.exp(-a / 85))).toFixed(2),
      elite: 1.45,
      poor: 1.82
    },
    {
      k: "bench",
      name: "Bench Press",
      icon: "\u{1F4AA}",
      attr: "strength",
      unit: "reps",
      low: 0,
      fmt: a => Math.round(40 * (1 - Math.exp(-a / 95))),
      elite: 32,
      poor: 8
    },
    {
      k: "vert",
      name: "Vertical Jump",
      icon: "\u{1F998}",
      attr: "jumping",
      unit: "in",
      low: 0,
      fmt: a => Math.round(20 + 24 * (1 - Math.exp(-a / 95))),
      elite: 40,
      poor: 24
    },
    {
      k: "shuttle",
      name: "20-Yard Shuttle",
      icon: "\u26A1",
      attr: "quickness",
      unit: "s",
      low: 1,
      fmt: a => (4.75 - 1.05 * (1 - Math.exp(-a / 85))).toFixed(2),
      elite: 3.95,
      poor: 4.65
    },
    {
      k: "cone",
      name: "3-Cone Drill",
      icon: "\u{1F300}",
      attr: "agility",
      unit: "s",
      low: 1,
      fmt: a => (7.9 - 1.6 * (1 - Math.exp(-a / 85))).toFixed(2),
      elite: 6.55,
      poor: 7.7
    }
  ];
  function combineYearV139(e) {
    return !!(e && e.level === 6);
  }
  function combineLeadersV139(e) {
    try {
      const R = combineResultV139(e);
      return `<div class="card">
      <div class="l" style="font-size:11px;color:var(--gold);letter-spacing:2px;margin-bottom:4px">\u{1F4CB} COMBINE RESULTS</div>
      <div class="small" style="margin-bottom:10px">There is no statistical leaderboard this year, because there are no games this year. A combine is a stopwatch and a tape measure \u2014 this is where your numbers put you against the field.</div>
      <div class="sched-list">
        ${R.rows
          .map(
            r => `<div class="sched-row" style="grid-template-columns:34px 1fr auto auto">
          <span class="sched-wk" style="font-size:16px">${r.icon}</span>
          <span class="sched-opp">${r.name}</span>
          <span class="sched-score" style="font-variant-numeric:tabular-nums">${r.value}${r.unit === "s" ? "s" : r.unit === "in" ? "\u2033" : " " + r.unit}</span>
          <span class="sched-perf" style="color:${r.pct >= 82 ? "var(--good)" : r.pct >= 40 ? "var(--gold)" : "var(--blood)"}">${r.pct}%ile</span>
        </div>`
          )
          .join("")}
      </div>
      <div class="threshold-note mt" style="margin-top:10px">Overall <b style="color:var(--gold)">${R.grade}</b> \u00b7 ${R.score}th percentile of the field. ${R.say}</div>
    </div>`;
    } catch (_) {
      return '<div class="card"><div class="small">No combine numbers yet.</div></div>';
    }
  }
  function combineBoardV139(e) {
    try {
      const R = combineResultV139(e);
      return `<div class="card tight" id="combineV139" style="margin-top:12px;border-color:rgba(240,187,69,.45)">
      <div class="l" style="font-size:11px;color:var(--gold);letter-spacing:2px;margin-bottom:2px">\u{1F4CB} YOUR TESTING NUMBERS</div>
      <div class="small" style="margin-bottom:9px">One week, six drills, and every scout in the league with a stopwatch. These are measured off what you can actually do \u2014 there are no games here to hide in.</div>
      <div class="sched-list">
        ${R.rows
          .map(
            r => `<div class="sched-row" style="grid-template-columns:34px 1fr auto auto">
          <span class="sched-wk" style="font-size:16px">${r.icon}</span>
          <span class="sched-opp">${r.name}</span>
          <span class="sched-score" style="font-variant-numeric:tabular-nums">${r.value}${r.unit === "s" ? "s" : r.unit === "in" ? "\u2033" : " " + r.unit}</span>
          <span class="sched-perf" style="color:${r.pct >= 82 ? "var(--good)" : r.pct >= 40 ? "var(--gold)" : "var(--blood)"}">${r.grade}</span>
        </div>`
          )
          .join("")}
      </div>
      <div class="threshold-note mt" style="margin-top:9px">Combine grade <b style="color:var(--gold)">${R.grade}</b> \u00b7 ${R.score}th percentile \u00b7 best: <b>${R.best.name}</b> \u00b7 worst: <b>${R.worst.name}</b></div>
      <div class="small" style="margin-top:4px;color:var(--chalk-dim)">${R.say}</div>
    </div>`;
    } catch (_) {
      return "";
    }
  }
  function combineDrillV139(e, d) {
    const a = (e && e.attrs && e.attrs[d.attr]) || 10,
      v = Number(d.fmt(a));
    const pct = clamp99(d.low ? (d.poor - v) / (d.poor - d.elite) : (v - d.poor) / (d.elite - d.poor), 0, 1);
    return {
      k: d.k,
      name: d.name,
      icon: d.icon,
      attr: d.attr,
      unit: d.unit,
      value: d.fmt(a),
      pct: +(pct * 100).toFixed(0),
      grade: pct >= 0.82 ? "ELITE" : pct >= 0.62 ? "STRONG" : pct >= 0.4 ? "SOLID" : pct >= 0.2 ? "BELOW" : "POOR"
    };
  }
  function combineResultV139(e) {
    const rows = COMBINE_V139.map(d => combineDrillV139(e, d));
    const mean = rows.reduce((s, r) => s + r.pct, 0) / (rows.length || 1);
    const best = rows.slice().sort((x, y) => y.pct - x.pct)[0],
      worst = rows.slice().sort((x, y) => x.pct - y.pct)[0];
    return {
      rows,
      score: +mean.toFixed(0),
      best,
      worst,
      grade:
        mean >= 82
          ? "A+"
          : mean >= 70
            ? "A"
            : mean >= 58
              ? "B+"
              : mean >= 46
                ? "B"
                : mean >= 32
                  ? "C"
                  : mean >= 18
                    ? "D"
                    : "F",
      say:
        mean >= 82
          ? "He tested like a first-rounder."
          : mean >= 70
            ? "Scouts left with his name underlined."
            : mean >= 58
              ? "A good week — nothing scared anybody off."
              : mean >= 46
                ? "He did not hurt himself."
                : mean >= 32
                  ? "The tape will have to carry him."
                  : "That was a hard week to watch."
    };
  }
  window.__COMBINE_V139 = {
    drills: COMBINE_V139,
    year: combineYearV139,
    one: combineDrillV139,
    all: combineResultV139
  };
  function maxSeasons() {
    const e = state.player;
    if (!e) return 5;
    const t = LEVELS[e.level].seasons,
      a = nodeLvl("extra") + nodeLvl("unstoppable") * 2 + pathVal("extraSeasons", 0);
    return e.level <= 2 ? t + a : e.level <= 4 ? t + 1 + a : e.level === 5 ? t + a : e.level === 6 ? t + a : t + 2 + a;
  }
  /* v139: the combine is ONE year — it is a draft week, not a season */ function minSeasons() {
    const e = state.player;
    if (!e) return 1;
    const t = LEVELS[e.level];
    return e.level === 5 ? Math.min(2, t.seasons) : e.level >= 7 ? 1 : Math.min(t.seasons, maxSeasons());
  }
  const minSeasV139 = minSeasons;
  minSeasons = function () {
    return Math.max(1, minSeasV139() - nodeLvl("earlyDeclare"));
  }; /* v139 Apex: Early Declaration */
  function calcOvr(e, t, a) {
    if (!t) return 0;
    const s = POSITIONS[t].w;
    let n = 0,
      i = 0;
    for (const d in s) ((n += (e[d] || 10) * s[d]), (i += s[d]));
    const r = n / i;
    let l = 99 * (1 - Math.exp(-r / 78));
    if ((r > 215 && (l = 99 + Math.pow(r - 215, 0.93) * 0.83), a)) {
      const d = 1 + nodeLvl("perfectFrame") * 0.4;
      l += physFit(a, t) * d;
    }
    return Math.round(clamp99(l, 1, 999));
  }
  function devRating(e) {
    const t = POSITIONS[e.pos].w;
    let a = 0,
      s = 0;
    for (const n in t) ((a += (e.attrs[n] || 10) * t[n]), (s += t[n]));
    return Math.round((a / s) * 4);
  }
  function playerOvr(e) {
    return calcOvr(e.attrs, e.pos, e.body);
  }
  function ovrTier(e) {
    return e >= 180
      ? { label: "GALAXY-CLASS", color: "#ff5a5a" }
      : e >= 140
        ? { label: "Interstellar", color: "#c9b8ff" }
        : e >= 100
          ? { label: "Transcendent", color: "#8ec3ee" }
          : e >= 95
            ? { label: "Hall of Fame", color: "#f0bb45" }
            : e >= 90
              ? { label: "All-Pro", color: "#f0bb45" }
              : e >= 85
                ? { label: "All-Star", color: "#6bbf59" }
                : e >= 80
                  ? { label: "UFF Starter", color: "#6bbf59" }
                  : e >= 74
                    ? { label: "UFF Fringe", color: "#c9a13b" }
                    : e >= 64
                      ? { label: "Draftable", color: "#c9a13b" }
                      : e >= 50
                        ? { label: "Prospect", color: "#a9b8ad" }
                        : { label: "Developing", color: "#8a988e" };
  }
  function suggestPositions(e, t) {
    const a = { QB: 7.18, RB: 7.91, WR: 7.9, TE: 2.98, OL: 1.19, DL: 2.62, LB: 5.75, CB: 6.96, S: 7.22 },
      s = { QB: 0, RB: -0.35, WR: 0, TE: 0.35, OL: 0.55, DL: 1, LB: -0.1, CB: -0.35, S: 0.25 };
    return Object.keys(POSITIONS)
      .map(n => {
        const i = calcOvr(e, n, t),
          r = t ? physFit(t, n) : 0;
        return { pos: n, ovr: i, phys: r, fitScore: i - (a[n] || 0) + (s[n] || 0) };
      })
      .sort((n, i) => i.fitScore - n.fitScore || i.ovr - n.ovr);
  }
  const SAVE_KEY = "gridiron_save_v1";
  function saveGame() {
    try {
      window.GridironStorage
        ? window.GridironStorage.save(state)
        : localStorage.setItem(SAVE_KEY, JSON.stringify(state));
    } catch (e) {
      console.warn("Running It Back save failed", e);
    }
  }
  function loadSave() {
    try {
      if (window.GridironStorage) return window.GridironStorage.load();
      const e = localStorage.getItem(SAVE_KEY);
      if (e) return JSON.parse(e);
    } catch (e) {
      console.warn("Running It Back load failed", e);
    }
    return null;
  }
  let toastTimer;
  function playSfx(e) {
    if (settingOn("sound"))
      try {
        const t = window.AudioContext || window.webkitAudioContext;
        if (!t) return;
        const a = playSfx.ctx || (playSfx.ctx = new t()),
          s = a.createOscillator(),
          n = a.createGain();
        (s.connect(n), n.connect(a.destination));
        const i = { tap: [180, 0.035], good: [520, 0.11], big: [760, 0.18], bad: [120, 0.16], coin: [920, 0.09] },
          r = i[e] || i.tap;
        ((s.type = e === "bad" ? "sawtooth" : "sine"),
          s.frequency.setValueAtTime(r[0], a.currentTime),
          e === "big" && s.frequency.exponentialRampToValueAtTime(1180, a.currentTime + r[1]),
          n.gain.setValueAtTime(1e-4, a.currentTime),
          n.gain.exponentialRampToValueAtTime(0.055, a.currentTime + 0.01),
          n.gain.exponentialRampToValueAtTime(1e-4, a.currentTime + r[1]),
          s.start(),
          s.stop(a.currentTime + r[1] + 0.02));
      } catch {}
  }
  function haptic(e) {
    if (settingOn("haptics") && navigator.vibrate)
      try {
        navigator.vibrate(e || 18);
      } catch {}
  }
  function showToast(e) {
    const t = byId("toast");
    ((t.textContent = e),
      t.classList.add("show"),
      playSfx("tap"),
      clearTimeout(toastTimer),
      (toastTimer = setTimeout(() => t.classList.remove("show"), 1900)));
  }
  function bigMoment(e, t, a) {
    const s = byId("momentBanner"),
      n = byId("cinemaFlash");
    s &&
      ((s.innerHTML = e + (t ? `<small>${t}</small>` : "")),
      (s.style.borderColor = a === "bad" ? "var(--blood)" : a === "good" ? "var(--good)" : "var(--gold)"),
      s.classList.remove("go"),
      n.classList.remove("go"),
      s.offsetWidth,
      s.classList.add("go"),
      n.classList.add("go"),
      playSfx(a === "bad" ? "bad" : "big"),
      haptic(a === "bad" ? [35, 45, 35] : [20, 35, 55]));
  }
  function ur(e) {
    const t = playerOvr(e),
      a = LEVELS[e.level],
      s = e.seasonStats || {};
    return [
      { ic: "📈", text: `Reach ${a.need + 8} OVR`, done: t >= a.need + 8, reward: "+1 momentum" },
      { ic: "🏆", text: "Win a championship at this level", done: (e.titles || 0) > 0, reward: "+legacy" },
      { ic: "🔥", text: "Post an A-grade season", done: !!(s.grade && s.grade[0] === "A"), reward: "+recruiting" }
    ];
  }
  function fr(e) {
    const t = ur(e);
    return `<div class="card objective-card"><div class="objective-head"><div class="objective-icon">🎯</div><div><div class="objective-kicker">CAREER STORYLINE</div><div class="objective-title">${t.filter(s => s.done).length}/3 objectives complete</div></div></div><div class="objective-list">${t.map(s => `<div class="objective-row ${s.done ? "done" : ""}"><span>${s.done ? "✓" : s.ic}</span><span>${s.text}</span><span class="objective-reward">${s.reward}</span></div>`).join("")}</div></div>`;
  }
  function showTutorial() {
    if (state.tutorialSeen) return;
    let e = 0;
    const t = [
        [
          "BUILD THE PLAYER",
          "Choose a position, train the attributes that matter, and shape a career that feels different every run."
        ],
        [
          "PLAY THE MOMENTS",
          "Watch games live, chase season targets, survive injuries, and beat rivals on the way up."
        ],
        [
          "CREATE A LEGACY",
          "Prestige, gear, mastery, records, and championships permanently improve every future career."
        ]
      ],
      a = document.createElement("div");
    ((a.className = "onboard"), document.body.appendChild(a));
    const s = () => {
      const n = t[e];
      ((a.innerHTML = `<div class="onboard-card"><div class="onboard-step">WELCOME · ${e + 1}/3</div><div class="onboard-title">${n[0]}</div><div class="onboard-copy">${n[1]}</div><div class="onboard-dots">${t.map((i, r) => `<i class="${r === e ? "on" : ""}"></i>`).join("")}</div><button class="btn" id="onNext">${e === 2 ? "Start Your Legacy" : "Next"}</button></div>`),
        (byId("onNext").onclick = () => {
          (playSfx("good"),
            haptic(18),
            e < 2
              ? (e++, s())
              : ((state.tutorialSeen = !0),
                saveGame(),
                a.remove(),
                bigMoment("WELCOME TO RUNNING IT BACK", "Your story starts now.", "good")));
        }));
    };
    s();
  } /* v139: what is BANKED rides the chip too — v136 C holds a live player's PP until the career
   * settles, and until now the only place that number appeared was the career-end card. */
  function syncCounters() {
    ((byId("prestigeCount").textContent = Math.round((state.prestige || 0) * 10) / 10),
      (byId("ppCount").textContent = state.pp));
    const b = byId("ppBankChipV139"),
      n = bankedV136();
    if (b) {
      b.hidden = !(n > 0);
      b.textContent = n > 0 ? " \u00b7 \u{1F3E6} " + ppShortV139(n) : "";
      b.title =
        n > 0
          ? n.toLocaleString() +
            " PP is banked from this career — it is paid into your account the moment the career ends, won or cut."
          : "";
    }
  }
  /* the topbar is one nowrap line on a phone, so the bank is compact and the tooltip has the number */
  function ppShortV139(n) {
    return n < 1e3
      ? String(n)
      : n < 1e6
        ? (n / 1e3).toFixed(n < 1e4 ? 1 : 0).replace(/\.0$/, "") + "K"
        : (n / 1e6).toFixed(n < 1e7 ? 1 : 0).replace(/\.0$/, "") + "M";
  } /* ===== v54 AVAILABILITY — injuries that actually cost you games =====
   * The old per-game chance was (12 - injuryResist*0.25)% clamped to a 0.5% FLOOR,
   * and any developed player sits on that floor: measured over full seasons, the
   * `injured` flag never fired once, so the whole v18 layer below it — severity,
   * weeksRemaining, sitting out — could never run. Games were never missed.
   *
   * Chance now keys on WHO YOU ARE PLAYING, as asked: the gap between your rating
   * and the opponent's. Two or three levels of class above them and the hits stop
   * landing; outmatched and you get worn down. Fatigue, injuryResist, perks,
   * prestige and personality all still multiply in.
   *
   * Calibrated so an average season misses one or two games (see rollInjuryV18 for
   * the severity split) and a lost season stays genuinely rare. */
  /* v54: the week plan's own injury rate (the "1.2% injury" on the plan cards) used
   * to be rolled as a SECOND independent probability on top of the model's — so the
   * real chance was chance x 0.012, and nobody ever got hurt. It is a relative risk,
   * so treat it as one: a cautious plan damps the model, a reckless one amplifies it. */
  function injPlanMultV54(a) {
    try {
      window.__INJ_DEBUG && window.__INJ_DEBUG.planMult++;
    } catch (_) {}
    return clamp99(
      (a && a.injury != null ? a.injury : 0.06) / TU("injPlanBase", 0.06),
      TU("injPlanMin", 0.55),
      TU("injPlanMax", 1.6)
    );
  }
  window.__injPlanMultV54 = injPlanMultV54;
  function injChanceV54(e, opts) {
    try {
      window.__INJ_DEBUG && window.__INJ_DEBUG.chance++;
    } catch (_) {}
    opts = opts || {};
    const mine = playerPower(e);
    // The opponent's rating is already plumbed through as `oppBoost`, which the
    // score model builds as (theirRating - leagueAverage)/180 — so invert that
    // rather than thread a new argument through every caller.
    const base = 45 + e.level * 5.5;
    const oppR =
      opts.oppRating != null
        ? Number(opts.oppRating)
        : opts.oppBoost != null
          ? base + Number(opts.oppBoost) * 180
          : opts.wk && opts.wk.opponentV11 && opts.wk.opponentV11.rating != null
            ? Number(opts.wk.opponentV11.rating)
            : base;
    const gap = mine - oppR; // + = you outclass them
    // 20-30 points of class above the opponent roughly halves the risk; being
    // outmatched by the same margin raises it by half again.
    const gapMult = clamp99(1 - gap / TU("injGapScale", 60), TU("injGapMin", 0.28), TU("injGapMax", 1.9));
    const resist = (e.attrs && e.attrs.injuryResist) || 10;
    const resistMult = clamp99(
      1.35 - resist * 0.009 - Math.max(0, resist - 100) * TU("injResistPastK", 0.001),
      TU("injResistFloor", 0.25),
      1.4
    ); /* v141: past 100 it keeps buying something */
    const c = e.conditionV11 || null;
    const fatMult =
      1 + ((opts.fatigue != null ? Number(opts.fatigue) : (c && c.fatigue) || 0) / 100) * TU("injFatigueK", 0.9);
    /* v111: accumulated wear is a term in THIS roll, not a second coin flip beside it. `load` is
     * the signed ledger v111 keeps (0 for a body that has never gone past a normal week), so at
     * every default this multiplier is exactly 1 and nothing below it moves. */
    const loadV111 = opts.load != null ? Number(opts.load) : (e._wearV111 && e._wearV111.load) || 0;
    const loadMult = clamp99(1 + (loadV111 / 100) * TU("wearInjK", 1), TU("wearInjMin", 0.8), TU("wearInjMax", 2.4));
    let m = TU("injBase", 0.17) * gapMult * resistMult * fatMult * loadMult;
    if (hasTrait(e, "ironFrame")) m *= 0.7;
    if (hasTrait(e, "glassBones")) m *= 1.35;
    m *=
      Math.max(0.15, 1 - treeFx("injDown") - gearFx("injDown") - gearV147("injChance")) * (seasonModFx("injMult") || 1);
    /* v120: asking the coach for more snaps than he trusts you with is a term in THIS roll too — the
     * forecast passes it; a game-day caller with just the week gets it read off the week's dial */
    let askK = 1;
    try {
      askK =
        opts.askMul != null
          ? Number(opts.askMul)
          : opts.wk && opts.wk.usageV111 && window.__V120
            ? window.__V120.askMul(e, opts.wk.usageV111)
            : 1;
    } catch (_) {
      askK = 1;
    }
    m *= clamp99(askK || 1, 1, 3);
    return clamp99(m, 0.015, 0.55);
  }
  /* v54: a young body bounces back, but it must not ERASE the injury — the age band
   * cancelled 82% of them outright at ages <=12, which on its own was enough to make
   * the whole layer invisible. Scaled down to a real edge (~26% at that age) instead
   * of an eraser; the age band still shortens what does land. */
  window.__injChanceV54 = injChanceV54;
  window.__injModelV54 = {
    chance: injChanceV54,
    roll: e => rollInjuryV18(e),
    mustSit: e => mustSitV18(e),
    ovr: e => playerPower(e)
  };
  /* v54: how healthy you are is worth a flat swing either way — fresh and clean is
   * an edge, not merely the absence of a penalty. */
  /* ===== v112 THE PRICE OF WALKING AWAY =====
   * Abandoning a character you do not like the look of and rolling another was free. It costs
   * now: the next man carries -5% on EVERY attribute until he is promoted one level up from the
   * level he was created on. It is charged through condMultV54 because that is the ONE multiplier
   * both halves of the game already agree on — the live sim reads it per attribute for the
   * you-player (_raw), the attribute sheet reads it as effAttrsV85().mult, the silent week
   * multiplies the grade by it and the live booking multiplies its delta by it — so there is no
   * screen where the number is true and another where it is not. The record lives on the SAVE
   * ROOT (o.rerollV112), the way o.milestones does, so Hl()/prestigeReset cannot wipe it and a
   * reload cannot forget it. A career that ENDED is not an abandonment: both the cut screen and
   * the retirement screen set player._settled before they pay out, and a settled player is never
   * charged — neither is a first-ever character, where there is no player to abandon. It does not
   * STACK (always the one -5%), but every reroll RE-ARMS it at the new man's level, so rolling
   * again can only push the debt further out, never erase it. ===== */
  function abandonedV112() {
    const e = typeof state < "u" && state && state.player;
    return !!(e && !e._settled);
  }
  function armRerollV112(e, t) {
    const a = state.rerollV112;
    state.rerollV112 = {
      active: !0,
      pct: TU("rerollPenaltyPct", 5),
      level: e.level | 0,
      career: state.careers | 0,
      prev: t || "",
      count: ((a && a.count) || 0) + 1
    };
  }
  function rerollActiveV112(e) {
    const t = typeof state < "u" && state && state.rerollV112;
    if (!t || !t.active || !e) return null;
    if (nodeLvl("cleanSlate") > 0) return null;
    /* v134 Apex: Clean Slate */ if (state.player && e !== state.player) return null;
    if ((t.career | 0) !== (state.careers | 0)) return null;
    if ((e.level | 0) > (t.level | 0)) return null;
    return t;
  }
  function rerollMulV112(e) {
    const t = rerollActiveV112(e);
    return t ? clamp99(1 - (t.pct || 5) / 100, 0.5, 1) : 1;
  }
  function rerollWhyV112(e) {
    const t = rerollActiveV112(e);
    return t
      ? "You left " +
          (t.prev || "a player") +
          " unfinished to roll another. \u2212" +
          t.pct +
          "% to every attribute until you are promoted out of " +
          ((LEVELS[t.level] && LEVELS[t.level].name) || "this level") +
          "."
      : "";
  }
  function rerollChipV112(e) {
    const t = rerollActiveV112(e || (typeof state < "u" && state && state.player));
    return t
      ? `<span class="meta rr-chip-v112" style="color:#e08a8a" title="${rerollWhyV112(e || state.player).replace(/"/g, "&quot;")}">\u26a0 \u2212${t.pct}% reroll${t.count > 1 ? " \u00d7" + t.count : ""}</span>`
      : "";
  }
  function rerollNoteV112(e) {
    const t = rerollActiveV112(e || (typeof state < "u" && state && state.player));
    return t
      ? `<div class="threshold-note rr-note-v112" style="color:#e08a8a;border-color:rgba(224,138,138,.5);margin-bottom:8px">\u26a0 <b>REROLL PENALTY \u2212${t.pct}%</b> \u00b7 ${rerollWhyV112(e || state.player)}</div>`
      : "";
  }
  /* v150 A: true when there is nothing to warn about, else a Promise<boolean> off the in-app dialog */
  function rerollGateV112() {
    if (!abandonedV112() || nodeLvl("cleanSlate") > 0) return !0;
    const e = state.player;
    return askV150(
      "\u26a0 REROLL PENALTY\n\n" +
        e.name +
        " hasn't finished his career. Abandon him now and your NEXT player starts with \u2212" +
        TU("rerollPenaltyPct", 5) +
        "% to EVERY attribute \u2014 in games and on the sheet \u2014 until he is promoted one level.\n\nFinish this career instead and there is no penalty, and you keep his prestige.\n\nRoll a new character anyway?",
      { title: "Reroll penalty", ok: "Roll a new character", danger: !0 }
    );
  }
  /* ===== v120 FATIGUE IS A SLOPE =====
   * The body used to be a step: fresh (+5%) under 25, nothing to 69, then −10% at 70 and no worse at
   * 100. It is a slope now: fresh under freshFatigue, nothing to fatSlopeFrom (40), then a straight
   * line down to condWornMax (−20%) at 100 — which passes −10% at the old worn line of 70, so a body
   * at 70 plays exactly as it did — and playing hurt is at least the old −10% wherever fatigue sits.
   * One function, read by condMultV54 (the sim, the sheet, the silent week), the body ledger's row
   * and the season projection, so they cannot disagree. */
  function fatigueMulV120(fat, inj) {
    const x = Number(fat) || 0,
      from = TU("fatSlopeFrom", 40);
    let m = 1;
    if (x <= TU("freshFatigue", 25)) m = TU("condFresh", 1.05);
    else if (x > from)
      m =
        1 -
        ((x - from) / Math.max(1, 100 - from)) * (1 - TU("condWornMax", 0.8)) * fatKeepV146(); /* v146 C: Iron Lungs */
    if (inj) m = Math.min(m, TU("condWorn", 0.9));
    return m;
  }
  window.__fatigueMulV120 = fatigueMulV120;
  /* ===== v139 THE SEASON'S CHOICE IS PAID IN YARDS, NOT IN A NUMBER =====
   *
   * A season choice advertising "-7 performance for the rest of the season" moved the perf
   * RATING and nothing else. Measured over 250 resolved weeks an arm, on the same player and
   * the same week: no choice 28.1, -7 gave 27.4, -25 gave 24.1, +25 gave 34.0. So the card
   * promised seven and the season paid about seven TENTHS — and the live game never saw it at
   * all, because the score and the stat line are emergent from the play-by-play and the perf
   * grade is derived FROM the box score afterwards rather than fed into it. `condMultV54` is
   * the one multiplier the live sim, `effAttrsV85`, the silent week and the live booking all
   * already agree on, and it read only the reroll debt and fatigue. Nothing routed the choice
   * into it, so a player who had just sold out his season played exactly as well as one who
   * had not.
   *
   * It rides that multiplier now, so the choice is paid where the player can feel it: in his
   * effective attributes, and therefore in his yards. Fixing the mechanism fixes the dilution
   * too — the emergent box score drops, and the perf grade, which is modulated by that box
   * score, follows it down to something like the number on the card.
   *
   * The biggest swing the game offers is +/-25, mapped to +/-10% of attributes: about half of
   * fatigue's own range, which is the right order for a decision rather than a catastrophe.
   * A player with no choice made multiplies by exactly 1 and is untouched. */
  function eventPerfMulV139(e) {
    const p = ((e && e.eventChoice && e.eventChoice.perf) || 0) + ((e && e.eventBenchV128) || 0);
    if (!p) return 1;
    return clamp99(1 + p * TU("eventPerfAttrK", 0.004), 0.8, 1.12);
  }
  window.__eventPerfMulV139 = eventPerfMulV139;
  function condMultV54(e) {
    const rr = rerollMulV112(e) * eventPerfMulV139(e); /* v112 reroll debt · v139 the season's choice */
    const c = e && e.conditionV11;
    if (!c) return rr;
    return fatigueMulV120(c.fatigue, c.injury) * rr; /* v120: a slope, not a step */
  }
  window.__condMultV54 = condMultV54;
  /* ===== v73 BODY LEDGER — what your body is worth in the NEXT game =====
   * The condition card reported three raw numbers — fatigue, recovery capacity,
   * mental load — and a line naming the injury. Every one of them is an INPUT to the
   * model, and none of them is the thing a player needs in order to decide anything:
   * what does this body do to the next game, and is that worth spending points on?
   * So a season could go badly with the reason sitting right there on screen in a
   * form nobody could read, and injuryResist was a stat you bought on faith.
   *
   * The ledger answers the actual question. It states one signed NET figure — the
   * rating this body adds to or takes off the next game — itemises where it came
   * from, prices the week's injury risk in expected games missed, and quotes what the
   * NEXT skill point of durability buys, in the same units. Nothing about the model
   * changes: every number below is read out of injChanceV54 / condMultV54 / the
   * rollInjuryV18 severity split, which is why the marginal-resist figure is measured
   * by asking the real chance function rather than re-deriving its formula. ===== */
  function bodyLedgerV73(e) {
    const c = cv18(e),
      inj = c.injury,
      fat = c.fatigue || 0;
    const played = (e.weekResults || []).filter(w => w && w.played && !w.satOut && w.perf != null);
    // the rating this body would post if it were fresh — the base the multiplier acts
    // on, so the swing comes out in the units the player reads on every other screen
    const recent = played.slice(-4);
    const base = recent.length ? recent.reduce((t, w) => t + w.perf, 0) / recent.length : 58;
    const mult = condMultV54(e),
      swing = base * (mult - 1);
    const nx = (e.weekResults || []).find(w => w && !w.played) || null;
    const left = (e.weekResults || []).filter(w => w && !w.played).length;
    const sit = mustSitV18(e);
    const risk = sit ? 0 : injChanceV54(e, { wk: nx });
    // rollInjuryV18's own split: 45% knock (no games), 33% one, 15% two, 6.6% three-to-
    // five, 0.4% season. Keep it as a dial so a re-tuned split can be told to the ledger.
    const EXPM = TU("injExpMissed", 0.92);
    const missNext = risk * EXPM,
      missRest = missNext * Math.max(1, left);
    // what one more point of durability buys — measured, not re-derived. The real
    // chance function is asked with the stat one higher and put straight back.
    let riskUp = risk;
    if (!sit && e.attrs) {
      const r0 = e.attrs.injuryResist;
      try {
        e.attrs.injuryResist = r0 + 1;
        riskUp = injChanceV54(e, { wk: nx });
      } finally {
        e.attrs.injuryResist = r0;
      }
    }
    const rows = [];
    rows.push(
      fat <= TU("freshFatigue", 25)
        ? { k: "FRESH LEGS", v: "+" + Math.round((mult - 1) * 100) + "% to every attribute", d: swing, good: true }
        : inj || mult < 0.995
          ? {
              k: inj ? "PLAYING HURT" : fat >= TU("wornFatigue", 70) ? "WORN DOWN" : "WEARING DOWN",
              v: Math.round((mult - 1) * 100) + "% to every attribute",
              d: swing,
              good: false
            }
          : { k: "STEADY", v: "no swing either way", d: 0, good: null }
    );
    if (inj)
      rows.push({
        k: inj.seasonEnding ? "SEASON-ENDING" : inj.knock ? "KNOCK" : "INJURY",
        v:
          inj.name + " · severity " + inj.severity + (inj.weeksRemaining > 0 ? " · " + inj.weeksRemaining + " wk" : ""),
        d: 0,
        good: false
      });
    rows.push({
      k: "INJURY RISK",
      v: sit
        ? "you do not play this week"
        : Math.round(risk * 100) + "% this game · " + Math.round(missNext) + " games expected",
      d: 0,
      good: risk <= 0.12 ? true : risk >= 0.24 ? false : null
    });
    rows.push({
      k: "DURABILITY",
      v:
        "resist " +
        Math.round((e.attrs && e.attrs.injuryResist) || 0) +
        (sit ? "" : " · +1 point takes risk to " + Math.round(riskUp * 100) + "%"),
      d: 0,
      good: null,
      gain: risk - riskUp
    });
    return {
      mult,
      base,
      swing,
      risk,
      riskUp,
      missNext,
      missRest,
      left,
      sit,
      inj,
      fat,
      rows,
      net: swing,
      verdict: sit ? "UNAVAILABLE" : swing >= 0.8 ? "NET POSITIVE" : swing <= -0.8 ? "NET NEGATIVE" : "NEUTRAL"
    };
  }
  window.__BODY_V73 = { ledger: e => bodyLedgerV73(e) };
  /* ===== v111 YOUR GAME, YOUR BODY — the involvement dial, the wear ledger, the focus =====
   * Three things the career never let the player decide, all of them priced off machinery that
   * was already here.
   *
   * (1) INVOLVEMENT. `usageV111` turns the pregame dial into two numbers the engine can act on:
   *     `share` — the fraction of his unit's snaps he is ON THE FIELD for, and `touchMul` — how
   *     much of the ball comes his way on the snaps he plays. A rest snap is a real rest snap:
   *     the engine swaps a BACKUP into his roster slot for that play, so FieldSim fields the
   *     backup, the commentary names the backup and `pe()` credits the backup. Nothing is
   *     suppressed after the fact, which is what keeps stat-credit truth intact.
   *
   * (2) WEAR. `forecastV111` prices the NEXT game before it is played and `chargeV111` bills the
   *     body from what he ACTUALLY did. The price rides on how much he did (snaps x touches, with
   *     the heavy end of the dial superlinear), his durability (the same `injuryResist` curve
   *     `injChanceV54` uses, plus ironFrame/glassBones), the opponent (the same rating gap
   *     `injChanceV54` already computes) and the stakes (a championship costs more than week
   *     three). The bill is charged in the three places the game already reads: `conditionV11
   *     .fatigue`, a `load` term inside `injChanceV54` itself, and a lingering stat cut carried in
   *     `_tempStatBuffsV25`-shaped entries with a `games` countdown, so `effAttrsV85` and every
   *     sheet show it with no new plumbing.
   *
   *     The number is a DELTA, not a ratchet: a game costs `wearBase x work` and the week gives
   *     `wearRecover` back, both of them through the SAME multiplier, so at "normal" the two
   *     cancel to exactly nothing no matter who he is playing, how brittle he is or what the
   *     fixture is worth. The ledger does not move. That is the whole reason the defaults reproduce today
   *     exactly, and it puts the tradeoff where the player can see it — in the DIAL, not in the
   *     schedule. Go heavy and it accumulates; go limited and it unwinds.
   *
   * (3) THE FOCUS. `focusForV111` is exactly three position-appropriate picks, each a 1.2x
   *     MULTIPLIER on one attribute for that game only. `_tempStatBuffsV25` entries were flat adds;
   *     a `mul` field is honoured now by `effAttrsV85` AND by the engine's own attribute accessor
   *     (`_raw`), applied AFTER `condMultV54` and clamped so it can never compound into nonsense.
   *
   * State: `week.usageV111` and `week.focusV111` are written by the pregame screen; `player
   * ._wearV111 = {load, lingering}` is owned here. `window.__V111` is the hook. ===== */
  const KEYS_V111 = ["limited", "reduced", "normal", "heavy", "everysnap"];
  /* Above "normal" the SHARE cannot rise — he already plays every snap of his unit today — so the
   * top half of the dial buys TOUCHES and pays for them with the body. That is the honest reading.
   * Every number here is a live dial: TU("useShareLimited"/"useShareReduced"/"useShareNormal"/
   * "useShareHeavy"/"useShareEvery") and TU("useTouchLimited"/"useTouchReduced"/"useTouchNormal"/
   * "useTouchHeavy"/"useTouchEvery") — named in the rows below so DEV can find them. */
  const USE_V111 = {
    limited: {
      label: "LIMITED",
      icon: "🪑",
      share: ["useShareLimited", 0.55],
      touch: ["useTouchLimited", 0.7],
      desc: "Rotational. You watch a lot of football from the sideline and your body thanks you for it."
    },
    reduced: {
      label: "REDUCED",
      icon: "🔃",
      share: ["useShareReduced", 0.75],
      touch: ["useTouchReduced", 0.85],
      desc: "A managed load — off the field on the long drives, fresh when the game turns."
    },
    normal: {
      label: "NORMAL",
      icon: "🏈",
      share: ["useShareNormal", 1],
      touch: ["useTouchNormal", 1],
      desc: "Your share. The snaps the coach trusts you with, and your ordinary share of the ball."
    },
    heavy: {
      label: "HEAVY",
      icon: "🔥",
      share: ["useShareHeavy", 1],
      touch: ["useTouchHeavy", 1.25],
      desc: "Featured. You ask for more than your share — he gives what he trusts you with, and the ball comes looking for you."
    },
    everysnap: {
      label: "EVERY SNAP",
      icon: "⚡",
      share: ["useShareEvery", 1],
      touch: ["useTouchEvery", 1.5],
      desc: "You ask for everything and they ride you until the wheels come off — the biggest numbers, and the biggest bill."
    }
  };
  const useKeyV111 = k => (KEYS_V111.indexOf(k) >= 0 ? k : "normal");
  const curWeekV111 = pl => ((pl && pl.weekResults) || []).find(w => w && !w.played) || null;
  /* ===== v120 THE COACH DECIDES YOUR SNAPS =====
   * NORMAL used to be every snap the unit takes, from game one. It is now the share the COACH hands
   * him, and a stranger gets about half of them: trustShareV120 runs from trustShareMin at trust 0
   * to all of them at 100, so the first game is played on a part of the snaps and the share climbs
   * as trust does. Below NORMAL the dial still sells snaps back. Above it he ASKS for more than his
   * share: the coach gives a part of the extra, more the more he trusts him (askSayV120), and the
   * asking itself is paid in body — askMulV120 multiplies the wear and the injury roll (the forecast,
   * the bill and the game-day roll alike, through injChanceV54) and shrinks to nothing at full trust.
   * More trust is more say, and only ever for MORE snaps; the bottom of the ladder never needs it. */
  const trustV120 = pl => clamp99(pl && pl.coachTrust != null ? Number(pl.coachTrust) : 50, 0, 100) / 100;
  const trustShareV120 = pl =>
    clamp99(
      clamp99(TU("trustShareMin", 0.3) + TU("trustShareSpan", 0.7) * trustV120(pl), 0.2, 1) *
        roleShareV146B(pl) *
        (1 + gearV147("snapShare")),
      0.05,
      1
    ); /* v146 B: a UFF backup plays a part of what the coach would give a starter */
  const askOverV120 = k => ({ heavy: TU("askOverHeavy", 0.25), everysnap: TU("askOverEvery", 0.5) })[k] || 0;
  const askMulV120 = (pl, k) => {
    const over = askOverV120(useKeyV111(k));
    return over ? 1 + over * TU("askDistrustK", 1) * (1 - trustV120(pl)) : 1;
  };
  const askSayV120 = (pl, k) => {
    const over = askOverV120(useKeyV111(k));
    return over ? 1 + over * (TU("askSayFloor", 0.35) + (1 - TU("askSayFloor", 0.35)) * trustV120(pl)) : 1;
  };
  const shareOfV111 = (k, pl) => {
    pl = pl || (typeof state < "u" && state ? state.player : null);
    const t = trustShareV120(pl);
    return clamp99(
      askOverV120(k) ? t * askSayV120(pl, k) : clamp99(TU(USE_V111[k].share[0], USE_V111[k].share[1]), 0.2, 1) * t,
      0.05,
      1
    );
  };
  window.__V120 = {
    trust: trustV120,
    trustShare: trustShareV120,
    askOver: askOverV120,
    askMul: askMulV120,
    askSay: askSayV120,
    share: shareOfV111
  }; /* the UI and injChanceV54 live outside this scope: they read the hook */
  const touchOfV111 = k => clamp99(TU(USE_V111[k].touch[0], USE_V111[k].touch[1]), 0.3, 2.2);
  function usageV111(pl, wk) {
    pl = pl || (typeof state < "u" && state ? state.player : null);
    wk = wk === undefined ? curWeekV111(pl) : wk;
    const k = useKeyV111(wk && wk.usageV111),
      r = USE_V111[k];
    return {
      key: k,
      label: r.label,
      icon: r.icon,
      desc: r.desc,
      share: shareOfV111(k, pl),
      touchMul: touchOfV111(k),
      askMul: askMulV120(pl, k)
    };
  }
  /* how much football he played, as one number: snaps are close to linear, touches are not — a
   * back handed the ball twenty-five times pays more than proportionally for the last five. */
  const workV111 = (share, touchMul) =>
    Math.pow(clamp99(share, 0.05, 1), TU("wearSnapPow", 0.9)) *
    Math.pow(clamp99(touchMul, 0.2, 2.5), TU("wearTouchPow", 1.6));
  /* the same durability curve injChanceV54 runs on, and the same two traits */
  function durMulV111(pl) {
    let m = clamp99(1.35 - ((pl && pl.attrs && pl.attrs.injuryResist) || 10) * 0.009, 0.45, 1.4);
    if (hasTrait(pl, "ironFrame")) m *= TU("wearIron", 0.78);
    if (hasTrait(pl, "glassBones")) m *= TU("wearGlass", 1.3);
    return m;
  }
  /* the same rating gap injChanceV54 computes — outclass them and the hits stop landing.
   * v126: and HOW they play, not just how good they are. Every opponent has carried a
   * `physicality` since v11 and nothing has ever read it; a side that hits at 90 leaves you in a
   * different state on Sunday night than a finesse side of exactly the same rating, and the
   * condition card now says so by name. */
  function oppMulV111(pl, wk) {
    const base = 45 + ((pl && pl.level) || 0) * 5.5,
      op = wk && wk.opponentV11,
      oppR = op && op.rating != null ? Number(op.rating) : base,
      phys = op && op.physicality != null ? Number(op.physicality) : 55;
    return (
      clamp99(1 - (playerPower(pl) - oppR) / TU("wearGapScale", 70), TU("wearGapMin", 0.5), TU("wearGapMax", 1.7)) *
      clamp99(1 + (phys - 55) / TU("wearPhysScale", 190), TU("wearPhysMin", 0.85), TU("wearPhysMax", 1.2))
    );
  }
  /* ===== v126 THE OPPONENT HAS A FACE, AND HE WEARS ON YOU =====
   * The next-opponent card was a name, a confidence dial, a fog count and a RECOMMENDED COUNTER —
   * a plan the player has not picked himself since the pregame wizard took the call. So the one
   * concrete thing on the card was the one thing it could not act on, and everything you might
   * actually want to know about the side you are about to play (how good they are against your
   * level, whether they can score or only stop you, how fast they play and how hard they hit) was
   * generated and thrown away.
   *
   * `oppReadV126` reads it back: the tier, the rating gap in points, offence and defence as
   * separate numbers, and tempo / physicality / pressure in words rather than as raw 0-100 dials.
   * `oppWearSayV126` is the other half — the wear ledger already prices the opponent (`oppMulV111`)
   * and your body (`durMulV111`), it just never NAMED either, so "wear and tear" was a number with
   * no cause. It says both now, with the multiplier each one contributes and the fatigue and injury
   * they add up to for the next game. ===== */
  const TEMPO_V126 = [
    [45, "grinds the clock"],
    [65, "plays at a normal tempo"],
    [101, "plays fast and snaps early"]
  ];
  const PHYS_V126 = [
    [45, "finesse — they will not wear you down"],
    [62, "physical"],
    [78, "punishing — this one hurts on Monday"],
    [101, "brutal — the most violent fixture on the schedule"]
  ];
  const PRESS_V126 = [
    [45, "sits back and covers"],
    [65, "mixes its pressure"],
    [101, "blitzes on any down"]
  ];
  const sayV126 = (tbl, v) => (tbl.find(r => v < r[0]) || tbl[tbl.length - 1])[1];
  function oppReadV126(op, pl) {
    if (!op) return null;
    const mine = playerPower(pl),
      gap = Math.round(mine - (op.rating || 0));
    const tier =
      gap <= -12
        ? ["FAR BETTER THAN YOU", "var(--blood)"]
        : gap <= -4
          ? ["BETTER THAN YOU", "#ff9b93"]
          : gap < 4
            ? ["AN EVEN MATCH", "var(--gold)"]
            : gap < 12
              ? ["BEATABLE", "#9fe6b0"]
              : ["OUTCLASSED BY YOU", "var(--good)"];
    const side =
      op.offense > op.defense + 6
        ? "an offence that will score on anybody"
        : op.defense > op.offense + 6
          ? "a defence that does not give anything up"
          : "a balanced side with no obvious hole";
    return {
      gap,
      tier: tier[0],
      col: tier[1],
      rating: op.rating,
      off: op.offense,
      def: op.defense,
      mine,
      tempo: sayV126(TEMPO_V126, op.tempo || 55),
      phys: sayV126(PHYS_V126, op.physicality || 55),
      press: sayV126(PRESS_V126, op.pressure || 55),
      physN: op.physicality || 55,
      identity:
        "They are " +
        side +
        ". They " +
        sayV126(TEMPO_V126, op.tempo || 55) +
        ", they are " +
        sayV126(PHYS_V126, op.physicality || 55) +
        ", and on defence the unit " +
        sayV126(PRESS_V126, op.pressure || 55) +
        "."
    };
  }
  /* the wear sentence: who is hitting you, how hard your body takes it, and what the two come to */
  function oppWearSayV126(pl, wk) {
    try {
      wk = wk === undefined ? curWeekV111(pl) : wk;
      const op = wk && wk.opponentV11;
      if (!op) return "";
      const rd = oppReadV126(op, pl),
        om = oppMulV111(pl, wk),
        dm = durMulV111(pl),
        fc = forecastV111(pl, wk),
        dur = (pl.attrs && pl.attrs.injuryResist) || 10,
        pc = v => (v >= 1 ? "+" : "−") + Math.round(Math.abs(v - 1) * 100) + "%";
      return `<div class="threshold-note" style="margin-top:8px;color:#cdd8e8">🏟️ <b>${escHtml(op.name || wk.opp || "THIS WEEK'S OPPONENT")}</b> · rated <b>${op.rating}</b> against your <b>${rd.mine}</b>, and they are <b style="color:${rd.physN >= 62 ? "#ff9b93" : "#9fe6b0"}">${rd.phys}</b>.<br>
      That fixture moves this week's wear <b style="color:${om >= 1 ? "#ff9b93" : "#9fe6b0"}">${pc(om)}</b>; your durability of <b>${Math.round(dur)}</b> moves it <b style="color:${dm >= 1 ? "#ff9b93" : "#9fe6b0"}">${pc(dm)}</b>.
      ${fc ? `Together they put you at <b>${fc.fatigueAfter} fatigue</b> after the game (from ${fc.fatigueNow}) and <b style="color:${fc.injPct >= 12 ? "#ff9b93" : "#9fe6b0"}">${fc.injPct}% injury risk</b> in it.` : ""}
      <span style="color:var(--chalk-dim)">A harder, more physical side costs you more of your body for the same snaps — and every 10 points of Durability takes about 9% back off both numbers.</span></div>`;
    } catch (_) {
      return "";
    }
  }
  /* a semifinal costs more than week three. The schedule marks the big ones three ways at once and
   * this reads all three, because a week that says it is a semifinal has to be priced as one no
   * matter which of its markers a caller happened to set: `playoff`/`roundIdx` against ea(level)'s
   * round list first (what cn() writes and what the bracket carries), then the `importance` the
   * opponent row carries and the ROUND NAME itself — the same vocabulary the scorebug and the body
   * card already read off. A save with no marker at all falls back to where the fixture sits in the
   * season, and treats the run-in as the fixtures that decide it. */
  const ROUND_RE_V111 = {
    final: /final(?!ist)|championship|title|super\s*bowl/i,
    semi: /semi|conference\s*(final|championship)/i
  };
  function stakesV111(pl, wk) {
    if (!wk) return { mul: 1, label: "Regular week" };
    const riv = wk.opponentV11 && wk.opponentV11.rivalry ? TU("wearStakesRival", 1.1) : 1,
      imp = String((wk.opponentV11 && wk.opponentV11.importance) || ""),
      rnd = String(wk.round || "");
    if (wk.playoff) {
      const rounds = (typeof playoffRoundNames === "function" && playoffRoundNames(pl.level)) || [],
        last = Math.max(0, rounds.length - 1),
        i = wk.roundIdx != null ? wk.roundIdx : last;
      if (i >= last) return { mul: TU("wearStakesFinal", 1.7) * riv, label: wk.round || "Championship" };
      if (i === last - 1) return { mul: TU("wearStakesSemi", 1.42) * riv, label: wk.round || "Semifinal" };
      return { mul: TU("wearStakesPlayoff", 1.22) * riv, label: wk.round || "Playoff" };
    }
    /* no `playoff` flag, but the week still says what it is — honour it rather than pricing a
     * championship as week three because one field was not set */
    if (ROUND_RE_V111.semi.test(rnd)) return { mul: TU("wearStakesSemi", 1.42) * riv, label: rnd || "Semifinal" }; // before the final test: "Semifinal" ENDS in "final"
    if (imp === "championship" || ROUND_RE_V111.final.test(rnd))
      return { mul: TU("wearStakesFinal", 1.7) * riv, label: rnd || "Championship" };
    if (imp === "playoff" || /playoff|quarter|wild\s*card|round\s*of/i.test(rnd))
      return { mul: TU("wearStakesPlayoff", 1.22) * riv, label: rnd || "Playoff" };
    const reg = ((pl && pl.weekResults) || []).filter(w => w && !w.playoff),
      n = reg.length || 1,
      i = reg.indexOf(wk),
      at = i < 0 ? 0 : (i + 1) / n,
      from = clamp99(TU("wearStakesLateFrom", 0.6), 0, 0.95),
      mul = (1 + TU("wearStakesLate", 0.22) * clamp99((at - from) / (1 - from), 0, 1)) * riv;
    return { mul, label: at >= 0.88 ? "Season on the line" : at >= from ? "Run-in" : "Regular week" };
  }
  const loadOfV111 = pl =>
    clamp99((pl && pl._wearV111 && pl._wearV111.load) || 0, TU("wearLoadMin", -40), TU("wearLoadMax", 180));
  /* a body already carrying wear breaks down faster than a fresh one */
  const wornMulV111 = pl =>
    1 +
    (Math.max(0, loadOfV111(pl)) / 100) * TU("wearCompound", 0.3) +
    (Math.max(0, ((pl && pl.conditionV11 && pl.conditionV11.fatigue) || 0) - TU("freshFatigue", 25)) / 100) *
      TU("wearFatCompound", 0.25);
  /* One game's bill, SIGNED, and this is the shape that matters. What the game takes out of him is
   * `wearBase x work`; what the week gives back is `wearRecover`, priced in the SAME units because a
   * hard fixture is a hard week whether he plays forty snaps of it or twelve. BOTH terms are then
   * scaled by the same multiplier, which is what makes "normal" cost exactly nothing for every body,
   * every opponent and every fixture — the property the whole feature rests on, and the reason the
   * involvement DIAL and not the schedule is what moves this ledger.
   *
   * The multiplier is not quite the same on the two sides of that crossover. Damage rides the whole
   * stack; recovery rides a capped one (`wearRecoverCapMul`), so a brutal fixture never becomes the
   * best rest week on the schedule — nobody heals a season's wear by sitting out one afternoon in
   * the semifinal. Both branches hand back zero at `work == 1`, so the cap can never reopen the
   * hole it was closing: the old form scaled only the damage by M and left recovery flat, which
   * meant an ORDINARY starter (dur x opp is already past 2 for most of a career) was quietly
   * billed for a week he played exactly as he always had. */
  function costV111(pl, wk, work) {
    const dur = durMulV111(pl),
      opp = oppMulV111(pl, wk),
      st = stakesV111(pl, wk),
      worn = wornMulV111(pl),
      M = dur * opp * st.mul * worn,
      dmg0 = TU("wearBase", 12) * work,
      rec0 = TU("wearRecover", 12),
      Mx = dmg0 >= rec0 ? M : Math.min(M, TU("wearRecoverCapMul", 2)),
      dmg = dmg0 * Mx,
      rec = rec0 * Mx;
    return { gross: dmg, rec, dur, opp, st, worn, work, mul: M, mulUsed: Mx, load: dmg - rec };
  }
  /* Accumulated load past the threshold is carried onto the field as a real cut. It is priced as a
   * PERCENTAGE and only then turned into points, because three points off a pee-wee's speed of
   * twelve is a quarter of the kid and off a professional's eighty-five it is nothing. What the
   * lingering entry carries is the points — a plain signed `amt` with a `games` countdown, which is
   * the shape `_tempStatBuffsV25` already understood. */
  const cutPctV111 = L =>
    -clamp99(Math.round((L - TU("wearCutFrom", 45)) / TU("wearCutScale", 7)), 0, TU("wearCutMaxPct", 12));
  const cutPtsV111 = (pl, pct, ks) =>
    ks.reduce((t, k) => t - Math.max(1, Math.round((((pl.attrs && pl.attrs[k]) || 50) * -pct) / 100)), 0);
  /* which numbers go first: the two his position leans on hardest — never durability or grit,
   * which is what he would be spending points on to stop this happening again */
  function cutStatsV111(pl) {
    const w = (pl && pl.pos && POSITIONS[pl.pos] && POSITIONS[pl.pos].w) || {},
      ks = Object.keys(w)
        .filter(k => ATTR_INFO[k] && k !== "injuryResist" && k !== "grit" && k !== "discipline")
        .sort((a, b) => w[b] - w[a]);
    return ks.length ? ks.slice(0, TU("wearCutStats", 2)) : ["speed"];
  }
  function forecastV111(pl, wk, key) {
    pl = pl || (typeof state < "u" && state ? state.player : null);
    if (!pl || !pl.attrs) return null;
    wk = wk === undefined ? curWeekV111(pl) : wk;
    /* v120: the work is measured against the share the coach GIVES him — NORMAL is his normal week and
     * costs nothing by construction, whatever his trust; the rungs below pay back, the rungs above cost
     * their extra snaps, their extra ball and the price of asking */
    const k = useKeyV111(key || (wk && wk.usageV111)),
      ask = askMulV120(pl, k),
      work = workV111(shareOfV111(k, pl) / trustShareV120(pl), touchOfV111(k)) * ask,
      C = costV111(pl, wk, work),
      fat = cv18(pl).fatigue || 0,
      fatigueAfter = clamp99(fat + C.load * wearFatKV147(), 0, 100),
      loadAfter = clamp99(loadOfV111(pl) + C.load, TU("wearLoadMin", -40), TU("wearLoadMax", 180)),
      risk = mustSitV18(pl) ? 0 : injChanceV54(pl, { wk, load: loadAfter, fatigue: fatigueAfter, askMul: ask }),
      cutPct = cutPctV111(loadAfter),
      cutStats = cutPct < 0 ? cutStatsV111(pl) : [],
      statCut = cutPtsV111(pl, cutPct, cutStats);
    return {
      key: k,
      load: Math.round(C.load * 10) / 10,
      loadNow: Math.round(loadOfV111(pl)),
      loadAfter: Math.round(loadAfter),
      fatigueAfter: Math.round(fatigueAfter),
      fatigueNow: Math.round(fat),
      injPct: Math.round(risk * 1000) / 10,
      gamesMissed: Math.round(risk * TU("injExpMissed", 0.92) * 100) / 100,
      statCut,
      statCutPct: cutPct,
      cutStats,
      stakes: C.st.mul,
      stakesLabel: C.st.label,
      oppMul: C.opp,
      durMul: C.dur,
      wornMul: C.worn,
      work,
      askMul: ask,
      parts: [
        { label: "Workload · " + USE_V111[k].label, mul: work },
        { label: "Durability", mul: C.dur },
        { label: "Opponent", mul: C.opp },
        { label: C.st.label, mul: C.st.mul },
        { label: "Body already carrying", mul: C.worn }
      ].concat(ask > 1.001 ? [{ label: "Asking above your share", mul: ask }] : [])
    };
  }
  /* the bill, from what he ACTUALLY did. `played` is the engine's own count — {snaps, teamSnaps,
   * touchMul, touches} — and the dial is only the fallback when the engine did not report. */
  function chargeV111(pl, wk, played) {
    pl = pl || (typeof state < "u" && state ? state.player : null);
    if (!pl || !pl.attrs) return { load: 0, fatigueAfter: 0, lingering: [] };
    wk = wk === undefined ? curWeekV111(pl) : wk;
    played = played || {};
    const u = usageV111(pl, wk),
      share = played.teamSnaps > 0 ? clamp99(played.snaps / played.teamSnaps, 0.05, 1) : u.share,
      touch = played.touchMul != null ? clamp99(played.touchMul, 0.3, 2.5) : u.touchMul,
      C = costV111(
        pl,
        wk,
        workV111(share / trustShareV120(pl), touch) * askMulV120(pl, u.key)
      ) /* v120: against the share the coach gives him, and the asking is billed whatever he actually played */,
      W = pl._wearV111 || (pl._wearV111 = { load: 0, lingering: [] });
    W.load = clamp99((W.load || 0) + C.load, TU("wearLoadMin", -40), TU("wearLoadMax", 180));
    const c = cv18(pl);
    c.fatigue = clamp99((c.fatigue || 0) + C.load * wearFatKV147(), 0, 100);
    const pct = cutPctV111(W.load),
      ks = pct < 0 ? cutStatsV111(pl) : [],
      cut = cutPtsV111(pl, pct, ks);
    W.lingering = (W.lingering || []).filter(b => b && (b.games | 0) > 0 && b.v111 !== "wear");
    /* the percentage is turned into POINTS against the attribute he owns right now, and never
     * rounds away to nothing — a cut the sheet cannot show is not a cut. The entry is then an
     * ordinary flat `_tempStatBuffsV25` row with a `games` countdown on it. */
    ks.forEach(sk =>
      W.lingering.push({
        stat: sk,
        amt: -Math.max(1, Math.round((((pl.attrs && pl.attrs[sk]) || 50) * -pct) / 100)),
        games: TU("wearCutGames", 2),
        v111: "wear"
      })
    );
    W.last = {
      load: Math.round(C.load * 10) / 10,
      share: Math.round(share * 100) / 100,
      touchMul: Math.round(touch * 100) / 100,
      snaps: played.snaps || 0,
      teamSnaps: played.teamSnaps || 0,
      touches: played.touches || 0,
      cut,
      cutPct: pct,
      fatigue: Math.round(c.fatigue)
    };
    return {
      load: W.last.load,
      loadAfter: Math.round(W.load),
      fatigueAfter: Math.round(c.fatigue),
      statCut: cut,
      statCutPct: pct,
      lingering: W.lingering.slice()
    };
  }
  /* A game is spent: today's one-game buffs are dropped exactly as they always were, and only an
   * entry carrying a `games` countdown survives it. A watched week runs this TWICE — ca() pre-books
   * the game and then lt() plays it — so the week itself carries the stamp; clearing a list twice
   * was idempotent, counting a clock down twice is not. */
  function decayWearV111(pl, wk) {
    if (!pl) return;
    wk = wk === undefined ? curWeekV111(pl) : wk;
    if (wk) {
      if (wk._wearSpentV111) return;
      wk._wearSpentV111 = !0;
    }
    const kept = (pl._tempStatBuffsV25 || [])
      .filter(b => b && (b.games | 0) > 1)
      .map(b => Object.assign({}, b, { games: (b.games | 0) - 1 }));
    pl._tempStatBuffsV25 = kept.length ? kept : null;
    const W = pl._wearV111;
    if (W && W.lingering)
      W.lingering = W.lingering
        .map(b => Object.assign({}, b, { games: (b.games | 0) - 1 }))
        .filter(b => (b.games | 0) > 0);
  }
  /* ===== v111 THE FOCUS — three ways to spend a Saturday =====
   * Three position-appropriate picks, one stat each, 1.2x for that game only. Rows are
   * [key, name, icon, stat, description, alsoEngineKeys?].
   *
   * `stat` is the career attribute — it is what the sheet draws and what the player thinks he is
   * buying. A sixth entry names any EXTRA key FieldSim builds an agent from that the same pick
   * ought to move: a back's burst really is `burst` to the agents, and a corner living in a
   * receiver's hip pocket is `coverage` as much as awareness. Those keys are not career attributes,
   * so they never appear on the sheet and never double the visible gain — they only make the pick
   * mean on the grass what it says on the card. */
  const FOCUS_V111 = {
    QB: [
      ["arm", "Air It Out", "🚀", "throwing", "Let the arm eat — every throw goes up with more on it."],
      ["poise", "Ice In The Veins", "🧊", "awareness", "Stand in, read it out, take what the defence gives."],
      ["release", "Quick Release", "⚡", "quickness", "Get it out before the rush can get home."]
    ],
    RB: [
      [
        "truck",
        "Run Them Over",
        "🚛",
        "strength",
        "Lower the shoulder. Somebody is going to get hurt and it will not be you."
      ],
      ["shifty", "Make Them Miss", "🌀", "agility", "Live in the cutback. Feet first, contact last."],
      [
        "burst",
        "Hit It And Go",
        "💨",
        "acceleration",
        "One cut, full speed, gone before the second level closes.",
        ["burst"]
      ]
    ],
    WR: [
      ["hands", "Sure Hands", "🧤", "catching", "Everything inside the frame gets caught, contested or not."],
      ["separate", "Get Open", "🌀", "agility", "Win at the top of the route and the ball is already there."],
      ["deep", "Take The Top Off", "💨", "speed", "Run past them. Make the safety earn his afternoon."]
    ],
    TE: [
      ["hands", "Sure Hands", "🧤", "catching", "Be the answer on third down and catch everything in the middle."],
      ["inline", "Dig Them Out", "🧱", "blocking", "Live in the trench and finish every block on the ground."],
      ["power", "Move The Pile", "🚛", "strength", "Catch it short and drag the linebacker for the sticks."]
    ],
    OL: [
      ["anchor", "Set The Anchor", "🧱", "strength", "Nobody comes through you today. The pocket holds."],
      ["technique", "Perfect Technique", "📐", "blocking", "Hands inside, feet alive, no beaten reps."],
      ["cerebral", "Call The Front", "🧠", "awareness", "See the stunt before it comes and pass it off clean."]
    ],
    DL: [
      ["bull", "Bull Rush", "🚛", "strength", "Walk the tackle back into his own quarterback."],
      ["first", "First Step", "⚡", "quickness", "Beat the snap. Be past him before he is out of his stance."],
      ["finish", "Finish The Play", "🔨", "tackling", "Get there and put him on the ground — no arm tackles."]
    ],
    LB: [
      ["thump", "Bring The Wood", "🔨", "tackling", "Fill the gap and hit the runner, not the hole."],
      ["read", "Read And React", "🧠", "awareness", "Diagnose it on the first step and arrive before the play does."],
      ["sideline", "Sideline To Sideline", "💨", "speed", "Chase everything. Nothing gets outside you today."]
    ],
    CB: [
      ["lock", "Lock Him Down", "🔒", "awareness", "Live in his hip pocket and take the route away.", ["coverage"]],
      ["ball", "Play The Ball", "🧤", "catching", "Turn and find it. Every throw your way is your ball."],
      ["close", "Close The Window", "💨", "speed", "Drive on the throw and be there when it arrives."]
    ],
    S: [
      ["hits", "Set The Tone", "🔨", "tackling", "Make the crossers hear footsteps for four quarters."],
      ["range", "Cover The Field", "💨", "speed", "Play centre field. Nothing gets behind you.", ["coverage"]],
      ["ball", "High Point It", "🙌", "jumping", "Go up and take it away at the top of the jump."]
    ],
    K: [
      ["leg", "Swing Big", "🦵", "strength", "Extra range. Trot out for anything inside sixty."],
      ["nerve", "Ice Water", "🧊", "discipline", "The crowd does not exist. Same swing every time."],
      ["place", "Pure Strike", "📐", "awareness", "Down the middle, every time, wind or no wind."]
    ],
    ATH: [
      ["power", "Play Strong", "🚛", "strength", "Win every collision you are in."],
      ["quick", "Play Fast", "💨", "speed", "Outrun the problem."],
      ["smart", "Play Smart", "🧠", "awareness", "Be in the right place before anyone else is."]
    ]
  };
  /* The keys FieldSim's `makeAgents` asks the engine's accessor for, by name. A focus is only
   * worth anything if its stat is one of THESE — the roster itself carries a shorter list
   * (speed/accel/agility/strength/catching/tackling/awareness/hands/power/coverage/blocking/burst),
   * and the accessor answers a flat 45 for anything it is missing. That fallback is still a number
   * the you-player's multiplier scales, so the buff lands on the agent either way: today it lifts
   * him off the league's flat 45 on that key, and once the roster carries the key it lifts his real
   * value instead. What it must never do is name a stat nobody asks for. */
  /* v141: the roster carries every one of these now (qr() and Wr's h()); the flat-45 fallback below is history. */
  const SIM_KEYS_V111 = [
    "speed",
    "acceleration",
    "burst",
    "quickness",
    "agility",
    "awareness",
    "strength",
    "blocking",
    "tackling",
    "coverage",
    "catching",
    "jumping",
    "throwing",
    "vision",
    "stamina",
    "grit",
    "discipline",
    "ballControl"
  ];
  function focusForV111(pos) {
    const L = FOCUS_V111[pos] || FOCUS_V111.ATH,
      mul = clamp99(TU("focusMul", 1.2), 1, TU("focusMulCeil", 1.35));
    return L.map(r => ({
      key: r[0],
      name: r[1],
      icon: r[2],
      stat: r[3],
      statName: (ATTR_INFO[r[3]] && ATTR_INFO[r[3]].name) || r[3],
      mul,
      desc: r[4],
      also: r[5] || []
    }));
  }
  function buffForV111(wk) {
    const pl = typeof state < "u" && state ? state.player : null;
    wk = wk === undefined ? curWeekV111(pl) : wk;
    if (!wk || !wk.focusV111) return null;
    const hit = focusForV111((pl && pl.pos) || "ATH").find(x => x.key === wk.focusV111);
    return hit ? { key: hit.key, stat: hit.stat, mul: hit.mul, name: hit.name, icon: hit.icon, also: hit.also } : null;
  }
  /* the v111 entries ALONE — a lingering cut and this game's focus — in _tempStatBuffsV25's own
   * shape. Kept separate from the roster's list so nothing the game already does is disturbed. */
  function ownBuffsV111(pl) {
    const out = [];
    if (!pl) return out;
    const W = pl._wearV111;
    W &&
      W.lingering &&
      W.lingering.forEach(b => {
        if (b && b.stat && (b.games | 0) > 0) out.push(b);
      });
    const fb = buffForV111(undefined);
    if (fb && fb.stat && pl === (typeof state < "u" && state ? state.player : null)) {
      out.push({ stat: fb.stat, mul: fb.mul, v111: "focus" });
      (fb.also || []).forEach(k => out.push({ stat: k, mul: fb.mul, v111: "focusAlso" }));
    }
    return out;
  }
  /* everything the attribute sheet and the engine should see this game */
  function wearBuffsV111(pl) {
    return ((pl && pl._tempStatBuffsV25) || []).slice().concat(ownBuffsV111(pl));
  }
  window.__V111 = {
    KEYS: KEYS_V111,
    usage: (pl, wk) => usageV111(pl, wk),
    forecast: (pl, wk, k) => forecastV111(pl, wk, k),
    charge: (pl, wk, played) => chargeV111(pl, wk, played),
    focusFor: pos => focusForV111(pos),
    buffFor: wk => buffForV111(wk),
    buffs: pl => wearBuffsV111(pl || (typeof state < "u" && state ? state.player : null)),
    ownBuffs: pl => ownBuffsV111(pl || (typeof state < "u" && state ? state.player : null)),
    decay: (pl, wk) => decayWearV111(pl || (typeof state < "u" && state ? state.player : null), wk),
    wear: pl =>
      (pl || (typeof state < "u" && state ? state.player : null) || {})._wearV111 || { load: 0, lingering: [] },
    work: workV111,
    cost: (pl, wk, work) => costV111(pl, wk, work),
    stakes: (pl, wk) => stakesV111(pl, wk),
    SIM_KEYS: SIM_KEYS_V111,
    USE: USE_V111,
    FOCUS: FOCUS_V111
  };
  /* ===== v111 THE SEASON IS ON HIM — wear and tear in the body card =====
   * The v73 ledger answers one question and answers it well: what is this body
   * worth in the NEXT game. It says nothing about the season, because until the
   * involvement dial existed there was no season-long decision to say anything
   * about — every week charged the body the same amount and the player had no
   * hand on the tap. Now he does, and the ledger's silence is the whole problem:
   * he can spend his legs a week at a time with no statement of the bill, and the
   * only place the bill arrives is a semifinal he watches in a tracksuit.
   *
   * So this section prices the tap. It reads the load his body is already carrying
   * and shows WHERE it came from (the heavy weeks stand up out of the season as
   * bars you can point at), it prices the next game at the involvement he has
   * actually chosen — the wear it adds, the injury chance, the games it is
   * expected to cost, the rating he carries into the game after it — and it
   * itemises the three things that set that price: his own durability, the
   * opponent, and what the fixture is worth. Then it says the tradeoff out loud in
   * one line, because that line is the feature and the table is only the evidence.
   *
   * Everything here is a READ. Nothing in this block moves a number the sim uses.
   * The model is window.__V111 (agent A); every call into it is guarded and falls
   * back to a local estimate built from injChanceV54 / condMultV54 / the schedule,
   * so the card is honest on a save that has never met the model at all. ===== */
  const WEAR_KEYS_V111 = ["limited", "reduced", "normal", "heavy", "everysnap"];
  const WEAR_LBL_V111 = {
    limited: "LIMITED",
    reduced: "REDUCED",
    normal: "NORMAL",
    heavy: "HEAVY",
    everysnap: "EVERY SNAP"
  };
  const WEAR_SHARE_V111 = { limited: 0.5, reduced: 0.75, normal: 1, heavy: 1.22, everysnap: 1.45 };
  const WEAR_STAKE_V111 = { routine: 1, evaluation: 1.12, rivalry: 1.18, playoff: 1.32, championship: 1.55 };
  function wearApiV111() {
    try {
      const a = window.__V111;
      return a && typeof a.forecast == "function" ? a : null;
    } catch (_) {
      return null;
    }
  }
  function wearNextV111(e) {
    try {
      return ((e && e.weekResults) || []).find(w => w && !w.played) || null;
    } catch (_) {
      return null;
    }
  }
  // the model owns the vocabulary once it exists; this list is only the standalone copy
  function wearKeysV111() {
    const a = wearApiV111();
    return a && Array.isArray(a.KEYS) && a.KEYS.length ? a.KEYS : WEAR_KEYS_V111;
  }
  function wearLblV111(k) {
    return (
      WEAR_LBL_V111[k] ||
      String(k || "")
        .replace(/[_-]/g, " ")
        .toUpperCase()
    );
  }
  function wearKeyV111(e, wk) {
    const k = wk && wk.usageV111;
    return wearKeysV111().indexOf(k) >= 0 ? k : "normal";
  }
  function wearOppRefV111(e) {
    return 45 + ((e && e.level) || 0) * 5.5;
  }
  // the local estimate — the same shape __V111.forecast returns, built out of numbers
  // that already exist on this screen, so a worktree with no model still reads true
  function wearStubV111(e, wk, key) {
    const share = WEAR_SHARE_V111[key] != null ? WEAR_SHARE_V111[key] : 1;
    const c = (typeof cv18 == "function" && cv18(e)) || {},
      fat = c.fatigue || 0;
    const resist = (e && e.attrs && e.attrs.injuryResist) || 10;
    const durMul = clamp99(
      TU("wearDurHiV111", 1.5) - resist * TU("wearDurKV111", 0.009),
      TU("wearDurLoV111", 0.55),
      TU("wearDurHiV111", 1.5)
    );
    const op = (wk && wk.opponentV11) || null,
      ref = wearOppRefV111(e);
    const phys = op && op.physicality != null ? ((Number(op.physicality) - 50) / 100) * TU("wearPhysKV111", 0.3) : 0;
    const oppMul = clamp99(1 + (((op && op.rating) || ref) - ref) / TU("wearOppScaleV111", 70) + phys, 0.65, 1.8);
    const imp = (op && op.importance) || (wk && wk.playoff ? "playoff" : "routine");
    const stakes = WEAR_STAKE_V111[imp] || 1;
    const worn =
      1 +
      clamp99(((e && e._wearV111 && e._wearV111.load) || 0) / TU("wearScaleV111", 120), 0, 1.4) *
        TU("wearWornKV111", 0.35);
    // the multipliers compound, so the product is compressed rather than clipped — a
    // brutal week stays the most expensive week without becoming the whole season, and
    // a neutral one (every driver at 1) still comes out at exactly the base
    const mul = clamp99(
      Math.pow(share * durMul * oppMul * stakes * worn, TU("wearMulGammaV111", 0.72)),
      TU("wearMulMinV111", 0.3),
      TU("wearMulMaxV111", 3.4)
    );
    const load = Math.max(0, TU("wearBaseLoadV111", 9) * mul);
    let risk = 0;
    try {
      risk = typeof mustSitV18 == "function" && mustSitV18(e) ? 0 : injChanceV54(e, { wk });
    } catch (_) {
      risk = 0.12;
    }
    const injPct =
      clamp99(risk * share * (1 + (stakes - 1) * TU("wearStakeInjKV111", 0.5)), 0, TU("wearInjCapV111", 0.7)) * 100;
    const fatigueAfter = clamp99(fat + load * TU("wearFatigueKV111", 0.62) - TU("wearRestV111", 6), 0, 100);
    const mAfter = fatigueMulV120(fatigueAfter, false); /* v120: the same slope the sim plays on */
    let base = 58;
    try {
      base = bodyLedgerV73(e).base;
    } catch (_) {}
    return {
      load,
      fatigueAfter,
      injPct,
      gamesMissed: (injPct / 100) * TU("injExpMissed", 0.92),
      statCut: base * (mAfter - 1),
      parts: [
        { label: "Durability", mul: durMul },
        { label: "Opponent", mul: oppMul },
        { label: "Stakes", mul: stakes },
        { label: "Already worn", mul: worn }
      ],
      stakes,
      oppMul,
      durMul,
      stub: !0
    };
  }
  function wearForecastV111(e, wk, key) {
    const api = wearApiV111();
    if (api) {
      try {
        const r = api.forecast(e, wk, key);
        if (r && isFinite(Number(r.load))) return r;
      } catch (_) {}
    }
    return wearStubV111(e, wk, key);
  }
  // what a week ALREADY cost. Agent A stamps the charge on the week; a week that
  // predates the model (or a save that never met it) is estimated the same way the
  // forecast is, and says so, rather than leaving a hole in the season.
  function wearWeekLoadV111(e, w) {
    if (!w || !w.played || w.satOut) return null;
    let v = w.wearV111 != null ? w.wearV111 : w.wearLoadV111 != null ? w.wearLoadV111 : w.loadV111;
    if (v && typeof v == "object") v = v.load;
    if (v != null && isFinite(Number(v))) return { load: Math.max(0, Number(v)), est: !1 };
    try {
      return { load: wearStubV111(e, w, wearKeyV111(e, w)).load, est: !0 };
    } catch (_) {
      return { load: 0, est: !0 };
    }
  }
  function wearHistV111(e) {
    const out = [];
    try {
      ((e && e.weekResults) || []).forEach((w, i) => {
        const L = wearWeekLoadV111(e, w);
        if (!L) return;
        out.push({
          week: w.week || i + 1,
          opp: w.opp || "",
          key: wearKeyV111(e, w),
          load: L.load,
          est: L.est,
          playoff: !!w.playoff,
          importance: (w.opponentV11 && w.opponentV11.importance) || "routine"
        });
      });
    } catch (_) {}
    return out;
  }
  // the band names the MILEAGE, never the freshness — the v73 ledger three inches
  // above is already talking about fatigue, and a body can be rested today and still
  // be carrying a season it will not survive
  function wearBandV111(pct) {
    return pct < TU("wearBandFreshV111", 20)
      ? { n: "LIGHT", c: "#57e07a", d: "barely been touched" }
      : pct < TU("wearBandManagedV111", 45)
        ? { n: "MANAGED", c: "#7fe0a0", d: "carrying it fine" }
        : pct < TU("wearBandLoadedV111", 70)
          ? { n: "LOADED", c: "var(--gold)", d: "the season is on him" }
          : pct < TU("wearBandWornV111", 88)
            ? { n: "HIGH MILEAGE", c: "#ffb74d", d: "running on what is left" }
            : { n: "BREAKING DOWN", c: "#ff8a80", d: "one hit from the sideline" };
  }
  function wearLingerV111(e) {
    const out = [],
      seen = {};
    const add = b => {
      if (!b || !b.stat) return;
      const g = Math.round(Number(b.games != null ? b.games : b.weeks));
      if (!(g > 0)) return;
      const amt = b.max ? -10 : Math.round(Number(b.amt) || 0),
        k = b.stat + "|" + g + "|" + amt;
      if (seen[k]) return;
      seen[k] = 1;
      let nm = b.stat;
      try {
        nm = (window.__statLabelV25 && window.__statLabelV25(b.stat)) || b.stat;
      } catch (_) {}
      out.push({ stat: b.stat, name: String(nm), amt, games: g });
    };
    try {
      ((e && e._tempStatBuffsV25) || []).forEach(add);
    } catch (_) {}
    try {
      ((e && e._wearV111 && e._wearV111.lingering) || []).forEach(add);
    } catch (_) {}
    return out.sort((a, b) => a.amt - b.amt);
  }
  // the three drivers, always present and always in the same order, whether the model
  // named them in parts[] or the estimate had to work them out
  function wearDriversV111(e, wk, fc) {
    const P = Array.isArray(fc.parts) ? fc.parts : [];
    const pick = re => P.find(p => p && re.test(String(p.label || "")));
    const op = (wk && wk.opponentV11) || null;
    const resist = Math.round((e && e.attrs && e.attrs.injuryResist) || 0);
    const d = pick(/dur|resist|tough|frame/i),
      o2 = pick(/opp|matchup|defen|front/i),
      s = pick(/stake|round|fixture|import|occasion/i);
    const stakeName =
      (wk && wk.round) ||
      (op && op.importance === "championship"
        ? "Championship"
        : op && op.importance === "playoff"
          ? "Playoff"
          : op && op.importance === "rivalry"
            ? "Rivalry game"
            : op && op.importance === "evaluation"
              ? "Evaluation game"
              : "Regular week");
    const extra = P.filter(p => p && p !== d && p !== o2 && p !== s && isFinite(Number(p.mul)));
    return [
      { k: "DURABILITY", mul: d ? Number(d.mul) : fc.durMul != null ? Number(fc.durMul) : 1, note: "resist " + resist },
      {
        k: "OPPONENT",
        mul: o2 ? Number(o2.mul) : fc.oppMul != null ? Number(fc.oppMul) : 1,
        note: ((wk && wk.opp) || "TBD") + (op && op.rating != null ? " · " + Math.round(op.rating) : "")
      },
      { k: "STAKES", mul: s ? Number(s.mul) : fc.stakes != null ? Number(fc.stakes) : 1, note: stakeName }
    ].concat(extra.map(p => ({ k: String(p.label || "OTHER").toUpperCase(), mul: Number(p.mul), note: "" })));
  }
  // the fixture the season is actually pointed at, and what reaching it costs at this
  // rate — 1-(1-p)^n over the games in between, which is the number the player is
  // really deciding about when he turns the dial up in week three
  function wearTargetV111(e, wk, fc) {
    const left = ((e && e.weekResults) || []).filter(w => w && !w.played);
    if (!left.length) return null;
    let t =
      left
        .slice()
        .reverse()
        .find(
          w =>
            w &&
            ((w.opponentV11 && w.opponentV11.importance === "championship") ||
              /championship|final/i.test(String(w.round || "")))
        ) || left[left.length - 1];
    // if the big one IS the next game there is nothing to save legs for on the way to
    // it — point the line at the end of the season instead
    if (left.indexOf(t) === 0 && left.length > 1) t = left[left.length - 1];
    const idx = left.indexOf(t),
      n = Math.max(0, idx);
    const p = clamp99(Number(fc.injPct) || 0, 0, 100) / 100;
    return {
      name: String(
        t.round ||
          (t.opponentV11 && t.opponentV11.importance === "championship" ? "the championship" : "") ||
          "week " + (t.week || left.length)
      ),
      games: n,
      miss: n > 0 ? (1 - Math.pow(1 - p, n)) * 100 : 0,
      last: t
    };
  }
  function wearReadV111(e) {
    const wk = wearNextV111(e),
      key = wearKeyV111(e, wk);
    const api = wearApiV111();
    let usage = null;
    try {
      if (api && typeof api.usage == "function") usage = api.usage(e, wk);
    } catch (_) {}
    const hist = wearHistV111(e);
    let season = hist.reduce((t, h) => t + h.load, 0);
    const known = e && e._wearV111 && isFinite(Number(e._wearV111.load)) ? Number(e._wearV111.load) : null;
    const load = known != null ? known : season;
    // the model knows the TOTAL; where the per-week charges had to be estimated, the
    // bars are rescaled to it, so the shape is the season's and the sum is the truth
    if (known != null && season > 0 && known > 0 && hist.every(h => h.est)) {
      const k = known / season;
      hist.forEach(h => (h.load *= k));
      season = known;
    }
    const scale = TU("wearScaleV111", 120),
      pct = clamp99((load / scale) * 100, 0, 100);
    const KS = wearKeysV111(),
      fc = wearForecastV111(e, wk, key),
      lo = wearForecastV111(e, wk, KS[0]);
    return {
      wk,
      key,
      keys: KS,
      lightest: KS[0],
      label: (usage && usage.label) || wearLblV111(key),
      usage,
      api: !!api,
      hist,
      season,
      load,
      scale,
      pct,
      band: wearBandV111(pct),
      fc,
      lo,
      drivers: wearDriversV111(e, wk, fc),
      linger: wearLingerV111(e),
      target: wearTargetV111(e, wk, fc),
      loTarget: wearTargetV111(e, wk, lo),
      stub: !!fc.stub
    };
  }
  function wearCssV111() {
    if (document.getElementById("wearv111css")) return;
    document.head.insertAdjacentHTML(
      "beforeend",
      `<style id="wearv111css">
  .wearv111{margin-top:11px;padding-top:9px;border-top:1px solid rgba(255,255,255,.12)}
  .wearv111-head{display:flex;align-items:flex-start;justify-content:space-between;gap:8px}
  .wearv111-band{font:800 17px Oswald,sans-serif;letter-spacing:.6px;line-height:1.1;margin-top:1px}
  .wearv111-band s{text-decoration:none;display:block;font:400 10.5px system-ui;color:var(--chalk-dim);letter-spacing:0}
  .wearv111-num{font:800 26px Oswald,sans-serif;line-height:1;text-align:right;white-space:nowrap}
  .wearv111-num small{display:block;font:700 8.5px Oswald,sans-serif;letter-spacing:1.6px;color:var(--chalk-dim);margin-top:2px}
  .wearv111-bar{height:6px;border-radius:4px;background:rgba(0,0,0,.34);overflow:hidden;margin:7px 0 0}
  .wearv111-bar i{display:block;height:100%;border-radius:4px}
  .wearv111-spark{display:flex;align-items:flex-end;gap:2px;height:21px;margin:5px 0 3px}
  .wearv111-spark u{flex:1 1 0;min-width:3px;text-decoration:none;border-radius:2px 2px 0 0;background:#5c6b80;opacity:.85}
  .wearv111-spark u.hot{background:#ff8a80;opacity:1}.wearv111-spark u.cool{background:#57e07a;opacity:.8}
  .wearv111-spark u.est{opacity:.42}
  .wearv111-sub{font:400 10.5px system-ui;color:var(--chalk-dim);line-height:1.35}
  .wearv111-next{margin-top:9px;border:1px solid rgba(255,255,255,.1);border-radius:9px;padding:7px 8px;background:rgba(0,0,0,.16)}
  .wearv111-kick{font:700 9px Oswald,sans-serif;letter-spacing:1.5px;color:var(--chalk-dim);margin-bottom:5px}
  .wearv111-kick b{color:var(--gold);letter-spacing:1.5px}
  .wearv111-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(62px,1fr));gap:5px}
  .wearv111-grid>div{text-align:center;background:rgba(255,255,255,.045);border-radius:7px;padding:4px 2px}
  .wearv111-grid b{display:block;font:800 15px Oswald,sans-serif;line-height:1}
  .wearv111-grid small{display:block;font:700 7.5px Oswald,sans-serif;letter-spacing:.9px;color:var(--chalk-dim);margin-top:2px}
  .wearv111-drv{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:4px;margin-top:5px}
  .wearv111-drv span{display:block;background:rgba(255,255,255,.04);border-radius:7px;padding:3px 5px;min-width:0}
  .wearv111-xtra{font:400 9.5px system-ui;color:var(--chalk-dim);margin-top:4px}
  .wearv111-drv i{display:block;font-style:normal;font:700 8px Oswald,sans-serif;letter-spacing:1.1px;color:var(--chalk-dim)}
  .wearv111-drv b{font:800 13px Oswald,sans-serif}
  .wearv111-drv s{text-decoration:none;display:block;font:400 9.5px system-ui;color:var(--chalk-dim);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
  .wearv111-ling{display:flex;flex-wrap:wrap;gap:4px;margin-top:7px}
  .wearv111-ling em{font-style:normal;display:inline-flex;align-items:baseline;gap:4px;border:1px solid #ff8a8055;background:#ff8a8014;border-radius:9px;padding:2px 7px;font:700 10px Oswald,sans-serif;letter-spacing:.7px;color:#ff8a80}
  .wearv111-ling em s{text-decoration:none;font:400 9.5px system-ui;color:var(--chalk-dim)}
  .wearv111-call{margin-top:7px;border-left:3px solid var(--gold);background:rgba(255,255,255,.045);border-radius:0 8px 8px 0;padding:5px 7px;font:400 11px system-ui;line-height:1.4;color:#dbe4f0}
  .wearv111-call b{color:var(--gold)}.wearv111-call b.bad{color:#ff8a80}.wearv111-call b.good{color:#7fe0a0}
  @media(max-width:380px){.wearv111-num{font-size:22px}.wearv111-band{font-size:15px}}
  </style>`
    );
  }
  function wearSectionV111(e) {
    try {
      if (!e || !e.attrs) return "";
      wearCssV111();
      const R = wearReadV111(e),
        B = R.band,
        n1 = v => Math.round(v),
        sgn = v => (v >= 0 ? "+" : "−") + Math.abs(Math.round(v));
      // the season as bars: every played week at what it cost, the heavy ones standing up
      const mx = Math.max(1, ...R.hist.map(h => h.load)),
        mn = R.hist.length ? Math.min(...R.hist.map(h => h.load)) : 0;
      // the bars are drawn against a floor UNDER the quietest week, not against zero —
      // a run of similar weeks with one heavy one in it has to LOOK like that — and they
      // are COLOURED by the dial he turned, not by their own size, because "which weeks
      // did I go heavy" is the question the chart is here to answer
      const flr = mn * TU("wearSparkFloorV111", 0.55);
      const spark = R.hist
        .slice(-TU("wearSparkWeeksV111", 16))
        .map(h => {
          const cls = h.key === "heavy" || h.key === "everysnap" ? "hot" : h.key === "normal" ? "" : "cool";
          return `<u class="${cls}${h.est ? " est" : ""}" title="Wk ${h.week} · ${escHtml(h.opp || "")} · ${wearLblV111(h.key)} · ${n1(h.load)} wear${h.est ? " (est.)" : ""}" style="height:${clamp99(((h.load - flr) / Math.max(0.001, mx - flr)) * 100, 14, 100)}%"></u>`;
        })
        .join("");
      const heavy = R.hist.filter(h => h.key === "heavy" || h.key === "everysnap"),
        hvLoad = heavy.reduce((t, h) => t + h.load, 0);
      const story = !R.hist.length
        ? `No games on the body yet — this season starts him fresh.`
        : heavy.length
          ? `${R.hist.length} game${R.hist.length === 1 ? "" : "s"} on him. The <b style="color:#ff8a80">${heavy.length} heavy week${heavy.length === 1 ? "" : "s"}</b> in red put on ${n1(hvLoad)} of it — ${Math.round((hvLoad / Math.max(1, R.season)) * 100)}% of the season's load.`
          : `${R.hist.length} game${R.hist.length === 1 ? "" : "s"} on him, none of them heavy. The tall bars are the hard fixtures, not your dial.`;
      // the next game, priced at the involvement he has actually picked
      const fc = R.fc,
        out = typeof mustSitV18 == "function" && mustSitV18(e);
      const next = !R.wk
        ? ""
        : `<div class="wearv111-next">
      <div class="wearv111-kick">NEXT GAME · <b>${escHtml(R.label)}</b> · ${escHtml(R.wk.opp || "TBD")}${R.wk.round ? " · " + escHtml(R.wk.round) : ""}</div>
      <div class="wearv111-grid">
        <div><b style="color:${(fc.load || 0) < -0.05 ? "#7fe0a0" : R.hist.length && fc.load >= mx * TU("wearHotFracV111", 0.95) ? "#ff8a80" : "var(--chalk)"}">${sgn(fc.load || 0)}</b><small>WEAR</small></div>
        <div><b style="color:${fc.injPct >= 24 ? "#ff8a80" : fc.injPct <= 12 ? "#7fe0a0" : "var(--gold)"}">${n1(fc.injPct)}%</b><small>INJURY</small></div>
        <div><b>${(Number(fc.gamesMissed) || 0).toFixed(1)}</b><small>GAMES LOST</small></div>
        <div><b style="color:${fc.statCut <= -0.8 ? "#ff8a80" : fc.statCut >= 0.8 ? "#7fe0a0" : "var(--chalk)"}">${sgn(fc.statCut || 0)}</b><small>RATING AFTER</small></div>
      </div>
      <div class="wearv111-drv">${R.drivers
        .slice(0, 3)
        .map(
          d =>
            `<span><i>${escHtml(d.k)}</i><b style="color:${d.mul >= 1.25 ? "#ff8a80" : d.mul >= 1.08 ? "var(--gold)" : d.mul <= 0.92 ? "#7fe0a0" : "var(--chalk)"}">×${(Number(d.mul) || 1).toFixed(2)}</b><s>${escHtml(d.note || "")}</s></span>`
        )
        .join("")}</div>
      ${(() => {
        const x = R.drivers.slice(3).filter(d => Math.abs((Number(d.mul) || 1) - 1) >= 0.01); // a driver doing nothing is not a driver
        return x.length
          ? `<div class="wearv111-xtra">${x.map(d => escHtml(d.k.toLowerCase()) + " ×" + (Number(d.mul) || 1).toFixed(2)).join(" · ")}</div>`
          : "";
      })()}</div>`;
      const ling = R.linger.length
        ? `<div class="wearv111-ling">${R.linger.map(l => `<em>${escHtml(String(l.name).toUpperCase())} ${l.amt >= 0 ? "+" : "−"}${Math.abs(l.amt)} <s>${l.games} game${l.games === 1 ? "" : "s"} left</s></em>`).join("")}</div>`
        : "";
      // the line the whole feature exists to put on screen
      const T = R.target,
        LT = R.loTarget;
      const call = out
        ? `⚖️ You are not playing this week. The load comes off while it passes — that is the only upside of sitting.`
        : T && T.games > 0
          ? `⚖️ At <b>${escHtml(R.label)}</b> every week you reach <b>${escHtml(T.name)}</b> with a <b class="${T.miss >= 30 ? "bad" : ""}">${n1(T.miss)}%</b> chance of missing it${LT && LT.miss < T.miss - 1 ? ` — dialling back to <b class="good">${escHtml(wearLblV111(R.lightest))}</b> makes it <b class="good">${n1(LT.miss)}%</b>` : ""}. ${fc.statCut <= -0.8 ? `And you carry <b class="bad">${sgn(fc.statCut)}</b> rating into the game after this one.` : `Your legs hold for now.`}`
          : `⚖️ This is the last one. Spend everything — there is no game after it to save legs for.`;
      return `<div class="wearv111">
      <div class="wearv111-head"><div><div class="impact-kicker">WEAR &amp; TEAR · THE SEASON ON HIM</div>
        <div class="wearv111-band" style="color:${B.c}">${B.n}<s>${escHtml(B.d)} · ${n1(R.season)} this season${R.stub ? " · estimated" : ""}</s></div></div>
        <div class="wearv111-num" style="color:${B.c}">${n1(R.load)}<small>LOAD / ${n1(R.scale)}</small></div></div>
      <div class="wearv111-bar"><i style="width:${Math.max(2, R.pct)}%;background:${B.c}"></i></div>
      ${R.hist.length ? `<div class="wearv111-spark">${spark}</div>` : ""}
      <div class="wearv111-sub">${story}</div>
      ${next}${ling}
      <div class="wearv111-call">${call}</div></div>`;
      // the section is a READ: it never takes the body card down with it. But a silent
      // empty string is how a broken section hides, so the failure is recorded where a
      // check can see it rather than only in the console.
    } catch (err) {
      try {
        console.warn("v111 wear section", err);
        window.__V111_BODY && (window.__V111_BODY.lastError = String((err && err.message) || err));
      } catch (_) {}
      return "";
    }
  }
  window.__V111_BODY = {
    read: e => wearReadV111(e || (typeof state == "object" && state && state.player)),
    section: e => wearSectionV111(e || (typeof state == "object" && state && state.player)),
    hist: e => wearHistV111(e || (typeof state == "object" && state && state.player)),
    forecast: (e, k) => {
      const p = e || (typeof state == "object" && state && state.player),
        w = wearNextV111(p);
      return wearForecastV111(p, w, k || wearKeyV111(p, w));
    },
    band: wearBandV111,
    keys: wearKeysV111,
    hasModel: () => !!wearApiV111(),
    lastError: null
  };
  function rollGamePerf(e, t = 0, a) {
    a = a || {};
    const s = playerPower(e),
      n = LEVELS[e.level],
      i = n.need - 6 + chaosOppBoost() + seasonModFx("peerShift"),
      r = 1 + nodeLvl("clutch") * 0.02,
      l = 1 + (e.attrs.grit - 10) * 0.003 * r,
      d = 1 + (e.attrs.stamina - 10) * 0.0015;
    let c = hasTrait(e, "streaky") ? 23 : 15;
    c *= Math.max(0.5, 1 - treeFx("varDown") - gearV147("varDown")) * (seasonModFx("varMult") || 1);
    let u = (s - i) * 2.4 + randRange(-c, c) * l + 50 + (t / n.games) * 2 + ((state && state._fateBoost) || 0);
    (a.playoff && ((u += treeFx("playoffPerf") + seasonModFx("playoffPerf")), hasTrait(e, "xfactor") && (u += 12)),
      hasTrait(e, "butterFingers") && Math.random() < 0.12 && (u -= 18),
      Math.random() < (hasTrait(e, "streaky") ? 0.06 : 0.04) && u > 40 && (u *= 1.5),
      (u *= d));
    const p = clamp99(xi(u), 1, 100);
    const y = Math.random() < injChanceV54(e, a);
    try {
      window.__INJ_DEBUG && y && (window.__INJ_DEBUG.hit = (window.__INJ_DEBUG.hit || 0) + 1);
    } catch (_) {}
    return { perf: Math.round(p), ovr: s, injured: y };
  }
  const POS_STATS = {
    QB: {
      primary: "passYds",
      label: "Passing",
      stats: [
        { key: "passYds", name: "Pass Yds", per: [140, 165, 190, 210, 235, 265, 290, 270] },
        { key: "passTD", name: "Pass TD", per: [1.2, 1.4, 1.6, 1.9, 2.1, 2.4, 2.6, 2.6] },
        { key: "int", name: "INT", per: [1.6, 1.4, 1.2, 1.1, 1, 0.9, 0.8, 0.8], lowerBetter: !0 }
      ]
    },
    RB: {
      primary: "rushYds",
      label: "Rushing",
      stats: [
        { key: "rushYds", name: "Rush Yds", per: [55, 70, 80, 90, 100, 115, 130, 105] },
        { key: "rushTD", name: "Rush TD", per: [0.7, 0.9, 1, 1.1, 1.2, 1.3, 1.4, 1.2] },
        { key: "ypc", name: "Yds/Carry", per: [4, 4.3, 4.5, 4.7, 4.9, 5.1, 5.3, 4.8], rate: !0 }
      ]
    },
    WR: {
      primary: "recYds",
      label: "Receiving",
      stats: [
        { key: "rec", name: "Catches", per: [3, 3.5, 4, 4.5, 5, 5.5, 6, 5] },
        { key: "recYds", name: "Rec Yds", per: [45, 55, 65, 72, 80, 90, 100, 85] },
        { key: "recTD", name: "Rec TD", per: [0.5, 0.6, 0.7, 0.8, 0.9, 1, 1.1, 0.9] }
      ]
    },
    TE: {
      primary: "recYds",
      label: "Receiving",
      stats: [
        { key: "rec", name: "Catches", per: [2, 2.5, 3, 3.5, 4, 4.5, 5, 4] },
        { key: "recYds", name: "Rec Yds", per: [28, 34, 40, 46, 52, 60, 68, 55] },
        { key: "recTD", name: "Rec TD", per: [0.3, 0.4, 0.5, 0.6, 0.7, 0.8, 0.9, 0.7] }
      ]
    },
    OL: {
      primary: "grade",
      label: "Blocking",
      stats: [
        { key: "grade", name: "Blk Grade", per: [62, 64, 66, 68, 70, 73, 76, 75], rate: !0 },
        { key: "pancakes", name: "Pancakes", per: [2, 3, 3.5, 4, 4.5, 5, 5.5, 5] },
        { key: "sacksAllowed", name: "Sacks All.", per: [2.5, 2.2, 2, 1.8, 1.5, 1.2, 1, 1], lowerBetter: !0 }
      ]
    },
    DL: {
      primary: "sacks",
      label: "Pass Rush",
      stats: [
        { key: "tackles", name: "Tackles", per: [2.5, 3, 3.5, 4, 4.5, 5, 5.5, 4.5] },
        { key: "sacks", name: "Sacks", per: [0.4, 0.5, 0.6, 0.7, 0.8, 0.9, 1, 0.8] },
        { key: "tfl", name: "TFL", per: [0.8, 1, 1.1, 1.3, 1.5, 1.7, 1.9, 1.5] }
      ]
    },
    LB: {
      primary: "tackles",
      label: "Defense",
      stats: [
        { key: "tackles", name: "Tackles", per: [5, 6, 7, 8, 9, 10, 11, 9] },
        { key: "sacks", name: "Sacks", per: [0.3, 0.4, 0.4, 0.5, 0.6, 0.7, 0.8, 0.6] },
        { key: "tfl", name: "TFL", per: [0.9, 1.1, 1.3, 1.5, 1.7, 1.9, 2.1, 1.7] }
      ]
    },
    CB: {
      primary: "pd",
      label: "Coverage",
      stats: [
        { key: "tackles", name: "Tackles", per: [2.5, 3, 3.5, 4, 4.5, 5, 5.5, 4.5] },
        { key: "pd", name: "Pass Def", per: [1, 1.2, 1.4, 1.6, 1.8, 2, 2.2, 1.8] },
        { key: "int", name: "INT", per: [0.2, 0.25, 0.3, 0.35, 0.4, 0.5, 0.6, 0.45] }
      ]
    },
    S: {
      primary: "tackles",
      label: "Defense",
      stats: [
        { key: "tackles", name: "Tackles", per: [4, 5, 5.5, 6, 6.5, 7, 7.5, 6.5] },
        { key: "pd", name: "Pass Def", per: [0.7, 0.9, 1, 1.2, 1.4, 1.6, 1.8, 1.4] },
        { key: "int", name: "INT", per: [0.2, 0.25, 0.3, 0.35, 0.4, 0.45, 0.5, 0.4] }
      ]
    }
  };
  Object.values(POS_STATS).forEach(e =>
    e.stats.forEach(t => {
      const a = t.per[7];
      t.per[8] = t.lowerBetter ? +(a * 0.85).toFixed(2) : t.rate ? +(a * 1.2).toFixed(1) : +(a * 1.45).toFixed(1);
    })
  );
  function mr(e, t) {
    const a = LEVELS[t].need - 6 + chaosOppBoost();
    return clamp99(xi((e - a) * 2.4 + 50 + treeFx("perfFlat")), 1, 100);
  }
  function prodStats(e, t, a, ng) {
    const s = LEVELS[a].games,
      n = POS_STATS[e],
      i = perfScale(t),
      r = t / 100,
      l = 1 + treeFx("statProd") + seasonModStatProd(e),
      d = {};
    return (
      n.stats.forEach(c => {
        const u = c.per[a],
          h = masteryMult(c);
        const gk = ng ? 0 : gearProdV147(c);
        /* v147 C: the gear's production line, where statProd lands — never the baseline Ha() */ c.rate
          ? (d[c.key] = +(u * (0.72 + 0.42 * Math.pow(t / 90, 1.4)) * h * (1 + gk)).toFixed(1))
          : c.lowerBetter
            ? (d[c.key] = Math.max(1, Math.round(u * s * Math.max(0.2, 1.35 - Math.pow(r, 1.2) * 0.95) * h * (1 - gk))))
            : (d[c.key] = Math.round(u * s * Math.min(i, 3.4) * l * h * (1 + gk)));
      }),
      d
    );
  }
  function baselineStats(e, t) {
    return prodStats(e, 60, t, 1);
  }
  function pr(e, t, a) {
    const s = prodStats(e, t, a),
      n = baselineStats(e, a),
      i = POS_STATS[e].primary,
      r = n[i] || 1;
    return (s[i] || 0) / r;
  }
  const ADVANCE_BASE = [0, 0.28, 0.48, 0.75, 1.02, 1.62, 1.92, 1.84, 2.7];
  function advanceChance(e, t, a, s, n) {
    n = n || 1;
    const i = Math.min(t + 1, LEVELS.length - 1);
    if (t <= 2 && chaosTotal() === 0) return clamp99(96 + (e - LEVELS[i].need) * 0.3, 82, 99.5);
    const r = (ADVANCE_BASE[i] || 0) + (t <= 2, 0),
      l =
        LEVELS[i].need +
        chaosTotal() * 0.45 +
        (state && state.player && state.player.level === t ? pressNeedV131(state.player) : 0);
    /* v131: the Prodigy is judged against a higher bar — the "harsher evaluations" his card promised and nothing delivered */ if (
      a != null &&
      s != null
    ) {
      const c = pr(a, s, t) / (1 + (n - 1) * 0.35),
        u = Math.pow(Math.max(0.05, c), 0.5),
        h = (e - l) / 8,
        p = clamp99(0.5 + h, 0, 1.5),
        m = i === LEVELS.length - 1,
        y = clamp99((e - l) / 30, 0, 1),
        C = m ? r * (1 - y * 0.28) : r * (1 - y * 0.45),
        P = (u * 0.38 + p * 0.64 - C * 0.52 - 0.72) / 0.14;
      let _adv = clamp99(
        (1 / (1 + Math.exp(-P))) * 100 +
          pathVal("advBonus", 0) +
          treeFx("advFlat") +
          seasonModFx("advFlat") +
          gearV147("callUp"),
        0.3,
        99.5
      );
      // v17: national standing drives promotion — you cannot be #1 of 180k kids in the
      // country and get left behind. Elite rank sets a floor the base odds can't undercut.
      try {
        if (state && state.player && state.player.level === t) _adv = Math.max(_adv, rankChanceV88(state.player));
      } catch (_e) {} // v88: the ranking curve is the floor
      return _adv;
    }
    const d = mr(e, t);
    return advanceChance(e, t, a || yr(), d, n);
  }
  function yr() {
    return (state.player && state.player.pos) || "RB";
  }
  function fmtLeadVal(e, t) {
    return Math.round(+t);
  }
  function gr(e, t) {
    const a = perfScale(t),
      s = t / 100,
      n = (i, r) => Math.max(0, Math.round(i * a + randRange(-r, r)));
    switch (e.pos) {
      case "QB":
        return {
          "Pass Yds": n(207, 28),
          TD: n(1.6, 1),
          INT: Math.max(0, Math.round((1 - s) * 2.4 + randRange(-0.6, 0.7)))
        };
      case "RB":
        return { "Rush Yds": n(84, 18), TD: n(0.9, 1), YPC: (2.6 + s * 4.6 + randRange(-0.5, 0.5)).toFixed(0) };
      case "WR":
        return { Rec: n(4.6, 1.5), Yds: n(99, 16), TD: n(0.6, 1) };
      case "TE":
        return { Rec: n(3.4, 1.2), Yds: n(58, 12), TD: n(0.45, 1) };
      case "OL":
        return {
          Pancakes: n(3.4, 1.5),
          "Sacks Allow": Math.max(0, Math.round((1 - s) * 3)),
          Grade: (s * 40 + 55 + randRange(-5, 5)).toFixed(0)
        };
      case "DL":
        return { Tackles: n(3.2, 1.4), Sacks: n(0.8, 0.8), TFL: n(1, 0.8) };
      case "LB":
        return { Tackles: n(7, 2), Sacks: n(0.4, 0.7), TFL: n(1.1, 0.8) };
      case "CB":
        return { Tackles: n(3.2, 1.4), PDef: n(1.3, 0.8), INT: n(0.3, 0.7) };
      case "S":
        return { Tackles: n(5.4, 2), PDef: n(0.9, 0.8), INT: n(0.35, 0.7) };
    }
    return {};
  }
  function simSeason(e) {
    const t = LEVELS[e.level],
      a = PROGRAMS[e.training || "balanced"],
      s = {};
    const _fv124 = planFateSeasonV124(e, e.training || "balanced");
    ATTR_KEYS.forEach(R => (s[R] = e.attrs[R] || 0));
    const n = e.eventChoice;
    let i = 0,
      r = 1,
      l = 0;
    if (n) {
      const R = e.level,
        O = 6 + R * 4;
      n.perf && (i += n.perf);
      const Pe = nodeLvl("genius") > 0 || state.path === "prodigy";
      if (n.reqAttr) {
        const Ae = rivalReqV128(n.reqBase, R);
        i += Pe || (e.attrs[n.reqAttr] || 0) >= Ae ? 0 : -(n.perf || 0) * 1.6;
      } /* v128: a locked choice should never reach here, but a loaded save might carry one */
      if (n.gritReqBase) {
        const Ae = eventReq(n.gritReqBase, R);
        i += Pe || e.attrs.grit >= Ae ? 0 : -(n.perf || 0) * 1.6;
      }
      if (n.awareReqBase) {
        const Ae = eventReq(n.awareReqBase, R);
        i += Pe || e.attrs.awareness >= Ae ? 0 : -(n.perf || 0) * 1.6;
      }
      if (
        (n.inj && (r += n.inj * 3),
        n.growth && (l += n.growth),
        n.bench && Math.random() < n.bench && (i -= 14),
        n.focusGainBase)
      ) {
        const Ae = 1 + nodeLvl("combineKing") + (state.path === "prodigy" ? 1 : 0);
        for (const De in n.focusGainBase)
          e.attrs[De] = clamp99((e.attrs[De] || 10) + Math.round(n.focusGainBase[De] * 0.25 * O) * Ae, 1, attrCap());
      }
      (n.gritGainBase && (e.attrs.grit = clamp99(e.attrs.grit + Math.round(n.gritGainBase * 0.3 * O), 1, attrCap())),
        n.awareGainBase &&
          (e.attrs.awareness = clamp99(e.attrs.awareness + Math.round(n.awareGainBase * 0.3 * O), 1, attrCap())),
        n.disciplineGainBase &&
          (e.attrs.discipline = clamp99(
            (e.attrs.discipline || 10) + Math.round(n.disciplineGainBase * 0.3 * O),
            1,
            attrCap()
          )));
    }
    e.contracts || (rollSeasonMod(e), genContracts(e));
    let d = 0,
      c = 0,
      u = 0,
      h = 100,
      p = 0,
      m = 0,
      y = 0,
      C = 0,
      V = 0;
    const P = [],
      v = e.weekResults ? e.weekResults.filter(R => !R.playoff) : null,
      j = v && v.length === t.games && v.every(R => R.played) ? v : null;
    const _vmV128 = (n && n.varMult) || 1,
      _rwV128 =
        n && n.rivalV128
          ? rivalWeekV128(t.games)
          : -1; /* v128: VARIANCE is a number now — the same reshape around 50 the persona sliders use */
    for (let R = 0; R < t.games; R++) {
      let O;
      if (j) O = j[R];
      else {
        O = __aiSeasonGame(e, { perfSeed: i, oppBoost: R === _rwV128 ? TU("rivalOppBoost", 0.38) : 0 });
        if (_vmV128 !== 1) O.perf = clamp99(Math.round(50 + (O.perf - 50) * _vmV128), 1, 100);
        if (R === _rwV128) O.rivalV128 = !0;
        const Pe = (1 - nodeLvl("medic") * 0.05 - nodeLvl("unstoppable") * 0.15) * pathVal("injuryMult", 1),
          Ae = 1 - nodeLvl("motor") * 0.015;
        O.injured &&
          (O.injured =
            Math.random() < injPlanMultV54(a) * r * Math.max(0.1, Pe) * Ae * (O.rivalV128 ? RIVAL_MULT_V128() : 1));
      }
      /* v128: the rivalry costs double */ ((d += O.perf),
        (c = Math.max(c, O.perf)),
        (h = Math.min(h, O.perf)),
        O.perf >= 72 && p++,
        O.perf >= 84 && m++,
        O.injured && u++,
        (C += O.us),
        (V += O.them),
        O.won && y++,
        P.push(O));
    }
    const U = d / t.games,
      ce = P.length - y,
      Y = y / P.length,
      _ = playoffRoundNames(e.level);
    let ie = [],
      B = !1,
      b = 0,
      pe = _.length > 0 && madePlayoffs(y, P.length);
    if (e.weekResults && e.playoffState) {
      const R = e.playoffState;
      ((pe = R.qualified),
        (B = !!R.champion),
        (ie = e.weekResults.filter(O => O.playoff && O.played)),
        (b = ie.filter(O => O.won).length));
    } else if (pe) {
      for (let R = 0; R < _.length; R++) {
        const O = cn(e, R);
        if (((O.played = !0), ie.push(O), O.won)) b++;
        else break;
      }
      B = b === _.length;
    }
    ie.forEach(R => {
      (R.perf >= 72 && p++, R.perf >= 84 && m++, R.injured && u++, (c = Math.max(c, R.perf)));
    });
    // v17 pick sixes — season total summed from the AI-simmed box scores (career total
    // is accumulated per-game in the V11 resolver ca(), so it is not re-added here)
    const _pick6 = [...P, ...ie].reduce((_n, R) => _n + ((R && R.statLine && R.statLine.pick6) || 0), 0);
    const Q = {};
    P.forEach(R => {
      const O = gr(e, R.perf);
      for (const Pe in O) {
        const Ae = parseFloat(O[Pe]);
        Q[Pe] = (Q[Pe] || 0) + (isNaN(Ae) ? 0 : Ae);
      }
    });
    for (const R in Q) Q[R] = Math.round(Q[R]);
    const Me = 1 + nodeLvl("talent") * 0.018 + nodeLvl("gym") * 0.015 + nodeLvl("mastermind") * 0.025,
      Fe = nodeLvl("prodigy") > 0 && e.seasonsSinceStart < 3 ? 1.16 : 1,
      he = clamp99((U - 32) / 58, 0, 1.15),
      Mt =
        b * 0.035 +
        (B ? 0.06 : 0) +
        (P.some(R => R && R.rivalV128 && R.won)
          ? TU("rivalGrowth", 0.06)
          : 0) /* v128: everything the rivalry pays lands double, growth included */,
      da = clamp99(1 - u * 0.055, 0.72, 1);
    let xt = 1;
    (hasTrait(e, "lateBloomer") && (xt *= e.age >= 16 ? 1.07 : 0.95),
      hasTrait(e, "slowStarter") && (xt *= e.seasonsSinceStart < 3 ? 0.94 : 1.05));
    const ua =
        treeFx("eGrowth") +
        gearFx("growth") +
        Math.max(0, tierGrowth(e) - 1) +
        Math.max(0, pathVal("growthMult", 1) - 1) +
        Math.max(0, l),
      fa = clamp99(1 + ua * 0.22, 0.88, 1.34),
      xe = clamp99(e.potential * Me * Fe * xt * da * fa, 0.62, 1.58),
      st = (0.42 + he * 1.18 + Mt) * xe,
      Tt =
        (0.34 + he * 0.42) *
        (1 + (a.priorityBonus || 0)) *
        (1 + nodeLvl("quickstudy") * 0.035) *
        (hasTrait(e, "gymRat") ? 1.08 : 1) *
        (_fv124 ? _fv124.growth : 1) *
        (1 + gearV147("training")),
      Rt = a.focus ? 0 : (0.16 + he * 0.12) * (1 + (a.priorityBonus || 0)),
      Ye = POSITIONS[e.pos].w,
      $ = Math.max(...Object.values(Ye)),
      J = {};
    ATTR_KEYS.forEach(R => {
      const O = Ye[R] ? 0.92 + 0.16 * (Ye[R] / $) : 0.86;
      let Pe = st * O + Rt;
      (a.focus && a.focus.includes(R) && (Pe += Tt), (J[R] = Math.max(0, Pe)));
    });
    const w = Object.values(state.tree || {}).reduce((R, O) => R + O, 0),
      k = clamp99(
        30 +
          effectivePrestige(state.prestige) * 1.9 +
          w * 0.68 +
          nodeLvl("freak") * 12 +
          nodeLvl("superhuman") * 20 +
          nodeLvl("primeGenes") * 9 +
          treeFx("ceilPlus") +
          chaosTotal() * 1.6 +
          (pathVal("growthMult", 1) - 1) * 36 +
          (state.path === "phenom" ? 30 : 0),
        30,
        attrCap()
      );
    e.growthBank = e.growthBank || {};
    const T = {};
    for (const R of ATTR_KEYS) {
      const O = e.attrs[R] || 1;
      const kR = Math.min(attrCap(), Math.round(k * ((e.statCeilV17 && e.statCeilV17[R]) || 1)));
      let Pe = clamp99(1 - (O / attrCap()) * 0.55, 0.35, 1);
      if (O >= kR - 18) {
        const Ro = (O - (kR - 18)) / 18;
        Pe *= clamp99(1 - Ro * 0.88, 0.08, 1);
      }
      const Ae = J[R] * Pe,
        De = (e.growthBank[R] || 0) + Ae,
        _t = Math.max(0, Math.floor(De + 1e-9));
      ((e.growthBank[R] = De - _t), _t > 0 && ((e.attrs[R] = clamp99(O + _t, 1, attrCap())), (T[R] = _t)));
    }
    e.potentialCeil = Math.round(k);
    const K = {};
    if (
      (ATTR_KEYS.forEach(R => (K[R] = e.attrs[R] || 0)),
      a.cost && !nodeLvl("workhorse") && (_fv124 ? _fv124.tradeMul : 1) > 0)
    )
      for (const R in a.cost)
        e.attrs[R] = clamp99(e.attrs[R] + Math.round(a.cost[R] * (_fv124 ? _fv124.tradeMul : 1)), 1, attrCap());
    nodeLvl("nutrition") && e.body && (e.body.muscle = clamp99(e.body.muscle + nodeLvl("nutrition") * 2, 20, 95));
    const g =
        nodeLvl("coachable") +
        nodeLvl("vet") * 2 +
        nodeLvl("proDay") +
        nodeLvl("mastermind") * 2 +
        pathVal("bonusPoints", 0) +
        (hasTrait(e, "coachsSon") ? 1 : 0),
      N = a.bonusPoints || 0,
      H = m * (hasTrait(e, "bigGameHunter") ? 2 : 1) + Math.floor(p / 3);
    let oe = Math.round(y * 0.5);
    (Y >= 0.9 ? (oe += 4) : Y >= 0.7 ? (oe += 2) : Y < 0.35 && (oe -= 1),
      (oe += b * 2),
      B && (oe += 4 + Math.round(e.level * 0.8)),
      (oe = Math.max(0, oe)));
    let be = 0,
      ke = !1;
    if (
      B &&
      ((be = Math.round(
        (2 + Math.round(e.level * 0.8)) * (1 + treeFx("titleMult") + seasonModFx("ppMult")) * chaosPPMult() * eraMult()
      )),
      bankPPV136(be, "title"),
      (e.titles = (e.titles || 0) + 1),
      (state.titlesWon = (state.titlesWon || 0) + 1),
      e.level >= 7)
    ) {
      const R = e.level >= 8;
      ((state.rings = (state.rings || 0) + (R ? 3 : 1)),
        (e.nflRings = (e.nflRings || 0) + 1),
        (ke = !0),
        !state.chaosUnlocked && chaosUnlockReady(e)
          ? ((state.chaosUnlocked = !0),
            (state.chaosCap = Math.max(state.chaosCap || 0, 6)),
            typeof document < "u" &&
              byId("toast") &&
              showToast("🔥 CHAOS MODE UNLOCKED — mastery requirements complete!"))
          : !state.chaosUnlocked &&
            typeof document < "u" &&
            byId("toast") &&
            showToast("💍 Ring earned. Chaos still requires: " + chaosUnlockReq(e)),
        state.chaosUnlocked &&
          chaosMaxed() &&
          ((state.chaosCap = (state.chaosCap || 0) + (R ? 10 : 6)),
          typeof document < "u" &&
            byId("toast") &&
            showToast("⛓️ CHAOS CLEARANCE — capacity raised to " + state.chaosCap + "!")),
        tryNextEra() &&
          typeof document < "u" &&
          byId("toast") &&
          showToast("🌌 NEW ERA: " + eraName() + " — permanent +20% PP!"),
        dropGear(R ? 3 : 2, "Championship loot"));
    }
    const ge = 4 + e.level * 3,
      $e =
        Math.round((U / 94) * ge + ge * 0.6 + e.attrs.awareness * 0.02) +
        g +
        N +
        H +
        oe +
        Math.floor(treeFx("pointsFlat") + gearFx("pointsFlat") + seasonModFx("pointsFlat") + gearV147("points")) +
        repsV146(),
      de = prodStats(e.pos, U, e.level),
      ne = Object.values(state.tree || {}).reduce((R, O) => R + O, 0),
      ue = 30 + effectivePrestige(state.prestige) * 0.65 + Math.sqrt(ne) * 0.75,
      me = 45 + (playerOvr(e) - t.need) * 0.85,
      Pt = [28, 35, 43, 52, 62, 72, 78, 84, 90][e.level] || 50,
      _e = e.performanceExpectationV12 || 0,
      Va = clamp99(Math.max(Pt, me, ue, _e), 26, 95),
      Oa = expectedGrade(U, Va, e.snapShare || 0.5, 58 + e.level * 2).grade;
    e.performanceExpectationV12 = _e ? _e * 0.45 + U * 0.55 : U * 0.9;
    const Te = [],
      Ba = e.level >= 5;
    (B
      ? Te.push({
          icon: "💍",
          name:
            (e.level >= 7 ? "League Champion" : e.level >= 5 ? "National Champion" : "Champions") + " — " + teamName(e)
        })
      : b > 0 && Te.push({ icon: "🎟️", name: "Playoff Run (" + b + " win" + (b > 1 ? "s" : "") + ")" }),
      Y >= 0.999 && B && Te.push({ icon: "💯", name: "Perfect Season" }),
      U >= 83
        ? Te.push({ icon: "🏆", name: Ba ? "League MVP" : "State Player of the Year" })
        : U >= 86
          ? Te.push({ icon: "🏅", name: "Player of the Year" })
          : U >= 74
            ? Te.push({ icon: "⭐", name: Ba ? "All-American" : "All-Conference" })
            : U >= 72
              ? Te.push({ icon: "✅", name: "All-District" })
              : U >= 64 && Te.push({ icon: "🎖️", name: "Team Captain" }),
      m >= 3
        ? Te.push({ icon: "🔥", name: `${m}× Player of the Week` })
        : c >= 95 && Te.push({ icon: "💥", name: "Game of the Week" }),
      p >= Math.ceil(t.games * 0.7) && Te.push({ icon: "📈", name: "Model of Consistency" }),
      u === 0 && U >= 62 && Te.push({ icon: "🩹", name: "Ironman (0 injuries)" }));
    _pick6 > 0 &&
      ((de.pick6 = _pick6), Te.push({ icon: "🏈", name: _pick6 + "× Pick Six" + (_pick6 > 1 ? "es" : "") }));
    const qe = POS_STATS[e.pos]?.primary;
    if (qe && de[qe] != null) {
      const R = baselineStats(e.pos, e.level)[qe] || 1;
      if (de[qe] >= R * 1.4) {
        const O = {
          passYds: "Passing Title",
          rushYds: "Rushing Title",
          recYds: "Receiving Title",
          tackles: "Tackle Champion",
          sacks: "Sack Leader",
          pd: "Lockdown Award",
          grade: "Trench Award"
        };
        O[qe] && Te.push({ icon: "👑", name: O[qe] });
      }
    }
    (nodeLvl("compound") > 0 &&
      bankPPV136(
        Math.min(50 * nodeLvl("compound"), Math.round((state.pp + bankedV136()) * 0.04 * nodeLvl("compound"))),
        "compound"
      ) /* v134 Apex: Compound Interest */,
      (e.lastGains = T),
      (e.points += $e),
      e.seasonsAtLevel++,
      e.totalSeasons++,
      e.seasonsSinceStart++,
      e.age++,
      (e.awards = (e.awards || []).concat(Te)),
      lr(e));
    const Ct = nodeLvl("sculpt");
    if (Ct && e.body && e.pos) {
      const R = POS_BODY[e.pos],
        O = Ct;
      (e.body.height < R.h[0]
        ? (e.body.height = Math.min(R.h[0], e.body.height + 1))
        : e.body.height > R.h[0] && (e.body.height = Math.max(R.h[0], e.body.height - 1)),
        e.body.weight < R.w[0]
          ? (e.body.weight = Math.min(R.w[0], e.body.weight + O * 2))
          : e.body.weight > R.w[0] && (e.body.weight = Math.max(R.w[0], e.body.weight - O * 2)),
        (e.body.muscle = clamp99(e.body.muscle + O, 20, 95)));
    }
    const se = playerOvr(e);
    ((state.recordOvr || 0) < se && (state.recordOvr = se), (e.peakOvr = Math.max(e.peakOvr || 0, se)));
    let X = 0;
    e.level >= 7 &&
      ((e.nflSeasons = (e.nflSeasons || 0) + 1), (X = al(e) * (e.level >= 8 ? 2 : 1)), bankPPV136(X, "season"));
    /* v139: the second, UFF-only decline is gone — ageCutV139 in dc() is the one cut */ let Ke = !1;
    if (e.nemesis) {
      const R = POS_STATS[e.pos],
        O = R.stats.find(_t => _t.key === R.primary),
        Pe = 0.62 + Math.min(0.9, (e.totalSeasons || 0) * 0.04),
        Ae = O.per[e.level] * (O.rate ? 1 : t.games) * (0.45 + Pe * 0.85),
        De = de[O.key] || 0;
      ((Ke = O.lowerBetter ? De <= Ae : De >= Ae),
        Ke &&
          ((e.nemesis.beaten = (e.nemesis.beaten || 0) + 1),
          bankPPV136(Math.round(2 * chaosPPMult() * eraMult()), "nemesis")));
    }
    ((e.lastSeasonLine = { level: e.level, pos: e.pos, statLine: { ...de }, avg: Math.round(U) }),
      (e.lastTeamRecord = {
        level: e.level,
        wins: y,
        losses: ce,
        pf: C,
        pa: V,
        champion: B,
        roundsWon: b,
        qualified: pe
      }));
    const So = baselineStats(e.pos, e.level),
      Mo = advanceChance(se, e.level, e.pos, U, tierComp(e)),
      xo = {
        avg: Math.round(U),
        best: c,
        statLine: de,
        teamWins: y,
        standoutGames: p,
        injuries: u,
        beatNemesis: Ke,
        playoffs: { qualified: pe, roundsWon: b, champion: B }
      },
      To = Xr(e, xo);
    return {
      avg: Math.round(U),
      best: c,
      worst: h,
      injuries: u,
      newOvr: se,
      pointsEarned: $e,
      contractResults: To,
      salary: X,
      beatNemesis: Ke,
      power: Math.round(playerPower(e)),
      gains: T,
      attrsBefore: s,
      attrsAfter: K,
      priorityStats: a.focus || [],
      developmentModel: "performance-mainline-v1",
      agg: Q,
      sampleGames: P,
      grade: gradeFloorV139(Oa, Mo),
      awards: Te,
      training: e.training,
      statLine: de,
      target: So,
      chance: Mo,
      standoutGames: p,
      dominantGames: m,
      perfPoints: H,
      teamBonus: oe,
      tier: currentTier(e),
      record: `${y}-${ce}`,
      teamWins: y,
      teamLosses: ce,
      ptsFor: C,
      ptsAgainst: V,
      winPct: Y,
      playoffs: {
        qualified: pe,
        roundsWon: b,
        champion: B,
        rounds: _.length,
        titlePP: be,
        ringEarned: ke,
        games: ie.map(R => ({ perf: R.perf, us: R.us, them: R.them, won: R.won, round: R.round }))
      }
    };
  } /* ===== v52 NATIONAL RANK — standing follows PRODUCTION, not just the rating =====
   * This used to read the overall rating alone, through a logistic centred on
   * A[level].need-8. Mid-season that is almost exactly where a developing kid sits
   * — because OVR only reaches `need` at the END of a level — so it parked nearly
   * everyone at the 50th percentile. A player the leaders board had at #18 in the
   * country was told he was #200k of 420k on the very same screen.
   *
   * Standing now blends the rating with what he is ACTUALLY producing, measured by
   * kr() — the same function the leaders board ranks him with — so the two can no
   * longer contradict each other. With no stat line yet (a fresh level) there is
   * nothing to measure and the rating carries it alone, as before. */
  const RANK_OVR_K = 2; // how hard the rating can push a rank up or down
  function nationalRank(e, t) {
    const pool = NAT_POOL(e.level),
      pos = POS_POOL(e.level);
    const s = LEVELS[e.level].need - 8,
      n = 6 + e.level * 1,
      i = 1 + nodeLvl("spotlight") * 0.06;
    const ovrPct = 1 / (1 + Math.exp(-1.702 * (((t - s) / n) * i)));
    let posRank = null,
      prodRank = null;
    try {
      const ln = seasonPaceLine(e),
        cfg = POS_STATS[e.pos],
        prim = cfg && cfg.primary;
      if (ln && ln.line && prim && ln.line[prim] != null) {
        const sd = cfg.stats.find(x => x.key === prim);
        if (sd) {
          // Anchor on the LEADERS BOARD's own answer, and work in RANK space, not
          // percentile space. Percentiles are useless out in the tail — nudging a
          // #18-in-the-country by a tenth of a percentile moves him to #4,000 — so
          // the rating scales his rank instead: a weak one multiplies it, a strong
          // one divides it. The two screens can now differ by a sensible factor,
          // never by orders of magnitude.
          prodRank = prodRankFor(e.level, sd, ln.line[prim]);
          posRank = Math.max(1, Math.min(pos, Math.round(prodRank * (1 + (0.5 - ovrPct) * RANK_OVR_K))));
        }
      }
    } catch (_e) {}
    // no stat line at this level yet — nothing produced to measure, so the rating
    // carries it alone, exactly as it always did
    if (posRank == null) posRank = Math.max(1, Math.round((1 - ovrPct) * pos));
    const d = Math.max(1, Math.min(pool, Math.round(posRank * (pool / Math.max(1, pos)))));
    const l = 1 - (d - 1) / Math.max(1, pool);
    return {
      rank: d,
      of: pool,
      pct: Math.round(l * 100),
      posRank,
      posSize: pos,
      ovrPct: Math.round(ovrPct * 100),
      prodRank
    };
  }
  // every reference deferred: the pools are declared further down the file, so an
  // eager one lands in the temporal dead zone at parse time
  window.__RANK_V52 = {
    sn: (e, t) => nationalRank(e, t),
    kr: (l, sd, v) => prodRankFor(l, sd, v),
    natPool: e => NAT_POOL(e),
    posPool: e => POS_POOL(e),
    prodW: () => RANK_OVR_K,
    Ne: () => POS_STATS,
    ovr: e => playerOvr(e),
    line: e => seasonPaceLine(e)
  };
  function fmtInt(e) {
    return Math.round(e).toLocaleString("en-US");
  }
  const NEMESIS_FIRST = [
      "Jaylen",
      "Trey",
      "DeAndre",
      "Cooper",
      "Zane",
      "Marcus",
      "Kai",
      "Bryce",
      "Tank",
      "Julian",
      "Semaj",
      "Rome",
      "Deacon",
      "Axel",
      "Micah",
      "Dre",
      "Tobias",
      "Knox",
      "Cyrus",
      "Jett",
      "Omari",
      "Beau",
      "Lincoln",
      "Rashad",
      "Quincy"
    ],
    NEMESIS_LAST = [
      "Washington",
      "Kingsley",
      "Duval",
      "Steele",
      "Achebe",
      "Fontaine",
      "Rhodes",
      "Beckett",
      "Okoro",
      "Silva",
      "Cross",
      "Vega",
      "Dunham",
      "Frost",
      "Mercer",
      "Bell",
      "Hargrove",
      "Whitfield",
      "Stroud",
      "Ansah",
      "Calloway",
      "Pryor",
      "Redmond",
      "Bishop",
      "Lattimore"
    ],
    Ii = [2e5, 12e4, 66e3, 31e3, 12e3, 1800, 140, 64, 11];
  /* ===== v52 ONE NATIONAL POPULATION — every rank divides the same pie =====
   * There were THREE population tables for the same thing: A[].slots on the level
   * table, a hardcoded array inside sn() for the rank card, and Ii for the leaders
   * board. Ii turned out to be A[].slots/9 at nearly every level (the board was
   * right); sn()'s copy had drifted badly — Middle School read 420k against the
   * table's 600k, JV 95k against 280k — so the two screens quoted different
   * denominators for the same population. Both now derive from A[].slots. */

  /* ===== v125 THE TOP OF THE COUNTRY IS ABSURD =====
   * One line decided every national rank in the game: br() mapped production onto a quality
   * factor with `(a/(n*s)-.45)/.85`, so the #1 player in the country was whoever produced 1.85x
   * his level's baseline -- at EVERY level, from Pee Wee to the UFF. A perf-75 season put you top
   * five in America; a perf-85 season was #1 of 1.8 million kids. The board agreed with itself
   * (Ni() generated its top 25 off the same .85) so nothing ever looked wrong; it was just far
   * too easy.
   *
   * The elite line is per-LEVEL now, and it is written in the one unit the whole game already
   * shares: a multiple of the UFF per-game baseline, `per[7]`. The national leader at Pee Wee
   * puts up FIVE TIMES what a UFF starter does -- 425 receiving yards a game, 525 rushing -- and
   * the multiple slides down through the grades (4.2x, 3.4x, 3.0x, 2.6x at Varsity) to something
   * merely great in college and the pros. That is not a joke number: the gap between a genuine
   * national-best nine-year-old and the rest of his league IS that wide, and it is the reason a
   * Pee Wee #1 is worth being.
   *
   * eliteRV125 turns that into the multiple of THIS level's own baseline that br() should call
   * 1.65, and the slope falls out of it. Nothing else moves: the median is still the median (a
   * baseline season is still a baseline season), the rate stats and the lower-is-better stats are
   * untouched -- a yards-per-carry is not five times anything -- and the floor is the old 1.85, so
   * no level ever gets EASIER than it was. Ni() draws its top 25 off the same number, so the board
   * shows the stats the rank now demands. `window.__V125`; `rankcheck.mjs` / `v125check.mjs`. */
  const ELITE_NFL_V125 = [5.0, 4.2, 3.4, 3.0, 2.6, 2.3, 2.4, 2.6, 2.8]; // the national leader, as a multiple of the UFF baseline
  function eliteRV125(level, sd) {
    if (!sd || sd.rate || sd.lowerBetter) return 1.85; // a rate is not 5x anything, and neither is an INT count
    const per = sd.per || [],
      pro = per[7] != null ? per[7] : per[per.length - 1],
      own = per[level];
    if (!pro || !own) return 1.85;
    return clamp99(((ELITE_NFL_V125[level] != null ? ELITE_NFL_V125[level] : 2.6) * pro) / own, 1.85, 20);
  }
  function eliteSlopeV125(level, sd) {
    return (eliteRV125(level, sd) - 0.45) / 1.65;
  } // what br() divides by
  window.__V125 = {
    nfl: ELITE_NFL_V125,
    r: (l, sd) => eliteRV125(l, sd),
    slope: (l, sd) => eliteSlopeV125(l, sd),
    leader: (l, pos, key) => {
      const c = POS_STATS[pos],
        sd = c && c.stats.find(x => x.key === (key || c.primary));
      if (!sd) return null;
      const r = eliteRV125(l, sd);
      return {
        r,
        perGame: +(sd.per[l] * r).toFixed(1),
        season: Math.round(sd.per[l] * r * LEVELS[l].games),
        vsPro: +((sd.per[l] * r) / sd.per[7]).toFixed(2)
      };
    }
  };
  const NAT_POOL = e => (LEVELS[e] && LEVELS[e].slots) || 5e3; // everyone at this level
  const POS_POOL = e => Math.max(1, Math.round(NAT_POOL(e) / 9));
  function eliteScore(e, t, a) {
    const s = LEVELS[e].games,
      n = t.per[e];
    return t.rate
      ? (a / n - 0.82) / 0.32
      : t.lowerBetter
        ? (1.2 - a / (n * s)) / 0.5
        : (a / (n * s) - 0.45) / eliteSlopeV125(e, t);
  }
  function prodRankFor(e, t, a) {
    const s = POS_POOL(e),
      n = eliteScore(e, t, a);
    if (n <= 0.06) return s;
    if (n < 0.4) {
      const r = 26 * Math.pow(2.3, 5),
        l = (n - 0.06) / 0.34;
      return clamp99(Math.round(s - (s - r) * l), 1, s);
    }
    const i = Math.round(26 * Math.pow(0.92 / n, 5));
    return clamp99(i, 1, s);
  }
  function genNationalLeaders(e, t, a) {
    const s = mulberry32((a || 1) + t.charCodeAt(0) * 7919 + (t.charCodeAt(1) || 7) * 131 + e * 104729),
      n = POS_STATS[t],
      i = LEVELS[e].games,
      r = [];
    for (let l = 0; l < 25; l++) {
      const d = 1.65 - Math.pow(l / 24, 0.8) * 0.73 + (s() - 0.5) * 0.05,
        c = {};
      (n.stats.forEach(u => {
        const h = 0.95 + s() * 0.1,
          p = u.per[e],
          sl = eliteSlopeV125(e, u);
        u.rate
          ? (c[u.key] = +(p * (0.82 + d * 0.32) * h).toFixed(1))
          : u.lowerBetter
            ? (c[u.key] = Math.max(1, Math.round(p * i * Math.max(0.25, 1.2 - d * 0.5) * h)))
            : (c[u.key] = Math.round(
                p *
                  i *
                  (0.45 + d * sl) *
                  h *
                  (sl <= 0.88 && (u.key === "rushYds" || u.key === "recYds") ? 1 + Math.max(0, d - 0.9) * 0.55 : 1)
              ));
      }),
        r.push({
          name:
            NEMESIS_FIRST[Math.floor(s() * NEMESIS_FIRST.length)] +
            " " +
            NEMESIS_LAST[Math.floor(s() * NEMESIS_LAST.length)],
          team: TOWNS[Math.floor(s() * TOWNS.length)],
          q: d,
          line: c,
          me: !1
        }));
    }
    return r;
  }
  function seasonPaceLine(e) {
    if (e.weekResults) {
      const t = e.weekResults.filter(s => s.played && !s.playoff);
      if (!t.length) return null;
      const a = t.reduce((s, n) => s + n.perf, 0) / t.length;
      return { line: prodStats(e.pos, a, e.level), pace: !0, games: t.length };
    }
    return e.lastSeasonLine && e.lastSeasonLine.level === e.level && e.lastSeasonLine.pos === e.pos
      ? { line: e.lastSeasonLine.statLine, pace: !1 }
      : null;
  }
  function $r(e, t) {
    const a = mulberry32(t + e.level * 7907 + 13),
      s = LEVELS[e.level].games,
      n = MASCOTS /* v123: the standings draw from the same crest-matched pool */,
      i = TOWNS.filter(m => m !== (e.teamIdentity && e.teamIdentity.town)),
      r = [];
    let l = 0,
      d = 0,
      c = 0,
      u = 0,
      h = !1;
    if (e.weekResults)
      e.weekResults
        .filter(m => m.played && !m.playoff)
        .forEach(m => {
          ((h = !0), m.won ? l++ : d++, (c += m.us), (u += m.them));
        });
    else if (e.lastTeamRecord && e.lastTeamRecord.level === e.level) {
      const m = e.lastTeamRecord;
      ((l = m.wins), (d = m.losses), (c = m.pf), (u = m.pa), (h = !0));
    }
    r.push({ name: teamName(e), me: !0, w: l, l: d, pf: c, pa: u });
    const p = Math.max(1, l + d);
    for (let m = 0; m < 9; m++) {
      const y = 0.18 + a() * 0.68;
      let C = 0,
        V = 0,
        P = 0;
      for (let j = 0; j < p; j++)
        (a() < y && C++, (V += Math.round(10 + y * 24 + a() * 14)), (P += Math.round(12 + (1 - y) * 22 + a() * 14)));
      const v = i[Math.floor(a() * i.length)];
      r.push({ name: v + " " + n[Math.floor(a() * n.length)], me: !1, w: C, l: p - C, pf: V, pa: P });
    }
    return (
      r.sort((m, y) => y.w - m.w || y.pf - y.pa - (m.pf - m.pa)),
      { teams: r, haveMine: h, gamesPlayed: p, totalGames: s }
    );
  }
  let leadTab = "leaders",
    leadPos = null,
    leadStat = null;
  function screenLeaders() {
    const e = state.player;
    if (!e || !e.pos) {
      goView("menu");
      return;
    }
    const t = LEVELS[e.level];
    leadPos || (leadPos = e.pos);
    const a = e.seasonSeed || 1;
    let s = "";
    /* v139: the combine year has NO GAMES, so a national leaders board for it was twenty-five
     * invented rushing lines for a week nobody played. The board shows the combine's own numbers
     * instead — yours, against the field's, off the same drills the season screen tested. */
    if (leadTab === "leaders" && combineYearV139(e)) {
      s = combineLeadersV139(e);
    } else if (leadTab === "leaders") {
      const n = POS_STATS[leadPos];
      (!leadStat || !n.stats.some(m => m.key === leadStat)) && (leadStat = n.primary);
      const i = n.stats.find(m => m.key === leadStat),
        r = genNationalLeaders(e.level, leadPos, a);
      let l = leadPos === e.pos ? seasonPaceLine(e) : null;
      (l && r.push({ name: e.name, team: e.teamIdentity ? e.teamIdentity.town : "—", line: l.line, me: !0 }),
        r.sort((m, y) => (i.lowerBetter ? m.line[leadStat] - y.line[leadStat] : y.line[leadStat] - m.line[leadStat])));
      const d = r.findIndex(m => m.me),
        c = l ? (d < 25 ? d + 1 : prodRankFor(e.level, i, l.line[leadStat])) : null,
        u = r.slice(0, 25),
        h = u.some(m => m.me),
        p = n.stats.filter(m => m.key !== leadStat);
      s = `
      <div class="chips">${Object.keys(POSITIONS)
        .map(m => `<button class="chip ${m === leadPos ? "on" : ""}" onclick="setLeadPos('${m}')">${m}</button>`)
        .join("")}</div>
      <div class="chips" style="margin-top:6px">${n.stats.map(m => `<button class="chip gold ${m.key === leadStat ? "on" : ""}" onclick="setLeadSort('${m.key}')">${m.name}${m.lowerBetter ? " ▼" : ""}</button>`).join("")}</div>
      ${l && l.pace ? `<div class="threshold-note" style="margin-top:8px">📈 Mid-season: your row shows your <b>full-season pace</b> through ${l.games} game${l.games > 1 ? "s" : ""}.</div>` : ""}
      <div class="card" style="margin-top:10px;padding:10px 10px">
        <div class="lb-hdr"><span class="lbr">#</span><span class="lbn">Player</span><span class="lbv">${i.name}</span><span class="lbo">${p.map(m => m.name).join(" · ")}</span></div>
        ${u
          .map(
            (m, y) => `
          <div class="lb2-row ${m.me ? "me" : ""}">
            <span class="lbr">${y + 1}</span>
            <span class="lbn">${m.me ? "<b>" + escHtml(m.name) + " (You)</b>" : escHtml(m.name)}<small>${escHtml(m.team)}</small></span>
            <span class="lbv">${fmtLeadVal(i, m.line[leadStat])}</span>
            <span class="lbo">${p.map(C => fmtLeadVal(C, m.line[C.key])).join(" · ")}</span>
          </div>`
          )
          .join("")}
        ${
          l && !h
            ? `
          <div class="lb2-gap">···</div>
          <div class="lb2-row me">
            <span class="lbr">${fmtInt(c)}</span>
            <span class="lbn"><b>${escHtml(e.name)} (You)</b><small>${e.teamIdentity ? escHtml(e.teamIdentity.town) : ""}</small></span>
            <span class="lbv">${fmtLeadVal(i, l.line[leadStat])}</span>
            <span class="lbo">${p.map(m => fmtLeadVal(m, l.line[m.key])).join(" · ")}</span>
          </div>`
            : ""
        }
        ${!l && leadPos === e.pos ? '<div class="small center" style="margin-top:8px;color:var(--chalk-dim)">Play a season at this level to enter the national board.</div>' : ""}
      </div>
      ${l ? `<div class="small center" style="margin-top:6px">You rank <b style="color:var(--gold)">#${fmtInt(c)}</b> of ${fmtInt(POS_POOL(e.level))} ${leadPos}s nationally in ${i.name}${c === 1 ? " — 👑 NATIONAL LEADER!" : ""}</div>` : ""}
      ${(() => {
        /* v20: rank thresholds — what national rank realistically gets you to the next level */
        const nx = LEVELS[e.level + 1];
        if (!nx)
          return '<div class="card tight" style="margin-top:10px"><div class="small center">🏔️ You\'re at the top level — there is nowhere left to climb.</div></div>';
        const pool = NAT_POOL(e.level);
        const cur = nationalRank(e, playerOvr(e));
        const _NEED = [0.5, 0.55, 0.62, 0.7, 0.93, 0.985, 0.997, 0.99999];
        const _need = _NEED[Math.min(Math.max(0, e.level), _NEED.length - 1)];
        const _od = pp =>
          Math.round(
            pp >= _need
              ? Math.min(99.5, 72 + ((pp - _need) / Math.max(1e-4, 1 - _need)) * 27.5)
              : Math.max(2, (pp / _need) * 34)
          );
        const _pts = [...new Set([0.99999, 0.999, 1 - (1 - _need) * 0.18, _need, Math.max(0.35, _need - 0.2)])]
          .filter(x => x > 0 && x < 1)
          .sort((a, b) => b - a);
        const tiers = _pts.map(pp => [
          pp,
          _od(pp) + "%",
          pp >= 1 - (1 - _need) * 0.18 ? "near-lock" : pp >= _need ? "clears the bar" : "below the bar — long odds"
        ]);
        let hit = -1;
        const rows = tiers
          .map((tr, ix) => {
            const cut = Math.max(1, Math.round((1 - tr[0]) * pool) + 1);
            const mine = hit < 0 && cur.rank <= cut;
            if (mine) hit = ix;
            return `<div class="tgt-row" style="${mine ? "background:rgba(240,187,69,.10);border-radius:8px;padding:3px 6px;margin:0 -6px" : ""}">
            <span class="tgt-name">${mine ? "▶ " : ""}Top <b>#${fmtInt(cut)}</b> overall</span>
            <span class="tgt-need" style="color:${ix < 2 ? "var(--good)" : ix < 4 ? "var(--gold)" : "#e0a84f"}">${tr[1]} <small style="color:var(--chalk-dim)">${tr[2]}</small></span>
          </div>`;
          })
          .join("");
        return `<div class="card" style="margin-top:12px">
          <div class="l" style="font-size:11px;color:var(--gold);letter-spacing:2px;margin-bottom:6px">🎯 WHAT RANK REACHES ${nx.name.toUpperCase()}</div>
          <div class="small" style="margin-bottom:7px">Of <b>${fmtInt(pool)}</b> players at this level, your national rank drives promotion odds. You sit at <b style="color:var(--gold)">#${fmtInt(cur.rank)}</b>${hit >= 0 ? " — inside a threshold" : " — outside every threshold"}.</div>
          ${rows}
          <div class="threshold-note" style="margin-top:7px">Below these cutoffs, odds fall back to your OVR + production alone — usually a long shot. Climb the board to force the call-up.</div>
        </div>`;
      })()}
    `;
    } else {
      const n = $r(e, a),
        i = 4;
      s = `
      <div class="small" style="margin:4px 0 8px">Your league this season. Finish with a <b style="color:var(--gold)">60%+ win rate</b> to make the playoffs — win it all for a ring.</div>
      <div class="card" style="padding:10px">
        <div class="lb-hdr"><span class="lbr">#</span><span class="lbn">Team</span><span class="lbv">W-L</span><span class="lbo">PF · PA · DIFF</span></div>
        ${n.teams
          .map((r, l) => {
            const d = r.pf - r.pa;
            return `${l === i ? '<div class="cutline">— PLAYOFF CUT —</div>' : ""}
          <div class="lb2-row ${r.me ? "me" : ""}">
            <span class="lbr">${l + 1}</span>
            <span class="lbn">${r.me ? "<b>" + escHtml(r.name) + " (You)</b>" : escHtml(r.name)}</span>
            <span class="lbv">${r.w}-${r.l}</span>
            <span class="lbo">${r.pf} · ${r.pa} · <b style="color:${d >= 0 ? "var(--good)" : "#e08a8a"}">${d >= 0 ? "+" : ""}${d}</b></span>
          </div>`;
          })
          .join("")}
      </div>
      ${n.haveMine ? "" : '<div class="small center" style="margin-top:6px;color:var(--chalk-dim)">Play games this season to post your record.</div>'}
    `;
    }
    ((byId("screen").innerHTML = `
    <div class="eyebrow">${t.name} · ${escHtml(teamName(e))}</div>
    <div class="h1">National Stats</div>
    <div class="btn-row" style="margin:10px 0 4px">
      <button class="branch-tab ${leadTab === "leaders" ? "active" : ""}" style="flex:1;padding:9px;border-radius:9px;border:1px solid ${leadTab === "leaders" ? "var(--gold)" : "var(--line)"};background:${leadTab === "leaders" ? "rgba(240,187,69,.13)" : "transparent"};color:${leadTab === "leaders" ? "var(--gold)" : "var(--chalk-dim)"};font-family:'Oswald';font-weight:600;cursor:pointer" onclick="setStatsTab('leaders')">🏅 STAT LEADERS</button>
      <button class="branch-tab ${leadTab === "standings" ? "active" : ""}" style="flex:1;padding:9px;border-radius:9px;border:1px solid ${leadTab === "standings" ? "var(--gold)" : "var(--line)"};background:${leadTab === "standings" ? "rgba(240,187,69,.13)" : "transparent"};color:${leadTab === "standings" ? "var(--gold)" : "var(--chalk-dim)"};font-family:'Oswald';font-weight:600;cursor:pointer" onclick="setStatsTab('standings')">🏟 STANDINGS</button>
    </div>
    ${s}
  `),
      (byId("dock").innerHTML = `
    <div class="btn-row">
      <button class="btn ghost" onclick="go('rank')">🥊 Recruiting Board</button>
      <button class="btn secondary" onclick="go(S.player.weekResults?'season':'hub')">Back</button>
    </div>`));
  }
  function setStatsTab(e) {
    ((leadTab = e), screenLeaders());
  }
  function setLeadPos(e) {
    ((leadPos = e), (leadStat = null), screenLeaders());
  }
  function setLeadSort(e) {
    ((leadStat = e), screenLeaders());
  }
  function render() {
    syncCounters();
    const e = state.view,
      t = document.querySelector && document.querySelector(".topbar");
    if ((t && (t.style.display = e === "menu" ? "none" : ""), e === "menu")) return screenMenuLegacy();
    if (e === "highscore") return window.__hsRender && window.__hsRender(state);
    if (e === "leaderboard") return window.__lbRender && window.__lbRender(state);
    if (e === "daily") return window.__dailyRender && window.__dailyRender(state);
    if (e === "settings") return screenSettings();
    if (e === "choosePos") return screenChoosePos();
    if (e === "hub") return screenHub();
    if (e === "training") return screenTraining();
    if (e === "event") return screenEvent();
    if (e === "sim") return nl();
    if (e === "season") return screenSeason();
    if (e === "live") return ol();
    if (e === "result") return screenResult();
    if (e === "declineResult") return Ol();
    if (e === "tier") return screenTier();
    if (e === "upgrade") return Bl();
    if (e === "rank") return Dl();
    if (e === "stats") return screenLeaders();
    if (e === "challenges") return rr();
    if (e === "dynasty") return screenDynasty();
    if (e === "hof") return screenHof();
    if (e === "locker") return screenLocker();
    if (e === "shop") return screenPrestige();
    if (e === "path") return screenPath();
    if (e === "gameover") return screenGameOver();
    if (e === "win") return screenWin();
    if (e === "club") return clubV146B();
    screenMenuLegacy();
  }
  function screenMenuLegacy() {
    const e = state.player && state.player.pos,
      t = state.player,
      a = Object.values(state.tree || {}).reduce((s, n) => s + n, 0);
    ((byId("screen").innerHTML = `
    <div class="hero">
      <div class="hero-badge">🏈 A Football Life Simulator</div>
      <div class="hero-title"><span class="lead">RUNNING IT BACK</span><span class="big">FROM PEE WEE<br>TO THE LEAGUE</span></div>
      <div class="hero-rule"></div>
      <div class="hero-tag">Take one player from age 8 to the UFF. Pick a position, train, play live seasons, and survive every cut on the road to greatness.</div>
    </div>
    ${
      e
        ? `<div class="card continue-card tap mt" style="margin-top:16px;border-color:var(--gold)" onclick="go('hub')">
      <div class="continue-ovr">${playerOvr(t)}<small>OVR</small></div>
      <div class="l" style="font-size:10px;color:var(--gold);letter-spacing:2px;margin-bottom:5px">▶ CONTINUE CAREER</div>
      <div class="name-row"><span class="pname">${escHtml(t.name)}</span></div>
      <div class="meta-row"><span class="meta"><b>${LEVELS[t.level].name}</b></span><span class="meta">${t.pos}</span><span class="meta">${"★".repeat(t.stars)}</span><span class="meta">${t.body ? (bd => fmtHeight(bd.height) + " · " + bd.weight + "lb" + (bd.grown ? "" : " · 📈" + fmtHeight(t.body.height)))(bodyOfV112(t)) : ""}</span></div>
    </div>`
        : ""
    }
    <div class="card mt" style="margin-top:14px;">
      <div class="l" style="font-size:10px;color:var(--chalk-dim);letter-spacing:2px;margin-bottom:10px">🏆 YOUR LEGACY${(state.era || 0) > 0 ? ` · <span style="color:#c9b8ff">🌌 ${eraName()}</span>` : ""}${state.path ? ` · <span style="color:${PATHS[state.path].color}">${PATHS[state.path].icon} ${PATHS[state.path].name.toUpperCase()}</span>` : ""}</div>
      <div class="statline">
        <div class="statbox"><div class="n" style="color:var(--gold)">${HONOR_ICON_V130}${state.prestige}</div><div class="l">Honors</div></div>
        <div class="statbox"><div class="n">${state.careers}</div><div class="l">Careers</div></div>
        <div class="statbox"><div class="n" style="color:${state.nflReached ? "var(--good)" : "var(--chalk)"}">${state.nflReached || 0}</div><div class="l">UFF Reached</div></div>
      </div>
      <div class="statline" style="margin-top:10px">
        <div class="statbox"><div class="n">${state.pp}</div><div class="l">Prestige Pts</div></div>
        <div class="statbox"><div class="n">${LEVELS[state.bestLevel].name.split(" ")[0]}</div><div class="l">Best Level</div></div>
        <div class="statbox"><div class="n">${a}</div><div class="l">Upgrades</div></div>
      </div>
      ${
        state.recordOvr || state.titlesWon
          ? `<div class="statline" style="margin-top:10px">
        <div class="statbox"><div class="n" style="color:var(--gold)">${state.recordOvr || "—"}</div><div class="l">Best OVR Ever</div></div>
        <div class="statbox"><div class="n" ${state.rings ? 'style="color:#ff8a80"' : ""}>${state.rings ? state.rings + " 💍" : (state.titlesWon || 0) + " 🏆"}</div><div class="l">${state.rings ? "UFF Rings" : "Titles Won"}</div></div>
        <div class="statbox"><div class="n">${Object.keys(state.challenges || {}).length}/${CHALLENGES.length}</div><div class="l">Challenges</div></div>
      </div>`
          : ""
      }
    </div>
  `),
      (byId("dock").innerHTML = `
    ${
      e
        ? `<button class="btn" onclick="go('hub')">Continue Career</button>
         <div style="height:8px"></div>
         <div class="btn-row">
           <button class="btn secondary" onclick="go('shop')">🌳 Prestige</button>
           <button class="btn secondary" onclick="confirmNew()">New Career</button>
         </div>`
        : `<button class="btn" onclick="startCareer()">▶ Start New Career</button>
         ${state.pp > 0 || a ? `<div style="height:8px"></div><button class="btn secondary" onclick="go('shop')">🌳 Prestige Tree · ${state.pp} PP</button>` : ""}`
    }
    <div style="height:8px"></div>
    <button class="btn" style="background:linear-gradient(90deg,#f0bb45,#e0484f);color:#0b111b;font-weight:700" onclick="go('highscore')">⚡ SCORE ATTACK${state.highScore ? ` · BEST ${state.highScore.toLocaleString()}` : ""}</button>
    <div style="height:8px"></div>
    <div class="btn-row">
      <button class="btn ghost" onclick="go('leaderboard')">🏆 Leaderboard</button>
      <button class="btn ghost" onclick="go('challenges')">🎯 Goals</button>
      <button class="btn ghost" onclick="go('hof')">🏛️ Hall</button>
      <button class="btn ghost" onclick="go('locker')">🎒 Locker</button>
      <button class="btn ghost" onclick="go('settings')">⚙</button>
    </div>
  `));
  }
  function confirmNew() {
    if (abandonedV112()) {
      startCareer();
      return;
    }
    return askV150(
      "Start a Run it back? Your current player will be replaced (no prestige gained unless you finish their career first).",
      { title: "New career", ok: "Start over" }
    ).then(ok => ok && startCareer(!0));
  }
  function toggleRow(e, t, a) {
    const s = settingOn(e);
    return `<div class="toggle-row" onclick="toggleSetting('${e}')">
    <div class="toggle-info"><div class="toggle-label">${t}</div><div class="toggle-desc">${a}</div></div>
    <div class="switch ${s ? "on" : ""}"><i></i></div>
  </div>`;
  }
  function screenSettings() {
    const e = (() => {
      try {
        return (localStorage.getItem(SAVE_KEY) || "").length;
      } catch {
        return 0;
      }
    })();
    ((byId("screen").innerHTML = `
    <div class="eyebrow">Menu</div>
    <div class="h1">Settings & Save</div>
    <div class="card">
      <div class="l" style="font-size:11px;color:var(--gold);letter-spacing:2px;margin-bottom:10px">🎮 LIVE GAME</div>
      ${toggleRow("skipOpp", "Skip opponent drives", "Only watch plays while your team has the ball")}
      ${toggleRow("onlyInvolved", "My plays only", "Jump straight to plays you're personally involved in")}
      ${toggleRow("fastSim", "Faster live sim", "Speed up the default play animation")}
      ${toggleRow("sound", "Sound effects", "Broadcast cues, rewards, and game-impact audio")}
      ${toggleRow("haptics", "Haptic feedback", "Vibration for touchdowns, setbacks, and major choices")}
    </div>
    <div class="card">
      <div class="l" style="font-size:11px;color:var(--gold);letter-spacing:2px;margin-bottom:4px">📐 FIELD VIEW</div>
      <div class="small" style="margin-bottom:10px">Adjust the 2.5D perspective — one dial shrinks the far field, numbers AND players together (always consistent) — plus size, spacing, and zoom.</div>
      ${(function () {
        var L = window.__CAM_MODES_V112 || [{ id: "broadcast", n: "Broadcast", d: "" }];
        var c = Math.max(0, Math.min(L.length - 1, Math.round((state.settings && state.settings.fxCam) || 0)));
        return `<div class="fx-row">
        <div class="fx-head"><span class="fx-label">Camera</span><span class="fx-val" id="fxCam_val">${L[c].n}</span></div>
        <div style="display:grid;grid-template-columns:repeat(2,1fr);gap:6px;margin:2px 0 4px">${L.map(function (m, i) {
          return `<button class="btn camopt112 ${i === c ? "secondary" : "ghost"}" style="padding:8px 4px;font-size:12px" onclick="camModeSet112(${i})">${m.n}</button>`;
        }).join("")}</div>
        <div class="fx-desc" id="fxCam_desc">${L[c].d}</div>
      </div>`;
      })()}
      ${(function () {
        var L = window.__WX_MODES_V144 || [{ id: "auto", n: "Auto", d: "" }];
        var c = Math.max(0, Math.min(L.length - 1, Math.round((state.settings && state.settings.fxWx) || 0)));
        return `<div class="fx-row">
        <div class="fx-head"><span class="fx-label">Weather &amp; time of day</span><span class="fx-val" id="fxWx_val">${L[c].n}</span></div>
        <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:6px;margin:2px 0 4px">${L.map(function (m, i) {
          return `<button class="btn wxopt144 ${i === c ? "secondary" : "ghost"}" style="padding:8px 2px;font-size:11px" onclick="wxModeSet144(${i})">${m.n}</button>`;
        }).join("")}</div>
        <div class="fx-desc" id="fxWx_desc">${L[c].d}</div>
      </div>`;
      })()}
      ${[
        [
          "fxDepth",
          "Field perspective (depth)",
          "How hard everything recedes — 0 is flat top-down; higher = the far (north) field, numbers and players all shrink harder, always in step",
          0,
          0.9,
          0.01,
          0.78
        ],
        ["fxSize", "Player size", "Overall size of every player", 0.5, 1.9, 0.05, 1.32],
        ["fxSpread", "Player spread", "Sideline-to-sideline spacing", 0.7, 1.4, 0.05, 1],
        ["fxZoom", "Field zoom", "How large the field is drawn", 0.85, 1.5, 0.02, 1.16],
        [
          "fxCamZoom",
          "Camera zoom strength",
          "How hard the camera pulls in on a runner who breaks into open space. 0 holds one width for the whole play",
          0,
          1.6,
          0.05,
          1
        ],
        [
          "fxLight",
          "Lighting intensity",
          "How hard the floodlights burn — the wash across the turf, the glow and beams off the masts, and how deep everything's shadow falls. 0 puts the lights out (flat, grey, deep corners); 200% floods the place",
          0,
          2,
          0.05,
          1
        ]
      ]
        .map(function (r) {
          var k = r[0],
            lab = r[1],
            desc = r[2],
            mn = r[3],
            mx = r[4],
            st = r[5],
            df = r[6];
          var val = state.settings && state.settings[k] != null ? state.settings[k] : df;
          return `<div class="fx-row">
        <div class="fx-head"><span class="fx-label">${lab}</span><span class="fx-val" id="${k}_val">${window.__fxFmt(k, val)}</span></div>
        <input class="fx-slider" type="range" min="${mn}" max="${mx}" step="${st}" value="${val}" oninput="fieldFxSet('${k}',this.value,0)" onchange="fieldFxSet('${k}',this.value,1)">
        <div class="fx-desc">${desc}</div>
      </div>`;
        })
        .join("")}
      <button class="btn ghost" style="margin-top:4px" onclick="fieldFxReset()">↺ Reset to defaults</button>
    </div>
    ${(function () {
      const L = lineageV136(),
        sur = famSurnameV139(),
        n = (L.fathers || []).length;
      if (!sur && !n) return "";
      return `<div class="card">
      <div class="l" style="font-size:11px;color:#c9b8ff;letter-spacing:2px;margin-bottom:8px">👨‍👦 FAMILY NAME</div>
      <div class="small">Every generation carries one surname — you name each son yourself. Change it here and it is changed for the whole line: the player you have now and all ${n} father${n === 1 ? "" : "s"} on the books.</div>
      <div style="height:10px"></div>
      <div class="name-edit-v96"><input id="famNameV139" value="${escHtml(sur)}" maxlength="18" autocomplete="off" spellcheck="false" aria-label="Family name"></div>
      <div style="height:8px"></div>
      <button class="btn secondary" onclick="famRenameV139(document.getElementById('famNameV139').value)">Rename the Line</button>
    </div>`;
    })()}
    <div class="card">
      <div class="l" style="font-size:11px;color:var(--chalk-dim);letter-spacing:2px;margin-bottom:8px">💾 SAVE DATA</div>
      <div class="small">Your progress saves automatically after every action, right here on your device. Prestige, upgrades, and your current player all persist.</div>
      <div class="threshold-note mt" style="margin-top:8px">Save size: ${Math.round(e / 1024)} KB · ${state.careers} careers played</div>
    </div>
    <div class="card">
      <div class="l" style="font-size:11px;color:var(--chalk-dim);letter-spacing:2px;margin-bottom:8px">📤 BACKUP / TRANSFER</div>
      <div class="small">Export a backup code to move your save to another device, or import one here.</div>
      <div style="height:10px"></div>
      <button class="btn secondary" onclick="exportSave()">Copy Backup Code</button>
      <div style="height:8px"></div>
      <button class="btn ghost" onclick="importSave()">Paste & Import Code</button>
    </div>
    <div class="card">
      <div class="l" style="font-size:11px;color:var(--blood);letter-spacing:2px;margin-bottom:8px">⚠ DANGER ZONE</div>
      <div class="small">Reset wipes everything — prestige, upgrades, and your player. This can't be undone.</div>
      <div style="height:10px"></div>
      <button class="btn danger" onclick="hardReset()">Erase All Progress</button>
    </div>
    <div class="center" style="padding:8px 0 4px;opacity:.6">
      <div style="font-family:'Oswald';font-weight:700;letter-spacing:2px;font-size:15px">GRID<span style="color:var(--gold)">IRON</span></div>
      <div class="small" style="margin-top:2px">A Football Life Simulator · v5.0 — INTERSTELLAR</div>
    </div>
  `),
      (byId("dock").innerHTML =
        `<button class="btn secondary" onclick="go(S.player&&S.player.pos?'hub':'menu')">Back</button>`));
  }
  function exportSave() {
    try {
      const e = btoa(unescape(encodeURIComponent(JSON.stringify(state))));
      return navigator.clipboard
        ? navigator.clipboard.writeText(e).then(
            () => showToast("Backup code copied!"),
            () => showCodeV150(e)
          )
        : showCodeV150(e);
    } catch {
      showToast("Export failed");
    }
  }
  /* v150 A: the pasted code goes to storage and the page reloads through boot(), so the migrations run on
   * it (the old path set `state = t` and skipped every one of them). `code` skips the paste box. */
  function importSave(code) {
    if (code != null) return importCodeV150(code);
    const D = window.ribDialog;
    return D && D.prompt
      ? D.prompt("Paste your backup code:", "", { title: "Import save", ok: "Next", multiline: !0 }).then(
          e => (e && e.trim() ? importCodeV150(e) : !1)
        )
      : (showToast("Import needs the save panel — use Save File & Backups"), Promise.resolve(!1));
  }
  function hardReset() {
    return askV150("Erase ALL progress permanently? Everything will be lost — this cannot be undone.", {
      title: "Erase everything",
      ok: "Erase",
      danger: !0
    }).then(ok => {
      if (!ok) return !1;
      try {
        window.ribSave && window.ribSave.backups.snapshot("before-erase");
      } catch (e) {}
      return ((state = freshState()), saveGame(), showToast("Progress erased"), goView("menu"), !0);
    });
  } /* ===== v136 D THE LINEAGE — the prestige system is a father's lesson to his son =====
   * A career ends. The next player is not a stranger with the same account behind him — he is the
   * SON, and what the old man learned is what he starts with. That is what the prestige tree always
   * was; it just never said so. `o.lineageV136` is the family: the generation, the surname, the
   * fathers (one record per finished or abandoned career: name, position, the league he reached,
   * how it ended, seasons, peak) and, derived, the running count of years the family has spent on
   * the field. `lineageEndV136` writes the father when a career settles (or is walked away from);
   * `lineageBirthV136` names the son — a new first name, the family's surname (the joke is the
   * coach's) — and bumps the generation. `familyV136()` is the one read every screen makes: the hub,
   * the position screen, the tree, the career-end card, the Hall of Fame, the menu feed and the
   * coach all say it. Nothing in the tree's maths moved: the nodes are the inheritance. */
  function lineageV136() {
    state.lineageV136 || (state.lineageV136 = { gen: 0, surname: "", fathers: [] });
    return state.lineageV136;
  }
  function surnameV136(n) {
    const p = String(n || "")
      .trim()
      .split(/\s+/);
    return p.length > 1 ? p[p.length - 1] : p[0] || "";
  }
  function ordV136(n) {
    const s = ["th", "st", "nd", "rd"],
      v = n % 100;
    return n + (s[(v - 20) % 10] || s[v] || s[0]);
  }
  function familyV136() {
    const L = lineageV136(),
      e = state && state.player,
      gen = Math.max(1, L.gen || 1),
      F = L.fathers || [],
      f = F.length ? F[F.length - 1] : null;
    const years = F.reduce((s, x) => s + (x.seasons || 0), 0) + ((e && e.totalSeasons) || 0);
    return {
      gen,
      ordinal: ordV136(gen),
      surname: L.surname || (e && surnameV136(e.name)) || "",
      years,
      father: f,
      fathers: F,
      nodes: Object.values(state.tree || {}).reduce((s, n) => s + n, 0)
    };
  }
  function fateSayV136(f) {
    if (!f) return "";
    return f.fate === "won"
      ? "made the UFF"
      : f.fate === "walked"
        ? "walked away at " + f.levelName
        : "got cut at " + f.levelName;
  }
  function fatherRecordV136(e, level, fate) {
    const lv = Math.max(0, Math.min(LEVELS.length - 1, level | 0));
    return {
      name: e.name,
      pos: e.pos,
      level: lv,
      levelName: (LEVELS[lv] || {}).name || "",
      fate,
      seasons: e.totalSeasons | 0,
      peak: e.peakOvr || playerOvr(e),
      titles: e.titles || 0,
      gen: Math.max(1, lineageV136().gen || 1),
      origin: e.originNameV11 || ""
    };
  }
  function lineageEndV136(e, level, fate) {
    try {
      const L = lineageV136();
      if (!e || e._lineageDoneV136) return;
      e._lineageDoneV136 = !0;
      L.gen || (L.gen = 1);
      L.surname || (L.surname = surnameV136(e.name));
      L.fathers.push(fatherRecordV136(e, level, fate));
      L.fathers.length > 40 && L.fathers.splice(0, L.fathers.length - 40);
    } catch (_) {}
  }
  function lineageBirthV136(son) {
    try {
      const L = lineageV136();
      if (!L.fathers.length) {
        L.gen = L.gen || 1;
        L.surname || (L.surname = surnameV136(son.name));
        return;
      }
      L.gen = L.fathers.length + 1;
      const f = L.fathers[L.fathers.length - 1];
      son.name = randPick(FIRST_NAMES) + " " + L.surname;
      son.genV136 = L.gen;
      son.fatherV136 = f.name;
    } catch (_) {}
  }
  function lineageRowV136(e) {
    try {
      const F = familyV136();
      return `<div class="meta-row lineage-row-v136" style="margin-top:4px"><span class="meta" style="color:#c9b8ff">👨‍👦 <b>${F.ordinal.toUpperCase()} GENERATION</b> · THE ${escHtml(F.surname.toUpperCase())} LINE · <b>${F.years}</b> FAMILY YEAR${F.years === 1 ? "" : "S"}</span>${F.father ? `<span class="meta" style="color:var(--chalk-dim)">son of <b>${escHtml(F.father.name)}</b>, who ${fateSayV136(F.father)}</span>` : ""}</div>`;
    } catch (_) {
      return "";
    }
  }
  function lineageCardV136(e) {
    try {
      const F = familyV136();
      if (F.gen <= 1 || !F.father) return "";
      const f = F.father;
      return `<div class="card tight lineage-card-v136" style="border-color:#8a74d6"><div class="l" style="font-size:11px;color:#c9b8ff;letter-spacing:2px;margin-bottom:6px">👨‍👦 ${F.ordinal.toUpperCase()} GENERATION · THE ${escHtml(F.surname.toUpperCase())} LINE</div>
    <div class="small">Son of <b>${escHtml(f.name)}</b> (${escHtml(f.pos)}), who ${fateSayV136(f)} after ${f.seasons} season${f.seasons === 1 ? "" : "s"}${f.titles ? ` and ${f.titles} title${f.titles === 1 ? "" : "s"}` : ""}. What he learned is yours: <b style="color:var(--gold)">${F.nodes} lesson${F.nodes === 1 ? "" : "s"}</b> in the tree, and the family's <b>${F.years}</b> years on the field behind you.</div></div>`;
    } catch (_) {
      return "";
    }
  }
  function lineageHofV136() {
    try {
      const F = familyV136();
      if (!F.fathers.length) return "";
      return `<div class="h2">👨‍👦 The ${escHtml(F.surname)} Line <span style="color:var(--chalk-dim);font-size:12px;font-weight:400">— ${F.gen} generation${F.gen === 1 ? "" : "s"} · ${F.years} years on the field</span></div>
    <div class="card tight lineage-hof-v136">${F.fathers
      .slice()
      .reverse()
      .map(
        f =>
          `<div class="lineage-gen-v136"><span class="lg-gen">${ordV136(f.gen || 1).toUpperCase()}</span><span class="lg-name"><b>${escHtml(f.name)}</b> <small>${escHtml(f.pos)}</small></span><span class="lg-line">${escHtml(f.levelName)} · ${f.fate === "won" ? "🏆 made it" : f.fate === "walked" ? "🚪 walked" : "✗ cut"} · ${f.seasons} szn · ${f.peak} OVR</span></div>`
      )
      .join("")}
    ${state.player ? `<div class="lineage-gen-v136 now"><span class="lg-gen">${F.ordinal.toUpperCase()}</span><span class="lg-name"><b>${escHtml(state.player.name)}</b> <small>${escHtml(state.player.pos || "")}</small></span><span class="lg-line">playing now · ${state.player.totalSeasons | 0} szn</span></div>` : ""}</div>`;
    } catch (_) {
      return "";
    }
  }
  window.__LINEAGE_V136 = {
    family: familyV136,
    end: lineageEndV136,
    birth: lineageBirthV136,
    fateSay: fateSayV136,
    ord: ordV136,
    raw: () => lineageV136()
  };
  function startCareer(passed) {
    if (passed !== !0) {
      const g = rerollGateV112();
      if (g !== !0) return (Promise.resolve(g).then(ok => ok && startCareer(!0)), void 0);
    } /* v150 A: the warning is an in-app dialog now, so the career starts when it is answered */
    const _ab = abandonedV112(),
      _pv = _ab ? state.player.name : "";
    _ab && lineageEndV136(state.player, state.player.level, "walked");
    ((state.player = newPlayer()),
      state.careers++,
      lineageBirthV136(state.player),
      _ab && armRerollV112(state.player, _pv),
      (function () {
        try {
          window.__randomizeTeamLookV22 &&
            window.__randomizeTeamLookV22((state.careers || 1) * 7919 + Math.floor(Math.random() * 99991));
        } catch (e) {}
      })(),
      saveGame(),
      goView("choosePos"));
  } /* ===== v139 ONE SURNAME, A NEW FIRST NAME EVERY GENERATION =====
   * The line already handed the son the family surname (v136 D) and then let the name box overwrite
   * the whole thing, so a second-generation player could be typed into a different family and the
   * lineage row would go on calling it THE <old name> LINE. The surname belongs to the LINE now:
   * from the second generation on, the name box edits the FIRST name and the surname sits beside it
   * as a chip, and the one place the family name can actually change is Settings — where changing it
   * RETROFITS, rewriting the living player and every father on the books so the line reads as one
   * family top to bottom. The first generation is unchanged: he names himself, surname and all. */
  function famLockedV139() {
    const L = lineageV136();
    return !!(L.surname && (L.gen || 0) > 1 && (L.fathers || []).length);
  }
  function famSurnameV139() {
    const L = lineageV136();
    return L.surname || (state.player && surnameV136(state.player.name)) || "";
  }
  function famFirstV139(n) {
    const p = String(n || "")
      .trim()
      .split(/\s+/);
    return p.length > 1 ? p.slice(0, -1).join(" ") : p[0] || "";
  }
  function famSwapV139(n, sur) {
    const f = famFirstV139(n);
    return sur ? (f ? f + " " + sur : sur) : f;
  }
  function famRenameV139(sur) {
    const t = cleanNameV150(
      String(sur || "")
        .replace(/[^A-Za-z'\- ]/g, "")
        .replace(/\s+/g, " ")
        .trim(),
      18
    ); /* v150 A: the same cleaner as every other name */
    const L = lineageV136(),
      was = famSurnameV139();
    if (!t || t === was) return !1;
    L.surname = t;
    state.player && (state.player.name = famSwapV139(state.player.name, t));
    (L.fathers || []).forEach(f => {
      f.name = famSwapV139(f.name, t);
    });
    /* the Hall of Fame keeps its own copies, so the family's own men are rewritten there too */
    (state.hof || []).forEach(h => {
      h && h.name && surnameV136(h.name) === was && (h.name = famSwapV139(h.name, t));
    });
    return (saveGame(), !0);
  }
  window.famRenameV139 = function (v) {
    famRenameV139(v) && showToast("The line is now the " + famSurnameV139() + " family");
  };
  window.__LINEAGE_V139 = {
    locked: famLockedV139,
    surname: famSurnameV139,
    first: famFirstV139,
    rename: famRenameV139
  };
  /* ===== v150 A THE BUGS THE AUDIT FOUND =====
   * Four of the audit's confirmed bugs meet here, and all of them are about what the game TRUSTS.
   * A name was stored raw and printed raw, so `<img src onerror=…>` in the name box ran code on the
   * hub, the leaders, the Hall and both career-end screens. Two layers now: every way text gets IN
   * (the name box, the family name, the Team Creator, the leaderboard handle, a save coming back from
   * storage or an import) goes through `cleanNameV150` — control and bidi characters out, no `<>`,
   * quotes and `&` swapped for their typographic twins so a name can never close an attribute or an
   * inline handler, a length cap — and the screens that print a name print `escHtml(name)` anyway.
   * `cleanSaveV150` is the same pass over a whole save at boot, so a save written before this is
   * cleaned the first time it loads. `checkSaveV150` is the shape an import must have before it is
   * allowed near storage (the import itself now goes through the boot, by a reload, so every lazy
   * migration runs on it). `askV150` is the in-app confirm: `ribDialog` (v149 D) when it is there,
   * the browser's own only if the platform file never loaded. */
  function cleanNameV150(v, max) {
    return String(v == null ? "" : v)
      .normalize("NFC")
      .replace(/[\u0000-\u001f\u007f-\u009f\u200b-\u200f\u2028-\u202e\u2060-\u206f\ufeff]/g, "")
      .replace(/[<>`\\{}]/g, "")
      .replace(/&/g, "\uff06")
      .replace(/"/g, "\u201d")
      .replace(/'/g, "\u2019")
      .replace(/\s+/g, " ")
      .trim()
      .slice(0, max || TU("nameMaxV150", 24));
  }
  function cleanSaveV150(s) {
    if (!s || typeof s != "object") return 0;
    let n = 0;
    const cap = TU("nameStoreMaxV150", 40),
      fix = (o, k) => {
        if (o && typeof o[k] == "string") {
          const c = cleanNameV150(o[k], cap);
          c !== o[k] && ((o[k] = c), n++);
        }
      },
      p = s.player;
    if (p && typeof p == "object") {
      fix(p, "name");
      const T = p.teamIdentity;
      T && typeof T == "object" && ["town", "mascot", "college", "dfl"].forEach(k => fix(T, k));
      p.nemesis && fix(p.nemesis, "name");
    }
    const L = s.lineageV136;
    L && typeof L == "object" && (fix(L, "surname"), Array.isArray(L.fathers) && L.fathers.forEach(f => fix(f, "name")));
    Array.isArray(s.hof) && s.hof.forEach(h => fix(h, "name"));
    return n;
  }
  const HOSTILE_V150 = /<\s*\/?\s*(script|iframe|object|embed|base|meta|link|frame|svg)\b|<[^>]*\bon[a-z]+\s*=|javascript\s*:/i;
  function checkSaveV150(t) {
    if (!t || typeof t != "object" || Array.isArray(t)) return "not a save";
    if (typeof t.prestige != "number" || !isFinite(t.prestige)) return "no prestige on it";
    if (t.pp != null && (typeof t.pp != "number" || !isFinite(t.pp))) return "a broken PP balance";
    if (t.player != null && (typeof t.player != "object" || Array.isArray(t.player))) return "a broken player";
    if (t.player && t.player.attrs != null && typeof t.player.attrs != "object") return "a broken attribute sheet";
    if (t.tree != null && (typeof t.tree != "object" || Array.isArray(t.tree))) return "a broken prestige tree";
    for (const k of ["hof", "inventory"]) if (t[k] != null && !Array.isArray(t[k])) return "a broken " + k;
    let seen = 0,
      bad = "";
    const walk = (v, d) => {
      if (bad || d > TU("saveScanDepthV150", 12) || ++seen > TU("saveScanMaxV150", 200000)) return;
      if (typeof v == "string") HOSTILE_V150.test(v) && (bad = "markup inside it");
      else if (v && typeof v == "object") for (const k in v) walk(v[k], d + 1);
    };
    return (walk(t, 0), bad);
  }
  function askV150(msg, o) {
    const D = window.ribDialog;
    if (D && D.confirm) return D.confirm(msg, o || {});
    try {
      return Promise.resolve(!!window.confirm(msg));
    } catch (e) {
      return Promise.resolve(!1);
    }
  }
  /* an imported save goes to storage and the page reloads, so boot() migrates it exactly like a cold start */
  function importCodeV150(code) {
    if (window.ribSave && window.ribSave.importText) return window.ribSave.importText(code);
    let t;
    try {
      const x = String(code || "").trim();
      t = x[0] === "{" ? JSON.parse(x) : JSON.parse(decodeURIComponent(escape(atob(x.replace(/\s+/g, "")))));
      t && t.format === "rib-save" && t.save && (t = typeof t.save == "string" ? JSON.parse(t.save) : t.save);
    } catch {
      return (showToast("Invalid code"), Promise.resolve(!1));
    }
    const why = checkSaveV150(t);
    if (why) return (showToast("That save can't be imported: " + why), Promise.resolve(!1));
    return askV150("Replace the save on this device with the imported one?", { title: "Import save", ok: "Import", danger: !0 }).then(
      ok => {
        if (!ok) return !1;
        try {
          window.GridironStorage ? window.GridironStorage.save(t) : localStorage.setItem(SAVE_KEY, JSON.stringify(t));
        } catch (e) {
          return (showToast("Import failed"), !1);
        }
        return (setTimeout(() => location.reload(), 60), !0);
      }
    );
  }
  function showCodeV150(code) {
    const D = window.ribDialog;
    return D && D.show
      ? D.show({
          title: "Your backup code",
          message: "Copy this code and keep it somewhere safe. Paste it into Settings › Import to bring the career back.",
          input: { value: code, multiline: !0, readonly: !0 },
          buttons: [{ label: "Done", value: !0, kind: "primary" }]
        })
      : (showToast("Copy failed — try Export to file"), Promise.resolve(!1));
  }
  window.__V150A = {
    clean: cleanNameV150,
    cleanSave: cleanSaveV150,
    checkSave: checkSaveV150,
    ask: askV150,
    importCode: importCodeV150,
    recovered: () => window.__saveRecoveredV150 || null
  };
  /* ===== v96 A NAME OF HIS OWN — the rolled name is a starting point, not a sentence ===== */
  window.setPlayerNameV96 = function (v) {
    if (!state.player) return;
    const t = cleanNameV150(v, TU("nameMaxV150", 24));
    t && ((state.player.name = famLockedV139() ? famFirstV139(t) + " " + famSurnameV139() : t), saveGame());
  };
  window.rerollNameV96 = function () {
    if (!state.player) return;
    ((state.player.name = randPick(FIRST_NAMES) + " " + (famLockedV139() ? famSurnameV139() : randPick(LAST_NAMES))),
      saveGame());
    const e = document.getElementById("playerNameV96");
    e && (e.value = famLockedV139() ? famFirstV139(state.player.name) : state.player.name);
  };
  function screenChoosePos() {
    const e = state.player,
      t = suggestPositions(e.attrs, e.body),
      a = t[0].ovr,
      s = ATTR_KEYS.slice()
        .sort((i, r) => e.attrs[r] - e.attrs[i])
        .slice(0, 3),
      n = bodyOfV112(e) || e.body,
      pj = e.body;
    ((byId("screen").innerHTML = `
    <div class="eyebrow">Age ${e.age} · ${LEVELS[e.level].name}</div>
    <div class="h1 name-edit-v96"><input id="playerNameV96" value="${String(
      famLockedV139() ? famFirstV139(e.name) : e.name
    )
      .replace(/&/g, "&amp;")
      .replace(
        /"/g,
        "&quot;"
      )}" maxlength="24" autocomplete="off" spellcheck="false" aria-label="${famLockedV139() ? "First name" : "Player name"}" oninput="setPlayerNameV96(this.value)">${famLockedV139() ? `<span class="fam-name-v139" title="The family name. Change it in Settings.">${escHtml(famSurnameV139())}</span>` : ""}<button type="button" class="name-dice-v96" onclick="rerollNameV96()" title="Roll another name">🎲</button></div>
    <div class="small name-hint-v96">${famLockedV139() ? "Tap to name him — the " + escHtml(famSurnameV139()).toUpperCase() + " name is the line's" : "Tap the name to make it yours"}</div>
    ${lineageCardV136(e)}
    ${rerollNoteV112(e)}
    <div class="card tight" style="border-color:#4a90c9">
      <div class="l" style="font-size:11px;color:#7ab6e0;letter-spacing:2px;margin-bottom:6px">🧍 PHYSICAL PROFILE · TODAY, AGE ${e.age} — ${bodyLabel(n)}</div>
      <div class="statline">
        <div class="statbox"><div class="n">${fmtHeight(n.height)}</div><div class="l">Height</div></div>
        <div class="statbox"><div class="n">${n.weight}</div><div class="l">Weight (lb)</div></div>
        <div class="statbox"><div class="n">${n.muscle}</div><div class="l">Muscle</div></div>
      </div>
      ${n.grown ? "" : `<div class="threshold-note proj-v112" style="margin-top:8px">📈 <b style="color:var(--gold)">PROJECTS TO</b> ${fmtHeight(pj.height)} · ${pj.weight} lb · ${pj.muscle} muscle — <b>${bodyLabel(pj)}</b> by ${TU("growAdultAge", 22)}. Scouts grade the frame he is going to get, so the position fit below reads that projection.</div>`}
    </div>
    ${
      Array.isArray(e.traitOfferV112) && e.traitOfferV112.length
        ? `<div class="card tight trait-offer-v112" id="traitOfferV112" style="border-color:var(--gold)">
      <div class="l" style="font-size:11px;color:var(--gold);letter-spacing:2px;margin-bottom:7px">🎲 CHOOSE A TRAIT · ${e.traitOfferV112.length} OF ${TRAIT_POOL_V112.length} DRAWN</div>
      ${e.traitOfferV112
        .map(i => {
          const r = TRAITS[i];
          return r
            ? `<div class="pos-card trait-card-v112" data-trait-v112="${i}" onclick="pickTraitV112('${i}')">
        <div class="pos-abbr" style="font-size:26px">${r.icon}</div>
        <div class="pos-info"><div class="pn">${r.name}</div><div class="pd">${r.desc}</div>
        <span class="fit-tag fit-${r.good > 0 ? "natural" : r.good < 0 ? "poor" : "good"}">${r.good > 0 ? "UPSIDE" : r.good < 0 ? "RISK" : "DOUBLE-EDGED"}</span></div>
      </div>`
            : "";
        })
        .join("")}
      <div class="small" style="margin-top:6px;opacity:.75">Tap one — the other is gone for this career. Lock a position and the game picks for you.</div>
    </div>`
        : ""
    }
    ${
      e.traits && e.traits.length
        ? `<div class="card tight" style="border-color:var(--gold)">
      <div class="l" style="font-size:11px;color:var(--gold);letter-spacing:2px;margin-bottom:7px">🎲 THIS PLAYER'S TRAITS</div>
      ${e.traits
        .map(i => {
          const r = TRAITS[i];
          return r
            ? `<div style="display:flex;gap:8px;align-items:baseline;margin:3px 0"><span class="trait-chip ${r.good < 0 ? "neg" : r.good > 0 ? "pos" : ""}">${r.icon} ${r.name}</span><span class="small" style="flex:1">${r.desc}</span></div>`
            : "";
        })
        .join("")}
    </div>`
        : ""
    }
    <div class="sub">Standout traits: ${s.map(i => `<b style="color:var(--gold)">${ATTR_INFO[i].name}</b>`).join(", ")}. Your body shape gives a natural edge at some positions — pick where it fits.</div>
    <div class="h2">Choose a Position</div>
    ${t
      .map(i => {
        const r = i.ovr >= a - 1 ? "natural" : i.ovr >= a - 5 ? "good" : "poor",
          l = r === "natural" ? "NATURAL FIT" : r === "good" ? "DECENT" : "POOR FIT",
          d = i.phys,
          c =
            d >= 4
              ? `<span style="color:var(--good)">+${d} build</span>`
              : d <= -4
                ? `<span style="color:#e08a8a">${d} build</span>`
                : '<span style="color:var(--chalk-dim)">±0 build</span>';
        return `<div class="pos-card ${r === "natural" ? "best" : ""}" onclick="pickPos('${i.pos}')">
        <div class="pos-abbr">${i.pos}</div>
        <div class="pos-info">
          <div class="pn">${POSITIONS[i.pos].name}</div>
          <div class="pd">${POS_BODY[i.pos].note} · ${c}</div>
          <span class="fit-tag fit-${r}">${l}</span>
        </div>
        <div class="pos-fit"><div class="ovr" style="color:${r === "natural" ? "var(--gold)" : "var(--chalk)"}">${i.ovr}</div><div class="lbl">OVR</div></div>
      </div>`;
      })
      .join("")}
  `),
      (byId("dock").innerHTML =
        '<div class="small center">Build bonus reads your PROJECTED adult frame — scouts grade what you grow into, not what you weigh today. Develop your body between careers in the Prestige → Body tree.</div>'));
  }
  function pickPos(e) {
    (traitAutoV112(state.player), (state.player.pos = e), genRivals(state.player), saveGame(), goView("hub"));
  }
  function screenHub() {
    const e = state.player,
      t = LEVELS[e.level],
      a = playerOvr(e),
      s = e.level >= 8 || (t.key === "nfl" && !((e.nflRings || 0) >= 1));
    t.key === "nfl" && (e.nflRings || 0) >= 1;
    const n = maxSeasons() - e.seasonsAtLevel,
      i = minSeasons(),
      r = e.seasonsAtLevel >= i;
    e.level === 5 && r && n > 0;
    const l = nationalRank(e, a),
      d = s ? 100 : declareChanceV88(e),
      c = s ? null : baselineStats(e.pos, e.level),
      u = POS_STATS[e.pos],
      h = d >= 70 ? "var(--good)" : d >= 45 ? "var(--gold)" : "var(--blood)",
      p = n <= 1 && d < 60 && !s,
      m = s
        ? ""
        : u.stats
            .map(C => {
              const V = fmtLeadVal(C, c[C.key]);
              return `<div class="tgt-row"><span class="tgt-name">${C.name}</span><span class="tgt-need" style="color:var(--gold)">${V}${C.lowerBetter ? " or fewer" : ""}</span></div>`;
            })
            .join("");
    byId("screen").innerHTML = `
    <div class="card player-hero">
      <div class="pos-badge">${e.pos}</div>
      <div class="name-row"><span class="pname">${escHtml(e.name)}</span><span class="page">· ${e.pos} · Age ${e.age}</span></div>
      <div class="meta-row">
        <span class="meta"><b>${t.name}</b></span>
        <span class="meta">Season <b>${e.seasonsAtLevel + 1}</b></span>
        <span class="meta">${"★".repeat(e.stars)}<span style="color:#3b4759">${"★".repeat(5 - e.stars)}</span> recruit</span>
        ${currentTier(e) ? `<span class="meta" style="color:var(--gold)">${currentTier(e).icon} ${currentTier(e).name}</span>` : ""}
      </div>
      <div class="meta-row" style="margin-top:4px">
        <span class="meta" style="color:#9fb8a6">🏟 ${escHtml(teamName(e))}</span>
        ${(e.titles || 0) > 0 ? `<span class="meta" style="color:var(--gold)">💍×${e.titles}</span>` : ""}
        ${e.declareBonus ? `<span class="meta" style="color:var(--gold)">😤 +${e.declareBonus}%</span>` : ""}
        ${rerollChipV112(e)}
      </div>
      ${lineageRowV136(e)}
      ${
        e.traits && e.traits.length
          ? `<div class="meta-row" style="margin-top:5px">
        ${e.traits
          .map(C => {
            const V = TRAITS[C];
            return V
              ? `<span class="trait-chip ${V.good < 0 ? "neg" : V.good > 0 ? "pos" : ""}" title="${V.desc}">${V.icon} ${V.name}</span>`
              : "";
          })
          .join("")}
      </div>`
          : ""
      }
      ${
        e.body
          ? (() => {
              const bd = bodyOfV112(e);
              return `<div class="meta-row body-row-v112" style="margin-top:4px;font-size:13px;color:#7ab6e0">
        <span class="meta" style="color:#7ab6e0">🧍 ${fmtHeight(bd.height)}</span>
        <span class="meta" style="color:#7ab6e0">${bd.weight} lb</span>
        <span class="meta" style="color:#7ab6e0">${bodyLabel(bd)}</span>
        ${bd.grown ? "" : `<span class="meta proj-v112" style="color:var(--gold)" title="Projected adult frame by ${TU("growAdultAge", 22)}">📈 projects ${fmtHeight(e.body.height)} · ${e.body.weight} lb</span>`}
        <span class="meta" style="color:${physFit(e.body, e.pos) >= 0 ? "var(--good)" : "#e08a8a"}" title="Build fit is graded on the projected adult frame">${physFit(e.body, e.pos) >= 0 ? "+" : ""}${physFit(e.body, e.pos)} fit</span>
      </div>`;
            })()
          : ""
      }
      <div class="progress-wrap">
        <div class="progress-label"><span>Overall Rating</span><span><b style="color:${ovrTier(a).color}">${a}</b> OVR${gearPowerBonus() > 0 ? ` <b class="pwr-badge">⚡+${gearPowerBonus()} PWR</b>` : ""} · <span style="color:${ovrTier(a).color};font-size:12px">${ovrTier(a).label}</span></span></div>
        <div class="bar"><i style="width:${clamp99(a, 0, 100)}%"></i></div>
        ${e.potentialCeil ? `<div class="threshold-note" style="margin-top:5px;font-size:11px">📈 Potential ceiling: <b style="color:var(--gold)">~${Math.round(99 * (1 - Math.exp(-(e.potentialCeil + 12) / 78)))} OVR</b> — raise it with <b>Prestige</b> & upgrades.</div>` : ""}
      </div>
    </div>

    ${seasonModBanner(e)}
    ${fr(e)}
    ${e.nemesis && Math.random() < 0.4 ? nemesisBanner(e) : ""}
    ${
      e.contracts && e.contracts.length
        ? `<div class="card tight" style="border-color:rgba(90,208,201,.45)">
      <div class="l" style="font-size:10px;color:#5ad0c9;letter-spacing:2px;margin-bottom:6px">📜 SEASON CONTRACTS — cash in at season's end</div>
      ${e.contracts.map(C => `<div class="contract-row"><span>◇ ${C.desc}</span><b>+${C.pp} PP</b></div>`).join("")}
    </div>`
        : ""
    }
    ${
      s
        ? `
    <div class="card"><div class="threshold-note" style="color:var(--gold)">🏆 You made the UFF. Keep playing to build your legacy.</div></div>`
        : `
    <div class="card" style="border-color:${h}">
      <div class="l" style="font-size:11px;color:var(--chalk-dim);letter-spacing:2px;margin-bottom:8px">🎯 PROMOTION TARGET · ${LEVELS[e.level + 1].name.toUpperCase()}</div>
      <div class="small" style="margin-bottom:8px">Post a season around these ${u.label.toLowerCase()} numbers to earn a strong shot at moving up:</div>
      <div class="tgt-grid">${m}</div>
      <div class="divider" style="margin:10px 0"></div>
      <div class="chance-box">
        <div class="chance-num" style="color:${h}">${Math.round(d)}%</div>
        <div class="chance-info">
          <div class="chance-label" style="color:${h}">Current promotion odds</div>
          <div class="small">Based on your national rank — <b>#${l.rank.toLocaleString()}</b> of ${fmtInt(l.of)} — and your ${a} OVR. The higher you finish in the country, the surer the call.</div>
        </div>
      </div>
      <div class="chance-bar"><i style="width:${d}%;background:${h}"></i></div>
      <div class="threshold-note" style="margin-top:8px">${p ? `<span class="miss">⚠ ${n} season${n > 1 ? "s" : ""} left — odds are low. Train hard or you'll wash out.</span>` : `You have <b>${n}</b> season${n > 1 ? "s" : ""} to build up before you're forced to declare.`}</div>
    </div>`
    }

    <div class="card tight" onclick="go('rank')" style="cursor:pointer">
      <div class="sb-row">
        <div><div class="l" style="font-size:12px;color:var(--chalk-dim);letter-spacing:1px">NATIONAL RANK · ${t.name.toUpperCase()}</div>
        <div style="font-family:'Oswald';font-weight:700;font-size:19px">#${fmtInt(l.rank)} <span style="color:var(--chalk-dim);font-weight:400;font-size:14px">overall</span></div>
        <div style="font-size:13px;color:var(--gold)">#${fmtInt(l.posRank)} among ${e.pos}s</div></div>
        <div style="text-align:right"><div style="font-family:'Oswald';font-size:22px;color:var(--gold)">${Math.round(l.pct)}%ile</div>
        <div style="font-size:12px;color:var(--chalk-dim)">tap for rivals ▸</div></div>
      </div>
    </div>

    <div class="h2">Attributes</div>
    <div class="card">
      ${bodyBadgeV85(e)}<div class="attrs">${ATTR_KEYS.map(C => attrRow(C, e)).join("")}</div>
      <div class="attrs-legend-v85"><span><i style="background:#e0645a"></i>your body's cut next game</span><span><i style="background:#57e07a"></i>lift next game</span><span><i style="border:1.5px solid var(--good);box-sizing:border-box;background:repeating-linear-gradient(90deg,transparent 0 3px,#57e07a26 3px 5px)"></i>expected by season's end · ${(PROGRAMS[e.training || "balanced"] || {}).name || "current plan"}</span></div>
    </div>
    ${e.totalSeasons === 0 && state.careers <= 1 ? `<div class="card tight" style="border-color:var(--gold)"><div class="small"><b style="color:var(--gold)">How it works:</b> Each level has a stat target. Play seasons and spend upgrade points to raise your production and your <b>promotion odds</b>. When you're ready, <b>declare</b> to roll for the next level — hit your target for a strong shot. A declare is <b>one shot</b>: miss it, or run out of seasons, and the career ends.</div></div>` : ""}
  `;
    const y = n <= 0;
    byId("dock").innerHTML = `
    ${e.points > 0 ? `<button class="btn secondary" onclick="go('upgrade')">Spend ${e.points} Upgrade Point${e.points > 1 ? "s" : ""}</button><div style="height:8px"></div>` : ""}
    ${
      r && !s
        ? `<button class="btn" ${e.level === 7 ? 'style="background:linear-gradient(180deg,#b9a6ff,#7a5adf);color:#120a2e;box-shadow:0 4px 0 #3c2a8a, 0 6px 16px rgba(122,90,223,.4)"' : ""} onclick="declareFromHub()">${e.level === 7 ? "🛸 Answer the Interstellar Call" : e.level === 5 ? "🎓 Declare for the Draft" : "⬆️ Declare for " + LEVELS[e.level + 1].name} · ${Math.round(d)}%</button>
         <div class="small center declare-stakes-v150" style="margin-top:8px;color:var(--blood)">⚠️ One shot — a failed declare ends the career.</div>
         ${y ? '<div class="small center" style="margin-top:4px;color:var(--gold)">Final season played — you must declare now.</div>' : `<div style="height:8px"></div><button class="btn secondary" onclick="startSeason()">▶ Play Another Season (${e.seasonsAtLevel + 1}/${maxSeasons()})</button>`}`
        : `<button class="btn" onclick="startSeason()">▶ Play ${t.games}-Game Season (${e.seasonsAtLevel + 1}/${maxSeasons()})</button>`
    }
    ${!s && !r ? `<div style="height:8px"></div><div class="small center" style="color:var(--gold)">📚 ${t.grade} — ${i - e.seasonsAtLevel} more season${i - e.seasonsAtLevel > 1 ? "s" : ""} to play${e.level === 5 ? " before you can declare" : ""}.</div>` : ""}
    ${t.key === "nfl" && !(e.nflRings >= 1) ? '<div style="height:8px"></div><div class="small center" style="color:#c9b8ff">🛸 Rumors of an <b>Interstellar League</b>… they only call <b>champions</b>. Win a ring.</div>' : ""}
    <div style="height:8px"></div>
    ${/* v139: the tree and the menu are on the bottom bar now — this row is what the bar has no room for */ ""}
    <div class="btn-row">
      <button class="btn ghost" onclick="setStatsTab('leaders');go('stats')">📊 Stats</button>
      <button class="btn ghost" onclick="go('challenges')">🎯 Goals</button>
    </div>
  `;
  } // v17: national standing → promotion floor. The rank on the declare screen IS
  // the rank that decides your fate: #1 in the country is an automatic call-up.
  /* ===== v88 THE CALL-UP FOLLOWS THE RANKING =====
   * The declare roll used a synthetic season rating (qt without the season) and a
   * percentile FLOOR that, at the combine, wanted the top 0.3% of 1,500 — rank 4 or
   * better — before the ranking counted for anything. A national #1 by double the
   * stats could post a stellar year and get left behind on a rating roll. Now one
   * curve answers every screen: your national rank against the SHARE of this level's
   * pool that actually moves up (ADV_V88 — 9% of college reaches the combine, about a
   * third of the combine sticks in the league, a handful of the league gets the
   * interstellar call). #1 in the country is ~99%; the last man inside the share is a
   * coin flip; well outside it is single digits. The rating roll still stands as a
   * floor for a player whose numbers beat his rank. Hub card, season screen and the
   * roll itself all read declareChanceV88, so the number you saw is the number that
   * decides. Dials: advShareK (scales every share), advSoft (how fast it falls off). */
  const ADV_V88 = [0.5, 0.45, 0.35, 0.25, 0.12, 0.09, 0.32, 0.003, 0.003];
  function rankCurveV88(rank, of, level) {
    const top = Math.max(0, (rank - 1) / Math.max(1, of)),
      adv = ADV_V88[Math.min(Math.max(0, level), ADV_V88.length - 1)] * TU("advShareK", 1);
    const z = (adv - top) / (adv * TU("advSoft", 0.5));
    return clamp99(50 + 49.5 * Math.tanh(z), 0.3, 99.5);
  }
  function rankChanceV88(e) {
    try {
      const rk = nationalRank(e, playerOvr(e));
      if (!rk || !rk.of) return 0;
      return rankCurveV88(rk.rank, rk.of, e.level);
    } catch (_) {
      return 0;
    }
  }
  function __natAdvFloor(e) {
    return rankChanceV88(e);
  }
  function declareChanceV88(e) {
    const s = e.seasonStats,
      fresh =
        s &&
        s.chance != null &&
        e.lastSeasonLine &&
        e.lastSeasonLine.level === e.level &&
        e.lastSeasonLine.pos === e.pos;
    const base = fresh ? s.chance : advanceChance(playerOvr(e), e.level);
    return Math.max(Math.min(TU("declareCeilV139", 99), base + (e.declareBonus || 0)), rankChanceV88(e));
  } /* v139: a stellar season tops out at 99, not 97 */
  window.__V88 = {
    curve: rankCurveV88,
    rankChance: e => rankChanceV88(e || state.player),
    declareChance: e => declareChanceV88(e || state.player),
    ADV: ADV_V88
  };
  function declareFromHub() {
    const e = state.player;
    if (e.seasonsAtLevel < minSeasons()) {
      showToast("You must finish this level first!");
      return;
    }
    const t = declareChanceV88(e);
    (e.seasonStats || (e.seasonStats = {}),
      (e.seasonStats.chance = t),
      Math.random() * 100 < t ? advanceLevel() : failDeclareV77(e, t));
  }
  window.__mkTempBuffsV25 = function (pl, sign, bold, good) {
    var pos = (pl && pl.pos) || "ATH";
    var POS = {
      QB: ["throwing", "awareness", "vision", "acceleration"],
      RB: ["speed", "agility", "acceleration", "ballControl", "vision"],
      WR: ["speed", "catching", "agility", "jumping"],
      TE: ["catching", "blocking", "strength", "agility"],
      OL: ["blocking", "strength", "awareness"],
      DL: ["strength", "tackling", "quickness", "acceleration"],
      LB: ["tackling", "awareness", "speed", "strength"],
      CB: ["speed", "agility", "jumping", "awareness"],
      S: ["tackling", "speed", "awareness", "jumping"],
      K: ["awareness", "discipline"]
    };
    var pool = (POS[pos] || ["speed", "strength", "agility", "awareness", "acceleration"]).filter(function (k) {
      return ATTR_INFO && ATTR_INFO[k];
    });
    if (!pool.length) pool = ["speed", "strength", "agility", "awareness"];
    var picks = [],
      p = pool.slice();
    var n = 1 + (bold > 0.55 || Math.random() < 0.4 ? 1 : 0);
    for (var i = 0; i < n && p.length; i++) {
      var idx = Math.floor(Math.random() * p.length);
      picks.push(p.splice(idx, 1)[0]);
    }
    return picks.map(function (k) {
      if (sign > 0) {
        var amt = Math.round(3 + good * 4 + bold * 3 + Math.random() * 3);
        return { stat: k, amt: amt, max: false };
      }
      return { stat: k, amt: -Math.round(5 + (1 - good) * 8 + bold * 3), max: false };
    });
  };
  window.__statLabelV25 = function (k) {
    return (ATTR_INFO && ATTR_INFO[k] && ATTR_INFO[k].name) || k;
  };
  function pregamePlayerStatsV25(pl) {
    if (!pl || !pl.attrs) return "";
    const cap = attrCap();
    const buffs = {};
    (pl._tempStatBuffsV25 || []).forEach(b => {
      buffs[b.stat] = b;
    });
    const EF = effAttrsV85(pl); // v85: the body's swing on top of the wheel's
    const posW = (pl.pos && POSITIONS[pl.pos] && POSITIONS[pl.pos].w) || {};
    // v111: the game focus, if one is picked this week — the sheet names the stat it sharpens
    let FOC = null;
    try {
      const wk = (pl.weekResults || []).find(w => w && !w.played);
      if (wk && window.__V111_UI && window.__V111_UI.focusRow) FOC = window.__V111_UI.focusRow(pl, wk) || null;
    } catch (_) {
      FOC = null;
    }
    let list = ATTR_KEYS.filter(k => ATTR_INFO[k] && (posW[k] || buffs[k] || EF.delta[k] || (FOC && FOC.stat === k)));
    if (list.length < 6) list = ATTR_KEYS.filter(k => ATTR_INFO[k]).slice(0, 10);
    if (FOC && ATTR_INFO[FOC.stat] && list.indexOf(FOC.stat) < 0) list = [FOC.stat].concat(list);
    /* v126: the bar fills to the SOFT CAP, not the absolute wall. Against we() — 250 at prestige —
     * a Pee Wee's 12 drew a 5% sliver and every stat on the sheet looked identical and hopeless.
     * The number that governs THIS season is the cap the next point starts costing more at, so the
     * bar is drawn against that; anything PAST it is a second colour (gold) on a scale stretched to
     * fit, with a tick left where the cap sits — so "over the cap" reads as the achievement it is
     * instead of a bar pinned at the end. The buff/cut overlay is unchanged. */
    const row = k => {
      const base = pl.attrs[k] || 0,
        b = buffs[k],
        eff = EF.eff[k] != null ? EF.eff[k] : b ? base + (b.max ? 10 : b.amt) : base,
        amt = eff - base;
      const sc = clamp99(typeof drSoftCap === "function" ? drSoftCap(pl, k) : cap, 1, cap);
      const scale = Math.min(cap, Math.max(sc, base, eff)),
        pc = v => clamp99((v / scale) * 100, 0, 100);
      const lo = Math.min(base, eff),
        bp = pc(base),
        ep = pc(eff),
        up = amt > 0,
        dn = amt < 0;
      const under = pc(Math.min(lo, sc)),
        over = lo > sc ? Math.max(0, pc(lo) - pc(sc)) : 0,
        capT = scale > sc + 0.5 ? pc(sc) : 0;
      return (
        `<div style="display:flex;align-items:center;gap:8px;margin:3px 0">` +
        `<span style="flex:0 0 106px;font:600 11px Barlow Condensed,sans-serif;color:var(--chalk-dim)">${ATTR_INFO[k].name}${statInfoBtnV142(k)}</span>` +
        `<div style="flex:1;height:8px;border-radius:5px;background:#1a2330;position:relative;overflow:hidden">` +
        `<i style="position:absolute;left:0;top:0;bottom:0;width:${under}%;background:linear-gradient(90deg,#3a6cff,#6b9bff)"></i>` +
        (over
          ? `<i style="position:absolute;left:${pc(sc)}%;top:0;bottom:0;width:${over}%;background:linear-gradient(90deg,#f0bb45,#ffd77a)"></i>`
          : ``) +
        (up
          ? `<i style="position:absolute;left:${bp}%;top:0;bottom:0;width:${Math.max(0, ep - bp)}%;background:#57e07a"></i>`
          : ``) +
        (dn
          ? `<i style="position:absolute;left:${ep}%;top:0;bottom:0;width:${Math.max(0, bp - ep)}%;background:#e0645a"></i>`
          : ``) +
        (capT
          ? `<b style="position:absolute;left:${capT}%;top:-1px;bottom:-1px;width:2px;background:rgba(255,255,255,.75);border-radius:1px"></b>`
          : ``) +
        `</div>` +
        `<span style="flex:0 0 auto;min-width:64px;text-align:right;font:700 11px Oswald,sans-serif;color:${up ? "#57e07a" : dn ? "#e0645a" : "var(--chalk)"}">${Math.round(eff)}${up ? " (+" + amt + ")" : dn ? " (" + amt + ")" : ""}<span style="display:block;font:600 8.5px Oswald,sans-serif;letter-spacing:.6px;color:${base > sc ? "#f0bb45" : "var(--chalk-dim)"}">${base > sc ? "OVER " + Math.round(sc) : "/ " + Math.round(sc) + " cap"}</span></span>` +
        (FOC && FOC.stat === k
          ? `<span style="flex:0 0 auto;font:800 9px Oswald,sans-serif;color:#0b1119;background:#8fe0a0;border-radius:9px;padding:2px 5px">×${(Math.round((Number(FOC.mul) || 1.2) * 100) / 100).toFixed(1)}</span>`
          : ``) +
        `</div>`
      );
    };
    return (
      `<div id="preStatsV25" style="margin:12px 14px 4px;padding:10px 12px;border:1px solid var(--line);border-radius:10px;background:rgba(255,255,255,.03)">` +
      `<div style="font:700 11px Oswald,sans-serif;letter-spacing:1px;color:var(--chalk);margin-bottom:7px">📋 YOUR STATS <span style="color:var(--chalk-dim);font-weight:400">· what you take onto the field, body and focus included</span></div>` +
      `<div style="font:400 10.5px system-ui;color:var(--chalk-dim);margin:-3px 0 7px;line-height:1.4">Each bar fills to that stat's <b style="color:#6b9bff">soft cap</b> — the line where the next point starts costing more than one skill point. Anything past it is drawn in <b style="color:#f0bb45">gold</b>, with a tick where the cap sits.</div>` +
      (typeof rerollNoteV112 === "function"
        ? rerollNoteV112(pl)
        : "") /* v131: the price of starting over belongs where you read the sheet it changed */ +
      bodyBadgeV85(pl) +
      (FOC
        ? `<div id="preFocusV111" style="display:flex;align-items:center;gap:6px;margin:0 0 7px;font:600 11px Barlow Condensed,sans-serif;color:#8fe0a0"><span>🎯 GAME FOCUS · ${FOC.name || FOC.key}</span><b style="font:700 11px Oswald,sans-serif">×${(Math.round((Number(FOC.mul) || 1.2) * 100) / 100).toFixed(1)} ${(ATTR_INFO[FOC.stat] && ATTR_INFO[FOC.stat].name) || FOC.stat}</b><span style="color:var(--chalk-dim);font-weight:400">this game only</span></div>`
        : ``) +
      list.map(row).join("") +
      `</div>`
    );
  }
  window.pregamePlayerStatsV25 = pregamePlayerStatsV25;
  /* v112 D: the pregame wizard's last page states the body's swing in one line beside the sheet */
  window.__v112BodySwingD = function (pl) {
    try {
      const E = effAttrsV85(pl),
        L = bodyLedgerV73(pl),
        c = cv18(pl);
      return {
        mult: E.mult,
        pct: Math.round((E.mult - 1) * 100),
        fatigue: Math.round(c.fatigue || 0),
        sit: !!L.sit,
        inj: !!L.inj
      };
    } catch (_) {
      return null;
    }
  };
  /* ===== v85 THE BODY ON THE SHEET, AND THE SEASON AHEAD =====
   * Two things the attribute sheet never said. (1) What the body is doing to the
   * numbers: condMultV54 is the swing the game actually applies (worn or hurt −10%,
   * fresh +5%) and _tempStatBuffsV25 is this game's wheel swing — so every attribute
   * shows the EFFECTIVE value next to the true one, with the cut or the gain drawn
   * on its track, and the sheet carries the injury-risk badge (% this game, games
   * expected missed). (2) Where the season is taking each number: the season
   * resolver's own gain formula (tt) run as an expected value — no dice — from the
   * current training program, the games played and the games left, drawn as a
   * hollow green extension the real gain fills in at season's end. */
  function effAttrsV85(e) {
    const out = { mult: 1, buffs: {}, muls: {}, eff: {}, delta: {}, gear: {} };
    if (!e || !e.attrs) return out;
    const mult = (out.mult = condMultV54(e));
    /* v111: the sheet reads the wheel's flat swing AND v111's own entries — a lingering wear cut
     * counting its games down, and this game's focus pick, which is a MULTIPLIER rather than an
     * add. The multiplier lands after condMultV54 and is clamped, so no two buffs can compound
     * into a number the player never earned. With no focus and no wear this is the old list. */
    (typeof wearBuffsV111 === "function" ? wearBuffsV111(e) : e._tempStatBuffsV25 || []).forEach(b => {
      if (!b || !b.stat) return;
      if (b.mul != null) out.muls[b.stat] = (out.muls[b.stat] || 1) * (Number(b.mul) || 1);
      else out.buffs[b.stat] = (out.buffs[b.stat] || 0) + (b.max ? 10 : Number(b.amt) || 0);
    });
    ATTR_KEYS.forEach(k => {
      const a = e.attrs[k] || 0,
        mu = out.muls[k] ? clamp99(out.muls[k], TU("focusMulFloor", 0.6), TU("focusMulCeil", 1.35)) : 1,
        v = Math.max(
          1,
          Math.round((Math.round(a * mult) + (out.buffs[k] || 0)) * mu) + (out.gear[k] = gearAttrV147(k))
        );
      /* v147 C: gear lands last, as in _raw */ out.eff[k] = v;
      out.delta[k] = v - a;
    });
    return out;
  }
  function bodyBadgeV85(e) {
    try {
      if (!e || !e.weekResults || !e.weekResults.some(w => !w.played)) return "";
      const L = bodyLedgerV73(e),
        c = cv18(e),
        fat = c.fatigue || 0,
        pct = Math.round((L.mult - 1) * 100),
        sw = L.sit
          ? ""
          : pct > 0
            ? "+" + pct + "% to every attribute"
            : pct < 0
              ? pct + "% to every attribute"
              : "no swing";
      const col = L.sit || L.mult < 1 ? "#ff8a80" : L.mult > 1 ? "#57e07a" : "var(--chalk-dim)";
      const head = L.sit
        ? "🩹 OUT"
        : L.inj
          ? "🩹 PLAYING HURT"
          : fat >= TU("wornFatigue", 70)
            ? "🥵 WORN DOWN"
            : L.mult < 0.995
              ? "🥵 WEARING DOWN"
              : L.mult > 1
                ? "⚡ FRESH LEGS"
                : "STEADY";
      return (
        `<div class="bodyv85" title="Injury risk this game, the games it is expected to cost, and the swing your body puts on every attribute below">` +
        `<span style="color:${col}">${head}${sw ? " · " + sw : ""}</span>` +
        `<span><b style="color:${L.sit ? "#ff8a80" : L.risk >= 0.24 ? "#ff8a80" : L.risk <= 0.12 ? "#57e07a" : "var(--gold)"}">${L.sit ? "—" : Math.round(L.risk * 100) + "%"}</b> injury risk</span>` +
        `<span><b>${Math.round(L.missNext)}</b> games expected missed</span>` +
        `<span>fatigue <b style="color:${fat >= 75 ? "var(--blood)" : fat >= 50 ? "var(--gold)" : "var(--good)"}">${Math.round(fat)}</b></span></div>`
      );
    } catch (_) {
      return "";
    }
  }
  function projectSeasonGainsV85(e, training, preseason) {
    /* v124: a projection never ROLLS the program's fate — it shows the EXPECTED season, so a
     * volatile program's preview sits between its two outcomes instead of promising either. */
    const _fv124 = { growth: planFateExpectV124(training || e.training || "balanced").growth };
    // training: a program key to project INSTEAD of the one chosen (the offseason board
    // asks this for every card); preseason: the whole schedule is still ahead
    const out = {};
    try {
      if (!e || !e.attrs || !e.pos || !POSITIONS[e.pos]) return out;
      const t = LEVELS[e.level];
      if (!t) return out;
      const wk = preseason ? [] : (e.weekResults || []).filter(R => !R.playoff);
      if (wk.length && wk.every(R => R.played)) return out; // the season is in the books: nothing left to project
      const a = PROGRAMS[training || e.training || "balanced"] || PROGRAMS.balanced,
        n = e.eventChoice;
      let i = 0,
        l = 0;
      if (n) {
        const Pe = nodeLvl("genius") > 0 || state.path === "prodigy";
        n.perf && (i += n.perf);
        if (n.reqAttr) {
          const Ae = rivalReqV128(n.reqBase, e.level);
          i += Pe || (e.attrs[n.reqAttr] || 0) >= Ae ? 0 : -(n.perf || 0) * 1.6;
        }
        if (n.gritReqBase) {
          const Ae = eventReq(n.gritReqBase, e.level);
          i += Pe || e.attrs.grit >= Ae ? 0 : -(n.perf || 0) * 1.6;
        }
        if (n.awareReqBase) {
          const Ae = eventReq(n.awareReqBase, e.level);
          i += Pe || e.attrs.awareness >= Ae ? 0 : -(n.perf || 0) * 1.6;
        }
        n.growth && (l += n.growth);
        n.bench && (i -= 14 * n.bench);
      }
      // the expected season average: the games played as they were, the games left at
      // the rating the schedule-maker expects (the mean of et, with the dice removed)
      const played = wk.filter(R => R.played && R.perf != null),
        left = Math.max(0, t.games - played.length);
      let exp;
      if (played.length) exp = played.reduce((s, R) => s + R.perf, 0) / played.length;
      else {
        const s = playerPower(e),
          need =
            t.need -
            6 +
            (typeof chaosOppBoost == "function" ? chaosOppBoost() : 0) +
            (typeof seasonModFx == "function" ? seasonModFx("peerShift") || 0 : 0);
        exp = clamp99((s - need) * 2.4 + 50 + (i / t.games) * 2, 1, 100);
      }
      const U = (played.reduce((s, R) => s + R.perf, 0) + exp * left) / Math.max(1, t.games),
        u = played.filter(R => R.injured).length;
      const ps = e.playoffState,
        b = ps && e.weekResults ? e.weekResults.filter(R => R.playoff && R.played && R.won).length : 0,
        B = !!(ps && ps.champion);
      const Me = 1 + nodeLvl("talent") * 0.018 + nodeLvl("gym") * 0.015 + nodeLvl("mastermind") * 0.025,
        Fe = nodeLvl("prodigy") > 0 && e.seasonsSinceStart < 3 ? 1.16 : 1,
        he = clamp99((U - 32) / 58, 0, 1.15),
        Mt = b * 0.035 + (B ? 0.06 : 0),
        da = clamp99(1 - u * 0.055, 0.72, 1);
      let xt = 1;
      (hasTrait(e, "lateBloomer") && (xt *= e.age >= 16 ? 1.07 : 0.95),
        hasTrait(e, "slowStarter") && (xt *= e.seasonsSinceStart < 3 ? 0.94 : 1.05));
      const ua =
          treeFx("eGrowth") +
          gearFx("growth") +
          Math.max(0, tierGrowth(e) - 1) +
          Math.max(0, pathVal("growthMult", 1) - 1) +
          Math.max(0, l),
        fa = clamp99(1 + ua * 0.22, 0.88, 1.34),
        xe = clamp99(e.potential * Me * Fe * xt * da * fa, 0.62, 1.58),
        st = (0.42 + he * 1.18 + Mt) * xe,
        Tt =
          (0.34 + he * 0.42) *
          (1 + (a.priorityBonus || 0)) *
          (1 + nodeLvl("quickstudy") * 0.035) *
          (hasTrait(e, "gymRat") ? 1.08 : 1) *
          (_fv124 ? _fv124.growth : 1) *
          (1 + gearV147("training")),
        Rt = a.focus ? 0 : (0.16 + he * 0.12) * (1 + (a.priorityBonus || 0)),
        Ye = POSITIONS[e.pos].w,
        $ = Math.max(...Object.values(Ye));
      const w = Object.values(state.tree || {}).reduce((R, O) => R + O, 0),
        k = clamp99(
          30 +
            effectivePrestige(state.prestige) * 1.9 +
            w * 0.68 +
            nodeLvl("freak") * 12 +
            nodeLvl("superhuman") * 20 +
            nodeLvl("primeGenes") * 9 +
            treeFx("ceilPlus") +
            chaosTotal() * 1.6 +
            (pathVal("growthMult", 1) - 1) * 36 +
            (state.path === "phenom" ? 30 : 0),
          30,
          attrCap()
        );
      const bank = e.growthBank || {};
      for (const R of ATTR_KEYS) {
        const O = e.attrs[R] || 1,
          Oq = Ye[R] ? 0.92 + 0.16 * (Ye[R] / $) : 0.86;
        let J = st * Oq + Rt;
        a.focus && a.focus.includes(R) && (J += Tt);
        J = Math.max(0, J);
        const kR = Math.min(attrCap(), Math.round(k * ((e.statCeilV17 && e.statCeilV17[R]) || 1)));
        let Pe = clamp99(1 - (O / attrCap()) * 0.55, 0.35, 1);
        if (O >= kR - 18) {
          const Ro = (O - (kR - 18)) / 18;
          Pe *= clamp99(1 - Ro * 0.88, 0.08, 1);
        }
        let g = (bank[R] || 0) + J * Pe;
        a.cost && a.cost[R] && !nodeLvl("workhorse") && (g += a.cost[R]);
        out[R] = Math.round(g * 10) / 10;
      }
    } catch (_) {}
    return out;
  }
  // one computation per sheet render: Vr is called once per attribute
  function sheetCtxV85(t) {
    const c = t.conditionV11 || {},
      key = [
        c.fatigue,
        c.injury && c.injury.name,
        JSON.stringify(t._tempStatBuffsV25 || 0),
        t.training,
        (t.weekResults || []).filter(w => w.played).length,
        ATTR_KEYS.reduce((s, k) => s + (t.attrs[k] || 0), 0)
      ].join("|");
    const m = attrRow._m;
    if (m && m.t === t && m.key === key && Date.now() - m.at < 400) return m;
    return (attrRow._m = { t, key, at: Date.now(), ef: effAttrsV85(t), pj: projectSeasonGainsV85(t) });
  }
  window.__V85 = { effAttrs: effAttrsV85, project: projectSeasonGainsV85, bodyBadge: bodyBadgeV85 };
  /* ===== v89 THE MENU'S OWN FEED =====
   * The main menu used to scrape its numbers off the hidden legacy screen's text
   * (`valueByLabel("HALL OF FAME POINTS")` found nothing and showed 0). One function
   * hands it everything the new menu draws: the player, the season so far, the
   * objectives with their live checks, the team's custom identity and colors, and
   * the legacy totals — from the state, in one call. Returns null with no state. */
  window.__RIB_MENU_DATA_V89 = function () {
    try {
      const e = state && state.player;
      const hof = (state && state.hof) || [];
      const S = {
        prestige: (state && state.prestige) || 0,
        pp: (state && state.pp) || 0,
        careers: (state && state.careers) || 0,
        nflReached: (state && state.nflReached) || 0,
        interstellar: hof.filter(h => h && (h.level || 0) >= 8).length,
        hallBest: hof.length ? hof[0].goat || 0 : 0,
        enshrined: (state && state.careersCompleted) || 0,
        challenges: Object.keys((state && state.challenges) || {}).length,
        challengesOf: typeof CHALLENGES != "undefined" ? CHALLENGES.length : 0,
        era: (state && state.era) || 0,
        eraName: state && state.era ? eraName() : "",
        bestLevel: state && LEVELS[state.bestLevel || 0] ? LEVELS[state.bestLevel || 0].name : "",
        titles: (state && state.titlesWon) || 0,
        rings: (state && state.rings) || 0,
        recordOvr: (state && state.recordOvr) || 0,
        highScore: (state && state.highScore) || 0,
        lineage: (function () {
          try {
            const F = familyV136();
            return {
              gen: F.gen,
              ordinal: F.ordinal,
              surname: F.surname,
              years: F.years,
              father: F.father
                ? { name: F.father.name, level: F.father.levelName, fate: F.father.fate, say: fateSayV136(F.father) }
                : null
            };
          } catch (e) {
            return null;
          }
        })()
      };
      if (!e || !e.pos) return { hasCareer: !1, state: S };
      const lv = LEVELS[e.level] || LEVELS[0],
        wk = (e.weekResults || []).filter(w => w && !w.playoff),
        played = wk.filter(w => w.played),
        last = played.length ? played[played.length - 1] : null,
        nxt = wk.find(w => !w.played) || null;
      const orig = (typeof getOrigin == "function" && getOrigin(e)) || null,
        traits = (e.traits || [])
          .map(k =>
            TRAITS[k]
              ? { id: k, name: TRAITS[k].name, icon: TRAITS[k].icon, good: TRAITS[k].good, desc: TRAITS[k].desc }
              : null
          )
          .filter(Boolean);
      const done = e.completedObjectivesV11 || {},
        mine = e.legacyObjectivesV11 || [],
        stamps = e.objectiveStampsV89 || {},
        objs = (typeof LEGACY_OBJECTIVES != "undefined" ? LEGACY_OBJECTIVES : []).map(u => {
          let ok = !!done[u.id];
          if (!ok) {
            try {
              ok = !!u.check(e);
            } catch (_) {}
          }
          let st = stamps[u.id];
          if (ok && !st) {
            st = stamps[u.id] = { t: Date.now(), y: (e.totalSeasons || 0) + 1, lv: e.level || 0 };
            e.objectiveStampsV89 = stamps;
          }
          return {
            id: u.id,
            icon: u.icon,
            title: u.title,
            desc: u.description,
            reward: u.reward,
            done: ok,
            mine: mine.includes(u.id),
            at:
              st && st.t
                ? new Date(st.t).toLocaleDateString(undefined, { month: "numeric", day: "numeric", year: "2-digit" })
                : "",
            atYear: st ? st.y : 0
          };
        });
      let rank = null;
      try {
        rank = nationalRank(e, playerOvr(e));
      } catch (_) {}
      let chance = null;
      try {
        chance = declareChanceV88(e);
      } catch (_) {}
      const tc = window.__GRIDIRON_TEAM_CUSTOM__ || {};
      return {
        hasCareer: !0,
        state: S,
        player: {
          name: e.name,
          pos: e.pos,
          level: e.level,
          levelName: lv.name,
          levelKey: lv.key,
          age: e.age,
          stars: e.stars || 0,
          ovr: playerOvr(e),
          softMaxOvr: softMaxOvrV134(e),
          height: e.body ? fmtHeight(bodyOfV112(e).height) : "",
          weight: e.body ? bodyOfV112(e).weight + " lb" : "",
          heightProj: e.body ? fmtHeight(e.body.height) : "",
          weightProj: e.body ? e.body.weight + " lb" : "",
          grownV112: e.body ? !!bodyOfV112(e).grown : !0,
          archetype: orig
            ? { id: orig.id, name: orig.name, icon: orig.icon, strength: orig.strength, weakness: orig.weakness }
            : null,
          path: state.path
            ? { id: state.path, name: (PATHS[state.path] && PATHS[state.path].name) || state.path }
            : null,
          traits,
          seasonsAtLevel: e.seasonsAtLevel || 0,
          totalSeasons: e.totalSeasons || 0,
          depthRole: e.depthRole || "",
          rank: rank ? { rank: rank.rank, of: rank.of, pct: rank.pct } : null,
          chance,
          goals: ur(e),
          objectives: objs,
          points: e.points || 0
        },
        season: {
          games: lv.games,
          played: played.length,
          weeks: wk.map(w => ({
            played: !!w.played,
            won: !!w.won,
            us: w.us,
            them: w.them,
            opp: w.opp,
            sat: !!w.satOut,
            perf: w.perf,
            stat: w.played && w.statLine ? w.statLine : null
          })),
          inProgress: !!(e.weekResults && nxt),
          last: last
            ? {
                won: !!last.won,
                us: last.us,
                them: last.them,
                opp: last.opp,
                stat: last.statLine || null,
                sat: !!last.satOut,
                week: wk.indexOf(last) + 1
              }
            : null,
          nextOpp: nxt ? nxt.opp : null,
          nextWeek: nxt ? wk.indexOf(nxt) + 1 : null
        },
        team: {
          school: tc.schoolName || "",
          name: tc.teamName || "",
          colors: tc.col || null,
          logo: tc.logo != null ? tc.logo : null,
          logoCss: window.TEAM_LOGOS_V44 && tc.logo != null ? window.TEAM_LOGOS_V44.cssFull(tc.logo) : ""
        }
      };
    } catch (err) {
      return null;
    }
  };
  /* ===== v101 WHOLE NUMBERS ON THE SHEET =====
   * Attributes are bought, banked and displayed in whole points, but the sheet was printing
   * whatever the model happened to hold — a banked gain, a conditioned effective value, a
   * flat per-game swing — so a row could read "13.4 -> 12.8". Every player-facing number on
   * the skills surfaces goes through W1 now: one rounding, applied where it is DRAWN, so the
   * maths underneath keeps its precision and the reader never sees a fraction of a point. */
  function wholeNum(v) {
    const n = Number(v);
    return Number.isFinite(n) ? Math.round(n) : 0;
  }
  window.__W1_V101 = wholeNum;
  function attrRow(e, t) {
    const a = wholeNum(t.attrs[e]),
      s = wholeNum(t.lastGains[e] || 0),
      n = t.pos && POSITIONS[t.pos].w[e],
      i = clamp99((a / attrCap()) * 100, 0, 100),
      r = ATTR_INFO[e].metric,
      l = r ? `<span class="attr-metric" title="${r.label}">${r.label}: <b>${r.fmt(a)}</b></span>` : "";
    // v85: the body's cut (or lift) for the next game, and the season's expected gain
    const X = sheetCtxV85(t),
      ev = wholeNum(X.ef.eff[e] != null ? X.ef.eff[e] : a),
      d = ev - a,
      ie = clamp99((ev / attrCap()) * 100, 0, 100),
      pj = X.pj[e] || 0,
      pn = Math.round(pj),
      pStart = Math.max(i, ie),
      pw = pn > 0 ? Math.min(100 - pStart, Math.max(TU("projMinPct", 2.4), (pj / attrCap()) * 100)) : 0;
    const effTag = d
      ? `<span class="eff ${d < 0 ? "dn" : "up"}" title="What you take onto the field next game: your body ${d < 0 ? "takes " + -d + " off" : "adds " + d + " to"} this attribute${X.ef.buffs[e] ? " (includes this game's wheel swing " + (X.ef.buffs[e] > 0 ? "+" : "") + wholeNum(X.ef.buffs[e]) + ")" : ""}">→ ${ev}</span>`
      : "";
    const projTag =
      pn > 0
        ? `<span class="projn" title="Expected gain by season's end on your current plan (${(PROGRAMS[t.training || "balanced"] || {}).name || "training"}) — the hollow bar is where this number is headed">▹+${pn}</span>`
        : pn < 0
          ? `<span class="projn neg" title="Expected change by season's end on your current plan — this program costs the attribute">▹${pn}</span>`
          : "";
    return `<div class="attr ${s ? "up" : ""}${d < 0 ? " worn" : d > 0 ? " fresh" : ""}">
    <div class="top"><span class="an">${ATTR_INFO[e].icon} ${ATTR_INFO[e].name}${statInfoBtnV142(e)}${n ? " •" : ""}${t.statCeilV17 && t.statCeilV17[e] > 1 ? ` <span style="color:#f0bb45;font:700 9px Oswald,sans-serif;margin-left:3px;letter-spacing:.5px" title="Personality raised this stat's MAX LEVEL by ${Math.round((t.statCeilV17[e] - 1) * 100)}%">▲${Math.round((t.statCeilV17[e] - 1) * 100)}% CAP</span>` : ""}</span>
      <span class="av">${a}${s ? `<span class="plus">+${s}</span>` : ""}${effTag}${projTag}</span></div>
    <div class="track ${ovrGradeClass(a)}"><i style="width:${i}%"></i>${d < 0 ? `<b class="loss" style="left:${ie}%;width:${Math.max(0, i - ie)}%"></b>` : d > 0 ? `<b class="gain" style="left:${i}%;width:${Math.max(0, ie - i)}%"></b>` : ""}${pw > 0 ? `<b class="proj" style="left:${pStart}%;width:${pw}%"></b>` : ""}</div>
    ${l}
  </div>`;
  }
  function advanceLevel() {
    const e = state.player,
      t = nationalRank(e, playerOvr(e));
    (e.career.push({
      level: LEVELS[e.level].name,
      ovr: playerOvr(e),
      age: e.age,
      rank: t.rank,
      posRank: t.posRank,
      titles: e.titles || 0
    }),
      e.level++,
      (e.seasonsAtLevel = 0),
      (e.declareBonus = 0),
      (e.playoffState = null),
      e.level > state.bestLevel && (state.bestLevel = e.level));
    const a = grantMilestone(e.level);
    if (LEVELS[e.level].key === "nfl" && !e._wonShown) {
      ((e._wonShown = !0), (state.view = "win"), saveGame(), render());
      return;
    }
    (e.level >= 8 && showToast("🛸 WELCOME TO THE INTERSTELLAR LEAGUE — 96 beings in the galaxy. You are one."),
      genRivals(e),
      saveGame());
    const s = LEVELS[e.level].key;
    if (tiersFor(s) && !(e.tiers && e.tiers[s])) {
      ((state.view = "tier"), render());
      return;
    }
    (a > 0
      ? showToast("🎉 Advanced to " + LEVELS[e.level].name + "! First time here: +" + a + " PP!")
      : showToast("Advanced to " + LEVELS[e.level].name + "!"),
      goView("hub"));
  } /* ===== v139 THE PROGRAM YOU PICKED IS THE TEAMS YOU FACE =====
   * A program's `comp` moved exactly one number — the divisor inside `qt` that decides how hard it
   * is to stand out nationally — and nothing else. The schedule was identical at a Blue-Blood and a
   * Mid-Major, so "Competition Brutal" was a word on a card. It shifts the opponents themselves now,
   * through the one place an opponent is built (`rt`), which is what `oppMulV111`'s wear, the
   * next-opponent card and the scouting report all read. And because the cards can now state a real
   * number, they do: the AVERAGE OPPONENT OVERALL you will face at each one.
   *
   * The reward moved too. The extra recruit star was handed over the moment you committed, before
   * you had played a down. It is paid at the END OF A SEASON in which you held your own — a C or
   * better — and until then the card says so. It stays on offer every season at that level. */
  const TIER_GRADE_OK_V139 = ["A+", "A", "B+", "B", "C"];
  function tierOppShiftV139(level) {
    try {
      const p = state && state.player;
      if (!p || p.level !== level) return 0;
      const t = currentTier(p);
      return t ? TU("tierOppSpreadV139", 26) * (t.comp - 1) : 0;
    } catch (_) {
      return 0;
    }
  }
  function tierOppOvrV139(level, tier) {
    return Math.round(48.5 + level * 5.7 + TU("tierOppSpreadV139", 26) * (((tier && tier.comp) || 1) - 1));
  }
  function tierRewardSayV139(tier) {
    const r = [];
    tier.stars && r.push("+" + tier.stars + " recruit \u2605");
    tier.ppMult > 1 && r.push("+" + Math.round((tier.ppMult - 1) * 100) + "% PP");
    tier.growth !== 1 &&
      r.push((tier.growth > 1 ? "+" : "\u2212") + Math.round(Math.abs(tier.growth - 1) * 100) + "% growth");
    return r.join(" \u00b7 ");
  }
  function tierRewardV139(e, t) {
    try {
      const tier = currentTier(e);
      if (!tier || !tier.stars || !t) return;
      const key = LEVELS[e.level].key;
      if (e._tierStarPaidV139 === key) return;
      const g = String((t.grade && t.grade.grade) || t.grade || "");
      if (TIER_GRADE_OK_V139.indexOf(g) < 0) {
        t.tierRewardV139 = { ok: !1, grade: g, stars: tier.stars, name: tier.name };
        return;
      }
      e.stars = clamp99((e.stars || 1) + tier.stars, 1, 5);
      e._tierStarPaidV139 = key;
      t.tierRewardV139 = { ok: !0, grade: g, stars: tier.stars, name: tier.name };
      const aw = { icon: "\u2B50", name: "Held his own at " + tier.name + " (+" + tier.stars + "\u2605)" };
      Array.isArray(t.awards) && t.awards.push(aw);
      Array.isArray(e.awards) && e.awards.push(aw);
    } catch (_) {}
  }
  window.__TIER_V139 = {
    oppOvr: tierOppOvrV139,
    shift: tierOppShiftV139,
    say: tierRewardSayV139,
    reward: tierRewardV139,
    opp: (n, l, w, sd, r) => createOpponentProfile(n, l, w, sd, r),
    tiers: k => tiersFor(k),
    of: e => currentTier(e)
  };
  function screenTier() {
    const e = state.player,
      t = LEVELS[e.level],
      a = tiersFor(t.key);
    if (!a) {
      goView("hub");
      return;
    }
    ((byId("screen").innerHTML = `
    <div class="eyebrow">${t.name} · Choose Your Program</div>
    <div class="h1">Where Will You Play?</div>
    <div class="sub">Tougher programs really do face stronger teams — the average opponent is on every card — and they develop you <b style="color:var(--good)">faster</b>, but you have to produce more to stand out nationally. Easier programs are safer and grow you slower.</div>
    <div class="mt" style="margin-top:14px">
      ${a
        .map(
          (s, n) => `
        <div class="pos-card ${n === 0 ? "best" : ""}" onclick="chooseTier('${s.key}')">
          <div class="pos-abbr" style="font-size:26px">${s.icon}</div>
          <div class="pos-info">
            <div class="pn">${s.name} ${s.stars ? `<span class="fit-tag fit-natural">+${s.stars}★</span>` : ""}</div>
            <div class="pd">${s.desc}</div>
            <div class="tier-tags">
              <span class="tier-tag" style="color:${s.comp > 1.1 ? "var(--blood)" : s.comp < 1 ? "var(--good)" : "var(--gold)"}">Opponents ~${tierOppOvrV139(e.level, s)} OVR</span>
              <span class="tier-tag" style="color:${s.comp > 1.1 ? "var(--blood)" : s.comp < 1 ? "var(--good)" : "var(--gold)"}">Competition ${s.comp >= 1.3 ? "Brutal" : s.comp >= 1.1 ? "Tough" : s.comp < 1 ? "Light" : "Fair"}</span>
              <span class="tier-tag" style="color:var(--good)">Growth ${s.growth >= 1.3 ? "+++" : s.growth >= 1.1 ? "++" : s.growth < 1 ? "–" : "+"}</span>
              <span class="tier-tag" style="color:var(--gold)">Exposure ${s.ppMult >= 1.5 ? "Elite" : s.ppMult >= 1.2 ? "High" : "Normal"}</span>
            </div>
            <div class="pd" style="color:var(--gold);margin-top:5px">Pays <b>${tierRewardSayV139(s)}</b>${s.stars ? " — the ★ at the end of any season here you grade C or better" : ""}</div>
          </div>
        </div>`
        )
        .join("")}
    </div>
  `),
      (byId("dock").innerHTML =
        '<div class="small center">Ambitious players pick the powerhouse: harder path, but the growth and exposure pay off on the road to the UFF.</div>'));
  }
  function chooseTier(e) {
    const t = state.player,
      a = LEVELS[t.level],
      s = tiersFor(a.key).find(n => n.key === e);
    (t.tiers || (t.tiers = {}),
      (t.tiers[a.key] = e),
      delete t._tierStarPaidV139,
      saveGame(),
      showToast(
        "Committed to " + s.name + "!"
      ) /* v139: the ★ is earned at season's end, not handed over at the door */,
      goView("hub"));
  }
  function startSeason() {
    ((state.player.eventChoice = null), (state.player.pendingEvent = null), goView("training"));
  }
  function screenTraining() {
    const e = state.player,
      t = LEVELS[e.level],
      a = recommendTraining(e);
    ((byId("screen").innerHTML = `
    <div class="eyebrow">${t.name} · Offseason</div>
    <div class="h1">Choose Your Training</div>
    <div class="sub">This is where careers are built. Each program develops specific stats — and some carry real injury risk. Match it to what your player needs.<br><span style="color:var(--chalk-dim)">Each stat shows what your next <b>+1</b> costs in skill points: <b style="color:#7fe0a0">1 pt</b> below its soft cap, then <b style="color:#f0bb45">2</b>, <b style="color:#ff9a5a">3</b>, <b style="color:#ff8a80">4+</b> above it. Priority growth into a capped stat is growth you cannot afford to keep.</span></div>
    <div class="mt" style="margin-top:14px">
      ${Object.entries(PROGRAMS)
        .map(([s, n]) => {
          const i = s === a,
            r = n.injury >= 1.4 ? "var(--blood)" : n.injury >= 1.1 ? "var(--gold)" : "var(--good)",
            l = n.injury >= 1.4 ? "HIGH RISK" : n.injury >= 1.1 ? "SOME RISK" : n.injury < 0.9 ? "SAFE" : "STEADY",
            pj = projectSeasonGainsV85(e, s, !0),
            gv = p => {
              const v = Math.round(pj[p] || 0);
              return v >= 0 ? "+" + v : String(v);
            },
            rng = ks => {
              const vs = ks.map(p => pj[p] || 0);
              if (!vs.length) return "";
              const lo = Math.min(...vs),
                hi = Math.max(...vs),
                F = v => String(Math.round(v));
              return lo === hi || F(lo) === F(hi) ? "+" + F(hi) : "+" + F(lo) + " to +" + F(hi);
            },
            others = ATTR_KEYS.filter(p => !(n.focus || []).includes(p) && !(n.cost && n.cost[p])),
            c = n.focus
              ? `FOCUS STATS ${rng(n.focus)} · OTHERS ${rng(others)} THIS SEASON`
              : `EVERY STAT ${rng(ATTR_KEYS)} THIS SEASON`,
            u = (n.focus || [])
              .map(
                p =>
                  `<span class="train-chip">${ATTR_INFO[p].icon} ${ATTR_INFO[p].name} <b class="v85g" style="color:var(--good);margin:0 6px 0 3px">${gv(p)}</b>${capBadgeV67(e, p)}</span>`
              )
              .join(""),
            cp = capPlanV67(e, n.focus),
            h = n.cost
              ? Object.entries(n.cost)
                  .map(([p, m]) => `<span class="train-chip neg">▼ ${ATTR_INFO[p].name} ${m}</span>`)
                  .join("")
              : "";
          return `<div class="pos-card ${i ? "best" : ""} train-card" onclick="chooseTraining('${s}')">
          <div class="train-head">
            ${window.RIB_SKILL_ICO ? window.RIB_SKILL_ICO("tp_" + s, n.icon, 54) : `<span style="font-size:24px">${n.icon}</span>`}
            <div style="flex:1;min-width:0">
              <div class="pn">${n.name} ${i ? '<span class="fit-tag fit-natural">SUGGESTED</span>' : ""}</div>
              <div class="train-meta"><span style="color:var(--good)">${c}</span> · <span style="color:${r}">${l}</span>${n.bonusPoints ? ` · <span style="color:var(--gold)">+${n.bonusPoints} pts</span>` : ""}${cp ? ` · <span style="color:${cp.col}">${cp.label}</span>` : ""}</div>
            </div>
          </div>
          <div class="train-chips">${
            u ||
            `<span class="train-chip">⚖️ All stats evenly</span>` +
              Object.keys(POSITIONS[e.pos].w)
                .sort((p, m) => POSITIONS[e.pos].w[m] - POSITIONS[e.pos].w[p])
                .slice(0, 4)
                .map(
                  p => `<span class="train-chip">${ATTR_INFO[p].icon} ${ATTR_INFO[p].name}${capBadgeV67(e, p)}</span>`
                )
                .join("")
          }${h}</div>
        </div>`;
        })
        .join("")}
    </div>
  `),
      (byId("dock").innerHTML =
        '<div class="small center">All stats grow from season performance. Your program marks priority stats for a modest extra boost.</div>'));
  }
  function recommendTraining(e) {
    /* v124: no conditioning short-circuit — the whole sheet is scored, durability included */ const t =
      trainWhyV124(e);
    return t && PROGRAMS[t.prog] ? t.prog : "balanced";
  }
  function chooseTraining(e) {
    ((state.player.training = e), saveGame());
    const t = state.player;
    t.midInjury = Math.random() < 0.35;
    const a = SEASON_EVENTS.filter(
        n =>
          !(
            t.level < (n.minLevel || 0) ||
            (n.maxLevel != null && t.level > n.maxLevel) ||
            (n.cond && !n.cond({ player: t, midInjury: t.midInjury }))
          )
      ),
      s = a.find(n => n.id === "combine" && t.level === 6);
    if (s) {
      ((t.pendingEvent = s.id), goView("event"));
      return;
    }
    if (a.length && Math.random() < 0.7) {
      const n = [];
      (a.forEach(i => {
        for (let r = 0; r < (i.weight || 1); r++) n.push(i);
      }),
        (t.pendingEvent = randPick(n).id),
        goView("event"));
      return;
    }
    goView("sim");
  }
  function screenEvent() {
    const e = state.player,
      t = SEASON_EVENTS.find(s => s.id === e.pendingEvent);
    if (!t) {
      goView("sim");
      return;
    }
    const a = LEVELS[e.level];
    ((byId("screen").innerHTML = `
    <div class="eyebrow">${a.name} · Season Event</div>
    <div class="h1">${t.title}</div>
    <div class="card" style="border-color:var(--gold)">
      <div style="font-family:'Special Elite';font-size:15px;line-height:1.5;color:var(--chalk)">${typeof t.text == "function" ? t.text({ player: e }) : t.text}</div>
    </div>
    <div class="h2">Your Decision</div>
    ${
      t.id === "bigGame" && TU("rivalSpinOnWeekV136", 1)
        ? rivalDeferCardV136(e)
        : t.choices
            .map((s, n) => {
              let i = "";
              if (s.eff.gritReqBase) {
                const l = eventReq(s.eff.gritReqBase, e.level);
                i =
                  e.attrs.grit >= l
                    ? `<span class="fit-tag fit-natural">GRIT ${e.attrs.grit} ✓</span>`
                    : `<span class="fit-tag fit-poor">NEEDS GRIT ${l}</span>`;
              }
              if (s.eff.awareReqBase) {
                const l = eventReq(s.eff.awareReqBase, e.level);
                i =
                  e.attrs.awareness >= l
                    ? `<span class="fit-tag fit-natural">IQ ${e.attrs.awareness} ✓</span>`
                    : `<span class="fit-tag fit-poor">NEEDS IQ ${l}</span>`;
              }
              /* ===== v128: what it does, what it takes, and how wide it swings =====
               * The five options used to carry a mood ("+Big performance, real injury risk") and a
               * requirement nobody could clear, which then silently took a -1.6x rating penalty. Every
               * option now states its effects — built from its own `eff`, so the card cannot drift from
               * the model — states its VARIANCE in words, and is genuinely LOCKED when the attribute is
               * not there, rather than open and punishing. */
              const lk = rivalLockV128(s.eff, e),
                sayV = rivalSayV128(s.eff, e),
                vr = rivalVarV128(s.eff);
              if (lk)
                i = lk.ok
                  ? `<span class="fit-tag fit-natural">${escHtml(lk.name.toUpperCase())} ${lk.have} ✓</span>`
                  : `<span class="fit-tag fit-poor">🔒 NEEDS ${escHtml(lk.name.toUpperCase())} ${lk.need} — YOU HAVE ${lk.have}</span>`;
              const dead = (s.eff.ppCost && state.pp < s.eff.ppCost) || (lk && !lk.ok);
              return (
                s.eff.ppCost &&
                  state.pp < s.eff.ppCost &&
                  (i = `<span class="fit-tag fit-poor">NEED ${s.eff.ppCost} PP</span>`),
                `<div class="pos-card" style="${dead ? "opacity:.45;pointer-events:none" : ""}" onclick="chooseEvent(${n})">
        <div class="pos-info">
          <div class="pn">${s.label} ${i}</div>
          ${sayV.length ? `<ul class="rivsay-v128">${sayV.map(x => `<li>${x}</li>`).join("")}</ul>` : `<div class="pd">${s.sub || ""}</div>`}
          <div class="rivvar-v128">VARIANCE <b style="color:${vr.col}">${vr.band}</b> · <span>${vr.line}</span></div>
          ${lk && !lk.ok ? `<div class="rivlock-v128">🔒 Locked. Train <b>${escHtml(lk.name)}</b> to <b>${lk.need}</b> — you are at <b>${lk.have}</b>, ${lk.need - lk.have} short.</div>` : ""}
        </div>
        <div class="pos-fit" style="font-size:22px">${dead ? "🔒" : "▸"}</div>
      </div>`
              );
            })
            .join("")
    }
  `),
      (byId("dock").innerHTML =
        '<div class="small center">Every option states exactly what it does. A locked one needs the attribute it names — build the player, then take the choice.</div>'));
  }
  window.__ZaV18 = SEASON_EVENTS;
  /* ===== v136 A THE RIVAL IS SPUN ON GAME WEEK =====
   * Rivalry Week's five approaches were decided at the season's start, on the event screen, by the
   * v13 story wheel rolling over the buttons — weeks before the game it was about, on a screen that
   * had nothing to do with the rival. The event screen now only INTRODUCES the rivalry (`rivalDeferV136`
   * books the fixture with a pending choice) and the approach is spun in the PREGAME of the rivalry
   * game, on its own page after the plan wheel, from a deck that leaves every locked approach off the
   * wheel (`rivalDeckV136`), weighted by the same personality appetite the plan wheel uses
   * (`rivalWeightsV136`). `rivalApplyV136` writes the winning `eff` into `player.eventChoice` — the
   * same object `ca()` and the season roll always read — so it counts from that game on, and
   * `rivalResolveV136` is the safety net `ca()` runs first on any rivalry week reached without the
   * page (a skipped pregame, a simmed week). `TU("rivalSpinOnWeekV136",0)` puts the choice back on the
   * event screen. `window.__V136_A`. */
  function rivalPendingV136(pl) {
    const c = pl && pl.eventChoice;
    return !!(c && c.rivalV128 && c.pendingV136);
  }
  function rivalChoicesV136() {
    const ev = SEASON_EVENTS.find(z => z.id === "bigGame");
    return ev ? ev.choices : [];
  }
  function rivalWeightsV136(pl, choices) {
    let pr = 0;
    try {
      const G = window.__GROWTH_V42,
        V = window.__PREGAME_V51;
      pr = V && V.appetite && G && G.persona ? V.appetite(G.persona(pl)) : 0;
    } catch (e) {
      pr = 0;
    }
    const raw = choices.map(c => {
      const e = c.eff || {},
        b = clamp99(
          ((e.perf || 0) / 16) * 0.45 + (e.inj || 0) * 1.6 + ((e.varMult || 1) - 1) * 0.5 + (e.bench || 0) * 0.5 + 0.1,
          0,
          1
        ),
        al = clamp99((1 + pr * (2 * b - 1)) / 2, 0, 1);
      return { bold: b, w: 0.45 + 0.55 * Math.pow(al, 1.3) };
    });
    const n = raw.length,
      fl = clamp99(TU("wheelWedgeFloor", 0.035), 0, 1 / (n + 1)),
      T = raw.reduce((s, r) => s + r.w, 0) || 1;
    return raw.map(r => ({ bold: r.bold, w: fl + (r.w / T) * (1 - fl * n) }));
  }
  function rivalDeckV136(pl) {
    const all = rivalChoicesV136().map((c, i) => ({ i, label: c.label, eff: c.eff, lock: rivalLockV128(c.eff, pl) })),
      open = all.filter(c => !c.lock || c.lock.ok),
      W = rivalWeightsV136(pl, open);
    open.forEach((c, k) => {
      c.w = W[k].w;
      c.bold = W[k].bold;
    });
    return { all, open };
  }
  function rivalDecideV136(pl) {
    const deck = rivalDeckV136(pl);
    if (!deck.open.length) return null;
    const tot = deck.open.reduce((s, c) => s + c.w, 0);
    let r = Math.random() * tot,
      idx = 0;
    for (let i = 0; i < deck.open.length; i++) {
      r -= deck.open[i].w;
      if (r <= 0) {
        idx = i;
        break;
      }
    }
    return { deck, idx, pick: deck.open[idx] };
  }
  function rivalApplyV136(pl, pick, how) {
    if (!pl || !pick) return;
    pl.eventChoice = Object.assign({}, pick.eff, {
      rivalV128: !0,
      pendingV136: !1,
      chosenV136: pick.label,
      howV136: how || "wheel"
    });
    pl.eventLog = (pl.eventLog || []).concat({ title: "Rivalry Week", choice: pick.label });
    try {
      saveGame();
    } catch (e) {}
  }
  function rivalResolveV136(pl, how) {
    if (!rivalPendingV136(pl)) return null;
    const d = rivalDecideV136(pl);
    if (!d) return null;
    rivalApplyV136(pl, d.pick, how || "auto");
    return d;
  }
  function rivalDeferV136() {
    const t = state.player,
      a = t && SEASON_EVENTS.find(n => n.id === t.pendingEvent);
    if (!t || !a) return;
    t.eventChoice = { rivalV128: !0, perf: 0, varMult: 1, pendingV136: !0 };
    t.eventLog = (t.eventLog || []).concat({ title: a.title, choice: "The approach is spun on game week" });
    saveGame();
    goView("sim");
  }
  function rivalDeferCardV136(e) {
    const D = rivalDeckV136(e);
    return `<div class="card tight rival-defer-v136" style="border-color:var(--gold)"><div class="l" style="font-size:11px;color:var(--gold);letter-spacing:2px;margin-bottom:6px">🎡 SPUN ON GAME WEEK</div>
    <div class="small">Five approaches are on the board. The wheel picks yours in the <b>pregame of the rivalry game</b> — your personality loads it, a locked approach is off the wheel — and it counts from that game on.</div>
    <ul class="rivsay-v128" style="margin-top:8px">${D.all.map(c => `<li>${c.lock && !c.lock.ok ? "🔒 " : "▸ "}<b>${escHtml(c.label)}</b>${c.lock && !c.lock.ok ? ` <span style="color:#ff9b93">— needs ${escHtml(c.lock.name)} ${c.lock.need}, you have ${c.lock.have}</span>` : ""}</li>`).join("")}</ul></div>
    <button class="btn" onclick="rivalDeferV136()">GOT IT — SEE YOU ON RIVALRY WEEK</button>`;
  }
  window.rivalDeferV136 = rivalDeferV136;
  window.__V136_A = {
    pending: rivalPendingV136,
    deck: rivalDeckV136,
    decide: rivalDecideV136,
    apply: rivalApplyV136,
    resolve: rivalResolveV136,
    say: rivalSayV128,
    variance: rivalVarV128,
    lock: rivalLockV128,
    defer: rivalDeferV136,
    spins: 0
  };
  function chooseEvent(e) {
    const t = state.player,
      a = SEASON_EVENTS.find(n => n.id === t.pendingEvent),
      s = a.choices[e];
    const _lkV128 = rivalLockV128(s.eff, t);
    if (_lkV128 && !_lkV128.ok) {
      showToast("🔒 Locked — you need " + _lkV128.need + " " + _lkV128.name + " and you have " + _lkV128.have + ".");
      return;
    } /* v128: locked means locked */
    (s.eff.ppCost && (state.pp -= s.eff.ppCost),
      (t.eventChoice = s.eff),
      (function () {
        try {
          const fx = s.eff,
            c = typeof cv18 === "function" ? cv18(t) : null;
          if (!c) return;
          if (fx.restWeeks) {
            const cur = c.injury;
            (!cur || !cur.seasonEnding) &&
              (c.injury = {
                name: "Recovery rest",
                severity: 1,
                weeksRemaining: fx.restWeeks,
                recurrence: 0.04,
                resting: !0
              });
          }
          if (fx.healFatigue) c.fatigue = Math.max(0, (c.fatigue || 0) - fx.healFatigue);
        } catch (_e) {}
      })(),
      (t.eventLog = (t.eventLog || []).concat({ title: a.title, choice: s.label })),
      saveGame(),
      goView("sim"));
  }
  const ROSTER_FIRST = [
      "Jaylen",
      "Marcus",
      "DeShawn",
      "Tyler",
      "Cameron",
      "Malik",
      "Trey",
      "Xavier",
      "Cole",
      "Bryce",
      "Damon",
      "Kaiden",
      "Rashad",
      "Elijah",
      "Jaxon",
      "Dominic",
      "Isaiah",
      "Devin",
      "Cade",
      "Amari",
      "Zion",
      "Kobe",
      "Deon",
      "Landon",
      "Micah",
      "Tariq",
      "Jamal",
      "Nolan",
      "Rex",
      "Hunter"
    ],
    ROSTER_LAST = [
      "Carter",
      "Brooks",
      "Sylla",
      "Okafor",
      "Ramirez",
      "Bennett",
      "Washington",
      "Freeman",
      "Toledo",
      "Nakamura",
      "Boateng",
      "Vance",
      "Ellison",
      "Duval",
      "Krause",
      "Mbeki",
      "Santos",
      "Rivera",
      "Holloway",
      "Pierce",
      "Dresden",
      "Ansah",
      "Coleman",
      "Reyes",
      "Njoku",
      "Fontaine",
      "Barlow",
      "Greer",
      "Adeyemi",
      "Stroud"
    ];
  function randName() {
    return randPick(ROSTER_FIRST) + " " + randPick(ROSTER_LAST);
  } /* ===== v141 EVERY STAT IS ON THE FIELD =====
   * FieldSim's makeAgents asks the engine's accessor for eighteen keys by name (SIM_KEYS_V111 is the
   * list). The you-player's roster entry used to carry TWELVE — and one of those under the wrong name
   * (`accel`, asked for as `acceleration`) — so nine sheet stats (quickness, throwing, vision, jumping,
   * stamina, grit, discipline, ball control, and the sustained half of acceleration) never reached the
   * agent at all: the accessor's fallback answered a flat 45 for every man on the field, which the
   * league normalise then turned into ~80 at Pee Wee and the 20 floor at the UFF (measured: every UFF
   * quarterback threw as a 21, every UFF defender reacted in 409 ms, every Pee Wee kid was a composed
   * genius). And the twelve that did arrive were squeezed through `99·(1−e^(−v/78))`, which puts 250
   * at 95 and 350 at 98 — three roster points for the hundred sheet points the 5x wall charges.
   *   `simScaleV141` is the one curve a sheet value takes onto the roster: OVR's own curve below the
   * wall, continued past it (250 → ~116, 350 → ~172), so a stat sits on the same scale `_lgAvg`
   * compares it against. `qr()` emits all eighteen keys (the old `accel`/`hands`/`power` aliases
   * kept), `h()` in `Wr` generates them for the AI with position bumps, the accessor's fallback is
   * the man's OWN OVR (which normalises to league-average, never a level-dependent constant), the
   * normalise eases through a soft knee to 99 instead of clamping at 95, and no position's star floor
   * sits under `TU("starFloorMinV141")`. Durability gets a FieldSim read (`dur`: how much speed a
   * carrier keeps through a glancing hit) and its injury curve keeps working past 100.
   * `window.__V141`; `v141check.mjs`, then `scoreneutralcheck.mjs`, `equaltalentcheck.mjs`. */
  function simScaleV141(v) {
    v = Number(v) || 10;
    const w = TU("simWallV141", 215);
    return v <= w ? 99 * (1 - Math.exp(-v / 78)) : 99 * (1 - Math.exp(-w / 78)) + Math.pow(v - w, 0.93) * 0.83;
  }
  function youSimAttrs(e, t) {
    const a = state.player;
    if (!a || !a.attrs) return null;
    const s = i => clamp99(Math.round(simScaleV141(i || 10)), 5, 999),
      n = a.attrs,
      acc = s(n.acceleration),
      cat = s(n.catching),
      str = s(n.strength);
    return {
      speed: s(n.speed),
      acceleration: acc,
      accel: acc,
      burst: acc,
      quickness: s(n.quickness),
      agility: s(n.agility),
      strength: str,
      power: str,
      catching: cat,
      hands: cat,
      throwing: s(n.throwing),
      tackling: s(n.tackling),
      blocking: s(n.blocking),
      awareness: s(n.awareness),
      coverage: s(n.awareness * 0.5 + n.speed * 0.5),
      vision: s(n.vision),
      jumping: s(n.jumping),
      stamina: s(n.stamina),
      grit: s(n.grit),
      discipline: s(n.discipline),
      ballControl: s(n.ballControl),
      injuryResist: s(n.injuryResist)
    };
  }
  window.__V141 = {
    simScale: simScaleV141,
    rosterKeys: () => Object.keys(youSimAttrs() || {}),
    floorMin: () => TU("starFloorMinV141", 0.6)
  };
  function jerseyNum(e) {
    const a = {
      QB: [1, 19],
      RB: [20, 49],
      WR: [10, 19],
      TE: [80, 89],
      OL: [50, 79],
      DL: [90, 99],
      LB: [40, 59],
      CB: [20, 39],
      S: [20, 39]
    }[e] || [1, 99];
    return randInt(a[0], a[1]);
  }
  function buildGameRosters(e, t, a, s, n, i, r) {
    const l = ["QB", "RB", "RB", "WR", "WR", "TE", "OL", "OL", "OL", "OL", "OL"],
      d = ["DL", "DL", "DL", "DL", "LB", "LB", "LB", "CB", "CB", "S", "S"],
      c = ["QB", "RB", "WR", "TE", "OL"].includes(e);
    ((i = i || (state.player ? state.player.level : 0)), (r = r || (state.player ? playerOvr(state.player) : 40)));
    const u = [18, 30, 42, 54, 66, 78, 86, 90][i] || 55;
    function h(Y, _) {
      const ie = clamp99(_, 5, 99),
        B = () => ie + randRange(-14, 14),
        b = {
          speed: B(),
          accel: B(),
          agility: B(),
          strength: B(),
          catching: B(),
          tackling: B(),
          awareness: B(),
          hands: B(),
          power: B(),
          coverage: B(),
          blocking: B(),
          burst: B(),
          quickness: B(),
          throwing: B(),
          vision: B(),
          jumping: B(),
          stamina: B(),
          grit: B(),
          discipline: B(),
          ballControl: B(),
          injuryResist: B()
        };
      for (const Q in b) b[Q] = clamp99(Math.round(b[Q]), 5, 99);
      const pe = Math.random();
      (Y === "RB" &&
        (pe < 0.4
          ? ((b.speed = clamp99(b.speed + 14, 5, 99)),
            (b.burst = clamp99(b.burst + 12, 5, 99)),
            (b.strength = clamp99(b.strength - 8, 5, 99)))
          : pe < 0.7 &&
            ((b.strength = clamp99(b.strength + 15, 5, 99)),
            (b.power = clamp99(b.power + 12, 5, 99)),
            (b.speed = clamp99(b.speed - 8, 5, 99)))),
        Y === "WR" &&
          (pe < 0.45
            ? ((b.speed = clamp99(b.speed + 16, 5, 99)), (b.burst = clamp99(b.burst + 12, 5, 99)))
            : ((b.catching = clamp99(b.catching + 12, 5, 99)),
              (b.hands = clamp99(b.hands + 12, 5, 99)),
              (b.strength = clamp99(b.strength + 6, 5, 99)))),
        Y === "QB" &&
          ((b.awareness = clamp99(b.awareness + 8, 5, 99)),
          pe < 0.4 && ((b.speed = clamp99(b.speed + 16, 5, 99)), (b.agility = clamp99(b.agility + 12, 5, 99)))),
        Y === "TE" && ((b.strength = clamp99(b.strength + 10, 5, 99)), (b.blocking = clamp99(b.blocking + 10, 5, 99))),
        Y === "OL" &&
          ((b.blocking = clamp99(b.blocking + 18, 5, 99)),
          (b.strength = clamp99(b.strength + 14, 5, 99)),
          (b.speed = clamp99(b.speed - 16, 5, 99))),
        Y === "DL" && ((b.strength = clamp99(b.strength + 16, 5, 99)), (b.power = clamp99(b.power + 12, 5, 99))),
        Y === "LB" && ((b.tackling = clamp99(b.tackling + 14, 5, 99)), (b.strength = clamp99(b.strength + 8, 5, 99))),
        Y === "CB" &&
          ((b.speed = clamp99(b.speed + 14, 5, 99)),
          (b.coverage = clamp99(b.coverage + 16, 5, 99)),
          (b.agility = clamp99(b.agility + 10, 5, 99))),
        Y === "S" && ((b.coverage = clamp99(b.coverage + 10, 5, 99)), (b.tackling = clamp99(b.tackling + 10, 5, 99))));
      /* v141: the nine keys the roster never carried, shaped by position like the twelve above */
      const K = (k, d) => {
        b[k] = clamp99(b[k] + d, 5, 99);
      };
      Y === "QB" && (K("throwing", TU("aiQbThrowBumpV141", 18)), K("vision", 8), K("discipline", 6), K("grit", 4));
      Y === "RB" && (K("vision", 10), K("ballControl", 10), K("quickness", 6), K("jumping", 4), K("stamina", 4));
      Y === "WR" && (K("jumping", 10), K("quickness", 6), K("stamina", 4));
      Y === "TE" && (K("jumping", 6), K("grit", 4));
      Y === "OL" && (K("discipline", 6), K("grit", 6), K("stamina", 4), K("quickness", -6), K("jumping", -8));
      Y === "DL" && (K("quickness", 10), K("grit", 6), K("jumping", 2));
      Y === "LB" && (K("discipline", 8), K("vision", 6), K("grit", 6), K("quickness", 4), K("stamina", 6));
      Y === "CB" && (K("quickness", 10), K("jumping", 8), K("discipline", 2));
      Y === "S" && (K("vision", 8), K("jumping", 6), K("discipline", 4), K("grit", 4));
      b.acceleration = b.accel = b.burst; /* one sheet stat, three names — they agree, as qr() makes them agree */
      if (!TU("v141Keys", 1))
        for (const Q of [
          "acceleration",
          "quickness",
          "throwing",
          "vision",
          "jumping",
          "stamina",
          "grit",
          "discipline",
          "ballControl",
          "injuryResist"
        ])
          delete b[Q]; /* kill-switch: the pre-v141 roster */
      return b;
    }
    function p(Y, _) {
      const ie = clamp99(_, 0.1, 1),
        B = (pe, Q) => Math.max(0, Math.round(pe * ie + randRange(-Q, Q))),
        b = { pos: Y };
      switch (Y) {
        case "QB":
          ((b.pass = B(240, 50)),
            (b.td = B(2.4, 1)),
            (b.int = Math.max(0, Math.round((1 - ie) * 2.2))),
            (b.comp = B(21, 4)),
            (b.att = b.comp + B(11, 3)));
          break;
        case "RB":
          ((b.rush = B(88, 28)), (b.td = B(1.1, 1)), (b.carries = B(18, 5)), (b.rec = B(20, 12)));
          break;
        case "WR":
          ((b.rec = B(74, 34)), (b.td = B(0.8, 1)), (b.rec_c = B(5, 3)), (b.tgt = b.rec_c + B(3, 2)));
          break;
        case "TE":
          ((b.rec = B(48, 22)), (b.td = B(0.6, 1)), (b.rec_c = B(4, 2)), (b.tgt = b.rec_c + B(2, 2)));
          break;
        case "OL":
          ((b.pancake = B(5, 3)), (b.sackAllowed = Math.max(0, Math.round((1 - ie) * 3))));
          break;
        case "DL":
          ((b.tackle = B(4, 2)), (b.sack = B(1, 1)), (b.tfl = B(1.4, 1)), (b.qbhit = B(3, 2)));
          break;
        case "LB":
          ((b.tackle = B(8, 3)),
            (b.sack = B(0.6, 1)),
            (b.tfl = B(1.6, 1)),
            (b.int = Math.random() < ie * 0.25 ? 1 : 0));
          break;
        case "CB":
          ((b.tackle = B(4, 2)), (b.pd = B(1.8, 1)), (b.int = Math.random() < ie * 0.3 ? 1 : 0));
          break;
        case "S":
          ((b.tackle = B(6, 3)), (b.pd = B(1.3, 1)), (b.int = Math.random() < ie * 0.25 ? 1 : 0));
          break;
      }
      return b;
    }
    function m(Y) {
      return clamp99(Math.round(u * (0.72 + Y * 0.5) + randRange(-4, 4)), 3, 999);
    }
    function y(Y) {
      const _ = (ie, B) =>
        ie.map(b => {
          const pe = clamp99(Y + randRange(-0.15, 0.15), 0.05, 1.7),
            Q = m(pe);
          return { name: randName(), num: jerseyNum(b), pos: b, isOff: B, ovr: Q, attrs: h(b, Q), stat: p(b, pe) };
        });
      return { off: _(l, !0), def: _(d, !1) };
    }
    const _oppMul = typeof window < "u" && window.__oppMulV22 != null ? window.__oppMulV22 : 0.9 + Math.random() * 0.4;
    const _prF = Math.min(
      1,
      (state.prestige || 0) / 15 +
        (state.rosterPrestigeV158 ? Object.values(state.rosterPrestigeV158).reduce((A2, B2) => A2 + B2, 0) : 0) / 50 +
        ((state.tree ? Object.values(state.tree).reduce((A2, B2) => A2 + B2, 0) : 0) / 70) *
          clamp99(TU("teamQualK", 0.1), 0, 1)
    ); /* ===== v76 MARGIN CURVE (1/5) — where the blowouts came from =====
     * usQ is .42 + seed*.35 + prestige*1.05 (up to 1.65) while oppQ can only ever reach
     * (1.14-.72)*2 = .84, so a prestiged career fields a roster built at up to TWICE the
     * opponent's quality factor. Every player attribute derives from that factor, so the
     * mismatch compounded through every snap of a twelve-drive game: a +10 scoreboard-OVR
     * edge produced a +28.6 average margin, and one game finished 151 points.
     *
     * An earlier cut of v76 fixed it HERE, pulling the two factors toward their midpoint.
     * That was wrong twice over and the checks caught both. The two factors are not on
     * the same scale, so a shared "midpoint" tied the opponent's strength to OUR prestige
     * and cost the v68 team-quality nerf its meaning (`teamqualcheck` went 9.0x -> 2.6x).
     * And it compressed the BADGE as well, collapsing the matchup range from +28 to +15 —
     * which does not make blowouts closer, it deletes the lopsided fixtures they happen
     * in. The schedule is supposed to contain mismatches; what should not follow from one
     * is a 100-0 scoreline. So the roster is left exactly as it was, and the whole curve
     * is carried by the four play-level levers below, which act on the game rather than
     * on the team sheet. ===== */
    const C = clamp99(
        0.42 + t * 0.35 + _prF * 1.05 + clubQV146B(state.player) /* v146 B: the club he signed with */,
        0.3,
        1.65
      ),
      V = clamp99((_oppMul - 0.72) * 2, 0.36, 1.16),
      P = y(C),
      v = y(V),
      j = c ? P.off : P.def,
      U = j.findIndex(Y => Y.pos === e);
    U >= 0 &&
      (j[U] = {
        name: (state.player && state.player.name) || "You",
        num: jerseyNum(e),
        pos: e,
        isOff: c,
        you: !0,
        ovr: r,
        attrs: youSimAttrs(),
        stat: a
      });
    (function () {
      const seen = new Set();
      [...P.off, ...P.def, ...v.off, ...v.def].forEach(pl => {
        if (!pl || pl.you) {
          pl && pl.name && seen.add(pl.name);
          return;
        }
        let nm2 = pl.name,
          guard = 0;
        while (seen.has(nm2) && guard++ < 40) nm2 = randName();
        pl.name = nm2;
        seen.add(nm2);
      });
    })();
    const ce = Y => Math.round([...Y.off, ...Y.def].reduce((_, ie) => _ + ie.ovr, 0) / 22);
    return (
      (P.ovr = ce(P)),
      (v.ovr = ce(v)),
      (P.offOvr = Math.round(P.off.reduce((Y, _) => Y + _.ovr, 0) / 11)),
      (P.defOvr = Math.round(P.def.reduce((Y, _) => Y + _.ovr, 0) / 11)),
      (v.offOvr = Math.round(v.off.reduce((Y, _) => Y + _.ovr, 0) / 11)),
      (v.defOvr = Math.round(v.def.reduce((Y, _) => Y + _.ovr, 0) / 11)),
      { us: P, opp: v, usQ: C, oppQ: V }
    );
  }
  function qi(e, t, a) {
    const s = {
        pass: 0,
        rush: 0,
        rec: 0,
        td: t.td || 0,
        tackle: t.tackle || 0,
        sack: t.sack || 0,
        int: t.int || 0,
        pd: t.pd || 0,
        pick6: t.pick6 || 0,
        comp: 0,
        att: 0,
        rec_c: 0,
        tgt: 0,
        carries: 0,
        longest: 0,
        fum: 0,
        ff: 0,
        tfl: 0,
        qbhit: 0,
        pancake: 0,
        sackAllowed: 0
      },
      n = (i, r) => Math.max(0, Math.round(i / Math.max(1, r)));
    switch (e) {
      case "QB":
        ((s.pass = t.pass || 0),
          (s.rush = t.rush || 0),
          (s.att = Math.max(n(s.pass, 8), 8)),
          (s.comp = Math.min(s.att, Math.max(n(s.pass, 12), Math.round(s.att * 0.6)))),
          (s.longest = s.pass > 0 ? clamp99(Math.round(s.pass * 0.28), 8, 80) : 0),
          (s.int = Math.max(0, a.int || 0)));
        break;
      case "RB":
        ((s.rush = t.rush || 0),
          (s.rec = t.rec || 0),
          (s.carries = t.carries > 0 ? t.carries : Math.max(n(s.rush, 5), s.rush > 0 ? 6 : 0)),
          (s.rec_c = t.rec_c > 0 ? t.rec_c : s.rec > 0 ? Math.max(1, n(s.rec, 9)) : 0),
          (s.longest = s.rush > 0 ? clamp99(Math.round(s.rush * 0.35), 4, 75) : 0),
          (s.fum = a.fum || 0));
        break;
      case "WR":
      case "TE":
        ((s.rec = t.rec || 0),
          (s.rush = t.rush || 0),
          (s.rec_c = t.rec_c > 0 ? t.rec_c : s.rec > 0 ? Math.max(1, n(s.rec, 13)) : 0),
          (s.tgt = s.rec_c + (s.rec_c > 0 ? randInt(1, 3) : randInt(0, 2))),
          (s.longest = s.rec > 0 ? clamp99(Math.round(s.rec * 0.4), 6, 80) : 0));
        break;
      case "OL":
        ((s.pancake = a.pancake || 0), (s.sackAllowed = a.sackAllowed || 0));
        break;
      case "DL":
        ((s.sack = t.sack || 0),
          (s.tackle = t.tackle || 0),
          (s.tfl = Math.max(a.tfl || 0, s.sack)),
          (s.qbhit = a.qbhit || 0),
          (s.ff = a.ff || 0));
        break;
      case "LB":
        ((s.tackle = t.tackle || 0),
          (s.sack = t.sack || 0),
          (s.tfl = a.tfl || 0),
          (s.int = t.int || 0),
          (s.ff = a.ff || 0));
        break;
      case "CB":
      case "S":
        ((s.tackle = t.tackle || 0),
          (s.int = t.int || 0),
          (s.pd = t.pd || 0),
          (s.tfl = a.tfl || 0),
          (s.tgt = a.tgt || 0));
        break;
    }
    return s;
  } /* ===== v101 THE PLAYBOOK — forty-two calls instead of nine =====
   * The offence had NINE plays: four runs (inside, power, sweep, draw) and five passes
   * (quick, dropback, shot, fade, screen). Those are not plays, they are FAMILIES — every
   * inside run hit the same two gaps and every dropback drew its routes out of the same
   * medium pool, so a whole season of football was nine pictures on a loop.
   *
   * A play here is a real call. It names:
   *   - the family it belongs to (`base`), which is one of the original nine, so every
   *     downstream consumer — the pressure model, the render fallback, the tail curve, the
   *     scramble rules — keeps working on the same vocabulary it always had;
   *   - what the sim should actually DO with it: a run names its POINT OF ATTACK (the gap
   *     the back is aiming at) and a pass names the ROUTES it is built out of, both fed
   *     straight into the FieldSim as `opts.gap` / `opts.routes`;
   *   - when it gets called (`fit`), as a weight over the situation rather than a chain of
   *     if/elses — short yardage pulls Iso and Sneak to the front, third-and-long pulls
   *     Dagger and the screens, a two-minute deficit pulls Four Verticals;
   *   - and what the broadcast calls it, so the commentary says "Counter Trey" and
   *     "Y-Cross" instead of "Power —" for the eleventh time.
   * `fit` returns 0 to mean "never here". A situation nobody fits falls back to the family
   * the old chain would have produced, so the caller can never come up empty. */
  const PLAYBOOK_V101 = [
    /* ---- runs: the point of attack is the play ---------------------------------- */
    {
      id: "iz",
      name: "Inside Zone",
      fam: "run",
      base: "inside",
      gap: "B",
      tag: "Inside zone — ",
      fit: c => 1.4 + (c.toGo <= 6 ? 0.5 : 0)
    },
    { id: "splz", name: "Split Zone", fam: "run", base: "inside", gap: "A", tag: "Split zone — ", fit: c => 1.0 },
    {
      id: "duo",
      name: "Duo",
      fam: "run",
      base: "power",
      gap: "B",
      tag: "Duo — ",
      fit: c => 0.9 + (c.toGo <= 4 ? 0.6 : 0)
    },
    {
      id: "iso",
      name: "Iso",
      fam: "run",
      base: "power",
      gap: "A",
      tag: "Iso — ",
      fit: c => 0.5 + (c.toGo <= 2 ? 1.8 : 0) + (c.pos >= 95 ? 1.6 : 0)
    },
    {
      id: "powo",
      name: "Power O",
      fam: "run",
      base: "power",
      gap: "B",
      tag: "Power O — ",
      fit: c => 0.8 + (c.toGo <= 3 ? 1.1 : 0)
    },
    {
      id: "ctr",
      name: "Counter Trey",
      fam: "run",
      base: "power",
      gap: "C",
      tag: "Counter — ",
      fit: c => 0.9 + (c.down <= 2 ? 0.4 : 0)
    },
    { id: "trap", name: "Trap", fam: "run", base: "inside", gap: "A", tag: "Trap — ", fit: c => 0.7 },
    { id: "wham", name: "Wham", fam: "run", base: "inside", gap: "B", tag: "Wham — ", fit: c => 0.5 },
    {
      id: "dive",
      name: "QB Sneak",
      fam: "run",
      base: "power",
      gap: "A",
      tag: "Sneak — ",
      fit: c => (c.toGo <= 1 ? 3.2 : 0)
    },
    { id: "stre", name: "Outside Zone", fam: "run", base: "sweep", gap: "C", tag: "Outside zone — ", fit: c => 1.1 },
    {
      id: "toss",
      name: "Toss Crack",
      fam: "run",
      base: "sweep",
      gap: "D",
      tag: "Toss — ",
      fit: c => 0.8 + (c.toGo >= 7 ? 0.3 : 0)
    },
    {
      id: "jet",
      name: "Jet Sweep",
      fam: "run",
      base: "sweep",
      gap: "D",
      tag: "Jet sweep — ",
      fit: c => 0.7 + (c.down <= 2 ? 0.3 : 0)
    },
    { id: "pnp", name: "Pin & Pull", fam: "run", base: "sweep", gap: "C", tag: "Pin and pull — ", fit: c => 0.7 },
    {
      id: "rev",
      name: "Reverse",
      fam: "run",
      base: "sweep",
      gap: "D",
      tag: "Reverse — ",
      fit: c => 0.25 + (c.down <= 2 && c.toGo >= 6 ? 0.3 : 0)
    },
    { id: "zread", name: "Zone Read Keep", fam: "run", base: "sweep", gap: "C", tag: "Zone read — ", fit: c => 0.6 },
    {
      id: "qbpow",
      name: "QB Power",
      fam: "run",
      base: "power",
      gap: "B",
      tag: "QB power — ",
      fit: c => (c.pos >= 92 || c.toGo <= 2 ? 0.8 : 0.2)
    },
    {
      id: "draw",
      name: "Draw",
      fam: "run",
      base: "draw",
      gap: "A",
      tag: "Draw — ",
      fit: c => 0.3 + (c.down >= 3 && c.toGo >= 7 ? 2.4 : 0)
    },
    {
      id: "ldraw",
      name: "Lead Draw",
      fam: "run",
      base: "draw",
      gap: "B",
      tag: "Lead draw — ",
      fit: c => 0.25 + (c.down >= 3 && c.toGo >= 7 ? 1.4 : 0) + (c.hurry ? 0.4 : 0)
    },

    /* ---- passes: the routes are the play ---------------------------------------- */
    {
      id: "slf",
      name: "Slant-Flat",
      fam: "pass",
      base: "quick",
      routes: ["slant", "flat", "hitch"],
      tag: "Slant-flat — ",
      fit: c => 1.2 + (c.toGo <= 5 ? 0.8 : 0) + (c.hurry ? 0.5 : 0)
    },
    {
      id: "stik",
      name: "Stick",
      fam: "pass",
      base: "quick",
      routes: ["stick", "flat", "hitch"],
      tag: "Stick — ",
      fit: c => 1.0 + (c.toGo <= 6 ? 0.7 : 0)
    },
    {
      id: "snag",
      name: "Snag",
      fam: "pass",
      base: "quick",
      routes: ["snag", "flat", "corner"],
      tag: "Snag — ",
      fit: c => 0.9 + (c.pos >= 85 ? 0.6 : 0)
    },
    {
      id: "mesh",
      name: "Mesh",
      fam: "pass",
      base: "quick",
      routes: ["drag", "shallow", "corner"],
      tag: "Mesh — ",
      fit: c => 0.9 + (c.toGo <= 8 ? 0.4 : 0)
    },
    {
      id: "htch",
      name: "Hitch-Seam",
      fam: "pass",
      base: "quick",
      routes: ["hitch", "seam", "curl"],
      tag: "Hitch-seam — ",
      fit: c => 0.8 + (c.hurry ? 0.6 : 0)
    },
    {
      id: "drv",
      name: "Drive",
      fam: "pass",
      base: "dropback",
      routes: ["dig", "drag", "post"],
      tag: "Drive — ",
      fit: c => 1.1
    },
    {
      id: "dag",
      name: "Dagger",
      fam: "pass",
      base: "dropback",
      routes: ["dagger", "seam", "curl"],
      tag: "Dagger — ",
      fit: c => 0.8 + (c.toGo >= 7 ? 1.0 : 0)
    },
    {
      id: "lvl",
      name: "Levels",
      fam: "pass",
      base: "dropback",
      routes: ["dig", "shallow", "go"],
      tag: "Levels — ",
      fit: c => 1.0
    },
    {
      id: "ycrs",
      name: "Y-Cross",
      fam: "pass",
      base: "dropback",
      routes: ["over", "post", "flat"],
      tag: "Y-cross — ",
      fit: c => 1.0
    },
    {
      id: "flood",
      name: "Sail",
      fam: "pass",
      base: "dropback",
      routes: ["sail", "flat", "go"],
      tag: "Sail — ",
      fit: c => 0.9 + (c.toGo >= 6 ? 0.5 : 0)
    },
    {
      id: "curl",
      name: "Curl-Flat",
      fam: "pass",
      base: "dropback",
      routes: ["curl", "flat", "comeback"],
      tag: "Curl-flat — ",
      fit: c => 1.0 + (c.toGo >= 5 && c.toGo <= 10 ? 0.5 : 0)
    },
    {
      id: "smsh",
      name: "Smash",
      fam: "pass",
      base: "dropback",
      routes: ["corner", "hitch", "go"],
      tag: "Smash — ",
      fit: c => 0.9 + (c.pos >= 80 ? 0.7 : 0)
    },
    {
      id: "cmbk",
      name: "Comeback",
      fam: "pass",
      base: "dropback",
      routes: ["comeback", "out", "seam"],
      tag: "Comeback — ",
      fit: c => 0.7 + (c.toGo >= 8 ? 0.6 : 0)
    },
    {
      id: "4vrt",
      name: "Four Verticals",
      fam: "pass",
      base: "shot",
      routes: ["go", "seam", "go", "seam"],
      tag: "Four verticals — ",
      fit: c => 0.5 + (c.toGo >= 10 ? 0.8 : 0) + (c.margin <= -9 && c.quarter >= 4 ? 1.8 : 0)
    },
    {
      id: "pwhl",
      name: "Post-Wheel",
      fam: "pass",
      base: "shot",
      routes: ["post", "wheel", "flat"],
      tag: "Post-wheel — ",
      fit: c => 0.5 + (c.down <= 2 ? 0.4 : 0)
    },
    {
      id: "slug",
      name: "Sluggo",
      fam: "pass",
      base: "shot",
      routes: ["sluggo", "drag", "post"],
      tag: "Sluggo — ",
      fit: c => 0.4 + (c.down <= 2 && c.toGo <= 4 ? 0.9 : 0)
    },
    {
      id: "yank",
      name: "Yankee",
      fam: "pass",
      base: "shot",
      routes: ["deep_cross", "deep_post", "checkdown"],
      tag: "Yankee — ",
      fit: c => 0.4 + (c.margin <= -9 ? 0.7 : 0)
    },
    {
      id: "bub",
      name: "Bubble Screen",
      fam: "pass",
      base: "screen",
      routes: ["bubble", "go", "go"],
      tag: "Bubble — ",
      fit: c => 0.6 + (c.down >= 3 && c.toGo >= 8 ? 1.1 : 0)
    },
    {
      id: "tun",
      name: "Tunnel Screen",
      fam: "pass",
      base: "screen",
      routes: ["tunnel", "go", "go"],
      tag: "Tunnel screen — ",
      fit: c => 0.5 + (c.down >= 3 && c.toGo >= 8 ? 0.9 : 0)
    },
    {
      id: "rbsc",
      name: "Back Screen",
      fam: "pass",
      base: "screen",
      routes: ["screen", "go", "go"],
      tag: "Screen — ",
      fit: c => 0.5 + (c.down >= 3 && c.toGo >= 8 ? 1.0 : 0)
    },
    {
      id: "fade",
      name: "Back-Shoulder",
      fam: "pass",
      base: "fade",
      routes: ["fade", "slant", "flat"],
      tag: "Back-shoulder — ",
      fit: c => (c.pos >= 90 ? 2.4 : 0.15)
    },
    {
      id: "cfade",
      name: "Corner Fade",
      fam: "pass",
      base: "fade",
      routes: ["corner", "fade", "spot"],
      tag: "Corner fade — ",
      fit: c => (c.pos >= 90 ? 1.9 : 0.1)
    },
    {
      id: "pabt",
      name: "Play-Action Boot",
      fam: "pass",
      base: "dropback",
      routes: ["cross", "flat", "post"],
      tag: "Play-action boot — ",
      pa: 1,
      fit: c => (c.down <= 2 ? 1.0 : 0.2)
    },
    {
      id: "pasht",
      name: "Play-Action Shot",
      fam: "pass",
      base: "shot",
      routes: ["post", "go", "drag"],
      tag: "Play-action shot — ",
      pa: 1,
      fit: c => (c.down <= 2 && c.toGo >= 4 ? 0.8 : 0.15)
    }
  ];
  window.__PLAYBOOK_V101 = PLAYBOOK_V101;
  /* The call: weight every play that fits, pick one. `bias` is the family the old chain
   * would have chosen — it stays a thumb on the scale rather than a verdict, so the
   * situational logic that has always been here still shapes the mix. */
  function pickPlayV101(fam, bias, c) {
    if (!TU("playbookV101", 1)) return null;
    const pool = PLAYBOOK_V101.filter(p => p.fam === fam);
    let tot = 0;
    const ws = [];
    for (const p of pool) {
      let w = 0;
      try {
        w = Math.max(0, Number(p.fit(c)) || 0);
      } catch (e) {
        w = 0;
      }
      if (w > 0 && bias && p.base === bias) w *= TU("playbookBiasK", 2.2);
      ws.push(w);
      tot += w;
    }
    if (tot <= 0) return null;
    let r = Math.random() * tot;
    for (let i = 0; i < pool.length; i++) {
      r -= ws[i];
      if (r <= 0) return pool[i];
    }
    return pool[pool.length - 1];
  }
  window.__pickPlayV101 = pickPlayV101;
  function simGameV2(e, t) {
    /* ===== v16 EMERGENT GAME ENGINE ==========================================
     * The game is no longer scripted outcome-first. Every drive is resolved
     * play-by-play (FieldSim agents when available), on a real game clock,
     * with alternating possessions, kickoffs, and carried-over field position.
     * The final score EMERGES from the plays — nothing is pre-decided.
     * Payload shape per play is unchanged (plus a new `clock` field).
     * Tunables: RIB_TUNE.qtrMinutes (default 9). ============================= */
    window.__fullGameBalanceV39 = !0;
    const a = e / 100,
      i = (w, k) => Math.max(0, Math.round(w * a + randRange(-k, k))),
      r = {
        pass: 0,
        rush: 0,
        rec: 0,
        td: 0,
        tackle: 0,
        sack: 0,
        int: 0,
        pd: 0,
        comp: 0,
        att: 0,
        rec_c: 0,
        tgt: 0,
        carries: 0,
        longest: 0,
        fum: 0,
        ff: 0,
        tfl: 0,
        qbhit: 0
      };
    switch (t) {
      case "QB":
        ((r.pass = i(250, 50)),
          (r.td = i(3, 1)),
          (r.att = i(32, 6)),
          (r.comp = Math.min(r.att, i(22, 5))),
          (r.int = Math.max(0, Math.round((1 - a) * 2))),
          (r.longest = i(38, 12)),
          (r.rush = i(18, 12)));
        break;
      case "RB":
        ((r.rush = i(100, 30)),
          (r.td = i(1.5, 1)),
          (r.carries = i(20, 5)),
          (r.longest = i(28, 10)),
          (r.rec = i(22, 14)),
          (r.rec_c = i(3, 2)),
          (r.fum = Math.random() < (1 - a) * 0.4 ? 1 : 0));
        break;
      case "WR":
        ((r.rec = i(85, 30)),
          (r.td = i(1, 1)),
          (r.rec_c = i(6, 3)),
          (r.tgt = r.rec_c + i(3, 2)),
          (r.longest = i(34, 14)));
        break;
      case "TE":
        ((r.rec = i(60, 22)),
          (r.td = i(0.8, 1)),
          (r.rec_c = i(5, 2)),
          (r.tgt = r.rec_c + i(2, 2)),
          (r.longest = i(24, 10)));
        break;
      case "OL":
        ((r.tackle = 0), (r.pancake = i(6, 3)), (r.sackAllowed = Math.max(0, Math.round((1 - a) * 3))));
        break;
      case "DL":
        ((r.sack = i(1.2, 1)),
          (r.tackle = i(4, 2)),
          (r.tfl = i(1.6, 1)),
          (r.qbhit = i(3, 2)),
          (r.ff = Math.random() < a * 0.3 ? 1 : 0));
        break;
      case "LB":
        ((r.tackle = i(9, 3)),
          (r.sack = i(0.6, 1)),
          (r.tfl = i(1.8, 1)),
          (r.int = Math.random() < a * 0.3 ? 1 : 0),
          (r.ff = Math.random() < a * 0.25 ? 1 : 0));
        break;
      case "CB":
        ((r.tackle = i(4, 2)), (r.int = i(0.5, 1)), (r.pd = i(2, 1)), (r.tgt = i(7, 2)));
        break;
      case "S":
        ((r.tackle = i(7, 3)), (r.int = i(0.4, 1)), (r.pd = i(1.5, 1)), (r.tfl = i(0.6, 1)));
        break;
    }
    const l = { sacks: 0 },
      c = buildGameRosters(
        t,
        a,
        r,
        l,
        {},
        state.player ? state.player.level : 0,
        state.player ? playerOvr(state.player) : 40
      ),
      u = [];
    // v20 stamina: every game starts on a full tank — clear cross-play carry-over
    [...c.us.off, ...c.us.def, ...c.opp.off, ...c.opp.def].forEach(w => {
      if (w) {
        delete w._gasV20;
        delete w._gassedPlaysV20;
        delete w._wearV30;
      }
    });
    let h = 0,
      p = 0;
    // clear stale FieldSim choreography logs (e.g. from a skipped previous game)
    try {
      if (window.__FieldSim && window.__FieldSim._Q) window.__FieldSim._Q.length = 0;
    } catch (w) {}
    const P = {
        pass: 0,
        rush: 0,
        rec: 0,
        td: 0,
        tackle: 0,
        sack: 0,
        int: 0,
        pd: 0,
        pick6: 0,
        carries: 0,
        rec_c: 0,
        ff: 0,
        fum: 0
      } /* v109: ff/fum were ++'d from undefined → NaN */,
      v = {
        yds: 0,
        pass: 0,
        rush: 0,
        first: 0,
        pen: 0,
        penYds: 0,
        turn: 0,
        sacks: 0,
        third_c: 0,
        third_a: 0,
        air: 0,
        yac: 0
      },
      j = {
        yds: 0,
        pass: 0,
        rush: 0,
        first: 0,
        turn: 0,
        air: 0,
        yac: 0,
        third_c: 0,
        third_a: 0,
        pen: 0,
        penYds: 0,
        sacks: 0
      },
      U = ["QB", "RB", "WR", "TE"].includes(t),
      ce = ["DL", "LB", "CB", "S"].includes(t),
      // v111: when the career path runs a game it hands the buff list down through the global; a bare
      // __simGameV2 call gets none, so v111's OWN entries (a lingering wear cut, this game's focus) are
      // picked up here instead. `ownBuffs` never returns the roster's list, so nothing already working
      // changes shape — at every default both sides of this are empty.
      _tb111 =
        window.__youTempBuffsV25 != null
          ? window.__youTempBuffsV25
          : (function () {
              try {
                return window.__V111 ? window.__V111.ownBuffs(state.player) : null;
              } catch (_) {
                return null;
              }
            })(),
      _raw = (w, k) => {
        let _v = w && w.attrs && w.attrs[k];
        if (!(_v > 0)) _v = TU("v141Fallback", 1) ? (w && Number(w.ovr)) || 45 : 45;
        /* v141: own OVR, never a level-dependent constant (v141Fallback 0 = the old flat 45) */ if (w && w.you) {
          if (window.__condMultV54) _v *= window.__condMultV54(state.player);
          var _tb = _tb111,
            _mu = 1;
          if (_tb)
            for (var _bi = 0; _bi < _tb.length; _bi++) {
              if (_tb[_bi].stat === k) {
                if (_tb[_bi].mul != null) _mu *= Number(_tb[_bi].mul) || 1;
                else _v += _tb[_bi].max ? 10 : _tb[_bi].amt;
              }
            }
          if (_mu !== 1) _v *= clamp99(_mu, TU("focusMulFloor", 0.6), TU("focusMulCeil", 1.35));
          /* v111: the focus is a multiplier, and it lands AFTER the body's swing */ _v +=
            (treeFx("perfFlat") || 0) +
            ((window.__youPersonaFxV20 && window.__youPersonaFxV20.perfFlat) || 0) +
            gearAttrV147(k); /* v147 C: the gear on him */
        }
        return _v;
      },
      // league-normalized attribute accessor: every league plays real football —
      // outcomes ride on RELATIVE strength, not absolute ratings (a high-school
      // game shouldn't end 6-3 because everyone is 22 OVR)
      _lgAvg = (() => {
        const g = [...c.us.off, ...c.us.def, ...c.opp.off, ...c.opp.def];
        return g.reduce((N, H) => N + (Number(H.ovr) || 45), 0) / Math.max(1, g.length);
      })(),
      // v40 single-star AI calibration: a 20-OVR outlier should move a game, not
      // become the whole game. Compress only the attribute advantage above his own
      // unit's peers; equal teams and ordinary rating gaps remain completely intact.
      /* v141: above the knee the scale eases toward 99 instead of stopping at 95, so the band the 5x wall charges for is visible on the agent */
      /* v141: the YOU-PLAYER at a ball-carrier position gets a LOWER ceiling (simKneeTopPosV141) — past a ~20-point edge over the tacklers every whiff, hurdle and broken-tackle roll saturates and they stack, and a back at 90 ran for 440 a game. The knee starts 12 under whichever ceiling applies. */
      kneeV141 = (x, pos) => {
        const KT = TU("simKneeTopPosV141", { RB: 72, WR: 76, TE: 76 }),
          A = Math.min(TU("simKneeTopV141", 99), (pos && KT[pos]) || 99),
          T = TU("simKneeTailV141", 22),
          K = Math.min(TU("simKneeV141", 80), A - 12);
        if (x > K) x = K + ((A - K) * (x - K)) / (x - K + T);
        return clamp99(Math.round(x), 20, 99);
      },
      __kneeExportV141 = (window.__kneeV141 = kneeV141),
      _starUnits = [c.us.off, c.us.def, c.opp.off, c.opp.def],
      _starUnit = new Map(),
      _starInit = _starUnits.forEach(unit => unit.forEach(pl => _starUnit.set(pl, unit))),
      _starScaleDefault = { QB: 0.55, RB: 0.18, WR: 0.36, TE: 0.32, OL: 0.55, DL: 1, LB: 0.27, CB: 0.28, S: 0.4 },
      _starScale = (w, k) => {
        const unit = _starUnit.get(w);
        if (!unit || unit.length < 2) return 1;
        const peers = unit.filter(pl => pl !== w),
          peerOvr = peers.reduce((sum, pl) => sum + (Number(pl.ovr) || 45), 0) / peers.length,
          gap = (Number(w && w.ovr) || peerOvr) - peerOvr,
          blend = clamp99((gap - 8) / 2, 0, 1),
          override = window.__starImpactScalesV40 && window.__starImpactScalesV40[w.pos],
          floor = override == null ? Math.max(_starScaleDefault[w.pos] ?? 0.5, TU("starFloorMinV141", 0.6)) : override;
        return 1 - (1 - floor) * blend;
      },
      _starPeerRaw = (w, k) => {
        const unit = _starUnit.get(w),
          peers = unit && unit.filter(pl => pl !== w);
        return peers && peers.length ? peers.reduce((sum, pl) => sum + _raw(pl, k), 0) / peers.length : _lgAvg;
      },
      _starOutlier = (unit, pos) => {
        if (!unit || unit.length < 2) return null;
        const candidates = unit.filter(pl => !pos || pl.pos === pos);
        if (!candidates.length) return null;
        const player = candidates.reduce(
            (best, pl) => ((Number(pl.ovr) || 0) > (Number(best.ovr) || 0) ? pl : best),
            candidates[0]
          ),
          peers = unit.filter(pl => pl !== player),
          peerOvr = peers.reduce((sum, pl) => sum + (Number(pl.ovr) || 45), 0) / Math.max(1, peers.length),
          blend = clamp99(((Number(player.ovr) || peerOvr) - peerOvr - 8) / 10, 0, 1);
        return blend > 0 ? { player, blend } : null;
      },
      _starFrontRating = (unit, front) => {
        if (!front.length) return 45;
        const base = front.reduce((sum, pl) => sum + _(pl, "tackling") + _(pl, "strength"), 0) / (front.length * 2);
        return [
          ["S", 0.12],
          ["LB", 0.1]
        ].reduce((rating, [pos, weight]) => {
          const star = _starOutlier(unit, pos);
          return star
            ? rating + ((_(star.player, "tackling") + _(star.player, "strength")) / 2 - base) * weight * star.blend
            : rating;
        }, base);
      },
      _ = (w, k) => {
        const raw = _raw(w, k),
          scale = _starScale(w, k),
          peer = scale < 0.999 ? _starPeerRaw(w, k) : raw,
          adjusted = peer + (raw - peer) * scale;
        return kneeV141(51 + (adjusted - _lgAvg) * 1.15, w && w.you ? w.pos : null);
      } /* v141: the carrier ceiling is for the you-player — an AI back at Pee Wee sits at 85 by the normalise, and the game was tuned on that */,
      Y = (w, k) => {
        const g = w.filter(N => k.includes(N.pos));
        return g.length ? randPick(g) : w[0];
      },
      ie = w => (w ? { O: c.us, D: c.opp } : { O: c.opp, D: c.us }),
      pe = w => w && w.you,
      nm = w => {
        const g = String((w && w.name) || "");
        return g.split(" ")[1] || g || "—";
      };
    // v31 realism profile: one set of game conditions and team identities drives
    // every situational decision instead of each snap behaving like a coin flip.
    const avgAttr = (arr, key) => arr.reduce((s, pl) => s + _(pl, key), 0) / Math.max(1, arr.length),
      teamProfile = tm => {
        const qb = Y(tm.off, ["QB"]),
          rb = Y(tm.off, ["RB"]),
          ol = tm.off.filter(pl => pl.pos === "OL"),
          rec = tm.off.filter(pl => ["WR", "TE"].includes(pl.pos)),
          sec = tm.def.filter(pl => ["CB", "S"].includes(pl.pos)),
          front = tm.def.filter(pl => ["DL", "LB"].includes(pl.pos));
        return {
          qbIq: _(qb, "awareness"),
          qbSpeed: _(qb, "speed"),
          rbSkill: (_(rb, "speed") + _(rb, "burst") + _(rb, "strength")) / 3,
          ballSecurity: (_(rb, "awareness") + _(rb, "strength")) / 2,
          line: avgAttr(ol, "blocking"),
          receivers: (avgAttr(rec, "catching") + avgAttr(rec, "speed")) / 2,
          front: (avgAttr(front, "strength") + avgAttr(front, "tackling")) / 2,
          coverage: (avgAttr(sec, "coverage") + avgAttr(sec, "speed")) / 2,
          discipline: avgAttr([...tm.off, ...tm.def], "awareness")
        };
      },
      profiles = { us: teamProfile(c.us), them: teamProfile(c.opp) },
      weatherRoll = Math.random(),
      weather = weatherRoll < 0.1 ? "rain" : weatherRoll < 0.15 ? "wind" : weatherRoll < 0.18 ? "snow" : "clear",
      __wxSide = typeof window < "u" ? (window.__WX_V79 = weather) : weather,
      /* v144 H: a game is played at a time of day as well as in a weather. Rain and snow are night
       * games as often as any other; a CLEAR game is the one that can be played in the sun. The roll is
       * published beside the weather so the renderer can read both, and the sim itself does not care —
       * nothing below reads `dayV144`, so no outcome moves. */
      dayV144 = weather === "clear" && Math.random() < TU("dayChanceV144", 0.42),
      __daySide = typeof window < "u" ? (window.__WX_DAY_V144 = dayV144) : dayV144,
      weatherFx = {
        pass: weather === "rain" ? 0.94 : weather === "wind" ? 0.91 : weather === "snow" ? 0.9 : 1,
        kick: weather === "wind" ? 0.88 : weather === "snow" ? 0.9 : weather === "rain" ? 0.95 : 1,
        fumble: weather === "rain" ? 1.35 : weather === "snow" ? 1.22 : 1
      };
    const usage = new Map(),
      touches = pl => usage.get(pl) || 0,
      addTouch = pl => usage.set(pl, touches(pl) + 1);
    /* ===== v111 THE DIAL REACHES THE SNAP =====
     * `share` is the share of HIS unit's snaps he is on the field for. A rest snap is not a
     * suppressed stat line — the roster array carries a nameless-but-real BACKUP in his slot for
     * that play, so FieldSim fields the backup, `B`/`b` cannot find `z.you` to hand him the ball,
     * the commentary names the backup and `pe()` credits the backup. `touchMul` scales the lead
     * back's share and the target rate on the snaps he DOES play.
     *
     * At "normal" the share is 1 and the multiplier is 1: no roll is made, no substitution happens
     * and every expression below is the one that was here before. */
    const _u111 = (() => {
        try {
          return window.__V111 && state && state.player ? window.__V111.usage(state.player) : null;
        } catch (_) {
          return null;
        }
      })(),
      _share111 = _u111 ? _u111.share : 1,
      _touch111 = _u111 ? _u111.touchMul : 1,
      _slot111 = (() => {
        if (_share111 >= 0.999) return null;
        let i = c.us.off.findIndex(w => w && w.you);
        if (i >= 0) return { arr: c.us.off, i, off: !0 };
        i = c.us.def.findIndex(w => w && w.you);
        return i >= 0 ? { arr: c.us.def, i, off: !1 } : null;
      })(),
      _you111 = _slot111 ? _slot111.arr[_slot111.i] : null,
      // the man who takes his snaps: a real depth piece, a shade off him, so the TEAM's afternoon is
      // barely moved by resting its star — which is the point. What changes is whose name is on it.
      _sub111 = _you111
        ? (() => {
            const q = clamp99(TU("subQualityV111", 0.92), 0.5, 1),
              at = {};
            for (const k in _you111.attrs || {}) at[k] = clamp99(Math.round((_you111.attrs[k] || 45) * q), 5, 99);
            return {
              name: randName(),
              num: _you111.num,
              pos: _you111.pos,
              isOff: _you111.isOff,
              ovr: Math.round((_you111.ovr || 45) * q),
              attrs: at,
              stat: {}
            };
          })()
        : null;
    let _on111 = !0,
      _snapOn111 = 0,
      _snapTm111 = 0;
    _sub111 && _starUnit.set(_sub111, _slot111.arr);
    // ---- play resolvers (FieldSim agents when available, formula fallback) ----
    /* ===== v76 MARGIN CURVE (2/5) — a talent edge buys a better offence, not a
     * different sport. Diagnosed rather than guessed: at a -5.8 scoreboard gap a team
     * averaged 2.42 yards a play, and at +14.5 it averaged 5.39. Play count (156 vs
     * 158) and punts (12.4 vs 12.2) were IDENTICAL, so none of the blowout came from
     * extra possessions or turnovers — every point of it came from yards per play,
     * which was responding at .146 yds/play per point of gap and then compounding
     * through a dozen drives into 45-5.
     *
     * `dampV76` pulls that response down to TU("gapYardKeep") of itself, solved on the
     * measured line rather than applied as a blanket scale: the multiplier is the ratio
     * of the yards-per-play we WANT at this gap to the yards-per-play the resolvers
     * just produced. It sits on the resolved gain, so it damps the FieldSim path and
     * the formula fallback alike — which matters, because the sim resolves most snaps
     * and its physics is guarded by its own checks and should not be reached into.
     *
     * `gtV76` is the garbage-time half: `milk` only ever fired in Q4 inside five
     * minutes, so a team up forty in the second quarter kept throwing. Once the lead is
     * clear the starters come out and the playbook shrinks, which is both what really
     * happens and what stops the tail. ===== */
    let _gapV76 = 0,
      _leadV76 = 0;
    function dampV76(y) {
      if (!y || !isFinite(y)) return y;
      const g = _gapV76,
        B0 = TU("gapYardBase", 3.3),
        K = TU("gapYardSlope", 0.146),
        R = clamp99(TU("gapYardKeep", 0.13), 0, 1);
      // want / have, both measured off the same parity baseline
      const have = B0 + K * g,
        want = B0 + K * R * g;
      let m = have > 0.4 ? want / have : 1;
      m *= gtV76();
      // The diagnostic that started all this found the favoured team converting 43% of
      // its third downs against the underdog's 23% — a wider discrepancy than yards per
      // play, and the one that actually turns field position into points. A drive that
      // dies on third down at midfield costs the same seven points as one stopped on the
      // goal line, and costs them without denying anybody a goal-line moment.
      // Scaled by the SIZE of the edge, not switched on by its sign. A flat multiplier
      // here meant a team one point better on the scoreboard had its third downs cut by
      // the full amount — a cliff at zero, and the reason the you-player was losing 41%
      // of his yards in games that were barely mismatches at all.
      try {
        if (typeof down !== "undefined" && down >= 3 && _gapV76 > 0) {
          const t = clamp99(_gapV76 / Math.max(1, TU("thirdDownFull", 6)), 0, 1);
          m *= 1 - (1 - clamp99(TU("thirdDownK", 0.28), 0.2, 1)) * t;
        }
      } catch (e) {}
      // Only ever CUT. Boosting the underdog to close the gap was measured doing the
      // opposite of the job: the favourite lost 28% of its yards and the underdog gained
      // 50%, the two multipliers cancelled (mean 0.984 across a game) and total scoring
      // went UP while the margin stayed. Damping one side is symmetric anyway — when you
      // are the underdog it is THEIR offence that carries the positive gap.
      m = clamp99(m, TU("gapYardMin", 0.22), 1);
      // debug surface for scripts/blowoutcheck.mjs — how often the damper actually
      // fires, and which way, so a tuning pass measures the lever instead of guessing
      try {
        const Z = window.__DAMP_V76;
        if (Z) {
          Z.n++;
          Z.g += g;
          Z.m += m;
          Z.y += y;
          if (m < 0.999) Z.dn++;
          else if (m > 1.001) Z.up++;
        }
      } catch (e) {}
      return y > 0 ? y * m : y; // a loss is a loss; never turn one into a gain
    }
    /* The offence that is already clear stops pressing. What counts as "clear" is not a
     * fixed lead — it is the lead RELATIVE TO WHAT THIS MATCHUP SHOULD PRODUCE, which is
     * marginPerOvr x the scoreboard gap. A three-score lead in a game you were supposed
     * to win by three scores is just the game going to form and nothing damps; three
     * scores clear in an even game is a rout, and the starters come out.
     *
     * Keying it this way is what actually lands the curve. Damping the raw yardage
     * mismatch alone plateaus at a slope near 2.1 however hard it is driven, because
     * most of the compounding is not in yards per play at all — measured, a favoured
     * team also converted 43% of third downs against 23%, and turned the ball over 0.12
     * times a game against 1.15. This term reaches all of that at once, because a team
     * that cannot move the ball cannot convert or hold a lead either.
     *
     * It is deliberately a LATE brake, not an early one: nothing is damped until the
     * game is already a score clear of its own expectation, so a fast start still runs
     * away and the rare statement win survives. */
    /* ===== v76 MARGIN CURVE (3/5) — the takeaway swing =====
     * The yardage damper alone plateaued: across a 3.7x range of its own dial the
     * margin barely moved past a slope of ~1.9. The diagnostic said why. Between a -5.8
     * and a +14.5 gap the play count (156 vs 158) and the punts (12.4 vs 12.2) were
     * IDENTICAL — what differed was yards per play (2.42 -> 5.39) and, once the yards
     * were damped, the two things yards do not touch: third-down conversion (43% vs 23%)
     * and TURNOVERS (0.12 vs 1.15 a game). A one-per-game takeaway swing is worth about
     * five points on its own, and it compounds, because every takeaway is a drive the
     * favourite gains and the underdog loses.
     *
     * So this damps the takeaway swing at its source: the more outmatched the offense on
     * the field, the less of the gap's turnover penalty it actually pays. It is CUT-ONLY
     * and one-sided by design (an earlier two-sided version cancelled itself out — the
     * underdog's boost undid the favourite's damp and total scoring rose while the margin
     * stayed put), and it never touches the baseline rate two even teams play at. ===== */
    function toV76() {
      const g = -_gapV76; // how far the team WITH THE BALL is outmatched
      if (g <= 0) return 1;
      // Continuous, not a ramp down to a floor. A saturating damper stops working exactly
      // where it is needed most: with a ramp that reached full strength by +8, every gap
      // past that was undamped and the margin went on climbing unopposed — a +28 edge
      // still averaged a 43-point win. This one keeps decaying for as long as the
      // mismatch grows, and never reaches zero.
      return 1 / (1 + g / Math.max(1, TU("toFull", 8)));
    }
    function gtV76() {
      // Garbage time is a lead AND a clock, never a lead alone. Keyed on the lead only,
      // this fired in the first quarter of even games — a nine-point lead in Q1 is not a
      // decided game, and damping it dragged league-wide yards per carry under 3.2 in
      // mirrored-roster games that v76 has no business touching at all (`equaltalentcheck`).
      // It ramps in through the second half and only reaches full strength in the fourth.
      let late = 0;
      try {
        if (typeof quarter !== "undefined") {
          late = quarter >= 4 ? 1 : quarter === 3 ? 0.55 : 0;
          if (quarter >= 4 && typeof clock !== "undefined" && clock <= 420) late = 1;
        }
      } catch (e) {}
      if (late <= 0) return 1;
      const want = TU("marginPerOvr", 0.7) * Math.max(0, _gapV76),
        over = _leadV76 - want,
        a2 = TU("gtStart", 8),
        b2 = Math.max(a2 + 1, TU("gtFull", 20)),
        d = clamp99(TU("gtDamp", 0.9), 0, 0.95) * late;
      return over <= a2 ? 1 : clamp99(1 - ((over - a2) / (b2 - a2)) * d, 1 - d, 1);
    }
    /* ===== v76 MARGIN CURVE (4/5) — the goal-line stand =====
     * Shaving yards could not land the curve on its own: driven from a 90% keep down to
     * 10%, the measured slope only moved from ~3.0 to ~1.9, because most of a blowout is
     * not yards per play. The same favoured team also converted 43% of its third downs
     * against 23%, and turned the ball over 0.12 times a game against 1.15 — a yardage
     * multiplier reaches none of that.
     *
     * A drive that dies on the one does. This is the discrete lever: once the game is
     * running clear of what the matchup should produce, a would-be touchdown is instead
     * stopped short and the offence is left with a field goal or a fourth down. That is
     * worth four to seven points in a single stroke, it is ordinary football, and it
     * keeps the play-by-play honest — the drive really did stall, the yards really were
     * gained, and because `_e` never gets set the you-player is never credited a
     * touchdown he did not score.
     *
     * It is probabilistic and starts only once the leader is a converted score past
     * expectation, so a genuine statement win still gets to happen. ===== */
    function standV76() {
      // Capped low on purpose. An earlier pass landed the margin curve with this at .92,
      // which meant a favoured team was stopped on the goal line NINE TIMES IN TEN — the
      // curve was right and the football was absurd, and it wiped the you-player's
      // touchdowns out completely (0.09 a game to 0.00 across 104 sampled games). A
      // goal-line stand is a thing that happens sometimes. The rest of the correction is
      // taken on third down instead, which costs the same points without staging a
      // last-minute miracle on every possession.
      // The cap rises with the mismatch, for the same reason toV76 is continuous: a flat
      // cap is a damper that quietly switches off past the gap it was tuned at.
      const mx = clamp99(TU("standMax", 0.34) + Math.max(0, _gapV76) * TU("standMaxK", 0.012), 0, 0.72);
      // Two terms, because they do different jobs. The GAP term runs from the opening
      // kickoff and is what sets the average — a defence that is outclassed between the
      // twenties still stiffens inside its own ten, which is the oldest cliche in the
      // sport and happens to be the only thing that moves a mean. The RUNAWAY term is
      // the tail brake: it comes in once the game is already past what the matchup
      // should produce, and it is what keeps a fast start from turning into 70-0.
      const gapP = TU("standGapK", 0.07) * Math.max(0, _gapV76),
        want = TU("marginPerOvr", 0.7) * Math.max(0, _gapV76),
        over = _leadV76 - want,
        a2 = TU("standStart", 4),
        b2 = Math.max(a2 + 1, TU("standFull", 24)),
        lateP = over > a2 ? ((over - a2) / (b2 - a2)) * mx : 0;
      return Math.random() < clamp99(gapP + lateP, 0, mx);
    }
    function B(w, concept, play) {
      const { O: k, D: T } = ie(w),
        K = (() => {
          const yu = k.off.find(z => z.you),
            rbs = k.off.filter(z => z.pos === "RB"),
            qb = Y(k.off, ["QB"]);
          if (rbs.length) {
            if (qb && Math.random() < 0.055) return qb;
            const sr = rbs.slice().sort((A, B2) => (B2.ovr || 0) - (A.ovr || 0)),
              lead = yu && yu.pos === "RB" ? yu : sr[0],
              backup = sr.find(pl => pl !== lead) || lead;
            let leadShare = clamp99(0.72 - touches(lead) * 0.012, 0.46, 0.72);
            if (_touch111 !== 1 && lead && lead.you)
              leadShare = clamp99(
                leadShare * _touch111,
                0.3,
                TU("leadShareMaxV111", 0.97)
              ); /* v111: the dial buys carries — only ever HIS carries */
            return Math.random() < leadShare ? lead : backup;
          }
          return qb || Y(k.off, ["RB", "RB", "QB"]);
        })(),
        g = T.def.filter(ge => ["DL", "LB", "S"].includes(ge.pos)),
        N = _starFrontRating(T.def, g),
        H = k.off.filter(ge => ge.pos === "OL"),
        oe = H.length ? H.reduce((ge, $e) => ge + _($e, "blocking"), 0) / H.length : 45;
      addTouch(K);
      const __r2 =
        window.__FieldSim &&
        window.__FieldSim.run(
          w,
          k,
          T,
          K,
          _,
          concept,
          Object.assign(play && play.gap ? { gap: play.gap, play: play.id } : {}, {
            fieldPos: typeof pos !== "undefined" ? pos : 50,
            down: typeof down !== "undefined" ? down : 1,
            toGo: typeof toGo !== "undefined" ? toGo : 10
          })
        );
      /* v101: the call names the gap. v103: and the sticks, so a back can strain for them */ let base;
      if (__r2) base = __r2;
      else {
        const be = (_(K, "strength") * 0.5 + _(K, "burst") * 0.5) * 0.5 + oe * 0.5 - N * 0.85;
        let ke = Math.round(2.8 + be * 0.045 + randRange(-2.5, 3));
        (be < -7 && Math.random() < 0.38 && (ke = randInt(-2, 1)),
          Math.random() < clamp99((_(K, "speed") - N) / 360 + 0.015, 0.008, 0.11) &&
            (ke += randInt(7, 13 + Math.round(_(K, "speed") / 8))),
          (ke = clamp99(ke, -5, 60)),
          (base = { carrier: K, yards: ke, breakaway: ke >= 15 }));
      }
      // v16.1 run-curve calibration: the agent sim compresses gains into 0-5 yds
      // (its 7-defender pursuit converges before a back can break clean). Real backs
      // gash the second level and break the occasional long one, so promote a
      // fraction of stuffed runs into the medium/long tail — rate & size driven by
      // the carrier's burst/speed vs the front seven. buildPlayScript renders the
      // longer run (the sim log won't match these, so it's dropped).
      if (base && base.yards >= 0 && base.yards <= 2) {
        // concept shapes the tail: draws are boom-or-bust (more gashes), power runs
        // grind (fewer big gains but they stay positive), sweeps hit the edge.
        const cMul =
          concept === "draw" ? TU("drawGashMul", 1.15) : concept === "power" ? 0.55 : concept === "sweep" ? 1.05 : 1; // v81: the sim sells the draw itself now
        const edge = (_(K, "burst") + _(K, "speed")) / 2 - N,
          gashP = clamp99((0.05 + edge * 0.0025) * cMul, 0.02, 0.14),
          sustainP = clamp99(0.58 + (oe - N) * 0.006, 0.38, 0.75);
        if (Math.random() < gashP) {
          const r3 = Math.random(),
            big = concept === "draw",
            gain =
              r3 < (big ? 0.72 : 0.8)
                ? randInt(6, 11)
                : r3 < 0.97
                  ? randInt(12, 18)
                  : randInt(19, 30 + Math.max(0, Math.round(edge * 0.4)));
          dropSimLog();
          base = { carrier: K, yards: gain, breakaway: gain >= 15 };
        } else if (Math.random() < sustainP) {
          dropSimLog();
          base = { carrier: K, yards: randInt(4, 8), breakaway: !1 };
        }
      }
      if (base && base.yards > 0) {
        const y2 = Math.round(dampV76(base.yards));
        if (y2 !== base.yards) {
          retagSimLog(y2, base.yards);
          base = { ...base, yards: y2, breakaway: y2 >= 15 };
        }
      } /* v103: the picture follows the number */
      return base;
    }
    function b(w, concept, ctx = {}) {
      const { O: k, D: T } = ie(w),
        K = Y(k.off, ["QB"]),
        g = (() => {
          const g0 =
            concept === "screen"
              ? Y(k.off, ["RB", "WR", "TE"])
              : concept === "shot"
                ? Y(k.off, ["WR", "WR", "TE"])
                : Y(k.off, ["WR", "WR", "TE", "RB"]);
          const yu = k.off.find(z => z.you);
          if (yu && yu !== g0) {
            const tp = clamp99(
                (yu.pos === "WR" ? 0.32 : yu.pos === "TE" ? 0.2 : yu.pos === "RB" ? 0.1 : 0) * _touch111,
                0,
                0.62
              ) /* v111: the dial buys targets */,
              ok = concept === "shot" ? ["WR", "TE"].includes(yu.pos) : ["WR", "TE", "RB"].includes(yu.pos);
            if (tp && ok && Math.random() < tp) return yu;
          }
          return g0;
        })(),
        N = (() => {
          /* v87: no coin flip puts YOU on the target — the sim covers by alignment, and the formula fallback never picks you */ const safety =
            _starOutlier(T.def, "S");
          if (safety && safety.player && !safety.player.you && Math.random() < 0.2 * safety.blend) return safety.player;
          const others = T.def.filter(z => !z.you);
          return Y(others.length ? others : T.def, ["CB", "S", "LB"]);
        })();
      const need = typeof down !== "undefined" && down >= 3 ? Math.max(1, toGo) : 0;
      const __r =
        window.__FieldSim &&
        window.__FieldSim.pass(w, k, T, K, g, N, _, concept, {
          pressured: !!ctx.pressured,
          moving: !!ctx.moving,
          pa: !!ctx.pa,
          fieldPos: typeof pos !== "undefined" ? pos : 50,
          down: typeof down !== "undefined" ? down : 1,
          toGo: typeof toGo !== "undefined" ? toGo : 10,
          routes: (ctx.play && ctx.play.routes) || null,
          play: (ctx.play && ctx.play.id) || null
        }); /* v101: the call names the routes */
      if (__r) {
        if (__r.complete && __r.yards > 0) {
          const y2 = Math.max(1, Math.round(dampV76(__r.yards)));
          if (y2 !== __r.yards) {
            retagSimLog(y2, __r.yards);
            const air2 = Math.min(__r.air || 0, y2);
            return { ...__r, yards: y2, breakaway: y2 >= 20, air: air2, yac: Math.max(0, y2 - air2) };
          }
        } /* v103: the picture follows the number */
        return __r;
      }
      const depth0 =
          concept === "shot"
            ? 24
            : concept === "fade"
              ? 18
              : concept === "dropback"
                ? 11
                : concept === "quick"
                  ? 5
                  : concept === "screen"
                    ? 1
                    : 8,
        depth = need && concept !== "screen" ? Math.max(depth0, Math.min(need + 1, 18)) : depth0,
        H = _(g, "speed") * 0.5 + _(g, "agility") * 0.5 - _(N, "coverage") * 0.85 + _(K, "awareness") * 0.25,
        poise = clamp99((_(K, "awareness") - 35) / 100, 0, 0.55),
        pressureAcc = ctx.pressured ? (ctx.moving ? 0.78 : 0.86) + poise * (ctx.moving ? 0.16 : 0.1) : 1,
        oe = clamp99((0.57 - depth * 0.008 + H / 150) * weatherFx.pass * pressureAcc, 0.07, 0.91);
      if (!(Math.random() < oe)) {
        const ge = clamp99(
          0.012 +
            depth * 0.0022 +
            (_(N, "coverage") - _(K, "awareness")) * 0.0015 +
            (ctx.pressured ? 0.012 : 0) +
            (ctx.moving ? 0.012 : 0),
          0,
          0.18
        );
        return {
          qb: K,
          rec: g,
          cover: N,
          complete: !1,
          intercepted: Math.random() < ge,
          yards: 0,
          pressured: !!ctx.pressured,
          moving: !!ctx.moving
        };
      }
      let ke = Math.round(depth + ((_(g, "catching") + _(K, "awareness")) / 2) * 0.045 + randRange(-2, 4)),
        explosiveP = clamp99(
          (_(g, "speed") - _(N, "coverage")) / 240 +
            (concept === "shot" ? 0.13 : concept === "screen" ? 0.07 : 0.035) -
            (ctx.pressured ? 0.035 : 0),
          0.01,
          0.3
        );
      (Math.random() < explosiveP && (ke += randInt(concept === "shot" ? 12 : 8, 18 + Math.round(_(g, "speed") / 7))),
        (ke = clamp99(ke, 1, 80)));
      need &&
        ke < need &&
        Math.random() < clamp99((_(K, "awareness") - 40) / 120, 0.05, 0.45) &&
        (ke = need + randInt(0, 3));
      ke = Math.max(1, Math.round(dampV76(ke)));
      const air = clamp99(
        Math.round(
          concept === "screen" ? randRange(-1, 2) : ke * (concept === "shot" ? 0.75 : 0.45 + Math.random() * 0.3)
        ),
        0,
        ke
      );
      return {
        qb: K,
        rec: g,
        cover: N,
        complete: !0,
        intercepted: !1,
        yards: ke,
        breakaway: ke >= 20,
        air,
        yac: Math.max(0, ke - air),
        pressured: !!ctx.pressured,
        moving: !!ctx.moving
      };
    }
    // keep the FieldSim render queue aligned when we alter a play's bookkept yards:
    // the log we're discarding is the one this play just pushed — the TAIL, not the
    // head. Popping the tail (was shift(), a bug) keeps every other play's log lined
    // up with its render, so the agent-sim animation + its tackle/sprint/line events
    // actually reach the broadcast view instead of falling back to the old script.
    function dropSimLog() {
      try {
        const g = window.__FieldSim && window.__FieldSim._Q;
        g && g.length && g.pop();
      } catch (w) {}
    }
    /* ===== v103 KEEP THE PICTURE — retag, don't orphan =====
     * `takeLog` matches a queued play log to the play being rendered by its EXACT yardage. So
     * every time the engine reshaped a sim's yards afterwards — dampV76's margin brake is the
     * common one — the log it had just pushed could never match anything again: the play fell
     * back to the legacy choreographer and the whole agent sim (the block wins, the drag, the
     * pile) never reached the screen, while the orphan sat in the queue getting in the way of
     * later matches. The log is still the RIGHT log for this play; only the number moved. So
     * retag it instead of abandoning it. The log for the play just resolved is the TAIL, which
     * is the same entry dropSimLog pops. */
    /* v139: two things were wrong with retagging blind.
     *
     * It writes to the queue's TAIL on the assumption that the tail is the play just resolved.
     * That holds only when this play actually pushed a log — and plays resolved by the formula
     * fallback push none, so the tail is then some EARLIER play, and retagging it rewrites a
     * good play's yardage to a number from a different snap. That play can never match again,
     * and (since v139 cuts the picture to the number) it would also have had its log truncated
     * to a foreign yardage. So the caller now says what it is reshaping FROM, and the retag
     * only happens if the tail really is holding that number.
     *
     * And retagging alone made the log MATCH a number it no longer depicted — the whole point
     * of the match is that this log IS this play, so the picture is cut to the reshaped number
     * too, exactly as the clamp's is. */
    function retagSimLog(y, y0) {
      try {
        const F = window.__FieldSim,
          g = F && F._Q,
          h = g && g.length && g[g.length - 1];
        if (!h || !h.sig) return;
        if (y0 !== undefined && h.sig.yards !== y0) return; // not our log — leave it alone
        if (!isFinite(y)) return; // never write a number nothing can match
        h.sig.yards = y;
        if ((h.sig.kind === "run" || h.sig.kind === "pass") && F.fitLogV139) h.log = F.fitLogV139(h.log, y);
      } catch (w) {}
    }
    function xe() {
      return `(${h}–${p})`;
    }
    function st() {
      const w = c.us.off.filter(k => ["RB", "WR", "TE"].includes(k.pos) && !k.you);
      return w.length ? `${randPick(w).name}` : "your team";
    }
    function Tt() {
      const w = c.opp.off.filter(k => ["RB", "WR", "TE", "QB"].includes(k.pos));
      return w.length ? `${randPick(w).name}` : "the opponent";
    }
    function Ye(w, k, g, N) {
      (w &&
        g > 0 &&
        (t === "QB" && k
          ? (P.pass += g)
          : ["WR", "TE", "RB"].includes(t) && k
            ? (P.rec += g)
            : ["RB", "QB"].includes(t) && (P.rush += g)),
        N && w && P.td++);
    }
    // your-player flavor, bucketed by the ACTUAL result so the line matches the yards:
    // stuffed / short / solid chunk / breakaway. (yd defaults keep old callers working.)
    function J2(w, yd, brk) {
      yd = yd == null ? 6 : yd;
      const run =
        brk || yd >= 20
          ? [
              "You break it outside and take it to the house region — huge run!",
              "You hit the second level and PULL AWAY!",
              "One cut and you're GONE down the sideline!"
            ]
          : yd >= 8
            ? [
                "You hit the hole and burst through for a big gain!",
                "Great vision — you find the crease and go.",
                "You lower the shoulder and drag the pile forward!"
              ]
            : yd >= 3
              ? [
                  "You follow your blocks for a solid pickup.",
                  "You bounce it outside and turn upfield.",
                  "You grind out the tough yards up the middle."
                ]
              : [
                  "You get stonewalled at the line.",
                  "Nowhere to run — you're swallowed up.",
                  "You fight for every inch but they stack you up."
                ];
      const rec =
        brk || yd >= 20
          ? [
              "You beat your man deep — TAKE IT TO THE HOUSE region!",
              "You haul it in and race past the secondary — big play!",
              "You high-point it and turn on the jets!"
            ]
          : yd >= 10
            ? [
                "You haul it in over the middle and rumble!",
                "You snag it at the sideline and turn upfield.",
                "You beat the coverage for a big grab!"
              ]
            : yd >= 3
              ? [
                  "You catch it and fight for a few extra.",
                  "Quick hitter — you turn and pick up yards.",
                  "You reel it in underneath for a solid gain."
                ]
              : [
                  "You corral it but they wrap you up fast.",
                  "You make the grab and they drop you immediately.",
                  "Short catch — no room to work."
                ];
      return randPick(w === "run" ? run : rec);
    }
    // ---- game state ----------------------------------------------------------
    // league level 0 (Pee Wee) → ~7 (Pro): kf scales kicking legs, kick range, and how
    // often young teams go for 2 because kicks are genuinely hard at that age.
    const LVL = (state.player && state.player.level) || 0,
      kf = clamp99(LVL / 7, 0, 1);
    const leagueQ = [6, 7, 8, 9, 10, 12, 15, 15][LVL] || 9,
      QSEC = Math.round(clamp99(window.TU ? window.TU("qtrMinutes", leagueQ) : leagueQ, 3, 15) * 60);
    let quarter = 1,
      clock = QSEC,
      gameOver = !1,
      plays = 0;
    const MAXP = 400,
      rc1 = Math.random() < 0.5 ? "us" : "them",
      rc2 = rc1 === "us" ? "them" : "us";
    let T = null,
      pos = 25,
      marker = 35,
      down = 1,
      toGo = 10;
    const topSec = { us: 0, them: 0 },
      TO = { us: 3, them: 3 },
      other = w => (w === "us" ? "them" : "us"),
      fmtC = w => {
        w = Math.max(0, Math.round(w));
        return Math.floor(w / 60) + ":" + String(w % 60).padStart(2, "0");
      },
      spotTxt = w => (w < 50 ? "own " + Math.round(w) : w > 50 ? "opp " + Math.round(100 - w) : "midfield");
    /* ===== v109 THE GAME HAS A CLOCK =====
     * Every row carries the UNCLAMPED `period` beside the clamped `quarter` and `toLeft` (the timeouts each side
     * still holds). Header-like rows (`header:true`, no snap) now exist for the coin toss, the end of every period,
     * the two-minute warning and a called timeout, and the extra point is its own row instead of a suffix.
     * `window.__V109_D` is the hook the check reads. The warning is PENDING from `spend()` and flushed before the
     * next row is pushed, so it lands after the play that crossed 2:00 and never between a touchdown and its try. */
    const rowMetaV109 = () => ({ period: quarter, toLeft: { us: TO.us, them: TO.them } }),
      V109 = (window.__V109_D = window.__V109_D || {
        games: 0,
        oob: { passN: 0, passSim: 0, runN: 0, runSim: 0 },
        xp: 0,
        twopt: 0,
        timeouts: 0,
        warnings: 0,
        periods: 0,
        measures: 0,
        kneelQ2: 0,
        toss: 0
      });
    V109.games++;
    let warnPendV109 = !1;
    function flushWarnV109() {
      if (!warnPendV109) return;
      warnPendV109 = !1;
      V109.warnings++;
      u.push(
        Object.assign(
          {
            offense: T,
            event: "warning",
            sub: "TWO-MINUTE WARNING",
            yards: 0,
            involved: !1,
            big: !1,
            isPass: !1,
            scored: !1,
            penalty: !1,
            desc: "— ⏱️ TWO-MINUTE WARNING —",
            startBall: pos,
            endBall: pos,
            quarter: Math.min(quarter, 5),
            clock: "2:00",
            usScore: h,
            themScore: p,
            down: Math.min(4, down),
            toGo: Math.max(1, Math.round(toGo)),
            stat: { ...P },
            header: !0,
            secs: 0,
            runoff: 0
          },
          rowMetaV109()
        )
      );
    }
    function hdrV109(ev, desc, extra) {
      flushWarnV109();
      u.push(
        Object.assign(
          {
            offense: T,
            event: ev,
            yards: 0,
            involved: !1,
            big: !1,
            isPass: !1,
            scored: !1,
            penalty: !1,
            desc,
            startBall: pos,
            endBall: pos,
            quarter: Math.min(quarter, 5),
            clock: fmtC(clock),
            usScore: h,
            themScore: p,
            down: Math.min(4, down),
            toGo: Math.max(1, Math.round(toGo)),
            stat: { ...P },
            header: !0,
            secs: 0,
            runoff: 0
          },
          rowMetaV109(),
          extra || {}
        )
      );
    }
    /* the tail of the FieldSim queue is THIS play's log only if the queue grew since the resolver was called */
    const qLenV109 = () => {
        try {
          const g = window.__FieldSim && window.__FieldSim._Q;
          return g ? g.length : 0;
        } catch (w) {
          return 0;
        }
      },
      simTailV109 = q0 => {
        try {
          const g = window.__FieldSim && window.__FieldSim._Q;
          return g && g.length > q0 ? g[g.length - 1].log : null;
        } catch (w) {
          return null;
        }
      },
      simTackleV109 = L => {
        if (!L || !L.events) return null;
        for (let i = L.events.length - 1; i >= 0; i--) if (L.events[i].type === "tackle") return L.events[i];
        return null;
      },
      simOobV109 = q0 => {
        const tk = simTackleV109(simTailV109(q0));
        return !!(tk && (tk.oob || tk.plane === "sideline"));
      },
      /* left / right / middle from where the sim put the ball down: the top of the picture is the offense's LEFT when it
       * attacks to the right (us), its RIGHT when it attacks to the left (them) */
      simSideV109 = (q0, us) => {
        const tk = simTackleV109(simTailV109(q0));
        if (!tk || tk.y == null) return null;
        const d = tk.y - 220;
        if (Math.abs(d) < TU("dirDeadPx", 28)) return "middle";
        return d < 0 === !!us ? "left" : "right";
      },
      GAP_DIR_V109 = { A: "up the gut", B: "inside", C: "off tackle", D: "around the end" },
      runDirV109 = (side, play, concept) => {
        const g = play && play.gap,
          base =
            concept === "sweep"
              ? "on the sweep"
              : concept === "draw"
                ? "on the draw"
                : GAP_DIR_V109[g] || (concept === "power" ? "behind the power blocks" : "inside");
        if (!side || side === "middle") return base;
        return base + " " + side;
      },
      tkTxtV109 = (X, you) => {
        const a = X && X.tackler && !(you && pe(X.tackler)) ? nm(X.tackler) : null,
          b = X && X.assist && !(you && pe(X.assist)) ? nm(X.assist) : null;
        return a && b ? `, brought down by ${a} and ${b}` : a ? `, tackled by ${a}` : b ? `, ${b} in on the stop` : "";
      };
    function header(reason) {
      flushWarnV109();
      u.push(
        Object.assign(
          {
            offense: T,
            event: "drive",
            yards: 0,
            involved: !1,
            big: !1,
            isPass: !1,
            scored: !1,
            desc: `— ${T === "us" ? "🟢 YOUR OFFENSE takes the field" : "🔴 Opponent has the ball"} at the ${spotTxt(pos)}${reason ? " · " + reason : ""} —`,
            startBall: pos,
            endBall: pos,
            quarter: Math.min(quarter, 5),
            clock: fmtC(clock),
            usScore: h,
            themScore: p,
            down: 1,
            toGo: Math.max(1, Math.round(toGo)),
            stat: { ...P },
            header: !0
          },
          rowMetaV109()
        )
      );
    }
    function newDrive(team, spot, reason) {
      T = team;
      pos = clamp99(Math.round(spot), 1, 99);
      down = 1;
      marker = Math.min(100, pos + 10);
      toGo = marker - pos;
      header(reason);
    }
    function kickoffTo(team, tag, onside) {
      if (onside) {
        // trailing team gambles on an onside kick — recovers ~22%
        if (Math.random() < 0.22) {
          newDrive(other(team), clamp99(48 + randInt(-3, 6), 1, 60), "RECOVERS the onside kick!");
          return;
        }
        newDrive(team, clamp99(46 + randInt(-2, 8), 1, 70), "onside kick fails — short field");
        return;
      }
      const retTm = team === "us" ? c.us : c.opp,
        returner = Y(retTm.off, ["RB", "WR"]),
        retSkill = (_(returner, "speed") + _(returner, "agility")) / 2,
        touchP = clamp99(0.46 + kf * 0.3 + (weather === "wind" ? -0.08 : 0), 0.35, 0.82),
        w = Math.random();
      let g = 25,
        N = "touchback";
      w >= touchP && w < 0.985
        ? ((g = clamp99(Math.round(20 + (retSkill - 50) * 0.12 + randRange(-7, 9)), 10, 42)), (N = "kick returned"))
        : w >= 0.985 &&
          ((g = clamp99(randInt(37, 55) + Math.round((retSkill - 55) * 0.15), 30, 65)), (N = "a HUGE return!"));
      // v82: a returned kickoff is an agent play — the kicking team's lanes against the wedge
      let __ko = null;
      if (N !== "touchback") {
        const kUs = other(team) === "us",
          __kk = ie(kUs),
          gross = clamp99(Math.round(50 + kf * 14 + randRange(-6, 6)), 40, 64);
        __ko =
          window.__FieldSim &&
          window.__FieldSim.kickoff &&
          window.__FieldSim.kickoff(kUs, __kk.O, __kk.D, _, { gross, goalLx: -35 * 5.88 });
        if (__ko) {
          const ret = clamp99(__ko.ret, 0, 99);
          g = clamp99(65 - gross + ret, 1, 99);
          N = ret >= 35 ? "a HUGE return!" : "kick returned";
          if (!__ko.td) {
            flushWarnV109();
            u.push(
              Object.assign(
                {
                  offense: other(team),
                  event: "kickoff",
                  yards: 0,
                  involved: !1,
                  big: ret >= 35,
                  isPass: !1,
                  scored: !1,
                  penalty: !1,
                  desc: `🦶 Kickoff — ${gross} yards, returned ${ret} to the ${g}.`,
                  startBall: 35,
                  endBall: clamp99(100 - g, 1, 99),
                  quarter: Math.min(quarter, 5),
                  clock: fmtC(clock),
                  usScore: h,
                  themScore: p,
                  down: 1,
                  toGo: 10,
                  preDown: 1,
                  preToGo: 10,
                  stat: { ...P },
                  team: { ...v },
                  oppStat: { ...j },
                  koResult: ret >= 35 ? "bigReturn" : "return",
                  gross,
                  retYds: ret,
                  clockStopped: null,
                  secs: 0,
                  runoff: 0
                },
                rowMetaV109()
              )
            );
          } else if (typeof dropSimLog === "function") dropSimLog();
        }
      }
      // rare kickoff return TD — the returner takes it the distance
      if (
        N !== "touchback" &&
        (__ko ? __ko.td : Math.random() < clamp99(0.004 + (retSkill - 60) * 0.00008, 0.0015, 0.006))
      ) {
        team === "us" ? (h += 6) : (p += 6);
        const tr =
          quarter >= 5
            ? null
            : tryAfter(team === "us"); /* v109: the try is rolled here (same order) and booked as its own row below */
        flushWarnV109();
        u.push(
          Object.assign(
            {
              offense: team,
              event: "run",
              yards: 100,
              involved: !1,
              big: !0,
              isPass: !1,
              scored: !0,
              penalty: !1,
              desc: `🏈💨 KICKOFF RETURN TOUCHDOWN — housed on the return!${tr ? "" : " WALK-OFF!"} ${xe()}`,
              startBall: 1,
              endBall: 100,
              quarter: Math.min(quarter, 5),
              clock: fmtC(clock),
              usScore: h,
              themScore: p,
              down: 1,
              toGo: 10,
              preDown: 1,
              preToGo: 10,
              stat: { ...P },
              team: { ...v },
              oppStat: { ...j },
              koResult: "td",
              gross: __ko ? clamp99(Math.round(50 + kf * 14), 40, 64) : 0,
              retYds: 100,
              clockStopped: "score",
              secs: 0,
              runoff: 0
            },
            rowMetaV109()
          )
        );
        tr && pushTryV109(tr, team);
        if (quarter >= 5) {
          gameOver = !0;
          return;
        }
        kickoffTo(other(team));
        return;
      }
      newDrive(team, g, (tag ? tag + " · " : "") + N);
    }
    // rolls the quarter when the clock hits 0; returns true if it handed the
    // ball out itself (halftime / OT kickoff), so callers skip their own flip
    function quarterRoll() {
      if (quarter === 1 || quarter === 3) {
        periodV109(quarter === 1 ? "END OF 1ST" : "END OF 3RD");
        quarter++;
        clock = QSEC;
        return !1;
      }
      if (quarter === 2) {
        periodV109("HALFTIME");
        quarter = 3;
        clock = QSEC;
        TO.us = 3;
        TO.them = 3;
        kickoffTo(rc2, "Second-half kickoff");
        return !0;
      }
      if (h !== p) {
        gameOver = !0;
        return !0;
      }
      periodV109(quarter === 4 ? "END OF REGULATION" : "OVERTIME");
      quarter = Math.min(quarter + 1, 9);
      clock = Math.round(QSEC * 0.6);
      kickoffTo(Math.random() < 0.5 ? "us" : "them", "OVERTIME — next score wins");
      return !0;
    }
    /* v109: the period turns on its own row (the quarter used to flip silently) */
    function periodV109(sub) {
      V109.periods++;
      hdrV109(
        "period",
        `— ${sub === "HALFTIME" ? "⏸ HALFTIME" : sub === "OVERTIME" ? "🔁 OVERTIME — next score wins" : "⏹ " + sub} ${xe()} —`,
        { sub }
      );
    }
    // down/toGo carry the PRE-SNAP situation (what the down actually was when the ball
    // was snapped) so the scoreboard and the renderer's coverage shell read the right
    // down & distance; preDown/preToGo remain as explicit aliases.
    function mkPlay(w) {
      flushWarnV109();
      u.push(
        Object.assign(
          {
            offense: w.T0,
            event: "run",
            yards: 0,
            involved: !1,
            big: !1,
            isPass: !1,
            scored: !1,
            penalty: !1,
            desc: "",
            startBall: w.pre.pos,
            endBall: w.pre.pos,
            quarter: w.pre.q,
            clock: w.pre.clock,
            usScore: h,
            themScore: p,
            down: Math.min(4, w.pre.down),
            toGo: Math.max(1, Math.round(w.pre.toGo)),
            preDown: w.pre.down,
            preToGo: w.pre.toGo,
            stat: { ...P },
            team: { ...v },
            oppStat: { ...j },
            clockStopped: null,
            secs: 0,
            runoff: 0
          },
          rowMetaV109(),
          w.o
        )
      );
    }
    // PAT / two-point try — returns a suffix for the TD desc; mutates score
    /* v109 THE EXTRA POINT IS A PLAY — the rolls are the same rolls in the same order (so the totals cannot move);
     * only the booking changed: the touchdown row books 6, then `pushTryV109` books the 1 or 2 on its own row. */
    function tryAfter(usSide) {
      const m2 = usSide ? h - p : p - h;
      const chartGo = quarter >= 4 && [-2, -5, -10, -16].includes(m2),
        youthGo = kf < 0.5 && Math.random() < 0.5 - kf; // young teams go for 2 more — kicks are hard
      if (chartGo || youthGo) {
        const good = Math.random() < 0.47;
        return { kind: "twopt", good, pts: good ? 2 : 0 };
      }
      const patP = clamp99(0.8 + 0.19 * kf, 0.7, 0.99),
        good = Math.random() < patP;
      return { kind: "xp", good, pts: good ? 1 : 0 };
    }
    function pushTryV109(tr, side) {
      const us = side === "us",
        spot = tr.kind === "xp" ? (LVL >= 7 ? TU("xpSpotPro", 15) : TU("xpSpotYouth", 3)) : TU("twoptSpot", 3),
        dist = spot + 17,
        q0 = qLenV109();
      us ? (h += tr.pts) : (p += tr.pts);
      tr.kind === "xp" ? V109.xp++ : V109.twopt++;
      // the kick is drawn by the FG family from the try spot; the sim's own block roll is only a picture here, and a
      // picture that disagrees with the verdict is dropped rather than shown
      // the sim writes stamina and wear back onto the 22 men it uses (v20/v30), and a kick the base game never
      // simulated must not tire anybody: the tanks are snapshotted around it and put back exactly
      if (tr.kind === "xp") {
        try {
          const k = ie(us),
            all = [...k.O.off, ...k.O.def, ...k.D.off, ...k.D.def],
            snap = all.map(w => [w, w._gasV20, w._gassedPlaysV20, w._wearV30]),
            r =
              window.__FieldSim &&
              window.__FieldSim.fg &&
              window.__FieldSim.fg(us, k.O, k.D, _, { dist, pos: 100 - spot, good: tr.good });
          snap.forEach(([w, g, gp, wr]) => {
            g === undefined ? delete w._gasV20 : (w._gasV20 = g);
            gp === undefined ? delete w._gassedPlaysV20 : (w._gassedPlaysV20 = gp);
            wr === undefined ? delete w._wearV30 : (w._wearV30 = wr);
          });
          if (r && r.blocked && qLenV109() > q0) dropSimLog();
        } catch (w) {}
      }
      const desc =
        tr.kind === "xp"
          ? tr.good
            ? `🦶 Extra point is GOOD. ${xe()}`
            : `❌ Extra point is NO GOOD! ${xe()}`
          : tr.good
            ? `✅ 2-PT try is GOOD! ${xe()}`
            : `❌ 2-PT try FAILS. ${xe()}`;
      u.push(
        Object.assign(
          {
            offense: side,
            event: tr.kind,
            yards: 0,
            involved: !1,
            big: !1,
            isPass: !1,
            scored: tr.good,
            penalty: !1,
            desc,
            startBall: 100 - spot,
            endBall: tr.good && tr.kind === "twopt" ? 100 : 100 - spot,
            quarter: Math.min(quarter, 5),
            clock: fmtC(clock),
            usScore: h,
            themScore: p,
            down: 1,
            toGo: spot,
            preDown: 1,
            preToGo: spot,
            stat: { ...P },
            team: { ...v },
            oppStat: { ...j },
            try: { kind: tr.kind, good: tr.good, pts: tr.pts, dist },
            clockStopped: "score",
            secs: 0,
            runoff: 0
          },
          rowMetaV109()
        )
      );
      flushWarnV109();
    }
    // ---- the game -------------------------------------------------------------
    T = rc1;
    V109.toss++;
    hdrV109(
      "toss",
      `🪙 Coin toss — ${rc2 === "us" ? "you win it and defer" : "the opponent wins it and defers"}; ${rc1 === "us" ? "you" : "the opponent"} will receive.`,
      { sub: "COIN TOSS", tossWinner: rc2, receives: rc1 }
    ); /* v109 */
    kickoffTo(rc1, "Opening kickoff");
    while (!gameOver && plays < MAXP) {
      plays++;
      const usDrive = T === "us",
        T0 = T,
        margin = usDrive ? h - p : p - h,
        _v76 =
          ((_gapV76 = usDrive ? c.us.ovr - c.opp.ovr : c.opp.ovr - c.us.ovr),
          (_leadV76 = Math.max(0, margin)),
          (window.__toMultV76 = toV76())),
        pre = { pos, down, toGo: Math.max(1, Math.round(toGo)), clock: fmtC(clock), q: Math.min(quarter, 5) },
        hurry = margin < 0 && ((quarter === 2 && clock <= 150) || (quarter >= 4 && clock <= 330)),
        milk = margin > 0 && quarter === 4 && clock <= 300,
        kickDist = 100 - pos + 17;
      const spend = g => {
        const N = Math.min(g, clock),
          b4 = clock;
        clock -= N;
        topSec[T0] += N;
        if ((quarter === 2 || quarter === 4) && b4 > 120 && clock <= 120) warnPendV109 = !0;
      }; /* v109: crossing 2:00 arms the warning */
      flushWarnV109();
      // max realistic FG range grows with league level (a pee-wee leg tops out ~34, a
      // pro leg ~57), so young teams punt or go for it instead of trotting out 50-yarders
      const fgMax = Math.round((42 + kf * 17) * (weather === "wind" ? 0.95 : weather === "snow" ? 0.97 : 1));
      // ---- victory formation: kneel it out -----------------------------------
      // v109: and the end of the first half too — a team that is not chasing points, out of field-goal range, with the
      // half down to a kneel a down (TU("kneelQ2Sec") per remaining down; 42 s here would have kneeled two minutes away)
      if (
        down <= 3 &&
        ((quarter === 4 && margin > 0 && clock <= (4 - down) * 42) ||
          (quarter === 2 &&
            margin >= 0 &&
            kickDist > fgMax &&
            clock <= (4 - down) * TU("kneelQ2Sec", 20) &&
            TU("kneelQ2", 1)))
      ) {
        quarter === 2 && V109.kneelQ2++;
        pos = clamp99(pos - 1, 1, 99);
        toGo = marker - pos;
        down++;
        mkPlay({
          T0,
          pre,
          o: {
            event: "kneel",
            yards: -1,
            endBall: pos,
            desc: `${nm(Y((usDrive ? c.us : c.opp).off, ["QB"]))} takes a knee to ${quarter === 2 ? "run out the half" : "kill the clock"}. ${xe()}`,
            secs: 3,
            runoff: 39
          }
        });
        spend(3);
        spend(39);
        clock <= 0 && quarterRoll();
        continue;
      }
      // ---- kick decisions ------------------------------------------------------
      const fgSteal =
        clock <= 18 && kickDist <= fgMax && (quarter === 2 || (quarter === 4 && margin <= 0 && margin >= -3));
      let kicking = null;
      if (fgSteal) {
        kicking = "fg";
        if (TO[T0] > 0) {
          TO[T0]--;
          V109.timeouts++;
          hdrV109(
            "timeout",
            `⏱️ ${usDrive ? "You call" : "Opponent calls"} timeout to get the field-goal unit on (${TO[T0]} left).`,
            { team: T0, toTeam: T0, decorative: !0 }
          );
        }
      } /* v109: the clock is not touched — same cost shape as today */
      else if (down === 4) {
        const desperate = quarter >= 4 && margin < 0 && clock <= 240,
          short = toGo <= 2,
          midfield = pos >= 45,
          edge = (profiles[T0].line + profiles[T0].rbSkill - profiles[other(T0)].front * 2) / 100,
          go =
            (desperate && (kickDist > fgMax || (margin < -3 && toGo <= 6))) ||
            (short && midfield && Math.random() < clamp99(0.44 + pos * 0.003 + edge, 0.3, 0.82)) ||
            (quarter >= 4 && margin <= -9 && clock <= 480 && pos >= 40);
        if (!go) kicking = kickDist <= fgMax + (desperate ? 5 : 0) ? "fg" : "punt";
      }
      if (kicking === "fg") {
        // v82: the kick is an agent play — the rush decides the block, the leg decides the make
        const __mkP =
            kickDist <= 32
              ? 0.97
              : kickDist <= 39
                ? 0.9
                : kickDist <= 45
                  ? 0.82
                  : kickDist <= 49
                    ? 0.72
                    : kickDist <= 52
                      ? 0.6
                      : kickDist <= 57
                        ? 0.38
                        : 0.2,
          __good =
            Math.random() <
            clamp99(
              __mkP * (0.78 + 0.22 * kf) * weatherFx.kick + ((usDrive ? c.us.ovr : c.opp.ovr) - 55) * 0.002,
              0.05,
              0.99
            ),
          __fk = ie(usDrive),
          __fg =
            window.__FieldSim &&
            window.__FieldSim.fg &&
            window.__FieldSim.fg(usDrive, __fk.O, __fk.D, _, { dist: kickDist, pos, good: __good });
        // rare block — the kicking team loses it, other team takes over at the spot
        if (__fg ? __fg.blocked : Math.random() < clamp99(0.012 - kf * 0.006, 0.004, 0.012)) {
          mkPlay({
            T0,
            pre,
            o: {
              event: "fg",
              scored: !1,
              desc: usDrive
                ? `🧱 ${kickDist}-yard field goal is BLOCKED!`
                : `🧱 Opponent's ${kickDist}-yard field goal is BLOCKED!`,
              kick: { dist: kickDist, good: !1, blocked: !0 },
              clockStopped: "turnover",
              secs: 5
            }
          });
          spend(5);
          if (clock <= 0) {
            if (quarterRoll() || gameOver) continue;
          }
          newDrive(other(T0), clamp99(107 - pos, 1, 99), "takes over on the blocked kick");
          continue;
        }
        // youth legs shank even chip shots; range accuracy climbs with level (kf)
        const good = __good;
        good && (usDrive ? (h += 3) : (p += 3));
        mkPlay({
          T0,
          pre,
          o: {
            event: "fg",
            scored: good,
            desc: good
              ? usDrive
                ? `✅ ${kickDist}-yard field goal is GOOD. ${xe()}`
                : `Opponent nails a ${kickDist}-yard field goal. ${xe()}`
              : usDrive
                ? `❌ ${kickDist}-yard field goal is NO GOOD — pushed wide!`
                : `Opponent's ${kickDist}-yard field goal is NO GOOD!`,
            kick: { dist: kickDist, good, blocked: !1 },
            clockStopped: good ? "score" : "turnover",
            secs: 5
          }
        });
        spend(5);
        if (good && quarter >= 5) {
          gameOver = !0;
          continue;
        }
        if (clock <= 0) {
          if (quarterRoll() || gameOver) continue;
        }
        good ? kickoffTo(other(T0)) : newDrive(other(T0), clamp99(107 - pos, 1, 99), "takes over on the missed kick");
        continue;
      }
      if (kicking === "punt") {
        // blocked punt — receiving team takes over deep in kicking territory
        if (Math.random() < clamp99(0.005 - kf * 0.002, 0.002, 0.005)) {
          mkPlay({
            T0,
            pre,
            o: {
              event: "punt",
              endBall: clamp99(100 - pos, 1, 99),
              desc: `🧱 PUNT IS BLOCKED! ${usDrive ? "You" : "The opponent"} lose it at the spot.`,
              puntResult: "blocked",
              gross: 0,
              returnYds: 0,
              netYds: 0,
              clockStopped: "turnover",
              secs: 6
            }
          });
          spend(6);
          if (clock <= 0) {
            if (quarterRoll() || gameOver) continue;
          }
          newDrive(other(T0), clamp99(100 - pos + randInt(0, 6), 1, 99), "takes over on the blocked punt");
          continue;
        }
        // muffed punt — returner bobbles it, kicking team recovers (turnover for returner)
        if (Math.random() < 0.012) {
          const land = clamp99(pos + Math.round(26 + kf * 16), 20, 96),
            kickRec = Math.random() < 0.48;
          mkPlay({
            T0,
            pre,
            o: {
              event: "punt",
              endBall: clamp99(100 - (100 - land), 1, 99),
              desc: `😱 MUFFED PUNT — the returner can't handle it and ${kickRec ? "the coverage team" : "the return team"} recovers!`,
              puntResult: "muff",
              gross: land - pos,
              returnYds: 0,
              netYds: land - pos,
              muffRecoveredBy: kickRec ? T0 : other(T0),
              clockStopped: "turnover",
              secs: 7
            }
          });
          spend(7);
          if (clock <= 0) {
            if (quarterRoll() || gameOver) continue;
          }
          kickRec
            ? newDrive(T0, land, "recovers the muffed punt")
            : newDrive(other(T0), clamp99(100 - land, 1, 99), "recovers its own muff");
          continue;
        }
        // punt distance scales with league level (a pee-wee punt is short and wobbly)
        const gross = clamp99(Math.round(26 + kf * 16 + randRange(-7, 9)), 16, Math.max(20, 99 - pos)),
          land = pos + gross;
        let rec, note;
        if (land >= 99) {
          rec = 20;
          note = "touchback";
        } else {
          const deep = 100 - land;
          if (deep <= 10 && Math.random() < 0.5) {
            rec = deep;
            note = "downed at the " + deep;
          } else {
            const rtTm = other(T0) === "us" ? c.us : c.opp,
              rt = Y(rtTm.off, ["RB", "WR"]),
              rs = (_(rt, "speed") + _(rt, "agility")) / 2;
            // v82: the punt is an agent play — coverage lanes, jammers, the returner's decision
            const __pk = ie(usDrive),
              __st =
                window.__FieldSim &&
                window.__FieldSim.punt &&
                window.__FieldSim.punt(usDrive, __pk.O, __pk.D, _, { gross, deep, pos, goalLx: -pos * 5.88 });
            // punt return TD — the returner houses it
            if (__st ? __st.td : Math.random() < clamp99(0.004 + (rs - 60) * 0.00008, 0.0015, 0.006)) {
              if (__st && typeof dropSimLog === "function") dropSimLog();
              const rt = other(T0);
              rt === "us" ? (h += 6) : (p += 6);
              const tr = quarter >= 5 ? null : tryAfter(rt === "us");
              mkPlay({
                T0,
                pre,
                o: {
                  event: "punt",
                  scored: !0,
                  endBall: 100,
                  desc: `🏈💨 PUNT RETURN TOUCHDOWN — taken all the way back!${tr ? "" : " WALK-OFF!"} ${xe()}`,
                  puntResult: "td",
                  gross,
                  returnYds: 100 - land,
                  netYds: -pos,
                  clockStopped: "score",
                  secs: 7
                }
              });
              spend(7);
              tr && pushTryV109(tr, rt); /* v109: the try, on its own row */
              if (quarter >= 5) {
                gameOver = !0;
                continue;
              }
              if (clock <= 0) {
                if (quarterRoll() || gameOver) continue;
              }
              kickoffTo(T0);
              continue;
            }
            const fairP = clamp99(0.38 - (rs - 50) * 0.003 + (deep < 8 ? 0.18 : 0), 0.16, 0.62),
              ret = __st
                ? clamp99(__st.ret, 0, 60)
                : Math.random() < fairP
                  ? 0
                  : clamp99(Math.round(6 + (rs - 50) * 0.1 + randRange(-4, 8)), 0, 22);
            rec = clamp99(deep + ret, 1, 60);
            note = __st
              ? __st.fair
                ? "fair catch"
                : ret
                  ? `returned ${ret} yards`
                  : "no return"
              : ret
                ? `returned ${ret} yards`
                : "fair catch";
          }
        }
        mkPlay({
          T0,
          pre,
          o: {
            event: "punt",
            endBall: clamp99(100 - rec, 1, 99),
            desc: `🦶 4th down punt — ${gross} yards, ${note}. Possession flips.`,
            puntResult:
              note === "touchback"
                ? "touchback"
                : note === "fair catch"
                  ? "fair"
                  : /^downed/.test(note)
                    ? "downed"
                    : /^returned/.test(note)
                      ? "return"
                      : "downed",
            gross,
            returnYds: /^returned (\d+)/.test(note) ? +RegExp.$1 : 0,
            netYds: clamp99(100 - rec, 1, 99) - pos,
            clockStopped: "turnover",
            secs: 7
          }
        });
        spend(7);
        if (clock <= 0) {
          if (quarterRoll() || gameOver) continue;
        }
        newDrive(other(T0), rec, "after the punt");
        continue;
      }
      // ---- penalty: typed fouls with real consequences ------------------------
      // Offensive fouls push the offense back and REPLAY the down (they can never gain
      // a down on a flag). Defensive fouls move the offense up; holding/PI/face-mask are
      // AUTOMATIC first downs, DPI is a spot foul. Undisciplined teams (low awareness)
      // draw more flags, and the flag names the actual player who committed it.
      // v30: only PRE-SNAP fouls are random here (jumpy linemen, bad clock management) —
      // holding / DPI / face mask are now thrown ON the play, from what the sim actually
      // recorded happening (see the flag-on-the-play block after the play resolves).
      {
        const aw = arr => arr.reduce((s, pl) => s + _(pl, "awareness"), 0) / Math.max(1, arr.length),
          offAw = aw(usDrive ? c.us.off : c.opp.off),
          defAw = aw(usDrive ? c.opp.def : c.us.def),
          penRate = clamp99(0.026 + (52 - (offAw + defAw) / 2) * 0.0012 + (hurry ? 0.006 : 0), 0.012, 0.07);
        if (Math.random() < penRate) {
          const onOff = Math.random() < clamp99(0.5 + (defAw - offAw) * 0.006, 0.3, 0.7),
            offFouls = [
              { n: "False Start", yd: 5, u: "OL" },
              { n: "Delay of Game", yd: 5, u: "QB" },
              { n: "Illegal Formation", yd: 5, u: "WR" }
            ],
            defFouls = [
              { n: "Offside", yd: 5, u: "DL" },
              { n: "Encroachment", yd: 5, u: "DL" },
              { n: "Neutral Zone Infraction", yd: 5, u: "DL" }
            ],
            foul = randPick(onOff ? offFouls : defFouls),
            pool = onOff ? (usDrive ? c.us.off : c.opp.off) : usDrive ? c.opp.def : c.us.def,
            cand = pool.filter(x => x.pos === foul.u),
            who = cand.length ? randPick(cand) : randPick(pool),
            rawYd = foul.spot ? randInt(11, 20) : foul.yd,
            dyd = onOff ? -Math.min(rawYd, pos - 1) : Math.min(rawYd, 99 - pos);
          pos = clamp99(pos + dyd, 1, 99);
          let note;
          if (onOff) {
            toGo = marker - pos;
            toGo <= 0 && ((marker = Math.min(100, pos + 10)), (toGo = marker - pos));
            note = "replay the down";
          } else if (foul.auto || pos >= marker) {
            down = 1;
            marker = Math.min(100, pos + 10);
            toGo = marker - pos;
            note = foul.auto ? "automatic first down" : "first down";
          } else {
            toGo = marker - pos;
            note = "replay the down";
          }
          const committed = onOff === usDrive;
          committed ? (v.pen++, (v.penYds += Math.abs(dyd))) : (j.pen++, (j.penYds += Math.abs(dyd)));
          const meFlag = committed && pe(who);
          mkPlay({
            T0,
            pre,
            o: {
              event: "penalty",
              penalty: !0,
              yards: dyd,
              endBall: pos,
              involved: meFlag,
              desc: `🚩 ${foul.n}${meFlag ? " on YOU" : `, ${onOff ? "offense" : "defense"} (#${who.num || ""} ${nm(who)})`} — ${Math.abs(dyd)} yards, ${note}.`,
              foul: {
                name: foul.n,
                on: committed ? "us" : "them",
                side: onOff ? "offense" : "defense",
                player: { num: who.num || "", name: nm(who) },
                yards: Math.abs(dyd),
                spot: pos,
                result: onOff
                  ? "replay"
                  : note === "automatic first down"
                    ? "automaticFirst"
                    : note === "first down"
                      ? "firstDown"
                      : "replay",
                preSnap: !0
              },
              clockStopped: "penalty",
              secs: 4
            }
          });
          spend(4);
          clock <= 0 && quarterRoll();
          continue;
        }
      }
      // ---- play call ------------------------------------------------------------
      // v111: is he out there for this one? His unit only, and only when the dial asks for a rest.
      if (_slot111) {
        const mine111 = usDrive ? _slot111.off : !_slot111.off;
        if (mine111) {
          _snapTm111++;
          _on111 = Math.random() < _share111;
          _on111 && _snapOn111++;
        }
        _slot111.arr[_slot111.i] = mine111 && !_on111 ? _sub111 : _you111;
      }
      let passP;
      if (down === 1) passP = 0.48;
      else if (down === 2) passP = toGo >= 8 ? 0.62 : toGo <= 3 ? 0.4 : 0.52;
      else passP = toGo >= 8 ? 0.86 : toGo >= 4 ? 0.68 : 0.36;
      passP += -0.1 + kf * 0.1; // youth football is run-heavy; sophistication rises by level
      {
        const op = profiles[T0],
          dp = profiles[other(T0)],
          identity = (op.qbIq + op.receivers - op.rbSkill - op.line) / 180,
          matchup = (op.receivers - dp.coverage) / 220;
        passP += clamp99(identity + matchup, -0.14, 0.14);
        pos <= 10 && (passP -= 0.11);
        pos >= 80 && (passP += 0.04);
      }
      (pos >= 97 || (toGo <= 1 && pos >= 90)) && (passP = Math.min(passP, 0.35));
      hurry ? (passP = Math.max(passP, 0.85)) : milk && (passP = Math.min(passP, 0.3));
      // v76: a team that is clear of the script goes to the ground — which is what a
      // staff really does with a lead, and it is also what gives the carries BACK. The
      // dampers cost the you-player about a third of his yardage; running the ball to
      // burn clock returns volume to the backfield while still bleeding points off the
      // scoreboard, so the correction is paid in scoring rather than in his stat line.
      if (gtV76() < 1) passP = Math.min(passP, TU("gtPassCap", 0.32));
      margin <= -14 && quarter >= 3 && (passP += 0.12);
      usDrive && (passP += ((nodeLvl("callPass") || 0) - (nodeLvl("callRun") || 0)) * 0.07);
      // v23: your adopted PREGAME game-plan bends your play-mix toward the pass rate
      // the coach agreed to run. Blended (not forced) so down/distance still matters.
      if (usDrive && window.__gameScriptBiasV23 != null) {
        passP = passP * 0.45 + window.__gameScriptBiasV23 * 0.55;
      }
      passP = clamp99(passP, 0.05, 0.97);
      const isPassCall = Math.random() < passP,
        { O: Ok, D: Dk } = ie(usDrive),
        qb2 = Y(Ok.off, ["QB"]);
      // pick a concept from the situation — surfaced in commentary and fed to the
      // resolver so a "deep shot" actually throws deep and a "screen" stays short.
      let concept;
      if (isPassCall) {
        const goalToGo = pos >= 90;
        if (goalToGo) concept = "fade";
        else if (down >= 3 && toGo >= 8 && Math.random() < 0.28) concept = "screen";
        else if (margin <= -9 && quarter >= 4) concept = "shot";
        else if (down <= 2 && toGo <= 4 && Math.random() < 0.16) concept = "shot";
        else if (toGo <= 4 || hurry) concept = Math.random() < 0.5 ? "quick" : "dropback";
        else concept = Math.random() < 0.12 ? "shot" : "dropback";
      } else {
        if (pos >= 97 || toGo <= 1) concept = "power";
        else if (down >= 3 && toGo >= 7 && Math.random() < 0.4) concept = "draw";
        else concept = Math.random() < 0.3 ? "sweep" : "inside";
      }
      // v101: the chain above still names the FAMILY; the playbook names the actual call inside
      // it, and the call is what the sim and the commentary get.
      const playV101 = pickPlayV101(isPassCall ? "pass" : "run", concept, {
        down,
        toGo,
        pos,
        quarter,
        margin,
        hurry: !!hurry
      });
      if (playV101) concept = playV101.base;
      const paV81 =
        isPassCall &&
        (concept === "dropback" || concept === "shot") &&
        down <= 2 &&
        (playV101 && playV101.pa
          ? Math.random() < TU("paCalledRate", 0.72)
          : Math.random() < TU("paRate", 0.24)); /* v81: play action sells the run first */
      const cTag =
        (paV81 ? "Play action — " : "") +
        (playV101
          ? playV101.tag
          : {
              screen: "Screen — ",
              shot: "Deep shot — ",
              fade: "Fade — ",
              quick: "Quick game — ",
              dropback: "",
              draw: "Draw — ",
              power: "Power — ",
              sweep: "Sweep — ",
              inside: ""
            }[concept] || "");
      let de = 0,
        ne = "run",
        ue = "",
        me = !1,
        Pt = !1,
        _e = !1,
        endB = pos,
        flip = null,
        oob = !1,
        isPassFam = !1,
        pickSix = null,
        oobSim = !1,
        tkV109 = null,
        asV109 = null,
        tkGuessV109 = !1,
        dirV109 = null,
        foulV109 = null,
        measureV109 = !1,
        fdV109 = !1,
        trV109 = null; /* v109: what the row will say about the clock, the tackle and the flag */
      if (isPassCall) {
        isPassFam = !0;
        const olB = (w => (w.length ? w.reduce((g, N) => g + _(N, "blocking"), 0) / w.length : 45))(
            Ok.off.filter(w => w.pos === "OL")
          ),
          dlR = (w => {
            if (!w.length) return 45;
            const base = w.reduce((g, N) => g + (_(N, "strength") + _(N, "burst")) / 2, 0) / w.length,
              star = _starOutlier(Dk.def, "DL");
            return star
              ? base + ((_(star.player, "strength") + _(star.player, "burst")) / 2 - base) * 0.2 * star.blend
              : base;
          })(Dk.def.filter(w => w.pos === "DL")),
          pressureBase = clamp99(
            0.15 + (dlR - olB) * 0.004 + (concept === "shot" ? 0.055 : concept === "quick" ? -0.05 : 0),
            0.06,
            0.34
          ),
          sackP = clamp99(
            pressureBase * (0.48 - (_(qb2, "awareness") - 50) * 0.003 - (_(qb2, "speed") - 50) * 0.0015),
            0.025,
            0.19
          ),
          scrP = clamp99(pressureBase * (0.22 + (_(qb2, "speed") - 55) * 0.003), 0.018, 0.13),
          pressureP = clamp99(pressureBase - sackP - scrP, 0.025, 0.2),
          r2 = Math.random();
        if (r2 < sackP) {
          // SACK — resolved in the trenches, not pre-rolled into the box score
          isPassFam = !1;
          de = -clamp99(Math.round(7 - (_(qb2, "speed") - 50) * 0.035 + randRange(-3, 3)), 2, 12);
          ne = "sack";
          // stat-credit truth: the sack belongs to the sacker the play names — YOU get
          // the stat only when YOU are that man, never a side roll on a teammate's sack
          const starRush = _starOutlier(Dk.def, "DL"),
            skr = starRush && Math.random() < 0.4 * starRush.blend ? starRush.player : Y(Dk.def, ["DL", "DL", "LB"]),
            meSk = !usDrive && pe(skr);
          const stripP =
            clamp99(
              0.045 + (_(skr, "strength") - _(qb2, "awareness")) * 0.0015 + (weatherFx.fumble - 1) * 0.08,
              0.02,
              0.14
            ) * toV76();
          if (Math.random() < stripP) {
            // strip sack — defender power, QB awareness and weather matter
            const defRecP = clamp99(0.48 + (dlR - olB) * 0.004, 0.32, 0.68),
              defRec = Math.random() < defRecP;
            if (defRec) {
              ne = "turnover";
              flip = { spot: clamp99(100 - clamp99(pos + de, 1, 99), 1, 99), why: "scoops up the fumble" };
              usDrive ? v.turn++ : j.turn++;
              ue = `💥 STRIP SACK — ${nm(skr)} knocks it loose and the defense recovers the FUMBLE!`;
            } else ue = `💥 STRIP SACK — ${nm(skr)} jars it free, but the offense dives on the loose ball.`;
            meSk &&
              (P.sack++,
              P.tackle++,
              (me = !0),
              (ue = defRec
                ? `💥 STRIP SACK — YOU blindside ${nm(qb2)} and your defense recovers!`
                : `💥 STRIP SACK — YOU knock it loose, but the offense recovers.`));
          } else {
            ue = `🪖 SACKED — ${nm(skr)} drags down ${nm(qb2)} for a loss of ${-de}.`;
            meSk && (P.sack++, P.tackle++, (me = !0), (ue = `🪖 SACK — YOU bury ${nm(qb2)} for a loss of ${-de}!`));
            usDrive && t === "QB" && (me = !0);
          }
          !usDrive ? v.sacks++ : j.sacks++;
          pos = clamp99(pos + de, 1, 99);
          endB = pos;
          usDrive ? ((v.pass += de), (v.yds += de)) : ((j.pass += de), (j.yds += de));
        } else if (r2 < sackP + scrP) {
          // SCRAMBLE — the QB escapes and takes off
          isPassFam = !1;
          de = clamp99(Math.round(3 + (_(qb2, "speed") - 50) * 0.09 + randRange(-3, 5)), -2, 26);
          ne = "scramble";
          if (pos + de >= 100) {
            if (standV76()) {
              de = Math.max(0, 99 - pos);
            } else {
              de = 100 - pos;
              _e = !0;
            }
          }
          pos = clamp99(pos + de, 1, 99);
          endB = _e ? 100 : pos;
          usDrive ? ((v.rush += de), (v.yds += de)) : ((j.rush += de), (j.yds += de));
          me = usDrive && t === "QB";
          me && ((P.rush += Math.max(0, de)), _e && P.td++);
          ue = _e
            ? usDrive
              ? me
                ? "🏈 TOUCHDOWN — YOU take off on the SCRAMBLE and score! "
                : `🏈 TOUCHDOWN — ${nm(qb2)} SCRAMBLES in for the score! `
              : `${nm(qb2)} SCRAMBLES in — touchdown for the opponent. `
            : usDrive
              ? me
                ? `💨 You escape the pocket — SCRAMBLE for ${de}!`
                : `${nm(qb2)} escapes and SCRAMBLES for ${de}.`
              : `${nm(qb2)} SCRAMBLES for ${de}.`;
        } else {
          const pressured = r2 < sackP + scrP + pressureP,
            moving = pressured && Math.random() < clamp99(0.35 + (_(qb2, "speed") - 50) * 0.006, 0.2, 0.72);
          const _q0 = qLenV109();
          let X = b(usDrive, concept, { pressured, moving, pa: paV81, play: playV101 });
          if (pressured) {
            const throwawayP = clamp99(0.08 + (_(qb2, "awareness") - 40) * 0.004, 0.04, 0.3),
              batP = clamp99(0.025 + (dlR - 50) * 0.001, 0.015, 0.07),
              pr = Math.random();
            if (pr < batP) X = { ...X, complete: !1, intercepted: !1, batted: !0 };
            else if (pr < batP + throwawayP) X = { ...X, complete: !1, intercepted: !1, throwaway: !0 };
          }
          // v32: coverage and pocket contact can create the less-visible penalties
          // missing from v30. Each requires a matching play event; none are blind rolls.
          const defHold =
              !X.complete &&
              !X.intercepted &&
              !X.throwaway &&
              !X.batted &&
              Math.random() < clamp99(0.018 + (_(X.rec, "speed") - _(X.cover, "coverage")) * 0.0004, 0.008, 0.035),
            opi = X.complete && ["shot", "fade"].includes(concept) && Math.random() < 0.014,
            rough = pressured && !X.intercepted && Math.random() < clamp99(0.01 + (dlR - 55) * 0.00035, 0.006, 0.025);
          if (opi || defHold || rough) {
            dropSimLog();
            const committedOff = opi,
              onUs = committedOff === usDrive,
              who = opi ? X.rec : defHold ? X.cover : Y(Dk.def, ["DL", "LB"]),
              raw = opi ? -10 : defHold ? 5 : 15,
              dyd = raw < 0 ? -Math.min(-raw, pos - 1) : Math.min(raw, 99 - pos);
            pos = clamp99(pos + dyd, 1, 99);
            if (committedOff) {
              toGo = marker - pos;
            } else {
              down = 1;
              marker = Math.min(100, pos + 10);
              toGo = marker - pos;
            }
            onUs ? (v.pen++, (v.penYds += Math.abs(dyd))) : (j.pen++, (j.penYds += Math.abs(dyd)));
            const label = opi ? "OFFENSIVE PASS INTERFERENCE" : defHold ? "DEFENSIVE HOLDING" : "ROUGHING THE PASSER";
            mkPlay({
              T0,
              pre,
              o: {
                event: "penalty",
                penalty: !0,
                yards: dyd,
                endBall: pos,
                desc: `🚩 ${label} (#${who.num || ""} ${nm(who)}) — ${Math.abs(dyd)} yards${committedOff ? ", replay the down." : ", automatic first down."}`,
                foul: {
                  name: label,
                  on: onUs ? "us" : "them",
                  side: committedOff ? "offense" : "defense",
                  player: { num: who.num || "", name: nm(who) },
                  yards: Math.abs(dyd),
                  spot: pos,
                  result: committedOff ? "replay" : "automaticFirst"
                },
                clockStopped: "penalty",
                secs: 5
              }
            });
            spend(5);
            clock <= 0 && quarterRoll();
            continue;
          }
          // v30 FLAG ON THE PLAY — DPI: the sim recorded the defender IN the receiver on a
          // real route with the ball arriving incomplete; some of those draw the flag.
          if (
            !X.complete &&
            !X.intercepted &&
            X.flags &&
            X.flags.dpi &&
            Math.random() < (window.TU ? window.TU("dpiFlagP", 0.2) : 0.2)
          ) {
            dropSimLog();
            const who = X.flags.dpi,
              dyd = Math.min(randInt(11, 22), 99 - pos);
            pos = clamp99(pos + dyd, 1, 99);
            down = 1;
            marker = Math.min(100, pos + 10);
            toGo = marker - pos;
            usDrive ? (j.pen++, (j.penYds += dyd)) : (v.pen++, (v.penYds += dyd));
            const meFlag = !usDrive && pe(who);
            mkPlay({
              T0,
              pre,
              o: {
                event: "penalty",
                penalty: !0,
                yards: dyd,
                endBall: pos,
                involved: meFlag,
                desc: `🚩 DEFENSIVE PASS INTERFERENCE${meFlag ? " — flag on YOU" : ` (#${who.num || ""} ${nm(who)})`} — spot foul, ${dyd} yards, automatic first down.`,
                foul: {
                  name: "Defensive Pass Interference",
                  on: usDrive ? "them" : "us",
                  side: "defense",
                  player: { num: who.num || "", name: nm(who) },
                  yards: dyd,
                  spot: pos,
                  result: "automaticFirst",
                  spotFoul: !0
                },
                clockStopped: "penalty",
                secs: 4
              }
            });
            spend(4);
            clock <= 0 && quarterRoll();
            continue;
          }
          if (X.sack) {
            // v82: the sim's quarterback ate the ball — same books as a trench sack, sacker named by the sim
            isPassFam = !1;
            de = clamp99(X.yards, -12, -1);
            ne = "sack";
            const skr = X.sacker || Y(Dk.def, ["DL", "DL", "LB"]),
              meSk = !usDrive && pe(skr);
            ue = `🪖 SACKED — ${nm(skr)} gets home and ${nm(qb2)} takes the sack for a loss of ${-de}.`;
            meSk &&
              (P.sack++,
              P.tackle++,
              (me = !0),
              (ue = `🪖 SACK — YOU get home and ${nm(qb2)} has nowhere to go: loss of ${-de}!`));
            usDrive && t === "QB" && (me = !0);
            !usDrive ? v.sacks++ : j.sacks++;
            pos = clamp99(pos + de, 1, 99);
            endB = pos;
          } else if (X.scramble) {
            // v87: the sim's quarterback saw the lane and took it — booked like the formula scramble, tackler named by the sim
            isPassFam = !1;
            de = clamp99(Math.round(X.yards), -4, 45);
            ne = "scramble";
            if (pos + de >= 100) {
              if (standV76()) {
                de = Math.max(0, 99 - pos);
              } else {
                de = 100 - pos;
                _e = !0;
              }
            }
            pos = clamp99(pos + de, 1, 99);
            endB = _e ? 100 : pos;
            usDrive ? ((v.rush += de), (v.yds += de)) : ((j.rush += de), (j.yds += de));
            me = usDrive && t === "QB";
            me && ((P.rush += Math.max(0, de)), _e && P.td++);
            ue = _e
              ? usDrive
                ? me
                  ? "🏈 TOUCHDOWN — YOU see the lane, tuck it and score! "
                  : `🏈 TOUCHDOWN — ${nm(qb2)} sees the lane and SCRAMBLES in! `
                : `${nm(qb2)} finds a lane and scrambles in — touchdown for the opponent. `
              : usDrive
                ? me
                  ? `💨 Nothing open — you see the lane and SCRAMBLE for ${de}!`
                  : `${nm(qb2)} sees the lane and SCRAMBLES for ${de}.`
                : `${nm(qb2)} tucks it and scrambles for ${de}.`;
            if (!usDrive && ce && !_e && (pe(X.tackler) || pe(X.assist))) {
              P.tackle++;
              me = !0;
              ue += pe(X.tackler) ? " 🔨 You run him down." : " 🤝 You're in on the stop.";
            }
          } else if (X.complete) {
            de = X.yards;
            ne = "pass";
            if (pos + de >= 100) {
              if (standV76()) {
                de = Math.max(0, 99 - pos);
              } else {
                de = 100 - pos;
                _e = !0;
              }
            } else if (pos + de < 1) {
              dropSimLog();
              de = 1 - pos;
            }
            pos = clamp99(pos + de, 1, 99);
            endB = _e ? 100 : pos;
            Pt = !!X.breakaway;
            // air yards vs YAC (prorated if the gain was clamped at the goal line)
            const airY = clamp99(Math.min(X.air != null ? X.air : Math.round(de * 0.6), de), 0, de),
              yacY = Math.max(0, de - airY);
            usDrive
              ? ((v.pass += de), (v.yds += de), (v.air += airY), (v.yac += yacY))
              : ((j.pass += de), (j.yds += de), (j.air += airY), (j.yac += yacY));
            {
              const _r = Math.random();
              oobSim = simOobV109(_q0);
              V109.oob.passN++;
              oobSim && V109.oob.passSim++;
              const _ps = clamp99(TU("oobPassSim", 0.047), 0, 0.99);
              oob = oobSim || _r < Math.max(0, (TU("oobPassRate", 0.18) - _ps) / (1 - _ps));
            } /* v109: one roll either way; p' keeps the aggregate at the target */
            {
              const _sd = simSideV109(_q0, usDrive);
              dirV109 = _sd ? ((X.air || 0) >= TU("deepAirYd", 15) ? "deep" : "short") + " " + _sd : null;
              tkV109 = X.tackler ? nm(X.tackler) : null;
              asV109 = X.assist ? nm(X.assist) : null;
            }
            if (_e) {
              const sc = t === "QB" ? 0.85 : t === "RB" ? 0.12 : t === "WR" ? 0.4 : t === "TE" ? 0.28 : 0;
              // stat authority = the resolved actors: YOU only get credit when YOU are the QB/receiver on the play
              me = usDrive && (t === "QB" ? pe(X.qb) && Math.random() < sc : pe(X.rec));
              usDrive && pe(X.rec) && P.rec_c++;
              usDrive && Ye(me, !0, de, !0);
              ue = usDrive
                ? me
                  ? "🏈 TOUCHDOWN — YOU find the end zone! "
                  : `🏈 TOUCHDOWN — ${nm(X.qb)} hits ${nm(X.rec)} for the score! `
                : `${nm(X.qb)} finds ${nm(X.rec)} — touchdown for the opponent. `;
            } else {
              me = usDrive && (t === "QB" ? pe(X.qb) && Math.random() < 0.92 : pe(X.rec));
              usDrive && pe(X.rec) && P.rec_c++;
              {
                const _tk = tkTxtV109(X, !usDrive && ce),
                  _dr = dirV109 ? " " + dirV109 : ""; /* v109: where he caught it and who put him down */
                ue = usDrive
                  ? me
                    ? J2("recv", de, X.breakaway)
                    : `${nm(X.qb)} finds ${nm(X.rec)}${_dr} for ${de}${X.breakaway ? " — big gain!" : ""}${_tk}.`
                  : `${nm(X.qb)} completes to ${nm(X.rec)}${_dr} for ${de}${_tk}.`;
              }
              usDrive && Ye(me, !0, de, !1);
              if (!usDrive && ce && (pe(X.tackler) || pe(X.assist))) {
                P.tackle++;
                me = !0;
                ue += pe(X.tackler) ? " 🔨 You bring him down." : " 🤝 You're in on the stop.";
              }
            }
          } else if (X.intercepted) {
            ne = "turnover";
            de = 0;
            const air = randInt(4, 18),
              ret = randInt(0, 12),
              spotOff = clamp99(pos + air - ret, 2, 98);
            endB = spotOff;
            flip = { spot: clamp99(100 - spotOff, 1, 99), why: "after the interception" };
            usDrive ? v.turn++ : j.turn++;
            me =
              !usDrive &&
              pe(
                X.cover
              ); /* v117: the pick belongs to the man the play NAMES. The old fallback handed it to you on a coin flip whenever the play named nobody — a team-mate's interception, and his pick six, booked to you by Math.random(); `X.cover` is the sim's ball man (v110) or the formula's cover man, and neither is ever missing */
            // v17 PICK SIX — the return can go the distance. Field position drives the
            // base odds; your own picks add your speed/coverage. House it → defense scores 6.
            const _retDist = usDrive ? spotOff : 100 - spotOff;
            let _p6 = clamp99(0.05 + ((100 - _retDist) / 100) * 0.15, 0.03, 0.24);
            if (me) {
              const _you = c.us.def.find(z => z.you);
              _p6 = clamp99(_p6 * 1.7 + (((_you && _(_you, "speed")) || 55) - 50) / 700, 0.03, 0.55);
            }
            if (Math.random() < _p6) {
              pickSix = usDrive ? "them" : "us";
              flip = null;
              endB = usDrive ? 0 : 100;
              if (me) {
                P.pick6 = (P.pick6 || 0) + 1;
                P.int++;
                P.td++;
                ue = "🙌🏈 PICK SIX!!! YOU jump the route and take it ALL THE WAY BACK!";
              } else
                ue = usDrive
                  ? `🛑🏈 PICK SIX! ${nm(X.cover)} jumps the route and returns it for six!`
                  : "🙌🏈 PICK SIX! Your defense takes it to the house!";
            } else {
              me
                ? (P.int++, (ue = "🙌 INTERCEPTION — YOU jump the route and pick it off!"))
                : (ue = `🛑 INTERCEPTED — ${nm(X.cover)} picks off ${nm(X.qb)}!`);
            }
          } else {
            ne = "incomplete";
            de = 0;
            endB = pos;
            me = usDrive && U && pe(X.rec) && Math.random() < 0.4;
            ue = X.batted
              ? `🖐 ${nm(X.qb)}'s pass is BATTED at the line.`
              : X.throwaway
                ? `${nm(X.qb)} escapes pressure and throws it away.`
                : usDrive
                  ? me
                    ? "Pass sails just past your fingertips."
                    : `${nm(X.qb)}'s pass falls incomplete.`
                  : "Incomplete pass.";
            !usDrive &&
              ce &&
              X.swat &&
              pe(X.cover) &&
              (P.pd++,
              (me = !0),
              (ue = "🖐 PASS BREAKUP — you knock it away!")); /* v87: a break-up is the sim's swat, not a 60% roll */
          }
          X.pressured &&
            X.complete &&
            (ue += X.moving ? " 🎯 Delivered accurately while moving." : " 🎯 Stands in against the rush.");
        }
      } else {
        // ---- called run ----
        /* v103: this blind pre-roll used to be the ONLY way a run was fumbled — resolved outside
         * FieldSim, so nobody was named, nothing animated, and the play simply reported a turnover.
         * The sim strips the ball at the pile now (v103 THE GRIP TICK) and says who punched it out
         * and who fell on it, so the pre-roll keeps only the share the sim cannot see — the
         * mesh-point muff, the ball on the ground before contact. */
        const runCarrier = Y(Ok.off, ["RB", "RB", "QB"]),
          hitPower = avgAttr(
            Dk.def.filter(pl => ["DL", "LB", "S"].includes(pl.pos)),
            "tackling"
          ),
          fumbleP =
            clamp99((0.012 + (hitPower - _(runCarrier, "awareness")) * 0.00035) * weatherFx.fumble, 0.004, 0.035) *
            toV76() *
            TU("fumblePreK", 0.35);
        if (Math.random() < fumbleP) {
          // fumble — resolved without FieldSim so the render queue stays aligned
          de = randInt(-1, 4);
          const spotOff = clamp99(pos + de, 2, 98),
            nearOff = Ok.off.filter(pl => ["OL", "RB", "QB"].includes(pl.pos)).length,
            nearDef = Dk.def.filter(pl => ["DL", "LB", "S"].includes(pl.pos)).length,
            defRecP = clamp99(0.48 + (nearDef - nearOff) * 0.025 + (hitPower - 50) * 0.002, 0.3, 0.7),
            defRec = Math.random() < defRecP;
          endB = spotOff;
          me = !1; /* v87: this fumble is resolved without the sim, so nobody is named — no coin-flip credit */
          if (defRec) {
            ne = "turnover";
            flip = { spot: clamp99(100 - spotOff, 1, 99), why: "recovers the fumble" };
            usDrive ? v.turn++ : j.turn++;
            ue = me
              ? "💥 FORCED FUMBLE — you punch it out and the defense recovers!"
              : `💥 FUMBLE — ${nm(runCarrier)} loses it and the defense wins the scramble!`;
          } else {
            ne = "run";
            pos = spotOff;
            usDrive ? ((v.rush += de), (v.yds += de)) : ((j.rush += de), (j.yds += de));
            ue = `💥 FUMBLE — ${nm(runCarrier)} loses it, but the offense recovers in the pile.`;
          }
        } else {
          const _q0 = qLenV109(),
            X = B(usDrive, concept, playV101);
          de = X.yards;
          ne = "run";
          /* v103 WHAT THE TAPE SAYS — the sim knows whether he was dragged down or drove through
           * it, so the play description says so instead of reporting a bare number of yards. */
          const _drag = X.dragYd || 0,
            _strainRun = !!X.strain;
          /* v103 THE STRIP THE SIM NAMED — the ball came out at the pile, and unlike the pre-roll
           * above this one knows exactly who punched it out, who was carrying it and who fell on
           * it. It also keeps its render log, so the fumble ANIMATES instead of being a line of
           * text about a play nobody saw. */
          if (X.fumble) {
            const F = X.fumble,
              fby = F.forcedBy,
              spotOff = clamp99(pos + de, 2, 98);
            endB = spotOff;
            const meF = !usDrive && pe(fby),
              meLost = usDrive && pe(F.by);
            // stat-credit truth: punching the ball out is a FORCED FUMBLE, not a tackle — the play
            // has no tackle event to trace it to, and crediting one here fails creditcheck
            meF && (P.ff++, (me = !0));
            meLost && (P.fum++, (me = !0));
            if (F.defRec) {
              // `de` stays the ground he had made before it came out: the sim's render log is
              // matched to the play by its yards, and zeroing it here threw the animation away
              ne = "turnover";
              flip = { spot: clamp99(100 - spotOff, 1, 99), why: "recovers the fumble" };
              usDrive ? v.turn++ : j.turn++;
              ue = meF
                ? `💥 FORCED FUMBLE — YOU rip it out of ${nm(F.by)} and your defense recovers!`
                : meLost
                  ? `💥 FUMBLE — ${nm(fby)} punches it out of YOUR hands and the defense recovers.`
                  : `💥 FUMBLE — ${nm(fby)} rips it away from ${nm(F.by)} and the defense recovers!`;
            } else {
              pos = spotOff;
              usDrive ? ((v.rush += de), (v.yds += de)) : ((j.rush += de), (j.yds += de));
              ue = meF
                ? `💥 YOU punch it out of ${nm(F.by)} — but the offense falls on it.`
                : `💥 FUMBLE — ${nm(fby)} jars it loose from ${nm(F.by)}, and the offense recovers in the pile.`;
            }
            // no early exit: the shared tail below books the play, spends the clock and — when the
            // defence fell on it — hands the ball over through `flip`, exactly as every other
            // turnover in the game does
          }
          // v30 FLAG ON THE PLAY — holding: the sim recorded a blocker beaten instantly
          // (he grabbed cloth); some of those draw the flag and the run comes back.
          if (X.flags && X.flags.hold && Math.random() < (window.TU ? window.TU("holdFlagP", 0.5) : 0.5)) {
            dropSimLog();
            const who = X.flags.hold,
              dyd = -Math.min(10, pos - 1);
            pos = clamp99(pos + dyd, 1, 99);
            toGo = marker - pos;
            toGo <= 0 && ((marker = Math.min(100, pos + 10)), (toGo = marker - pos));
            usDrive ? (v.pen++, (v.penYds += Math.abs(dyd))) : (j.pen++, (j.penYds += Math.abs(dyd)));
            const meFlag = usDrive && pe(who);
            mkPlay({
              T0,
              pre,
              o: {
                event: "penalty",
                penalty: !0,
                yards: dyd,
                endBall: pos,
                involved: meFlag,
                desc: `🚩 HOLDING${meFlag ? " on YOU" : `, offense (#${who.num || ""} ${nm(who)})`} — the run comes back, ${Math.abs(dyd)} yards, replay the down.`,
                foul: {
                  name: "Holding",
                  on: usDrive ? "us" : "them",
                  side: "offense",
                  player: { num: who.num || "", name: nm(who) },
                  yards: Math.abs(dyd),
                  spot: pos,
                  result: "replay"
                },
                clockStopped: "penalty",
                secs: 4
              }
            });
            spend(4);
            clock <= 0 && quarterRoll();
            continue;
          }
          if (pos + de >= 100) {
            if (standV76()) {
              de = Math.max(0, 99 - pos);
            } else {
              de = 100 - pos;
              _e = !0;
            }
          } else if (pos + de < 1) {
            dropSimLog();
            de = 1 - pos;
          }
          pos = clamp99(pos + de, 1, 99);
          endB = _e ? 100 : pos;
          Pt = !!X.breakaway;
          usDrive ? ((v.rush += de), (v.yds += de)) : ((j.rush += de), (j.yds += de));
          {
            const _r = Math.random();
            oobSim = simOobV109(_q0);
            V109.oob.runN++;
            oobSim && V109.oob.runSim++;
            const _ps = clamp99(TU("oobRunSim", 0), 0, 0.99);
            oob = oobSim || _r < Math.max(0, (TU("oobRunRate", 0.12) - _ps) / (1 - _ps));
          } /* v109 */
          {
            const _sd = simSideV109(_q0, usDrive);
            dirV109 = runDirV109(_sd, playV101, concept);
            tkV109 = X.tackler ? nm(X.tackler) : null;
            asV109 = X.assist ? nm(X.assist) : null;
            if (!X.tackler && !X.fumble && pos + de < 100) {
              const _pool = Dk.def.filter(z => !z.you);
              if (_pool.length) {
                tkGuessV109 = !0;
                tkV109 = nm(Y(_pool, de >= 7 ? ["S", "CB", "LB"] : ["DL", "LB", "LB"]));
              }
            }
          } /* v109: a name for the fallback runs (never the you-player, never a stat); the sim's own tackler wins when it has one */
          if (_e) {
            me = usDrive && pe(X.carrier);
            me && t === "RB" && P.carries++;
            usDrive && Ye(me, !1, de, !0);
            ue = usDrive
              ? me
                ? "🏈 TOUCHDOWN — YOU punch it in! "
                : `🏈 TOUCHDOWN — ${nm(X.carrier)} punches it in! `
              : `${Tt()} reaches the end zone for the opponent. `;
          } else {
            me = usDrive && pe(X.carrier);
            me && t === "RB" && P.carries++;
            {
              const _tk = tkTxtV109(
                X,
                !usDrive && ce
              ); /* v109: the direction and the tackler — the you-player keeps his J2 line */
              ue = usDrive
                ? me
                  ? J2("run", de, X.breakaway)
                  : X.breakaway
                    ? `${nm(X.carrier)} breaks free ${dirV109} for ${de}${_tk}!`
                    : de <= 1
                      ? `${nm(X.carrier)} is stuffed ${dirV109}${_tk}.`
                      : `${nm(X.carrier)} rushes ${dirV109} for ${de}${_tk}.`
                : `${nm(X.carrier)} rushes ${dirV109} for ${de}${_tk}.`;
            }
            // v103: the sim knows HOW he was brought down — say it
            if (_strainRun && de > 0) ue += ` He drove for the extra.`;
            else if (_drag >= TU("dragSayYd", 1.6)) ue += ` He carried the tackler ${_drag.toFixed(1)} more.`;
            usDrive && Ye(me, !1, de, !1);
            // v30: a wrap from dead behind can catch the mask — 15 tacked onto the run
            if (X.flags && X.flags.fm && Math.random() < (window.TU ? window.TU("fmFlagP", 0.08) : 0.08)) {
              const _fmw = X.flags.fm,
                _fma = Math.min(15, 99 - pos);
              pos = clamp99(pos + _fma, 1, 99);
              endB = pos;
              usDrive ? (j.pen++, (j.penYds += _fma)) : (v.pen++, (v.penYds += _fma));
              !usDrive &&
                pe(_fmw) &&
                (me =
                  !0); /* v117: a flag on YOU is a play you are in — the row says so, the way the holding and DPI rows already do. No stat rides on it */
              ue += ` 🚩 FACE MASK on the tackle${!usDrive && pe(_fmw) ? " — flag on YOU" : ` (#${_fmw.num || ""} ${nm(_fmw)})`} — ${_fma} tacked on.`;
              foulV109 = {
                name: "Face Mask",
                on: usDrive ? "them" : "us",
                side: "defense",
                player: { num: _fmw.num || "", name: nm(_fmw) },
                yards: _fma,
                spot: pos,
                result: "tackedOn",
                liveBall: !0
              }; /* v109 */
            }
            /* v103 THE HORSE COLLAR — the sim flags a grab from dead behind that hauled him down by
             * the shoulders. Same shape as the face mask above: a live candidate the game rolls,
             * fifteen yards and an automatic first down. */
            if (X.flags && X.flags.hc && Math.random() < TU("hcFlagP", 0.5)) {
              const _hcw = X.flags.hc,
                _hca = Math.min(15, 99 - pos);
              pos = clamp99(pos + _hca, 1, 99);
              endB = pos;
              down = 1;
              marker = Math.min(100, pos + 10);
              toGo = marker - pos;
              usDrive ? (j.pen++, (j.penYds += _hca)) : (v.pen++, (v.penYds += _hca));
              !usDrive && pe(_hcw) && (me = !0); /* v117: same — the flag names him, so the row is his */
              ue += ` 🚩 HORSE COLLAR on the tackle${!usDrive && pe(_hcw) ? " — flag on YOU" : ` (#${_hcw.num || ""} ${nm(_hcw)})`} — ${_hca} yards, automatic first down.`;
              foulV109 = {
                name: "Horse Collar",
                on: usDrive ? "them" : "us",
                side: "defense",
                player: { num: _hcw.num || "", name: nm(_hcw) },
                yards: _hca,
                spot: pos,
                result: "automaticFirst",
                liveBall: !0
              }; /* v109 */
            }
            if (!usDrive && ce) {
              // stat-credit truth: YOU get the tackle only when the sim names you the
              // tackler or a genuine gang assist — no coin-flip credit on plays where
              // the tackler is unknown (formula fallback / promoted gash runs)
              const _solo = pe(X.tackler),
                _asst = !_solo && pe(X.assist);
              if (_solo || _asst) {
                ["DL", "LB"].includes(t) && de <= 1 && Math.random() < 0.3
                  ? (P.tackle++, (me = !0), (Pt = !0), (ue = "💥 TACKLE FOR LOSS — you blow up the run!"))
                  : (P.tackle++,
                    (me = !0),
                    (ue = _asst
                      ? "🤝 You rally to the ball — in on the gang tackle."
                      : "🔨 You read it and make the tackle."));
              }
            }
          }
        }
      }
      // ---- scoring / downs -----------------------------------------------------
      /* ===== v87 SAFETY — tackled in his own end zone ===== */
      let safetyNow = !1;
      if (
        !_e &&
        !flip &&
        !pickSix &&
        (ne === "run" || ne === "pass" || ne === "scramble" || ne === "sack") &&
        pre.pos + de <= 0 &&
        TU("safetyOn", 1)
      ) {
        safetyNow = !0;
        usDrive ? (p += 2) : (h += 2);
        pos = 1;
        endB = 1;
        flip = { spot: clamp99(40 + randRange(-8, 8), 25, 55), why: "takes the free kick after the safety" };
        ue += usDrive
          ? " 🚨 SAFETY — tackled in your own end zone. Two points to the defense."
          : " 🚨 SAFETY — the opponent is tackled in his own end zone. Two points!";
      }
      let scoredNow = !1;
      if (pickSix) {
        // defensive return TD — the intercepting side scores, then kicks back off
        scoredNow = !0;
        const _sc = pickSix === "us";
        _sc ? (h += 6) : (p += 6);
        if (quarter >= 5) {
          _sc ? (h += 1) : (p += 1);
          ue += `WALK-OFF! ${xe()}`;
        } else {
          trV109 = tryAfter(_sc);
          ue += ` ${xe()}`;
        } /* v109: same roll, its own row */
      } else if (_e) {
        scoredNow = !0;
        usDrive ? (h += 6) : (p += 6);
        if (quarter >= 5) {
          usDrive ? (h += 1) : (p += 1);
          ue += `WALK-OFF! ${xe()}`;
        } else {
          trV109 = tryAfter(usDrive);
          ue += ` ${xe()}`;
        } /* v109 */
      } else if (ne === "run" || ne === "pass" || ne === "scramble" || ne === "sack" || ne === "incomplete") {
        if (pre.down === 3) {
          if (usDrive) {
            v.third_a++;
            pos >= marker && v.third_c++;
          } else {
            j.third_a++;
            pos >= marker && j.third_c++;
          }
        }
        /* v109 THE STICKS COME OUT — a spot inside a yard of the marker is measured; the arithmetic below is untouched */
        measureV109 = ne !== "incomplete" && Math.abs(pos - marker) < TU("measureYd", 1);
        measureV109 && V109.measures++;
        fdV109 = pos >= marker && ne !== "incomplete";
        if (pos >= marker && ne !== "incomplete") {
          ue += " 🟢 First down!";
          usDrive ? v.first++ : j.first++;
          down = 1;
          marker = Math.min(100, pos + 10);
          toGo = marker - pos;
        } else {
          down++;
          toGo = marker - pos;
          if (down > 4 && !flip) {
            ue += " — Turnover on downs!";
            flip = { spot: clamp99(100 - pos, 1, 99), why: "takes over on downs" };
          }
        }
      }
      // prepend the concept tag on ordinary run/pass snaps (scores keep their own flavor)
      const dTag = cTag && !_e && (ne === "run" || ne === "pass" || ne === "incomplete") ? cTag : "";
      /* v109 THE CLOCK TELLS THE TRUTH — why it stopped (or null when it runs), the seconds the snap took, and the
       * runoff that follows (written below once the timeout decision is made) */
      const secsV109 =
          ne === "sack"
            ? 7
            : ne === "scramble"
              ? 8
              : concept === "shot"
                ? 8
                : concept === "quick" || concept === "screen"
                  ? 4
                  : ne === "run"
                    ? 6
                    : 5,
        stopV109 =
          scoredNow || safetyNow
            ? "score"
            : flip || pickSix
              ? "turnover"
              : ne === "incomplete"
                ? "incomplete"
                : oob
                  ? "oob"
                  : null;
      mkPlay({
        T0,
        pre,
        o: {
          event: ne,
          yards: de,
          involved: me,
          big: Pt || Math.abs(de) >= 15 || safetyNow,
          isPass: isPassFam,
          scored: _e || !!pickSix,
          pickSix: !!pickSix,
          safety: safetyNow,
          noHuddle: !!hurry,
          concept,
          desc: dTag + ue,
          endBall: endB,
          clockStopped: stopV109,
          secs: secsV109,
          runoff: 0,
          oob,
          oobSim,
          tackler: tkV109,
          assist: asV109,
          tacklerSim: !!tkV109 && !tkGuessV109,
          dir: dirV109,
          measure: measureV109,
          firstDown: fdV109,
          foul: foulV109,
          gap: (playV101 && playV101.gap) || null,
          play: (playV101 && playV101.id) || null
        }
      });
      const rowV109 = u[u.length - 1];
      // ---- clock & possession ---------------------------------------------------
      spend(secsV109);
      if (scoredNow) {
        trV109 && pushTryV109(trV109, pickSix ? pickSix : T0); /* v109: the try after the six */
        if (quarter >= 5) {
          gameOver = !0;
          continue;
        }
        if (clock <= 0 && (quarterRoll() || gameOver)) continue;
        // a team that just scored but is still trailing late gambles on an onside kick
        const kickTo = pickSix ? T0 : other(T0),
          scorer = pickSix ? other(T0) : T0,
          scMargin = scorer === "us" ? h - p : p - h,
          onside = !pickSix && quarter >= 4 && clock <= 200 && scMargin < 0 && scMargin >= -16 && Math.random() < 0.8;
        kickoffTo(kickTo, "", onside);
        continue;
      }
      if (flip) {
        if (clock <= 0 && (quarterRoll() || gameOver)) continue;
        newDrive(other(T0), flip.spot, flip.why);
        continue;
      }
      if (clock <= 0) {
        quarterRoll();
        continue;
      }
      const stops = ne === "incomplete" || oob;
      if (!stops) {
        let runoff = milk ? 39 : hurry ? (ne === "run" ? 16 : 11) : 24 + randInt(-3, 4);
        // late-half timeout: the trailing team burns one to stop the clock and save time
        const late = quarter >= 4 ? clock <= 180 : quarter === 2 && clock <= 120,
          trailing = margin < 0 ? T0 : margin > 0 ? other(T0) : null;
        if (late && trailing && TO[trailing] > 0 && runoff > 10 && Math.random() < 0.7) {
          flushWarnV109();
          TO[trailing]--;
          runoff = 3;
          rowV109.clockStopped = "timeout";
          V109.timeouts++; /* the warning (if the snap crossed 2:00) lands before the timeout is charged */
          hdrV109(
            "timeout",
            `⏱️ ${trailing === "us" ? "You call" : "Opponent calls"} timeout (${TO[trailing]} left).`,
            { team: trailing, toTeam: trailing }
          ); /* v109: its own row — the scorebug counts it */
        }
        rowV109.runoff = runoff;
        spend(runoff);
        clock <= 0 && quarterRoll();
      }
    }
    // somebody has to win — if the play cap ever hits with a tie, a walk-off FG
    if (h === p) {
      const w = c.us.ovr === c.opp.ovr ? Math.random() < 0.5 : c.us.ovr > c.opp.ovr;
      w ? (h += 3) : (p += 3);
      u.push(
        Object.assign(
          {
            offense: w ? "us" : "them",
            event: "fg",
            yards: 0,
            involved: !1,
            big: !0,
            isPass: !1,
            scored: !0,
            penalty: !1,
            desc: w ? `✅ WALK-OFF field goal is GOOD! ${xe()}` : `Opponent nails the walk-off field goal. ${xe()}`,
            startBall: 75,
            endBall: 75,
            quarter: Math.min(quarter, 5),
            clock: "0:00",
            usScore: h,
            themScore: p,
            down: 4,
            toGo: 3,
            preDown: 4,
            preToGo: 3,
            stat: { ...P },
            team: { ...v },
            oppStat: { ...j },
            kick: { dist: 42, good: !0, blocked: !1 },
            clockStopped: "score",
            secs: 0,
            runoff: 0
          },
          rowMetaV109()
        )
      );
    }
    window.__fullGameBalanceV39 = !1;
    l.sacks = v.sacks;
    const Mt2 = v.third_a > 0 ? `${Math.min(v.third_c, v.third_a)}/${v.third_a}` : "0/0",
      xt = {
        yds: v.yds,
        pass: v.pass,
        rush: v.rush,
        first: v.first,
        third: Mt2,
        top: fmtC(topSec.us),
        pen: v.pen,
        penYds: v.penYds,
        turn: v.turn,
        sacks: v.sacks,
        air: v.air,
        yac: v.yac
      },
      ua = {
        yds: j.yds,
        pass: j.pass,
        rush: j.rush,
        first: j.first,
        third: j.third_a > 0 ? `${Math.min(j.third_c, j.third_a)}/${j.third_a}` : "0/0",
        top: fmtC(topSec.them),
        pen: j.pen,
        penYds: j.penYds,
        turn: j.turn,
        sacks: j.sacks,
        air: j.air,
        yac: j.yac
      },
      fa = qi(t, P, r),
      /* v111: what the focus was actually worth to the AGENTS. The multiplier lands inside `_raw`,
       * which is the accessor FieldSim asks for every key it builds an agent from — so this row puts
       * his number against a team-mate's on the same key, and against the roster value it started
       * from. `v111Acheck` reads it: a focus that does nothing on the field shows up here. */
      _fx111 = (() => {
        try {
          const fbs = (_tb111 || []).filter(b => b && b.mul != null);
          if (!fbs.length) return null;
          const me = c.us.off.find(w => w && w.you) || c.us.def.find(w => w && w.you);
          if (!me) return null;
          const unit = c.us.off.indexOf(me) >= 0 ? c.us.off : c.us.def,
            peer = unit.find(w => w && !w.you) || null;
          return fbs.map(fb => ({
            stat: fb.stat,
            kind: fb.v111 || "buff",
            mul: fb.mul,
            rosterHas: !!(me.attrs && me.attrs[fb.stat] != null),
            base: (me.attrs && me.attrs[fb.stat]) || 45,
            raw: _raw(me, fb.stat),
            peerRaw: peer ? _raw(peer, fb.stat) : null,
            att: _(me, fb.stat),
            peerAtt: peer ? _(peer, fb.stat) : null
          }));
        } catch (_e) {
          return null;
        }
      })();
    return {
      plays: u,
      usScore: h,
      themScore: p,
      stat: fa,
      team: xt,
      oppTeam: ua,
      roster: c,
      focusV111: _fx111,
      /* v111: what he ACTUALLY did, which is what the body is billed from */
      snapsV111: {
        on: _slot111 ? _snapOn111 : null,
        team: _slot111 ? _snapTm111 : null,
        share: _share111,
        touchMul: _touch111,
        key: _u111 ? _u111.key : "normal",
        touches: (fa.carries || 0) + (fa.rec_c || 0) + (fa.att || 0)
      }
    };
  }
  window.__simGameV2 = simGameV2;
  /* v22.2: pregame preview built from the SAME roster builder the game uses (Wr),
   * so the displayed team OVRs + top-5 MATCH the team you actually play. The old
   * pregame generated a separate scouting-scale roster (__GRIDIRON_GENERATE_ROSTER_V157)
   * whose numbers were unrelated to the in-game opponent — hence the mismatch. */
  // v22.3: deterministic per-opponent team-strength multiplier (0.9x..1.3x of the
  // level base). Keyed on opponent name + season so the SAME opponent gets the
  // SAME strength in the pregame preview and the live game — varied week to week.
  function __oppMulForV22(name) {
    let h = 0;
    const s = String(name || "") + "|" + ((state.player && state.player.seasonSeed) || 0);
    for (let i = 0; i < s.length; i++) h = (h * 31 + s.charCodeAt(i)) >>> 0;
    return 0.94 + ((h % 1000) / 1000) * 0.2;
  }
  window.__previewMatchupV22 = function (pos, perf) {
    try {
      const e = state.player;
      if (!e || !e.pos) return null;
      const seed = clamp99(Math.round(Number(perf != null && !isNaN(perf) ? perf : __seasonSeed(e))), 5, 100);
      const _wk = (e.weekResults || []).find(w => !w.played);
      const _prev = window.__oppMulV22;
      window.__oppMulV22 = __oppMulForV22(_wk && _wk.opp);
      const c = buildGameRosters(pos || e.pos, seed / 100, {}, { sacks: 0 }, {}, e.level, playerOvr(e));
      window.__oppMulV22 = _prev;
      const roster = t =>
        [...t.off, ...t.def].map(x => ({ name: x.name, pos: x.pos, ovr: Math.round(x.ovr || 0), you: !!x.you }));
      return { seed, us: { ovr: c.us.ovr, players: roster(c.us) }, opp: { ovr: c.opp.ovr, players: roster(c.opp) } };
    } catch (err) {
      return null;
    }
  };
  /* ===== v17 AI-DRIVEN SEASON GAMES ========================================
   * Every season game — watched OR quick-simmed — is now resolved by the
   * emergent physics engine (__simGameV2). Running, passing, INTs, pick-sixes,
   * the final score, and your performance grade all EMERGE from play-by-play
   * agent football instead of a pre-rolled formula. et()/ia() survive only as a
   * fallback when the engine is unavailable. */
  function __seasonSeed(e) {
    const s = playerPower(e),
      n = LEVELS[e.level],
      base = (s - (n.need - 6)) * 1.35 + 52;
    return clamp99(Math.round(base + ((state && state._fateBoost) || 0)), 8, 99);
  }
  function __aiSeasonGame(e, opts) {
    opts = opts || {};
    let seed = __seasonSeed(e);
    const gm = Math.max(1, LEVELS[e.level].games);
    seed += ((opts.perfSeed || 0) / gm) * 2;
    // v23: the rest-of-season "Season Momentum" boost was removed (too strong — it
    // compounded a flat rating bump into every game). Story-arc picks now grant a
    // single-game, personality-driven outcome only (see the wheel land() handler).
    // v20: the next-game boost is no longer an abstract "perf" bump — it is a REAL
    // +1% per point to every attribute for this one game, applied inside the sim's
    // attribute accessor (_raw), and it may push stats past the player's normal cap.
    let _sb = 0;
    if (e._nextGameBoost) {
      _sb = Math.round(e._nextGameBoost);
      e._nextGameBoost = 0;
    }
    if (opts.playoff) seed += treeFx("playoffPerf") + seasonModFx("playoffPerf");
    seed += clutchV131(
      e,
      opts
    ); /* v131: the Small-Town Prospect is clutch, for real, in the games that decide a season */
    if (opts.oppBoost) seed -= opts.oppBoost * 30; // tougher slate lowers effective quality
    seed = clamp99(seed, 5, 100);
    const _gsB = e._gameScriptV23 && e._gameScriptV23.gsPass != null ? e._gameScriptV23.gsPass : null;
    e._gameScriptV23 = null;
    const _fm146 = formRollV146(opts.varMult); /* v146 D: the plan's swing, rolled with the game it swings */
    /* v111: the list the sim plays with is the wheel's swing PLUS v111's own — a lingering wear cut
     * still counting down, and this game's focus multiplier. Spending the game is a decay now, not a
     * wipe: an entry with no `games` countdown is dropped exactly as it always was. */
    let g = null;
    try {
      window.__youStatBoostPctV20 = _sb;
      window.__youTempBuffsV25 = wearBuffsV111(e).concat(_fm146.buffs);
      window.__gameScriptBiasV23 = _gsB;
      /* v146 D: the watched game is played with THESE inputs (lt reads the stash), so the game you see is the game that was projected */
      e._simInV146 = {
        wk: (e.weekResults || []).indexOf(curWeekV111(e)),
        seed,
        buffs: window.__youTempBuffsV25.slice(),
        gs: _gsB
      };
      g = window.__simGameV2(seed, e.pos);
    } catch (_) {
      g = null;
    } finally {
      window.__youStatBoostPctV20 = 0;
      window.__youTempBuffsV25 = null;
      decayWearV111(e);
      window.__gameScriptBiasV23 = null;
    }
    const etRoll = rollGamePerf(e, opts.perfSeed || 0, opts),
      injRoll = etRoll.injured;
    const grader = typeof gradeGame === "function" ? gradeGame : window.__gradeGame;
    if (!g || !g.stat || typeof grader !== "function") {
      const sc = ia(e, etRoll.perf, opts);
      return {
        perf: etRoll.perf,
        ovr: playerPower(e),
        injured: injRoll,
        us: sc.us,
        them: sc.them,
        won: sc.won,
        statLine: null
      };
    }
    const snaps = (g.plays || []).filter(pl => pl && pl.involved).length || 12;
    const grade = grader(e.pos, g.stat, snaps);
    const us = g.usScore != null ? g.usScore : 0,
      them = g.themScore != null ? g.themScore : 0;
    // The SCORE and STAT LINE (yards, TDs, INTs, pick-sixes) are 100% emergent — they
    // come straight from the play-by-play. The PERF grade stays on its calibrated scale
    // (national rank / advancement depend on it), but is MODULATED by how the real box
    // score actually graded and by the result — so your rating rides on the AI game,
    // without the full-game box-score grader inflating everyone to an A+.
    const dev = clamp99((grade.score - 70) / 24, -0.8, 1),
      winBump = us > them ? 3 : us < them ? -2 : 0;
    let perf = clamp99(Math.round(etRoll.perf + dev * 13 + winBump), 1, 100);
    // v73: the body's cut of this game, recorded where it is actually charged. A bad
    // grade that came from a bad body has to be able to SAY so afterwards, and the
    // only place that is knowable is here, either side of the multiplier.
    const _preBody = perf;
    perf = clamp99(Math.round(perf * condMultV54(e)), 1, 100);
    return {
      perf,
      bodyCostV73: perf - _preBody,
      ovr: playerPower(e),
      injured: injRoll,
      us,
      them,
      won: us > them,
      statLine: g.stat,
      gameGrade: grade.grade,
      snaps,
      snapsV111: g.snapsV111 || null,
      formV146: _fm146.pts
    };
  }
  /* v20 PREGAME ODDS — a real win probability computed from the SAME inputs the
   * engine will play the game with: your effective perf seed (OVR vs level need,
   * prestige, season boosts), the opponent's rating relative to level average
   * (the exact oppBoost term the sim applies), playoff pressure, and your active
   * next-game stat boost. Curve calibrated against __simGameV2 win rates
   * (seed 10 → ~38%, seed 90 → ~71%). */
  window.__gameOddsV20 = function () {
    const e = state.player;
    if (!e || !e.weekResults) return null;
    const wk = e.weekResults.find(w => !w.played);
    if (!wk) return null;
    let seed = __seasonSeed(e);
    const oppR = wk.opponentV11 && wk.opponentV11.rating != null ? Number(wk.opponentV11.rating) : 45 + e.level * 5.5;
    const rel = oppR - (45 + e.level * 5.5);
    const oppBoost = rel / 180 + (wk.playoff ? 0.08 : 0);
    seed -= oppBoost * 30;
    if (wk.playoff) seed += treeFx("playoffPerf") + seasonModFx("playoffPerf");
    seed = clamp99(seed, 5, 100);
    const nb = e._nextGameBoost || 0;
    let p = 0.5 + (seed - 48) * (window.TU ? window.TU("winChanceSlope", 0.0042) : 0.0042) + nb * 0.004;
    p = clamp99(p, 0.06, 0.94);
    return {
      win: Math.round(p * 100),
      seed: Math.round(seed),
      oppRel: Math.round(rel),
      playoff: !!wk.playoff,
      statBoost: nb
    };
  };
  /* ===== v76 MARGIN CURVE (5/5) — the simmed week and the watched week agree =====
   * `ia` generated the two scores as two INDEPENDENT draws: mine off my own player
   * rating, theirs off an opponent-strength shift, with +/-9 of noise on each. It
   * therefore had no idea what the scoreboard gap was, and measured across a 60-point
   * sweep of it the margin only moved 0.23 points per point of gap — against 3.34 for
   * the live engine. The same fixture was a rout if you watched it and a coin-flip if
   * you simmed it.
   *
   * Both paths now read the SAME pair of team ratings, built by the same arithmetic
   * Wr() uses for the live rosters, and both aim at the same curve. `ia` models the
   * MARGIN directly rather than two scores, which is the only way to put it on a
   * stated line; the total is then split around a level-appropriate scoring baseline.
   * The tail is a sum of two uniforms rather than one, so it is bell-shaped like the
   * live engine's rather than flat. ===== */
  function teamPairV76(e, opts) {
    opts = opts || {};
    const u = [18, 30, 42, 54, 66, 78, 86, 90][e.level] || 55;
    const prF = Math.min(
      1,
      (state.prestige || 0) / 15 +
        (state.rosterPrestigeV158 ? Object.values(state.rosterPrestigeV158).reduce((a2, b2) => a2 + b2, 0) : 0) / 50 +
        ((state.tree ? Object.values(state.tree).reduce((a2, b2) => a2 + b2, 0) : 0) / 70) *
          clamp99(TU("teamQualK", 0.1), 0, 1)
    );
    // the same seed the live engine builds its roster from, so the two agree
    let _sd = opts.seed;
    if (_sd == null) {
      try {
        _sd = __seasonSeed(e);
      } catch (_e) {
        _sd = 55;
      }
    }
    const t = clamp99((Number(_sd) || 55) / 100, 0.05, 1);
    let oppMul = 1.04;
    try {
      const wk = opts.wk;
      if (wk && wk.opp && typeof __oppMulForV22 === "function") oppMul = __oppMulForV22(wk.opp);
    } catch (_e) {}
    const C = clamp99(
        0.42 + t * 0.35 + prF * 1.05 + (opts.clubQ != null ? opts.clubQ : clubQV146B(e)) /* v146 B */,
        0.3,
        1.65
      ),
      V = clamp99((oppMul - 0.72) * 2, 0.36, 1.16),
      ovr = Q => clamp99(Math.round(u * (0.72 + Q * 0.5)), 3, 999);
    // a playoff/seeding shift arrives as oppBoost — (theirRating - levelAvg)/180 —
    // so it is folded in as an OVR move on their side rather than a separate axis
    return { us: ovr(C), opp: clamp99(Math.round(ovr(V) + (Number(opts.oppBoost) || 0) * 40), 3, 999) };
  }
  window.__TEAMPAIR_V76 = (e, opts) => teamPairV76(e || state.player, opts);
  /* ===== v68 TEAM QUALITY IS A NUDGE, NOT A CHEAT CODE =====
   * Every team-quality node fed one raw sum here, capped at .34 — against a base of
   * .38 + .04/level and an opponent sitting at ~.48. Stacked out that was nearly a
   * DOUBLING of your own scoring rate, so a prestiged save won on the strength of the
   * tree rather than on the player, which is the one thing the career is about. The
   * whole prestige contribution now runs through TU("teamQualK") at a tenth of its
   * old weight: still a real, stacking edge, no longer the game.
   * The bornLeader TRAIT is untouched — it is not bought, and it was never the
   * problem. The node descriptions below were restated to the numbers this actually
   * pays. ===== */
  function ia(e, t, a) {
    a = a || {};
    const s = e.level;
    playerOvr(e);
    const pair = teamPairV76(e, a),
      gap = pair.us - pair.opp;
    // the stated curve, the same one the live engine is held to
    let margin = TU("marginPerOvr", 0.7) * gap;
    // the prestige team-quality nodes still buy an edge, on the same points scale
    margin +=
      Math.min(
        0.34,
        nodeLvl("goodProgram") * 0.05 * Math.min(1, (s + 1) / 4) +
          nodeLvl("dynastyTeam") * 0.08 +
          treeFx("teamQual") +
          gearV147("teamQual")
      ) *
        TU("teamQualK", 0.1) *
        40 +
      (hasTrait(e, "bornLeader") ? 2.4 : 0);
    // and YOUR night still tilts it — a big game is worth a score, not a rout
    margin += clamp99(((t - 60) / 100) * TU("perfMarginK", 9), -7, 9);
    margin += (Yo() + seasonModFx("oppShift")) * -26; // seeding / schedule shifts
    // bell-shaped, not flat: two uniforms so the tail thins out the way the live
    // engine's does instead of stopping dead at a rectangle edge
    const sd = TU("marginSd", 9.4);
    margin += (randRange(-1, 1) + randRange(-1, 1)) * sd;
    // total points: a level-appropriate baseline per team, wobbled a little
    const base = clamp99(TU("ptsBase", 12.5) + s * TU("ptsPerLevel", 1.15) + randRange(-3.5, 4.5), 6, 34);
    let y = Math.max(0, Math.round(base + margin / 2)),
      C = Math.max(0, Math.round(base - margin / 2));
    return (y === C && (Math.random() < 0.5 ? (y += 3) : (C += 3)), { us: y, them: C, won: y > C });
  } // debug surface for scripts/teamqualcheck.mjs: the score generator itself, so the
  // check measures the real thing rather than a re-derivation of the formula
  try {
    window.__TEAMQUAL_V68 = { score: (pl, perf, opt) => ia(pl, perf, opt) };
  } catch (e) {}
  /* v128: one fixture is the rivalry. It is played against a boosted opponent, its injury roll is
   * doubled, and the row carries the flag so the schedule, the pregame and the growth roll can all
   * read it. With no rivalry stage this season nothing below changes. */
  function buildSeasonSchedule(e) {
    const t = LEVELS[e.level],
      a = PROGRAMS[e.training || "balanced"];
    let s = 0;
    const n = e.eventChoice;
    n && n.perf && (s += n.perf);
    const riv = n && n.rivalV128 ? rivalWeekV128(t.games) : -1,
      vm = (n && n.varMult) || 1,
      i = [];
    for (let r = 0; r < t.games; r++) {
      const isR = r === riv,
        l = __aiSeasonGame(e, { perfSeed: s, oppBoost: isR ? TU("rivalOppBoost", 0.38) : 0 }),
        d = (1 - nodeLvl("medic") * 0.05 - nodeLvl("unstoppable") * 0.15) * pathVal("injuryMult", 1),
        c = 1 - nodeLvl("motor") * 0.015;
      if (vm !== 1) l.perf = clamp99(Math.round(50 + (l.perf - 50) * vm), 1, 100);
      l.injured &&
        (l.injured = Math.random() < injPlanMultV54(a) * Math.max(0.1, d) * c * (isR ? RIVAL_MULT_V128() : 1));
      ((l.opp = randOppName(e.level)),
        (l.played = !1),
        isR && ((l.rivalV128 = !0), (l.importance = "rivalry")),
        i.push(l));
    }
    return i;
  }
  function playerPower(e) {
    return !e || !e.pos ? 0 : playerOvr(e) + gearFx("power");
  }
  function gearPowerBonus(e) {
    return Math.max(0, Math.round(gearFx("power")));
  }
  const ERA_NAMES = [
    "ROOKIE ERA",
    "THE ASCENSION",
    "THE TAKEOVER",
    "THE DYNASTY",
    "THE EMPIRE",
    "THE MYTH",
    "THE LEGEND",
    "THE IMMORTAL",
    "THE ETERNAL",
    "THE INFINITE"
  ];
  function eraName() {
    const e = state.era || 0;
    return e < ERA_NAMES.length ? ERA_NAMES[e] : "ERA " + (e + 1) + " — BEYOND";
  }
  function eraMult() {
    return Math.pow(1.2, state.era || 0);
  }
  function nextEraChaos() {
    return (state.era || 0) * 15;
  }
  function tryNextEra() {
    return chaosTotal() >= nextEraChaos() && (state.era || 0) < 999 ? ((state.era = (state.era || 0) + 1), !0) : !1;
  }
  const RARITIES = [
      { key: "common", name: "Common", color: "#9aa6b5", mult: 1, w: 52 },
      { key: "rare", name: "Rare", color: "#5aa0dc", mult: 1.7, w: 30 },
      { key: "epic", name: "Epic", color: "#a05ac9", mult: 2.8, w: 13 },
      { key: "legendary", name: "Legendary", color: "#f0bb45", mult: 4.5, w: 4.2 },
      { key: "mythic", name: "Mythic", color: "#ff5a5a", mult: 7.5, w: 0.8 }
    ],
    GEAR_SLOTS = [
      { key: "cleats", icon: "👟", name: "Cleats" },
      { key: "gloves", icon: "🧤", name: "Gloves" },
      { key: "chain", icon: "📿", name: "Chain" }
    ],
    GEAR_EFFECTS = [
      { key: "power", name: "Power", fmt: e => "+" + Math.round(e) + " Power", base: 1.2 },
      { key: "perfFlat", name: "Conditioning", fmt: e => "+" + Math.round(e) + " to ALL stats every game", base: 0.8 },
      { key: "ppMult", name: "Prestige", fmt: e => "+" + Math.round(e * 100) + "% PP", base: 0.05 },
      { key: "growth", name: "Growth", fmt: e => "+" + Math.round(e * 100) + "% growth", base: 0.03 },
      { key: "injDown", name: "Protection", fmt: e => "−" + Math.round(e * 100) + "% injuries", base: 0.05 },
      { key: "startAll", name: "Talent", fmt: e => "+" + Math.round(e) + " all starting attrs", base: 1.5 },
      { key: "pointsFlat", name: "Coaching", fmt: e => "+" + Math.round(e) + " points/season", base: 0.8 }
    ],
    GEAR_ADJECTIVES = {
      common: ["Worn", "Standard", "Team-Issue"],
      rare: ["Polished", "Custom", "Signature"],
      epic: ["Phantom", "Vapor", "Stealth"],
      legendary: ["Golden", "Hall-of-Fame", "Prime"],
      mythic: ["GOAT", "Celestial", "Forbidden"]
    };
  function rollGear(e) {
    const t = Math.min(30, chaosTotal() * 0.25),
      a = RARITIES.map((u, h) => {
        let p = u.w;
        return (h >= 2 && (p += t * (h - 1)), h < (e || 0) && (p = 0), p);
      }),
      s = a.reduce((u, h) => u + h, 0);
    let n = Math.random() * s,
      i = 0;
    for (let u = 0; u < a.length; u++)
      if (((n -= a[u]), n <= 0)) {
        i = u;
        break;
      }
    const r = RARITIES[i],
      l = randPick(GEAR_SLOTS),
      d = randPick(GEAR_EFFECTS),
      c = +(d.base * r.mult * (0.85 + Math.random() * 0.3)).toFixed(2);
    return gearRollV147(
      {
        id: Date.now() + "_" + randInt(100, 999),
        slot: l.key,
        rarity: r.key,
        name: randPick(GEAR_ADJECTIVES[r.key]) + " " + l.name,
        eff: d.key,
        val: c,
        icon: l.icon
      },
      state && state.player ? state.player.level | 0 : 0
    ); /* v147 C: the roll is made once, here */
  }
  function gearFx(e) {
    if (!state || !state.equipped) return 0;
    let t = 0;
    return (
      GEAR_SLOTS.forEach(a => {
        const s = state.equipped[a.key];
        s && s.eff === e && (t += s.val);
      }),
      t
    );
  }
  function dropGear(e, t) {
    state.inventory || (state.inventory = []);
    const a = rollGear(e);
    (state.inventory.push(a),
      state.inventory.length > 40 && state.inventory.sort((n, i) => rarityIndex(i) - rarityIndex(n)).splice(40));
    const s = RARITIES.find(n => n.key === a.rarity);
    return (
      typeof document < "u" &&
        byId("toast") &&
        showToast(
          `🎁 ${t || "Drop"}: <b style="color:${s.color}">${a.name}</b> — ${GEAR_EFFECTS.find(n => n.key === a.eff).fmt(a.val)}${(a.mods || []).length ? " · " + a.mods.map(m => gearTxtV147(m.k, m.v)).join(" · ") : ""}`
        ),
      a
    );
  }
  function rarityIndex(e) {
    return RARITIES.findIndex(t => t.key === e.rarity);
  }
  function equipGear(e) {
    const t = (state.inventory || []).find(a => a.id === e);
    t && (state.equipped || (state.equipped = {}), (state.equipped[t.slot] = t), saveGame(), screenLocker());
  }
  function scrapGear(e) {
    const t = (state.inventory || []).findIndex(i => i.id === e);
    if (t < 0) return;
    const a = state.inventory[t];
    if (state.equipped && state.equipped[a.slot] && state.equipped[a.slot].id === e) {
      showToast("Unequip it first.");
      return;
    }
    const n = [1, 3, 8, 20, 50][rarityIndex(a)];
    (state.inventory.splice(t, 1),
      bankPPV136(n, "scrap"),
      showToast("♻️ Scrapped for +" + n + " PP" + (bankingV136() ? " (banked)" : "")),
      saveGame(),
      screenLocker());
  }
  const SEASON_MODS = [
    {
      key: "passRen",
      icon: "🎯",
      name: "Passing Renaissance",
      desc: "League opens up: +12% passing & receiving production",
      statPos: ["QB", "WR", "TE"],
      statProd: 0.12
    },
    {
      key: "ground",
      icon: "🐗",
      name: "Ground & Pound Year",
      desc: "Smashmouth football: +12% rushing production",
      statPos: ["RB", "QB"],
      statProd: 0.12
    },
    {
      key: "defense",
      icon: "🛡️",
      name: "Year of the Defense",
      desc: "+12% defensive production, offense grinds",
      statPos: ["DL", "LB", "CB", "S"],
      statProd: 0.12
    },
    {
      key: "flu",
      icon: "🤒",
      name: "Injury-Plagued Season",
      desc: "Injuries +30%, but PP earned +20%",
      injMult: 1.3,
      ppMult: 0.2
    },
    {
      key: "primetime",
      icon: "🌙",
      name: "Prime Time Slate",
      desc: "Every eye on you: +4 playoff grade, +15% PP",
      playoffPerf: 4,
      ppMult: 0.15
    },
    {
      key: "weather",
      icon: "🌪️",
      name: "Weather Chaos",
      desc: "Wild swings — anything can happen any week",
      varMult: 1.5
    },
    {
      key: "drought",
      icon: "🏜️",
      name: "Talent Drought",
      desc: "Weak class — competition is softer this year",
      peerShift: -3
    },
    {
      key: "golden",
      icon: "🌟",
      name: "Golden Generation",
      desc: "Loaded league: tougher peers, +25% PP",
      peerShift: 4,
      ppMult: 0.25
    },
    {
      key: "cupcake",
      icon: "🧁",
      name: "Cupcake Schedule",
      desc: "Soft opponents — easier wins all season",
      oppShift: -0.07
    },
    {
      key: "gauntlet",
      icon: "⚔️",
      name: "The Gauntlet",
      desc: "Brutal schedule: tougher teams, +3 pts/season",
      oppShift: 0.07,
      pointsFlat: 3
    },
    {
      key: "scoutFair",
      icon: "🔭",
      name: "Scouting Combine Fair",
      desc: "Scouts everywhere: +4% advance chance",
      advFlat: 4
    },
    {
      key: "mediaCirc",
      icon: "📺",
      name: "Media Circus",
      desc: "Cameras love drama: +20% PP, wilder games",
      ppMult: 0.2,
      varMult: 1.25
    }
  ];
  function rollSeasonMod(e) {
    e.seasonMod = Math.random() < 0.75 ? randPick(SEASON_MODS).key : null;
  }
  function seasonModFx(e) {
    const t = state && state.player;
    if (!t || !t.seasonMod) return 0;
    const a = SEASON_MODS.find(s => s.key === t.seasonMod);
    return (a && a[e]) || 0;
  }
  function seasonModStatProd(e) {
    const t = state && state.player;
    if (!t || !t.seasonMod) return 0;
    const a = SEASON_MODS.find(s => s.key === t.seasonMod);
    return !a || !a.statProd ? 0 : !a.statPos || a.statPos.includes(e) ? a.statProd : 0;
  }
  function seasonModBanner(e) {
    if (!e.seasonMod) return "";
    const t = SEASON_MODS.find(a => a.key === e.seasonMod);
    return t ? `<div class="mod-banner">${t.icon} <b>${t.name}</b> — ${t.desc}</div>` : "";
  }
  function genContracts(e) {
    const t = LEVELS[e.level],
      a = POS_STATS[e.pos],
      s = a.stats.find(d => d.key === a.primary),
      n = Math.round(s.per[e.level] * (s.rate ? 1 : t.games) * 1.25),
      i = 0.7 + e.level * 0.45,
      r = [
        { id: "grade", desc: "Post a season grade of 62+", pp: Math.round(3 * i), check: d => d.avg >= 62 },
        {
          id: "primStat",
          desc: `${s.lowerBetter ? "Hold " + s.name + " under" : "Rack up " + n + "+ " + s.name} ${s.lowerBetter ? n : ""}`.trim(),
          pp: Math.round(4 * i),
          check: d => (s.lowerBetter ? d.statLine[s.key] <= n : d.statLine[s.key] >= n)
        },
        {
          id: "wins",
          desc: `Win ${Math.ceil(t.games * 0.7)}+ games`,
          pp: Math.round(3 * i),
          check: d => d.teamWins >= Math.ceil(t.games * 0.7)
        },
        {
          id: "standout",
          desc: "3+ standout games (72+ grade)",
          pp: Math.round(3 * i),
          check: d => d.standoutGames >= 3
        },
        { id: "healthy", desc: "Stay healthy — zero injuries", pp: Math.round(2 * i), check: d => d.injuries === 0 },
        {
          id: "playoff",
          desc: "Win a playoff game",
          pp: Math.round(4 * i),
          check: d => d.playoffs && d.playoffs.roundsWon >= 1
        },
        {
          id: "ring",
          desc: "WIN IT ALL — take the championship",
          pp: Math.round(7 * i),
          check: d => d.playoffs && d.playoffs.champion
        },
        { id: "heater", desc: "Drop one monster game (84+ grade)", pp: Math.round(4 * i), check: d => d.best >= 84 },
        {
          id: "nemesis",
          desc: "Finish ahead of your nemesis nationally",
          pp: Math.round(4 * i),
          check: d => d.beatNemesis
        }
      ],
      l = [];
    for (; l.length < 3 && r.length;) {
      const d = randInt(0, r.length - 1);
      l.push(r.splice(d, 1)[0]);
    }
    ((e.contracts = l.map(d => ({ id: d.id, desc: d.desc, pp: d.pp }))), (e.contractsSeason = e.totalSeasons));
  }
  function Xr(e, t) {
    if ((!e.contracts || (e.contractsSeason, e.totalSeasons - 0), !e.contracts)) return [];
    const a = LEVELS[e.level],
      s = POS_STATS[e.pos],
      n = s.stats.find(l => l.key === s.primary),
      i = Math.round(n.per[e.level] * (n.rate ? 1 : a.games) * 1.25),
      r = e.contracts.map(l => {
        let d = !1;
        try {
          switch (l.id) {
            case "grade":
              d = t.avg >= 62;
              break;
            case "primStat":
              d = n.lowerBetter ? t.statLine[n.key] <= i : t.statLine[n.key] >= i;
              break;
            case "wins":
              d = t.teamWins >= Math.ceil(a.games * 0.7);
              break;
            case "standout":
              d = t.standoutGames >= 3;
              break;
            case "healthy":
              d = t.injuries === 0;
              break;
            case "playoff":
              d = t.playoffs && t.playoffs.roundsWon >= 1;
              break;
            case "ring":
              d = t.playoffs && t.playoffs.champion;
              break;
            case "heater":
              d = t.best >= 84;
              break;
            case "nemesis":
              d = !!t.beatNemesis;
              break;
          }
        } catch {}
        const c = d ? Math.round(l.pp * chaosPPMult() * eraMult()) : 0;
        return (
          d && (bankPPV136(c, "objective"), (state.objectivesCompleted = (state.objectivesCompleted || 0) + 1)),
          { ...l, ok: d, paid: c }
        );
      });
    return ((e.contracts = null), r);
  }
  const NEMESIS_TAUNTS = [
    "says you peaked in Pee Wee.",
    "called you 'a practice-squad body' on a podcast.",
    "posted your missed tackle. Again.",
    "says he'd start over you on any roster in America.",
    "liked a post ranking you outside the top 100.",
    "told scouts you're 'all hype, no film.'"
  ];
  function rollNemesis(e) {
    e.nemesis = { name: randPick(NEMESIS_FIRST) + " " + randPick(NEMESIS_LAST), pos: e.pos, beaten: 0 };
  }
  function nemesisBanner(e) {
    return e.nemesis ? `<div class="nem-banner">😈 <b>${e.nemesis.name}</b> ${randPick(NEMESIS_TAUNTS)}</div>` : "";
  }
  function hofScore(e) {
    return Math.round(
      e.peak * 2 + e.power * 0.5 + e.titles * 15 + e.reached * 12 + e.seasons * 1.5 + (e.rings || 0) * 25
    );
  } /* ===== v134 THE BUSTS HAVE A BOX SCORE =====
   * A bust in the Hall was a name, a position and five numbers: peak OVR, titles, seasons, GOAT score.
   * The career itself -- every season's line, the awards, where he ranked, what the sheet looked like at
   * the end -- was thrown away the moment he was enshrined, even though the player carried all of it
   * (`seasonLogV77`, `careerTotalsV77`, the awards on every season row). It is kept now: `hofSnapV134`
   * writes the totals, the per-season log, the final sheet, the origin and the awards onto the bust,
   * and the Hall's rows open into a full card (`hofOpenV134`). A bust enshrined before this says so
   * instead of pretending. `window.__HOF_V134`. */
  function hofSnapV134(e, t) {
    try {
      const T = typeof careerTotalsV77 == "function" ? careerTotalsV77(e) : null,
        log = (e.seasonLogV77 || []).slice(-SEASON_LOG_MAX);
      const top = ATTR_KEYS.slice()
        .sort((a, b) => (e.attrs[b] || 0) - (e.attrs[a] || 0))
        .slice(0, 6)
        .map(k => ({
          k,
          name: (ATTR_INFO[k] && ATTR_INFO[k].name) || k,
          icon: (ATTR_INFO[k] && ATTR_INFO[k].icon) || "",
          v: Math.round(e.attrs[k] || 0)
        }));
      const awards = {};
      log.forEach(r =>
        (r.awards || []).forEach(a => {
          awards[a.name] = (awards[a.name] || 0) + 1;
        })
      );
      const orig = typeof getOrigin == "function" ? getOrigin(e) : null;
      return {
        v: 134,
        totals: T
          ? {
              games: T.games,
              wins: T.wins,
              losses: T.losses,
              titles: T.titles,
              playoffWins: T.playoffWins,
              awards: T.awards,
              bestAvg: Math.round(T.bestAvg),
              peakOvr: Math.round(T.peakOvr),
              lines: T.lines.map(l => ({
                key: l.key,
                name: l.name,
                rate: l.rate,
                total: l.rate ? +(+l.total).toFixed(1) : Math.round(l.total),
                per: +(+l.per).toFixed(1)
              }))
            }
          : null,
        log: log.map(r => ({
          n: r.n,
          level: r.level,
          levelName: r.levelName,
          age: r.age,
          ovr: r.ovr,
          avg: Math.round(r.avg || 0),
          grade: r.grade,
          wins: r.wins,
          losses: r.losses,
          champion: !!r.champion,
          roundsWon: r.roundsWon || 0,
          line: r.statLine || {},
          awards: (r.awards || []).map(a => a.icon + " " + a.name)
        })),
        attrs: top,
        awards: Object.entries(awards)
          .sort((a, b) => b[1] - a[1])
          .map(([n, c]) => ({ n, c })),
        origin: orig ? { name: orig.name, icon: orig.icon } : null,
        team: typeof teamName == "function" ? teamName(e) : "",
        age: e.age,
        traits: (e.traits || []).map(k => (TRAITS[k] ? TRAITS[k].icon + " " + TRAITS[k].name : k)),
        levelsReached: (e.career || []).map(c => ({ level: c.level, ovr: c.ovr, age: c.age })),
        endOvr: playerOvr(e),
        stars: e.stars || 0
      };
    } catch (_) {
      return null;
    }
  }
  function enshrineHof(e, t, a) {
    state.hof || (state.hof = []);
    const s = {
      name: e.name,
      pos: e.pos,
      peak: e.peakOvr || playerOvr(e),
      power: Math.round(playerPower(e)),
      titles: e.titles || 0,
      rings: e.nflRings || 0,
      reached: t,
      seasons: e.totalSeasons,
      won: !!a,
      era: state.era || 0,
      when: state.careersCompleted || 0,
      box: hofSnapV134(e, t),
      gen: Math.max(1, lineageV136().gen || 1)
    };
    ((s.goat = hofScore(s)),
      state.hof.push(s),
      state.hof.sort((r, l) => l.goat - r.goat),
      state.hof.length > 60 && (state.hof.length = 60));
    const n = state.careersCompleted || 0,
      i = [5, 10, 25, 50].filter(r => n >= r).length;
    (i > (state.hofWings || 0) &&
      ((state.hofWings = i),
      typeof document < "u" && byId("toast") && showToast("🏛️ Museum wing opened — permanent +5% PP!")),
      state.posMastery || (state.posMastery = {}),
      state.posMastery[e.pos] || (state.posMastery[e.pos] = {}),
      t >= 7 && (state.posMastery[e.pos].nfl = !0),
      e.nflRings > 0 && (state.posMastery[e.pos].ring = !0));
  }
  function hofWings() {
    return (state && state.hofWings) || 0;
  }
  function posMasteryCount(e) {
    return !state || !state.posMastery ? 0 : Object.values(state.posMastery).filter(t => t[e]).length;
  }
  function al(e) {
    return Math.round(
      (6 + (e.titles || 0) * 2 + (e.nflSeasons || 0)) * chaosPPMult() * eraMult() * (1 + hofWings() * 0.05)
    );
  }
  function playoffRoundNames(e) {
    return e === 6
      ? []
      : e >= 8
        ? ["Orbital Wild Card", "Lunar Semifinal", "Galactic Conference Final", "THE INTERSTELLAR CHAMPIONSHIP"]
        : e === 7
          ? ["Wild Card", "Conference Final", "LEAGUE CHAMPIONSHIP"]
          : e >= 5
            ? ["Playoff Semifinal", "NATIONAL CHAMPIONSHIP"]
            : e >= 2
              ? ["Semifinal", "CHAMPIONSHIP"]
              : ["CHAMPIONSHIP"];
  }
  function madePlayoffs(e, t) {
    return t > 0 && e / t >= 0.6;
  }
  function cn(e, t) {
    const a = PROGRAMS[e.training || "balanced"];
    let s = 0;
    const n = e.eventChoice;
    n && n.perf && (s += n.perf);
    const d = playoffRoundNames(e.level),
      c = t === d.length - 1,
      i = __aiSeasonGame(e, { perfSeed: s, playoff: !0, oppBoost: 0.12 + t * 0.11 + (c && e.nemesis ? 0.04 : 0) }),
      r = (1 - nodeLvl("medic") * 0.05 - nodeLvl("unstoppable") * 0.15) * pathVal("injuryMult", 1),
      l = 1 - nodeLvl("motor") * 0.015;
    i.injured && (i.injured = Math.random() < injPlanMultV54(a) * Math.max(0.1, r) * l);
    return (
      (i.opp = c && e.nemesis ? e.nemesis.name + "'s " + randOppName(e.level) : randOppName(e.level)),
      (i.playoff = !0),
      (i.round = playoffRoundNames(e.level)[t]),
      (i.roundIdx = t),
      (i.played = !1),
      i
    );
  }
  function ensurePlayoffs(e) {
    const t = e.weekResults;
    if (!t) return;
    const a = t.filter(l => !l.playoff);
    if (a.some(l => !l.played)) return;
    const s = playoffRoundNames(e.level);
    if (!e.playoffState) {
      const l = a.filter(c => c.won).length,
        d = s.length > 0 && madePlayoffs(l, a.length);
      e.playoffState = { qualified: d, round: 0, alive: d, done: !d, champion: !1 };
    }
    const n = e.playoffState;
    if (n.done) return;
    const i = t.filter(l => l.playoff),
      r = i[i.length - 1];
    if (!(r && !r.played)) {
      if (r && r.played)
        if (r.won) {
          if ((n.round++, n.round >= s.length)) {
            ((n.champion = !0), (n.done = !0));
            return;
          }
        } else {
          ((n.alive = !1), (n.done = !0));
          return;
        }
      t.push(cn(e, n.round));
    }
  }
  function startSeasonGames() {
    const e = state.player;
    ((e.seasonSeed = randInt(1, 2e9)),
      (e.playoffState = null),
      rollSeasonMod(e),
      genContracts(e),
      (e.weekResults = buildSeasonSchedule(e)),
      (e.currentWeek = 0),
      goView("season"));
  }
  function screenSeason() {
    const e = state.player,
      t = LEVELS[e.level];
    ensurePlayoffs(e);
    const a = e.weekResults || [],
      s = a.filter(y => !y.playoff),
      n = s.filter(y => y.played && y.won).length,
      i = s.every(y => y.played),
      r = a.filter(y => y.played).length,
      l = e.playoffState,
      d = r >= a.length && (!i || (l && l.done)),
      c = a.filter(y => y.played && y.won).length,
      u = a.filter(y => y.played && !y.won).length,
      h = Math.ceil(s.length * 0.6);
    let p = "";
    if (i && l)
      l.champion
        ? (p = `<div class="card tight" style="border-color:var(--gold);background:linear-gradient(180deg,rgba(240,187,69,.18),rgba(15,21,32,.9))"><div class="center" style="font-family:'Oswald';font-weight:700;font-size:17px;color:var(--gold)">🏆 ${teamName(e).toUpperCase()} — CHAMPIONS!</div></div>`)
        : l.qualified
          ? l.alive
            ? (p =
                '<div class="card tight" style="border-color:var(--gold)"><div class="small center" style="color:var(--gold)">🔥 <b>PLAYOFFS!</b> Win or go home — every round gets tougher.</div></div>')
            : (p =
                '<div class="card tight" style="border-color:var(--blood)"><div class="small center" style="color:#e08a8a">💔 Knocked out of the playoffs. Great season, brutal ending.</div></div>')
          : (p = `<div class="card tight"><div class="small center">Season over — <b>${n}-${s.length - n}</b> wasn't enough for the playoffs (needed ${h} wins).</div></div>`);
    else if (!i && s.length) {
      const y = s.filter(V => !V.played).length;
      p =
        n + y >= h
          ? `<div class="threshold-note" style="margin:6px 0 0">🎯 Win <b style="color:var(--gold)">${Math.max(0, h - n)}</b> more of your last <b>${y}</b> to make the <b>playoffs</b> — titles pay big.</div>`
          : '<div class="threshold-note" style="margin:6px 0 0">Playoffs are out of reach this year — finish strong for development.</div>';
    }
    let m = 0;
    if (
      ((byId("screen").innerHTML = `
    <div class="eyebrow">${t.name} · ${escHtml(teamName(e))} · ${e.training ? PROGRAMS[e.training].name : "Season"}</div>
    <div class="h1">${combineYearV139(e) ? "The Combine" : "Season Schedule"}</div>
    ${combineYearV139(e) ? combineBoardV139(e) : ""}
    ${seasonModBanner(e)}
    <div class="sub">${d ? "All games played — finish the season to see your results." : `Record: <b style="color:var(--good)">${c}-${u}</b>`}${p && p.startsWith('<div class="threshold') ? p : ""}</div>
    ${p && !p.startsWith('<div class="threshold') ? p : ""}
    <div class="sched-list mt" style="margin-top:14px">
      ${a
        .map((y, C) => {
          const V = !y.played && C === a.findIndex(j => !j.played),
            P = y.played ? (y.won ? "sched-win" : "sched-loss") : V ? "sched-next" : "sched-upcoming",
            v = y.playoff
              ? '<span class="sched-wk" style="color:var(--gold)">🏆</span>'
              : `<span class="sched-wk">WK ${++m}</span>`;
          return `<div class="sched-row ${P}" ${y.playoff ? 'style="border-color:rgba(240,187,69,.5)"' : ""}>
          ${v}
          <span class="sched-opp">${y.playoff ? `<b style="color:var(--gold)">${y.round}</b> · ` : ""}${homeWeekV93(y, C) ? "vs" : "@"} ${y.opp}</span>
          ${y.played ? `<span class="sched-score">${y.us}–${y.them} ${y.won ? "W" : "L"}</span><span class="sched-perf">${y.perf} rtg</span>` : V ? '<span class="sched-status" style="color:var(--gold)">▶ NEXT UP</span>' : '<span class="sched-status">—</span>'}
        </div>`;
        })
        .join("")}
    </div>
  `),
      d)
    )
      byId("dock").innerHTML = `
      <button class="btn" onclick="finishSeasonGames()">Finish Season & See Results ▸</button>
      <div style="height:8px"></div>
      <div class="btn-row">
        <button class="btn ghost" onclick="go('stats')">📊 Nat'l Leaders</button>
        <button class="btn ghost" onclick="STATS_TAB='standings';go('stats')">🏟 Standings</button>
      </div>`;
    else {
      const y = a.find(P => !P.played),
        C = y && y.playoff ? y.round : "Week " + (s.findIndex(P => !P.played) + 1),
        V = a.filter(P => !P.played).length;
      byId("dock").innerHTML = `
      <button class="btn" onclick="playWeek(true)">▶ Play ${C} Live</button>
      <div style="height:8px"></div>
      <button class="btn secondary" onclick="playWeek(false)">⏩ Quick Play ${C}</button>
      ${V > 1 && !(y && y.playoff) ? '<div style="height:8px"></div><button class="btn ghost" onclick="simRemainingWeeks()">⏭ Sim Remaining Regular Season</button>' : ""}
      <div style="height:8px"></div>
      <div class="btn-row">
        <button class="btn ghost" onclick="go('stats')">📊 Leaders</button>
        <button class="btn ghost" onclick="STATS_TAB='standings';go('stats')">🏟 Standings</button>
      </div>
    `;
    }
  } /* ===== v18 INJURY LAYER — injuries actually cost games =====
   * Severity is rolled ONCE when a game injury happens: most are nagging knocks,
   * a real slice cost 1-3 weeks, a few cost 4-6, and season-enders exist but are
   * very rare (≈0.4% of injuries; up to ≈2% when you play worn down).
   * While conditionV11.injury.weeksRemaining > 0 the week auto-sims WITHOUT you:
   * no stats, no snaps, DNP grade — and the whistle only comes back when you heal. */
  const INJ18 = [
    ["Bruised ribs", 1, 1, 0.1],
    ["Ankle sprain", 1, 1, 0.18],
    ["Hamstring pull", 1, 1, 0.22],
    ["Turf toe", 1, 1, 0.2],
    ["Concussion", 2, 1, 0.15],
    ["Shoulder sprain", 2, 2, 0.24],
    ["High ankle sprain", 2, 2, 0.26],
    ["Broken hand", 2, 3, 0.2],
    ["MCL sprain", 2, 3, 0.3],
    ["Torn labrum", 3, 5, 0.34],
    ["Broken collarbone", 3, 4, 0.28],
    ["ACL tear", 3, 99, 0.42],
    ["Achilles rupture", 3, 99, 0.4]
  ];
  function cv18(e) {
    return (
      e.conditionV11 ||
      (e.conditionV11 = {
        fatigue: 14,
        susceptibility: 18,
        mentalLoad: 18,
        recovery: 55,
        injury: null,
        consecutiveGames: 0,
        lastUpdatedWeek: -1
      })
    );
  }
  function isWornV18(e) {
    const c = e && e.conditionV11;
    return c ? (c.fatigue || 0) >= 70 || !!c.injury : !1;
  }
  window.__isWornV18 = isWornV18;
  /* v54: the severity split, calibrated to the target the design asks for — an
   * average season misses one or two games, a lost season is rare.
   *   knock 45%  play on, -10% this game only     1 game 33%
   *   2 games 15%          3-5 games 6.6%         season-ending 0.4%
   * Expected games missed per injury ~0.9; at the tuned per-game chance that lands
   * around 1.2 games a season, and a season-ender in roughly 1 career year in 70.
   * It never returns null now: the old version dropped 65% of injuries on the floor,
   * which is the other half of why nothing ever came of them. */
  function rollInjuryV18(e) {
    try {
      window.__INJ_DEBUG && window.__INJ_DEBUG.roll++;
    } catch (_) {}
    const c = cv18(e),
      fat = c.fatigue || 0;
    let r = Math.random();
    if (fat >= 70) r = 1 - (1 - r) * 0.82; // worn down: the roll skews WORSE, toward the severe end
    const mk = (p, wk, sev) => ({
      name: p[0],
      severity: sev != null ? sev : p[1],
      weeksRemaining: wk,
      recurrence: p[3]
    });
    const P = TU("injKnockP", 0.45),
      P1 = P + TU("inj1P", 0.33),
      P2 = P1 + TU("inj2P", 0.15),
      P3 = P2 + TU("inj35P", 0.066);
    if (r < P) {
      const j = mk(INJ18[Math.floor(Math.random() * 4)], 0, 1);
      j.knock = !0;
      return j;
    }
    if (r < P1) return mk(INJ18[Math.floor(Math.random() * 9)], 1, 2);
    if (r < P2) return mk(INJ18[4 + Math.floor(Math.random() * 5)], 2, 2);
    if (r < P3) return mk(INJ18[9 + Math.floor(Math.random() * 2)], 3 + Math.floor(Math.random() * 3), 3);
    const j = mk(INJ18[11 + Math.floor(Math.random() * 2)], 99, 4);
    j.seasonEnding = !0;
    return j;
  }
  function materializeInjuryV18(e, wk) {
    if (!wk || wk._injRolled18) return;
    wk._injRolled18 = !0;
    const c = cv18(e);
    // v54: a knock costs no games — it is a one-week wear penalty. Clear last week's
    // before rolling this one, or isWornV18 would keep -10% on the player all season.
    if (c.injury && c.injury.knock && (c.injury.weeksRemaining || 0) <= 0) c.injury = null;
    if (!wk.injured) return;
    if (c.injury) {
      c.injury.weeksRemaining = (c.injury.weeksRemaining || 0) + 1;
      return;
    }
    let inj = rollInjuryV18(e);
    if (window.__forceSeasonEnderV134) {
      inj = inj || { name: "Torn ACL", severity: 3, weeksRemaining: 99, recurrence: 0.2 };
      inj.seasonEnding = !0;
      inj.weeksRemaining = 99;
    } /* dev: v134check forces the worst case */
    /* v134 Apex: Trainer's Room -- once a career, the season-ender is three games out instead */
    if (inj && inj.seasonEnding && nodeLvl("trainerRoom") > 0 && !e._trainerRoomUsedV134) {
      inj.seasonEnding = !1;
      inj.weeksRemaining = 3;
      inj.severity = Math.min(inj.severity || 2, 2);
      e._trainerRoomUsedV134 = !0;
      wk.trainerRoomV134 = !0;
      try {
        toast("🏥 TRAINER'S ROOM — " + inj.name + " was a season-ender. He is back in three.");
      } catch (x) {}
    }
    /* v146 C: Miracle Hands -- a 2+ game injury is shorter, never under one game; season-enders untouched */
    if (inj && !inj.seasonEnding && inj.weeksRemaining > 1) inj.weeksRemaining = healWeeksV146(inj.weeksRemaining);
    if (inj) {
      c.injury = inj;
      wk.injName = inj.name;
      try {
        toast(
          inj.seasonEnding
            ? "🚑 " + inj.name + " — OUT FOR THE SEASON"
            : inj.weeksRemaining > 0
              ? "🩹 " + inj.name + " — out " + inj.weeksRemaining + " game" + (inj.weeksRemaining > 1 ? "s" : "")
              : "🩹 " + inj.name
        );
      } catch (x) {}
    }
  }
  function mustSitV18(e) {
    const c = e && e.conditionV11,
      j = c && c.injury;
    return !!(j && (j.weeksRemaining || 0) > 0 && ((j.severity || 1) >= 2 || j.resting || j.seasonEnding));
  }
  window.__mustSitV18 = mustSitV18;
  function sitOutWeekV18(e, s) {
    const c = cv18(e),
      inj = c.injury;
    s.played = !0;
    s.satOut = !0;
    s.statLine = null;
    s.perf = Math.max(1, Math.round((s.perf || 40) * 0.2));
    s.snaps = 0;
    s.gameGrade = "DNP";
    s.injName = s.injName || (inj && inj.name) || "Injured";
    if (s.won && Math.random() < 0.3) {
      const u = s.us;
      s.us = Math.max(0, (s.them || 0) - 3 - Math.floor(Math.random() * 8));
      s.them = Math.max(u, s.them || 0);
      s.won = s.us > s.them;
    }
    if (inj && !inj.seasonEnding) {
      inj.weeksRemaining = Math.max(0, (inj.weeksRemaining || 0) - 1);
      if (inj.weeksRemaining <= 0) {
        e.recentlyRecoveredV18 = inj.name;
        c.injury = null;
        try {
          toast("✅ Healed: " + inj.name + " — cleared to play");
        } catch (x) {}
      }
    }
    c.fatigue = Math.max(0, (c.fatigue || 0) - 22);
  }
  function homeWeekV93(w, i) {
    return w && w.home != null ? !!w.home : (i || 0) % 2 === 0;
  }
  window.homeWeekV93 = homeWeekV93;
  function playWeek(e) {
    const t = state.player,
      a = t.weekResults.findIndex(n => !n.played);
    if (a < 0) {
      goView("season");
      return;
    }
    t.currentWeek = a;
    const s = t.weekResults[a];
    if (e) {
      window.__oppMulV22 = __oppMulForV22(s.opp);
      let _sb = 0;
      if (t._nextGameBoost) {
        _sb = Math.round(t._nextGameBoost);
        t._nextGameBoost = 0;
      }
      window.__youStatBoostPctV20 = _sb;
      const _in146 = liveInV146(t, a);
      window.__youTempBuffsV25 = _in146 ? _in146.buffs : wearBuffsV111(t);
      window.__gameScriptBiasV23 = _in146
        ? _in146.gs
        : t._gameScriptV23 && t._gameScriptV23.gsPass != null
          ? t._gameScriptV23.gsPass
          : null;
      t._gameScriptV23 = null;
      try {
        state._liveGame = simGameV2(_in146 ? _in146.seed : s.perf, t.pos);
      } finally {
        window.__youStatBoostPctV20 = 0;
        window.__youTempBuffsV25 = null;
        decayWearV111(t);
        window.__oppMulV22 = null;
        window.__gameScriptBiasV23 = null;
      }
      state._oppName = s.opp;
      window.__homeGameV93 = homeWeekV93(s, a);
      state.view = "live";
      render();
    } else {
      s.played = !0;
      goView("season");
    }
  }
  function simRemainingWeeks() {
    const e = state.player;
    (e.weekResults
      .filter(t => !t.playoff)
      .forEach(t => {
        if (t.played) return;
        if (mustSitV18(e)) {
          sitOutWeekV18(e, t);
        } else {
          const c = cv18(e);
          t.played = !0;
          materializeInjuryV18(e, t);
          c.fatigue = Math.min(100, (c.fatigue || 0) + 5);
        }
      }),
      saveGame(),
      goView("season"));
  }
  function finishWeekGame() {
    const e = state.player;
    if (e.weekResults && e.currentWeek != null) {
      const w = e.weekResults[e.currentWeek];
      w.played = !0;
      const g = state._liveGame;
      g && g.usScore != null && g.plays && bookLiveGameV85(e, w, g);
    }
    ((state._oppName = null), goView("season"));
  }
  function finishSeasonGames() {
    const e = state.player;
    if ((ensurePlayoffs(e), e.weekResults && e.weekResults.some(a => !a.played))) {
      goView("season");
      return;
    }
    // v18: the offseason heals — season-enders clear, lingering knocks fade, fatigue resets
    {
      const c = cv18(e);
      if (c.injury && (c.injury.seasonEnding || Math.random() < 0.7)) c.injury = null;
      c.fatigue = Math.max(0, Math.min(30, (c.fatigue || 0) - 40));
    }
    const t = simSeason(e);
    ((e.seasonStats = t),
      logSeasonV77(e, t),
      (e.weekResults = null),
      (e.currentWeek = null),
      completeChallenges(),
      (state.view = "result"),
      saveGame(),
      render());
  }
  function nl() {
    startSeasonGames();
  }
  let liveCtl = null;
  function watchLive() {
    ((state.view = "live"), render());
  } /* v123: the opponent comes out of the SAME world the player's own side does — the 120 towns and
   * the 88 crest-matched mascots — instead of nine hard-coded lists of five, which between them
   * held Ducks, Dallas, Miami and Denver. Youth and high school are schools (the mascot, shouted,
   * the way a schedule board reads them), college is a college, and the UFF and the Interstellar
   * League are clubs out of the fifty. */
  function randOppName(e) {
    if (e === 6) return randPick(["COMBINE FIELD", "DRAFT PROSPECTS", "ALL-STARS", "THE NATIONAL TEAM"]);
    if (e >= 7) return DFL_V123[(Math.random() * DFL_V123.length) | 0].toUpperCase();
    if (e === 5) return randPick(TOWNS) + " " + randPick(COLLEGE_V123);
    return randPick(MASCOTS).toUpperCase();
  }
  function ol() {
    const e = state.player,
      t = state._liveGame;
    LEVELS[e.level];
    const a = state._oppName || (state._oppName = randOppName(e.level)),
      s = e.name.split(" ")[1].slice(0, 8).toUpperCase(),
      n = a.split(" ").pop().slice(0, 8).toUpperCase(),
      i = ["DL", "LB", "CB", "S"].includes(e.pos);
    const _pl = (e.weekResults || []).filter(w => w.played),
      _uw = _pl.filter(w => w.won).length,
      usRec = _pl.length ? _uw + "-" + (_pl.length - _uw) : t.roster.us.ovr + " OVR",
      _os = Array.from(a || "").reduce((h, c) => (h * 31 + c.charCodeAt(0)) >>> 0, 7),
      _ow = _pl.length ? _os % (_pl.length + 1) : 0,
      oppRec = _pl.length ? _ow + "-" + (_pl.length - _ow) : t.roster.opp.ovr + " OVR";
    const TL44 = window.TEAM_LOGOS_V44,
      _tc44 = window.__GRIDIRON_TEAM_CUSTOM__ || {},
      usLogo44 = _tc44.logo != null ? _tc44.logo : TL44 ? TL44.forName(_tc44.teamName || s) : 0,
      oppLogo44 = TL44 ? TL44.forName(a) : 0,
      homeGame44 = (e.currentWeek || 0) % 2 === 0;
    try {
      window.__setFieldLogoV44 && window.__setFieldLogoV44(homeGame44 ? usLogo44 : oppLogo44);
    } catch (_e) {}
    ((byId("screen").innerHTML = `
    <div class="field-wrap">${(() => {
      try {
        window.__ribPaintScorebugV98 && window.__ribPaintScorebugV98(n);
      } catch (_e) {}
      return "";
    })()}
      <div class="live-scoreboard">
        <div class="sb-side us">
          <div class="sb-logo${TL44 ? " emblem-v44" : ""}" data-mark="${TL44 ? "" : s[0]}" title="${TL44 ? TL44.name(usLogo44) : ""}" style="${TL44 ? TL44.css(usLogo44) : ""}"></div>
          <div class="sb-meta"><span class="team">${s}</span><span class="rec">${usRec}</span></div>
          <div class="sc" id="usScore">0</div>
        </div>
        <div class="clock">
          <div class="qline"><b id="qtr">1</b><span>QTR</span></div>
          <div id="gameClock" style="font-family:Oswald,sans-serif;font-weight:700;font-size:12px;letter-spacing:.6px;color:var(--gold)">--:--</div>
          <div id="downDist">1st & 10</div>
        </div>
        <div class="sb-side them">
          <div class="sc" id="themScore">0</div>
          <div class="sb-meta"><span class="team">${n}</span><span class="rec">${oppRec}</span></div>
          <div class="sb-logo${TL44 ? " emblem-v44" : ""}" data-mark="${TL44 ? "" : n[0]}" title="${TL44 ? TL44.name(oppLogo44) : ""}" style="${TL44 ? TL44.css(oppLogo44) : ""}"></div>
        </div>
      </div>
      <div class="sb-dots"><i></i><i class="on"></i><i></i></div>
      ${(() => {
        // v18 live strip: current season + every active buff/negative you carry into this game
        try {
          const lv = LEVELS[e.level] || {},
            yr = (e.seasonsAtLevel || 0) + 1,
            age = e.age ? " · AGE " + e.age : "";
          const wk = (e.weekResults || [])[e.currentWeek] || null;
          const fx = (window.pregameTempStats ? window.pregameTempStats(e, wk) : []).slice(0, 10);
          const chips = fx
            .map(sx => `<span class="lb-chip ${sx.good ? "g" : "b"}">${sx.l}: <b>${sx.v}</b></span>`)
            .join("");
          return `<div class="live-buffline"><span class="lb-season">🏆 ${(lv.name || "SEASON").toUpperCase()} · YR ${yr} · WK ${(e.currentWeek || 0) + 1}${age}</span>${chips || '<span class="lb-chip g">No active modifiers — clean slate</span>'}</div>`;
        } catch (_x) {
          return "";
        }
      })()}
      <div class="live-down">
        <span class="watch-badge">${e.pos}</span>
        <span class="watch-info" id="featuredLabel"><b>${escHtml(e.name)} (${e.pos})</b><small>${s} ${i ? "DEFENSE" : "OFFENSE"}</small></span>
        <span id="playClock">● LIVE</span>
      </div>
      <canvas id="field" width="360" height="230"></canvas>
      <div class="commentary" id="commentary"><span class="ev-ic">🏈</span><div class="ev-body">Kickoff! ${escHtml(e.name)} takes the field…</div><span class="chev">›</span></div>
    </div>
    <div class="speed-row">
      ${[
        ["0.5", "½×", "◀◀"],
        ["1", "1×", "▶ ❚❚"],
        ["2", "2×", "▶▶"],
        ["4", "4×", "▶▶▶"]
      ]
        .map(
          ([r, l, d]) =>
            `<button class="speed-btn ${r === "1" ? "active" : ""}" data-spd="${r}" onclick="setSpeed(${r})">${l}<small>${d}</small></button>`
        )
        .join("")}
      <button class="speed-btn" onclick="skipLive()">SKIP<small>⏭</small></button>
    </div>
    <div class="boxscore-head">
      <div class="h2">${escHtml(e.name)} — <span style="color:var(--chalk-dim)">Live Box Score</span></div>
      <a onclick="document.getElementById('fullBoxCard').scrollIntoView({behavior:'smooth'})">VIEW FULL STATS ›</a>
    </div>
    <div class="live-stats" id="liveStats"></div>
    <div class="card tight" style="margin-top:12px" id="fullBoxCard">
      <div class="stat-sec-label">${i ? "DEFENSIVE" : "OFFENSIVE"} STATS</div>
      <div id="fullBox" class="fullbox"></div>
    </div>
    <div class="card tight">
      <div class="stat-sec-label">TEAM STATS</div>
      <div id="teamBox" class="teambox"></div>
    </div>
    <div class="h2" style="margin-top:10px">Every Player · Live Box Scores</div>
    <div class="roster-tabs">
      <button class="rtab active" data-rt="us" onclick="setRosterTab('us')">🟢 ${s}</button>
      <button class="rtab" data-rt="opp" onclick="setRosterTab('opp')">🔴 ${a}</button>
    </div>
    <div class="card tight"><div id="rosterBox" class="rosterbox"></div></div>
  `),
      (byId("dock").innerHTML = ""),
      startLivePlayback());
  }
  let rosterSide = "us";
  function setRosterTab(e) {
    ((rosterSide = e),
      document.querySelectorAll(".rtab[data-rt]").forEach(t => t.classList.toggle("active", t.dataset.rt === e)),
      renderLiveRoster());
  }
  function ll(e) {
    const t = e.stat || {};
    switch (e.pos) {
      case "QB":
        return `${t.comp || 0}/${t.att || 0}, ${t.pass || 0} yd, ${t.td || 0} TD${t.int ? `, ${t.int} INT` : ""}`;
      case "RB":
        return `${t.carries || 0} car, ${t.rush || 0} yd${t.td ? `, ${t.td} TD` : ""}${t.rec ? `, ${t.rec} rec yd` : ""}`;
      case "WR":
      case "TE":
        return `${t.rec_c || 0} rec, ${t.rec || 0} yd${t.td ? `, ${t.td} TD` : ""}`;
      case "OL":
        return `${t.pancake || 0} pancakes${t.sackAllowed ? `, ${t.sackAllowed} sack allowed` : ", clean sheet"}`;
      case "DL":
      case "LB":
        return `${t.tackle || 0} tkl${t.sack ? `, ${t.sack} sk` : ""}${t.tfl ? `, ${t.tfl} TFL` : ""}${t.int ? `, ${t.int} INT` : ""}`;
      case "CB":
      case "S":
        return `${t.tackle || 0} tkl${t.pd ? `, ${t.pd} PD` : ""}${t.int ? `, ${t.int} INT` : ""}`;
      default:
        return "";
    }
  }
  function renderLiveRoster() {
    const e = state._liveGame;
    if (!e || !e.roster) return;
    const t = byId("rosterBox");
    if (!t) return;
    const a = rosterSide === "us" ? e.roster.us : e.roster.opp,
      s = liveCtl ? clamp99((liveCtl.idx + 1) / e.plays.length, 0, 1) : 1,
      n = l => (l >= 85 ? "var(--good)" : l >= 65 ? "var(--gold)" : l >= 45 ? "var(--chalk)" : "#c98"),
      i = (l, d, c) =>
        `<div class="rgrouphdr">${l} <span style="color:var(--chalk-dim)">· unit ${c} OVR</span></div>` +
        d
          .map(u => {
            const h = { ...u.stat };
            for (const m in h) typeof h[m] == "number" && !["pos"].includes(m) && (h[m] = Math.round(h[m] * s));
            const p = ll({ pos: u.pos, stat: h });
            return `<div class="rrow ${u.you ? "you" : ""}">
      <span class="rnum">#${u.num}</span>
      <span class="rpos">${u.pos}</span>
      <span class="rovr" style="color:${n(u.ovr)}">${u.ovr}</span>
      <span class="rname">${escHtml(u.name)}${u.you ? " ⭐" : ""}</span>
      <span class="rstat">${p}</span>
    </div>`;
          })
          .join(""),
      r = `<div class="team-ovr-hdr"><span>TEAM OVERALL</span><b style="color:${n(a.ovr)}">${a.ovr}</b><span class="small">OFF ${a.offOvr} · DEF ${a.defOvr}</span></div>`;
    t.innerHTML = r + i("OFFENSE", a.off, a.offOvr) + i("DEFENSE", a.def, a.defOvr);
  }
  function liveBoxLine(e, t) {
    switch ((state._liveGame, t.att && Math.round((t.comp / t.att) * 100), e)) {
      case "QB":
        return [
          ["C/ATT", `${t.comp || 0}/${t.att || 0}`],
          ["YDS", t.pass || 0],
          ["TD", t.td || 0],
          ["INT", t.int || 0],
          ["LONG", t.longest || 0],
          ["RUSH", t.rush || 0]
        ];
      case "RB":
        return [
          ["CAR", t.carries || 0],
          ["RUSH YDS", t.rush || 0],
          ["AVG", t.carries ? Math.round(t.rush / t.carries) : 0],
          ["TD", t.td || 0],
          ["LONG", t.longest || 0],
          ["REC", t.rec || 0],
          ["FUM", t.fum || 0]
        ];
      case "WR":
      case "TE":
        return [
          ["REC", t.rec_c || 0],
          ["TGT", t.tgt || 0],
          ["YDS", t.rec || 0],
          ["AVG", t.rec_c ? Math.round(t.rec / t.rec_c) : 0],
          ["TD", t.td || 0],
          ["LONG", t.longest || 0]
        ];
      case "OL":
        return [
          ["PANCAKES", t.pancake || 0],
          ["SACKS ALL", t.sackAllowed || 0],
          ["PEN", randInt(0, 2)]
        ];
      case "DL":
        return [
          ["TKL", t.tackle || 0],
          ["SACK", t.sack || 0],
          ["TFL", t.tfl || 0],
          ["QB HIT", t.qbhit || 0],
          ["FF", t.ff || 0]
        ];
      case "LB":
        return [
          ["TKL", t.tackle || 0],
          ["SACK", t.sack || 0],
          ["TFL", t.tfl || 0],
          ["INT", t.int || 0],
          ["PICK 6", t.pick6 || 0],
          ["FF", t.ff || 0]
        ];
      case "CB":
        return [
          ["TKL", t.tackle || 0],
          ["PD", t.pd || 0],
          ["INT", t.int || 0],
          ["PICK 6", t.pick6 || 0],
          ["TGT", t.tgt || 0]
        ];
      case "S":
        return [
          ["TKL", t.tackle || 0],
          ["PD", t.pd || 0],
          ["INT", t.int || 0],
          ["PICK 6", t.pick6 || 0],
          ["TFL", t.tfl || 0]
        ];
      default:
        return [
          ["TKL", t.tackle || 0],
          ["TD", t.td || 0]
        ];
    }
  }
  function renderLiveBox(e) {
    const t = byId("fullBox");
    t &&
      (t.innerHTML = liveBoxLine(state.player.pos, e)
        .map(([a, s]) => {
          const n = parseFloat(s) > 0 && !["INT", "FUM", "SACKS ALL", "PEN"].includes(a);
          return `<div class="fb-item"><span class="fb-l">${a}</span><span class="fb-v ${n ? "pos" : ""}" id="fb-${a.replace(/[^a-z]/gi, "")}">${s}</span></div>`;
        })
        .join(""));
  }
  function dl() {
    const e = state._liveGame,
      t = e.team,
      a = e.oppTeam,
      s = byId("teamBox");
    if (!s) return;
    const n = [
      ["Total Yards", t.yds, a.yds],
      ["Passing", t.pass, a.pass],
      ["  · Air / YAC", `${t.air || 0} / ${t.yac || 0}`, `${a.air || 0} / ${a.yac || 0}`],
      ["Rushing", t.rush, a.rush],
      ["First Downs", t.first, a.first],
      ["3rd Down", t.third, "—"],
      ["Turnovers", t.turn, a.turn],
      ["Sacks", t.sacks, "—"],
      ["Penalties", `${t.pen}-${t.penYds}`, "—"],
      ["Time of Poss", t.top, "—"]
    ];
    s.innerHTML = n
      .map(
        ([i, r, l]) =>
          `<div class="tb-row"><span class="tb-l">${i}</span><span class="tb-a">${r}</span><span class="tb-b">${l}</span></div>`
      )
      .join("");
  }
  function ul(e, t) {
    const a = byId("teamBox");
    if (!a) return;
    t = t || {};
    const s = [
      ["Total Yards", e.yds || 0, t.yds || 0],
      ["Passing", e.pass || 0, t.pass || 0],
      ["  · Air / YAC", `${e.air || 0} / ${e.yac || 0}`, `${t.air || 0} / ${t.yac || 0}`],
      ["Rushing", e.rush || 0, t.rush || 0],
      ["First Downs", e.first || 0, t.first || 0],
      ["Turnovers", e.turn || 0, t.turn || 0],
      ["Penalties", `${e.pen || 0}-${e.penYds || 0}`, "—"]
    ];
    a.innerHTML = s
      .map(
        ([n, i, r]) =>
          `<div class="tb-row"><span class="tb-l">${n}</span><span class="tb-a">${i}</span><span class="tb-b">${r}</span></div>`
      )
      .join("");
  }
  function liveStatCols(e) {
    switch (e) {
      case "QB":
        return [
          ["pass", "PASS YDS"],
          ["td", "TD"],
          ["rush", "RUSH"]
        ];
      case "RB":
        return [
          ["rush", "RUSH YDS"],
          ["td", "TD"],
          ["rec", "REC YDS"]
        ];
      case "WR":
      case "TE":
        return [
          ["rec", "REC YDS"],
          ["td", "TD"],
          ["rush", "RUSH"]
        ];
      case "DL":
        return [
          ["sack", "SACKS"],
          ["tackle", "TACKLES"],
          ["td", "TD"]
        ];
      case "LB":
        return [
          ["tackle", "TACKLES"],
          ["sack", "SACKS"],
          ["int", "INT"]
        ];
      case "CB":
        return [
          ["tackle", "TACKLES"],
          ["pd", "PASS DEF"],
          ["int", "INT"]
        ];
      case "S":
        return [
          ["tackle", "TACKLES"],
          ["int", "INT"],
          ["pd", "PASS DEF"]
        ];
      default:
        return [
          ["tackle", "TACKLES"],
          ["td", "TD"],
          ["rush", "YDS"]
        ];
    }
  }
  function fl(e) {
    const t = state.player,
      a = liveStatCols(t.pos),
      s = { tackle: "🏈", sack: "🪖", int: "🧤", pd: "🖐", td: "🔥", rush: "💨", rec: "🙌", pass: "🎯" };
    ((byId("liveStats").innerHTML =
      a
        .map(
          ([n, i]) =>
            `<div class="live-stat" data-ic="${s[n] || "🏈"}"><div class="n" id="ls-${n}">${e[n] || 0}</div><div class="l">${i}</div></div>`
        )
        .join("") + `<div class="live-stat-minor-v96" id="liveMinorV96"></div>`),
      flMinorV96(e));
  }
  /* v96: the not-crucial line — everything the full box knows that the three big tiles don't, under them, refreshed every play */
  function flMinorV96(e) {
    const t = state.player,
      r = byId("liveMinorV96");
    if (!r || !t) return;
    const a = liveStatCols(t.pos),
      k = {
        rush: ["RUSH YDS", "RUSH"],
        td: ["TD"],
        rec: t.pos === "RB" ? ["REC"] : ["YDS"],
        pass: ["YDS"],
        tackle: ["TKL"],
        sack: ["SACK"],
        int: ["INT"],
        pd: ["PD"]
      },
      h = new Set(a.flatMap(([n]) => k[n] || []));
    r.innerHTML = liveBoxLine(t.pos, e || {})
      .filter(([l]) => !h.has(l))
      .map(([l, v]) => `<span><b>${v}</b>${l}</span>`)
      .join("");
  }
  function startLivePlayback() {
    (state.player,
      state._liveGame,
      (liveCtl = {
        idx: -1,
        speed: (liveCtl && liveCtl.speed) || (settingOn("fastSim") ? 2 : 1),
        playing: !0,
        anim: null,
        t: 0
      }),
      fl({}),
      renderLiveBox({}),
      dl(),
      renderLiveRoster(),
      document
        .querySelectorAll(".speed-btn[data-spd]")
        .forEach(e => e.classList.toggle("active", parseFloat(e.dataset.spd) === liveCtl.speed)),
      liveTick());
  }
  function setSpeed(e) {
    (liveCtl && (liveCtl.speed = e),
      document
        .querySelectorAll(".speed-btn[data-spd]")
        .forEach(t => t.classList.toggle("active", parseFloat(t.dataset.spd) === e)));
  }
  function skipLive() {
    (window.GridironPhaser && window.GridironPhaser.cancel(),
      liveCtl && ((liveCtl.playing = !1), liveCtl.anim && cancelAnimationFrame(liveCtl.anim)),
      endLive());
  }
  function vl(e) {
    return e
      ? e.header || e.event === "drive"
        ? !!((settingOn("skipOpp") && e.offense !== "us") || settingOn("onlyInvolved"))
        : !!((settingOn("onlyInvolved") && !e.involved) || (settingOn("skipOpp") && e.offense !== "us" && !e.involved))
      : !1;
  }
  function liveTick() {
    if (!liveCtl || !liveCtl.playing) return;
    const e = state._liveGame;
    if ((liveCtl.idx++, liveCtl.idx >= e.plays.length)) {
      endLive();
      return;
    }
    const t = e.plays[liveCtl.idx];
    if (vl(t)) {
      const i = byId("usScore"),
        r = byId("themScore");
      (i && (i.textContent = t.usScore),
        r && (r.textContent = t.themScore),
        liveCtl.idx % 4 === 0 && renderLiveRoster(),
        (liveCtl.anim = requestAnimationFrame(() => liveTick())));
      return;
    }
    const a = byId("qtr");
    a && (a.textContent = t.quarter > 4 ? "OT" : t.quarter);
    const a9 = byId("gameClock");
    a9 && t.clock && (a9.textContent = t.clock);
    a9 && (a9.classList.toggle("stopped", !!t.clockStopped), (a9.dataset.stop = t.clockStopped || ""));
    try {
      window.__ribPaintTimeoutsV109 && window.__ribPaintTimeoutsV109(t.toLeft);
    } catch (_e) {}
    const s = byId("downDist");
    if (s)
      if (t.header || t.event === "drive")
        s.textContent =
          t.event === "timeout"
            ? "TIMEOUT"
            : t.event === "warning"
              ? "2-MIN WARNING"
              : t.event === "period"
                ? t.sub || "END OF PERIOD"
                : t.event === "toss"
                  ? "COIN TOSS"
                  : t.offense === "us"
                    ? "OUR BALL"
                    : "DEFENSE";
      else if (t.event === "xp" || t.event === "twopt") s.textContent = t.event === "xp" ? "EXTRA POINT" : "2-PT TRY";
      else {
        const i = t.preDown || t.down,
          r = t.preToGo || t.toGo,
          l = i + (i === 1 ? "st" : i === 2 ? "nd" : i === 3 ? "rd" : "th");
        s.textContent = l + " & " + r;
      }
    t.fx = Xi(t);
    const n = byId("commentary");
    if (n) {
      const i = t.desc || "",
        r = t.header
          ? "hl"
          : t.scored && t.offense === "us"
            ? "big"
            : i.includes("SACK") || i.includes("INTERCEPTION") || i.includes("BREAKUP") || i.includes("FUMBLE")
              ? "hl"
              : i.includes("Opponent") || i.includes("TURNOVER") || i.includes("💔")
                ? "bad"
                : "",
        l = bl(t);
      let d = "🏈";
      (t.scored
        ? (d = t.offense === "us" ? "🔥" : "💔")
        : i.includes("FUMBLE")
          ? (d = "💥")
          : i.includes("INTERCEPT")
            ? (d = "🧤")
            : i.includes("SACK")
              ? (d = "🪖")
              : i.includes("First down")
                ? (d = "🔗")
                : t.fx && t.fx.miss
                  ? (d = "💨")
                  : t.event === "punt"
                    ? (d = "🦶")
                    : t.event === "fg" || t.event === "xp"
                      ? (d = "🎯")
                      : t.event === "timeout" || t.event === "warning"
                        ? (d = "⏱️")
                        : t.event === "period"
                          ? (d = "⏸")
                          : t.event === "toss"
                            ? (d = "🪙")
                            : t.penalty && (d = "🚩"),
        (n.className = "commentary" + (r ? " tone-" + r : "")),
        (n.innerHTML = `<span class="ev-ic">${d}</span><div class="ev-body"><span class="${r}">${t.desc}</span>${l ? `<span class="quip">🎙 ${l}</span>` : ""}</div><span class="chev">›</span>`));
    }
    if (t.header || t.event === "drive") {
      const i = byId("usScore"),
        r = byId("themScore");
      (i && (i.textContent = t.usScore),
        r && (r.textContent = t.themScore),
        yl(t),
        setTimeout(liveTick, Math.max(150, 600 / liveCtl.speed)));
      return;
    }
    kl(t, () => {
      if (!liveCtl || !state.player) return;
      /* v146: a play can finish after the career it belonged to is gone */ const i = byId("usScore"),
        r = byId("themScore");
      (i && (i.textContent = t.usScore),
        r && (r.textContent = t.themScore),
        liveStatCols(state.player.pos).forEach(([c]) => {
          const u = byId("ls-" + c);
          if (!u) return;
          const h = t.stat[c] || 0;
          parseInt(u.textContent) !== h &&
            ((u.textContent = h), u.classList.add("flash"), setTimeout(() => u.classList.remove("flash"), 400));
        }),
        flMinorV96(t.stat),
        renderLiveBox(qi(state.player.pos, t.stat, state._liveGame.stat)),
        t.team && ul(t.team, t.oppStat),
        liveCtl.idx % 3 === 0 && renderLiveRoster());
      const d = Math.max(90, 520 / liveCtl.speed);
      setTimeout(liveTick, d);
    });
  }
  function yl(e) {
    if (
      window.GridironPhaser &&
      window.GridironPhaser.drawStatic(((e.playerPos = state && state.player && state.player.pos), e))
    )
      return;
    const t = byId("field");
    if (!t) return;
    const a = t.getContext("2d"),
      s = t.width,
      n = t.height,
      i = e.offense === "us",
      r = i ? 1 : -1,
      l = i ? e.startBall : 100 - e.startBall,
      d = 26,
      c = 10,
      u = y => d + (y / 100) * (s - d * 2),
      h = y => c + (y / 53) * (n - c * 2),
      p = Zi(i, l, r, { isPass: !1, event: "run" }, state.player.pos);
    let m = l + r * (e.preToGo || 10);
    (r > 0 ? (m = Math.min(m, 100)) : (m = Math.max(m, 0)),
      eo(a, s, n, l, m, u),
      p.off.forEach(y => Wa(a, u(y.start.x), h(y.start.y), i, y)),
      p.def.forEach(y => Wa(a, u(y.start.x), h(y.start.y), !i, y)));
  }
  const gl = {
    bigRun: [
      "He hit the JETS — see ya!",
      "You can't teach that kind of speed, folks!",
      "Somebody call the fire department!",
      "Grown man business right there!",
      "He's got the wheels AND the paperwork!"
    ],
    juke: [
      "ANKLES. TAKEN.",
      "He left him grasping at pure air!",
      "That defender's gonna need a map to find his shoes!",
      "Put him on SKATES!",
      "Filthy. Absolutely filthy.",
      "Somebody check that man's hips for a license!"
    ],
    bigHit: [
      "OHHH he'll feel that one on Tuesday!",
      "That's a WELCOME-to-football hit!",
      "You could hear that from the parking lot!",
      "De-CLEATED!",
      "That hit rearranged his playlist!"
    ],
    pancake: [
      "FLAPJACK CITY!",
      "He just planted him like a rose bush!",
      "Somebody get the syrup — that's a PANCAKE!",
      "Buried him. Send flowers."
    ],
    td: [
      "Cue the fireworks! 🎆",
      "Dial 9-1-1, he is ON FIRE!",
      "That's six, baby!",
      "Straight to the HOUSE!",
      "Untouched! You can't catch what you can't see!"
    ],
    drop: [
      "Coach is gonna want that one back.",
      "Right through the bread basket!",
      "He heard footsteps on that one.",
      "Hands of stone on that snap."
    ],
    int: [
      "PICKED! Robbery in broad daylight!",
      "The quarterback wants a do-over on that one.",
      "Christmas came early for the defense!",
      "He threw that one straight to the wrong jersey!"
    ],
    sack: [
      "Planted him like a fence post!",
      "The pocket collapsed like a lawn chair!",
      "Nowhere to run, nowhere to hide!",
      "That's gonna leave a grass stain!"
    ],
    tackle: [
      "{T} was waiting on that one.",
      "Textbook wrap-up by {T}.",
      "{T} read it all the way.",
      "Nowhere to go — {T} had the lane closed."
    ],
    gang: [
      "{T} and {A} meet him there.",
      "Two men on him — {T} and {A} close it down.",
      "{A} cleans up what {T} started."
    ]
  };
  function bl(e) {
    if (!e || e.header || e.event === "drive") return null;
    const t = e.fx || {},
      a = e.desc || "";
    let s = null;
    return (
      e.scored && e.offense === "us"
        ? (s = "td")
        : a.includes("INTERCEPT")
          ? (s = "int")
          : a.includes("SACK")
            ? (s = "sack")
            : t.pancake && Math.random() < 0.8
              ? (s = "pancake")
              : t.miss
                ? (s = "juke")
                : e.big && (e.yards || 0) >= 15
                  ? (s = "bigRun")
                  : t.bigHit
                    ? (s = "bigHit")
                    : e.tackler &&
                        (e.event === "run" || e.event === "pass") &&
                        !e.scored &&
                        (e.yards || 0) <= 3 &&
                        Math.random() < 0.35
                      ? (s = e.assist ? "gang" : "tackle")
                      : e.event === "incomplete" && Math.random() < 0.4 && (s = "drop"),
      !s || (s !== "td" && Math.random() < 0.3)
        ? null
        : randPick(gl[s])
            .replace(/\{T\}/g, e.tackler || "the tackler")
            .replace(/\{A\}/g, e.assist || "a teammate")
    );
  }
  function Xi(e) {
    if (!e || e.header || e.event === "drive" || e.penalty) return {};
    const t = e.yards || 0,
      a = e.event === "run" || e.event === "pass",
      s = a && !e.scored,
      n = {};
    return (
      (n.miss =
        a &&
        ((t >= 8 && Math.random() < 0.55) || (t >= 4 && Math.random() < 0.18) || (e.scored && Math.random() < 0.5))),
      (n.spin = n.miss && Math.random() < 0.4),
      (n.bigHit = s && !n.miss && t <= 2 && Math.random() < 0.55),
      (n.pancake = e.event === "run" && t >= 6 && Math.random() < 0.32),
      n
    );
  }
  function pathPointAt(e, t) {
    if (t <= e[0].t) return e[0];
    for (let a = 1; a < e.length; a++)
      if (t <= e[a].t) {
        const s = e[a - 1],
          n = e[a],
          i = (t - s.t) / Math.max(1e-4, n.t - s.t);
        return { x: s.x + (n.x - s.x) * i, y: s.y + (n.y - s.y) * i };
      }
    return e[e.length - 1];
  }
  function routePath(e, t, a, s, n, i) {
    const r = t < i ? 1 : -1;
    switch (s) {
      case "slant":
        return [
          { x: e, y: t, t: 0 },
          { x: e + a * 1.5, y: t, t: 0.18 },
          { x: e + a * n, y: t + r * n * 0.8, t: 1 }
        ];
      case "drag":
        return [
          { x: e, y: t, t: 0 },
          { x: e + a * 2, y: t, t: 0.22 },
          { x: e + a * 3, y: t + r * 8, t: 0.6 },
          { x: e + a * n, y: t + r * 10, t: 1 }
        ];
      case "out":
        return [
          { x: e, y: t, t: 0 },
          { x: e + a * n, y: t, t: 0.6 },
          { x: e + a * n, y: t - r * 6, t: 1 }
        ];
      case "in":
        return [
          { x: e, y: t, t: 0 },
          { x: e + a * n, y: t, t: 0.6 },
          { x: e + a * n, y: t + r * 7, t: 1 }
        ];
      case "curl":
        return [
          { x: e, y: t, t: 0 },
          { x: e + a * (n + 2), y: t, t: 0.68 },
          { x: e + a * n, y: t + r * 1.5, t: 1 }
        ];
      case "post":
        return [
          { x: e, y: t, t: 0 },
          { x: e + a * n * 0.55, y: t, t: 0.5 },
          { x: e + a * n, y: t + r * n * 0.35, t: 1 }
        ];
      case "corner":
        return [
          { x: e, y: t, t: 0 },
          { x: e + a * n * 0.55, y: t, t: 0.5 },
          { x: e + a * n, y: t - r * n * 0.3, t: 1 }
        ];
      default:
        return [
          { x: e, y: t, t: 0 },
          { x: e + a * n, y: t, t: 1 }
        ];
    }
  }
  function pickRoute(e) {
    return e <= 5
      ? randPick(["slant", "drag", "curl"])
      : e <= 13
        ? randPick(["out", "in", "curl", "slant"])
        : randPick(["post", "corner", "go", "go"]);
  }
  function Zi(e, t, a, s, n) {
    const i = [],
      r = [];
    ([20.5, 23.5, 26.5, 29.5, 32.5].forEach((P, v) =>
      i.push({ pos: "OL", x: t, y: P, role: "block", lane: v, start: { x: t, y: P } })
    ),
      i.push({ pos: "QB", x: t - a * 4, y: 26.5, role: s.isPass ? "pass" : "hand", start: { x: t - a * 4, y: 26.5 } }),
      i.push({
        pos: "RB",
        x: t - a * 5,
        y: 26.5 + 3,
        role: s.event === "run" ? "carry" : "block",
        start: { x: t - a * 5, y: 26.5 + 3 }
      }),
      i.push({ pos: "WR", x: t, y: 26.5 - 16, role: "route", start: { x: t, y: 26.5 - 16 } }),
      i.push({ pos: "WR", x: t, y: 26.5 + 16, role: "route", start: { x: t, y: 26.5 + 16 } }),
      i.push({ pos: "WR", x: t - a * 1, y: 26.5 - 11, role: "route", start: { x: t - a * 1, y: 26.5 - 11 } }),
      i.push({
        pos: "TE",
        x: t,
        y: 26.5 + 8,
        role: s.event === "run" ? "block" : "route",
        start: { x: t, y: 26.5 + 8 }
      }),
      [26.5 - 7, 26.5 - 2.5, 26.5 + 2.5, 26.5 + 7].forEach((P, v) =>
        r.push({ pos: "DL", x: t + a * 1.5, y: P, role: "rush", lane: v, start: { x: t + a * 1.5, y: P } })
      ),
      [26.5 - 8, 26.5, 26.5 + 8].forEach(P =>
        r.push({ pos: "LB", x: t + a * 6, y: P, role: "pursue", start: { x: t + a * 6, y: P } })
      ),
      r.push({ pos: "CB", x: t + a * 3, y: 26.5 - 16, role: "cover", start: { x: t + a * 3, y: 26.5 - 16 } }),
      r.push({ pos: "CB", x: t + a * 3, y: 26.5 + 16, role: "cover", start: { x: t + a * 3, y: 26.5 + 16 } }),
      r.push({ pos: "S", x: t + a * 14, y: 26.5 - 7, role: "deep", start: { x: t + a * 14, y: 26.5 - 7 } }),
      r.push({ pos: "S", x: t + a * 14, y: 26.5 + 7, role: "deep", start: { x: t + a * 14, y: 26.5 + 7 } }));
    const h = ["QB", "RB", "WR", "TE", "OL"].includes(n),
      p = e && h,
      m = !e && !h;
    let y, C;
    p ? ((y = i), (C = !0)) : m ? ((y = r), (C = !0)) : ((y = i), (C = !1));
    let V = null;
    if (C) {
      const P = y.filter(v => v.pos === n);
      V = P.length ? P[0] : y[0];
    } else {
      const P = s.event === "run" ? ["RB", "QB"] : ["WR", "TE", "RB", "QB"];
      for (const v of P) {
        const j = y.filter(U => U.pos === v);
        if (j.length) {
          V = randPick(j);
          break;
        }
      }
      V || (V = y[0]);
    }
    return (V && ((V.featured = !0), (V.isMe = C)), { off: i, def: r, featuredIsMe: C, featuredPlayer: V });
  }
  function kl(e, t) {
    if (
      window.GridironPhaser &&
      window.GridironPhaser.animate(((e.playerPos = state && state.player && state.player.pos), e), t)
    )
      return;
    const a = byId("field");
    if (!a) {
      t();
      return;
    }
    const s = a.getContext("2d"),
      n = a.width,
      i = a.height,
      r = state.player,
      l = state._liveGame,
      d = e.offense === "us",
      c = d ? 1 : -1,
      u = d ? e.startBall : 100 - e.startBall,
      h = d ? e.endBall : 100 - e.endBall,
      p = 26.5,
      m = Zi(d, u, c, e, r.pos),
      y = e.fx || Xi(e),
      C = byId("featuredLabel");
    if (C) {
      const $ = r.name.split(" ")[1].slice(0, 8).toUpperCase();
      if (m.featuredIsMe)
        C.innerHTML = `<b>${escHtml(r.name)} (${r.pos})</b><small>${$} ${["DL", "LB", "CB", "S"].includes(r.pos) ? "DEFENSE" : "OFFENSE"}</small>`;
      else {
        const J = d ? l.roster && l.roster.us : l.roster && l.roster.opp;
        let w = "";
        if (J && m.featuredPlayer) {
          const k = [...J.off, ...J.def].filter(T => T.pos === m.featuredPlayer.pos && !T.you);
          k.length && (w = randPick(k).name);
        }
        C.innerHTML = `<b>${w || "opponent"} (${m.featuredPlayer ? m.featuredPlayer.pos : ""})</b><small>👁 WATCHING · ${d ? $ + " OFFENSE" : "OPPONENT OFFENSE"}</small>`;
      }
    }
    const V = 26,
      P = 10,
      v = $ => V + ($ / 100) * (n - V * 2),
      j = $ => P + ($ / 53) * (i - P * 2);
    let U = u + c * (e.preToGo || 10);
    c > 0 ? (U = Math.min(U, 100)) : (U = Math.max(U, 0));
    const ce = h - u,
      Y = (e.desc || "").includes("SACK"),
      _ = (e.desc || "").includes("INTERCEPT"),
      ie = e.event === "incomplete",
      B = e.event === "run" || e.event === "pass" || ie,
      b = e.event === "punt" || e.event === "fg",
      pe = B && !ie && !e.scored,
      Q = [];
    let Me = null,
      Fe = null,
      he = null,
      Mt = null;
    if (B) {
      if (e.isPass || ie || _) {
        const $ = m.off.find(g => g.pos === "QB"),
          J = m.off.filter(g => g.role === "route");
        Fe = J.length ? randPick(J) : $;
        const w = ie || _ ? Math.max(5, Math.abs(ce) || 8, 6) : Math.max(3, ce * 0.7),
          k = clamp99(u + c * w, 2, 98),
          T = Fe.start.y + (p - Fe.start.y) * 0.15;
        if (
          (($.path = [
            { x: $.start.x, y: $.start.y, t: 0 },
            { x: $.start.x - c * 3, y: $.start.y, t: 0.22 },
            { x: $.start.x - c * 3, y: $.start.y, t: 1 }
          ]),
          Y)
        )
          (($.path = [
            { x: $.start.x, y: $.start.y, t: 0 },
            { x: $.start.x - c * 3, y: $.start.y, t: 0.25 },
            { x: $.start.x - c * 4, y: $.start.y + randRange(-3, 3), t: 0.55 },
            { x: h, y: $.start.y + randRange(-2, 2), t: 0.8 },
            { x: h, y: $.start.y, t: 1 }
          ]),
            (Me = $),
            Q.push({ t: 0.8, type: "hit", x: h, y: $.start.y, big: !0, label: "SACKED!" }));
        else {
          Me = Fe;
          const g = Math.abs(k - Fe.start.x),
            N = pickRoute(g),
            H = routePath(Fe.start.x, Fe.start.y, c, N, g, p).map(oe => ({ x: oe.x, y: oe.y, t: oe.t * 0.58 }));
          if (((H[H.length - 1] = { x: k, y: T, t: 0.58 }), ie || _))
            (H.push({ x: k + c * 1, y: T, t: 1 }), Q.push({ t: 0.6, type: _ ? "int" : "deflect", x: k, y: T }));
          else {
            if (y.miss) {
              const oe = clamp99(k + c * Math.max(1, (h - k) * 0.45), 2, 98),
                be = T + randPick([-1, 1]) * 5;
              (H.push({ x: oe, y: be, t: 0.76 }),
                H.push({ x: h, y: T + randRange(-2, 2), t: 0.95 }),
                Q.push({ t: 0.74, type: "miss", x: oe, y: T, spin: y.spin }));
            } else H.push({ x: h, y: T + randRange(-3, 3), t: 0.95 });
            Q.push({ t: 0.58, type: "catch", x: k, y: T });
          }
          (H.push({ x: H[H.length - 1].x, y: H[H.length - 1].y, t: 1 }),
            (Fe.path = H),
            (Mt = H),
            (he = { from: { x: $.start.x - c * 3, y: $.start.y }, to: { x: k, y: T }, t0: 0.32, t1: 0.58 }));
        }
        J.forEach(g => {
          if (g === Fe) return;
          const N = 8 + Math.random() * 12;
          ((g.path = routePath(g.start.x, g.start.y, c, pickRoute(N), N, p).map(H => ({
            x: H.x,
            y: H.y,
            t: H.t * 0.9
          }))),
            g.path.push({ x: g.path[g.path.length - 1].x, y: g.path[g.path.length - 1].y, t: 1 }));
        });
        const K = m.off.find(g => g.pos === "RB");
        K &&
          K.role === "block" &&
          (K.path = [
            { x: K.start.x, y: K.start.y, t: 0 },
            { x: K.start.x + c * 2, y: K.start.y, t: 0.4 },
            { x: K.start.x + c * 2, y: K.start.y, t: 1 }
          ]);
      } else {
        const $ = m.off.find(T => T.role === "carry") || m.off.find(T => T.pos === "RB");
        Me = $;
        const J = p + randPick([-7, -4, 0, 4, 7]),
          w = [
            { x: $.start.x, y: $.start.y, t: 0 },
            { x: u - c * 1, y: ($.start.y + J) / 2, t: 0.2 },
            { x: u + c * 1.5, y: J, t: 0.38 }
          ];
        if (ce < 0)
          (w.push({ x: u + c * 0.5, y: J + randPick([-4, 4]), t: 0.6 }),
            w.push({ x: h, y: J + randRange(-2, 2), t: 0.9 }));
        else if (y.miss) {
          const T = clamp99(u + c * Math.max(2, ce * 0.45), 2, 98),
            K = J + randPick([-1, 1]) * 5.5;
          (w.push({ x: T, y: J, t: 0.58 }),
            w.push({ x: clamp99(T + c * 1.5, 2, 98), y: K, t: 0.68 }),
            w.push({ x: h, y: K + randRange(-3, 3), t: 0.93 }),
            Q.push({ t: 0.58, type: "miss", x: T, y: J, spin: y.spin }));
        } else
          (w.push({ x: clamp99(u + c * Math.max(1.5, ce * 0.5), 2, 98), y: J + randRange(-3, 3), t: 0.62 }),
            w.push({ x: h, y: J + randRange(-4, 4), t: 0.93 }));
        (w.push({ x: w[w.length - 1].x, y: w[w.length - 1].y, t: 1 }), ($.path = w));
        const k = m.off.find(T => T.pos === "QB");
        ((k.path = [
          { x: k.start.x, y: k.start.y, t: 0 },
          { x: u - c * 2, y: p + 1, t: 0.25 },
          { x: u - c * 4, y: p - 3, t: 0.6 },
          { x: u - c * 4, y: p - 3, t: 1 }
        ]),
          m.off
            .filter(T => T.role === "route")
            .forEach(T => {
              T.path = [
                { x: T.start.x, y: T.start.y, t: 0 },
                { x: T.start.x + c * 5, y: T.start.y + (p - T.start.y) * 0.2, t: 0.6 },
                { x: T.start.x + c * 6, y: T.start.y, t: 1 }
              ];
            }));
      }
      if (pe && !Y) {
        const $ = Me ? pathPointAt(Me.path, 0.93) : { y: p };
        Q.push({
          t: 0.93,
          type: "hit",
          x: h,
          y: $.y,
          big: y.bigHit || ce < 0,
          label: y.bigHit ? "BIG HIT!" : randPick(["WRAP!", "STICK!", "POW!", "CRUNCH!"])
        });
      }
      if ((e.scored && d && Q.push({ t: 0.95, type: "td" }), y.pancake)) {
        const $ = randInt(0, 3);
        Q.push({ t: 0.42, type: "pancake", lane: $ });
      }
    }
    const da = m.def.filter($ => $.role === "rush"),
      xt = m.off.filter($ => $.pos === "OL");
    if (
      (da.forEach(($, J) => {
        Q.some(k => k.type === "pancake" && k.lane === J)
          ? (($.path = [
              { x: $.start.x, y: $.start.y, t: 0 },
              { x: $.start.x - c * 0.5, y: $.start.y, t: 0.4 },
              { x: $.start.x - c * 0.5, y: $.start.y, t: 1 }
            ]),
            ($.flatAt = 0.42))
          : Y && J === 1
            ? ($.path = [
                { x: $.start.x, y: $.start.y, t: 0 },
                { x: $.start.x - c * 2, y: $.start.y - 4, t: 0.35 },
                { x: h, y: p, t: 0.8 },
                { x: h, y: p, t: 1 }
              ])
            : (($.path = [
                { x: $.start.x, y: $.start.y, t: 0 },
                { x: $.start.x - c * (B ? 2.2 : 1), y: $.start.y + randRange(-1, 1), t: 0.5 },
                { x: $.start.x - c * 2.6, y: $.start.y, t: 1 }
              ]),
              ($.engage = !0));
      }),
      xt.forEach($ => {
        (($.path = [
          { x: $.start.x, y: $.start.y, t: 0 },
          { x: $.start.x - c * 1.2, y: $.start.y, t: 0.5 },
          { x: $.start.x - c * 1.6, y: $.start.y, t: 1 }
        ]),
          ($.engage = !0));
      }),
      B && Me)
    ) {
      const $ = Me.path[Me.path.length - 1],
        J = m.def.filter(k => ["LB", "S", "CB"].includes(k.pos));
      J.sort(
        (k, T) =>
          Math.abs(k.start.y - $.y) +
          Math.abs(k.start.x - $.x) -
          (Math.abs(T.start.y - $.y) + Math.abs(T.start.x - $.x))
      );
      const w = Q.find(k => k.type === "miss");
      if (w && J.length > 1) {
        const k = J[0],
          T = clamp99(w.x - c * 0.5, 2, 98),
          K = w.y + (w.y < p ? -1 : 1) * 6;
        ((k.path = [
          { x: k.start.x, y: k.start.y, t: 0 },
          { x: k.start.x, y: k.start.y, t: 0.18 },
          { x: w.x, y: w.y, t: w.t },
          { x: T - c * 3, y: K, t: Math.min(1, w.t + 0.18) },
          { x: T - c * 3, y: K, t: 1 }
        ]),
          (k.whiff = !0));
        const g = J[1];
        pe
          ? (g.path = [
              { x: g.start.x, y: g.start.y, t: 0 },
              { x: g.start.x, y: g.start.y, t: 0.3 },
              { x: $.x, y: $.y, t: 0.93 },
              { x: $.x, y: $.y, t: 1 }
            ])
          : (g.path = [
              { x: g.start.x, y: g.start.y, t: 0 },
              { x: g.start.x, y: g.start.y, t: 0.3 },
              { x: $.x - c * 4, y: $.y + 2, t: 1 }
            ]);
      } else if (J.length) {
        const k = J[0];
        pe
          ? (k.path = [
              { x: k.start.x, y: k.start.y, t: 0 },
              { x: k.start.x, y: k.start.y, t: 0.2 },
              { x: $.x, y: $.y, t: 0.93 },
              { x: $.x, y: $.y, t: 1 }
            ])
          : (k.path = [
              { x: k.start.x, y: k.start.y, t: 0 },
              { x: k.start.x, y: k.start.y, t: 0.2 },
              { x: $.x - c * 4, y: $.y + 2, t: 1 }
            ]);
      }
      (m.def
        .filter(k => k.role === "cover")
        .forEach(k => {
          const T = m.off
            .filter(K => K.role === "route")
            .sort((K, g) => Math.abs(K.start.y - k.start.y) - Math.abs(g.start.y - k.start.y))[0];
          T && T.path && (k.mirror = T);
        }),
        m.def
          .filter(k => k.role === "deep")
          .forEach(k => {
            k.path ||
              (k.path = [
                { x: k.start.x, y: k.start.y, t: 0 },
                { x: k.start.x, y: k.start.y, t: 0.35 },
                { x: $.x + c * 2, y: ($.y + k.start.y) / 2, t: 1 }
              ]);
          }),
        m.def
          .filter(k => k.role === "pursue" && !k.path)
          .forEach(k => {
            k.path = [
              { x: k.start.x, y: k.start.y, t: 0 },
              { x: k.start.x, y: k.start.y, t: 0.25 },
              { x: ($.x + k.start.x) / 2, y: ($.y + k.start.y) / 2, t: 0.7 },
              { x: $.x + c * 1, y: $.y + randRange(-3, 3), t: 1 }
            ];
          }));
    }
    (b &&
      (he = {
        from: { x: u - c * 7, y: p },
        to: {
          x: clamp99(u + c * (e.event === "fg" ? Math.min(100 - u + 2, 40) : 38), 0, 100),
          y: p + randRange(-3, 3)
        },
        t0: 0.25,
        t1: 0.8,
        high: !0
      }),
      e.penalty && Q.push({ t: 0.45, type: "flag", x: u + c * 2, y: p + randRange(-6, 6) }));
    const ua = Math.max(560, (B ? 1650 : 1050) / liveCtl.speed),
      fa = performance.now(),
      xe = [];
    let st = -1,
      Tt = null,
      Rt = new Set();
    function Ye($) {
      if (!liveCtl || !liveCtl.playing) {
        t();
        return;
      }
      const J = Math.min(1, ($ - fa) / ua),
        w = Math.min(1, J / 0.88),
        k = w < 0.5 ? 2 * w * w : 1 - Math.pow(-2 * w + 2, 2) / 2;
      Q.forEach(g => {
        w >= g.t &&
          !Rt.has(g) &&
          (Rt.add(g),
          g.type === "hit" && ((st = $), (g.shakeBig = g.big)),
          g.type === "td" && ((Tt = Rl(c > 0 ? n - 14 : 14, i / 2)), (st = $)),
          g.type);
      });
      let T = 0,
        K = 0;
      if (st > 0) {
        const g = ($ - st) / 260;
        if (g < 1) {
          const N = (1 - g) * 3.2;
          ((T = (Math.random() - 0.5) * N * 2), (K = (Math.random() - 0.5) * N * 2));
        } else st = -1;
      }
      if ((s.save(), s.translate(T, K), eo(s, n, i, u, U, v), Mt && w > 0.04)) {
        (s.save(), (s.strokeStyle = "rgba(240,187,69,.5)"), (s.lineWidth = 1.4), s.setLineDash([4, 3]), s.beginPath());
        let g = !1;
        for (let N = 0; N <= Math.min(w, 0.95); N += 0.03) {
          const H = pathPointAt(Mt, N);
          g ? s.lineTo(v(H.x), j(H.y)) : (s.moveTo(v(H.x), j(H.y)), (g = !0));
        }
        (s.stroke(), s.setLineDash([]), s.restore());
      }
      if (Me && Me.path) {
        const g = pathPointAt(Me.path, k);
        ((xe.length === 0 || Math.hypot(g.x - xe[xe.length - 1].x, g.y - xe[xe.length - 1].y) > 0.9) &&
          xe.push({ x: g.x, y: g.y }),
          xe.length > 9 && xe.shift(),
          xe.forEach((N, H) => {
            (s.beginPath(),
              s.arc(v(N.x), j(N.y), 1.6 + H * 0.2, 0, 7),
              (s.fillStyle = `rgba(240,187,69,${0.05 + H * 0.03})`),
              s.fill());
          }));
      }
      if (
        (m.off.forEach(g => {
          let N = g.path
            ? pathPointAt(g.path, k)
            : { x: g.start.x + (g.engage ? c * Math.min(1.4, Math.abs(ce) * 0.08) * k : 0), y: g.start.y };
          (g.engage && (N = { x: N.x, y: N.y + Math.sin($ / 60 + g.start.y) * 0.35 }),
            (g._cx = N.x),
            (g._cy = N.y),
            Wa(s, v(N.x), j(N.y), d, g));
        }),
        m.def.forEach(g => {
          let N;
          if (g.flatAt != null && k >= g.flatAt) {
            ((N = pathPointAt(g.path, g.flatAt)),
              xl(s, v(N.x), j(N.y), !d),
              k < g.flatAt + 0.1 && Ss(s, v(N.x), j(N.y) - 4, "PANCAKE!", "#ffd97a"));
            return;
          }
          if (g.mirror && g.mirror.path) {
            const H = pathPointAt(g.mirror.path, Math.max(0, k - 0.07));
            N = { x: H.x + c * 1.5, y: H.y + (H.y < p ? 1.2 : -1.2) };
          } else
            g.path
              ? (N = pathPointAt(g.path, k))
              : g.engage
                ? (N = { x: g.start.x - c * 1.2 * k, y: g.start.y + Math.sin($ / 60 + g.start.y * 2) * 0.35 })
                : (N = { x: g.start.x, y: g.start.y });
          ((g._cx = N.x), (g._cy = N.y), Wa(s, v(N.x), j(N.y), !d, g));
        }),
        he && w >= he.t0 && w <= he.t1 + 0.02)
      ) {
        const g = clamp99((w - he.t0) / (he.t1 - he.t0), 0, 1),
          N = he.from.x + (he.to.x - he.from.x) * g,
          H = he.from.y + (he.to.y - he.from.y) * g,
          oe = (he.high ? 16 : 9) * Math.sin(g * Math.PI);
        (s.beginPath(),
          s.ellipse(v(N), j(H), 3, 1.6, 0, 0, 7),
          (s.fillStyle = "rgba(0,0,0,.25)"),
          s.fill(),
          s.save(),
          (s.shadowColor = "#f0bb45"),
          (s.shadowBlur = 6),
          Cl(s, v(N), j(H) - oe),
          s.restore());
      }
      if (
        (Q.forEach(g => {
          if (w < g.t || w > g.t + 0.16) return;
          const N = v(g.x || 0),
            H = j(g.y || 0);
          g.type === "hit"
            ? $l(s, N, H, g.big, g.label)
            : g.type === "miss"
              ? Sl(s, N, H, g.spin)
              : g.type === "catch"
                ? Ss(s, N, H - 8, "CATCH!", "#bfe3ae")
                : g.type === "deflect"
                  ? Ml(s, N, H)
                  : g.type === "int"
                    ? Ss(s, N, H - 6, "PICKED OFF!", "#e08a8a", !0)
                    : g.type === "flag" && Tl(s, N, H);
        }),
        Tt && Pl(s, Tt),
        e.scored &&
          d &&
          J > 0.86 &&
          (Ll(s, n, i, c),
          s.save(),
          (s.font = "bold 22px Oswald, sans-serif"),
          (s.textAlign = "center"),
          (s.textBaseline = "middle"),
          (s.fillStyle = "rgba(0,0,0,.45)"),
          s.fillRect(n / 2 - 84, i / 2 - 18, 168, 36),
          (s.fillStyle = "#ffd97a"),
          s.fillText("TOUCHDOWN!", n / 2, i / 2),
          s.restore()),
        (e.desc || "").includes("First down") &&
          J > 0.9 &&
          (s.save(),
          (s.strokeStyle = "rgba(255,255,255,.85)"),
          (s.lineWidth = 3),
          s.beginPath(),
          s.moveTo(v(U), 8),
          s.lineTo(v(U), i - 8),
          s.stroke(),
          (s.fillStyle = "#fff"),
          (s.font = "bold 10px Oswald"),
          (s.textAlign = "center"),
          s.fillText("🔗 FIRST DOWN", v(clamp99(U, 10, 90)), 20),
          s.restore()),
        J > 0.9 && !e.scored && e.yards != null && Math.abs(e.yards) >= 1 && !ie && B)
      ) {
        const g = Me ? pathPointAt(Me.path, 1) : { y: p };
        wl(s, v(h), j(g.y), e.yards);
      }
      (s.restore(), J < 1 ? (liveCtl.anim = requestAnimationFrame(Ye)) : t());
    }
    liveCtl.anim = requestAnimationFrame(Ye);
  }
  function wl(e, t, a, s, n) {
    e.save();
    const i = s > 0;
    ((e.fillStyle = i ? "#e8f5df" : "#f0b0a8"),
      (e.font = "bold 11px Oswald, sans-serif"),
      (e.textAlign = "center"),
      (e.textBaseline = "bottom"),
      (e.shadowColor = "rgba(0,0,0,.6)"),
      (e.shadowBlur = 3),
      e.fillText((s > 0 ? "+" : "") + s + " yd", clamp99(t, 20, 340), a - 14),
      e.restore());
  }
  function $l(e, t, a, s, n) {
    e.save();
    const i = s ? 15 : 11,
      r = s ? 5 : 4;
    ((e.strokeStyle = s ? "#ff9b6a" : "#ffd97a"), (e.lineWidth = s ? 2.5 : 2));
    for (let l = 0; l < (s ? 10 : 8); l++) {
      const d = (l / (s ? 10 : 8)) * Math.PI * 2 + 0.3;
      (e.beginPath(),
        e.moveTo(t + Math.cos(d) * r, a + Math.sin(d) * r),
        e.lineTo(t + Math.cos(d) * i, a + Math.sin(d) * i),
        e.stroke());
    }
    ((e.fillStyle = "#fff"),
      (e.font = "bold " + (s ? 11 : 9) + "px Oswald, sans-serif"),
      (e.textAlign = "center"),
      (e.textBaseline = "bottom"),
      (e.shadowColor = "rgba(0,0,0,.7)"),
      (e.shadowBlur = 3),
      e.fillText(n || "TACKLE", t, a - (s ? 16 : 13)),
      e.restore());
  }
  function Sl(e, t, a, s) {
    (e.save(),
      (e.strokeStyle = "#e0645a"),
      (e.lineWidth = 1.5),
      e.setLineDash([3, 2]),
      e.beginPath(),
      e.arc(t, a, 10, 0, Math.PI * 2),
      e.stroke(),
      e.setLineDash([]),
      (e.fillStyle = "#ffb3a8"),
      (e.font = "bold 9px Oswald, sans-serif"),
      (e.textAlign = "center"),
      (e.textBaseline = "bottom"),
      (e.shadowColor = "rgba(0,0,0,.7)"),
      (e.shadowBlur = 3),
      e.fillText(s ? "SPIN MOVE! 🌀" : "JUKED! MISSED!", t, a - 12),
      e.restore());
  }
  function Ss(e, t, a, s, n, i) {
    (e.save(),
      (e.fillStyle = n || "#fff"),
      (e.font = "bold " + (i ? 11 : 9) + "px Oswald, sans-serif"),
      (e.textAlign = "center"),
      (e.textBaseline = "bottom"),
      (e.shadowColor = "rgba(0,0,0,.75)"),
      (e.shadowBlur = 3),
      e.fillText(s, clamp99(t, 26, 334), a),
      e.restore());
  }
  function Ml(e, t, a) {
    (e.save(), (e.strokeStyle = "#5aa0dc"), (e.lineWidth = 2));
    for (let s = 0; s < 6; s++) {
      const n = (s / 6) * Math.PI * 2;
      (e.beginPath(), e.moveTo(t, a), e.lineTo(t + Math.cos(n) * 10, a + Math.sin(n) * 10), e.stroke());
    }
    ((e.fillStyle = "#9cd"),
      (e.font = "bold 8px Oswald, sans-serif"),
      (e.textAlign = "center"),
      (e.textBaseline = "bottom"),
      e.fillText("BROKEN UP", t, a - 12),
      e.restore());
  }
  function xl(e, t, a, s) {
    (e.save(),
      e.translate(t, a),
      e.rotate(0.5),
      e.beginPath(),
      e.ellipse(0, 0, 7.5, 3, 0, 0, 7),
      (e.fillStyle = s ? "#3a6f2e" : "#8a3830"),
      e.fill(),
      (e.strokeStyle = "rgba(0,0,0,.4)"),
      (e.lineWidth = 1),
      e.stroke(),
      (e.fillStyle = "rgba(255,255,255,.7)"),
      (e.font = "bold 7px Oswald"),
      (e.textAlign = "center"),
      (e.textBaseline = "middle"),
      e.fillText("✕", 0, 0),
      e.restore());
  }
  function Tl(e, t, a) {
    (e.save(),
      (e.fillStyle = "#ffe14d"),
      e.fillRect(t - 4, a - 4, 8, 8),
      (e.strokeStyle = "rgba(0,0,0,.5)"),
      e.strokeRect(t - 4, a - 4, 8, 8),
      (e.fillStyle = "#ffe14d"),
      (e.font = "bold 9px Oswald"),
      (e.textAlign = "center"),
      (e.textBaseline = "bottom"),
      e.fillText("🚩 FLAG", t, a - 7),
      e.restore());
  }
  function Rl(e, t) {
    const a = [];
    for (let s = 0; s < 26; s++)
      a.push({
        x: e,
        y: t + randRange(-60, 60),
        vx: randRange(-2.2, -0.4) * (e > 180 ? 1 : -1),
        vy: randRange(-2.4, -0.4),
        g: 0.09,
        c: randPick(["#ffd97a", "#6bbf59", "#e0645a", "#5aa0dc", "#fff"]),
        s: randRange(1.6, 3.2),
        rot: Math.random() * 7
      });
    return a;
  }
  function Pl(e, t) {
    t.forEach(a => {
      ((a.x += a.vx),
        (a.y += a.vy),
        (a.vy += a.g),
        (a.rot += 0.15),
        e.save(),
        e.translate(a.x, a.y),
        e.rotate(a.rot),
        (e.fillStyle = a.c),
        e.fillRect(-a.s / 2, -a.s / 2, a.s, a.s * 0.7),
        e.restore());
    });
  }
  function eo(e, t, a, s, n, i) {
    for (let h = 0; h < 10; h++)
      ((e.fillStyle = h % 2 ? "#143a22" : "#102e1b"), e.fillRect(26 + (h / 10) * (t - 52), 6, (t - 52) / 10, a - 12));
    const l = e.createRadialGradient(t / 2, a / 2, 10, t / 2, a / 2, t * 0.55);
    (l.addColorStop(0, "rgba(190,255,200,.05)"),
      l.addColorStop(1, "rgba(0,0,0,.16)"),
      (e.fillStyle = l),
      e.fillRect(26, 6, t - 52, a - 12));
    const d = (state.player && state.player.name ? state.player.name.split(" ")[1] : "HOME").slice(0, 7).toUpperCase(),
      c = ((state._oppName || "AWAY").split(" ").pop() || "AWAY").slice(0, 7).toUpperCase();
    let u = e.createLinearGradient(0, 0, 26, 0);
    (u.addColorStop(0, "rgba(38,180,80,.5)"),
      u.addColorStop(1, "rgba(20,90,45,.28)"),
      (e.fillStyle = u),
      e.fillRect(0, 6, 26, a - 12),
      (u = e.createLinearGradient(t - 26, 0, t, 0)),
      u.addColorStop(0, "rgba(150,40,45,.3)"),
      u.addColorStop(1, "rgba(210,55,60,.5)"),
      (e.fillStyle = u),
      e.fillRect(t - 26, 6, 26, a - 12),
      (e.strokeStyle = "rgba(90,230,130,.55)"),
      (e.lineWidth = 1.5),
      e.beginPath(),
      e.moveTo(26, 6),
      e.lineTo(26, a - 6),
      e.stroke(),
      (e.strokeStyle = "rgba(255,90,95,.55)"),
      e.beginPath(),
      e.moveTo(t - 26, 6),
      e.lineTo(t - 26, a - 6),
      e.stroke(),
      e.save(),
      (e.font = "bold 10px Oswald, sans-serif"),
      (e.textAlign = "center"),
      (e.textBaseline = "middle"),
      e.translate(26 / 2, a / 2),
      e.rotate(-Math.PI / 2),
      (e.fillStyle = "rgba(222,255,232,.8)"),
      (e.shadowColor = "rgba(90,230,130,.8)"),
      (e.shadowBlur = 6),
      e.fillText(d, 0, 0),
      e.restore(),
      e.save(),
      e.translate(t - 26 / 2, a / 2),
      e.rotate(Math.PI / 2),
      (e.font = "bold 10px Oswald, sans-serif"),
      (e.textAlign = "center"),
      (e.textBaseline = "middle"),
      (e.fillStyle = "rgba(255,226,226,.8)"),
      (e.shadowColor = "rgba(255,90,95,.8)"),
      (e.shadowBlur = 6),
      e.fillText(c, 0, 0),
      e.restore(),
      (e.strokeStyle = "rgba(255,255,255,.30)"),
      (e.lineWidth = 1));
    for (let h = 0; h <= 10; h++) {
      const p = 26 + (h / 10) * (t - 52);
      (e.beginPath(), e.moveTo(p, 6), e.lineTo(p, a - 6), e.stroke());
    }
    ((e.fillStyle = "rgba(255,255,255,.32)"),
      (e.font = "600 8px Oswald, sans-serif"),
      (e.textAlign = "center"),
      (e.textBaseline = "middle"));
    for (let h = 1; h <= 9; h++) {
      const p = h <= 5 ? h * 10 : (10 - h) * 10,
        m = 26 + (h / 10) * (t - 52);
      (e.fillText("" + p, m, 19), e.fillText("" + p, m, a - 19));
    }
    e.strokeStyle = "rgba(255,255,255,.14)";
    for (let h = 0; h < 50; h++) {
      const p = 26 + (h / 50) * (t - 52);
      (e.beginPath(),
        e.moveTo(p, a * 0.4),
        e.lineTo(p, a * 0.4 + 3),
        e.moveTo(p, a * 0.6),
        e.lineTo(p, a * 0.6 + 3),
        e.stroke());
    }
    (e.save(),
      (e.strokeStyle = "rgba(90,160,220,.95)"),
      (e.lineWidth = 2),
      (e.shadowColor = "rgba(90,160,220,.9)"),
      (e.shadowBlur = 5),
      e.beginPath(),
      e.moveTo(i(s), 6),
      e.lineTo(i(s), a - 6),
      e.stroke(),
      n != null &&
        Math.abs(n - s) > 0.5 &&
        ((e.strokeStyle = "rgba(240,187,69,.95)"),
        (e.shadowColor = "rgba(240,187,69,.9)"),
        e.beginPath(),
        e.moveTo(i(n), 6),
        e.lineTo(i(n), a - 6),
        e.stroke()),
      e.restore(),
      (e.strokeStyle = "rgba(255,255,255,.5)"),
      (e.lineWidth = 2),
      e.strokeRect(1, 6, t - 2, a - 12));
  }
  function Wa(e, t, a, s, n, i) {
    const r = n.featured,
      l = r ? 8 : 6;
    (n.whiff && (e.globalAlpha = 0.85),
      e.beginPath(),
      e.ellipse(t, a + l * 0.8, l * 0.9, l * 0.34, 0, 0, 7),
      (e.fillStyle = "rgba(0,0,0,.35)"),
      e.fill(),
      r &&
        (e.save(),
        (e.shadowColor = "rgba(240,187,69,.95)"),
        (e.shadowBlur = 10),
        e.beginPath(),
        e.arc(t, a, l + 3.5, 0, 7),
        (e.strokeStyle = "#ffe9ad"),
        (e.lineWidth = 2.2),
        e.stroke(),
        e.restore()));
    const d = r
        ? s
          ? ["#ffd870", "#c9962a"]
          : ["#ff8a80", "#c03a34"]
        : s
          ? ["#5fce74", "#26723a"]
          : ["#e86560", "#8e2f2c"],
      c = e.createRadialGradient(t - l * 0.35, a - l * 0.45, l * 0.2, t, a, l);
    (c.addColorStop(0, d[0]),
      c.addColorStop(1, d[1]),
      e.beginPath(),
      e.arc(t, a, l, 0, 7),
      (e.fillStyle = c),
      e.fill(),
      (e.lineWidth = 1),
      (e.strokeStyle = "rgba(0,0,0,.45)"),
      e.stroke(),
      e.beginPath(),
      e.ellipse(t - l * 0.25, a - l * 0.45, l * 0.5, l * 0.26, -0.5, 0, 7),
      (e.fillStyle = "rgba(255,255,255,.26)"),
      e.fill(),
      (e.fillStyle = r ? "#241a05" : "rgba(255,255,255,.95)"),
      (e.font = "bold " + (r ? 8 : 6.5) + "px Oswald, sans-serif"),
      (e.textAlign = "center"),
      (e.textBaseline = "middle"),
      e.fillText(n.pos, t, a + 0.5),
      (e.globalAlpha = 1));
  }
  function Cl(e, t, a) {
    (e.beginPath(),
      e.ellipse(t, a, 4, 2.6, 0, 0, 7),
      (e.fillStyle = "#8a4a24"),
      e.fill(),
      (e.strokeStyle = "#fff"),
      (e.lineWidth = 0.6),
      e.stroke());
  }
  function Ll(e, t, a, s) {
    ((e.fillStyle = s > 0 ? "rgba(255,90,95,.45)" : "rgba(90,230,130,.45)"),
      s > 0 ? e.fillRect(t - 26, 6, 26, a - 12) : e.fillRect(0, 6, 26, a - 12));
  }
  function endLive() {
    if (
      (window.GridironPhaser && window.GridironPhaser.cancel(),
      liveCtl && liveCtl.anim && cancelAnimationFrame(liveCtl.anim),
      (liveCtl = null),
      state.player.weekResults && state.player.currentWeek != null)
    ) {
      finishWeekGame();
      return;
    }
    state._oppName = null;
    const e = state.player.seasonStats;
    (ao(e), (state.view = "result"), saveGame(), render());
  }
  function quickSimSeason() {
    const e = state.player.seasonStats;
    (ao(e), completeChallenges(e), (state.view = "result"), saveGame(), render());
  }
  function ao(e) {
    if (e.record) return;
    let t = 0,
      a = 0,
      s = 0;
    (e.sampleGames.forEach(n => {
      const i = 10 + Math.round((n.perf / 100) * 32) + randInt(-4, 7),
        r = 10 + randInt(0, 26);
      ((a += i), (s += r), i > r && t++);
    }),
      (e.record = `${t}-${e.sampleGames.length - t}`),
      (e.ptsFor = a),
      (e.ptsAgainst = s));
  }
  function screenResult() {
    const e = state.player;
    setTimeout(() => {
      const v = e.seasonStats || {};
      v.grade && v.grade[0] === "A"
        ? bigMoment("BREAKOUT SEASON", `${v.grade} grade · scouts are taking notice`, "good")
        : v.grade &&
          ["D", "F"].includes(v.grade[0]) &&
          bigMoment("ROUGH SEASON", `${v.grade} grade · the pressure is rising`, "bad");
    }, 180);
    const t = e.seasonStats,
      a = LEVELS[e.level],
      s = t.newOvr,
      n = e.level >= 8 || (a.key === "nfl" && !((e.nflRings || 0) >= 1));
    a.key === "nfl" && (e.nflRings || 0) >= 1;
    const i = declareChanceV88(e),
      r = maxSeasons() - e.seasonsAtLevel,
      l = minSeasons(),
      d = e.seasonsAtLevel >= l,
      c = r <= 0 && !n,
      u =
        t.grade[0] === "A"
          ? "var(--good)"
          : t.grade[0] === "B"
            ? "var(--gold)"
            : t.grade[0] === "C"
              ? "#c9a13b"
              : "var(--blood)",
      h = e.rivals.filter(v => v.alive),
      p = h.filter(v => v.ovr < s).length,
      m = POS_STATS[e.pos],
      y = m.stats
        .map(v => {
          const j = t.statLine[v.key],
            U = t.target[v.key],
            ce = v.lowerBetter ? j <= U : j >= U,
            Y = fmtLeadVal(v, j),
            _ = fmtLeadVal(v, U);
          return `<div class="tgt-row ${ce ? "hit" : "miss"}">
      <span class="tgt-name">${v.name}</span>
      <span class="tgt-got">${Y}</span>
      <span class="tgt-sep">/</span>
      <span class="tgt-need">${_}${v.lowerBetter ? " max" : ""}</span>
      <span class="tgt-check">${ce ? "✓" : "▲"}</span>
    </div>`;
        })
        .join(""),
      C = i >= 70 ? "var(--good)" : i >= 45 ? "var(--gold)" : "var(--blood)",
      V = i >= 80 ? "Lock" : i >= 65 ? "Strong" : i >= 45 ? "Coin flip" : i >= 25 ? "Long shot" : "Slim";
    if (
      ((byId("screen").innerHTML = `
    <div class="eyebrow">${a.name} · ${t.training ? PROGRAMS[t.training].name : "Season"} Complete</div>
    <div class="h1">${t.record} Record</div>
    <div class="meta-row" style="margin-bottom:4px">
      <span class="injury-badge" style="background:${u === "var(--blood)" ? "rgba(178,59,59,.18)" : "rgba(107,191,89,.15)"};border-color:${u};color:${u}">SEASON GRADE ${t.grade}</span>
      ${t.injuries > 0 ? `<span class="injury-badge">🩹 ${t.injuries} injury${t.injuries > 1 ? "ies" : ""}</span>` : '<span class="injury-badge" style="background:rgba(107,191,89,.15);border-color:var(--good);color:var(--good)">🩹 Healthy season</span>'}
    </div>

    ${
      !n && d
        ? `<div class="card" style="border-color:var(--gold);background:linear-gradient(180deg,rgba(240,187,69,.14),rgba(15,21,32,.9))">
      <div style="display:flex;align-items:center;gap:12px">
        <div style="font-size:30px">${e.level === 5 ? "🎓" : "⬆️"}</div>
        <div style="flex:1">
          <div style="font-family:'Oswald';font-weight:700;font-size:17px;color:var(--gold)">${c ? "DECISION TIME" : "PROMOTION AVAILABLE"}</div>
          <div class="small">You've earned a shot at <b>${LEVELS[e.level + 1].name}</b> — <b style="color:${C}">${Math.round(i)}%</b> odds. <b style="color:var(--blood)">One shot</b> — miss the call-up and the career ends here. ${c ? "This is your final season at this level anyway." : "Play on to raise your odds, or declare below."}</div>
        </div>
      </div>
    </div>`
        : ""
    }

    <div class="card tight">
      <div class="l" style="font-size:11px;color:var(--chalk-dim);letter-spacing:2px;margin-bottom:6px">🏟️ TEAM SEASON</div>
      <div style="display:flex;justify-content:space-between;align-items:center;font-family:'Barlow Condensed';font-size:15px">
        <span>Record <b style="color:var(--gold);font-family:'Oswald'">${t.record}</b></span>
        <span>PF <b style="color:var(--good);font-family:'Oswald'">${t.ptsFor || 0}</b></span>
        <span>PA <b style="color:#e08a8a;font-family:'Oswald'">${t.ptsAgainst || 0}</b></span>
        <span>${t.winPct >= 0.7 ? "🔥 " + (t.winPct >= 0.9 ? "Title run" : "Winning season") : t.winPct < 0.35 ? "Rebuilding year" : "Even season"}</span>
      </div>
      ${t.teamBonus > 0 ? `<div class="threshold-note" style="margin-top:6px;color:var(--good)">🏆 Team success bonus: <b>+${t.teamBonus} upgrade points</b> for how the team performed.</div>` : ""}
    </div>

    ${
      t.contractResults && t.contractResults.length
        ? `<div class="card tight" style="border-color:rgba(90,208,201,.45)">
      <div class="l" style="font-size:10px;color:#5ad0c9;letter-spacing:2px;margin-bottom:6px">📜 SEASON CONTRACTS</div>
      ${t.contractResults.map(v => `<div class="contract-row ${v.ok ? "ok" : "miss"}"><span>${v.ok ? "✅" : "✖"} ${v.desc}</span><b>${v.ok ? "+" + v.paid + " PP" : "—"}</b></div>`).join("")}
    </div>`
        : ""
    }
    ${t.salary ? `<div class="card tight" style="border-color:rgba(87,224,122,.4)"><div class="small center">💵 UFF contract year ${e.nflSeasons || 1}: <b style="color:#57e07a">+${t.salary} PP salary</b>${e.age >= 30 ? ' · <span style="color:#e08a8a">⏳ age ' + e.age + " — the decline has begun</span>" : ""}</div></div>` : ""}
    ${t.beatNemesis && e.nemesis ? `<div class="card tight" style="border-color:rgba(255,90,95,.4)"><div class="small center">😈 You out-produced <b>${e.nemesis.name}</b> nationally — <b style="color:var(--gold)">+PP</b> and he knows it. (${e.nemesis.beaten}× and counting)</div></div>` : ""}
    ${
      t.playoffs && t.playoffs.champion
        ? `<div class="card" style="border-color:var(--gold);background:linear-gradient(180deg,rgba(240,187,69,.20),rgba(15,21,32,.92))">
      <div class="center" style="font-family:'Oswald';font-weight:700;font-size:19px;color:var(--gold)">💍 CHAMPIONS!</div>
      <div class="small center" style="margin-top:4px">${teamName(e)} win it all${t.playoffs.titlePP ? ` — <b style="color:var(--gold)">+${t.playoffs.titlePP} Prestige Points</b> banked instantly` : ""}.${t.playoffs.ringEarned ? ` <b style="color:#ff9b93">💍 CHAMPIONSHIP RING MINTED${state.chaosUnlocked && (state.rings || 0) <= 1 ? " — CHAOS MODE UNLOCKED!" : ""}</b>` : ""}</div>
    </div>`
        : t.playoffs && t.playoffs.qualified
          ? `<div class="card tight"><div class="small center">🎟️ Made the playoffs — ${t.playoffs.roundsWon > 0 ? `won ${t.playoffs.roundsWon} round${t.playoffs.roundsWon > 1 ? "s" : ""} before falling short` : "knocked out in round one"}. Every playoff win paid bonus points.</div></div>`
          : t.playoffs && t.playoffs.rounds > 0
            ? '<div class="card tight"><div class="small center" style="color:var(--chalk-dim)">Missed the playoffs (need a 60%+ win rate). Team success = bonus points, growth, and titles.</div></div>'
            : ""
    }

    ${
      t.awards.length
        ? `<div class="card tight" style="border-color:var(--gold)">
      <div class="l" style="font-size:11px;color:var(--gold);letter-spacing:2px;margin-bottom:6px">🏆 AWARDS & HONORS</div>
      ${t.awards.map(v => `<div style="font-family:'Oswald';font-size:16px;margin:2px 0">${v.icon} ${v.name}</div>`).join("")}
    </div>`
        : ""
    }

    ${
      n
        ? `
    <div class="card"><div class="threshold-note" style="color:var(--gold)">🏆 You're in the UFF. Keep stacking pro seasons and building your legend.</div></div>`
        : `
    <div class="h2">Season Stats vs. Promotion Target</div>
    <div class="card">
      <div class="tgt-head"><span class="tgt-name">${m.label}</span><span class="tgt-got">You</span><span class="tgt-sep"></span><span class="tgt-need">Target</span><span class="tgt-check"></span></div>
      ${y}
      <div class="divider" style="margin:10px 0"></div>
      <div class="chance-box">
        <div class="chance-num" style="color:${C}">${Math.round(i)}%</div>
        <div class="chance-info">
          <div class="chance-label" style="color:${C}">${V} shot at ${LEVELS[e.level + 1].name}</div>
          <div class="small">Your odds to make the jump if you declare now, based on how your production stacks up to the promotion target.</div>
        </div>
      </div>
      <div class="chance-bar"><i style="width:${i}%;background:${C}"></i></div>
    </div>`
    }

    <div class="h2">Development</div>
    <div class="card tight">
      <div class="progress-label"><span>Overall Rating</span><span><b>${s}</b></span></div>
      <div class="bar"><i style="width:${clamp99(s, 0, 100)}%"></i></div>
      <div class="threshold-note mt" style="margin-top:8px">All attributes received performance-based development. <b style="color:var(--gold)">${t.training && PROGRAMS[t.training].focus ? "Priority training" : "Balanced training"}</b> added only a modest extra boost.</div>
      <div style="display:grid;grid-template-columns:1fr auto;gap:5px 10px;margin-top:10px">
        ${ATTR_KEYS.map(v => {
          const j = t.attrsBefore ? t.attrsBefore[v] : e.attrs[v] - (t.gains[v] || 0),
            U = t.attrsAfter ? t.attrsAfter[v] : e.attrs[v],
            ce = (t.priorityStats || []).includes(v);
          return `<div style="font-size:13px;color:${ce ? "var(--gold)" : "var(--chalk-dim)"}">${ATTR_INFO[v].icon} ${ATTR_INFO[v].name}${ce ? " · PRIORITY" : ""}</div><div style="font-family:Oswald;font-size:13px;color:${U > j ? "var(--good)" : "var(--chalk-dim)"}">${Math.round(j)} → ${Math.round(U)}${U > j ? ` <small>(+${Math.round(U - j)})</small>` : ""}</div>`;
        }).join("")}
      </div>
      ${h.length ? `<div class="threshold-note" style="margin-top:6px">🥊 You outrank <b style="color:var(--gold)">${p}/${h.length}</b> position rivals.</div>` : ""}
    </div>

    ${
      t.standoutGames > 0 || t.dominantGames > 0
        ? `<div class="card tight" style="border-color:var(--good)">
      <div class="l" style="font-size:11px;color:var(--good);letter-spacing:2px;margin-bottom:6px">💪 GAME PERFORMANCE REWARDS</div>
      <div style="font-family:'Barlow Condensed';font-size:14px;line-height:1.5">
        ${t.dominantGames > 0 ? `<div><b style="color:var(--gold)">${t.dominantGames}</b> dominant game${t.dominantGames > 1 ? "s" : ""} (90+) → <span style="color:var(--good)">bonus development + ${t.dominantGames} upgrade pt${t.dominantGames > 1 ? "s" : ""}</span></div>` : ""}
        ${t.standoutGames > 0 ? `<div><b style="color:var(--gold)">${t.standoutGames}</b> standout game${t.standoutGames > 1 ? "s" : ""} (80+) → <span style="color:var(--good)">stronger performance-based development</span></div>` : ""}
        ${t.tier ? `<div style="color:var(--chalk-dim);margin-top:2px">Playing at ${t.tier.icon} ${t.tier.name} provided a modest development bonus.</div>` : ""}
      </div>
    </div>`
        : ""
    }

    <div class="pts-banner">
      <div><span class="n">+${t.pointsEarned}</span> <span class="l">UPGRADE POINTS EARNED</span>${t.perfPoints > 0 ? `<div style="font-size:11px;color:var(--good)">includes +${t.perfPoints} from big games</div>` : ""}</div>
      <div style="font-family:'Oswald';font-size:15px;color:var(--gold)">${e.points} total</div>
    </div>

    ${n ? "" : `<div class="small center" style="margin-top:4px">${d ? (c ? '<span class="miss">⚠ Final season at this level — you must declare now. Miss the roll and your career ends here.</span>' : `You have <b>${r}</b> season${r > 1 ? "s" : ""} left to raise your stats before you're forced to declare.`) : `<span style="color:var(--gold)">📚 You're still in ${a.grade.toLowerCase()} — play <b>${l - e.seasonsAtLevel}</b> more season${l - e.seasonsAtLevel > 1 ? "s" : ""} here before you can move up.</span>`}</div>`}
  `),
      n)
    ) {
      byId("dock").innerHTML = `
      ${e.points > 0 ? `<button class="btn" onclick="go('upgrade')">Spend ${e.points} Upgrade Points ▸</button><div style="height:8px"></div>` : ""}
      <button class="btn secondary" onclick="go('hub')">Back to Career</button>`;
      return;
    }
    const P = r > 0 && e.level >= 5;
    byId("dock").innerHTML = `
    ${e.points > 0 ? `<button class="btn secondary" onclick="go('upgrade')">Spend ${e.points} Upgrade Points</button><div style="height:8px"></div>` : ""}
    ${
      d
        ? `<button class="btn" onclick="declareAdvance()">${e.level === 5 ? "🎓 Declare for the Draft" : "Declare for " + LEVELS[e.level + 1].name} · ${Math.round(i)}%${e.declareBonus ? " 😤" : ""}</button>
         <div class="small center declare-stakes-v150" style="margin-top:6px;color:var(--blood)">⚠️ One shot — miss the roll and the career ends here.</div>
         ${P ? `<div style="height:8px"></div><button class="btn ghost" onclick="go('hub')">${e.level === 5 ? "Stay in School — Play Another Year" : "Play Another Season"}</button>` : ""}`
        : `<button class="btn" onclick="go('hub')">Continue — Play Next Season ▸</button>`
    }
    <div style="height:8px"></div>
    <button class="btn ghost" onclick="setStatsTab('leaders');go('stats')">📊 See National Leaders — where did your season rank?</button>
  `;
  }
  function declareAdvance() {
    const e = state.player,
      a = declareChanceV88(e);
    Math.random() * 100 < a ? advanceLevel() : failDeclareV77(e, a);
  } /* ===== v77 THE DECLARE IS THE CAREER — one shot, then the epitaph =====
   * Failing a declare used to be a slap on the wrist: you banked a Determination
   * bonus, played another season and took another swing, and the career only
   * actually ended when the level ran out of seasons. That made the single
   * biggest decision in the game the cheapest one — there was never a reason NOT
   * to declare the moment the button lit up. A failed declare now ENDS the
   * career on the spot, and this screen becomes its epitaph.
   *
   * An epitaph needs a record to read from, and the game kept none. `seasonStats`
   * is only the season just finished and `career` holds one row per LEVEL, so
   * "your best season" was not a question the save could answer. seasonLogV77 is
   * that missing archive — one compact row per finished season, written once from
   * fs() — and both the career totals and the best-season card read it. A save
   * from before this exists simply has a short log, and every screen below
   * degrades to whatever is in it.
   *
   * Rates are the trap in a career total: summing eight seasons of yards-per-carry
   * produces 38.4. Anything flagged `rate` in Ne[pos].stats is AVERAGED over the
   * seasons it was measured in; everything else is summed. */
  /* ===== v122 THE SEASON DEBRIEF =====
   * The season report card is a wall of numbers and no verdict: a grade, a record, a promotion bar
   * and fourteen attribute rows, none of which answer the only question a player actually has —
   * what happened, and what do I do about it. This is the answer, computed once when the season
   * rolls and handed to the coach to say out loud.
   *
   * It is built from what the season ACTUALLY did, not from a mood: the week rows carry the game
   * rating, the fatigue at each kickoff, the weekly-plan roll's band, the game-plan swing, the snaps
   * and the injuries, and they are cleared the moment the roll finishes — so `capV122()` reads them
   * BEFORE `fs()` runs and `buildV122()` joins that to the seasonStats the roll produced. Every note
   * carries the number it was measured from, and `weight` is how loudly it mattered, so the coach can
   * take the loudest three or four and leave the rest.
   *
   * `window.__DEBRIEF_V122` is the hook: `.get()` is the latest debrief (or null), `.seen(n)` marks a
   * season read so it is said once, `.build(pl)` recomputes for a check. */
  const DEBRIEF_SEEN_V122 = "rib.debriefSeen.v122";
  function capV122(pl) {
    const wk = ((pl && pl.weekResults) || []).filter(w => w && w.played);
    if (!wk.length) return null;
    const fat = wk.map(w => Math.round((w.healthAtKickoffV11 || {}).fatigue || 0));
    const bands = wk.map(w => (w.wheelV85 && w.wheelV85.band) || null).filter(Boolean);
    const swings = wk.map(w => Number((w.matchupV11 || {}).perf) || 0);
    return {
      games: wk.length,
      perf: wk.map(w => Math.round(Number(w.perf) || 0)),
      fatKick: fat,
      fatEnd: Math.round((wk[wk.length - 1].conditionResultV11 || {}).fatigue || fat[fat.length - 1] || 0),
      fatMean: Math.round(fat.reduce((a, b) => a + b, 0) / fat.length),
      wornGames: fat.filter(v => v >= TU("wornFatigue", 70)).length,
      slopeGames: fat.filter(v => v > TU("fatSlopeFrom", 40)).length,
      injured: wk.filter(w => w.injured).length,
      satOut: wk.filter(w => w.satOut).length,
      green: bands.filter(b => b === "green").length,
      red: bands.filter(b => b === "red").length,
      rolls: bands.length,
      swingUp: swings.filter(v => v > 0).length,
      swingDn: swings.filter(v => v < 0).length,
      swingNet: Math.round(swings.reduce((a, b) => a + b, 0)),
      shares: wk.map(w => Number(w.snapShare) || 0)
    };
  }
  /* the one number a note is ranked by: how far from ordinary it was */
  function buildV122(pl, cap) {
    if (!pl || !pl.seasonStats) return null;
    const t = pl.seasonStats,
      A_ = LEVELS[pl.level] || {},
      NX = LEVELS[pl.level + 1] || null,
      ovr = playerOvr(pl);
    const avg = Math.round(Number(t.avg) || 0),
      exp = Math.round(Number(pl.performanceExpectationV12) || 0);
    let rank = null;
    try {
      rank = window.__RANK_V52 ? window.__RANK_V52.sn(pl, ovr) : null;
    } catch (e) {}
    const left = Math.max(0, (typeof maxSeasons == "function" ? maxSeasons(pl) : 0) - (pl.seasonsAtLevel || 0));
    const chance = Math.round(Number(t.chance) || 0);
    const notes = [];
    const push = (k, w, head, body) => {
      if (w > 0) notes.push({ k, weight: Math.round(w * 10) / 10, head, body });
    };

    // ---- the body: did fatigue linger, and what did it cost on the field
    if (cap && cap.games) {
      const m = cap.fatMean,
        end = cap.fatEnd,
        cost = Math.round((1 - fatigueMulV120(m, false)) * 100);
      if (cap.wornGames > 0)
        push(
          "fatigue",
          4 + cap.wornGames,
          "Fatigue ran you down",
          `You played ${cap.wornGames} of ${cap.games} games worn out — every stat down hard on those days — and finished the year at ${end} fatigue. Sit a week next time.`
        );
      else if (cap.slopeGames >= Math.ceil(cap.games / 2))
        push(
          "fatigue",
          2.5 + cost / 6,
          "Fatigue dragged on you",
          `${cap.slopeGames} of ${cap.games} games started tired. About ${cost}% off everything, all year. It never broke you, it just shaved you.`
        );
      else
        push(
          "fatigue",
          1,
          "You stayed fresh",
          `Your legs held up — ${end} fatigue at the end. Fresh is plus five percent on every stat, and it is free.`
        );
      if (cap.injured || cap.satOut)
        push(
          "injury",
          2 + cap.injured * 1.5 + cap.satOut * 2,
          "The body cost you time",
          `${cap.injured} knock${cap.injured === 1 ? "" : "s"}${cap.satOut ? ` and ${cap.satOut} game${cap.satOut === 1 ? "" : "s"} on the sideline` : ""}. Durability buys availability, and availability is production.`
        );
    }

    // ---- expectations: the bar the grade was measured against
    if (exp > 0) {
      const d = avg - exp;
      push(
        "expect",
        Math.min(6, 1 + Math.abs(d) / 4),
        d >= 6 ? "You beat the bar" : d <= -6 ? "You fell short of the bar" : "You met the bar",
        d >= 6
          ? `They wanted about ${exp} out of you and you played ${avg}. That is why the grade reads ${String(t.grade || "").toUpperCase()}.`
          : d <= -6
            ? `The bar for you was about ${exp} and you played ${avg}. Not a disaster — but that gap is the grade.`
            : `They wanted about ${exp}, you played ${avg}. Right where they expected, no more.`
      );
    }

    // ---- luck: the rolls you did not control
    if (cap && cap.rolls) {
      const d = cap.green - cap.red,
        net = cap.swingNet;
      /* two different dice: the weekly-plan roll (green/red) and the game-plan swing. They can disagree,
       * so the sentence is built from both with the conjunction their signs actually earn. */
      const rolls = `${cap.red} of your ${cap.rolls} weekly plans backfired, ${cap.green} paid`;
      const plans =
        net > 0
          ? `the game plans added ${net} rating`
          : net < 0
            ? `the game plans cost you ${Math.abs(net)} rating`
            : "the game plans were a wash";
      const agree = (x, y) => (x >= 0 === y >= 0 ? "and" : "though");
      if (d <= -2 || net <= -6)
        push(
          "luck",
          2.5 + Math.abs(d) + Math.abs(net) / 6,
          "The dice were against you",
          `${rolls}, ${agree(d, net)} ${plans}. That part was not on you.`
        );
      else if (d >= 2 || net >= 6)
        push(
          "luck",
          2 + d + Math.abs(net) / 6,
          "The dice were kind",
          `${cap.green} of ${cap.rolls} weekly plans paid off, ${agree(d, net)} ${plans}. Some of this year was luck — do not bank on it twice.`
        );
      else
        push(
          "luck",
          1,
          "The rolls were a wash",
          `${cap.green} paid, ${cap.red} backfired, ${plans}. The year you got is the year you earned.`
        );
    }

    // ---- the ladder: where you rank and whether you are on track
    const gap = Math.round((A_.need || 0) - ovr);
    if (rank) {
      const top = Math.max(1, Math.round((rank.rank / Math.max(1, rank.of)) * 100));
      push(
        "rank",
        5,
        `#${rank.rank.toLocaleString()} in the country`,
        top <= 50
          ? `Top ${top}% of ${rank.of.toLocaleString()}, and #${(rank.posRank || 0).toLocaleString()} at your position. Rank follows production, production follows snaps.`
          : `Out of ${rank.of.toLocaleString()}, with #${(rank.posRank || 0).toLocaleString()} at your position. Nothing on the board yet — rank follows production, production follows snaps.`
      );
    }
    const room =
      left > 0 ? `${left} season${left === 1 ? "" : "s"} left here` : "no seasons left — it is this roll or nothing";
    push(
      "track",
      6,
      gap <= 0 ? `On track for ${NX ? NX.name : "the next level"}` : `${gap} rating short of the bar`,
      gap <= 0
        ? `The bar is ${A_.need} and you are at ${ovr}. Your call-up reads about ${chance}%, ${room}.`
        : chance >= 70
          ? `The bar is ${A_.need} and you are at ${ovr} — but this level takes nearly everyone, so your call-up still reads about ${chance}%. ${room[0].toUpperCase() + room.slice(1)}.`
          : `The bar is ${A_.need} and you are at ${ovr}. Your call-up reads about ${chance}%, ${room}.`
    );

    // ---- the snaps: v120's own loop, said plainly
    const share = Math.round((Number(t.snapShare) || 0) * 100),
      trust = Math.round(Number(t.coachTrust) || pl.coachTrust || 0);
    if (share)
      push(
        "snaps",
        share < 40 ? 4 : 2,
        share < 40 ? "You did not play enough" : "You earned your snaps",
        share < 40
          ? `${share}% of your unit's snaps at ${trust} coach trust. You cannot post numbers you never got on the field for. Play well and I hand you more.`
          : `${share}% of the snaps at ${trust} coach trust. That is a real role, and the production follows it.`
      );

    // ---- what to work on: the game's own recommendation, named
    let focus = null;
    try {
      const key = recommendTraining(pl),
        prog = PROGRAMS[key] || null,
        w = (POSITIONS[pl.pos] || {}).w || {};
      const weak = Object.keys(w)
        .filter(k => ATTR_INFO[k])
        .sort((a, b) => pl.attrs[a] / Math.max(1, w[a]) - pl.attrs[b] / Math.max(1, w[b]))[0];
      focus = {
        key: key,
        program: (prog && prog.name) || key,
        stat: weak || null,
        statName: weak ? ATTR_INFO[weak].name : null,
        why:
          key === "conditioning"
            ? "Your body is the thing holding you back — it is durability and legs before anything else."
            : weak
              ? `${ATTR_INFO[weak].name} is the weakest number your position actually reads, and it is the cheapest rating you can buy.`
              : "Spend it on the stats your position weights heaviest, heaviest first."
      };
    } catch (e) {
      focus = null;
    }

    notes.sort((a, b) => b.weight - a.weight);
    const g = String(t.grade || "").toUpperCase();
    return {
      season: pl.totalSeasons || 0,
      grade: g,
      record: t.record || "",
      avg: avg,
      exp: exp,
      ovr: ovr,
      level: A_.name || "",
      next: NX ? NX.name : null,
      need: A_.need || 0,
      chance: chance,
      seasonsLeft: left,
      rank: rank ? { rank: rank.rank, of: rank.of, pct: rank.pct, posRank: rank.posRank } : null,
      head: `${t.record || ""}${t.playoffs && t.playoffs.champion ? ", and a ring" : ""} — ${/^[AEF]/.test(g) ? "an" : "a"} ${g} season, ${avg} average rating.`,
      focus: focus,
      notes: notes,
      cap: cap || null,
      /* v134: what the coach's last line is built from -- the grade, the bar and where he landed against it,
       * how much the sheet grew, and whether he played the year hurt or worn */
      grade: String(t.grade || "").toUpperCase(),
      avg: avg,
      exp: exp,
      gainSum: Object.values(t.gains || {}).reduce((a, b) => a + (Number(b) > 0 ? Number(b) : 0), 0),
      worn: cap ? cap.wornGames || 0 : 0,
      hurt: cap ? (cap.injured || 0) + (cap.satOut || 0) : 0,
      games: cap ? cap.games || 0 : 0,
      won: !!(t.playoffs && t.playoffs.champion)
    };
  }
  /* the roll clears the week rows, so the capture has to happen in front of it */
  const fsV122 = finishSeasonGames;
  finishSeasonGames = function () {
    let cap = null;
    try {
      cap = capV122(state.player);
    } catch (e) {}
    fsV122();
    try {
      const pl = state.player;
      if (pl) pl.debriefV122 = buildV122(pl, cap);
    } catch (e) {}
  };
  window.__DEBRIEF_V122 = {
    get() {
      try {
        return (state && state.player && state.player.debriefV122) || null;
      } catch (e) {
        return null;
      }
    },
    seen(n) {
      try {
        localStorage.setItem(DEBRIEF_SEEN_V122, String(n));
      } catch (e) {}
    },
    lastSeen() {
      try {
        return localStorage.getItem(DEBRIEF_SEEN_V122);
      } catch (e) {
        return null;
      }
    },
    build(pl) {
      try {
        return buildV122(pl || state.player, capV122(pl || state.player));
      } catch (e) {
        return null;
      }
    },
    cap(pl) {
      try {
        return capV122(pl || state.player);
      } catch (e) {
        return null;
      }
    },
    key: DEBRIEF_SEEN_V122
  };
  const SEASON_LOG_MAX = 40;
  function logSeasonV77(e, t) {
    if (!e || !t) return;
    const pl = t.playoffs || {},
      log = (e.seasonLogV77 = e.seasonLogV77 || []);
    log.push({
      n: e.totalSeasons || log.length + 1,
      level: e.level,
      levelName: (LEVELS[e.level] || {}).name || "",
      pos: e.pos,
      age: e.age,
      ovr: t.newOvr || playerOvr(e),
      avg: t.avg || 0,
      grade: t.grade || "",
      record: t.record || "",
      wins: t.teamWins || 0,
      losses: t.teamLosses || 0,
      games: (t.teamWins || 0) + (t.teamLosses || 0) + (pl.games || []).length,
      champion: !!pl.champion,
      roundsWon: pl.roundsWon || 0,
      statLine: Object.assign({}, t.statLine || {}),
      awards: (t.awards || []).map(a => ({ icon: a.icon, name: a.name }))
    });
    if (log.length > SEASON_LOG_MAX) e.seasonLogV77 = log.slice(-SEASON_LOG_MAX);
  }
  function careerTotalsV77(e) {
    const log = (e && e.seasonLogV77) || [],
      cfg = POS_STATS[e.pos] || { stats: [] },
      sum = {},
      cnt = {};
    const T = {
      seasons: log.length,
      games: 0,
      wins: 0,
      losses: 0,
      titles: 0,
      playoffWins: 0,
      awards: 0,
      bestAvg: 0,
      peakOvr: 0,
      lines: []
    };
    for (const s of log) {
      T.games += s.games || 0;
      T.wins += s.wins || 0;
      T.losses += s.losses || 0;
      if (s.champion) T.titles++;
      T.playoffWins += s.roundsWon || 0;
      T.awards += (s.awards || []).length;
      T.bestAvg = Math.max(T.bestAvg, s.avg || 0);
      T.peakOvr = Math.max(T.peakOvr, s.ovr || 0);
      for (const st of cfg.stats) {
        const v = s.statLine && s.statLine[st.key];
        if (v == null || isNaN(+v)) continue;
        sum[st.key] = (sum[st.key] || 0) + +v;
        cnt[st.key] = (cnt[st.key] || 0) + 1;
      }
    }
    T.peakOvr = Math.max(T.peakOvr, e.peakOvr || 0, playerOvr(e));
    for (const st of cfg.stats) {
      if (!cnt[st.key]) continue;
      const av = sum[st.key] / cnt[st.key];
      T.lines.push({
        key: st.key,
        name: st.name,
        rate: !!st.rate,
        def: st,
        total: st.rate ? av : sum[st.key],
        per: av
      });
    }
    return T;
  }
  /* What made a season the BEST one. Production carries it (avg is the season
   * grade's own input), then the things a player actually remembers: a ring, a
   * playoff run, the hardware, wins — and the LEVEL, because a good year in the
   * league outranks the same numbers in high school. */
  function seasonScoreV77(s) {
    return (
      (s.avg || 0) +
      (s.champion ? 12 : 0) +
      (s.roundsWon || 0) * 3.5 +
      (s.awards || []).length * 2.5 +
      (s.wins || 0) * 0.6 +
      (s.level || 0) * 1.5
    );
  }
  function bestSeasonV77(e) {
    const log = (e && e.seasonLogV77) || [];
    return log.length ? log.reduce((a, b) => (seasonScoreV77(b) > seasonScoreV77(a) ? b : a)) : null;
  }
  /* The one exit. Both declare paths (the hub button and the season-result card)
   * land here on a miss: no Determination bonus, no season left over — the odds
   * that failed are kept only so the epitaph can state them. */
  function failDeclareV77(e, chance) {
    e.declareFailV77 = { chance: chance, level: e.level };
    ((state.view = "declineResult"), saveGame(), render());
  }
  function statRowsV77(pos, line, per) {
    const cfg = POS_STATS[pos] || { stats: [] };
    return cfg.stats
      .map(st => {
        const v = line && line[st.key];
        if (v == null || isNaN(+v)) return "";
        const shown = Math.round(+v).toLocaleString();
        const sub =
          per && per[st.key] != null
            ? `<span class="small" style="opacity:.65;margin-left:6px">${st.rate ? "" : fmtLeadVal(st, per[st.key]) + "/season"}</span>`
            : "";
        return `<div style="display:flex;justify-content:space-between;align-items:baseline;padding:3px 0;font-family:'Barlow Condensed';font-size:15px;border-bottom:1px solid rgba(255,255,255,.05)"><span>${st.name}${sub}</span><b style="font-family:'Oswald';font-size:17px;color:var(--gold)">${shown}</b></div>`;
      })
      .join("");
  }
  function careerCardsV77(e) {
    const T = careerTotalsV77(e),
      best = bestSeasonV77(e),
      per = {};
    T.lines.forEach(l => {
      if (!l.rate) per[l.key] = l.per;
    });
    const line = {};
    T.lines.forEach(l => (line[l.key] = l.total));
    const totals = T.seasons
      ? `
    <div class="h2">Career Totals</div>
    <div class="card">
      <div class="statline">
        <div class="statbox"><div class="n">${T.seasons}</div><div class="l">Seasons</div></div>
        <div class="statbox"><div class="n">${T.peakOvr}</div><div class="l">Peak OVR</div></div>
        <div class="statbox"><div class="n">${T.wins}-${T.losses}</div><div class="l">Team Record</div></div>
      </div>
      <div style="margin-top:10px">${statRowsV77(e.pos, line, per)}</div>
      <div class="meta-row" style="margin-top:10px;justify-content:center;flex-wrap:wrap;gap:6px">
        ${T.titles ? `<span class="injury-badge" style="background:rgba(240,187,69,.15);border-color:var(--gold);color:var(--gold)">💍 ${T.titles} title${T.titles > 1 ? "s" : ""}</span>` : ""}
        ${T.playoffWins ? `<span class="injury-badge">🎟️ ${T.playoffWins} playoff win${T.playoffWins > 1 ? "s" : ""}</span>` : ""}
        ${T.awards ? `<span class="injury-badge">🏅 ${T.awards} award${T.awards > 1 ? "s" : ""}</span>` : ""}
        ${best ? `<span class="injury-badge">📊 best year graded ${best.grade}</span>` : ""}
      </div>
    </div>`
      : `<div class="card tight"><div class="small center">No completed season to total up — the climb ended before a full year was in the books.</div></div>`;
    const bs = best
      ? `
    <div class="h2">Best Season</div>
    <div class="card" style="border-color:var(--gold);background:linear-gradient(180deg,rgba(240,187,69,.12),rgba(15,21,32,.9))">
      <div style="display:flex;justify-content:space-between;align-items:baseline">
        <div>
          <div style="font-family:'Oswald';font-weight:700;font-size:17px;color:var(--gold)">${best.levelName || "Season"} · age ${best.age}</div>
          <div class="small">Season ${best.n} · ${best.record || "—"} record${best.champion ? " · 💍 champions" : best.roundsWon ? ` · ${best.roundsWon} playoff win${best.roundsWon > 1 ? "s" : ""}` : ""}</div>
        </div>
        <div style="text-align:right">
          <div style="font-family:'Oswald';font-weight:700;font-size:22px;color:var(--gold)">${best.grade || "—"}</div>
          <div class="l" style="font-size:10px;letter-spacing:1px">GRADE · OVR ${best.ovr}</div>
        </div>
      </div>
      <div style="margin-top:10px">${statRowsV77(best.pos || e.pos, best.statLine)}</div>
      ${(best.awards || []).length ? `<div class="meta-row" style="margin-top:10px;flex-wrap:wrap;gap:6px">${best.awards.map(a => `<span class="injury-badge">${a.icon} ${a.name}</span>`).join("")}</div>` : ""}
    </div>`
      : "";
    return totals + bs;
  }
  // debug surface: the archive and both readings of it, for scripts/declarecheck.mjs
  window.__CAREER_V77 = {
    totals: careerTotalsV77,
    best: bestSeasonV77,
    score: seasonScoreV77,
    log: () => (state.player && state.player.seasonLogV77) || []
  };
  function Ol() {
    const e = state.player,
      t = LEVELS[e.level],
      nx = LEVELS[e.level + 1],
      ch = e.declareFailV77 && e.declareFailV77.chance;
    ((byId("screen").innerHTML = `
    <div class="banner fail">
      <div class="big-emoji">🥀</div>
      <div class="bt">Didn't Make the Cut</div>
      <div class="bs">${nx ? `The coaches at <b>${nx.name}</b> went another direction.` : "The call never came."} ${escHtml(e.name)}'s climb ends at <b>${t.name}</b>.</div>
    </div>
    <div class="card tight">
      <div class="small center">You declared${ch != null ? ` at <b style="color:var(--gold)">${Math.round(ch)}%</b>` : ""} and it didn't land. A declare is <b>one shot</b> — there is no next season to take another.</div>
      <div class="meta-row" style="justify-content:center;flex-wrap:wrap;gap:6px;margin-top:8px">
        <span class="injury-badge">🏟️ reached ${t.name}</span>
        <span class="injury-badge">final OVR ${playerOvr(e)}</span>
        <span class="injury-badge">age ${e.age}</span>
      </div>
    </div>
    ${careerCardsV77(e)}
    <div class="h2">Career Log</div>
    <div class="card"><div class="career-log">${e.career.map(h => `<div><span class="lvl-done">✓</span> ${h.level} — reached OVR ${h.ovr} at age ${h.age}</div>`).join("")}<div><span style="color:var(--blood)">✗</span> ${t.name} — declare failed at OVR ${playerOvr(e)}, age ${e.age}</div></div></div>
  `),
      (byId("dock").innerHTML = '<button class="btn danger" onclick="endCareer()">See Career Result ▸</button>'));
  }
  function Bl() {
    (state.player, screenUpgrade());
  }
  const UP_GROUPS_V97 = [
    ["PHYSICAL", ["speed", "acceleration", "quickness", "agility", "strength", "jumping", "stamina", "injuryResist"]],
    ["BALL SKILLS", ["catching", "throwing", "tackling", "blocking", "ballControl"]],
    ["MENTAL", ["awareness", "vision", "grit", "discipline"]]
  ];
  window.toggleUpGroupV97 = function (g) {
    const open = window.__upGroupV97 === g;
    window.__upGroupV97 = open ? "" : g;
    document.querySelectorAll(".up-group-v97").forEach(el => {
      el.classList.toggle("on", !open && el.dataset.g === g);
    });
    if (!open) {
      const el = document.querySelector('.up-group-v97[data-g="' + g + '"]');
      try {
        el && el.scrollIntoView({ block: "start", behavior: "smooth" });
      } catch (e) {}
    }
  };
  function screenUpgrade() {
    const e = state.player,
      t = playerOvr(e);
    ((byId("screen").innerHTML = `
    <div class="eyebrow">${e.pos} · Current OVR ${t}</div>
    <div class="h1">Train Your Player</div>
    <div class="sub">Invest points into attributes. Stats marked <span class="weight-tag">KEY</span> matter most for your position and raise your OVR the fastest.</div>
    ${(() => {
      const st = clamp99(Math.round(e.stars || 1), 1, 5),
        bp = Math.round((TU("drStarBase", 0.6) + (st - 1) * TU("drStarStep", 0.0625)) * 100),
        pp = Math.round((TU("drPrestigePct", 0.01) * (state.prestige || 0) + softPctV146()) * 100);
      return `<div class="threshold-note" style="margin-top:8px">📉 <b>Diminishing returns — no hard cap.</b> Each stat costs <b>1 pt</b> up to its <b style="color:var(--gold)">soft cap</b>, then <b>2, 3, 4…</b> per band of ${bandWV146()} above it${bandTopV146() < 1 / 0 ? ` (never more than <b>${bandTopV146()}</b>)` : ""}, and <b>×${wallMultV146()}</b> from <b>${wallAtV146()}</b> on. ★${st} sets your soft caps at <b>${bp}%</b> of ceiling${pp ? ` · Honors add <b style="color:#7fe0a0">+${pp}%</b>` : ""} — more RECRUIT stars (the ★ above) and more HONORS push the cheap zone massively higher. Gold values are past their soft cap.</div>`;
    })()}
    <div class="pts-banner mt" style="margin-top:14px">
      <div><span class="n" id="ptsLeft">${e.points}</span> <span class="l">POINTS TO SPEND</span></div>
      <div style="font-family:'Oswald';font-size:20px;color:var(--gold)" id="liveOvr">OVR ${t}</div>
    </div>
    ${(() => {
      const row = a => {
        const s = POSITIONS[e.pos].w[a] && POSITIONS[e.pos].w[a] >= 0.14,
          n = ATTR_INFO[a].metric;
        return `<div class="up-attr">
          <div class="info"><div class="un">${ATTR_INFO[a].icon} ${ATTR_INFO[a].name}${statInfoBtnV142(a)}${s ? ' <span class="weight-tag">KEY</span>' : ""}</div>
            <div class="desc">${ATTR_INFO[a].desc}</div>
            ${n ? `<div class="up-metric">${n.label}: <b id="mtr-${a}">${n.fmt(e.attrs[a])}</b></div>` : ""}
            <div class="up-cap" id="cap-${a}"></div></div>
          <button class="step" onclick="alloc('${a}',-1)" id="minus-${a}">−</button>
          <div class="uv" id="uv-${a}">${Math.round(e.attrs[a])}</div>
          <button class="step" onclick="alloc('${a}',1)" id="plus-${a}">+</button>
        </div>`;
      };
      /* v97: three folding groups instead of one seventeen-row wall; the group holding the most KEY stats opens first */
      const G = UP_GROUPS_V97.map(g => [g[0], g[1].filter(k => ATTR_KEYS.includes(k))])
          .concat([["OTHER", ATTR_KEYS.filter(k => !UP_GROUPS_V97.some(g => g[1].includes(k)))]])
          .filter(g => g[1].length),
        keyN = g => g[1].filter(k => POSITIONS[e.pos].w[k] && POSITIONS[e.pos].w[k] >= 0.14).length;
      if (window.__upGroupV97 == null || !G.some(g => g[0] === window.__upGroupV97))
        window.__upGroupV97 = G.slice().sort((a, b) => keyN(b) - keyN(a))[0][0];
      return G.map(
        g =>
          `<div class="card up-group-v97 ${g[0] === window.__upGroupV97 ? "on" : ""}" data-g="${g[0]}"><button type="button" class="up-group-head-v97" onclick="toggleUpGroupV97('${g[0]}')"><span>${g[0]}</span><small>${keyN(g) ? keyN(g) + " KEY · " : ""}${g[1].length} stats</small><i>▾</i></button><div class="up-group-body-v97">${g[1].map(row).join("")}</div></div>`
      ).join("");
    })()}
  `),
      refreshAllocButtons(),
      (byId("dock").innerHTML = `
    <div class="btn-row">
      <button class="btn secondary" onclick="autoAllocKey()">⚡ Auto: Key Stats</button>
      <button class="btn secondary" onclick="autoAllocSpread()">⚖️ Auto: Balanced</button>
    </div>
    <div style="height:8px"></div>
    <button class="btn" onclick="doneUpgrade()">Done</button>`));
  }
  function hs(e) {
    return Math.min(attrCap(), (e.potentialCeil || 70) + 12);
  }
  /* ===== v21 DIMINISHING RETURNS — the hard stat wall is GONE. Each stat has a
   * SOFT CAP instead: below it every +1 costs 1 point; above it the price climbs
   * in bands of TU("drBandWidth",10) — 2 pts, then 3, then 4… forever. The soft
   * cap = potential ceiling × (star base % + fixed % per prestige) × the stat's
   * personality ceiling multiplier: a 1★ low-prestige kid pays double early
   * (~60% of ceiling), a 5★ stacked-prestige legend keeps the cheap zone
   * massively higher (85% + 5%/prestige, uncapped). */
  /* v134: the OVR a man would carry with every attribute sitting on its soft cap -- the ceiling THIS
   * year's prices are written against. The main menu's ring fills to it and goes gold past it. */
  function softMaxOvrV134(e) {
    try {
      if (!e || !e.attrs || !e.pos) return 0;
      const a = {};
      ATTR_KEYS.forEach(k => {
        a[k] = drSoftCap(e, k);
      });
      return calcOvr(a, e.pos, e.body);
    } catch (_) {
      return 0;
    }
  } // the OVR AT the caps -- a man past them sits above it, which is the gold lap
  window.__softMaxOvrV134 = softMaxOvrV134;
  window.__treeLvlV134 = k => {
    try {
      return nodeLvl(k) | 0;
    } catch (_) {
      return 0;
    }
  }; /* v134: the sim asks the tree through this, never o.tree */
  function drSoftCap(e, k) {
    const st = clamp99(Math.round(e.stars || 1), 1, 5),
      base = TU("drStarBase", 0.6) + (st - 1) * TU("drStarStep", 0.0625),
      pres = TU("drPrestigePct", 0.01) * (state.prestige || 0) + softPctV146() + gearV147("softCap"),
      m = (e.statCeilV17 && e.statCeilV17[k]) || 1;
    return Math.min(attrCap(), Math.max(20, Math.round(hs(e) * (base + pres) * m)));
  }
  window.__drCostV97 = (e, k) => drCost(e, k);
  function drCost(e, k) {
    const v = e.attrs[k] || 0,
      sc = drSoftCap(e, k),
      base = v < sc ? 1 : Math.min(bandTopV146(), 2 + Math.floor((v - sc) / bandWV146()));
    return v >= wallAtV146() ? base * wallMultV146() : base;
  } /* v97: the 250 wall — every point past it costs five times the band price */ /* v21: auto-alloc pays the same diminishing-return costs, cheapest-first with a
   * round-robin tiebreak — it never buys a 3-pt band while a 1-pt stat is open,
   * which is exactly "stop pushing a stat once it's inefficient vs alternatives". */
  function drAutoAlloc(pool) {
    const e = state.player;
    let n = 0,
      g = 0;
    for (; e.points > 0 && g++ < 5e3;) {
      const open = pool.filter(i => (e.attrs[i] || 0) < attrCap() && drCost(e, i) <= e.points);
      if (!open.length) break;
      const mc = Math.min.apply(
        null,
        open.map(i => drCost(e, i))
      );
      let pick = null;
      for (let j = 0; j < pool.length; j++) {
        const i = pool[(n + j) % pool.length];
        if (open.indexOf(i) >= 0 && drCost(e, i) === mc) {
          pick = i;
          n = (n + j + 1) % pool.length;
          break;
        }
      }
      if (!pick) break;
      (e.attrs[pick]++,
        (e.points -= mc),
        (allocSpent[pick] = (allocSpent[pick] || 0) + 1),
        (allocCosts[pick] = allocCosts[pick] || []).push(mc));
    }
    screenUpgrade();
  }
  function autoAllocKey() {
    const e = state.player,
      a = POSITIONS[e.pos].w;
    drAutoAlloc(
      Object.keys(a)
        .sort((i, r) => a[r] - a[i])
        .slice(0, Math.min(6, Object.keys(a).length))
    );
  }
  function autoAllocSpread() {
    drAutoAlloc(ATTR_KEYS.slice());
  }
  let allocSpent = {},
    allocCosts = {};
  function alloc(e, t) {
    const a = state.player;
    if (t > 0) {
      const c = drCost(a, e);
      if (a.attrs[e] >= attrCap()) {
        showToast("Absolute limit reached");
        return;
      }
      if (a.points < c) {
        showToast(c > 1 ? "Needs " + c + " pts — diminishing returns past " + drSoftCap(a, e) : "No points left");
        return;
      }
      ((a.attrs[e] = Math.round(a.attrs[e]) + 1),
        (a.points -= c),
        (allocSpent[e] = (allocSpent[e] || 0) + 1),
        (allocCosts[e] = allocCosts[e] || []).push(c));
    } else {
      if ((allocSpent[e] || 0) <= 0) return;
      ((a.attrs[e] = Math.round(a.attrs[e]) - 1),
        (a.points += allocCosts[e] && allocCosts[e].length ? allocCosts[e].pop() : 1),
        allocSpent[e]--);
    }
    byId("uv-" + e).textContent = Math.round(a.attrs[e]);
    const n = byId("mtr-" + e),
      i = ATTR_INFO[e].metric;
    (n && i && (n.textContent = i.fmt(a.attrs[e])),
      (byId("ptsLeft").textContent = a.points),
      (byId("liveOvr").textContent = "OVR " + playerOvr(a)),
      refreshAllocButtons());
  }
  function refreshAllocButtons() {
    const e = state.player;
    ATTR_KEYS.forEach(a => {
      const s = byId("plus-" + a),
        n = byId("minus-" + a),
        v = byId("uv-" + a),
        c = drCost(e, a),
        sc = drSoftCap(e, a),
        over = (e.attrs[a] || 0) >= sc;
      (s &&
        ((s.disabled = e.points < c || (e.attrs[a] || 0) >= attrCap()),
        (s.innerHTML = c > 1 ? '+<i class="stepcost' + (c >= 4 ? " deep" : "") + '">' + c + "</i>" : "+"),
        (s.title = over
          ? "Past soft cap " + sc + " — each +1 costs " + c + " pts here"
          : "1 pt per +1 until soft cap " + sc)),
        v && ((v.style.color = over ? (c >= 4 ? "#ff8a80" : "var(--gold)") : ""), (v.title = "Soft cap: " + sc)),
        n && (n.disabled = (allocSpent[a] || 0) <= 0));
      const cp = byId("cap-" + a);
      cp &&
        (cp.innerHTML =
          (e.attrs[a] || 0) >= attrCap()
            ? '<b style="color:#ff8a80">MAXED</b> at the absolute limit ' + attrCap()
            : over
              ? "Past soft cap " +
                sc +
                ' — <b style="color:' +
                (c >= 4 ? "#ff8a80" : c === 3 ? "#ff9a5a" : "var(--gold)") +
                '">' +
                c +
                " pts per +1</b>"
              : "Soft cap " +
                sc +
                ' — <b style="color:#7fe0a0">1 pt per +1</b> for ' +
                wholeNum(sc - (e.attrs[a] || 0)) +
                " more");
    });
  }
  function doneUpgrade() {
    ((allocSpent = {}), (allocCosts = {}), saveGame(), showToast("Player upgraded!"), goView("hub"));
  }
  /* ===== v67 SOFT-CAP LEGIBILITY — the price of a point, stated where the choice is
   * made. v21 gave every stat a soft cap and a rising price above it (2, then 3, then
   * 4 skill points per +1, forever) but only the UPGRADE screen ever said so, and only
   * in a hover title — which on a phone is nothing at all. The season's TRAINING is
   * chosen a screen earlier, and that is the decision the price should inform:
   * committing a season to the Speed Academy is a completely different bet when those
   * four stats cost 1 point each than when they cost 4 and one is already pinned at
   * the absolute limit.
   *
   * So the band is named wherever a stat is: a badge on every focus chip on the
   * training board, a one-line verdict per program, and a permanent readout on the
   * upgrade rows instead of a tooltip. No pricing changes here — this is exactly the
   * drCost/drSoftCap v21 already charges, shown before it is paid. ===== */
  function capV67(e, k) {
    const v = e.attrs[k] || 0,
      cap = drSoftCap(e, k),
      abs = attrCap();
    return {
      v: wholeNum(v),
      cap,
      abs,
      cost: drCost(e, k),
      atMax: v >= abs,
      over: v >= cap,
      room: wholeNum(Math.max(0, cap - v))
    };
  } /* v101: whole points on the badge */
  // MAX is the absolute wall (v21 kept exactly one); everything else is a price, and
  // the price is the thing worth reading.
  function capBadgeV67(e, k) {
    const c = capV67(e, k);
    return c.atMax
      ? '<b class="capv67 c5">MAX</b>'
      : c.cost >= 4
        ? '<b class="capv67 c4">' + c.cost + " pts / +1</b>"
        : c.cost === 3
          ? '<b class="capv67 c3">3 pts / +1</b>'
          : c.cost === 2
            ? '<b class="capv67 c2">2 pts / +1</b>'
            : '<b class="capv67 c1">1 pt · ' + c.room + " to cap</b>";
  }
  // the verdict for a whole PROGRAM: the average price of the stats it prioritises,
  // which is what actually decides whether a season spent here is cheap or wasted
  function capPlanV67(e, keys) {
    const ks = (keys && keys.length ? keys : Object.keys(POSITIONS[e.pos].w)).filter(k => e.attrs[k] != null);
    if (!ks.length) return null;
    const cs = ks.map(k => capV67(e, k)),
      maxed = cs.filter(c => c.atMax).length,
      avg = cs.reduce((t, c) => t + (c.atMax ? 5 : c.cost), 0) / cs.length;
    return {
      n: ks.length,
      maxed,
      avg,
      label:
        maxed === ks.length
          ? "ALL CAPPED"
          : avg >= 3.2
            ? "MOSTLY CAPPED"
            : avg >= 2.2
              ? "EXPENSIVE (" + Math.round(avg) + " pts / +1)"
              : avg >= 1.35
                ? "PAST SOFT CAP (" + Math.round(avg) + " pts / +1)"
                : "CHEAP ZONE (1 pt / +1)",
      col:
        maxed === ks.length || avg >= 3.2
          ? "var(--blood)"
          : avg >= 2.2
            ? "#ff9a5a"
            : avg >= 1.35
              ? "var(--gold)"
              : "var(--good)"
    };
  }
  // debug surface for scripts/capcheck.mjs — the check has to be able to prove the
  // badge quotes the price the game would actually charge, which means reading the
  // same drCost the badge read, not a copy of the formula.
  try {
    window.__CAPV67 = {
      cap: (e, k) => drSoftCap(e, k),
      cost: (e, k) => drCost(e, k),
      abs: () => attrCap(),
      keyOfLabel: n => ATTR_KEYS.find(k => ATTR_INFO[k] && n.indexOf(ATTR_INFO[k].name) >= 0) || null
    };
  } catch (e) {}
  (function () {
    if (document.getElementById("capv67css")) return;
    const st = document.createElement("style");
    st.id = "capv67css";
    st.textContent =
      ".capv67{display:inline-block;margin-left:5px;font:700 9px Oswald,sans-serif;letter-spacing:.6px;padding:1px 5px;border-radius:9px;border:1px solid currentColor;opacity:.95;vertical-align:1px}" +
      ".capv67.c1{color:#7fe0a0}.capv67.c2{color:#f0bb45}.capv67.c3{color:#ff9a5a}.capv67.c4{color:#ff8a80}.capv67.c5{color:#ff8a80;background:rgba(255,138,128,.16)}" +
      ".up-cap{font:600 10px Barlow Condensed,sans-serif;letter-spacing:.4px;color:var(--chalk-dim);margin-top:2px}" +
      ".up-cap b{font-weight:700}";
    (document.head || document.documentElement).appendChild(st);
  })();
  function Dl() {
    const e = state.player,
      t = LEVELS[e.level],
      a = playerOvr(e),
      s = nationalRank(e, a),
      n = nodeLvl("scout") > 0,
      i = e.rivals.filter(c => c.alive).map(c => ({ name: c.name, ovr: c.ovr, stars: c.stars, me: !1 }));
    (i.push({ name: e.name, ovr: a, stars: e.stars, me: !0 }), i.sort((c, u) => u.ovr - c.ovr));
    const r = i.findIndex(c => c.me) + 1,
      l = i
        .map((c, u) => {
          const h = c.me ? 0 : c.ovr - a;
          return `<div class="lb-row ${c.me ? "me" : ""}">
      <div class="lb-rank">${u + 1}</div>
      <div class="lb-name">${escHtml(c.name)}${c.me ? " (You)" : ""} <span style="color:var(--gold);font-size:12px">${"★".repeat(c.stars)}</span></div>
      <div class="lb-ovr">${n || c.me ? c.ovr : "?"}${!c.me && n ? `<span style="font-size:11px;color:${h > 0 ? "#e08a8a" : "var(--good)"}"> ${h > 0 ? "+" + h : h}</span>` : ""}</div>
    </div>`;
        })
        .join(""),
      d = e.career.length
        ? s.rank < (e.career[e.career.length - 1].rank || s.rank)
          ? "climbing 📈"
          : "holding"
        : "new prospect";
    ((byId("screen").innerHTML = `
    <div class="eyebrow">${t.name} · Age ${e.age} Class</div>
    <div class="h1">Recruiting Board</div>
    <div class="sub">${escHtml(e.name)} vs. the ${e.pos}s chasing the same dream. ${n ? "Your Scouting Network reveals their exact ratings." : "Unlock <b>Scouting Network</b> in the Legacy tree to see rival ratings."}</div>

    <div class="card mt" style="margin-top:14px">
      <div class="statline">
        <div class="statbox"><div class="n">#${fmtInt(s.posRank)}</div><div class="l">National ${e.pos}</div></div>
        <div class="statbox"><div class="n">#${fmtInt(s.rank)}</div><div class="l">Overall</div></div>
        <div class="statbox"><div class="n">${Math.round(s.pct)}%</div><div class="l">Percentile</div></div>
      </div>
      <div class="threshold-note center mt" style="margin-top:10px">Status: <b style="color:var(--gold)">${"★".repeat(e.stars)}</b> recruit · ${d}</div>
    </div>

    <div class="h2">Your Position Group</div>
    <div class="card">
      <div class="small" style="margin-bottom:6px">Beat your rivals' OVR to climb the depth chart. They develop every season too — some will wash out, others break through.</div>
      <div class="leaderboard">${l}</div>
    </div>
    <div class="small center">You're #${r} in your immediate group of ${i.length}. Nationally, cracking the top means surviving every future cut.</div>
  `),
      (byId("dock").innerHTML =
        `<button class="btn secondary" onclick="go(S.player.seasonStats&&S.view==='rank'?'hub':'hub')">Back</button>`));
  }
  function endCareer() {
    ((state.view = "gameover"), render());
  }
  function screenGameOver() {
    const e = state.player,
      t = LEVELS[e.level],
      a = e.level,
      s =
        (1 +
          nodeLvl("endorse") * 0.2 +
          nodeLvl("agent") * 0.15 +
          nodeLvl("brand") * 0.35 +
          nodeLvl("goat") * 0.5 +
          treeFx("ppMult") +
          gearFx("ppMult") +
          gearV147("ppGain") +
          hofWings() * 0.05 +
          posMasteryCount("ring") * 0.03) *
        tierPPMult(e) *
        pathVal("ppMult", 1) *
        (hasTrait(e, "showman") ? 1.15 : 1) *
        eraMult(),
      n = nodeLvl("legacy") * e.totalSeasons,
      i = [1, 2, 4, 8, 15, 28, 45, 70, 120][Math.min(a, 8)] || 1,
      r = Math.max(1, Math.round(((i + e.totalSeasons * 0.35 + (e.titles || 0) * 4) * s + n) * chaosEarnedMult(a))),
      l = prestigeStarReward(e, a, !1);
    e._settled ||
      ((e._settled = !0),
      (e._ppBankV136 = flushBankV136()),
      (state.pp += r),
      (e._vaultPayV137 = r + (e._ppBankV136 || 0)),
      (state.prestige = +(state.prestige + honorPayV139(l)).toFixed(1)),
      (state.careersCompleted = (state.careersCompleted || 0) + 1),
      (e._starGain = honorPayV139(l)),
      (state.lastCareerAttrs = { ...e.attrs }),
      enshrineHof(e, a, !1),
      lineageEndV136(e, a, "cut"),
      dropGear(a >= 5 ? 1 : 0, "Career-end drop"),
      completeChallenges(),
      saveGame());
    const d =
        e.career
          .map(h => `<div><span class="lvl-done">✓</span> ${h.level} — reached OVR ${h.ovr} at age ${h.age}</div>`)
          .join("") +
        `<div><span style="color:var(--blood)">✗</span> ${t.name} — cut at OVR ${playerOvr(e)}, age ${e.age}</div>`,
      c = Object.values(TREE_NODES)
        .filter(h => nodeLvl(h.key) < h.max && nodeUnlocked(h) && state.pp >= nodeCost(h))
        .sort((h, p) => nodeCost(p) - nodeCost(h))
        .slice(0, 3),
      u = Math.round((s - 1) * 100);
    ((byId("screen").innerHTML = `
    <div class="banner fail">
      <div class="big-emoji">🥀</div>
      <div class="bt">End of the Road</div>
      <div class="bs">${escHtml(e.name)}'s dream ends at <b>${t.name}</b>. Not everyone makes it — but every rep made you tougher, and his son starts with everything he learned.</div>
    </div>
    <div class="card end-pay-v150">
      <div class="statline">
        <div class="statbox"><div class="n">${LEVELS[a].name.split(" ")[0]}</div><div class="l">Reached</div></div>
        <div class="statbox"><div class="n">+${l}</div><div class="l">Honors 🎖️</div></div>
        <div class="statbox"><div class="n">+${r + (e._ppBankV136 || 0)}</div><div class="l">PP Earned</div></div>
      </div>
      ${u > 0 ? `<div class="threshold-note" style="margin-top:8px;text-align:center">💰 Your legacy bonuses boosted PP earnings by <b style="color:var(--gold)">+${u}%</b></div>` : ""}
      ${e._ppBankV136 ? `<div class="threshold-note bank-note-v136" style="margin-top:6px;text-align:center">🏦 <b style="color:var(--gold)">+${e._ppBankV136} PP</b> of that was banked during the career — goals, titles, seasons — and paid now, at the end.</div>` : ""}
    </div>
    <div class="h2">Career Log</div>
    <div class="card"><div class="career-log">${d}</div></div>
    ${
      c.length
        ? `<div class="card tight end-legacy-v150" style="border-color:var(--gold)">
      <div class="l" style="font-size:11px;color:var(--gold);letter-spacing:2px;margin-bottom:8px">🌳 YOU CAN NOW AFFORD</div>
      ${c.map(h => `<div style="display:flex;justify-content:space-between;align-items:center;padding:4px 0;font-family:'Barlow Condensed';font-size:14px"><span>${h.icon} ${h.name}</span><span style="color:var(--gold);font-family:'Oswald'">${nodeCost(h)} PP</span></div>`).join("")}
    </div>`
        : ""
    }
    <div class="card tight end-legacy-v150">
      <div style="font-family:'Oswald';font-weight:600;font-size:16px;color:var(--gold)">His son picks it up</div>
      <div class="small mt" style="margin-top:6px">The ${escHtml(familyV136().surname)} line goes on: the next player is <b>${escHtml(e.name)}</b>'s son, and every lesson in the tree is his from day one — starting attributes, growth, points. Spend PP on the inheritance, then hand it to him.</div>
    </div>
  `),
      (byId("dock").innerHTML = `
    ${vaultPayBtnV137(e)}
    ${state.pp > 0 ? `<button class="btn" onclick="go('shop')">🌳 Spend ${state.pp} PP First</button><div style="height:8px"></div>` : ""}
    <button class="btn ${state.pp > 0 ? "secondary" : ""}" onclick="prestigeReset()">Run It Back — His Son's Career</button>
  `));
  }
  function screenWin() {
    const e = state.player;
    if (!e._settled) {
      e._settled = !0;
      const a =
          (1 +
            nodeLvl("endorse") * 0.2 +
            nodeLvl("agent") * 0.15 +
            nodeLvl("brand") * 0.35 +
            nodeLvl("goat") * 0.5 +
            treeFx("ppMult") +
            gearFx("ppMult") +
            gearV147("ppGain") +
            hofWings() * 0.05 +
            posMasteryCount("ring") * 0.03) *
          tierPPMult(e) *
          pathVal("ppMult", 1) *
          (hasTrait(e, "showman") ? 1.15 : 1) *
          eraMult(),
        s = 1 + nodeLvl("hof") * 0.3,
        n = Math.round(
          (85 + e.totalSeasons * 0.75 + (e.titles || 0) * 6) * a * s * chaosEarnedMult(Math.max(7, e.level))
        ),
        i = prestigeStarReward(e, Math.max(7, e.level), !0);
      ((e._ppBankV136 = flushBankV136()),
        (state.pp += n),
        (e._vaultPayV137 = (e._vaultPayV137 || 0) + n + (e._ppBankV136 || 0)),
        (state.prestige = +(state.prestige + honorPayV139(i)).toFixed(1)),
        (state.bestLevel = 7),
        (state.nflReached = (state.nflReached || 0) + 1),
        (state.careersCompleted = (state.careersCompleted || 0) + 1),
        (state.lastCareerAttrs = { ...e.attrs }),
        enshrineHof(e, 7, !0),
        lineageEndV136(e, Math.max(7, e.level), "won"),
        dropGear(1, "UFF career drop"),
        (e._ppGain = n),
        (e._starGain = honorPayV139(i)),
        completeChallenges(),
        saveGame());
    }
    const t = playerOvr(e);
    ((byId("screen").innerHTML = `
    <div class="banner nfl">
      <div class="big-emoji">🏆</div>
      <div class="bt">WELCOME TO THE UFF</div>
      <div class="bs">${escHtml(e.name)} — ${POSITIONS[e.pos].name} — made it all the way from the Pee Wee fields to the pros as a <b>${t} OVR</b> pro. A one-in-a-million story.</div>
    </div>
    <div class="card end-pay-v150">
      <div class="statline">
        <div class="statbox"><div class="n">${t}</div><div class="l">Final OVR</div></div>
        <div class="statbox"><div class="n">${e.totalSeasons}</div><div class="l">Seasons</div></div>
        <div class="statbox"><div class="n">+${e._ppGain + (e._ppBankV136 || 0)}</div><div class="l">PP Earned</div></div>
      </div>
    </div>
    <div class="h2">The Journey</div>
    <div class="card"><div class="career-log">${e.career.map(a => `<div><span class="lvl-done">✓</span> ${a.level} — OVR ${a.ovr} at ${a.age}</div>`).join("")}<div><span class="lvl-done" style="color:var(--gold)">★</span> The UFF — OVR ${t}, age ${e.age}</div></div></div>
    <div class="card tight end-legacy-v150"><div class="center" style="font-family:'Oswald';color:var(--gold);font-size:16px">+${e._starGain} HONORS ${HONOR_ICON_V130} · His son starts with everything he learned</div></div>
  `),
      (byId("dock").innerHTML = `
    ${vaultPayBtnV137(e)}
    <button class="btn" onclick="continueNFL()">Keep Playing UFF Seasons</button>
    <div style="height:8px"></div>
    <button class="btn secondary" onclick="prestigeReset()">Hand It to His Son</button>
  `));
  }
  function continueNFL() {
    ((state.view = "hub"), saveGame(), render());
  }
  function prestigeReset() {
    ((state.player = null), saveGame(), goView("menu"));
  }
  function screenPath() {
    const e = state.prestige >= PATH_HONORS,
      t = state.path;
    ((byId("screen").innerHTML = `
    <div class="eyebrow">Prestige Path · Permanent Archetype</div>
    <div class="h1">Choose Your Legend</div>
    <div class="sub">${e ? "Commit to a path that reshapes every future career. This is the deepest choice in the game — each path is a completely different way to reach the UFF. You can switch later for a Legacy Reset cost." : `Reach <b style="color:var(--gold)">${HONOR_ICON_V130} ${PATH_HONORS} Honors</b> to unlock a Prestige Path. You're at ${HONOR_ICON_V130} ${state.prestige}. Honors come from finishing careers — they are not recruit stars.`}</div>
    <div class="mt" style="margin-top:14px">
      ${Object.entries(PATHS)
        .map(([a, s]) => {
          const n = t === a;
          return `<div class="pos-card ${n ? "best" : ""}" style="${n ? "border-color:" + s.color : ""}" ${e ? `onclick="choosePath('${a}')"` : ""}>
          <div class="pos-abbr" style="font-size:28px">${s.icon}</div>
          <div class="pos-info">
            <div class="pn" style="color:${s.color}">${s.name} ${n ? '<span class="fit-tag fit-natural">ACTIVE</span>' : ""}</div>
            <div class="pd" style="font-style:italic;opacity:.7">"${s.tag}"</div>
            <div class="pd" style="margin-top:4px">${s.desc}</div>
          </div>
        </div>`;
        })
        .join("")}
    </div>
  `),
      (byId("dock").innerHTML = `<button class="btn secondary" onclick="go('shop')">Back to Prestige Tree</button>`));
  }
  function choosePath(e) {
    if (state.prestige < PATH_HONORS) {
      showToast("Reach " + HONOR_ICON_V130 + " " + PATH_HONORS + " Honors first");
      return;
    }
    if (state.path !== e) {
      if (state.path && state.path !== e) {
        if (state.prestige < 2) {
          showToast("Need " + HONOR_ICON_V130 + " 2 Honors to switch paths");
          return;
        }
        ((state.prestige -= 2),
          (state.pathResets = (state.pathResets || 0) + 1),
          showToast("Switched paths (−2 Honors)"));
      } else showToast("Path chosen: " + PATHS[e].name);
      ((state.path = e), saveGame(), screenPath());
    }
  }
  let branchTab = "physical";
  function screenPrestige() {
    const e = Object.values(state.tree || {}).reduce((n, i) => n + i, 0);
    byId("screen").innerHTML = `
    <div class="eyebrow">The Inheritance · ${familyV136().ordinal} generation · ${e} lesson${e === 1 ? "" : "s"} handed down</div>
    <div class="h1">What the Family Learned</div>
    <div class="sub">Every finished career is a father's lesson to the son who comes next. Spend the family's Prestige Points here — every node is handed down to every ${escHtml(familyV136().surname || "player")} after this one, forever.</div>
    <div class="pts-banner mt" style="margin-top:14px"><div><span class="n">${(state.pp || 0).toLocaleString("en-US")}</span> <span class="l">PRESTIGE POINTS</span>${bankedV136() > 0 ? `<div class="bank-v136">🏦 +${bankedV136()} banked · paid when this career ends</div>` : ""}</div>
      <div style="font-family:'Oswald';font-size:15px;color:var(--gold)">${HONOR_ICON_V130} ${state.prestige} Honors</div></div>

    <div class="btn-row" style="margin-bottom:14px;flex-wrap:wrap;gap:6px">
      ${Object.entries(TREE)
        .map(
          ([n, i]) =>
            `<button class="branch-tab ${branchTab === n ? "active" : ""}" onclick="setBranch('${n}')" style="flex:1;min-width:70px;padding:9px 4px;border-radius:9px;border:1px solid ${branchTab === n ? i.color : "var(--line)"};background:${branchTab === n ? i.color + "22" : "transparent"};color:${branchTab === n ? i.color : "var(--chalk-dim)"};font-family:'Oswald';font-weight:600;font-size:13px;cursor:pointer">${i.icon} ${i.name}</button>`
        )
        .join("")}
    </div>

    <div id="branchNodes">${branchNodesHtml(branchTab)}</div>
  `;
    const t = Math.max(0, Math.max(1, state.prestige) - (state.respecUsed || 0)),
      a = state.path
        ? PATHS[state.path].icon + " " + PATHS[state.path].name
        : state.prestige >= PATH_HONORS
          ? "⚡ Choose Path"
          : "🔒 Path (" + HONOR_ICON_V130 + PATH_HONORS + ")",
      s = state.chaosUnlocked
        ? `💍 Dynasty · ${state.rings || 0} Rings · Chaos ${chaosTotal()}`
        : (state.rings || 0) > 0
          ? `💍 Dynasty · ${state.rings} Rings`
          : "💍 Dynasty 🔒 (" + objectivesDone() + "/20 objectives)";
    byId("dock").innerHTML = `
    <button class="btn" style="background:linear-gradient(180deg,#ff7a72,#c23a3a);color:#fff;box-shadow:0 4px 0 #6e1e1e, 0 6px 14px rgba(0,0,0,.3)" onclick="go('dynasty')">${s}</button>
    <div style="height:8px"></div>
    <button class="btn secondary" onclick="go('path')">${a}</button>
    <div style="height:8px"></div>
    ${t > 0 ? `<button class="btn ghost" onclick="respecTree()">♻️ Free Respec (${t} left)</button><div style="height:8px"></div>` : ""}
    <button class="btn secondary" onclick="openVaultV137()">🏦 Visit the Vault</button>
    <div style="height:8px"></div>
    <button class="btn ghost" onclick="shopBack()">Back</button>`;
  }
  function branchNodesHtml(e) {
    const t = TREE[e];
    return t.nodes
      .map(a => {
        const s = nodeLvl(a.key),
          n = s >= a.max,
          i = nodeUnlocked(a),
          r = nodeCost(a),
          l = state.pp >= r && !n && i;
        let d = "";
        return (
          !i &&
            a.req &&
            (honorHasReqV130(a.req)
              ? (d = `🔒 Needs ${HONOR_ICON_V130} ${honorReqV130(a.req)} HONORS — you have ${honorsV130()}`)
              : a.req.node && (d = `🔒 Needs ${TREE_NODES[a.req.node].name} Lv ${a.req.lvl}`)),
          `<div class="shop-item" style="${i ? "" : "opacity:.55"};border-color:${s > 0 ? t.color : "var(--line)"}">
      <div class="ic">${a.icon}</div>
      <div class="si"><div class="st">${a.name} <span class="lvl" style="color:${t.color}">Lv ${s}/${a.max}</span></div>
        <div class="sd">${a.desc}</div>
        ${d ? `<div class="sd" style="color:var(--blood)">${d}</div>` : ""}
        ${s > 0 ? `<div class="track " style="height:5px;margin-top:5px;background:rgba(0,0,0,.3);border-radius:4px;overflow:hidden"><i style="display:block;height:100%;width:${(s / a.max) * 100}%;background:${t.color}"></i></div>` : ""}
      </div>
      <button class="buy ${n ? "max" : ""}" ${l ? "" : "disabled"} onclick="vaultBuy('${a.key}')">${n ? "MAX" : i ? ppFmtV146(r) + " PP" : "LOCKED"}</button>
    </div>`
        );
      })
      .join("");
  }
  function respecTree() {
    if (Math.max(1, state.prestige) - (state.respecUsed || 0) <= 0) {
      showToast("No free respecs left — earn more prestige");
      return;
    }
    let t = 0;
    (Object.entries(state.tree || {}).forEach(([a, s]) => {
      const n = TREE_NODES[a];
      if (n) for (let i = 0; i < s; i++) t += Math.round(n.cost * Math.pow(n.mult, i));
    }),
      (state.pp += t),
      (state.tree = {}),
      (state.respecUsed = (state.respecUsed || 0) + 1),
      saveGame(),
      showToast("♻️ Respec complete — " + t + " PP refunded"),
      screenPrestige());
  }
  function setBranch(e) {
    branchTab = e;
    const t = byId("branchNodes");
    (t && (t.innerHTML = branchNodesHtml(e)), screenPrestige());
  }
  function buyNode(e) {
    const t = TREE_NODES[e];
    if (nodeLvl(e) >= t.max) return;
    if (!nodeUnlocked(t)) {
      showToast("Locked — meet the requirement first");
      return;
    }
    const s = nodeCost(t);
    if (state.pp < s) {
      showToast("Not enough PP");
      return;
    }
    ((state.pp -= s),
      (state.tree[e] = (state.tree[e] || 0) + 1),
      saveGame(),
      showToast(t.name + " → Lv " + state.tree[e]),
      screenPrestige(),
      syncCounters());
  }
  function shopBack() {
    goView(state.player && state.player.pos ? "hub" : "menu");
  }
  function goView(e) {
    ((state.view = e), saveGame(), render());
  }
  function hypeState() {
    state.experience95 ||
      (state.experience95 = {
        coach: 50,
        hype: 18,
        streak: 0,
        bestStreak: 0,
        stories: [],
        weekStamp: "",
        dailyClaimed: "",
        seasonsGraded: 0
      });
    const e = state.experience95;
    return (
      Array.isArray(e.stories) || (e.stories = []),
      ["coach", "hype", "streak", "bestStreak", "seasonsGraded"].forEach(t => {
        e[t] == null && (e[t] = 0);
      }),
      e
    );
  }
  function escHtml(e) {
    return String(e ?? "").replace(
      /[&<>"']/g,
      t => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[t]
    );
  }
  function pushStory(e, t, a) {
    const s = hypeState();
    (s.stories.unshift({ icon: e, title: t, deck: a, at: Date.now() }), (s.stories = s.stories.slice(0, 8)));
  }
  function hubHeadline(e) {
    const t = hypeState(),
      a = LEVELS[e.level],
      s = playerOvr(e),
      n = teamName(e);
    return t.streak >= 4
      ? [`${n} can't stop winning`, `${e.name} has powered a ${t.streak}-game run and the noise is getting louder.`]
      : t.streak <= -3
        ? [
            `Pressure building around ${e.name}`,
            `A ${Math.abs(t.streak)}-game slide has the staff searching for answers.`
          ]
        : t.coach >= 82
          ? [`Coach hands ${e.name} the keys`, `${a.name} staff confidence has reached an elite level.`]
          : t.hype >= 78
            ? [`${e.name} is becoming must-see football`, `The fanbase believes this ${e.pos} could be special.`]
            : s >= a.need + 8
              ? [
                  `${e.name} looks ready for the next stage`,
                  `Evaluators say the ${e.pos} is outperforming the ${a.name} level.`
                ]
              : [
                  "Every snap is an audition",
                  `${e.name} enters the next game with development, reputation, and promotion odds on the line.`
                ];
  }
  function _l(e) {
    const t = hypeState(),
      a = hubHeadline(e),
      s = t.streak === 0 ? "—" : t.streak > 0 ? "W" + t.streak : "L" + Math.abs(t.streak);
    return `<div class="card pulse-card"><div class="pulse-top"><div class="pulse-mark">📡</div><div><div class="pulse-title">Career Pulse</div><div class="pulse-sub">Your performance now changes how the football world sees you.</div></div></div>
  <div class="pulse-grid"><div class="pulse-stat"><b style="color:${t.coach >= 70 ? "var(--good)" : t.coach < 35 ? "var(--blood)" : "var(--gold)"}">${Math.round(t.coach)}</b><small>Coach Trust</small><div class="pulse-meter"><i style="width:${clamp99(t.coach, 0, 100)}%"></i></div></div><div class="pulse-stat"><b style="color:${t.hype >= 70 ? "var(--gold)" : "var(--ice)"}">${Math.round(t.hype)}</b><small>Fan Hype</small><div class="pulse-meter"><i style="width:${clamp99(t.hype, 0, 100)}%"></i></div></div><div class="pulse-stat"><b style="color:${t.streak > 0 ? "var(--good)" : t.streak < 0 ? "var(--blood)" : "var(--chalk)"}">${s}</b><small>Form</small><div class="pulse-meter"><i style="width:${clamp99(50 + t.streak * 10, 4, 100)}%"></i></div></div></div></div>
  <div class="card headline-card"><div class="headline-label">LEAGUE WIRE · TOP STORY</div><div class="headline-text">${escHtml(a[0])}</div><div class="headline-deck">${escHtml(a[1])}</div></div>`;
  }
  function Ul(e) {
    const t = hypeState(),
      a = new Date().toISOString().slice(0, 10),
      s = e.weekResults || [],
      n = s.filter(c => c.played),
      i = n.filter(c => c.won).length,
      r = [
        { ic: "🏈", name: "Take the Field", desc: "Complete one game today", done: t.weekStamp === a, pp: 1 },
        { ic: "🔥", name: "Winning Standard", desc: "Win a game in the current season", done: i >= 1, pp: 1 },
        { ic: "📈", name: "Development Day", desc: "Hold 60+ coach trust", done: t.coach >= 60, pp: 1 }
      ],
      l = r.every(c => c.done),
      d = t.dailyClaimed === a;
    return (
      l &&
        !d &&
        ((t.dailyClaimed = a),
        bankPPV136(3, "daily"),
        pushStory(
          "🎁",
          "Daily Drive complete",
          bankingV136()
            ? "Three Prestige Points banked — paid when this career ends."
            : "Three Prestige Points added to your legacy."
        ),
        setTimeout(() => bigMoment("DAILY DRIVE COMPLETE", "+3 Prestige Points", "good"), 250),
        saveGame()),
      `<div class="card mission-card"><div class="objective-head"><div class="objective-icon">⚡</div><div><div class="objective-kicker" style="color:var(--violet)">DAILY DRIVE</div><div class="objective-title">${d ? "All rewards claimed" : r.filter(c => c.done).length + "/3 missions complete"}</div></div></div><div>${r.map(c => `<div class="mission-row ${c.done ? "done" : ""}"><div class="mi">${c.done ? "✅" : c.ic}</div><div class="mtx"><b>${c.name}</b><small>${c.desc}</small></div><div class="mission-prize">+${c.pp} PP</div></div>`).join("")}</div></div>`
    );
  }
  function storyFeedHtml() {
    const e = hypeState();
    return e.stories.length
      ? `<div class="h2">Career Headlines</div><div class="card tight"><div class="story-feed">${e.stories
          .slice(0, 4)
          .map(
            t =>
              `<div class="story-item"><div class="story-icon">${t.icon}</div><div class="story-copy"><b>${escHtml(t.title)}</b><small>${escHtml(t.deck)}</small></div></div>`
          )
          .join("")}</div></div>`
      : "";
  }
  window.__coachSwingV98 = function (perf, won) {
    const s = Number(perf) || 50,
      n = Math.max(-6, Math.min(7, Math.round((s - 58) / 8)));
    return won ? 3 + Math.max(0, n) : -3 + n;
  };
  function processWeek95(e, t) {
    if (!e || e._pulse95) return;
    e._pulse95 = !0;
    const a = hypeState(),
      s = Number(e.perf) || 50,
      n = clamp99(Math.round((s - 58) / 8), -6, 7),
      c0 = a.coach,
      sw98 =
        (e.coachPreview98 != null ? Number(e.coachPreview98) : window.__coachSwingV98(s, !!e.won)) *
        (e.rivalV128 ? (window.__V128 ? window.__V128.mult() : 2) : 1);
    e.coachDelta98 = clamp99(c0 + sw98, 0, 100) - c0;
    /* v128: the rivalry moves trust twice as far, either way */ (e.won
      ? ((a.streak = a.streak > 0 ? a.streak + 1 : 1),
        (a.coach = clamp99(c0 + sw98, 0, 100)),
        (a.hype = clamp99(a.hype + 4 + Math.max(0, n), 0, 100)))
      : ((a.streak = a.streak < 0 ? a.streak - 1 : -1),
        (a.coach = clamp99(c0 + sw98, 0, 100)),
        (a.hype = clamp99(a.hype - 2 + Math.min(0, n), 0, 100))),
      (a.bestStreak = Math.max(a.bestStreak, a.streak)),
      (a.weekStamp = new Date().toISOString().slice(0, 10)));
    const i = e.opp || "the opposition";
    (s >= 88
      ? (pushStory(
          "🌟",
          `${t.name} delivers a signature performance`,
          `${s} rating against ${i} sends evaluators scrambling.`
        ),
        setTimeout(() => bigMoment("SIGNATURE PERFORMANCE", `${s} game rating · the hype is real`, "good"), 220))
      : e.playoff && e.won
        ? (pushStory(
            "🏆",
            `${teamName(t)} survives and advances`,
            `A postseason win over ${i} keeps the title dream alive.`
          ),
          setTimeout(() => bigMoment("SURVIVE AND ADVANCE", `Postseason victory over ${i}`, "good"), 220))
        : e.won && a.streak >= 3
          ? pushStory(
              "🔥",
              `${teamName(t)} extends the streak`,
              `${a.streak} straight wins have transformed the season.`
            )
          : !e.won && s < 40
            ? (pushStory(
                "📉",
                `${t.name} faces hard questions`,
                `${s} rating in a loss to ${i}; the next response matters.`
              ),
              setTimeout(() => bigMoment("ROUGH NIGHT", "The film room will be uncomfortable", "bad"), 220))
            : pushStory(
                e.won ? "✅" : "❌",
                `${e.won ? "Win secured" : "Opportunity missed"} against ${i}`,
                `${t.name} posted a ${s} game rating.`
              ),
      saveGame());
  }
  function hn(e) {
    const t = e.seasonStats || {};
    t.games;
    const a = hypeState();
    let s = t.grade || "";
    if ((typeof s == "object" && (s = s.grade || s.letter || ""), !s)) {
      const n = e._lastRecord95 || { w: 0, l: 0 },
        i = n.w / Math.max(1, n.w + n.l),
        r = playerOvr(e),
        l = LEVELS[e.level].need,
        d = i * 55 + clamp99((r - l + 10) * 2, 0, 35) + clamp99((a.coach - 50) / 5, 0, 10);
      s = d >= 88 ? "A+" : d >= 80 ? "A" : d >= 72 ? "B+" : d >= 64 ? "B" : d >= 54 ? "C+" : d >= 45 ? "C" : "D";
    }
    return String(s).toUpperCase();
  }
  function gradeColor(e) {
    return e[0] === "A" ? "var(--good)" : e[0] === "B" ? "var(--gold)" : e[0] === "C" ? "#d39a55" : "var(--blood)";
  }
  function zl(e) {
    const t = hypeState(),
      a = hn(e),
      s = e._lastRecord95 || { w: 0, l: 0 };
    return `<div class="card center" style="border-color:${gradeColor(a)};background:linear-gradient(160deg,rgba(255,255,255,.055),rgba(13,18,28,.96))"><div class="eyebrow">SEASON REPORT CARD</div><div class="grade-ring" style="color:${gradeColor(a)}"><div class="season-grade">${a}</div></div><div class="h1">${s.w}-${s.l} · ${s.champ ? "Champions" : "Season Complete"}</div><div class="sub">Coach trust ${Math.round(t.coach)} · fan hype ${Math.round(t.hype)} · best run ${Math.max(0, t.bestStreak)} wins</div><div class="press-strip" style="justify-content:center;margin-top:11px"><span class="press-chip hot">${t.coach >= 70 ? "STAFF FAVORITE" : "PROVING GROUND"}</span><span class="press-chip">${t.hype >= 70 ? "FAN PHENOMENON" : "BUILDING A FOLLOWING"}</span><span class="press-chip">${playerOvr(e)} OVR</span></div></div>`;
  }
  function decorateScreen() {
    if (!state || !state.player || !state.player.pos) return;
    const e = state.player,
      t = byId("screen");
    if (t) {
      if (state.view === "hub") {
        const a = t.querySelector(".player-hero");
        (a && !t.querySelector(".pulse-card") && a.insertAdjacentHTML("afterend", _l(e) + Ul(e)),
          t.querySelector(".story-feed") || t.insertAdjacentHTML("beforeend", storyFeedHtml()));
      }
      if (state.view === "season") {
        const a = t.querySelector(".h1");
        a &&
          !t.querySelector(".press-strip") &&
          a.insertAdjacentHTML(
            "afterend",
            `<div class="press-strip"><span class="press-chip hot">COACH ${Math.round(hypeState().coach)}</span><span class="press-chip">HYPE ${Math.round(hypeState().hype)}</span><span class="press-chip">FORM ${hypeState().streak > 0 ? "W" + hypeState().streak : hypeState().streak < 0 ? "L" + Math.abs(hypeState().streak) : "EVEN"}</span><span class="press-chip">${escHtml(hubHeadline(e)[0])}</span></div>`
          );
      }
      state.view === "result" && !t.querySelector(".season-grade") && t.insertAdjacentHTML("afterbegin", zl(e));
    }
  }
  const Ql = render;
  render = function () {
    (hypeState(), Ql(), decorateScreen());
  };
  const Jl = playWeek;
  playWeek = function (e) {
    const t = state.player,
      a = t && t.weekResults ? t.weekResults.findIndex(n => !n.played) : -1,
      s = a >= 0 ? t.weekResults[a] : null;
    (Jl(e), !e && s && (processWeek95(s, t), render()));
  };
  const Xl = finishWeekGame;
  finishWeekGame = function () {
    const e = state.player,
      t = e && e.weekResults && e.currentWeek != null ? e.weekResults[e.currentWeek] : null;
    (Xl(), t && (processWeek95(t, e), render()));
  };
  const Zl = simRemainingWeeks;
  simRemainingWeeks = function () {
    const e = state.player,
      t = e && e.weekResults ? e.weekResults.filter(a => !a.played && !a.playoff) : [];
    (Zl(), t.forEach(a => processWeek95(a, e)), render());
  };
  const ec = finishSeasonGames;
  finishSeasonGames = function () {
    const e = state.player,
      t = (e.weekResults || []).slice();
    ((e._lastRecord95 = {
      w: t.filter(n => n.played && n.won).length,
      l: t.filter(n => n.played && !n.won).length,
      champ: !!(e.playoffState && e.playoffState.champion)
    }),
      ec());
    const a = hypeState(),
      s = hn(e);
    (a.seasonsGraded++,
      pushStory(
        s[0] === "A" ? "🏅" : "📰",
        `${s}-grade season enters the books`,
        `${e._lastRecord95.w}-${e._lastRecord95.l}${e._lastRecord95.champ ? " with a championship" : ""}; coach trust closes at ${Math.round(a.coach)}.`
      ),
      saveGame(),
      render(),
      setTimeout(
        () =>
          bigMoment(
            `${s}-GRADE SEASON`,
            e._lastRecord95.champ
              ? "Champions. A season they will remember."
              : `${e._lastRecord95.w}-${e._lastRecord95.l} · the story continues`,
            s[0] === "A" ? "good" : s[0] === "D" ? "bad" : "gold"
          ),
        260
      ));
  };
  LEVELS[3].need = 53;
  LEVELS[4].need = 65;
  LEVELS[5].need = 89;
  LEVELS[6].need = 101;
  LEVELS[7].need = 114;
  LEVELS[5].desc = "A scholarship is rare. Most generations never get one.";
  LEVELS[7].desc = "Making a roster is only the beginning. Produce immediately or get released.";
  function lo(e) {
    const t = (state && state.careersCompleted) || 0;
    return e === 3
      ? Math.max(0, 5 - t * 0.2)
      : e === 4
        ? Math.max(0, 20 - t * 0.46)
        : e === 5
          ? Math.max(0, 58 - t * 0.78)
          : e === 6
            ? Math.max(0, 75 - t * 1.05)
            : e >= 7
              ? Math.max(0, 30 - t * 0.48)
              : Math.max(0, 6 - t * 0.22);
  }
  function ensureDepth(e) {
    return e
      ? (e.coachTrust == null && (e.coachTrust = clamp99(28 + treeFx("coachStart") + randRange(-8, 8), 0, 100)),
        e.depthRole == null && (e.depthRole = "BENCH"),
        e.snapShare == null && (e.snapShare = 0.12),
        e.depthStarts == null && (e.depthStarts = 0),
        e.nflPoorYears == null && (e.nflPoorYears = 0),
        e)
      : null;
  }
  function depthChart(e) {
    ensureDepth(e);
    const t = LEVELS[e.level].need - 5 + lo(e.level) + chaosOppBoost(),
      a = playerPower(e) + e.coachTrust * 0.18 + treeFx("depthPower") * 20 - t;
    let s, n;
    return (
      a >= 16
        ? ((s = "FIRST STRING"), (n = 0.88))
        : a >= 7
          ? ((s = "STARTER"), (n = 0.7))
          : a >= -1
            ? ((s = "ROTATION"), (n = 0.46))
            : a >= -10
              ? ((s = "SECOND STRING"), (n = 0.27))
              : ((s = "BENCH"), (n = 0.1)),
      (n = clamp99(n + treeFx("snapFloor"), 0.06, 0.98)),
      (e.depthRole = s),
      (e.snapShare = n),
      n >= 0.65 && e.depthStarts++,
      { role: s, share: n, score: a, peer: t }
    );
  }
  function tc(e, t) {
    let a = Math.random(),
      s = 0.075 + treeFx("goodLuck"),
      n = 0.11;
    return (
      treeFx("varianceUp") && ((s += 0.025), (n += 0.035)),
      a < s
        ? {
            ...randPick([
              { n: "Viral highlight", v: 18 },
              { n: "Opponent injury", v: 13 },
              { n: "Perfect matchup", v: 15 },
              { n: "Coach calls your number", v: 20 }
            ]),
            good: !0
          }
        : a > 1 - n
          ? {
              ...randPick([
                { n: "Bad weather", v: -14 },
                { n: "Blown assignment", v: -20 },
                { n: "Fluke turnover", v: -18 },
                { n: "Minor knock", v: -11 },
                { n: "Teammate takes your snaps", v: -16 }
              ]),
              good: !1
            }
          : { n: "Normal game", v: 0, good: null }
    );
  }
  const ac = rollGamePerf;
  rollGamePerf = function (e, t, a) {
    ensureDepth(e);
    const s = depthChart(e),
      n = tc();
    let i = 0;
    (e.level <= 4 && (i += treeFx("youthPerf")),
      e.level >= 7 && (i += treeFx("nflPerf")),
      (a && a.playoff) || (i -= treeFx("regularPenalty")),
      e._bounceBack10 && (i += treeFx("bounceBack")));
    const r = ac(e, (t || 0) + n.v + i, a),
      l = 1 + treeFx("varianceUp");
    l > 1 && (r.perf = clamp99(Math.round(50 + (r.perf - 50) * l), 1, 100));
    const d = Math.sqrt(s.share);
    ((r.perf =
      r.perf >= 48
        ? clamp99(
            Math.round(
              Math.min(
                48 + (r.perf - 48) * (0.85 + 0.15 * d),
                s.share < 0.12 ? 76 : s.share < 0.22 ? 84 : s.share < 0.35 ? 92 : 100
              )
            ),
            1,
            100
          )
        : clamp99(Math.round(48 + (r.perf - 48) * (0.55 + 0.45 * d)), 1, 100)),
      treeFx("injUp") && (r.injured = r.injured || Math.random() < treeFx("injUp") * 0.03),
      (r.snapShare = s.share),
      (r.snaps = Math.max(3, Math.round((e.level >= 5 ? 68 : 52) * s.share * randRange(0.82, 1.18)))),
      (r.depthRole = s.role),
      (r.luck = n));
    const c =
      ((r.perf - 50) / 13) *
      (1 + (s.share < 0.4 ? treeFx("trustGain") : 0)) *
      (r.perf > 50 ? 1 + gearV147("trustGain") : 1);
    return (
      (e.coachTrust = clamp99(e.coachTrust + c - (r.injured ? 2 : 0), 0, 100)),
      (e.lastLuck10 = n),
      (e.lastSnaps10 = r.snaps),
      r
    );
  };
  const sc = advanceChance;
  advanceChance = function (e, t, a, s, n) {
    const r = e + ((a && { QB: 1, RB: 2, WR: -2.5, TE: 3.5, OL: 4.5, DL: -2, LB: 1.5, CB: 0, S: -1 }[a]) || 0);
    let l = sc(r, t, a, s, n) - lo(t);
    (t >= 4 && a && (l += { QB: 2, RB: 2, WR: -4, TE: 2, OL: 3, DL: -3, LB: 3, CB: -2, S: -2 }[a] || 0),
      t === 6 && (l += clamp99(((state.careersCompleted || 0) - 80) * 0.03, 0, 6)),
      t === 4 && (l -= treeFx("collegeAdvPenalty")),
      t === 6 && (l -= treeFx("nflAdvPenalty")),
      t === 3 && (state.careersCompleted || 0) < 3 && (l += 9));
    const d = t >= 5 ? 0.35 : t === 3 ? 2.75 : 1.25;
    return clamp99(l, d, 97);
  };
  const nc = newPlayer;
  newPlayer = function () {
    const e = nc();
    ensureDepth(e);
    const t = ["speed", "strength", "quickness", "acceleration", "agility", "jumping", "stamina"],
      a = ["awareness", "vision", "discipline"];
    return (
      t.forEach(
        s => (e.attrs[s] = clamp99(e.attrs[s] + treeFx("physicalStart") - treeFx("athleticStartPenalty"), 1, attrCap()))
      ),
      a.forEach(
        s => (e.attrs[s] = clamp99(e.attrs[s] + treeFx("mentalStart") - treeFx("mentalStartPenalty"), 1, attrCap()))
      ),
      (e.coachTrust = clamp99(e.coachTrust + treeFx("coachStart"), 0, 100)),
      depthChart(e),
      e
    );
  };
  const ic = simSeason;
  simSeason = function (e) {
    (ensureDepth(e), e._bounceBack10);
    const t = ic(e);
    depthChart(e);
    const a = t.avg || 0;
    if (
      ((e._bounceBack10 = a < 48),
      (t.depthRole = e.depthRole),
      (t.snapShare = e.snapShare),
      (t.coachTrust = Math.round(e.coachTrust)),
      (t.avgSnaps = Math.round(
        (t.sampleGames || []).reduce((s, n) => s + (n.snaps || 0), 0) / Math.max(1, (t.sampleGames || []).length)
      )),
      (t.luckGames = (t.sampleGames || []).filter(s => s.luck && s.luck.v).map(s => s.luck)),
      e.level >= 7)
    ) {
      const s = treeFx("protectedYears"),
        n = 58 - treeFx("cutRelief");
      a < n || e.coachTrust < 24 ? e.nflPoorYears++ : (e.nflPoorYears = Math.max(0, e.nflPoorYears - 1));
      let i = 0;
      (e.nflSeasons > s &&
        ((i = clamp99(
          (n - a) * 0.055 + (30 - e.coachTrust) * 0.018 + e.nflPoorYears * 0.12 + treeFx("cutRisk"),
          0,
          0.94
        )),
        (i *= Math.max(0.35, 1 - treeFx("cutSave")))),
        Math.random() < i && ((e.nflCutPending = !0), (t.nflCut = !0), (t.cutChance = Math.round(i * 100))));
    }
    return t;
  };
  function depthCard(e) {
    const t = depthChart(e),
      a = e.lastLuck10;
    return `<div class="card depth-card ${e.nflCutPending ? "cut-warning" : ""}"><div class="eyebrow">Depth Chart · Playing Time</div>
    <div style="display:flex;align-items:center;justify-content:space-between"><div><div class="depth-role">${t.role}</div><div class="small">Your role changes with OVR, practice reputation and game performance.</div></div><div class="ovr-big">${Math.round(t.share * 100)}<span style="font-size:13px">%</span></div></div>
    <div class="depth-grid"><div class="depth-cell"><div class="n">${Math.round(e.coachTrust)}</div><div class="l">Coach Trust</div></div><div class="depth-cell"><div class="n">${e.lastSnaps10 || 0}</div><div class="l">Last Snaps</div></div><div class="depth-cell"><div class="n">${e.depthStarts || 0}</div><div class="l">Career Starts</div></div></div>
    ${a && a.v ? `<div class="luck-pill ${a.good ? "luck-good" : "luck-bad"}">${a.good ? "🍀" : "⚠️"} ${a.n}: ${a.v > 0 ? "+" : ""}${a.v} game swing</div>` : ""}</div>`;
  }
  const oc = decorateScreen;
  decorateScreen = function () {
    oc();
    const e = state.player,
      t = byId("screen");
    if (!(!e || !t)) {
      if ((ensureDepth(e), state.view === "hub" && !t.querySelector(".depth-card"))) {
        const a = t.querySelector(".card");
        a && a.insertAdjacentHTML("afterend", depthCard(e));
      }
      if (state.view === "result" && !t.querySelector(".depth-card")) {
        /* v122 A: .season-grade is the LETTER, inside the 112px .grade-ring — inserting afterend put the whole depth card inside the ring, where it overflowed across the report card and everything under it. Anchor on the CARD. */ const sg =
            t.querySelector(".season-grade"),
          a = (sg && sg.closest(".card")) || t.querySelector(".card") || t.firstElementChild;
        (a && a.insertAdjacentHTML("afterend", depthCard(e)),
          e.nflCutPending &&
            (t.insertAdjacentHTML(
              "afterbegin",
              '<div class="banner fail"><div class="big-emoji">📉</div><div class="bt">Released</div><div class="bs">The UFF moves fast. Your recent production and coach trust were not enough to keep a roster spot.</div></div>'
            ),
            (byId("dock").innerHTML =
              '<button class="btn danger" onclick="endCareer()">Accept Release · End Career</button>')));
      }
    }
  };
  const rc = branchNodesHtml;
  branchNodesHtml = function (e) {
    const __h = rc(e);
    return (
      e === "fate" &&
        setTimeout(() => {
          document.querySelectorAll(".shop-item").forEach((t, a) => {
            const s = TREE.fate.nodes[a];
            if (s && s.trade && !t.classList.contains("fate-node")) {
              t.classList.add("fate-node");
              const n = t.querySelector(".si");
              n && n.insertAdjacentHTML("beforeend", `<div class="tradeoff">⚠ ${s.trade}</div>`);
            }
          });
        }, 0),
      __h
    );
  };
  const RETIRE_AGE = 45;
  const retireAgeV134 = () => RETIRE_AGE + nodeLvl("longevity");
  /* v134 Apex: Borrowed Time */ function ageProfile(e) {
    return e <= 12
      ? {
          key: "child",
          name: "YOUTH DEVELOPMENT",
          perf: -2,
          inj: 0.58,
          recovery: 0.82,
          growth: 1.18,
          decline: 0,
          desc: "Rapid recovery and learning, but limited physical consistency."
        }
      : e <= 17
        ? {
            key: "teen",
            name: "ADOLESCENT GROWTH",
            perf: 1,
            inj: 0.72,
            recovery: 0.7,
            growth: 1.14,
            decline: 0,
            desc: "Fast development and strong recovery. Performance is still volatile."
          }
        : e <= 21
          ? {
              key: "young",
              name: "YOUNG PROSPECT",
              perf: 3,
              inj: 0.84,
              recovery: 0.58,
              growth: 1.08,
              decline: 0,
              desc: "Athletic gains come quickly and injuries usually heal well."
            }
          : e <= 27
            ? {
                key: "prime",
                name: "ATHLETIC PRIME",
                perf: 6,
                inj: 1,
                recovery: 0.42,
                growth: 1,
                decline: 0,
                desc: "Peak explosiveness, durability and week-to-week production."
              }
            : e <= 31
              ? {
                  key: "veteran",
                  name: "VETERAN PRIME",
                  perf: 5,
                  inj: 1.14,
                  recovery: 0.3,
                  growth: 0.9,
                  decline: 0.2,
                  desc: "Experience sustains performance, but recovery starts slowing."
                }
              : e <= 35
                ? {
                    key: "decline",
                    name: "EARLY DECLINE",
                    perf: 2,
                    inj: 1.34,
                    recovery: 0.2,
                    growth: 0.72,
                    decline: 0.55,
                    desc: "Football IQ remains strong while physical tools begin fading."
                  }
                : e <= 39
                  ? {
                      key: "late",
                      name: "LATE CAREER",
                      perf: -3,
                      inj: 1.62,
                      recovery: 0.12,
                      growth: 0.48,
                      decline: 1,
                      desc: "Preparation can still win games, but injuries and decline accelerate."
                    }
                  : e <= 44
                    ? {
                        key: "legend",
                        name: "LEGENDARY LONGEVITY",
                        perf: -8,
                        inj: 2.05,
                        recovery: 0.06,
                        growth: 0.25,
                        decline: 1.65,
                        desc: "Every season is historic—and every snap carries serious risk."
                      }
                    : {
                        key: "retire",
                        name: "MANDATORY RETIREMENT",
                        perf: -14,
                        inj: 2.4,
                        recovery: 0,
                        growth: 0,
                        decline: 2.2,
                        desc: "League rules require retirement after the age-45 season."
                      };
  }
  function lc(e) {
    return e.key === "prime" || e.key === "young"
      ? "var(--good)"
      : e.key === "veteran" || e.key === "teen"
        ? "var(--gold)"
        : e.key === "child"
          ? "#8ec3ee"
          : "var(--blood)";
  }
  function ageCard(e) {
    const t = ageProfile(e.age),
      a = lc(t);
    return `<div class="card age-card"><div class="eyebrow">Age Curve · ${e.age}/${RETIRE_AGE}</div>
    <div style="display:flex;justify-content:space-between;gap:12px;align-items:center"><div><div class="h2" style="margin:0;color:${a}">${t.name}</div><div class="small">${t.desc}</div></div><div class="ovr-big" style="color:${a}">${e.age}</div></div>
    <div class="age-grid"><div><b>${t.perf >= 0 ? "+" : ""}${t.perf}</b><small>Game Performance</small></div><div><b>${Math.round(t.inj * 100)}%</b><small>Injury Risk</small></div><div><b>${Math.round(t.recovery * 100)}%</b><small>Recovery Chance</small></div><div><b>${Math.round(t.growth * 100)}%</b><small>Natural Growth</small></div></div>
    ${e.age >= 40 ? `<div class="threshold-note" style="color:var(--blood);margin-top:8px">⏳ ${Math.max(0, retireAgeV134() - e.age)} season${retireAgeV134() - e.age === 1 ? "" : "s"} remain before mandatory retirement.</div>` : ""}</div>`;
  }
  const cc = rollGamePerf;
  rollGamePerf = function (e, t, a) {
    const s = ageProfile(e.age),
      n = cc(e, (t || 0) + s.perf, a);
    return (
      n.injured && Math.random() < s.recovery * TU("ageCancelK", 0.32)
        ? ((n.injured = !1), (n.ageRecovery = !0))
        : !n.injured &&
          s.inj > 1 &&
          Math.random() < Math.min(0.22, (s.inj - 1) * 0.105) &&
          ((n.injured = !0), (n.ageInjury = !0)),
      (n.ageStage = s.name),
      (n.agePerf = s.perf),
      (n.ageInjuryMult = s.inj),
      n
    );
  }; /* ===== v139 THE YEARS TAKE THEIR CUT =====
   * Age was worth almost nothing. Two things declined a man and both were UFF-only: a flat
   * multiplier that started at 30 and topped out at 6% a season, and a handful of coin flips in
   * `dc` for a point here and there. A 33-year-old could hold 95 speed for a decade.
   *
   * Now the moment he is past his ATHLETIC PRIME — the band `mn()` has always named and nothing
   * ever read — the year takes a real share of every physical attribute: 5% the first season past
   * it, steepening through the veteran / decline / late / legend bands on the profile's own
   * `decline` factor to a 16% cap. At EVERY level, not just the UFF. Football IQ is exempt, as it
   * always was: awareness, vision and grit are what the old man has left. The cut is taken
   * attribute by attribute inside `dc`, which is the ONE place the years touch the sheet now (the
   * season roll's own multiplier is gone), so the v132 year-older screen shows every point of it.
   *
   * Two prestige nodes answer it, both priced for the endgame: SECOND WIND (5,000 PP) reads the
   * curve a year younger per level, to eight. (EVERGREEN, which halved what was left, is retired in v146 C.) */
  function ageForV139(e) {
    return Math.max(0, ((e && e.age) || 0) - nodeLvl("secondWind"));
  }
  function ageCutV139(e) {
    const d = ageProfile(ageForV139(e)).decline;
    if (!(d > 0)) return 0;
    const c = Math.min(TU("ageCutMaxV139", 0.16), TU("ageCutBaseV139", 0.05) * (1 + (d - 0.2) * 1.6));
    return Math.max(0, c) * (1 - gearV147("ageDecline"));
  } /* v146 C: Evergreen is retired — the years are real · v147 C: gear softens, never erases */
  /* every physical attribute on the sheet — the mind (awareness, vision, grit) is what he has left */
  let _ageAttrsV139 = null;
  function ageAttrsV139() {
    return (
      _ageAttrsV139 || (_ageAttrsV139 = ATTR_KEYS.filter(k => k !== "awareness" && k !== "vision" && k !== "grit"))
    );
  }
  window.__ageV139 = {
    age: ageForV139,
    cut: ageCutV139,
    attrs: ageAttrsV139,
    profile: e => ageProfile(ageForV139(e)),
    year: (e, t) => ageYear(e, t || {})
  };
  function ageYear(e, t) {
    const a = ageProfile(e.age);
    e.level >= 7 &&
      t.attrsAfter &&
      ATTR_KEYS.forEach(n => {
        t.attrsAfter[n] != null && (e.attrs[n] = t.attrsAfter[n]);
      });
    const s = {},
      cut = ageCutV139(e);
    if (
      cut > 0 &&
      (ageAttrsV139().forEach(i => {
        if (e.attrs[i] == null) return;
        const d = e.attrs[i],
          v = Math.max(Math.min(10, d), d - Math.max(1, Math.round(d * cut)));
        /* the floor never LIFTS a stat that is already under it */ v !== d &&
          ((e.attrs[i] = v), (s[i] = { from: d, to: v, delta: v - d }));
      }),
      e.age <= 36 && Math.random() < 0.42)
    ) {
      const i = Math.random() < 0.5 ? "awareness" : "vision",
        r = e.attrs[i];
      ((e.attrs[i] = Math.min(attrCap(), e.attrs[i] + 1)), (s[i] = { from: r, to: e.attrs[i], delta: 1 }));
    }
    (tierRewardV139(e, t),
      (t.ageChanges = s),
      (t.ageProfile = a),
      (t.youngRecoveries = (t.sampleGames || []).filter(n => n.ageRecovery).length),
      (t.ageInjuries = (t.sampleGames || []).filter(n => n.ageInjury).length),
      e.age >= retireAgeV134() && ((e.retirementPending = !0), (t.mandatoryRetirement = !0)));
  }
  const uc = simSeason;
  simSeason = function (e) {
    const t = uc(e);
    return (ageYear(e, t), t);
  };
  /* ===== v132 A YEAR OLDER — the offseason body screen =====
   * The season roll (`tt`) does three things to the man that the report card never showed him doing: he
   * turns a year older (`e.age++`), his frame moves along the growth curve toward the projected adult body
   * (`bodyNowV112`, v112 C — height and weight are DERIVED from age, never rolled), and past his prime the
   * age model (`dc`) takes attributes off him. The report card led with the record. This screen leads with
   * the body: it opens OVER the report card the first time it renders for a finished season, counts the age
   * up, grows the figure from last year's frame to this year's against a height rule with both marks on it,
   * counts height / weight / muscle up beside it, then lays out what the season built and — from the
   * veteran years on — what the years took, in red. Every number on it is read back from the same
   * functions the sheet uses (`bodyNowV112`, `zs`, `seasonStats.gains` / `ageChanges` / `ageProfile`), so it
   * cannot show a growth the game did not do. Tapping anywhere fast-forwards; the button hands over to the
   * report card, and the coach (v119/v122) waits at the door until it does. Once a season
   * (`player.growSeenV132` = `totalSeasons`); `TU("growScreenV132",0)` or `?noGrowV132` switches it off;
   * reduced motion shows the finished state. `window.__GROW_V132`; `growcheck.mjs`. */
  const GROW_CSS_V132 = `
#growV132{position:fixed;inset:0;z-index:400;display:flex;align-items:center;justify-content:center;padding:18px 14px calc(18px + env(safe-area-inset-bottom));box-sizing:border-box;overflow-y:auto;-webkit-overflow-scrolling:touch;color:#f2efe4;font-family:'Barlow Condensed','Oswald',sans-serif;background:radial-gradient(120% 80% at 50% 0%,#1a2740 0,#0b1119 55%,#05080c 100%);opacity:0;transition:opacity .38s ease}
#growV132.on{opacity:1}#growV132.out{opacity:0;pointer-events:none}
#growV132 .gw-fx{position:absolute;inset:0;width:100%;height:100%;pointer-events:none;mix-blend-mode:screen}
#growV132 .gw-glow{position:absolute;left:50%;top:38%;width:120vw;height:120vw;max-width:720px;max-height:720px;transform:translate(-50%,-50%);border-radius:50%;pointer-events:none;background:radial-gradient(circle,rgba(240,187,69,.22) 0,rgba(240,187,69,.06) 30%,rgba(240,187,69,0) 62%);animation:gwGlow 5s ease-in-out infinite}
#growV132.decline .gw-glow{background:radial-gradient(circle,rgba(178,59,59,.24) 0,rgba(178,59,59,.06) 30%,rgba(0,0,0,0) 62%)}
@keyframes gwGlow{0%,100%{opacity:.8;transform:translate(-50%,-50%) scale(1)}50%{opacity:1;transform:translate(-50%,-50%) scale(1.08)}}
#growV132 .gw-in{position:relative;width:100%;max-width:460px;margin:auto;text-align:center}
#growV132 .gw-eyebrow{font:600 11px/1 Oswald,sans-serif;letter-spacing:4px;color:#f0bb45;text-transform:uppercase;opacity:0;transform:translateY(8px);transition:opacity .5s ease,transform .5s ease}
#growV132 .gw-title{font:700 clamp(34px,10vw,52px)/.95 Oswald,sans-serif;letter-spacing:1px;margin-top:10px;text-transform:uppercase;background:linear-gradient(180deg,#fff6d8 0,#f0bb45 55%,#b98418 100%);-webkit-background-clip:text;background-clip:text;color:transparent;filter:drop-shadow(0 6px 18px rgba(240,187,69,.25));opacity:0;transform:translateY(12px) scale(.96);transition:opacity .55s ease .08s,transform .55s cubic-bezier(.2,.9,.3,1.2) .08s}
#growV132.decline .gw-title{background-image:linear-gradient(180deg,#ffd9d9 0,#e08a8a 50%,#8c2b2b 100%);filter:drop-shadow(0 6px 18px rgba(178,59,59,.3))}
#growV132.steady .gw-title{background-image:linear-gradient(180deg,#f7fbff 0,#bcd3ea 55%,#6f8aa8 100%);filter:drop-shadow(0 6px 18px rgba(120,160,220,.22))}
#growV132 .gw-sub{font-size:14px;letter-spacing:1.5px;color:#93a0b1;margin-top:8px;text-transform:uppercase;opacity:0;transition:opacity .5s ease .2s}
#growV132 .gw-age{display:flex;align-items:baseline;justify-content:center;gap:10px;margin:14px 0 2px;opacity:0;transition:opacity .4s ease}
#growV132 .gw-age b{font:700 clamp(58px,17vw,84px)/1 Oswald,sans-serif;color:#fff;text-shadow:0 0 30px rgba(240,187,69,.35);display:inline-block;min-width:1.1em}
#growV132 .gw-age b.pop{animation:gwPop .5s cubic-bezier(.2,1.2,.3,1)}
#growV132 .gw-age small{font:600 12px/1 Oswald,sans-serif;letter-spacing:3px;color:#93a0b1}
@keyframes gwPop{0%{transform:scale(1)}35%{transform:scale(1.18);text-shadow:0 0 40px rgba(240,187,69,.9)}100%{transform:scale(1)}}
#growV132 .gw-stage{font:600 11px/1 Oswald,sans-serif;letter-spacing:3px;color:#f0bb45;text-transform:uppercase;opacity:0;transition:opacity .4s ease}
#growV132.decline .gw-stage{color:#e08a8a}
#growV132 .gw-body{display:grid;grid-template-columns:132px 1fr;gap:12px;align-items:stretch;margin-top:16px;opacity:0;transform:translateY(10px);transition:opacity .5s ease,transform .5s ease}
#growV132 .gw-fig{position:relative;height:206px;border:1px solid rgba(255,255,255,.08);border-radius:14px;background:linear-gradient(180deg,rgba(255,255,255,.03),rgba(0,0,0,.25));overflow:hidden}
#growV132 .gw-rule{position:absolute;left:0;right:0;height:0;border-top:1px dashed rgba(255,255,255,.28);font:600 9px/1 Oswald,sans-serif;letter-spacing:1px;color:#93a0b1;text-align:left;padding-left:6px;transition:bottom 1.2s cubic-bezier(.2,.9,.3,1.1)}
#growV132 .gw-rule span{position:absolute;left:5px;top:-11px;background:#0b1119;padding:0 3px;border-radius:3px}
#growV132 .gw-rule.was span{left:auto;right:5px;top:2px;color:#6d7a8c}
#growV132 .gw-rule.now{border-top:1px solid #f0bb45;color:#f0bb45;box-shadow:0 0 12px rgba(240,187,69,.35)}
#growV132.decline .gw-rule.now{border-top-color:#e08a8a;color:#e08a8a;box-shadow:0 0 12px rgba(224,138,138,.3)}
#growV132 .gw-man,#growV132 .gw-ghost{position:absolute;left:50%;bottom:10px;width:128px;height:160px;transform-origin:50% 100%;transform:translateX(-50%)}
#growV132 .gw-ghost{opacity:.3;filter:grayscale(.6) brightness(.9)}
#growV132 .gw-ghost path,#growV132 .gw-ghost circle,#growV132 .gw-ghost rect{fill:none!important;stroke:#93a0b1;stroke-width:2;stroke-dasharray:4 3}
#growV132 .gw-cv{position:absolute;left:0;top:0;width:128px;height:160px;image-rendering:pixelated;image-rendering:crisp-edges;transform-origin:50% 100%}
#growV132 .gw-cv.hi{image-rendering:auto;filter:drop-shadow(0 6px 10px rgba(0,0,0,.55))}
#growV132 .gw-ghost .gw-cv.hi{filter:grayscale(.7) brightness(.85) opacity(.9)}
#growV132 .gw-man .gw-cv{animation:gwBreath 3.4s ease-in-out infinite}
@keyframes gwBreath{0%,100%{transform:scale(1,1)}50%{transform:scale(1.014,1.026)}}
#growV132 .gw-svg{position:absolute;left:32px;top:0;width:64px;height:160px}
#growV132 .gw-man{transition:transform 1.25s cubic-bezier(.2,.9,.25,1.15)}
#growV132 .gw-man .gw-svg{filter:drop-shadow(0 8px 14px rgba(0,0,0,.6))}
#growV132 .gw-man.flash .gw-cv,#growV132 .gw-man.flash .gw-svg{animation:gwFlash .6s ease}
@keyframes gwFlash{0%{filter:brightness(1)}30%{filter:brightness(1.9) drop-shadow(0 0 16px rgba(240,187,69,.9))}100%{filter:brightness(1)}}
#growV132 .gw-tiles{display:grid;grid-template-rows:repeat(3,1fr);gap:8px}
#growV132 .gw-tile{position:relative;display:flex;flex-direction:column;justify-content:center;align-items:flex-start;padding:8px 12px;border:1px solid rgba(255,255,255,.08);border-radius:12px;background:linear-gradient(180deg,rgba(255,255,255,.04),rgba(0,0,0,.22));text-align:left;overflow:hidden}
#growV132 .gw-tile small{font:600 10px/1 Oswald,sans-serif;letter-spacing:2.5px;color:#93a0b1}
#growV132 .gw-tile b{font:700 26px/1.05 Oswald,sans-serif;color:#fff;margin-top:3px;font-variant-numeric:tabular-nums}
#growV132 .gw-tile i{position:absolute;right:10px;top:50%;transform:translateY(-50%) scale(.6);font:700 13px/1 Oswald,sans-serif;font-style:normal;letter-spacing:1px;color:#0b1119;background:linear-gradient(180deg,#8ee07c,#4fa844);padding:5px 8px;border-radius:999px;opacity:0;transition:opacity .3s ease,transform .45s cubic-bezier(.2,1.4,.3,1)}
#growV132 .gw-tile i.on{opacity:1;transform:translateY(-50%) scale(1)}
#growV132 .gw-tile i.zero{background:#2a3444;color:#93a0b1}
#growV132 .gw-tile i.neg{background:linear-gradient(180deg,#e08a8a,#a33a3a);color:#fff}
#growV132 .gw-tile .gw-bar{position:absolute;left:0;bottom:0;height:3px;width:0;background:linear-gradient(90deg,#f0bb45,#ffe9a0);transition:width 1.2s cubic-bezier(.2,.9,.3,1)}
#growV132 .gw-sect{margin-top:14px;opacity:0;transform:translateY(8px);transition:opacity .45s ease,transform .45s ease;text-align:left}
#growV132 .gw-h{font:600 10px/1 Oswald,sans-serif;letter-spacing:3px;color:#f0bb45;text-transform:uppercase;margin-bottom:7px;display:flex;align-items:center;gap:8px}
#growV132 .gw-h:after{content:"";flex:1;height:1px;background:linear-gradient(90deg,rgba(240,187,69,.5),transparent)}
#growV132 .gw-h.red{color:#e08a8a}#growV132 .gw-h.red:after{background:linear-gradient(90deg,rgba(224,138,138,.5),transparent)}
#growV132 .gw-h.blue{color:#9cbcff}#growV132 .gw-h.blue:after{background:linear-gradient(90deg,rgba(156,188,255,.5),transparent)}
#growV132 .gw-pills{display:flex;flex-wrap:wrap;gap:6px}
#growV132 .gw-pill{display:inline-flex;align-items:center;gap:6px;padding:5px 9px;border-radius:999px;border:1px solid rgba(255,255,255,.1);background:rgba(255,255,255,.04);font-size:12.5px;letter-spacing:.6px;color:#dfe6ef;opacity:0;transform:translateY(6px) scale(.9);transition:opacity .3s ease,transform .4s cubic-bezier(.2,1.2,.3,1)}
#growV132 .gw-pill.on{opacity:1;transform:none}
#growV132 .gw-pill b{font:700 13px/1 Oswald,sans-serif;color:#8ee07c}
#growV132 .gw-pill.neg{border-color:rgba(224,138,138,.35);background:rgba(178,59,59,.12)}#growV132 .gw-pill.neg b{color:#ff9b93}
#growV132 .gw-pill.lift b{color:#f0bb45}
#growV132 .gw-note{font-size:13px;color:#93a0b1;margin-top:7px;line-height:1.35}
#growV132 .gw-ovr{display:flex;align-items:center;justify-content:center;gap:10px;margin-top:16px;font:700 15px/1 Oswald,sans-serif;letter-spacing:2px;color:#93a0b1;opacity:0;transition:opacity .4s ease}
#growV132 .gw-ovr b{font-size:28px;color:#fff}#growV132 .gw-ovr b.up{color:#8ee07c}#growV132 .gw-ovr b.dn{color:#ff9b93}
#growV132 .gw-ovr em{font-style:normal;color:#f0bb45}
#growV132 .gw-go{display:block;width:100%;margin-top:16px;padding:14px;border:0;border-radius:12px;background:linear-gradient(180deg,#ffd76f,#d9a028 55%,#b47c14);color:#1a1206;font:700 15px/1 Oswald,sans-serif;letter-spacing:2.5px;cursor:pointer;opacity:0;transform:translateY(6px);transition:opacity .4s ease,transform .4s ease;box-shadow:0 10px 26px rgba(240,187,69,.22)}
#growV132 .gw-go.on{opacity:1;transform:none}
#growV132 .gw-go:active{transform:scale(.98)}
#growV132.done .gw-eyebrow,#growV132.done .gw-title,#growV132.done .gw-sub,#growV132.done .gw-age,#growV132.done .gw-stage,#growV132.done .gw-body,#growV132.done .gw-sect,#growV132.done .gw-ovr{opacity:1;transform:none}
#growV132 .gw-skip{position:absolute;right:10px;top:calc(10px + env(safe-area-inset-top));font:600 10px/1 Oswald,sans-serif;letter-spacing:2px;color:#93a0b1;background:transparent;border:1px solid rgba(255,255,255,.12);border-radius:999px;padding:7px 10px;cursor:pointer}
@media (max-width:360px){#growV132 .gw-body{grid-template-columns:112px 1fr}#growV132 .gw-tile b{font-size:22px}}
@media (prefers-reduced-motion:reduce){#growV132 *{animation:none!important;transition:none!important}}
`;
  function growFigV132(cls) {
    return `<svg class="${cls}" viewBox="0 0 64 160" aria-hidden="true"><path d="M20 158 L20 112 Q18 96 24 84 L40 84 Q46 96 44 112 L44 158 Z" fill="#1d2a3d"/><rect x="14" y="150" width="14" height="8" rx="3" fill="#0b1119"/><rect x="36" y="150" width="14" height="8" rx="3" fill="#0b1119"/><path d="M10 46 Q32 34 54 46 L58 62 Q50 70 48 86 L16 86 Q14 70 6 62 Z" fill="#2c3e5c"/><path d="M6 62 Q3 52 10 46 L14 50 Q9 56 10 64 Z M58 62 Q61 52 54 46 L50 50 Q55 56 54 64 Z" fill="#3a5078"/><circle cx="32" cy="27" r="15" fill="#f0bb45"/><path d="M19 30 Q32 40 45 30 L45 36 Q32 44 19 36 Z" fill="#0b1119"/><path d="M17 26 Q32 20 47 26" stroke="#b98418" stroke-width="2" fill="none"/><rect x="24" y="52" width="16" height="10" rx="2" fill="#f0bb45" opacity=".85"/></svg>`;
  }
  function growDataV132(e) {
    const t = e.seasonStats || {},
      age = e.age,
      prev = age - 1,
      b = e.body || { height: 70, weight: 180, muscle: 50 };
    const b0 = bodyNowV112(b, prev),
      b1 = bodyNowV112(b, age),
      prof = t.ageProfile || ageProfile(age);
    const gains = Object.entries(t.gains || {})
      .filter(([, v]) => v > 0)
      .sort((a, c) => c[1] - a[1]);
    const ch = t.ageChanges || {},
      drops = Object.entries(ch).filter(([, v]) => v && v.delta < 0),
      lifts = Object.entries(ch).filter(([, v]) => v && v.delta > 0);
    const grown = b1.height > b0.height || b1.weight > b0.weight || b1.muscle > b0.muscle,
      decline = (prof.decline || 0) > 0 && (drops.length > 0 || age >= 30);
    let ovr0 = null;
    try {
      ovr0 = t.attrsBefore
        ? playerOvr(Object.assign({}, e, { attrs: Object.assign({}, e.attrs, t.attrsBefore) }))
        : null;
    } catch (_) {
      ovr0 = null;
    }
    const ovr1 = t.newOvr != null ? t.newOvr : playerOvr(e);
    const mode = decline ? "decline" : grown ? "grown" : "steady";
    const title = mode === "decline" ? "THE YEARS ARE TALKING" : mode === "grown" ? "A YEAR OLDER" : "FULLY GROWN";
    const sub =
      mode === "decline"
        ? "the body is losing the argument"
        : mode === "grown"
          ? "and the frame is catching up to the projection"
          : "the frame is finished — now it is all work";
    return {
      age,
      prev,
      b0,
      b1,
      adult: b,
      prof,
      gains,
      drops,
      lifts,
      grown,
      decline,
      mode,
      title,
      sub,
      level: LEVELS[e.level] ? LEVELS[e.level].name : "",
      season: e.totalSeasons || 1,
      ovr0,
      ovr1,
      team: (() => {
        try {
          return teamName(e);
        } catch (_) {
          return "";
        }
      })()
    };
  }
  let _gwAudio = null;
  function growBlipV132(f, ms, gain) {
    if (TU("growSoundV132", 1) === 0) return;
    try {
      const AC = window.AudioContext || window.webkitAudioContext;
      if (!AC) return;
      _gwAudio = _gwAudio || new AC();
      if (_gwAudio.state === "suspended") _gwAudio.resume();
      const t0 = _gwAudio.currentTime,
        o = _gwAudio.createOscillator(),
        g = _gwAudio.createGain();
      o.type = "triangle";
      o.frequency.setValueAtTime(f, t0);
      g.gain.setValueAtTime(0, t0);
      g.gain.linearRampToValueAtTime(gain || 0.05, t0 + 0.008);
      g.gain.exponentialRampToValueAtTime(0.0001, t0 + (ms || 45) / 1000);
      o.connect(g);
      g.connect(_gwAudio.destination);
      o.start(t0);
      o.stop(t0 + (ms || 45) / 1000 + 0.02);
    } catch (_) {}
  }
  function growSparksV132(cv, mode, burstAt) {
    const ctx = cv && cv.getContext && cv.getContext("2d");
    if (!ctx) return { stop() {}, burst() {} };
    const dpr = Math.min(2, window.devicePixelRatio || 1);
    let w = 0,
      h = 0,
      on = true,
      raf = 0,
      last = performance.now();
    const P = [];
    const size = () => {
      const r = cv.getBoundingClientRect();
      w = Math.max(1, r.width);
      h = Math.max(1, r.height);
      cv.width = Math.round(w * dpr);
      cv.height = Math.round(h * dpr);
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
    };
    size();
    const col =
      mode === "decline"
        ? ["#e08a8a", "#8c2b2b", "#c9a1a1"]
        : mode === "steady"
          ? ["#bcd3ea", "#9cbcff", "#ffffff"]
          : ["#f0bb45", "#ffe9a0", "#ffffff"];
    const R = (a, b) => a + Math.random() * (b - a);
    for (let i = 0; i < 40; i++)
      P.push({
        x: R(0, 1),
        y: R(0, 1),
        r: R(0.6, 1.8),
        vx: R(-0.01, 0.01),
        vy: mode === "decline" ? R(0.01, 0.03) : R(-0.03, -0.01),
        a: R(0.1, 0.4),
        c: col[i % 3],
        ph: R(0, 6.3)
      });
    const burst = n => {
      const cx = (burstAt && burstAt().x) || w * 0.5,
        cy = (burstAt && burstAt().y) || h * 0.4;
      for (let i = 0; i < (n || 60); i++) {
        const an = R(0, 6.283),
          sp = R(40, 240);
        P.push({
          x: cx / w,
          y: cy / h,
          r: R(1, 3),
          vx: (Math.cos(an) * sp) / w,
          vy: (Math.sin(an) * sp) / h - 0.25,
          a: R(0.6, 1),
          c: col[i % 3],
          life: R(0.6, 1.3),
          t: 0,
          g: 0.9,
          ph: 0
        });
      }
    };
    const tick = now => {
      if (!on) return;
      const dt = Math.min(0.05, (now - last) / 1000);
      last = now;
      ctx.clearRect(0, 0, w, h);
      for (let i = P.length - 1; i >= 0; i--) {
        const p = P[i];
        if (p.life != null) {
          p.t += dt;
          if (p.t > p.life) {
            P.splice(i, 1);
            continue;
          }
          p.vy += p.g * dt;
          p.x += p.vx * dt;
          p.y += p.vy * dt;
          ctx.globalAlpha = p.a * (1 - p.t / p.life);
        } else {
          p.x += p.vx * dt;
          p.y += p.vy * dt;
          if (p.y < -0.02) p.y = 1.02;
          if (p.y > 1.02) p.y = -0.02;
          if (p.x < -0.02) p.x = 1.02;
          if (p.x > 1.02) p.x = -0.02;
          ctx.globalAlpha = p.a * (0.6 + 0.4 * Math.sin(now / 500 + p.ph));
        }
        ctx.fillStyle = p.c;
        ctx.beginPath();
        ctx.arc(p.x * w, p.y * h, p.r, 0, 6.283);
        ctx.fill();
      }
      ctx.globalAlpha = 1;
      raf = requestAnimationFrame(tick);
    };
    raf = requestAnimationFrame(tick);
    window.addEventListener("resize", size);
    return {
      stop() {
        on = false;
        cancelAnimationFrame(raf);
        window.removeEventListener("resize", size);
      },
      burst
    };
  }
  /* ===== v133 THE BOY IN THE PICTURE =====
   * The growth screen's figure was a hand-drawn SVG that read as a bollard. It is the game's own man now —
   * the v91 sheet's front-facing idle (`idle_dn`, 48px, cut and recoloured by the chase's `cell()`, in the
   * team's colours off the menu feed) — drawn in two pieces so his PROPORTIONS follow his age: the helmet
   * (rows 0–17 of the cell) sits on the body (rows 18–47) at its own scale. `growStageV133(age)` is the
   * curve: a child is small with a helmet too big for him, a teen is all limbs (narrow, the helmet
   * settling), a college man is drawn to scale. His SIZE still comes from `bodyNowV132`'s frame through
   * the `.gw-man` transform; this only decides the shape inside it. The canvas breathes on its own clock
   * (`gwBreath`, off under reduced motion); the ghost is last year's shape at last year's size. With no
   * sheet decoded the SVG stands in. `window.__GROW_V132.stage` / `.fig`. */
  const GROW_NECK_V133 = 18; // the row of idle_dn where the helmet ends and the shoulders begin (measured)
  function growStageV133(age) {
    const a = clamp99(Number(age) || 18, 8, 22),
      L = (x0, y0, x1, y1) => y0 + (y1 - y0) * clamp99((a - x0) / (x1 - x0), 0, 1);
    const head = a <= 12 ? L(8, 1.5, 12, 1.32) : a <= 15 ? L(12, 1.32, 15, 1.12) : L(15, 1.12, 18, 1);
    const width = a <= 12 ? L(8, 1.06, 12, 1) : a <= 15 ? L(12, 1, 15, 0.84) : L(15, 0.84, 18, 1);
    return { head: +head.toFixed(3), width: +width.toFixed(3), name: a <= 12 ? "child" : a <= 17 ? "teen" : "grown" };
  }
  function growKitV133() {
    try {
      const d = window.__RIB_MENU_DATA_V89 && window.__RIB_MENU_DATA_V89();
      const c = d && d.team && d.team.colors;
      if (c && c.length >= 2 && /^#[0-9a-f]{6}$/i.test(c[0]) && /^#[0-9a-f]{6}$/i.test(c[1])) return [c[0], c[1]];
    } catch (_) {}
    return "you";
  }
  /* ===== v134 THE MAN IN THE PICTURE =====
   * v133 put the sheet's own idle man on the growth screen -- the right man, but the 48px cell blown up
   * three times over, so what stood there was a block of pixels the size of a hand and it did not read as a
   * person. The same cell exists in the source art at native size (getup.png, row 0, cell 4: 109x185),
   * and `scripts/build-grow-art.py` cuts it, palette-normalised exactly as the sheet is, into
   * public/grow/idle_dn_hi.png. `growHiV134` loads it once and recolours it with ribRecolor's own bands
   * (navy -> primary, gold -> secondary) into a canvas, and growDrawV133 draws THAT, smoothed, in the same
   * two pieces at the same age proportions -- the helmet on the body, a child's helmet too big for him.
   * The 48px cell is still the fallback until the file lands, so nothing waits. `__GROW_V132.hi`. */
  const GROW_HI_V134 = { src: null, img: null, tried: !1, cache: {}, neck: 0.372 }; // neck: where the helmet ends, as a fraction of the drawn height (row 18 of the cell's 44 ink rows)
  function growHiLoadV134(cb) {
    if (GROW_HI_V134.img) {
      cb && cb(GROW_HI_V134.img);
      return;
    }
    if (GROW_HI_V134.tried) {
      return;
    }
    GROW_HI_V134.tried = !0;
    const im = new Image();
    im.decoding = "async";
    im.onload = () => {
      GROW_HI_V134.img = im;
      cb && cb(im);
    };
    im.onerror = () => {
      GROW_HI_V134.img = null;
    };
    try {
      im.src = window.__RIB_ASSET ? window.__RIB_ASSET("grow/idle_dn_hi.png") : "./public/grow/idle_dn_hi.png";
    } catch (_) {
      im.src = "./public/grow/idle_dn_hi.png";
    }
  }
  function growHiCellV134(kit) {
    const im = GROW_HI_V134.img;
    if (!im) return null;
    const K = Array.isArray(kit) ? kit : null,
      key = K ? K.join("") : String(kit);
    if (GROW_HI_V134.cache[key]) return GROW_HI_V134.cache[key];
    const cv = document.createElement("canvas");
    cv.width = im.naturalWidth;
    cv.height = im.naturalHeight;
    const x = cv.getContext("2d");
    x.drawImage(im, 0, 0);
    if (K) {
      /* v139: this used to be guarded on `typeof ribRecolor == "function"` — a function that lives in
       * another script block and is NOT on window, so the guard was always false and the man on the
       * year-older screen wore the source art's blue and gold whatever the team's palette was. The
       * band logic below is ribRecolor's own, repeated here at full size; it needs nothing from it. */
      const img = x.getImageData(0, 0, cv.width, cv.height),
        d = img.data,
        hx = h => [parseInt(h.slice(1, 3), 16), parseInt(h.slice(3, 5), 16), parseInt(h.slice(5, 7), 16)],
        P = hx(K[0]),
        Q = hx(K[1]);
      for (let i = 0; i < d.length; i += 4) {
        if (d[i + 3] < 20) continue;
        const r = d[i],
          g = d[i + 1],
          b = d[i + 2],
          mx = Math.max(r, g, b),
          mn = Math.min(r, g, b),
          L = (mx + mn) / 2;
        if (L < 38) continue;
        const sat = mx ? (mx - mn) / mx : 0;
        let hue = 0;
        if (mx !== mn) {
          if (mx === r) hue = (60 * ((g - b) / (mx - mn)) + 360) % 360;
          else if (mx === g) hue = 60 * ((b - r) / (mx - mn)) + 120;
          else hue = 60 * ((r - g) / (mx - mn)) + 240;
        }
        let base = null,
          ref = 0;
        if (hue >= 190 && hue <= 265 && sat > 0.15) {
          base = P;
          ref = 95;
        } else if (hue >= 33 && hue <= 62 && sat > 0.3 && L > 60) {
          base = Q;
          ref = 165;
        }
        if (base) {
          const sc = Math.min(1.75, Math.max(0.25, L / ref));
          d[i] = Math.min(255, base[0] * sc);
          d[i + 1] = Math.min(255, base[1] * sc);
          d[i + 2] = Math.min(255, base[2] * sc);
        }
      }
      x.putImageData(img, 0, 0);
    }
    GROW_HI_V134.cache[key] = cv;
    return cv;
  }
  function growDrawHiV134(cv, age, kit) {
    const c = growHiCellV134(kit);
    if (!c || !cv || !cv.getContext) return null;
    const stg = growStageV133(age),
      W = 128,
      H = 160,
      dpr = Math.min(3, window.devicePixelRatio || 1);
    cv.width = Math.round(W * dpr);
    cv.height = Math.round(H * dpr);
    cv.classList.add("hi");
    const x = cv.getContext("2d");
    x.setTransform(dpr, 0, 0, dpr, 0, 0);
    x.imageSmoothingEnabled = true;
    x.imageSmoothingQuality = "high";
    x.clearRect(0, 0, W, H);
    x.save();
    x.fillStyle = "rgba(0,0,0,.42)";
    x.beginPath();
    x.ellipse(W / 2, H - 3, 24, 4.5, 0, 0, 6.283);
    x.fill();
    x.restore();
    // the drawn man's ink box inside the file (the cut pads him into a 128x200 box, feet three rows off the floor)
    const SW = c.width,
      SH = c.height,
      top = 3,
      bot = SH - 3,
      ink = bot - top,
      neck = Math.round(top + ink * GROW_HI_V134.neck);
    const headRows = neck - top,
      bodyRows = bot - neck;
    const k = Math.min(
      (H - 4) / (bodyRows + headRows * stg.head),
      (W - 4) / Math.max(SW * 0.78 * stg.width, SW * 0.66 * stg.head * Math.sqrt(stg.width))
    );
    const bw = SW * k * stg.width,
      bh = bodyRows * k,
      bx = (W - bw) / 2,
      by = H - 2 - bh;
    x.drawImage(c, 0, neck, SW, bodyRows, bx, by, bw, bh);
    const hk = k * stg.head,
      hw = SW * hk * Math.sqrt(stg.width),
      hh = headRows * hk,
      hx2 = (W - hw) / 2,
      hy = by + k * ink * 0.012 - hh; // the chin overlaps the shoulders by a hair
    x.drawImage(c, 0, top, SW, headRows, hx2, hy, hw, hh);
    return { mode: "hi", stage: stg, k: +k.toFixed(3) };
  }
  function growDrawV133(cv, age, kit) {
    const hi = growDrawHiV134(cv, age, kit);
    if (hi) return hi; // v134: the native-resolution man when the file is in; the cell until then
    if (!GROW_HI_V134.img && !GROW_HI_V134.tried)
      growHiLoadV134(() => {
        try {
          const g = document.getElementById("growV132");
          if (!g) return;
          g.querySelectorAll("canvas.gw-cv").forEach(cc => {
            const who = cc.closest("[data-gw-fig]");
            if (!who) return;
            const d = GROW_V132.last;
            if (!d) return;
            growDrawHiV134(cc, who.dataset.gwFig === "ghost" ? d.prev : d.age, growKitV133());
          });
        } catch (_) {}
      });
    const C = window.__CHASE_V94,
      c = C && C.cell ? C.cell("idle_dn", kit) : null;
    if (!c || !cv || !cv.getContext) return null;
    const stg = growStageV133(age),
      W = 128,
      H = 160,
      dpr = Math.min(3, window.devicePixelRatio || 1); // v133: the box is twice the cell's width, so the arms at full height never leave it
    cv.width = Math.round(W * dpr);
    cv.height = Math.round(H * dpr);
    const x = cv.getContext("2d");
    x.setTransform(dpr, 0, 0, dpr, 0, 0);
    x.imageSmoothingEnabled = false;
    x.clearRect(0, 0, W, H);
    x.save();
    x.fillStyle = "rgba(0,0,0,.42)";
    x.beginPath();
    x.ellipse(W / 2, H - 3, 22, 4, 0, 0, 6.283);
    x.fill();
    x.restore(); // the ground under him, drawn here rather than as a CSS filter (a filter rasterises the layer and smooths the pixels)
    const N = GROW_NECK_V133,
      bodyRows = 48 - N - 2,
      headRows = N - 2; // the cell's empty top and bottom rows are not drawn
    // one cell row in canvas px: the whole man fits the box's height, and his INK (the body spans columns
    // 11–37 of the cell, the helmet 12–34) fits its width — the arms were cut off at the sides otherwise
    const k = Math.min(
      (H - 4) / (bodyRows + headRows * stg.head),
      (W - 4) / Math.max(26 * stg.width, 22 * stg.head * Math.sqrt(stg.width))
    );
    const bw = 48 * k * stg.width,
      bh = bodyRows * k,
      bx = (W - bw) / 2,
      by = H - 2 - bh;
    x.drawImage(c, 0, N, 48, bodyRows, bx, by, bw, bh);
    const hk = k * stg.head,
      hw = 48 * hk * Math.sqrt(stg.width),
      hh = headRows * hk,
      hx = (W - hw) / 2,
      hy = by + k * 1.5 - hh; // the chin overlaps the shoulders by a row and a half
    x.drawImage(c, 0, 2, 48, headRows, hx, hy, hw, hh);
    return { mode: "sprite", stage: stg, k: +k.toFixed(3) };
  }
  const GROW_V132 = {
    open: false,
    last: null,
    el: null,
    fx: null,
    timers: [],
    finished: false,
    fig: null,
    get hi() {
      return GROW_HI_V134;
    }
  };
  try {
    growHiLoadV134();
  } catch (_) {} // v134: warm the native-resolution man at boot, so the first growth screen draws him
  function growCloseV132() {
    const g = GROW_V132.el;
    if (!g) return;
    GROW_V132.timers.forEach(clearTimeout);
    GROW_V132.timers = [];
    if (GROW_V132.fx) GROW_V132.fx.stop();
    GROW_V132.fx = null;
    g.classList.add("out");
    GROW_V132.open = false;
    GROW_V132.el = null;
    setTimeout(() => {
      try {
        g.remove();
      } catch (_) {}
    }, 420);
  }
  function growShowV132(e, opts) {
    opts = opts || {};
    if (!e) return null;
    const d = growDataV132(e);
    GROW_V132.last = d;
    if (!opts.replay) {
      e.growSeenV132 = e.totalSeasons;
      saveGame();
    }
    if (!document.getElementById("growCssV132")) {
      const st = document.createElement("style");
      st.id = "growCssV132";
      st.textContent = GROW_CSS_V132;
      document.head.appendChild(st);
    }
    const old = document.getElementById("growV132");
    if (old) old.remove();
    const RM = (() => {
      try {
        return window.matchMedia("(prefers-reduced-motion: reduce)").matches;
      } catch (_) {
        return false;
      }
    })();
    const fmtH = h => fmtHeight(h),
      fmtW = w => w + " lb",
      fmtM = m => m + "%";
    const dH = d.b1.height - d.b0.height,
      dW = d.b1.weight - d.b0.weight,
      dM = d.b1.muscle - d.b0.muscle;
    const adultH = Math.max(d.adult.height || 72, d.b1.height),
      adultW = Math.max(d.adult.weight || 200, d.b1.weight);
    // the figure is drawn to THIS year's frame (the box is his), last year's is the ghost inside it, and the
    // rule marks both — so an inch reads as an inch of daylight between the two lines, not as a 2% scale
    const refH = Math.max(1, d.b1.height),
      refW = Math.max(1, d.b1.weight);
    const scale = b => ({
      sy: Math.max(0.4, (0.98 * b.height) / refH),
      sx: Math.max(0.4, 0.98 * Math.sqrt(b.weight / refW))
    });
    const s0 = scale(d.b0),
      s1 = scale(d.b1);
    const ruleBottom = b => 10 + Math.round(160 * 0.98 * Math.max(0.4, b.height / refH)) + "px";
    const chip = (v, unit) => (v > 0 ? `+${v}${unit}` : v < 0 ? `${v}${unit}` : "—");
    const chipCls = v => (v > 0 ? "" : v < 0 ? "neg" : "zero");
    const pills = (list, cls, sign) =>
      list
        .map(([k, v]) => {
          const n = typeof v === "number" ? v : v.delta;
          const L = ATTR_INFO[k] || { icon: "", name: k };
          return `<span class="gw-pill ${cls}"><span>${L.icon} ${L.name}</span><b>${n > 0 ? "+" : ""}${n}</b></span>`;
        })
        .join("");
    const seasonPills = d.gains.length
      ? pills(d.gains, "gain")
      : `<span class="gw-pill on"><span>No attribute moved this season</span></span>`;
    const g = document.createElement("div");
    g.id = "growV132";
    g.className = "grow-v132 " + d.mode;
    g.innerHTML = `<div class="gw-glow"></div><canvas class="gw-fx"></canvas><button class="gw-skip" type="button" data-gw-skip>SKIP ▸</button>
    <div class="gw-in">
      <div class="gw-eyebrow">OFFSEASON · ${d.level} · YEAR ${d.season}</div>
      <div class="gw-title">${d.title}</div>
      <div class="gw-sub">${d.sub}${d.team ? ` · <b style="color:#dfe6ef;font-weight:600">${d.team}</b>` : ""}</div>
      <div class="gw-age"><b data-gw-age>${d.prev}</b><small>YEARS OLD</small></div>
      <div class="gw-stage" data-gw-stage>${d.prof.name}</div>
      <div class="gw-body">
        <div class="gw-fig">
          <div class="gw-rule was" style="bottom:${ruleBottom(d.b0)}"><span>${fmtH(d.b0.height)}</span></div>
          <div class="gw-rule now" data-gw-rule style="bottom:${ruleBottom(d.b0)}"><span data-gw-rule-lab>${fmtH(d.b0.height)}</span></div>
          <div class="gw-ghost" data-gw-fig="ghost"></div><div class="gw-man" data-gw-fig="man"></div>
        </div>
        <div class="gw-tiles">
          <div class="gw-tile"><small>HEIGHT</small><b data-gw-n="ht">${fmtH(d.b0.height)}</b><i class="${chipCls(dH)}">${chip(dH, '"')}</i><div class="gw-bar" data-gw-bar style="--to:${Math.round((100 * d.b1.height) / adultH)}%"></div></div>
          <div class="gw-tile"><small>WEIGHT</small><b data-gw-n="wt">${fmtW(d.b0.weight)}</b><i class="${chipCls(dW)}">${chip(dW, " lb")}</i><div class="gw-bar" data-gw-bar style="--to:${Math.round((100 * d.b1.weight) / adultW)}%"></div></div>
          <div class="gw-tile"><small>MUSCLE</small><b data-gw-n="ms">${fmtM(d.b0.muscle)}</b><i class="${chipCls(dM)}">${chip(dM, "")}</i><div class="gw-bar" data-gw-bar style="--to:${d.b1.muscle}%"></div></div>
        </div>
      </div>
      <div class="gw-sect" data-gw-sect><div class="gw-h">${d.mode === "decline" ? "WHAT THE SEASON STILL BUILT" : "THE SEASON'S WORK"}</div><div class="gw-pills">${seasonPills}</div></div>
      ${d.drops.length || d.decline ? `<div class="gw-sect" data-gw-sect><div class="gw-h red">WHAT THE YEARS TOOK</div><div class="gw-pills">${d.drops.length ? pills(d.drops, "neg") : `<span class="gw-pill neg on"><span>Nothing this year — but the clock is running</span></span>`}</div><div class="gw-note">${d.prof.desc || ""}${d.prof.decline ? ` Decline factor <b style="color:#ff9b93">${d.prof.decline}</b> · injury risk <b style="color:#ff9b93">${Math.round(d.prof.inj * 100)}%</b>.` : ""}</div></div>` : ""}
      ${d.lifts.length ? `<div class="gw-sect" data-gw-sect><div class="gw-h blue">WHAT THE YEARS GAVE</div><div class="gw-pills">${pills(d.lifts, "lift")}</div></div>` : ""}
      <div class="gw-ovr">OVR <b data-gw-ovr>${d.ovr0 != null ? d.ovr0 : d.ovr1}</b><em>▸</em><b class="${d.ovr0 != null && d.ovr1 > d.ovr0 ? "up" : d.ovr0 != null && d.ovr1 < d.ovr0 ? "dn" : ""}" data-gw-ovr1>${d.ovr1}</b></div>
      <button class="gw-go" type="button" data-gw-go>SEE THE REPORT CARD ▸</button>
    </div>`;
    document.body.appendChild(g);
    GROW_V132.el = g;
    GROW_V132.open = true;
    GROW_V132.finished = false;
    try {
      const C = window.__RIB_COACH;
      if (C && C.isOpen && C.close) C.close("grow");
    } catch (_) {}
    const man = g.querySelector(".gw-man"),
      ghost = g.querySelector(".gw-ghost");
    // v133: the boy himself — the sheet's idle man at this year's proportions, last year's as the ghost; the SVG only if the sheet never decoded
    {
      const kit = growKitV133();
      let fig = null;
      try {
        const cm = document.createElement("canvas");
        cm.className = "gw-cv";
        fig = growDrawV133(cm, d.age, kit);
        if (fig) {
          man.appendChild(cm);
          const cg = document.createElement("canvas");
          cg.className = "gw-cv";
          growDrawV133(cg, d.prev, kit);
          ghost.appendChild(cg);
        }
      } catch (_) {
        fig = null;
      }
      if (!fig) {
        man.innerHTML = growFigV132("gw-svg");
        ghost.innerHTML = growFigV132("gw-svg");
        fig = { mode: "svg", stage: growStageV133(d.age) };
      }
      GROW_V132.fig = fig;
    }
    man.style.transform = `translateX(-50%) scale(${s0.sx.toFixed(3)},${s0.sy.toFixed(3)})`;
    ghost.style.transform = `translateX(-50%) scale(${s0.sx.toFixed(3)},${s0.sy.toFixed(3)})`;
    const Q = (fn, ms) => {
      const id = setTimeout(() => {
        try {
          fn();
        } catch (_) {}
      }, ms);
      GROW_V132.timers.push(id);
      return id;
    };
    const count = (el, from, to, fmt, ms, tone) => {
      if (from === to) {
        el.textContent = fmt(to);
        return;
      }
      const t0 = performance.now();
      let lastV = from;
      const step = now => {
        if (!el.isConnected) return;
        const p = Math.min(1, (now - t0) / ms),
          v = Math.round(from + (to - from) * (1 - Math.pow(1 - p, 3)));
        if (v !== lastV) {
          lastV = v;
          el.textContent = fmt(v);
          growBlipV132(tone + Math.abs(v - from) * 6, 38, 0.035);
        }
        if (p < 1) requestAnimationFrame(step);
      };
      requestAnimationFrame(step);
    };
    const finish = () => {
      if (GROW_V132.finished) return;
      GROW_V132.finished = true;
      GROW_V132.timers.forEach(clearTimeout);
      GROW_V132.timers = [];
      g.classList.add("done");
      g.querySelector("[data-gw-age]").textContent = d.age;
      g.querySelector('[data-gw-n="ht"]').textContent = fmtH(d.b1.height);
      g.querySelector('[data-gw-n="wt"]').textContent = fmtW(d.b1.weight);
      g.querySelector('[data-gw-n="ms"]').textContent = fmtM(d.b1.muscle);
      man.style.transform = `translateX(-50%) scale(${s1.sx.toFixed(3)},${s1.sy.toFixed(3)})`;
      const r = g.querySelector("[data-gw-rule]");
      r.style.bottom = ruleBottom(d.b1);
      g.querySelector("[data-gw-rule-lab]").textContent = fmtH(d.b1.height);
      g.querySelectorAll("[data-gw-bar]").forEach(b => (b.style.width = b.style.getPropertyValue("--to")));
      g.querySelectorAll(".gw-tile i,.gw-pill,.gw-go").forEach(x => x.classList.add("on"));
      g.querySelector("[data-gw-ovr1]").textContent = d.ovr1;
    };
    g.addEventListener("click", ev => {
      if (ev.target.closest("[data-gw-go]") || ev.target.closest("[data-gw-skip]")) {
        growCloseV132();
        return;
      }
      if (!GROW_V132.finished) finish();
    });
    requestAnimationFrame(() => g.classList.add("on"));
    if (RM || opts.instant) {
      finish();
      return d;
    }
    GROW_V132.fx = growSparksV132(g.querySelector(".gw-fx"), d.mode, () => {
      const r = man.getBoundingClientRect(),
        gr = g.getBoundingClientRect();
      return { x: r.left + r.width / 2 - gr.left, y: r.top + r.height * 0.35 - gr.top };
    });
    // the timeline: the words, the age, the body, the sheet, the number, the door
    Q(() => {
      g.querySelector(".gw-eyebrow").style.opacity = 1;
      g.querySelector(".gw-eyebrow").style.transform = "none";
      const T = g.querySelector(".gw-title");
      T.style.opacity = 1;
      T.style.transform = "none";
      g.querySelector(".gw-sub").style.opacity = 1;
    }, 60);
    Q(() => {
      const a = g.querySelector(".gw-age");
      a.style.opacity = 1;
      const n = a.querySelector("b");
      count(n, d.prev, d.age, v => v, 520, 660);
      Q(() => {
        n.classList.add("pop");
        growBlipV132(880, 120, 0.06);
        g.querySelector("[data-gw-stage]").style.opacity = 1;
      }, 560);
    }, 520);
    Q(() => {
      const b = g.querySelector(".gw-body");
      b.style.opacity = 1;
      b.style.transform = "none";
      Q(() => {
        man.style.transform = `translateX(-50%) scale(${s1.sx.toFixed(3)},${s1.sy.toFixed(3)})`;
        const r = g.querySelector("[data-gw-rule]");
        r.style.bottom = ruleBottom(d.b1);
        count(g.querySelector('[data-gw-n="ht"]'), d.b0.height, d.b1.height, fmtH, 1100, 520);
        count(g.querySelector('[data-gw-n="wt"]'), d.b0.weight, d.b1.weight, fmtW, 1200, 440);
        count(g.querySelector('[data-gw-n="ms"]'), d.b0.muscle, d.b1.muscle, fmtM, 1000, 600);
        g.querySelectorAll("[data-gw-bar]").forEach(x => (x.style.width = x.style.getPropertyValue("--to")));
        Q(() => {
          g.querySelector("[data-gw-rule-lab]").textContent = fmtH(d.b1.height);
          man.classList.add("flash");
          if (GROW_V132.fx) GROW_V132.fx.burst(d.mode === "decline" ? 24 : 70);
          growBlipV132(d.mode === "decline" ? 220 : 1320, 160, 0.07);
          g.querySelectorAll(".gw-tile i").forEach((x, i) =>
            Q(() => {
              x.classList.add("on");
              growBlipV132(990 + i * 120, 60, 0.04);
            }, i * 110)
          );
        }, 1250);
      }, 200);
    }, 1500);
    Q(() => {
      let k = 0;
      g.querySelectorAll("[data-gw-sect]").forEach(sec => {
        sec.style.opacity = 1;
        sec.style.transform = "none";
        sec.querySelectorAll(".gw-pill").forEach(p => {
          Q(() => {
            p.classList.add("on");
            growBlipV132(p.classList.contains("neg") ? 260 : 740 + k * 20, 40, 0.03);
          }, k * 70);
          k++;
        });
      });
    }, 3300);
    Q(() => {
      const o1 = g.querySelector(".gw-ovr");
      o1.style.opacity = 1;
      if (d.ovr0 != null) count(g.querySelector("[data-gw-ovr1]"), d.ovr0, d.ovr1, v => v, 700, 700);
    }, 3900);
    Q(() => {
      g.querySelector("[data-gw-go]").classList.add("on");
      GROW_V132.finished = true;
    }, 4700);
    return d;
  }
  function growMaybeV132(e) {
    try {
      if (!e || !e.seasonStats || state.view !== "result") return false;
      if (TU("growScreenV132", 1) === 0) return false;
      try {
        if (/[?&]noGrowV132\b/.test(location.search)) return false;
      } catch (_) {}
      if (e.growSeenV132 === e.totalSeasons) return false;
      if (document.getElementById("growV132")) return false;
      growShowV132(e);
      return true;
    } catch (_) {
      return false;
    }
  }
  const _AlV132 = screenResult;
  screenResult = function () {
    const r = _AlV132.apply(this, arguments);
    growMaybeV132(state.player);
    return r;
  };
  window.__GROW_V132 = {
    get open() {
      return GROW_V132.open;
    },
    get last() {
      return GROW_V132.last;
    },
    get finished() {
      return GROW_V132.finished;
    },
    get fig() {
      return GROW_V132.fig;
    },
    stage: growStageV133,
    draw: growDrawV133,
    kit: growKitV133,
    data: growDataV132,
    show: opts => growShowV132(state.player, Object.assign({ replay: true }, opts || {})),
    close: growCloseV132,
    finish: () => {
      const g = GROW_V132.el;
      if (g && !GROW_V132.finished) g.click();
    }
  };

  const fc = decorateScreen;
  decorateScreen = function () {
    fc();
    const e = state.player,
      t = byId("screen");
    if (!(!e || !t)) {
      if (state.view === "hub" && !t.querySelector(".age-card")) {
        const a = t.querySelector(".depth-card") || t.querySelector(".card");
        a && a.insertAdjacentHTML("afterend", ageCard(e));
      }
      if (state.view === "result" && !t.querySelector(".age-card")) {
        const a = t.querySelector(".depth-card") || t.querySelector(".season-grade") || t.firstElementChild;
        a && a.insertAdjacentHTML("afterend", ageCard(e));
        const s = e.lastResult || state.lastResult;
        (s &&
          s.youngRecoveries > 0 &&
          t.insertAdjacentHTML(
            "afterbegin",
            `<div class="card tight" style="border-color:var(--good)"><div class="small">🩹 Youth recovery prevented <b style="color:var(--good)">${s.youngRecoveries}</b> game injur${s.youngRecoveries === 1 ? "y" : "ies"} this season.</div></div>`
          ),
          e.retirementPending &&
            (t.insertAdjacentHTML(
              "afterbegin",
              `<div class="banner nfl"><div class="big-emoji">🏛️</div><div class="bt">Mandatory Retirement — Age ${e.age}</div><div class="bs">No player may begin another season after age ${retireAgeV134()}. Your final season is complete.</div></div>`
            ),
            (byId("dock").innerHTML =
              `<button class="btn" onclick="endCareer()">Retire at Age ${e.age} · View Legacy</button>`)));
      }
    }
  };
  const hc = advanceLevel;
  advanceLevel = function () {
    if (state.player && state.player.age >= retireAgeV134()) {
      ((state.player.retirementPending = !0), (state.view = "result"), render());
      return;
    }
    return hc();
  };
  function boot() {
    ((state = loadSave() || freshState()), cleanSaveV150(state)); /* v150 A: a name from an older (or imported) save is cleaned on the way in */
    (function () {
      const R = window.__saveRecoveredV150;
      R &&
        setTimeout(
          () =>
            showToast(
              R.from === "fresh"
                ? "⚠ Your save could not be read — a copy was kept, and a new one started"
                : "⚠ Your save was damaged — restored from the backup" + (R.at ? " of " + new Date(R.at).toLocaleString() : "")
            ),
          TU("recoverToastMsV150", 2600)
        );
    })();
    if ((state.tree || (state.tree = {}), evergreenRefundV146(), state.shop)) {
      const t = {
        genetics: "genetics",
        talent: "talent",
        iron: "iron",
        coachable: "coachable",
        headstart: "headstart"
      };
      for (const a in state.shop)
        t[a] && state.shop[a] > 0 && (state.tree[t[a]] = Math.max(state.tree[t[a]] || 0, state.shop[a]));
      delete state.shop;
    }
    if (
      (state.player && !state.player.body && (state.player.body = Pi({ height: 0, weight: 0, muscle: 0 })),
      state.challenges || (state.challenges = {}),
      state.titlesWon == null && (state.titlesWon = 0),
      state.recordOvr == null && (state.recordOvr = 0),
      state.careersCompleted == null && (state.careersCompleted = 0),
      state.objectivesCompleted == null && (state.objectivesCompleted = 0),
      state.rings == null && (state.rings = 0),
      state.chaos || (state.chaos = {}),
      state.mastery || (state.mastery = {}),
      state.chaosUnlocked == null && (state.chaosUnlocked = !1),
      state.chaosCap == null && (state.chaosCap = Math.max(chaosTotal(), state.chaosUnlocked ? 6 : 0)),
      state.era == null && (state.era = 0),
      state.hof || (state.hof = []),
      state.hofWings == null && (state.hofWings = 0),
      state.posMastery || (state.posMastery = {}),
      state.inventory || (state.inventory = []),
      state.equipped || (state.equipped = {}),
      state.player && !state.player.nemesis && rollNemesis(state.player),
      state.player)
    ) {
      const t = state.player;
      (t.traits || (t.traits = rollTraits()),
        t.teamIdentity || (t.teamIdentity = newTeamIdentity()),
        t.titles == null && (t.titles = 0),
        t.declareBonus == null && (t.declareBonus = 0),
        t.seasonSeed || (t.seasonSeed = randInt(1, 2e9)),
        ensureDepth(t));
    }
    ((state.view === "sim" || state.view === "live" || state.view === "training" || state.view === "event") &&
      (state.view = "hub"),
      render(),
      setTimeout(showTutorial, 1850));
    const e = document.getElementById("splash");
    e &&
      (window.__splashDoneV94
        ? window.__splashDoneV94()
        : (setTimeout(() => e.classList.add("gone"), 1100),
          setTimeout(() => {
            e.parentNode && e.parentNode.removeChild(e);
          }, 1700)));
  }
  window.go = goView;
  /* v20 fix: dock Back buttons reference a global `S` (the state) that was never exported — every `go(S.player...)` onclick threw a ReferenceError, so Back appeared dead on menu-reachable screens. Expose the live state as a getter. */ try {
    Object.defineProperty(window, "S", { configurable: !0, get: () => state });
  } catch (_e) {
    window.S = state;
  }
  /* v20 personality side-effects (two-sided sliders): varMult widens/narrows game-to-game swings around 50, injMult scales injury rolls both ways. v23: perfFlat is NO LONGER a rating bump here — it is a REAL flat stat boost applied inside the sim's attribute accessor (_raw), so it shows up as actual production (and flows into the box-score grade) instead of an abstract "+performance" number. */
  const _pfxEt = rollGamePerf;
  rollGamePerf = function (e, t, a) {
    const fx = e && e.personaFxV20;
    if (!fx) return _pfxEt(e, t, a);
    const r = _pfxEt(e, t || 0, a);
    if (fx.varMult && fx.varMult !== 1) r.perf = clamp99(Math.round(50 + (r.perf - 50) * fx.varMult), 1, 100);
    if ((fx.injMult || 1) > 1 && !r.injured) r.injured = Math.random() < (fx.injMult - 1) * 0.06;
    else if ((fx.injMult || 1) < 1 && r.injured && Math.random() < 1 - fx.injMult) r.injured = !1;
    return r;
  };
  window.__V112_C = {
    htFrac: HT_FRAC_V112,
    wtFrac: WT_FRAC_V112,
    bodyNow: bodyNowV112,
    bodyOf: bodyOfV112,
    frac: fracV112,
    pool: TRAIT_POOL_V112,
    offer: traitOfferV112,
    auto: traitAutoV112,
    traits: TRAITS,
    fit: physFit,
    rank: suggestPositions,
    ovr: calcOvr,
    abandoned: abandonedV112,
    active: () => rerollActiveV112(state && state.player),
    mul: e => rerollMulV112(e || (state && state.player)),
    why: () => rerollWhyV112(state && state.player),
    arm: armRerollV112,
    ledger: () => (state && state.rerollV112) || null,
    cond: e => condMultV54(e || (state && state.player)),
    eff: e => effAttrsV85(e || (state && state.player))
  };
  window.startCareer = startCareer;
  window.confirmNew = confirmNew;
  window.pickPos = pickPos;
  window.advance = advanceLevel;
  window.startSeason = startSeason;
  window.alloc = alloc;
  window.doneUpgrade = doneUpgrade;
  window.autoAllocKey = autoAllocKey;
  window.autoAllocSpread = autoAllocSpread;
  window.endCareer = endCareer;
  window.prestigeReset = prestigeReset;
  window.buy = buyNode;
  window.shopBack = shopBack;
  window.continueNFL = continueNFL;
  window.chooseTraining = chooseTraining;
  window.chooseEvent = chooseEvent;
  window.setBranch = setBranch;
  window.watchLive = watchLive;
  window.quickSimSeason = quickSimSeason;
  window.startSeasonGames = startSeasonGames;
  window.playWeek = playWeek;
  window.simRemainingWeeks = simRemainingWeeks;
  window.finishSeasonGames = finishSeasonGames;
  window.finishWeekGame = finishWeekGame;
  window.setSpeed = setSpeed;
  window.skipLive = skipLive;
  window.setRosterTab = setRosterTab;
  window.respecTree = respecTree;
  window.choosePath = choosePath;
  window.exportSave = exportSave;
  window.importSave = importSave;
  window.hardReset = hardReset;
  window.toggleSetting = toggleSetting;
  window.applyFieldFx = function () {
    var el = document.querySelector("#field");
    if (el) {
      el.style.transform = "";
    }
    try {
      var sc = window.__gridironScene;
      sc && sc.refreshPersp && sc.refreshPersp();
    } catch (e) {}
  };
  function __pushFieldFx() {
    var s = (typeof state !== "undefined" && state && state.settings) || {};
    window.__FIELD_FX = {
      tilt: 0,
      depth: s.fxDepth == null ? 0.78 : s.fxDepth,
      size: s.fxSize == null ? 1.32 : s.fxSize,
      spread: s.fxSpread == null ? 1 : s.fxSpread,
      radius: s.fxRadius == null ? 300 : s.fxRadius,
      zoom: s.fxZoom == null ? 1.16 : s.fxZoom,
      light: s.fxLight == null ? 1 : s.fxLight,
      cam: s.fxCam == null ? 0 : s.fxCam,
      camZoom: s.fxCamZoom == null ? 1 : s.fxCamZoom,
      wx: s.fxWx == null ? 0 : s.fxWx
    };
    window.applyFieldFx();
  }
  window.__fxFmt = function (k, v) {
    v = +v;
    return k === "fxTilt"
      ? Math.round(v) + "°"
      : k === "fxRadius" || k === "fxPersp"
        ? Math.round(v) + "px"
        : Math.round(v * 100) + "%";
  };
  window.fieldFxSet = function (k, v, save) {
    state.settings || (state.settings = {});
    state.settings[k] = parseFloat(v);
    __pushFieldFx();
    var el = document.getElementById(k + "_val");
    el && (el.textContent = window.__fxFmt(k, state.settings[k]));
    if (save)
      try {
        saveGame();
      } catch (e) {}
  }; /* ===== v112 THE CAMERA HAS OPTIONS (the panel) — the picked behaviour is live before the panel closes ===== */
  window.camModeSet112 = function (i) {
    state.settings || (state.settings = {});
    var L = window.__CAM_MODES_V112 || [];
    state.settings.fxCam = Math.max(0, Math.min(L.length ? L.length - 1 : 3, i | 0));
    __pushFieldFx();
    try {
      saveGame();
    } catch (e) {}
    var v = document.getElementById("fxCam_val"),
      d = document.getElementById("fxCam_desc");
    v && (v.textContent = (L[state.settings.fxCam] || {}).n || "");
    d && (d.textContent = (L[state.settings.fxCam] || {}).d || "");
    [].forEach.call(document.querySelectorAll(".camopt112"), function (b, j) {
      b.className = "btn camopt112 " + (j === state.settings.fxCam ? "secondary" : "ghost");
    });
  }; /* ===== v144 H THE SKY HAS OPTIONS (the panel) — a look pinned over the week's roll, and nothing else ===== */
  window.wxModeSet144 = function (i) {
    state.settings || (state.settings = {});
    var L = window.__WX_MODES_V144 || [];
    state.settings.fxWx = Math.max(0, Math.min(L.length ? L.length - 1 : 4, i | 0));
    __pushFieldFx();
    try {
      saveGame();
    } catch (e) {}
    var v = document.getElementById("fxWx_val"),
      d = document.getElementById("fxWx_desc");
    v && (v.textContent = (L[state.settings.fxWx] || {}).n || "");
    d && (d.textContent = (L[state.settings.fxWx] || {}).d || "");
    [].forEach.call(document.querySelectorAll(".wxopt144"), function (b, j) {
      b.className = "btn wxopt144 " + (j === state.settings.fxWx ? "secondary" : "ghost");
    });
  };
  window.fieldFxReset = function () {
    state.settings || (state.settings = {});
    [
      "fxTilt",
      "fxPersp",
      "fxDepth",
      "fxSize",
      "fxSpread",
      "fxRadius",
      "fxZoom",
      "fxLight",
      "fxCam",
      "fxCamZoom",
      "fxWx"
    ].forEach(function (k) {
      delete state.settings[k];
    });
    try {
      saveGame();
    } catch (e) {}
    __pushFieldFx();
    state.view === "settings" && screenSettings();
  };
  __pushFieldFx();
  window.declareAdvance = declareAdvance;
  window.declareFromHub = declareFromHub;
  window.chooseTier = chooseTier;
  window.setStatsTab = setStatsTab;
  window.setLeadPos = setLeadPos;
  window.setLeadSort = setLeadSort;
  window.setChaos = setChaos;
  window.chaosMaxAll = chaosMaxAll;
  window.buyMastery = buyMastery;
  window.screenDynasty = screenDynasty;
  window.bigMoment = bigMoment;
  window.equipGear = equipGear;
  window.scrapGear = scrapGear;
  window.screenHof = screenHof;
  window.screenLocker = screenLocker;
  bootV140();
  __pushFieldFx(); /* ===== v140 THE BOOT CANNOT TAKE THE REST OF THE FILE WITH IT =====
   * `mc()` — load the save, restore its view, draw it — is called at the TOP LEVEL of this block,
   * hundreds of lines above the end of it. So a screen that throws while being restored does not
   * just fail to draw: it aborts this block's remaining top level, and everything still to be
   * assigned down there (`window.__GRIDIRON_AUDIT__`, the v137 vault glue, the patch layer) never
   * exists. The game dies on the splash, and because the save still holds that view it dies there
   * on every reload, for good. That is exactly how a save sitting on either career-end screen
   * bricked: `ms`/`no` call `vaultPayBtnV137`, which was assigned 600 lines below them.
   *
   * The call is guarded now, and the retry is DEFERRED to a task: by the time it runs this block has
   * finished, so everything the screen needs is defined and the same render usually just works —
   * which is the whole bug. If it still fails, the menu is a place you can play from, rather than a
   * dead splash. Nothing is swallowed: both attempts go to the console. */
  function safeBootV140(run, label) {
    try {
      run();
      return;
    } catch (e) {
      try {
        console.error('[v140] the boot step "' + label + '" threw; retrying once this script block has finished', e);
      } catch (_) {}
      setTimeout(function () {
        try {
          run();
          try {
            const b = document.querySelector("#splash .splash-title b");
            if (b && /^ERROR/.test(b.textContent || "")) b.textContent = "RUNNING IT BACK";
          } catch (_) {}
        } catch (e2) {
          try {
            console.error('[v140] "' + label + '" still will not run — falling back to the menu', e2);
          } catch (_) {}
          try {
            state.view = "menu";
          } catch (_) {}
          try {
            window.go ? window.go("menu") : render();
          } catch (_) {}
        }
      }, 0);
    }
  }
  function bootV140() {
    safeBootV140(function () {
      boot();
    }, "restore the saved screen");
  }
  function ensureWorld(e) {
    return e
      ? (Array.isArray(e.decisionQueue) || (e.decisionQueue = []),
        Array.isArray(e.careerArcs) || (e.careerArcs = []),
        e.worldState || (e.worldState = { coachEra: 0, teamChemistry: 50, programMomentum: 50, mediaHeat: 20 }),
        e.tempEffects || (e.tempEffects = {}),
        e.schoolRep == null && (e.schoolRep = 0),
        e.decisionCount == null && (e.decisionCount = 0),
        e)
      : null;
  }
  function co(e) {
    (ensureWorld(e),
      Object.keys(e.tempEffects).forEach(t => {
        const a = e.tempEffects[t];
        a && typeof a == "object" && a.games != null && (a.games--, a.games <= 0 && delete e.tempEffects[t]);
      }));
  }
  function Vn(e, t) {
    let a = 0;
    return (
      Object.values((e && e.tempEffects) || {}).forEach(s => {
        s && s.fx && s.fx[t] && (a += s.fx[t]);
      }),
      a
    );
  }
  const pc = [
    {
      id: "film",
      title: "The Film Room Is Waiting",
      copy: "Coaches found several correctable mistakes. How do you spend the night?",
      choices: [
        {
          n: "Stay Late With Coaches",
          d: "Build trust and awareness, but fatigue may follow.",
          risk: "Reliable · mild fatigue risk",
          good: 0.78,
          win: { trust: 5, mental: 1, arc: "Film-room regular" },
          lose: { trust: 1, injuryRisk: 0.08, nextPerf: -4 }
        },
        {
          n: "Study Alone",
          d: "Smaller trust gain, stronger personal development.",
          risk: "Safe · steady growth",
          good: 0.9,
          win: { mental: 1, rep: 1 },
          lose: { nextPerf: -2 }
        },
        {
          n: "Clear Your Head",
          d: "Prioritize recovery and confidence instead.",
          risk: "High variance",
          good: 0.58,
          win: { recovery: 1, nextPerf: 7, hype: 2 },
          lose: { trust: -4, media: 5 }
        }
      ]
    },
    {
      id: "practice",
      title: "A Starting Spot Is Within Reach",
      copy: "The staff opens a competitive practice after your latest performance.",
      choices: [
        {
          n: "Challenge the Player Ahead of You",
          d: "A direct depth-chart battle with major upside.",
          risk: "Risky · role-changing",
          good: 0.52,
          win: { trust: 8, depth: 10, hype: 4, arc: "Won a depth-chart duel" },
          lose: { trust: -5, injuryRisk: 0.16, nextPerf: -5 }
        },
        {
          n: "Master Your Current Role",
          d: "Bank trust through consistency.",
          risk: "Safe · lower ceiling",
          good: 0.86,
          win: { trust: 4, depth: 3, nextPerf: 2 },
          lose: { trust: -1 }
        },
        {
          n: "Ask for Special Teams Snaps",
          d: "Earn extra opportunities with added injury exposure.",
          risk: "Moderate · more playing time",
          good: 0.68,
          win: { trust: 6, snap: 0.1, rep: 2 },
          lose: { injuryRisk: 0.13, trust: -2 }
        }
      ]
    },
    {
      id: "media",
      title: "The Cameras Find You",
      copy: "A reporter asks about your role and the team after the game.",
      choices: [
        {
          n: "Praise the Team",
          d: "Low-risk leadership answer.",
          risk: "Safe · chemistry boost",
          good: 0.9,
          win: { trust: 3, chem: 7, hype: 2 },
          lose: { hype: -1 }
        },
        {
          n: "Say You Deserve More Snaps",
          d: "Apply public pressure to the coaching staff.",
          risk: "Volatile · large snap upside",
          good: 0.46,
          win: { snap: 0.15, hype: 9, arc: "Publicly demanded a larger role" },
          lose: { trust: -9, media: 12, arc: "Clashed with the staff" }
        },
        {
          n: "Guarantee a Win",
          d: "Create a high-pressure storyline for next week.",
          risk: "Boom or bust",
          good: 0.5,
          win: { nextPerf: 10, hype: 8, pp: 2 },
          lose: { nextPerf: -7, hype: -7, media: 10 }
        }
      ]
    },
    {
      id: "recovery",
      title: "Your Body Needs Attention",
      copy: "The medical staff offers three very different plans.",
      choices: [
        {
          n: "Full Recovery Protocol",
          d: "Protect health but sacrifice some preparation.",
          risk: "Safe · performance cost",
          good: 0.94,
          win: { recovery: 2, injuryRisk: -0.18, nextPerf: -2 },
          lose: { nextPerf: -4 }
        },
        {
          n: "Play Through It",
          d: "Keep momentum but increase injury risk.",
          risk: "Dangerous · performance upside",
          good: 0.57,
          win: { nextPerf: 9, hype: 4 },
          lose: { injuryRisk: 0.28, physical: -1 }
        },
        {
          n: "Experimental Treatment",
          d: "Could accelerate recovery or create a setback.",
          risk: "Very high variance",
          good: 0.43,
          win: { recovery: 3, physical: 1, nextPerf: 5 },
          lose: { injuryRisk: 0.35, nextPerf: -9 }
        }
      ]
    },
    {
      id: "team",
      title: "The Locker Room Is Splitting",
      copy: "Two groups of teammates are at odds. Your response may define your leadership reputation.",
      choices: [
        {
          n: "Mediate the Conflict",
          d: "Use leadership to repair chemistry.",
          risk: "Moderate · leadership roll",
          good: 0.66,
          win: { chem: 14, trust: 5, rep: 3, arc: "Locker-room leader" },
          lose: { chem: -6, trust: -2 }
        },
        {
          n: "Back the Veterans",
          d: "Gain veteran support but risk alienating younger players.",
          risk: "Political tradeoff",
          good: 0.65,
          win: { trust: 4, nextPerf: 5, chem: 3 },
          lose: { chem: -9, hype: -3 }
        },
        {
          n: "Stay Out of It",
          d: "Avoid immediate risk while the conflict grows.",
          risk: "Safe now · future cost",
          good: 0.82,
          win: { recovery: 1 },
          lose: { chem: -10, media: 5 }
        }
      ]
    },
    {
      id: "opportunity",
      title: "A Bigger Program Calls",
      copy: "A stronger team offers exposure and facilities, but your depth-chart standing would reset.",
      choices: [
        {
          n: "Take the Opportunity",
          d: "Higher program momentum; lose immediate role security.",
          risk: "Long-term upside",
          good: 0.62,
          win: { momentum: 15, rep: 5, depth: -8, arc: "Bet on a bigger program" },
          lose: { depth: -15, trust: -5 }
        },
        {
          n: "Stay Loyal",
          d: "Strengthen coach trust and local reputation.",
          risk: "Reliable",
          good: 0.9,
          win: { trust: 7, rep: 4, chem: 5, arc: "Chose loyalty" },
          lose: { momentum: -3 }
        },
        {
          n: "Use the Offer as Leverage",
          d: "Try to secure promises without leaving.",
          risk: "High social risk",
          good: 0.45,
          win: { trust: 4, snap: 0.12, hype: 6 },
          lose: { trust: -10, depth: -5, media: 8 }
        }
      ]
    }
  ];
  function vc(e, t) {
    ensureWorld(e);
    const a = pc.filter(n => !(e.level < 4 && n.id === "opportunity")),
      s = a[Math.floor(Math.random() * a.length)];
    return {
      uid: Date.now() + "-" + Math.random(),
      id: s.id,
      title: s.title,
      copy: s.copy,
      choices: s.choices,
      week: (t && t.week) || e.currentWeek,
      opp: (t && t.opp) || "",
      perf: Number(t && t.perf) || 50
    };
  }
  function yc(e, t) {
    (ensureWorld(e), e.decisionQueue.push(vc(e, t)), saveGame());
  }
  function gc() {
    const e = state.player;
    ensureWorld(e);
    const t = e.decisionQueue[0];
    return t
      ? `<div class="decision-overlay"><div class="decision-panel"><div class="decision-kicker">POST-GAME DECISION · ${t.opp ? escHtml(t.opp) : "CAREER CROSSROADS"}</div><div class="decision-title">${escHtml(t.title)}</div><div class="decision-copy">${escHtml(t.copy)} Your previous game rating was <b>${Math.round(t.perf)}</b>.</div>${t.choices.map((a, s) => `<button class="decision-choice" onclick="resolveDecision102(${s})"><b>${escHtml(a.n)}</b><small>${escHtml(a.d)}</small><span class="decision-risk">${escHtml(a.risk)} · ${Math.round(clamp99(a.good, 0, 1) * 100)}% base success</span></button>`).join("")}</div></div>`
      : "";
  }
  function bc(e) {
    ensureWorld(e);
    const t = e.worldState,
      a = e.careerArcs.slice(0, 4);
    return `<div class="card world-card"><div class="eyebrow">LIVING FOOTBALL WORLD</div><div class="h2" style="margin:2px 0 7px">Your Career Has Consequences</div><div class="depth-grid"><div class="depth-cell"><div class="n">${Math.round(t.teamChemistry)}</div><div class="l">Chemistry</div></div><div class="depth-cell"><div class="n">${Math.round(t.programMomentum)}</div><div class="l">Program</div></div><div class="depth-cell"><div class="n">${Math.round(t.mediaHeat)}</div><div class="l">Media Heat</div></div></div>${a.length ? `<div style="margin-top:8px">${a.map(s => `<span class="arc-chip ${s.tone || ""}">${escHtml(s.text)}</span>`).join("")}</div>` : '<div class="small" style="margin-top:8px">Major decisions will create permanent story arcs here.</div>'}</div>`;
  }
  const processWeek95Core = processWeek95;
  processWeek95 = function (e, t) {
    const a = !!(e && e._pulse95);
    (processWeek95Core(e, t), e && !a && co(t));
  };
  const kc = render;
  render = function () {
    kc();
    const e = state.player,
      t = byId("screen");
    !e ||
      !t ||
      (ensureWorld(e),
      state.view === "hub" &&
        !t.querySelector(".world-card") &&
        (t.querySelector(".depth-card") || t.firstElementChild).insertAdjacentHTML("afterend", bc(e)),
      e.decisionQueue.length &&
        !document.querySelector(".decision-overlay") &&
        document.body.insertAdjacentHTML("beforeend", gc()));
  };
  simRemainingWeeks = function () {
    showToast("Major decisions must be made after every game. Play games individually.");
  };
  const wc = rollGamePerf;
  rollGamePerf = function (e, t, a) {
    const r = wc(e, t || 0, a),
      l = Vn(e, "injuryRisk");
    return (
      l > 0 && !r.injured && Math.random() < l && (r.injured = !0),
      l < 0 && r.injured && Math.random() < Math.min(0.8, Math.abs(l) * 2) && (r.injured = !1),
      r
    );
  };
  const GAME_PLANS = [
    {
      id: "disciplined",
      icon: "📋",
      name: "Disciplined Execution",
      desc: "Reduce volatility, protect the ball, and build composure.",
      perf: 2,
      varMult: 0.55,
      inj: -0.05,
      snap: 0,
      trust: 1,
      comp: 3,
      mom: 1,
      tags: ["LOW VARIANCE", "SAFE"]
    },
    {
      id: "feature",
      icon: "🎯",
      name: "Demand the Spotlight",
      desc: "Ask the staff to feature you. More snaps and upside, but mistakes are magnified.",
      perf: 5,
      varMult: 1.28,
      inj: 0.05,
      snap: 0.1,
      trust: 0,
      comp: -1,
      mom: 2,
      tags: ["MORE SNAPS", "VOLATILE"]
    },
    {
      id: "explosive",
      icon: "⚡",
      name: "Chase the Highlight",
      desc: "Hunt explosive plays. Huge ceiling, higher injury and turnover risk.",
      perf: 7,
      varMult: 1.72,
      inj: 0.12,
      snap: 0.04,
      trust: -1,
      comp: -2,
      mom: 4,
      tags: ["BOOM/BUST", "INJURY RISK"]
    },
    {
      id: "team",
      icon: "🛡️",
      name: "Do the Dirty Work",
      desc: "Block, cover, and execute assignments. Lower box-score ceiling, strong trust gains.",
      perf: -1,
      varMult: 0.8,
      inj: 0.01,
      snap: 0.06,
      trust: 3,
      comp: 2,
      mom: 1,
      tags: ["COACH TRUST", "ROLE GROWTH"]
    },
    /* v18: five more prep paths — real buffs AND real costs, so the weekly scouting/prep choice matters */
    {
      id: "filmgrind",
      icon: "🎬",
      name: "Film Marathon",
      desc: "Bury yourself in tape. You will see the play before it happens — but the extra hours wear on the body.",
      perf: 4,
      varMult: 0.7,
      inj: 0.02,
      snap: 0.02,
      trust: 2,
      comp: 2,
      mom: 0,
      tags: ["PREPARED", "FATIGUE"]
    },
    {
      id: "recover",
      icon: "🛌",
      name: "Rest & Recover",
      desc: "A light week: treatment, sleep, and legs for Sunday. Heals fatigue and cuts injury risk, at the cost of sharpness.",
      perf: -2,
      varMult: 0.75,
      inj: -0.12,
      snap: -0.02,
      trust: 0,
      comp: 1,
      mom: -1,
      rest: 1,
      tags: ["HEALS BODY", "−INJURY RISK"]
    },
    {
      id: "shadow",
      icon: "🕵️",
      name: "Shadow Study",
      desc: "Scout one matchup obsessively and hunt him all game. Big edge if it hits, exposed if the plan changes.",
      perf: 6,
      varMult: 1.4,
      inj: 0.03,
      snap: 0.03,
      trust: 0,
      comp: -1,
      mom: 2,
      tags: ["SCOUTED EDGE", "VOLATILE"]
    },
    {
      id: "redzone",
      icon: "🎯",
      name: "Red Zone Package",
      desc: "Drill the money downs. Fewer touches, but the ones you get are near the paint.",
      perf: 3,
      varMult: 1.1,
      inj: 0.02,
      snap: -0.03,
      trust: 1,
      comp: 1,
      mom: 2,
      tags: ["SCORING", "FEWER SNAPS"]
    },
    {
      id: "enforcer",
      icon: "💪",
      name: "Set the Tone",
      desc: "Play violent and physical from the first snap. Intimidation buffs the whole team — your body pays the tax.",
      perf: 4,
      varMult: 1.2,
      inj: 0.1,
      snap: 0.04,
      trust: 1,
      comp: -2,
      mom: 5,
      tags: ["TEAM BUFF", "INJURY RISK"]
    }
  ];
  function ensureWeekly103(e) {
    return e
      ? (e.momentum103 == null && (e.momentum103 = 50),
        e.composure103 == null && (e.composure103 = 50),
        e.roleBattle103 == null && (e.roleBattle103 = 0),
        e.breakthroughs103 == null && (e.breakthroughs103 = 0),
        Array.isArray(e.weeklyHistory103) || (e.weeklyHistory103 = []),
        e.weeklyPlan103 === void 0 && (e.weeklyPlan103 = null),
        e)
      : null;
  }
  function fo(e) {
    const t = (e.depthRole || e.depthChart || "Bench").toString().toLowerCase();
    return t.includes("bench")
      ? "Second String"
      : t.includes("second")
        ? "Rotation"
        : t.includes("rotation")
          ? "Starter"
          : t.includes("starter") && !t.includes("first")
            ? "First String"
            : "Team Star";
  }
  function gamePlanById(e) {
    return GAME_PLANS.find(t => t.id === e) || GAME_PLANS[0];
  }
  function weeklyLoopCard(e) {
    ensureWeekly103(e);
    const t = e.weeklyPlan103 ? gamePlanById(e.weeklyPlan103) : null;
    return `<div class="card weekly-loop-card"><div class="eyebrow">WEEKLY COMPETITIVE LOOP</div><div class="h2" style="margin:2px 0 3px">Earn the Next Role</div><div class="small">Outperform your opportunities, build trust, and fill the battle meter to challenge for <b style="color:var(--gold)">${fo(e)}</b>.</div><div class="loop-grid"><div class="loop-cell"><div class="n" style="color:${e.momentum103 >= 60 ? "var(--good)" : e.momentum103 < 40 ? "#e08a8a" : "var(--chalk)"}">${Math.round(e.momentum103)}</div><div class="l">Momentum</div></div><div class="loop-cell"><div class="n" style="color:${e.composure103 >= 60 ? "#8ec3ee" : e.composure103 < 40 ? "#e08a8a" : "var(--chalk)"}">${Math.round(e.composure103)}</div><div class="l">Composure</div></div><div class="loop-cell"><div class="n" style="color:var(--gold)">${e.breakthroughs103 || 0}</div><div class="l">Breakthroughs</div></div></div><div class="role-track"><i style="width:${clamp99(e.roleBattle103, 0, 100)}%"></i></div><div class="small" style="margin-top:5px">Depth-chart battle: <b>${Math.round(e.roleBattle103)}%</b>${t ? `<br><span class="plan-chip">${t.icon} ${t.name}</span>` : ""}</div></div>`;
  }
  function gamePlanOverlay(e) {
    const t = state.player;
    ensureWeekly103(t);
    const a = t.weekResults ? t.weekResults.findIndex(n => !n.played) : -1,
      s = a >= 0 ? t.weekResults[a] : null;
    return `<div class="gameplan-overlay"><div class="gameplan-panel"><div class="decision-kicker">WEEKLY GAME PLAN · ${s ? escHtml(s.opp) : "NEXT OPPONENT"}</div><div class="decision-title">How will you earn your snaps?</div><div class="decision-copy">Your role is not guaranteed. Choose an approach that changes performance variance, playing time, trust, injury risk, and role-battle progress.</div>${GAME_PLANS.map((n, i) => `<button class="gameplan-choice" onclick="chooseGamePlan103('${n.id}',${e ? "true" : "false"})"><b>${n.icon} ${n.name}</b><small>${n.desc}</small><div class="gameplan-meta">${n.tags.map(r => `<span>${r}</span>`).join("")}</div></button>`).join("")}<button class="btn ghost" style="margin-top:12px" onclick="closeGamePlan103()">Back</button></div></div>`;
  }
  function prepareWeek103(e) {
    const t = state.player;
    if ((ensureWeekly103(t), t.decisionQueue && t.decisionQueue.length)) {
      showToast("Resolve the post-game decision first.");
      return;
    }
    document.querySelector(".gameplan-overlay") || document.body.insertAdjacentHTML("beforeend", gamePlanOverlay(!!e));
  }
  function closeGamePlan103() {
    const e = document.querySelector(".gameplan-overlay");
    e && e.remove();
  }
  function chooseGamePlan103(e, t) {
    const a = state.player;
    (ensureWeekly103(a), (a.weeklyPlan103 = e));
    const s = a.weekResults.findIndex(r => !r.played);
    if (s < 0) {
      closeGamePlan103();
      return;
    }
    const n = a.weekResults[s],
      i = gamePlanById(e);
    if (!n._plan103) {
      const l = n.perf - 60,
        d = randRange(-10, 10) * i.varMult,
        c = (a.composure103 - 50) * 0.075,
        u = (a.momentum103 - 50) * 0.055;
      ((n.perf = Math.round(clamp99(60 + l * i.varMult + i.perf + d + c + u, 15, 130))),
        i.snap && (a.snapShare = clamp99((a.snapShare || 0.1) + i.snap, 0.06, 0.98)),
        (a.coachTrust = clamp99((a.coachTrust || 50) + i.trust, 0, 100)));
      const h = hypeState();
      ((h.coach = clamp99((h.coach || 50) + i.trust, 0, 100)),
        (a.composure103 = clamp99(a.composure103 + i.comp, 0, 100)),
        (a.momentum103 = clamp99(a.momentum103 + i.mom, 0, 100)),
        i.inj > 0 && !n.injured && Math.random() < i.inj && (n.injured = !0),
        i.inj < 0 && n.injured && Math.random() < Math.abs(i.inj) * 2 && (n.injured = !1));
      const p = ia(a, n.perf, { oppBoost: n.playoff ? 0.08 : 0 });
      ((n.us = p.us), (n.them = p.them), (n.won = p.won), (n._plan103 = e));
    }
    (closeGamePlan103(), saveGame(), playWeek(!!t));
  }
  function vn(e, t) {
    if (!e || e._loop103) return;
    ((e._loop103 = !0), ensureWeekly103(t));
    const a = gamePlanById(e._plan103 || t.weeklyPlan103 || "disciplined"),
      s = clamp99(t.snapShare || 0.12, 0.06, 0.98),
      n = Math.max(0.28, s);
    let r =
      ((e.perf - 55) / Math.max(0.55, n * 1.45)) * 0.16 +
      (e.won ? 2 : -1) +
      (e.perf >= 85 ? 4 : 0) +
      (e.perf < 45 ? -4 : 0);
    (a.id === "team" && (r += 3),
      a.id === "feature" && e.perf >= 75 && (r += 4),
      e.injured && (r -= 4),
      (t.roleBattle103 = clamp99(t.roleBattle103 + r, 0, 125)),
      (t.momentum103 = clamp99(50 + (t.momentum103 - 50) * 0.92 + (e.won ? 5 : -5) + (e.perf - 60) * 0.11, 0, 100)),
      (t.composure103 = clamp99(
        50 +
          (t.composure103 - 50) * 0.95 +
          (Math.abs(e.perf - 60) < 13 ? 2 : 0) +
          (e.perf >= 80 ? 1 : 0) -
          (e.perf < 42 ? 3 : 0),
        0,
        100
      )));
    let l = !1;
    (t.roleBattle103 >= 100 &&
      ((t.roleBattle103 -= 100),
      t.breakthroughs103++,
      (t.coachTrust = clamp99((t.coachTrust || 50) + 10, 0, 100)),
      (t.points = (t.points || 0) + 1),
      (t.snapShare = clamp99((t.snapShare || 0.1) + 0.12, 0.06, 0.98)),
      (l = !0),
      pushStory(
        "🚀",
        "Depth-chart breakthrough",
        "You forced the staff to expand your role after producing with limited opportunities."
      )),
      t.weeklyHistory103.unshift({ perf: e.perf, won: e.won, plan: a.name, battle: Math.round(r), breakthrough: l }),
      (t.weeklyHistory103 = t.weeklyHistory103.slice(0, 8)),
      (t.weeklyPlan103 = null),
      l && setTimeout(() => bigMoment("ROLE EARNED", "More snaps. More responsibility. No guarantees.", "good"), 180));
  }
  const Sc = processWeek95;
  processWeek95 = function (e, t) {
    const a = !!(e && e._loop103);
    (Sc(e, t), e && !a && vn(e, t));
  };
  const Mc = render;
  render = function () {
    Mc();
    const e = state.player,
      t = byId("screen");
    !e ||
      !t ||
      (ensureWeekly103(e),
      (state.view === "hub" || state.view === "season") &&
        !t.querySelector(".weekly-loop-card") &&
        (
          t.querySelector(".world-card") ||
          t.querySelector(".objective-card") ||
          t.firstElementChild
        ).insertAdjacentHTML("afterend", weeklyLoopCard(e)),
      state.view === "season" &&
        (t
          .querySelectorAll('[onclick="playWeek(true)"]')
          .forEach(a => a.setAttribute("onclick", "prepareWeek103(true)")),
        t
          .querySelectorAll('[onclick="playWeek(false)"]')
          .forEach(a => a.setAttribute("onclick", "prepareWeek103(false)"))));
  };
  const xc = playWeek;
  playWeek = function (e) {
    const t = state.player;
    ensureWeekly103(t);
    const a = t.weekResults ? t.weekResults.findIndex(n => !n.played) : -1,
      s = a >= 0 ? t.weekResults[a] : null;
    if (s && !s._plan103) {
      prepareWeek103(!!e);
      return;
    }
    xc(e);
  };
  window.prepareWeek103 = prepareWeek103;
  window.chooseGamePlan103 = chooseGamePlan103;
  window.closeGamePlan103 = closeGamePlan103;
  function statColor(e, t) {
    return e < 35 ? "#ff6b72" : e > 68 ? (t === "comp" ? "#8ec3ee" : "#78e993") : "#f0bb45";
  }
  function Tc(e) {
    return e.depthRole || e.depthChart || "Bench";
  }
  weeklyLoopCard = function (e) {
    ensureWeekly103(e);
    const t = e.weeklyPlan103 ? gamePlanById(e.weeklyPlan103) : null,
      a = Tc(e),
      s = fo(e),
      n = clamp99(e.roleBattle103, 0, 100),
      i = Math.round(e.momentum103),
      r = Math.round(e.composure103);
    return `<div class="card weekly-loop-card impact-loop"><div class="impact-head"><div><div class="impact-kicker">WEEKLY COMPETITIVE LOOP</div><div class="h2" style="margin:2px 0 0">Fight for Every Snap</div></div><div class="impact-target">NEXT: ${escHtml(s)}</div></div><div class="role-arena"><div class="role-labels"><span class="role-current">${escHtml(a)}</span><span class="role-next">${escHtml(s)}</span></div><div class="role-race"><i style="width:${n}%"></i></div><div class="role-percent">${Math.round(n)}<small>% ROLE BATTLE</small></div></div><div class="dual-gauges"><div class="impact-gauge"><div class="gauge-head"><span>MOMENTUM</span><b style="color:${statColor(i, "mom")}">${i}</b></div><div class="gauge-scale"><i class="gauge-needle" style="left:${i}%"></i></div><div class="gauge-caption"><span>SLUMP</span><span>LOCKED IN</span></div></div><div class="impact-gauge"><div class="gauge-head"><span>COMPOSURE</span><b style="color:${statColor(r, "comp")}">${r}</b></div><div class="gauge-scale"><i class="gauge-needle" style="left:${r}%"></i></div><div class="gauge-caption"><span>RATTLED</span><span>ICE COLD</span></div></div></div><div class="breakthrough-counter"><span class="small">Career role breakthroughs</span><b>🚀 ${e.breakthroughs103 || 0}</b></div>${t ? `<div class="plan-selected"><div class="ic">${t.icon}</div><div><b>${t.name}</b><small>Active approach for the next game</small></div></div>` : ""}</div>`;
  };
  function yn(e) {
    return e.id === "disciplined"
      ? [45, 92, 20]
      : e.id === "feature"
        ? [82, 52, 58]
        : e.id === "explosive"
          ? [100, 22, 92]
          : [38, 78, 42];
  }
  function planColor(e) {
    return e === "disciplined" ? "#8ec3ee" : e === "feature" ? "#f0bb45" : e === "explosive" ? "#ff6870" : "#78e993";
  }
  gamePlanOverlay = function (e) {
    const t = state.player;
    ensureWeekly103(t);
    const a = t.weekResults ? t.weekResults.findIndex(n => !n.played) : -1,
      s = a >= 0 ? t.weekResults[a] : null;
    return `<div class="gameplan-overlay"><div class="gameplan-panel impact-plan"><div class="plan-hero"><div class="decision-kicker">WEEKLY TACTICAL MEETING</div><div class="decision-title">Choose Your Identity</div><div class="decision-copy" style="margin:7px auto 0;max-width:390px">This choice changes your snaps, volatility, trust, injury exposure, and path up the depth chart.</div><div class="opponent-chip">VS ${s ? escHtml(s.opp) : "NEXT OPPONENT"}</div></div><div class="plan-deck">${GAME_PLANS.map(
      n => {
        const i = yn(n);
        return `<button class="impact-plan-choice" style="--planColor:${planColor(n.id)}" onclick="chooseGamePlan103('${n.id}',${e ? "true" : "false"})"><div class="plan-icon">${n.icon}</div><div><b>${n.name}</b><small>${n.desc}</small><div class="gameplan-meta">${n.tags.map(l => `<span>${l}</span>`).join("")}</div></div><div class="plan-bars"><div class="mini-rating"><label><span>UPSIDE</span><span>${i[0]}</span></label><div class="mbar"><i style="width:${i[0]}%"></i></div></div><div class="mini-rating"><label><span>CONTROL</span><span>${i[1]}</span></label><div class="mbar"><i style="width:${i[1]}%"></i></div></div><div class="mini-rating"><label><span>RISK</span><span>${i[2]}</span></label><div class="mbar"><i style="width:${i[2]}%"></i></div></div></div></button>`;
      }
    ).join(
      ""
    )}<button class="btn ghost" style="margin:11px 0 4px" onclick="closeGamePlan103()">Back</button></div></div></div>`;
  };
  function breakthroughTakeover104(e) {
    const t = document.querySelector(".breakthrough-takeover");
    (t && t.remove(),
      document.body.insertAdjacentHTML(
        "beforeend",
        '<div class="breakthrough-takeover" onclick="this.remove()"><div class="bt-content"><div class="bt-icon">🏈</div><div class="bt-kicker">DEPTH CHART UPDATE</div><div class="bt-title">ROLE EARNED</div><div class="bt-sub">The coaching staff expanded your role. The next opportunity is bigger—and less forgiving.</div><div class="bt-reward"><span>+12% SNAPS</span><span>+10 TRUST</span><span>+1 POINT</span></div><div class="small" style="margin-top:14px">Tap anywhere to continue</div></div></div>'
      ),
      setTimeout(() => {
        const a = document.querySelector(".breakthrough-takeover");
        a && a.remove();
      }, 5200));
  }
  const Rc = vn;
  vn = function (e, t) {
    const a = t ? Number(t.breakthroughs103 || 0) : 0;
    (Rc(e, t), t && Number(t.breakthroughs103 || 0) > a && setTimeout(() => breakthroughTakeover104(), 220));
  };
  function Pc(e) {
    return (
      ensureWeekly103(e),
      `<div class="live-loop-strip"><div class="live-loop-stat"><b style="color:${statColor(e.momentum103, "mom")}">${Math.round(e.momentum103)}</b><small>Momentum</small></div><div class="live-loop-stat"><b style="color:${statColor(e.composure103, "comp")}">${Math.round(e.composure103)}</b><small>Composure</small></div><div class="live-loop-stat"><b>${Math.round(e.roleBattle103)}%</b><small>Role Battle</small><div class="live-role-mini"><i style="width:${clamp99(e.roleBattle103, 0, 100)}%"></i></div></div></div>`
    );
  }
  const Cc = render;
  render = function () {
    Cc();
    const e = state.player,
      t = byId("screen");
    if (!(!e || !t) && state.view === "live" && !t.querySelector(".live-loop-strip")) {
      const a = t.querySelector(".live-down") || t.querySelector(".field-wrap");
      a && a.insertAdjacentHTML("afterend", Pc(e));
    }
  };
  window.breakthroughTakeover104 = breakthroughTakeover104;
  window.__getGridironState = () => state;
  window.__getGridironLiveSpeed = () => (liveCtl && liveCtl.speed ? liveCtl.speed : 1);
  window.__GRIDIRON_LEGACY_VERSION__ = "10.4-impact-ui";
  function ensureV11(e) {
    if ((ensureAccountState(state), e)) {
      const t = LEVELS[e.level] ? LEVELS[e.level].need : 16,
        a = e.pos
          ? playerOvr(e)
          : Math.round(ATTR_KEYS.reduce((s, n) => s + (e.attrs[n] || 0), 0) / Math.max(1, ATTR_KEYS.length));
      (ensurePlayerState(state, e, ATTR_KEYS, t, a),
        e.decisionQueue && e.decisionQueue.length && (e.decisionQueue = []));
    }
  }
  ensureV11(state && state.player);
  const Lc = treeFx;
  treeFx = function (e) {
    return prestigeCap(e, Lc(e));
  };
  const Ec = pathVal;
  pathVal = function (e, t) {
    return pathCap(e, Ec(e, t));
  };
  GAME_PLANS.some(e => e.id === "recovery") ||
    GAME_PLANS.push({
      id: "recovery",
      icon: "🧊",
      name: "Recovery & Treatment",
      desc: "Reduce workload, accelerate healing, and protect the rest of the season.",
      perf: -3,
      varMult: 0.7,
      inj: -0.12,
      snap: -0.05,
      trust: 0,
      comp: 2,
      mom: -1,
      tags: ["HEAL", "LOW WORKLOAD"]
    });
  const Ac = newPlayer;
  newPlayer = function () {
    const e = Ac();
    return (
      (e.originOptionsV11 = selectOriginOptions(state, e).map(t => t.id)),
      (e.originV11 = null),
      (e._originAppliedV11 = !1),
      ensureV11(e),
      e
    );
  };
  function chooseOriginV11(e) {
    const t = state.player;
    if (!t || t.originV11) return;
    const a = applyOrigin(t, e, ATTR_KEYS);
    a &&
      ((t._originAppliedV11 = !0),
      ensureV11(t),
      saveGame(),
      render(),
      bigMoment(a.name, a.strength + " · " + a.weakness, "gold"));
  }
  const Oc = pickPos;
  pickPos = function (e) {
    if (!state.player.originV11) {
      showToast("Choose this career’s origin first.");
      return;
    }
    ((state.player.positionsPlayedV11 = state.player.positionsPlayedV11 || []),
      state.player.positionsPlayedV11.includes(e) || state.player.positionsPlayedV11.push(e),
      Oc(e),
      ensureV11(state.player));
  };
  function originCard(e) {
    if (e.originV11) {
      const a = ORIGINS.find(s => s.id === e.originV11);
      return a
        ? `<div class="card origin-card-v11 selected"><div class="eyebrow">RUN IDENTITY</div><div class="origin-selected-v11"><span>${a.icon}</span><div><b>${escHtml(a.name)}</b><small>${escHtml(a.description)}</small>${originSayV131(a)}</div></div></div>`
        : "";
    }
    return `<div class="card origin-card-v11"><div class="eyebrow">CHOOSE THIS CAREER'S IDENTITY</div><div class="h2" style="margin:3px 0 4px">Every Run Has a Story</div><div class="small">Origins add a real strength, a real weakness, and three legacy objectives. Every number below is applied to your sheet the moment you choose — nothing here is flavour.</div><div class="origin-grid-v11">${(
      e.originOptionsV11 || []
    )
      .map(a => ORIGINS.find(s => s.id === a))
      .filter(Boolean)
      .map(
        a =>
          `<button class="origin-choice-v11" onclick="chooseOriginV11('${a.id}')"><span class="origin-icon-v11">${a.icon}</span><b>${escHtml(a.name)}</b><small>${escHtml(a.description)}</small><em>▲ ${escHtml(a.strength)}</em><em class="bad">▼ ${escHtml(a.weakness)}</em>${originSayV131(a)}</button>`
      )
      .join("")}</div></div>`;
  }
  function chooseSpecializationV11(e) {
    SPECIALIZATIONS.some(t => t.id === e) &&
      ((state.specializationV11 = e), saveGame(), render(), showToast("Legacy focus changed."));
  }
  function jc() {
    const e = SPECIALIZATIONS.find(t => t.id === state.specializationV11) || SPECIALIZATIONS[0];
    return `<div class="card specialization-card-v11"><div class="eyebrow">PRESTIGE SPECIALIZATION · NO RAW OVR</div><div class="h2" style="margin:3px 0 5px">${e.icon} ${e.name}</div><div class="small">${e.description} Switch freely; this changes strategy rather than making every run automatically stronger.</div><div class="specialization-grid-v11">${SPECIALIZATIONS.map(t => `<button class="spec-btn-v11 ${t.id === state.specializationV11 ? "on" : ""}" onclick="chooseSpecializationV11('${t.id}')"><span>${t.icon}</span><b>${t.name}</b><small>${t.description}</small></button>`).join("")}</div></div>`;
  }
  const depthChartCore = depthChart;
  depthChart = function (e) {
    if (e && e._v11SeasonActive && e.roleRivalV11) {
      const t = clamp99(e.snapShare == null ? 0.12 : e.snapShare, 0.04, 0.98),
        a =
          t >= 0.82
            ? "FIRST STRING"
            : t >= 0.66
              ? "STARTER"
              : t >= 0.38
                ? "ROTATION"
                : t >= 0.16
                  ? "SECOND STRING"
                  : "BENCH";
      return (
        (e.depthRole = a),
        (e.snapShare = t),
        { role: a, share: t, score: (t - 0.5) * 40, peer: LEVELS[e.level].need }
      );
    }
    return depthChartCore(e);
  }; /* v128: one fixture on the schedule is THE RIVALRY. It is built here, when the schedule is, so
   * the season screen, the scouting card, the wear ledger and the week generator all read the same
   * row: the side is lifted (rating, both units, and how hard they hit, which is what oppMulV111
   * prices), the row carries the flag, and the "showboat" bench roll is made ONCE for the season
   * rather than per game. With no rivalry stage chosen, `riv` is -1 and nothing below changes. */
  buildSeasonSchedule = function (e) {
    ((e._seasonPerfBoost = 0), (e._nextGameBoost = 0));
    const t = LEVELS[e.level],
      a = [];
    const ev = e.eventChoice,
      riv = ev && ev.rivalV128 ? rivalWeekV128(t.games) : -1,
      lift = TU("rivalOppLift", 1.16);
    e.eventBenchV128 = ev && ev.bench && Math.random() < ev.bench ? -14 : 0;
    /* v139: the combine year has no opponents. Every week of it is one of the six drills, named for
     * the drill, so the schedule reads like a combine instead of four more football games against
     * "COMBINE FIELD" — and `A[6].games` is six, one week per drill. */
    const cmb = combineYearV139(e) ? COMBINE_V139 : null;
    for (let s = 0; s < t.games; s++) {
      const dr = cmb && cmb[s % cmb.length],
        n = dr ? dr.name.toUpperCase() : randOppName(e.level),
        i = createOpponentProfile(n, e.level, s + 1, e.seasonSeed),
        isR = s === riv;
      if (isR) {
        i.rating = Math.round(i.rating * lift);
        i.offense = Math.round(i.offense * lift);
        i.defense = Math.round(i.defense * lift);
        i.physicality = Math.min(99, Math.round((i.physicality || 55) * 1.18));
        i.importance = "rivalry";
        i.rivalry = !0;
      }
      if (!isR && riv >= 0 && i.importance === "rivalry") {
        i.importance = "routine";
        i.rivalry = !1;
      } // there is one rivalry on a schedule, and it is the one the week is named after
      a.push({
        week: s + 1,
        opp: n,
        home: s % 2 === 0,
        played: !1,
        generatedV11: !1,
        opponentV11: i,
        perf: null,
        us: null,
        them: null,
        won: null,
        injured: !1,
        rivalV128: isR,
        combineV139: dr ? dr.k : void 0
      });
    }
    return a;
  };
  cn = function (e, t) {
    const a = playoffRoundNames(e.level),
      s = t === a.length - 1,
      n = s && e.nemesis ? e.nemesis.name + "'s " + randOppName(e.level) : randOppName(e.level),
      i = createOpponentProfile(n, e.level, (e.weekResults || []).length + 1, e.seasonSeed, t);
    return (
      (i.importance = s ? "championship" : "playoff"),
      {
        week: (e.weekResults || []).length + 1,
        opp: n,
        home: t % 2 === 0,
        played: !1,
        generatedV11: !1,
        opponentV11: i,
        playoff: !0,
        round: a[t],
        roundIdx: t,
        perf: null,
        us: null,
        them: null,
        won: null,
        injured: !1
      }
    );
  };
  startSeasonGames = function () {
    const e = state.player;
    (ensureV11(e),
      (e.seasonSeed = randInt(1, 2e9)),
      (e.playoffState = null),
      (e._v11SeasonActive = !0),
      resetSeasonSystems(e),
      rollSeasonMod(e),
      genContracts(e),
      (e._v11SeasonActive = !1));
    const t = e.depthStarts || 0;
    (depthChartCore(e),
      (e.depthStarts = t),
      (e._v11SeasonActive = !0),
      (e.roleRivalV11 = null),
      ensureRival(e, LEVELS[e.level].need, seededRng(e.seasonSeed, e.level, e.pos, "season-rival")),
      (e.weekResults = buildSeasonSchedule(e)),
      (e.currentWeek = 0),
      (state.view = "season"),
      saveGame(),
      render());
  };
  function planStreak(e, t) {
    let a = 0;
    for (const s of e.planHistoryV11 || [])
      if (s === t) a++;
      else break;
    return a;
  }
  function resolveWeekV11(e, t, a) {
    if (t.generatedV11) return t;
    t.rivalV128 && rivalResolveV136(e, "auto");
    /* v136 A: a rivalry week reached without the page still gets its approach */ ensureV11(e);
    const s = gamePlanById(a),
      n =
        t.opponentV11 ||
        createOpponentProfile(t.opp, e.level, t.week || 1, e.seasonSeed, t.playoff ? t.roundIdx : void 0);
    ((t.opponentV11 = n), n.scouted || scoutOpponent(n, e, state.specializationV11));
    const i = planStreak(e, a),
      r = matchupPlanModifier(n, a, i, state.specializationV11),
      l = conditionModifiers(e),
      d =
        e.level >= 7
          ? lifestyleEffects(e)
          : { weeklyCost: 0, recovery: 0, perf: 0, injuryRisk: 0, recurrence: 0, fulfillment: 0, media: 0, fatigue: 0 },
      c = rollGamePerf(e, (s.perf || 0) + (r.perf || 0) + (l.perf || 0) + (d.perf || 0), { playoff: !!t.playoff }),
      __omSet = (window.__oppMulV22 = __oppMulForV22(t.opp || (n && n.name))),
      _evP =
        ((e.eventChoice && e.eventChoice.perf) || 0) +
        (e.eventBenchV128 ||
          0) /* v128: the season choice's own rating swing rode the silent path only — the week you actually play never saw it, so the card promised a number the game did not pay */,
      _g17 = __aiSeasonGame(e, {
        varMult:
          (s.varMult || 1) *
          ((e.eventChoice && e.eventChoice.varMult) || 1) /* v146 D: the plan's variance reaches the box score */,
        perfSeed: (s.perf || 0) + (r.perf || 0) + (l.perf || 0) + (d.perf || 0) + _evP,
        playoff: !!t.playoff,
        oppRating: n.rating,
        oppBoost: (n.rating - (45 + e.level * 5.5)) / 180 + (t.playoff ? 0.08 : 0),
        load: loadOfV111(e) /* v111: what he is already carrying is a term in this game's injury roll */
      }),
      __omClr = (window.__oppMulV22 = null),
      h = randRange(-6, 6) * (s.varMult || 1),
      p = ((e.composure103 || 50) - 50) * 0.06,
      m = ((e.momentum103 || 50) - 50) * 0.045;
    ((t.perf = Math.round(clamp99(_g17.perf + h + p + m, 1, 100))),
      /* v128: VARIANCE is the same reshape around 50 the persona sliders use — wider means the good
       * games get better AND the bad ones get worse, which is what the card now says out loud */
      (function () {
        const vm = (e.eventChoice && e.eventChoice.varMult) || 1;
        if (vm !== 1) t.perf = clamp99(Math.round(50 + (t.perf - 50) * vm), 1, 100);
      })(),
      (t.statLine = _g17.statLine),
      (t.formV146 = _g17.formV146 || 0),
      _g17.statLine && _g17.statLine.pick6 && (e.pick6Career = (e.pick6Career || 0) + _g17.statLine.pick6),
      (t.snapShare = Math.round(clamp99((e.snapShare || 0.12) * l.snapMultiplier, 0.04, 0.98) * 100) / 100),
      (t.snaps = Math.max(3, Math.round((e.level >= 5 ? 68 : 52) * t.snapShare * randRange(0.88, 1.12)))));
    let y = l.injuryRisk + (s.inj || 0) + (r.injuryRisk || 0) + (d.injuryRisk || 0);
    (a === "recovery" && (y -= 0.08),
      (y = tunedInjuryRisk(y + (c.injured ? 0.055 : 0), e) * (t.rivalV128 ? RIVAL_MULT_V128() : 1)),
      (t.injuryRiskV12 = y),
      /* v128: the rivalry costs double, and the card says so before you play it */ (t.lifeEffectsV12 = d),
      (function () {
        try {
          if (window.__INJ_DEBUG) {
            window.__INJ_DEBUG.resolve = (window.__INJ_DEBUG.resolve || 0) + 1;
            if (_g17.injured) window.__INJ_DEBUG.resolveHit = (window.__INJ_DEBUG.resolveHit || 0) + 1;
          }
        } catch (_) {}
      })(),
      (t.injured = _g17.injured && Math.random() < injPlanMultV54({ injury: y })));
    /* v54: materialise it HERE. This was only ever called from the sim-season path, so a week played normally flagged the injury and then dropped it — nothing ever got a severity or a weeksRemaining, so mustSitV18 could never become true and no game was ever missed. */ try {
      materializeInjuryV18(e, t);
    } catch (_) {}
    /* v111: the body is billed from what he ACTUALLY did — the engine's own snap and touch counts,
     * priced by his durability, the opponent and the stakes of the fixture. The week keeps the row
     * so the body section can say what this game cost. */
    try {
      t.snapsV111 = _g17.snapsV111 || null;
      t.wearV111 = chargeV111(e, t, _g17.snapsV111 || {});
    } catch (_) {}
    return (
      (t.us = _g17.us),
      (t.them = _g17.them),
      (t.won = _g17.won),
      (t.generatedV11 = !0),
      (t._plan103 = a),
      (t.planV11 = a),
      (t.matchupV11 = r),
      (t.healthAtKickoffV11 = { ...e.conditionV11 }),
      (e.weeklyPlan103 = a),
      (e.planHistoryV11 = e.planHistoryV11 || []),
      e.planHistoryV11.unshift(a),
      (e.planHistoryV11 = e.planHistoryV11.slice(0, 6)),
      s.snap && (e.snapShare = clamp99((e.snapShare || 0.1) + s.snap, 0.04, 0.98)),
      (e.coachTrust = clamp99((e.coachTrust || 50) + (s.trust || 0) + (r.trust || 0), 0, 100)),
      (e.composure103 = clamp99((e.composure103 || 50) + (s.comp || 0), 0, 100)),
      (e.momentum103 = clamp99((e.momentum103 || 50) + (s.mom || 0), 0, 100)),
      t
    );
  }
  function scoutCard(e, t) {
    const a = e.opponentV11;
    if (!a) return "";
    const s = a.scouted || scoutOpponent(a, t, state.specializationV11),
      n = gamePlanById(s.recommendedPlan);
    return `<div class="scout-card-v11"><div class="scout-head-v11"><div><span>${a.rivalry ? "🔥 RIVALRY" : "🔭 SCOUTING REPORT"}</span><b>${escHtml(a.name)}</b></div><div class="scout-confidence-v11">${s.confidence}%<small>CONFIDENCE</small></div></div><div class="scout-tags-v11"><span>${escHtml(s.matchupLabel)}</span><span>${escHtml(a.importance.toUpperCase())}</span><span>${a.rating} OPP RATING</span></div><div class="scout-facts-v11">${s.reveals.map(i => `<div>• ${escHtml(i)}</div>`).join("")}${s.hiddenCount ? `<div class="hidden-fact-v11">• ${s.hiddenCount} detail${s.hiddenCount > 1 ? "s" : ""} unresolved</div>` : ""}</div><div class="scout-rec-v11">Suggested counter: <b>${n.icon} ${escHtml(n.name)}</b>${s.confidence < 50 ? " <small>Low-confidence report</small>" : ""}</div></div>`;
  }
  function gamePlanOverlayV11(e) {
    const t = state.player;
    ensureV11(t);
    const a = t.weekResults ? t.weekResults.findIndex(n => !n.played) : -1,
      s = a >= 0 ? t.weekResults[a] : null;
    return s
      ? (s.opponentV11 ||
          (s.opponentV11 = createOpponentProfile(
            s.opp,
            t.level,
            s.week || a + 1,
            t.seasonSeed,
            s.playoff ? s.roundIdx : void 0
          )),
        scoutOpponent(s.opponentV11, t, state.specializationV11),
        `<div class="gameplan-overlay"><div class="gameplan-panel impact-plan v11-plan-panel"><div class="plan-hero"><div class="decision-kicker">WEEK ${s.week || a + 1} · NOTHING HAS BEEN ROLLED YET</div><div class="decision-title">Scout. Prepare. Then Play.</div><div class="decision-copy">Your choice now generates the game. Future games remain unresolved until their week.</div></div>${scoutCard(s, t)}<div class="plan-deck">${GAME_PLANS.map(
          n => {
            const i = yn(n),
              r = planColor(n.id),
              l = s.opponentV11.scouted?.recommendedPlan === n.id;
            return `<button class="impact-plan-choice ${l ? "recommended-v11" : ""}" style="--planColor:${r}" onclick="chooseGamePlanV11('${n.id}',${e ? "true" : "false"})"><div class="plan-icon">${n.icon}</div><div><b>${n.name}${l ? " · SCOUT PICK" : ""}</b><small>${n.desc}</small><div class="gameplan-meta">${n.tags.map(d => `<span>${d}</span>`).join("")}</div></div><div class="plan-bars"><div class="mini-rating"><label><span>UPSIDE</span><span>${i[0]}</span></label><div class="mbar"><i style="width:${i[0]}%"></i></div></div><div class="mini-rating"><label><span>CONTROL</span><span>${i[1]}</span></label><div class="mbar"><i style="width:${i[1]}%"></i></div></div><div class="mini-rating"><label><span>RISK</span><span>${i[2]}</span></label><div class="mbar"><i style="width:${i[2]}%"></i></div></div></div></button>`;
          }
        ).join("")}<button class="btn ghost" onclick="closeGamePlan103()">Back</button></div></div></div>`)
      : "";
  }
  gamePlanOverlay = gamePlanOverlayV11;
  function chooseGamePlanV11(e, t) {
    const a = state.player,
      s = a.weekResults.findIndex(i => !i.played);
    if (s < 0) return;
    const n = a.weekResults[s];
    (resolveWeekV11(a, n, e), closeGamePlan103(), saveGame(), startWeek(!!t));
  }
  prepareWeek103 = function (e) {
    const t = state.player;
    if (!t || !t.weekResults) return;
    const a = t.weekResults.find(n => !n.played);
    if (!a) return;
    if (a.generatedV11) {
      startWeek(!!e);
      return;
    }
    if (!e && TU("quickSilent", 1)) {
      silentWeekV85(t, a) || startWeek(!1);
      return;
    }
    const s = document.querySelector(".gameplan-overlay");
    (s && s.remove(), document.body.insertAdjacentHTML("beforeend", gamePlanOverlayV11(!!e)));
  };
  const playWeekCore = playWeek;
  function Fc(e, t, a) {
    return `<div class="decision-overlay moment-overlay-v11"><div class="decision-panel moment-panel-v11"><div class="decision-kicker">${escHtml((t.opponentV11?.importance || "IMPORTANT").toUpperCase())} MOMENT · ${escHtml(t.opp)}</div><div class="moment-score-v11"><span>${t.us}</span><small>–</small><span>${t.them}</span></div><div class="decision-title">${escHtml(a.title)}</div><div class="decision-copy">${escHtml(a.copy)} Your attributes, composure, condition, scouting, and choice all affect the roll.</div>${a.choices.map(s => `<button class="decision-choice moment-choice-v11" onclick="resolveMomentV11('${s.id}')"><b>${escHtml(s.label)}</b><small>${escHtml(s.description)}</small><span class="decision-risk">${escHtml(s.risk)} risk · displayed chance is intentionally approximate</span></button>`).join("")}</div></div>`;
  }
  function vo() {
    const e = state.player,
      t = e && e.weekResults && e.currentWeek != null ? e.weekResults[e.currentWeek] : null;
    if (1) return !1;
    if (
      !t ||
      ((t.momentsV11 = t.momentsV11 || makeHighLeverageMoments(e, t)),
      (t.momentIndexV11 = t.momentIndexV11 || 0),
      t.momentIndexV11 >= t.momentsV11.length)
    )
      return !1;
    const a = document.querySelector(".moment-overlay-v11");
    return (
      a && a.remove(),
      document.body.insertAdjacentHTML("beforeend", Fc(e, t, t.momentsV11[t.momentIndexV11])),
      !0
    );
  }
  function resolveMomentV11(e) {
    const t = state.player,
      a = t.weekResults[t.currentWeek],
      s = a.momentsV11[a.momentIndexV11],
      n = resolveHighLeverageMoment(t, a, s, e, state.specializationV11);
    ((t.highLeverageStatsV11 = t.highLeverageStatsV11 || { attempts: 0, successes: 0 }),
      t.highLeverageStatsV11.attempts++,
      n.success && t.highLeverageStatsV11.successes++,
      a.momentIndexV11++);
    const i = document.querySelector(".moment-overlay-v11");
    if (
      (i && i.remove(),
      bigMoment(
        n.success ? "CLUTCH EXECUTION" : "MOMENT MISSED",
        `${n.success ? "+" : ""}${n.perfDelta} game rating · ${Math.round(n.chance * 100)}% resolved chance`,
        n.success ? "good" : "bad"
      ),
      saveGame(),
      !vo())
    ) {
      const r = !!a.pendingLiveV11;
      ((a.momentsCompleteV11 = !0), delete a.pendingLiveV11, playWeekCore(r));
    }
  }
  function startWeek(e) {
    const t = state.player,
      a = t.weekResults.findIndex(n => !n.played);
    if (a < 0) {
      goView("season");
      return;
    }
    const s = t.weekResults[a];
    if (mustSitV18(t)) {
      t.currentWeek = a;
      sitOutWeekV18(t, s);
      saveGame();
      goView("season");
      return;
    }
    if (((t.currentWeek = a), t.level >= 7 && t.nflStateV11?.offers?.length)) {
      showNflOffersV11();
      return;
    }
    if (!s.generatedV11) {
      prepareWeek103(e);
      return;
    }
    ((s.pendingLiveV11 = !!e), !(!s.momentsCompleteV11 && vo()) && (delete s.pendingLiveV11, playWeekCore(!!e)));
  }
  playWeek = function (e) {
    const t = state.player,
      a = t && t.weekResults ? t.weekResults.find(s => !s.played) : null;
    if (!a) {
      goView("season");
      return;
    }
    if (mustSitV18(t)) {
      t.currentWeek = t.weekResults.indexOf(a);
      sitOutWeekV18(t, a);
      saveGame();
      goView("season");
      return;
    }
    if (!a.generatedV11) {
      prepareWeek103(!!e);
      return;
    }
    startWeek(!!e);
  };
  function Gc(e) {
    return e >= 0.82
      ? "FIRST STRING"
      : e >= 0.66
        ? "STARTER"
        : e >= 0.38
          ? "ROTATION"
          : e >= 0.16
            ? "SECOND STRING"
            : "BENCH";
  }
  function On(e, t) {
    !e ||
      !e.length ||
      e.forEach((a, s) =>
        setTimeout(
          () =>
            bigMoment(
              `${a.icon || "🏅"} ${t}`,
              `${a.title || a.name} · +${a.tokens || a.reward || 0} Legacy Token${(a.tokens || a.reward || 0) === 1 ? "" : "s"}`,
              "gold"
            ),
          260 + s * 500
        )
      );
  }
  processWeek95 = function (e, t) {
    if (!e || e._v11Processed) return;
    ((e._v11Processed = !0), ensureV11(t), processWeek95Core(e, t), co(t));
    const a = resolveRivalWeek(t, e, LEVELS[t.level].need, state.specializationV11);
    ((e.rivalResultV11 = a),
      (t.depthRole = Gc(t.snapShare || 0.1)),
      (t.snapShare || 0) >= 0.65 && (t.depthStarts = (t.depthStarts || 0) + 1),
      (t.roleBattle103 = Math.round(clamp99((t.snapShare || 0.1) * 100, 0, 100))));
    const s = updateConditionAfterGame(t, e, e.planV11 || "disciplined", state.specializationV11);
    ((e.conditionResultV11 = { fatigue: Math.round(s.condition.fatigue), injury: s.condition.injury?.name || null }),
      t.recentlyRecoveredV11 &&
        e.perf >= 80 &&
        ((t.comebackGamesV11 = (t.comebackGamesV11 || 0) + 1), (t.recentlyRecoveredV11 = 0)),
      t.level >= 7 &&
        ((e.nflEvaluationV11 = evaluateNflWeek(t, e, playerOvr(t))),
        e.nflEvaluationV11 &&
          e.nflEvaluationV11.previous !== e.nflEvaluationV11.status &&
          pushStory(
            "📋",
            "UFF role evaluation",
            `${e.nflEvaluationV11.previous} → ${e.nflEvaluationV11.status}; security ${e.nflEvaluationV11.security}.`
          )),
      maybeStartStoryArc(t, e));
    const n = evaluateMilestones(t, state),
      i = evaluateLegacyObjectives(t, state);
    (On(n, "MILESTONE"),
      On(i, "LEGACY OBJECTIVE"),
      a.defeated &&
        pushStory(
          "⚔️",
          `You passed ${a.rival.name}`,
          `The snap split shifted decisively after a ${e.perf}–${a.rivalPerf} weekly comparison.`
        ),
      (t.weeklyPlan103 = null),
      saveGame());
  };
  const Hc = simSeason;
  simSeason = function (e) {
    const t = Hc(e);
    if (
      (ensureV11(e),
      (e._v11SeasonActive = !1),
      (t.sampleGames || []).length >= 10 && t.injuries === 0 && (e.healthySeasonsV11 = (e.healthySeasonsV11 || 0) + 1),
      e.level >= 7)
    ) {
      const a = evaluateNflSeason(e, t, playerOvr(e));
      ((t.nflCareerV11 = a),
        (t.nflCutRollV146B = !!(e.nflCutPending || t.nflCut)) /* v146 B: the v10 roll is read, not dropped */,
        (e.nflCutPending = !1),
        (t.nflCut = !1));
    }
    return (evaluateMilestones(e, state), evaluateLegacyObjectives(e, state), t);
  };
  function fmtMoneyShort(e) {
    return e ? (e >= 1e6 ? "$" + Math.round(e / 1e6) + "M" : "$" + Math.round(e / 1e3) + "K") : "$0";
  }
  function nflSurvivalCard(e) {
    const t = ensureNflState(e, playerOvr(e));
    if (!t) return "";
    const a = String(t.status).replaceAll("-", " ").toUpperCase(),
      s = t.security < 25 ? "var(--blood)" : t.security < 50 ? "var(--gold)" : "var(--good)";
    return `<div class="card nfl-survival-v11"><div class="impact-head"><div><div class="impact-kicker">UFF ROSTER SURVIVAL</div><div class="h2" style="margin:2px 0 0">${escHtml(a)}</div></div><div class="nfl-security-v11" style="color:${s}">${Math.round(t.security)}<small>SECURITY</small></div></div><div class="nfl-security-track-v11"><i style="width:${clamp99(t.security, 0, 100)}%"></i></div><div class="nfl-grid-v11"><div><b>${Math.round(t.expectation)}</b><small>Expected Grade</small></div><div><b>${Math.round(t.schemeFit)}</b><small>Scheme Fit</small></div><div><b>${t.contract?.yearsRemaining || 0}</b><small>Years Left</small></div><div><b>${fmtMoneyShort(t.contract?.salary || 0)}</b><small>Salary</small></div></div>${strikeLineV146B(e)}${t.offers?.length ? `<button class="btn" style="margin-top:10px" onclick="showNflOffersV11()">Review ${t.offers.length} Roster Offer${t.offers.length > 1 ? "s" : ""}</button>` : '<div class="small" style="margin-top:9px">Poor games reduce snaps and security immediately. Strong play earns active status, starts, and extensions.</div>'}</div>`;
  }
  function qc(e) {
    const t = e.nflStateV11,
      a = t?.offers || [];
    return `<div class="decision-overlay nfl-offers-overlay-v11"><div class="decision-panel"><div class="decision-kicker">FREE AGENCY · CAREER SURVIVAL</div><div class="decision-title">The League Gives You ${a.length} Path${a.length === 1 ? "" : "s"}</div><div class="decision-copy">Role, scheme fit, security, salary, and guarantees differ. Signing continues the current career.</div>${a.map(s => `<button class="decision-choice nfl-offer-v11" onclick="acceptNflOfferV11('${s.id}')"><b>${escHtml(s.team)} · ${escHtml(s.role.replaceAll("-", " ").toUpperCase())}</b><small>${escHtml(s.scheme)} · ${s.years} year${s.years > 1 ? "s" : ""} · ${fmtMoneyShort(s.salary)} · ${Math.round(s.guaranteed * 100)}% guaranteed</small><span class="decision-risk">Security ${s.security} · Scheme fit ${s.schemeFit}</span></button>`).join("")}${a.length ? '<button class="btn danger" onclick="declineNflOffersV11()">Decline All · Retire</button>' : '<button class="btn danger" onclick="endCareer()">No Offers · Retire</button>'}</div></div>`;
  }
  function showNflOffersV11() {
    const e = state.player;
    if (!e?.nflStateV11?.offers?.length) {
      showToast("No active offers.");
      return;
    }
    const t = document.querySelector(".nfl-offers-overlay-v11");
    (t && t.remove(), document.body.insertAdjacentHTML("beforeend", qc(e)));
  }
  function acceptNflOfferV11(e) {
    const t = state.player,
      a = acceptNflOffer(t, e);
    if (!a) return;
    const s = document.querySelector(".nfl-offers-overlay-v11");
    (s && s.remove(),
      pushStory(
        "✍️",
        `Signed with the ${a.team}`,
        `${a.role.replaceAll("-", " ")} role · ${a.scheme} · security ${a.security}.`
      ),
      saveGame(),
      render(),
      bigMoment("ROSTER LIFELINE", `${a.team} · ${a.role.replaceAll("-", " ")}`, "good"));
  }
  function declineNflOffersV11() {
    const e = document.querySelector(".nfl-offers-overlay-v11");
    (e && e.remove(), endCareer());
  }
  function Bn(e) {
    // v73: lead with the one number the card exists to give — what this body is worth
    // in the NEXT game — then show the inputs underneath rather than instead.
    const t = ensureCondition(e),
      L = bodyLedgerV73(e);
    if (!document.getElementById("bodyv73css"))
      document.head.insertAdjacentHTML(
        "beforeend",
        `<style id="bodyv73css">
    .bodyv73-net{display:flex;align-items:baseline;gap:8px;margin:2px 0 1px}
    .bodyv73-net b{font:800 30px Oswald,sans-serif;line-height:1}
    .bodyv73-net span{font:700 10px Oswald,sans-serif;letter-spacing:2px}
    .bodyv73-sub{font:400 11px system-ui;color:var(--chalk-dim);line-height:1.35;margin-top:2px}
    .bodyv73-rows{margin:9px 0 2px;border-top:1px solid rgba(255,255,255,.09)}
    .bodyv73-row{display:flex;align-items:baseline;gap:8px;padding:5px 0;border-bottom:1px solid rgba(255,255,255,.06)}
    .bodyv73-row i{font-style:normal;font:700 9.5px Oswald,sans-serif;letter-spacing:1.3px;min-width:96px;color:var(--chalk-dim)}
    .bodyv73-row s{text-decoration:none;flex:1;font:400 11.5px system-ui;color:#cdd8e8}
    .bodyv73-row em{font-style:normal;font:700 13px Oswald,sans-serif}
    .bodyv73-row.good i{color:#7fe0a0}.bodyv73-row.bad i{color:#ff8a80}
  </style>`
      );
    const col = L.sit ? "var(--blood)" : L.net >= 0.8 ? "var(--good)" : L.net <= -0.8 ? "var(--blood)" : "var(--chalk)";
    const sign = L.net >= 0 ? "+" : "−";
    const rows = L.rows
      .map(
        r => `<div class="bodyv73-row ${r.good === true ? "good" : r.good === false ? "bad" : ""}">
      <i>${r.k}</i><s>${escHtml(r.v)}</s>${Math.abs(r.d) >= 0.5 ? `<em style="color:${r.d >= 0 ? "#7fe0a0" : "#ff8a80"}">${r.d >= 0 ? "+" : "−"}${Math.round(Math.abs(r.d))}</em>` : ""}</div>`
      )
      .join("");
    // the LAST game's charge, in the same currency, so a bad grade that came from a bad
    // body can be read as one rather than as bad play
    const lastHurt = (e.weekResults || []).filter(w => w && w.played).slice(-1)[0];
    const lastCost = lastHurt && (lastHurt.satOut ? null : lastHurt.bodyCostV73);
    const lastLine =
      lastHurt && lastHurt.satOut
        ? `<div class="bodyv73-sub" style="color:#ff8a80">🩹 You did not play last week — ${escHtml(lastHurt.injName || "injured")}.</div>`
        : lastCost != null && lastCost <= -1
          ? `<div class="bodyv73-sub" style="color:#ff8a80">🩹 Last game your body cost you <b style="color:#ff8a80">${Math.abs(lastCost)}</b> rating — that grade was the injury, not the play.</div>`
          : lastCost != null && lastCost >= 1
            ? `<div class="bodyv73-sub" style="color:#7fe0a0">💪 Last game fresh legs were worth <b style="color:#7fe0a0">+${lastCost}</b> rating on top of what you did.</div>`
            : "";
    return `<div class="card condition-card-v11">
    <div class="impact-head"><div><div class="impact-kicker">YOUR BODY · NEXT GAME</div>
      <div class="bodyv73-net"><b style="color:${col}">${L.sit ? "OUT" : sign + Math.round(Math.abs(L.net))}</b><span style="color:${col}">${L.sit ? "YOU DO NOT PLAY" : "RATING · " + L.verdict}</span></div></div>
      <div class="condition-number-v11" style="color:${t.fatigue >= 75 ? "var(--blood)" : t.fatigue >= 50 ? "var(--gold)" : "var(--good)"}">${Math.round(t.fatigue)}<small>FATIGUE</small></div></div>
    <div class="bodyv73-sub">${
      L.sit
        ? `Out with ${escHtml((L.inj && L.inj.name) || "injury")}${L.inj && L.inj.weeksRemaining > 0 ? ` for ${L.inj.weeksRemaining} more game${L.inj.weeksRemaining > 1 ? "s" : ""}` : ""}. The weeks still pass.`
        : `This body ${L.net >= 0.8 ? "adds" : L.net <= -0.8 ? "takes off" : "is worth about"} <b style="color:${col}">${sign}${Math.round(Math.abs(L.net))}</b> rating against your recent average of ${Math.round(L.base)}. Over the ${L.left} game${L.left === 1 ? "" : "s"} left you can expect to miss <b>${Math.round(L.missRest)}</b> to injury at this risk.`
    }</div>
    ${lastLine}
    ${oppWearSayV126(e)}
    <div class="bodyv73-rows">${rows}</div>
    ${wearSectionV111(e)}
    <div class="condition-bars-v11"><label><span>Fatigue</span><b>${Math.round(t.fatigue)}</b></label><div><i style="width:${t.fatigue}%"></i></div><label><span>Recovery capacity</span><b>${Math.round(t.recovery)}</b></label><div><i style="width:${t.recovery}%"></i></div><label><span>Mental load</span><b>${Math.round(t.mentalLoad)}</b></label><div><i style="width:${t.mentalLoad}%"></i></div></div>
    <div class="small">Fatigue is the dial you control: a heavy plan buys snaps and trust, a recovery week buys the number above back. Durability is the dial you BUY — every 10 points of injuryResist take about ${Math.round((L.risk - L.riskUp) * 1000)}% off this week's risk.</div></div>`;
  }
  function bo(e) {
    const t = ensureRival(e, LEVELS[e.level].need, seededRng(e.seasonSeed, e.level, e.pos, "ui-rival")),
      a = Math.round((e.snapShare || 0.1) * 100),
      s = Math.round((t.snapShare || 0.5) * 100);
    return `<div class="card rival-card-v11"><div class="impact-head"><div><div class="impact-kicker">NAMED ROLE BATTLE · ${escHtml(t.personality.toUpperCase())}</div><div class="h2" style="margin:2px 0 0">You vs. ${escHtml(t.name)}</div></div><div class="rival-health-v11">${Math.round(t.health)}<small>RIVAL HEALTH</small></div></div><div class="rival-versus-v11"><div><span>YOU</span><b>${a}%</b><small>${escHtml(e.depthRole || "BENCH")} · trust ${Math.round(e.coachTrust || 50)}</small></div><em>VS</em><div><span>${escHtml(t.position)}</span><b>${s}%</b><small>${t.ovr} OVR · trust ${Math.round(t.coachTrust)}</small></div></div><div class="snap-split-v11"><i style="width:${a}%"></i></div><div class="rival-trait-v11">${escHtml(t.trait)} · relationship ${Math.round(t.relationship)}</div>${t.history?.length ? `<div class="small">Last comparison: ${t.history[0].playerPerf} vs ${t.history[0].rivalPerf} · ${t.history[0].swing >= 0 ? "+" : ""}${t.history[0].swing}% snap swing</div>` : ""}</div>`;
  }
  function kn(e) {
    const t = ORIGINS.find(i => i.id === e.originV11),
      a = e.legacyObjectivesV11 || [],
      s = a.map(i => LEGACY_OBJECTIVES.find(r => r.id === i)).filter(Boolean),
      n = e.completedObjectivesV11 || {};
    return t
      ? `<div class="card identity-card-v11"><div class="impact-head"><div><div class="impact-kicker">RUN IDENTITY</div><div class="h2" style="margin:2px 0 0">${t.icon} ${escHtml(t.name)}</div></div><div class="legacy-token-v11">${state.legacyTokensV11 || 0}<small>LEGACY TOKENS</small></div></div><div class="small">${escHtml(t.description)}</div><div class="legacy-objectives-v11">${s.map(i => `<div class="${n[i.id] ? "done" : ""}"><span>${n[i.id] ? "✓" : i.icon}</span><b>${escHtml(i.title)}</b><small>${escHtml(i.description)}</small><em>+${i.reward}</em></div>`).join("")}</div></div>`
      : "";
  }
  function storyArcCard(e) {
    const t = e.activeStoryArcV11,
      a = (e.storyArcHistoryV11 || []).slice(0, 3);
    if (!t && !a.length) return "";
    const s = t ? STORY_ARCS.find(n => n.id === t.templateId) : null;
    return `<div class="card arc-card-v11"><div class="eyebrow">MULTI-WEEK CAREER ARCS</div>${s ? `<div class="active-arc-v11"><span>${s.icon}</span><div><b>${escHtml(s.title)}</b><small>Stage ${t.stage + 1}/${s.stages.length} · choices carry into future weeks</small></div></div>` : '<div class="small">No active storyline. Your performance and world state determine what begins next.</div>'}${a.length ? `<div class="arc-history-v11">${a.map(n => `<span class="${n.status}">${n.icon} ${escHtml(n.title)}</span>`).join("")}</div>` : ""}</div>`;
  }
  function _c(e) {
    const t = careerLegacyGrade(e),
      a = Object.keys(e.completedObjectivesV11 || {}).length,
      s = (e.legacyObjectivesV11 || []).length;
    return `<div class="card legacy-summary-v11"><div class="legacy-grade-v11">${t.grade}</div><div><div class="eyebrow">PARTIAL VICTORY · CAREER LEGACY</div><div class="h2" style="margin:2px 0">${escHtml(t.ending)}</div><div class="small">Legacy score ${t.score} · ${a}/${s} origin objectives · highest level ${LEVELS[e.level]?.name || "Unknown"}. A career can matter without reaching the UFF.</div></div></div>`;
  }
  function storyOverlay(e) {
    const t = e.storyDecisionQueueV11?.[0];
    return t
      ? `<div class="decision-overlay story-overlay-v11"><div class="decision-panel"><div class="decision-kicker">${t.icon} ${escHtml(t.arcTitle)} · MULTI-WEEK ARC</div><div class="decision-title">${escHtml(t.title)}</div><div class="decision-copy">${escHtml(t.copy)} This decision persists beyond one game.</div>${t.choices.map(a => `<button class="decision-choice" onclick="resolveStoryChoiceV11('${a.id}')"><b>${escHtml(a.label)}</b><small>${escHtml(a.description)}</small><span class="decision-risk">${escHtml(a.risk)} · ${Math.round(a.baseChance * 100)}% base chance</span></button>`).join("")}</div></div>`
      : "";
  }
  function resolveStoryChoiceV11(e) {
    const t = state.player,
      a = resolveStoryChoice(t, state, e, state.specializationV11);
    if (!a) return;
    const s = document.querySelector(".story-overlay-v11");
    (s && s.remove(),
      saveGame(),
      render(),
      bigMoment(
        a.success ? "STORYLINE BREAKTHROUGH" : "STORYLINE SETBACK",
        a.choice.label,
        a.success ? "good" : "bad"
      ));
  }
  function insertAfter(e, t) {
    e && t && e.insertAdjacentHTML("afterend", t);
  }
  function Kc(e) {
    const t = e.weekResults?.find(i => !i.played);
    if (!t) return "";
    t.opponentV11 ||
      (t.opponentV11 = createOpponentProfile(
        t.opp,
        e.level,
        t.week || 1,
        e.seasonSeed,
        t.playoff ? t.roundIdx : void 0
      ));
    const a = t.opponentV11.scouted || scoutOpponent(t.opponentV11, e, state.specializationV11),
      s = String(t.opponentV11.importance || "routine").toUpperCase();
    return `<div class="card opponent-card-v11" style="border-color:${["playoff", "championship"].includes(t.opponentV11.importance) ? "var(--gold)" : t.opponentV11.importance === "evaluation" ? "var(--blood)" : "var(--cyan)"}"><div class="impact-head"><div><div class="impact-kicker">NEXT OPPONENT · ${s}</div><div class="h2" style="margin:2px 0 0">${escHtml(t.opp)}</div></div><div class="scout-confidence-v11">${a.confidence}<small>SCOUT CONF.</small></div></div><div class="matchup-badge-v11">${escHtml(a.matchupLabel)} matchup · ${a.hiddenCount} detail${a.hiddenCount === 1 ? "" : "s"} hidden</div>${(() => {
      /* v126: who they actually are. The RECOMMENDED COUNTER used to sit here — a game plan the player has not chosen himself since the pregame wizard took the call, so the one concrete line on the card was the one thing it could not act on. */
      const r = oppReadV126(t.opponentV11, e);
      if (!r) return "";
      return `<div class="oppv126">
        <div class="oppv126-tier" style="color:${r.col}">${r.tier}<em>${r.gap >= 0 ? "+" : "−"}${Math.abs(r.gap)} rating vs you</em></div>
        <div class="oppv126-grid">
          <div><b>${r.rating}</b><small>TEAM RATING</small></div>
          <div><b>${r.off}</b><small>OFFENSE</small></div>
          <div><b>${r.def}</b><small>DEFENSE</small></div>
          <div><b style="color:${r.physN >= 62 ? "#ff9b93" : "#9fe6b0"}">${r.physN}</b><small>PHYSICALITY</small></div>
        </div>
        <div class="oppv126-say">${escHtml(r.identity)}</div>
      </div>`;
    })()}<div class="scout-facts-v11">${a.reveals.map(i => `<span>${escHtml(i)}</span>`).join("")}</div><div class="small">${a.hiddenCount ? `<b>${a.hiddenCount}</b> more detail${a.hiddenCount === 1 ? " is" : "s are"} still hidden — scouting confidence rises with Awareness, Film Study and the Scouting Notebook.` : "Your scouting is complete: there is nothing about this side you have not seen."}</div></div>`;
  }
  function In(e) {
    const t = careerLegacyGrade(e),
      a = Object.keys(e.completedObjectivesV11 || {}).length,
      s = (e.legacyObjectivesV11 || []).length;
    return `<div class="card legacy-progress-v11"><div class="legacy-grade-v11 small-grade">${t.grade}</div><div><div class="eyebrow">CAREER LEGACY IN PROGRESS</div><div class="h2" style="margin:2px 0">${escHtml(t.ending)}</div><div class="small">Score ${t.score} · ${a}/${s} identity goals complete · partial victories earn permanent Legacy Tokens without requiring an UFF career.</div></div></div>`;
  }
  function zc(e) {
    if (e.originV11 !== "position-convert") return "";
    const t = (e.positionChangesV11 || []).includes(e.level);
    return `<button class="btn ${t ? "ghost" : "secondary"}" style="margin-top:10px" ${t ? "disabled" : ""} onclick="showPositionChangeV11()">🔄 ${t ? "Position Changed This Level" : "Explore a Position Change"}</button>`;
  }
  const Qc = kn;
  kn = function (e) {
    const t = Qc(e);
    return t && t.replace(/<\/div>\s*$/, `${zc(e)}</div>`);
  };
  function Jc(e) {
    return `<div class="decision-overlay position-overlay-v11"><div class="decision-panel"><div class="decision-kicker">POSITION CONVERSION · ONE PER LEVEL</div><div class="decision-title">Trade Mastery for Versatility</div><div class="decision-copy">Changing positions resets your named role battle, cuts coach trust, and lowers immediate snaps. It can unlock a better long-term fit and completes part of your origin legacy.</div>${suggestPositions(
      e.attrs,
      e.body
    )
      .filter(a => a.pos !== e.pos)
      .slice(0, 6)
      .map(
        a =>
          `<button class="decision-choice" onclick="changePositionV11('${a.pos}')"><b>${a.pos} · ${escHtml(POSITIONS[a.pos].name)} · ${a.ovr} projected OVR</b><small>${escHtml(POS_BODY[a.pos].note)}</small><span class="decision-risk">New rival · −8 trust · reduced snaps</span></button>`
      )
      .join(
        ""
      )}<button class="btn ghost" onclick="document.querySelector('.position-overlay-v11')?.remove()">Cancel</button></div></div>`;
  }
  function showPositionChangeV11() {
    const e = state.player;
    if (!(!e || e.originV11 !== "position-convert")) {
      if ((e.positionChangesV11 || []).includes(e.level)) {
        showToast("You already changed positions at this level.");
        return;
      }
      (document.querySelector(".position-overlay-v11")?.remove(), document.body.insertAdjacentHTML("beforeend", Jc(e)));
    }
  }
  function changePositionV11(e) {
    const t = state.player;
    if (
      !t ||
      !POSITIONS[e] ||
      t.originV11 !== "position-convert" ||
      ((t.positionChangesV11 = t.positionChangesV11 || []), t.positionChangesV11.includes(t.level))
    )
      return;
    const a = t.pos;
    (t.positionChangesV11.push(t.level),
      (t.positionsPlayedV11 = t.positionsPlayedV11 || []),
      a && !t.positionsPlayedV11.includes(a) && t.positionsPlayedV11.push(a),
      t.positionsPlayedV11.includes(e) || t.positionsPlayedV11.push(e),
      (t.pos = e),
      (t.roleRivalV11 = null),
      (t.snapShare = clamp99(Math.min(t.snapShare || 0.12, 0.18), 0.04, 0.98)),
      (t.coachTrust = clamp99((t.coachTrust || 50) - 8, 0, 100)),
      (t.roleBattle103 = 0),
      genRivals(t),
      ensureRival(t, LEVELS[t.level].need, seededRng(t.seasonSeed, t.level, e, "position-change")),
      document.querySelector(".position-overlay-v11")?.remove(),
      pushStory(
        "🔄",
        `${t.name} converts from ${a} to ${e}`,
        "A new depth-chart battle begins with less trust but a different ceiling."
      ),
      saveGame(),
      render(),
      bigMoment("POSITION CHANGED", `${a} → ${e} · prove the fit`, "gold"));
  }
  weeklyLoopCard = function (e) {
    return bo(e).replace("card rival-card-v11", "card weekly-loop-card rival-card-v11");
  };
  const ed = prepareWeek103;
  prepareWeek103 = function (e) {
    const t = state.player;
    if (t && !t.originV11) {
      (render(), showToast("Choose this career’s origin first."));
      return;
    }
    ed(!!e);
  };
  const td = startWeek;
  startWeek = function (e) {
    const t = state.player;
    if (t && !t.originV11) {
      (render(), showToast("Choose this career’s origin first."));
      return;
    }
    td(!!e);
  };
  simRemainingWeeks = function () {
    const e = state.player;
    if (!e?.weekResults) return;
    const t = e.weekResults.filter(a => !a.playoff && !a.played);
    let n = 0,
      rolls = [];
    for (const a of t) {
      rolls.push(...autoStoryV90(e, a));
      /* v90: a queued story stage is answered and rolled here, not on screen */ if (!silentWeekV85(e, a)) break;
      /* v85: the wheel rolls, the fate roll lands, ca() books the engine game */ n++;
      processWeek95(a, e);
    }
    rolls.push(...autoStoryV90(e, null));
    window.__V90.last = rolls;
    (saveGame(),
      goView("season"),
      n &&
        showToast(
          `⏭ Simmed ${n} week${n === 1 ? "" : "s"}${rolls.length ? ` · ${rolls.length} story roll${rolls.length === 1 ? "" : "s"}: ${rolls.filter(r => r.success).length} up, ${rolls.filter(r => !r.success).length} down` : ""} — every week on the plan you last chose, every game played by the engine`
        ));
  };
  const ad = simSeason;
  simSeason = function (e) {
    ensureV11(e);
    const t = e.potentialCeil,
      a = e.potential,
      s = originGrowthMultiplier(e);
    (e.potentialCeil != null && (e.potentialCeil *= s), e.potential != null && (e.potential *= s));
    const n = ad(e);
    return (t != null && (e.potentialCeil = t), a != null && (e.potential = a), (n.originGrowthV11 = s), n);
  };
  const sd = resolveWeekV11;
  resolveWeekV11 = function (e, t, a) {
    const s = sd(e, t, a);
    if (!t.originPerfAppliedV11) {
      const n = originPerformanceModifier(e, t);
      if (((t.perf = Math.round(clamp99((t.perf || 50) + n, 1, 100))), n)) {
        const i = ia(e, t.perf, { oppBoost: t.playoff ? 0.08 : 0 });
        ((t.us = i.us), (t.them = i.them), (t.won = i.won));
      }
      t.originPerfAppliedV11 = !0;
    }
    return s;
  };
  function nd() {
    ensureAccountState(state);
    const e = state.legacyUnlocksV11 || {};
    return `<div class="card legacy-rewards-v11"><div class="impact-head"><div><div class="impact-kicker">PARTIAL-VICTORY REWARDS</div><div class="h2" style="margin:2px 0 0">Legacy Workshop</div></div><div class="legacy-token-v11">${state.legacyTokensV11 || 0}<small>TOKENS</small></div></div><div class="small">Milestones and identity objectives fund information, content, and strategic options—not large universal attribute boosts.</div><div class="legacy-shop-v11">${LEGACY_UNLOCKS.map(t => `<div class="legacy-shop-row-v11 ${e[t.id] ? "owned" : ""}"><span>${t.icon}</span><div><b>${escHtml(t.name)}</b><small>${escHtml(t.description)}</small></div><button ${e[t.id] || (state.legacyTokensV11 || 0) < t.cost ? "disabled" : ""} onclick="buyLegacyUnlockV11('${t.id}')">${e[t.id] ? "OWNED" : t.cost + " ◇"}</button></div>`).join("")}</div></div>`;
  }
  function buyLegacyUnlockV11(e) {
    const t = buyLegacyUnlock(state, e);
    if (t === !1) {
      showToast("Not enough Legacy Tokens.");
      return;
    }
    if (!t) {
      showToast("Already unlocked.");
      return;
    }
    (state.player && ensureV11(state.player), saveGame(), render(), bigMoment("LEGACY UNLOCKED", t.name, "gold"));
  }
  function od(e, t) {
    if (!state.legacyUnlocksV11?.["story-insight"]) return `${escHtml(t.risk)} · outcome information limited`;
    const a = e.worldState?.teamChemistry || 50,
      s = e.coachTrust || 50,
      n = e.attrs?.awareness || 10,
      i = clamp99(
        t.baseChance +
          (a - 50) / 600 +
          (s - 50) / 750 +
          (n - 20) / 900 +
          (state.specializationV11 === "architect" ? 0.08 : 0),
        0.12,
        0.94
      ),
      r = Math.round(clamp99(i - 0.05, 0.05, 0.98) * 100),
      l = Math.round(clamp99(i + 0.05, 0.05, 0.98) * 100);
    return `${escHtml(t.risk)} · estimated ${r}–${l}%`;
  }
  storyOverlay = function (e) {
    const t = e.storyDecisionQueueV11?.[0];
    return t
      ? `<div class="decision-overlay story-overlay-v11"><div class="decision-panel"><div class="decision-kicker">${t.icon} ${escHtml(t.arcTitle)} · MULTI-WEEK ARC</div><div class="decision-title">${escHtml(t.title)}</div><div class="decision-copy">${escHtml(t.copy)} This decision persists beyond one game.</div>${t.choices.map(a => `<button class="decision-choice" onclick="resolveStoryChoiceV11('${a.id}')"><b>${escHtml(a.label)}</b><small>${escHtml(a.description)}</small><span class="decision-risk">${od(e, a)}</span></button>`).join("")}</div></div>`
      : "";
  };
  bo = function (e) {
    const t = ensureRival(e, LEVELS[e.level].need, seededRng(e.seasonSeed, e.level, e.pos, "ui-rival")),
      a = Math.round((e.snapShare || 0.1) * 100),
      s = Math.round((t.snapShare || 0.5) * 100),
      n = !!state.legacyUnlocksV11?.["rival-dossier"];
    return `<div class="card rival-card-v11"><div class="impact-head"><div><div class="impact-kicker">NAMED ROLE BATTLE · ${escHtml(t.personality.toUpperCase())}</div><div class="h2" style="margin:2px 0 0">You vs. ${escHtml(t.name)}</div></div><div class="rival-health-v11">${Math.round(t.health)}<small>RIVAL HEALTH</small></div></div><div class="rival-versus-v11"><div><span>YOU</span><b>${a}%</b><small>${escHtml(e.depthRole || "BENCH")} · trust ${Math.round(e.coachTrust || 50)}</small></div><em>VS</em><div><span>${escHtml(t.position)}</span><b>${s}%</b><small>${t.ovr} OVR · trust ${Math.round(t.coachTrust)}</small></div></div><div class="snap-split-v11"><i style="width:${a}%"></i></div><div class="rival-trait-v11">${n ? `${escHtml(t.trait)} · ${t.potential} potential` : "Trait and potential hidden · unlock Rival Dossier"} · relationship ${Math.round(t.relationship)}</div>${t.history?.length ? `<div class="small">Last comparison: ${t.history[0].playerPerf} vs ${t.history[0].rivalPerf} · ${t.history[0].swing >= 0 ? "+" : ""}${t.history[0].swing}% snap swing</div>` : ""}</div>`;
  };
  function rd() {
    const e = state.player,
      t = byId("screen");
    if (!e || !t) return;
    if ((ensureV11(e), state.view === "choosePos")) {
      if (!t.querySelector(".origin-card-v11")) {
        const n = t.querySelector(".card");
        insertAfter(n, originCard(e));
      }
      t.querySelectorAll(".pos-card").forEach(n => n.classList.toggle("origin-locked-v11", !e.originV11));
    }
    if (state.view === "hub") {
      const n = t.querySelector(".player-hero");
      (!e.originV11 && !t.querySelector(".origin-card-v11") && insertAfter(n, originCard(e)),
        e.originV11 && !t.querySelector(".identity-card-v11") && insertAfter(n, kn(e)));
      const i = t.querySelector(".weekly-loop-card") || n;
      t.querySelector(".condition-card-v11") || insertAfter(i, Bn(e));
      const r = t.querySelector(".condition-card-v11") || i;
      (t.querySelector(".arc-card-v11") || insertAfter(r, storyArcCard(e)),
        e.level >= 7 &&
          !t.querySelector(".nfl-survival-v11") &&
          insertAfter(t.querySelector(".arc-card-v11") || r, nflSurvivalCard(e)),
        t.querySelector(".legacy-progress-v11") || t.insertAdjacentHTML("beforeend", In(e)));
    }
    if (state.view === "season") {
      const n = t.querySelector(".h1");
      !e.originV11 && !t.querySelector(".origin-card-v11") && insertAfter(n, originCard(e));
      const i = t.querySelector(".origin-card-v11") || n;
      t.querySelector(".opponent-card-v11") || insertAfter(i, Kc(e));
      const r = t.querySelector(".opponent-card-v11") || n;
      (t.querySelector(".condition-card-v11") || insertAfter(r, Bn(e)),
        t.querySelector(".arc-card-v11") || insertAfter(t.querySelector(".condition-card-v11") || r, storyArcCard(e)),
        e.level >= 7 &&
          !t.querySelector(".nfl-survival-v11") &&
          insertAfter(t.querySelector(".arc-card-v11") || r, nflSurvivalCard(e)));
      const l = e.weekResults || [];
      t.querySelectorAll(".sched-row").forEach((d, c) => {
        const u = l[c];
        !u?.opponentV11 ||
          d.querySelector(".importance-chip-v11") ||
          d
            .querySelector(".sched-opp")
            ?.insertAdjacentHTML(
              "beforeend",
              ` <small class="importance-chip-v11 ${u.opponentV11.importance}"${u.rivalV128 ? ' style="color:#ff7a7a;border-color:#ff7a7a88" title="The rivalry. A stronger side, and everything it pays lands double — coach trust, attribute growth, and the chance you get hurt."' : ""}>${u.rivalV128 ? "🔥 " : ""}${String(u.opponentV11.importance).toUpperCase()}</small>`
            );
        // v73: a week the body took a bite out of says so ON the week, so a run of poor
        // grades reads as the injury it was rather than as a run of poor play
        if (u && u.played && !d.querySelector(".bodyv73-chip")) {
          const bad = u.satOut
            ? "OUT · " + String(u.injName || "INJURED").toUpperCase()
            : u.bodyCostV73 <= -1
              ? "🩹 PLAYED HURT " + u.bodyCostV73
              : null;
          if (bad)
            d.querySelector(".sched-opp")?.insertAdjacentHTML(
              "beforeend",
              ` <small class="bodyv73-chip" style="font:700 9px Oswald,sans-serif;letter-spacing:1px;color:#ff8a80;border:1px solid #ff8a8055;border-radius:9px;padding:1px 5px;margin-left:4px">${bad}</small>`
            );
        }
        // v85: a week the wheel rolled in the background says so ON the week
        if (u && u.played && (u.wheelV85 || u.planRollV146) && !d.querySelector(".wheelv85-chip")) {
          const W = u.wheelV85 || Object.assign({ chosen: !0, live: !0 }, u.planRollV146),
            col = W.band === "green" ? "#57e07a" : W.band === "red" ? "#ff8a80" : "#b9c6d6",
            res =
              W.band === "green"
                ? "CLICKED"
                : W.band === "red"
                  ? "BACKFIRED"
                  : W.band === "scout"
                    ? "SCOUT PICK"
                    : "IT'LL DO";
          d.querySelector(".sched-opp")?.insertAdjacentHTML(
            "beforeend",
            ` <small class="wheelv85-chip" title="${W.chosen ? (W.live ? "Your plan · its roll came up at kickoff" : "Your plan, run again · its roll came up in the background") : "Rolled in the background"} · ${escHtml(W.lines || "")}" style="font:700 9px Oswald,sans-serif;letter-spacing:1px;color:${col};border:1px solid ${col}55;border-radius:9px;padding:1px 5px;margin-left:4px">${W.chosen ? "📋" : "🎡"} ${escHtml(String(W.name || W.id || "PLAN")).toUpperCase()} · ${res}</small>`
          );
        } /* v146 D: a chosen plan wears the clipboard, not the wheel */
      });
    }
    if (state.view === "shop") {
      const n = t.querySelector(".pts-banner");
      (t.querySelector(".specialization-card-v11") || insertAfter(n, jc()),
        t.querySelector(".legacy-rewards-v11") || insertAfter(t.querySelector(".specialization-card-v11") || n, nd()));
    }
    if (state.view === "result" && !t.querySelector(".legacy-progress-v11")) {
      const n = t.querySelector(".card") || t.firstElementChild;
      (insertAfter(n, In(e)),
        e.level >= 7 &&
          !t.querySelector(".nfl-survival-v11") &&
          insertAfter(t.querySelector(".legacy-progress-v11"), nflSurvivalCard(e)));
    }
    if ((state.view === "gameover" || state.view === "win") && !t.querySelector(".legacy-summary-v11")) {
      const n = t.querySelector(".banner") || t.firstElementChild;
      insertAfter(n, _c(e));
    }
    const a = e.storyDecisionQueueV11?.[0],
      s = document.querySelector(".story-overlay-v11");
    (a &&
      !s &&
      !["menu", "gameover", "win", "declineResult"].includes(state.view) &&
      document.body.insertAdjacentHTML("beforeend", storyOverlay(e)),
      !a && s && s.remove());
  }
  const ld = render;
  render = function () {
    (state?.player && ensureV11(state.player), ld(), rd());
  };
  /* ===== v136 B THE COACH'S SUMMARY IS A BUTTON =====
   * The season debrief (v122) fired on the report card on its own — with the tour on OR off — and the
   * only way to stop it was SKIP, which silenced it for good with nothing to bring it back. It is a
   * button now: 🗣️ COACH'S SUMMARY sits under the grade on the report card and opens the debrief
   * on demand, and beside it an AUTO switch says whether he pops in by himself after a season. The
   * switch IS the old `rib.debriefOff.v122` key (SKIP on the debrief still flips it off), and it is
   * completely separate from the COACH'S TOUR switch. `window.__V136_B`. */
  const COACH_SUM_KEY_V136 = "rib.debriefOff.v122";
  function coachSummaryAutoV136() {
    try {
      return localStorage.getItem(COACH_SUM_KEY_V136) !== "off";
    } catch (e) {
      return !0;
    }
  }
  function setCoachSummaryAutoV136(v) {
    try {
      localStorage.setItem(COACH_SUM_KEY_V136, v ? "on" : "off");
    } catch (e) {}
    try {
      window.__RIB_COACH && window.__RIB_COACH.debrief && window.__RIB_COACH.debrief.setOff(!v);
    } catch (e) {}
    const r = document.querySelector(".coach-sum-v136");
    r && r.remove();
    try {
      coachSummaryRowV136();
    } catch (e) {}
  }
  function coachSummaryV136() {
    try {
      const C = window.__RIB_COACH;
      if (C && C.summary && C.summary()) return !0;
    } catch (e) {}
    showToast("The coach has nothing to say until a season is in the books");
    return !1;
  }
  function coachSummaryRowV136() {
    if (!state || state.view !== "result") return;
    const sc = byId("screen");
    if (!sc || sc.querySelector(".coach-sum-v136")) return;
    const g = sc.querySelector(".season-grade"),
      card = (g && g.closest(".card")) || sc.querySelector(".card");
    if (!card) return;
    const on = coachSummaryAutoV136(),
      has = !!(state.player && state.player.debriefV122);
    card.insertAdjacentHTML(
      "afterend",
      `<div class="card tight coach-sum-v136"><div class="coach-sum-row-v136"><button class="btn secondary coach-sum-btn-v136" onclick="coachSummaryV136()" ${has ? "" : "disabled"}>🗣️ COACH'S SUMMARY</button><div class="toggle-row coach-sum-auto-v136" onclick="setCoachSummaryAutoV136(${on ? "false" : "true"})" role="switch" aria-checked="${on}"><div class="toggle-info"><div class="toggle-label">AUTO</div><div class="toggle-desc">${on ? "he pops in after every season" : "only when you press the button"}</div></div><div class="switch ${on ? "on" : ""}"><i></i></div></div></div><div class="small" style="margin-top:6px">What the year says — the grade, the bar, the body, the rank, and where to point next. Not part of the tour.</div></div>`
    );
  }
  window.coachSummaryV136 = coachSummaryV136;
  window.setCoachSummaryAutoV136 = setCoachSummaryAutoV136;
  window.__V136_B = {
    auto: coachSummaryAutoV136,
    setAuto: setCoachSummaryAutoV136,
    open: coachSummaryV136,
    row: coachSummaryRowV136,
    key: COACH_SUM_KEY_V136
  };
  const _qV136 = render;
  render = function () {
    _qV136();
    try {
      coachSummaryRowV136();
    } catch (e) {}
  }; /* ===== v85 THE WHEEL SPINS IN THE BACKGROUND =====
   * A quick-played week met the plan wheel and the pregame panel and then booked
   * the same engine game a played week does; "sim the rest" met nothing at all — it
   * flagged the weeks played and left the stat lines the schedule-maker rolled at
   * kickoff of the season, with no plan, no fate roll, no wheel swing, no condition,
   * no injury materialised. And the LIVE week booked one engine game in ca() and
   * then showed you a second one, so the box score you watched was never the one
   * the season counted. Three fixes, one path: silentWeekV85 rolls the v51 plan
   * wheel (decidePlan, the very same dice) with no wheel drawn, hands the pick to
   * chooseGamePlanV11 so the v50 fate roll and ca() run exactly as they do for a
   * played week, and the week is booked by the engine; sim-the-rest is that, week
   * by week, and it stops where a played season would stop (an UFF offer on the
   * table, a body that has to sit). bookLiveGameV85 writes the game you watched
   * over the one ca() pre-booked, moving the rating by the difference in grade. */
  function bookLiveGameV85(e, w, g) {
    const us = g.usScore,
      them = g.themScore,
      had = w.statLine ? { stat: w.statLine, us: w.us, them: w.them, snaps: w.snaps } : null;
    w.us = us;
    w.them = them;
    w.won = us > them;
    try {
      if (!g.stat) return;
      const grader = typeof gradeGame == "function" ? gradeGame : window.__gradeGame;
      if (typeof grader != "function") return;
      const snaps = (g.plays || []).filter(pl => pl && pl.involved).length || 12,
        gl = grader(e.pos, g.stat, snaps);
      const dev = v => clamp99((v - 70) / 24, -0.8, 1),
        wb = (a, b) => (a > b ? 3 : a < b ? -2 : 0);
      // the same modulation __aiSeasonGame applies, as a difference against what was booked
      let delta = 0;
      if (had) {
        const gb = grader(e.pos, had.stat, had.snaps || snaps);
        delta = (dev(gl.score) - dev(gb.score)) * 13 + (wb(us, them) - wb(had.us, had.them));
      }
      w.perf = clamp99(Math.round((Number(w.perf) || 50) + delta * condMultV54(e)), 1, 100);
      e.pick6Career = Math.max(
        0,
        (e.pick6Career || 0) + ((g.stat.pick6 || 0) - ((had && had.stat && had.stat.pick6) || 0))
      );
      w.statLine = g.stat;
      w.gameGrade = gl.grade;
      w.snaps = snaps;
      w.liveBookedV85 = !0;
    } catch (_) {}
  }
  function silentWeekV85(e, w) {
    if (!e || !w || w.played) return !1;
    if (mustSitV18(e)) {
      e.currentWeek = e.weekResults.indexOf(w);
      sitOutWeekV18(e, w);
      return !0;
    }
    if (e.level >= 7 && e.nflStateV11 && e.nflStateV11.offers && e.nflStateV11.offers.length) return !1; // an offer on the table needs an answer first
    window.__silentSimV85 = !0;
    try {
      if (!w.generatedV11) {
        let plan = null,
          roll = null;
        try {
          const deck = document.createElement("div");
          deck.innerHTML = gamePlanOverlayV11(!1);
          const V = window.__PREGAME_V51;
          roll = V && V.silentPlan ? V.silentPlan(e, deck) : null;
          if (roll) plan = roll.id;
          else {
            // v41's fallback, the scout pick, with the same composed buffs a drawn deck gets
            const card =
                deck.querySelector('.recommended-v11[onclick*="chooseGamePlanV11"]') ||
                deck.querySelector('[onclick*="chooseGamePlanV11"]'),
              mm = card && (card.getAttribute("onclick") || "").match(/chooseGamePlanV11\('([^']+)'/);
            if (mm) {
              plan = mm[1];
              e._nextGameBoost = 0;
              e._tempStatBuffsV25 = (window.__GROWTH_V42 && window.__GROWTH_V42.compose(e)) || null;
            }
          }
        } catch (_) {}
        if (!plan) plan = e.weeklyPlan103 || "balanced";
        w.wheelV85 = roll
          ? { id: roll.id, name: roll.name, icon: roll.icon, band: roll.band, lines: roll.lines, chosen: !!roll.chosen }
          : { id: plan, name: (gamePlanById(plan) || {}).name || plan, band: "scout", lines: "scout pick" };
        // fate roll (v50 wrapper) → ca() → the week is marked played: the played-week chain
        if (typeof window.chooseGamePlanV11 == "function") window.chooseGamePlanV11(plan, !1);
        else {
          resolveWeekV11(e, w, plan);
          startWeek(!1);
        }
      } else startWeek(!1);
    } catch (err) {
      window.__V85.lastSilentError = String((err && err.stack) || err);
    } finally {
      window.__silentSimV85 = !1;
    }
    return !!w.played;
  }
  window.__silentWeekV85 = silentWeekV85;
  window.bookLiveGameV85 = bookLiveGameV85;
  /* ===== v146 D THE PLAN IS YOURS, AND THE NUMBERS SAY WHAT IT COSTS (the engine) =====
   * The weekly plan used to be drawn off a wheel; it is now CHOSEN on the pregame's fifth page, and
   * this block is what the choice is priced with. Three things live here:
   *
   *  1. THE SWING IS REAL. Every plan has carried a `varMult` since v11, and all it ever did was
   *     widen the game RATING (`h = D(-6,6)*varMult` in ca()) — the box score never saw it, so
   *     "BOOM/BUST" was a word on a card. `formRollV146` rolls a form swing with the game it swings:
   *     ±`planFormPtsV146` points (3) × the plan's varMult × the season choice's varMult, flat onto
   *     every key the engine asks the you-player for, through the same buff list the focus and the
   *     plan roll ride. Mean zero, so it moves the spread and not the average. `TU("planFormV146",0)`
   *     switches it off.
   *  2. THE GAME YOU WATCH IS THE GAME THAT WAS BOOKED. __aiSeasonGame stashes the seed, the buff list
   *     and the coordinator's script it played the week with (`e._simInV146`); lt() plays the live
   *     game on those inputs (`liveInV146`). Before this the watched game ran on the week's RATING as
   *     its seed, after decayWearV111 had already dropped the plan roll and _gameScriptV23 had been
   *     nulled — so the projection, the booked game and the live game were three different games.
   *     `TU("liveSameInputsV146",0)` restores the old live inputs.
   *  3. THE PROJECTION. `projSampleV146` plays the week's real game — Yr, the seed __aiSeasonGame
   *     would use (`aiSeedV146`), the opponent's multiplier, the body's condMult, the focus, the
   *     coordinator's script — with NO plan effects, headless, one game a tick while the wizard is
   *     up (`projStartV146`, `TU("projGamesV146",12)` games a week). `projFitV146` pools those games
   *     into a per-stat reference by dividing each by the usage it was played at (`volV146`: the
   *     share ratio, and for a ball-carrier the touch dial through the same clamps Yr applies) and
   *     the focus it carried; `projectV146` puts any usage / focus / plan back on top: the plan's
   *     seed (`perf` and the scouting matchup, as ca() adds them), the plan roll's odds and stats
   *     (from the v51 block), the v50 fate roll's odds and attribute, and the form swing — every
   *     attribute effect converted to the accessor's normalised points (1.15 a point) weighted by the
   *     position's own weights, and priced at `projKappaV146` a point (measured, 60–80 games a side:
   *     ±6 agent points on every key moved an RB's yards ×1.35/×.83, a WR's ×1.62/×.67, an LB's
   *     tackles ×1.15/×.84; +10 moved a QB's passing ×1.36, an RB's rushing ×1.72). A fate hit is on the
   *     SHEET, so it also moves OVR → the seed (`projSeedKV146` a seed point — measured near zero
   *     everywhere but the QB, +20 seed = ×1.19 passing) → and et()'s rating (2.4 an OVR point).
   *     VARIANCE is one number: the 1σ spread of the position's headline stat around its projection,
   *     as a percentage — the engine's own game-to-game noise (measured from the samples; 60% of it
   *     thins with volume, `projThinV146`, the rest is per-game) combined with the plan's swing. The
   *     low–high on every row is the 80% band, stated in whole numbers.
   *  `scripts/v146Dcheck.mjs` holds the projection against the real booking path, game for game. */
  function formRollV146(vm) {
    if (vm == null || !TU("planFormV146", 1)) return { pts: 0, buffs: [] };
    const pts = Math.round(TU("planFormPtsV146", 3) * vm * (Math.random() * 2 - 1) * 10) / 10;
    return { pts, buffs: pts ? SIM_KEYS_V111.map(k => ({ stat: k, amt: pts, v146: "form" })) : [] };
  }
  function liveInV146(t, a) {
    const S = t && t._simInV146;
    if (!S || !TU("liveSameInputsV146", 1) || S.wk !== a) return null;
    delete t._simInV146;
    return S;
  }
  function aiSeedV146(e, opts) {
    opts = opts || {};
    let seed = __seasonSeed(e);
    const gm = Math.max(1, LEVELS[e.level].games);
    seed += ((opts.perfSeed || 0) / gm) * 2;
    if (opts.playoff) seed += treeFx("playoffPerf") + seasonModFx("playoffPerf");
    seed += clutchV131(e, opts);
    if (opts.oppBoost) seed -= opts.oppBoost * 30;
    return clamp99(seed, 5, 100);
  }
  /* the plan-free inputs ca() and __aiSeasonGame build for THIS week (the same arithmetic, read-only) */
  function projWeekInV146(e, w) {
    const n =
        w.opponentV11 ||
        createOpponentProfile(w.opp, e.level, w.week || 1, e.seasonSeed, w.playoff ? w.roundIdx : void 0),
      l = conditionModifiers(e),
      d = e.level >= 7 ? lifestyleEffects(e) : { perf: 0, injuryRisk: 0 },
      evP = ((e.eventChoice && e.eventChoice.perf) || 0) + (e.eventBenchV128 || 0),
      oppRating = n ? n.rating : 45 + e.level * 5.5;
    return {
      n,
      l,
      d,
      evP,
      basePerf: (l.perf || 0) + (d.perf || 0),
      opts: {
        playoff: !!w.playoff,
        oppRating,
        oppBoost: (oppRating - (45 + e.level * 5.5)) / 180 + (w.playoff ? 0.08 : 0),
        load: loadOfV111(e)
      }
    };
  }
  const PROJ_STATS_V146 = {
    QB: [
      ["comp", "Comp"],
      ["att", "Att"],
      ["pass", "Pass yds", 1],
      ["td", "TD"],
      ["int", "INT"],
      ["rush", "Rush yds"]
    ],
    RB: [
      ["carries", "Carries"],
      ["rush", "Rush yds", 1],
      ["td", "TD"],
      ["rec_c", "Catches"],
      ["rec", "Rec yds"]
    ],
    WR: [
      ["tgt", "Targets"],
      ["rec_c", "Catches"],
      ["rec", "Rec yds", 1],
      ["td", "TD"]
    ],
    TE: [
      ["tgt", "Targets"],
      ["rec_c", "Catches"],
      ["rec", "Rec yds", 1],
      ["td", "TD"]
    ],
    DL: [
      ["tackle", "Tackles", 1],
      ["sack", "Sacks"],
      ["ff", "Forced fum"]
    ],
    LB: [
      ["tackle", "Tackles", 1],
      ["sack", "Sacks"],
      ["int", "INT"],
      ["pd", "Pass def"]
    ],
    CB: [
      ["tackle", "Tackles", 1],
      ["pd", "Pass def"],
      ["int", "INT"]
    ],
    S: [
      ["tackle", "Tackles", 1],
      ["pd", "Pass def"],
      ["int", "INT"]
    ]
  };
  /* what a point of SEED (the team quality __aiSeasonGame hands Yr) is worth to his headline stat, per position */
  const kseedV146 = pos => {
    const K = TU("projSeedKV146", {
      QB: 0.008,
      RB: 0.004,
      WR: 0.004,
      TE: 0.004,
      DL: 0.002,
      LB: 0.002,
      CB: 0.002,
      S: 0.002
    });
    return K && K[pos] != null ? K[pos] : typeof K === "number" ? K : 0.008;
  };
  const kapV146 = pos => {
    const K = TU("projKappaV146", {
      QB: 0.02,
      RB: 0.04,
      WR: 0.055,
      TE: 0.05,
      DL: 0.02,
      LB: 0.023,
      CB: 0.022,
      S: 0.022
    });
    return K && K[pos] != null ? K[pos] : 0.03;
  };
  /* how many opportunities a usage step buys, relative to NORMAL — read off the same dials Yr reads:
   * the share is the chance he is on the field for a snap; a back's touch dial multiplies his lead
   * share (.72, clamped at leadShareMaxV111); a receiver's multiplies the chance a target is forced
   * his way (WR .32, TE .2, RB .1, capped at .62) on top of his natural share of the rest */
  function volV146(pos, k, sr, t) {
    if (pos === "RB" && (k === "carries" || k === "rush" || k === "td"))
      return (sr * Math.min(0.72 * t, TU("leadShareMaxV111", 0.97))) / 0.72;
    const b = { WR: 0.32, TE: 0.2, RB: 0.1 }[pos];
    if (b) {
      const q = TU("projNatTgtV146", 0.2),
        tt = Math.min(t, 0.62 / b);
      return (sr * (b * tt + (1 - b * tt) * q)) / (b + (1 - b) * q);
    }
    return sr;
  }
  const shareRelV146 = (e, k) => shareOfV111(useKeyV111(k), e) / Math.max(0.01, shareOfV111("normal", e));
  /* an attribute change in the accessor's own normalised points (1.15 a point), weighted by the position */
  function projPtsV146(e, dl) {
    const W = (POSITIONS[e.pos] || {}).w || {};
    let s = 0,
      t = 0;
    for (const k in W) {
      t += W[k];
      s += W[k] * (dl[k] || 0);
    }
    return t ? (1.15 * s) / t : 0;
  }
  const agentV146 = (e, k) => simScaleV141((e.attrs && e.attrs[k]) || 10) * condMultV54(e);
  function focusDeltaV146(e, key) {
    const r = key && focusForV111(e.pos).find(x => x.key === key);
    if (!r) return {};
    const d = {};
    [r.stat].concat(r.also || []).forEach(k => {
      d[k] = (Math.min(r.mul, TU("focusMulCeil", 1.35)) - 1) * agentV146(e, k);
    });
    return d;
  }
  let projV146 = { key: null, samples: [], timer: 0, on: !1, listeners: [] };
  const projKeyV146 = e => {
    const w = curWeekV111(e);
    return [
      e.name || "",
      e.pos,
      e.level | 0,
      e.totalSeasons | 0,
      (e.weekResults || []).indexOf(w),
      (e.weekResults || []).filter(x => x && x.played).length
    ].join("|");
  };
  function projSampleV146(e) {
    e = e || (state && state.player);
    const w = curWeekV111(e);
    if (!e || !w || !e.pos || w.played) return null;
    const I = projWeekInV146(e, w),
      seed = aiSeedV146(e, Object.assign({}, I.opts, { perfSeed: I.basePerf + I.evP })),
      u = usageV111(e, w),
      fb = buffForV111(w);
    let base = null;
    try {
      base = (window.__GROWTH_V42 && window.__GROWTH_V42.compose(e)) || null;
    } catch (_) {}
    const sv = {
      wx: window.__WX_V79,
      dy: window.__WX_DAY_V144,
      om: window.__oppMulV22,
      tb: window.__youTempBuffsV25,
      gs: window.__gameScriptBiasV23,
      sb: window.__youStatBoostPctV20,
      fb: state._fateBoost
    };
    let g = null;
    try {
      state._fateBoost = 0;
      window.__oppMulV22 = __oppMulForV22(w.opp || (I.n && I.n.name));
      window.__youTempBuffsV25 = (base || []).concat(ownBuffsV111(e));
      window.__gameScriptBiasV23 = e._gameScriptV23 && e._gameScriptV23.gsPass != null ? e._gameScriptV23.gsPass : null;
      window.__youStatBoostPctV20 = 0;
      g = simGameV2(seed, e.pos);
    } catch (_) {
      g = null;
    } finally {
      window.__WX_V79 = sv.wx;
      window.__WX_DAY_V144 = sv.dy;
      window.__oppMulV22 = sv.om;
      window.__youTempBuffsV25 = sv.tb;
      window.__gameScriptBiasV23 = sv.gs;
      window.__youStatBoostPctV20 = sv.sb;
      state._fateBoost = sv.fb;
    }
    if (!g || !g.stat) return null;
    /* the rating exactly as __aiSeasonGame builds it from this box score, before ca()'s plan terms */
    const grader = typeof gradeGame == "function" ? gradeGame : window.__gradeGame,
      snaps = (g.plays || []).filter(p => p && p.involved).length || 12;
    let dev = 0;
    try {
      dev = clamp99((grader(e.pos, g.stat, snaps).score - 70) / 24, -0.8, 1);
    } catch (_) {}
    let etp = 50;
    try {
      etp = rollGamePerf(e, I.basePerf + I.evP, I.opts).perf;
    } catch (_) {}
    const perf = clamp99(
      Math.round(
        clamp99(Math.round(etp + dev * 13 + (g.usScore > g.themScore ? 3 : g.usScore < g.themScore ? -2 : 0)), 1, 100) *
          condMultV54(e)
      ),
      1,
      100
    );
    return { stat: g.stat, key: u.key, sr: shareRelV146(e, u.key), touch: u.touchMul, focus: fb ? fb.key : null, perf };
  }
  /* a projection must not touch the career: et()'s wrappers roll a luck line and move coach trust, so
   * every top-level key of the player (and of the state) that a sample changed is put back as it was */
  const projSkipV146 = (k, v) => (k === "weekResults" || k === "player" ? undefined : v);
  function projGuardV146(e) {
    const pk = JSON.stringify(e, projSkipV146),
      ok_ = JSON.stringify(state, projSkipV146);
    return () => {
      try {
        const P = JSON.parse(pk),
          O = JSON.parse(ok_),
          put = (t, src) => {
            Object.keys(t).forEach(k => {
              if (k !== "weekResults" && k !== "player" && !(k in src)) delete t[k];
            });
            Object.keys(src).forEach(k => {
              if (JSON.stringify(t[k]) !== JSON.stringify(src[k])) t[k] = src[k];
            });
          };
        put(e, P);
        put(state, O);
      } catch (_) {}
    };
  }
  function projResetV146(e) {
    const k = projKeyV146(e);
    if (projV146.key !== k) {
      projV146.key = k;
      projV146.samples = [];
    }
  }
  function projSampleNV146(n, e) {
    e = e || (state && state.player);
    if (!e) return 0;
    projResetV146(e);
    let c = 0;
    for (let i = 0; i < n; i++) {
      const back = projGuardV146(e),
        s = projSampleV146(e);
      back();
      if (s) {
        projV146.samples.push(s);
        c++;
      }
    }
    projTellV146();
    return c;
  }
  function projTellV146() {
    projV146.listeners.slice().forEach(fn => {
      try {
        fn(projV146.samples.length);
      } catch (_) {}
    });
  }
  /* one game a tick while the pregame is up; a closed wizard, a simmed week or a full book stops it */
  function projStartV146() {
    const e = state && state.player;
    if (!e || window.__silentSimV85) return;
    projResetV146(e);
    if (projV146.on) return;
    projV146.on = !0;
    const tick = () => {
      projV146.timer = 0;
      const N = TU("projGamesV146", 12),
        p = state && state.player;
      if (
        !p ||
        !document.getElementById("pregameV1513") ||
        window.__silentSimV85 ||
        projV146.samples.length >= N ||
        projKeyV146(p) !== projV146.key
      ) {
        projV146.on = !1;
        return;
      }
      const back = projGuardV146(p),
        s = projSampleV146(p);
      back();
      if (s) {
        projV146.samples.push(s);
        projTellV146();
      } else {
        projV146.on = !1;
        return;
      }
      projV146.timer = setTimeout(tick, TU("projTickMsV146", 90));
    };
    projV146.timer = setTimeout(tick, TU("projFirstMsV146", 40));
  }
  function projStopV146() {
    if (projV146.timer) clearTimeout(projV146.timer);
    projV146.timer = 0;
    projV146.on = !1;
  }
  /* the pooled reference: each sample divided by the opportunities its usage bought and the focus it carried */
  function projFitV146(e) {
    const L = PROJ_STATS_V146[e.pos] || [],
      S = projV146.samples,
      kap = kapV146(e.pos),
      out = { n: S.length, st: {}, perf: null };
    if (!S.length) return out;
    const fa = {};
    S.forEach(s => {
      if (!(s.focus in fa)) fa[s.focus] = Math.exp(kap * projPtsV146(e, focusDeltaV146(e, s.focus)));
    });
    L.forEach(([k]) => {
      const xs = S.map(s => {
        const v = Math.max(0.05, volV146(e.pos, k, s.sr, s.touch));
        return { x: (Number(s.stat[k]) || 0) / (v * fa[s.focus]), v };
      });
      const mu = xs.reduce((a, b) => a + b.x, 0) / xs.length,
        cv2 = mu > 0 ? xs.reduce((a, b) => a + b.v * (b.x - mu) * (b.x - mu), 0) / xs.length / (mu * mu) : 0;
      out.st[k] = { mu, cv2 };
    });
    const pm = S.reduce((a, s) => a + s.perf, 0) / S.length;
    out.perf = { mu: pm, sd: Math.sqrt(S.reduce((a, s) => a + (s.perf - pm) * (s.perf - pm), 0) / S.length) };
    return out;
  }
  /* what one plan is, in the numbers the game applies it with */
  function planFactsV146(e, id) {
    e = e || (state && state.player);
    const w = curWeekV111(e),
      p = gamePlanById(id);
    if (!e || !w || !p || p.id !== id) return null;
    const I = projWeekInV146(e, w),
      gm = Math.max(1, LEVELS[e.level].games);
    const r = I.n
        ? matchupPlanModifier(I.n, id, planStreak(e, id), state.specializationV11)
        : { perf: 0, trust: 0, injuryRisk: 0, note: "" },
      rep = planStreak(e, id);
    const F = (window.__FATE_PLANS || {})[id];
    let fate = null;
    if (F) {
      try {
        const pity = state && state._fateMiss >= 2 ? 0.12 : 0,
          od = Math.min(0.97, (window.__fateOdds ? window.__fateOdds(F.odds) : F.odds) + pity),
          rolls = nodeLvl("fateReroll") >= 1 || nodeLvl("oracle") >= 1 ? 2 : 1,
          a = window.__fateAttrFor ? window.__fateAttrFor(id) : null;
        if (a)
          fate = {
            odds: 1 - Math.pow(1 - od, rolls),
            attr: a.attr,
            name: a.name,
            amount: a.amount,
            hedge: nodeLvl("fateHedge") >= 1 ? Math.max(1, Math.round(a.amount * 0.3)) : 0,
            label: F.label
          };
      } catch (_) {}
    }
    const vmR = (e.eventChoice && e.eventChoice.varMult) || 1,
      y0 =
        I.l.injuryRisk +
        (p.inj || 0) +
        (r.injuryRisk || 0) +
        ((I.d && I.d.injuryRisk) || 0) -
        (id === "recovery" ? 0.08 : 0);
    /* ca(): the game's own injury roll (injChanceV54, the number page 4 shows) survives only if a second roll under the plan's multiplier does */
    let pInj = null,
      injBase = null,
      injMul = null;
    try {
      const ch = injChanceV54(e, I.opts),
        yy = tunedInjuryRisk(y0 + ch * 0.055, e) * (w.rivalV128 ? RIVAL_MULT_V128() : 1);
      injBase = ch;
      injMul = Math.min(1, injPlanMultV54({ injury: yy }));
      pInj = ch * injMul;
    } catch (_) {}
    const fat = { disciplined: 5, feature: 11, explosive: 16, team: 9, recovery: -10 }[id];
    return {
      id,
      icon: p.icon,
      name: p.name,
      desc: p.desc,
      tags: p.tags || [],
      perf: p.perf || 0,
      match: r.perf || 0,
      matchNote: r.note || "",
      rep,
      ratingShift: (((p.perf || 0) + (r.perf || 0)) / gm) * 2,
      varMult: p.varMult || 1,
      vmR,
      formPts: TU("planFormV146", 1) ? TU("planFormPtsV146", 3) * (p.varMult || 1) * vmR : 0,
      gradeSwing: 6 * (p.varMult || 1) * vmR,
      inj: p.inj || 0,
      pInj,
      injBase,
      injMul,
      fatigue: fat == null ? 7 : fat,
      snap: p.snap || 0,
      trust: (p.trust || 0) + (r.trust || 0),
      comp: p.comp || 0,
      mom: p.mom || 0,
      fate,
      counter: !!(I.n && I.n.counterPlan === id),
      trap: !!(I.n && I.n.trapPlan === id)
    };
  }
  /* the projection for one usage / focus / plan. `band` is the v51 plan roll for that plan:
   * {g, n, r, statsG, statsR} — its odds and the stats a click or a backfire moves */
  function projectV146(st) {
    const e = state && state.player;
    if (!e || !e.pos) return null;
    const w = curWeekV111(e);
    if (!w) return null;
    projResetV146(e);
    st = st || {};
    const fit = projFitV146(e),
      L = PROJ_STATS_V146[e.pos] || [],
      kap = kapV146(e.pos),
      uk = useKeyV111(st.usage !== undefined ? st.usage : w.usageV111),
      sr = shareRelV146(e, uk),
      t = touchOfV111(uk);
    const P = st.plan ? planFactsV146(e, st.plan) : null,
      Afoc = Math.exp(kap * projPtsV146(e, focusDeltaV146(e, st.focus !== undefined ? st.focus : w.focusV111))),
      seedFx = Math.exp(kseedV146(e.pos) * (P ? P.ratingShift : 0));
    /* the plan's attribute scenarios: the roll (click / nothing / backfire) × the fate roll (hit / miss), each a point
     * shift — and a fate hit is written onto the SHEET, so it also lifts his OVR, which lifts the seed (__seasonSeed,
     * 1.35 a point) and et()'s rating (2.4 a point): that channel rides the scenario too */
    const B = st.band || null,
      sc = [],
      kS = kseedV146(e.pos);
    const one = (ks, a) => {
      const d = {};
      (ks || []).forEach(k => {
        d[k] = (d[k] || 0) + a;
      });
      return projPtsV146(e, d);
    };
    const bands = B
      ? [
          [B.g || 0, one(B.statsG, TU("projBandGV146", 4))],
          [B.n || 0, 0],
          [B.r || 0, one(B.statsR, -TU("projBandRV146", 3.5))]
        ]
      : [[1, 0]];
    const fz =
      P && P.fate
        ? (() => {
            const k = P.fate.attr,
              v = (e.attrs && e.attrs[k]) || 10,
              o0 = playerOvr(e),
              dd = a => [
                projPtsV146(e, { [k]: (simScaleV141(v + a) - simScaleV141(v)) * condMultV54(e) }),
                a ? playerOvr(Object.assign({}, e, { attrs: Object.assign({}, e.attrs, { [k]: v + a }) })) - o0 : 0
              ];
            const H = dd(P.fate.amount),
              Mi = P.fate.hedge ? dd(P.fate.hedge) : [0, 0];
            return [
              [P.fate.odds, H[0], H[1]],
              [1 - P.fate.odds, Mi[0], Mi[1]]
            ];
          })()
        : [[1, 0, 0]];
    bands.forEach(b => fz.forEach(z => sc.push([b[0] * z[0], b[1] + z[1], z[2]])));
    const lin = s => kap * s[1] + kS * 1.35 * s[2],
      c = kap * 1.15 * (P ? P.formPts : 0),
      sh = x => (x < 1e-6 ? 1 : Math.sinh(x) / x),
      EM = sc.reduce((a, s) => a + s[0] * Math.exp(lin(s)), 0) * sh(c),
      EM2 = sc.reduce((a, s) => a + s[0] * Math.exp(2 * lin(s)), 0) * sh(2 * c),
      dOvr = sc.reduce((a, s) => a + s[0] * s[2], 0);
    const z = TU("projBandZV146", 1.2816),
      rows = L.map(([k, label, hd]) => {
        const F = fit.st[k];
        if (!F) return { k, label, head: !!hd, mean: null };
        /* the engine's bounce thins with volume, but only partly: a breakaway, the script and the opponent are shared by
         * every touch of a game, so a share `projThinV146` of the variance is per-touch and the rest is per-GAME */
        const V = Math.max(0.05, volV146(e.pos, k, sr, t)),
          th = TU("projThinV146", 0.6),
          cv2 = F.cv2 * (th / V + 1 - th),
          m0 = F.mu * V * Afoc * seedFx,
          mean = m0 * EM,
          ex2 = m0 * m0 * EM2 * (1 + cv2),
          sd = Math.sqrt(Math.max(0, ex2 - mean * mean));
        /* a box score is whole numbers: the band is stated on the outcomes a game can actually print (0–2 tackles, not 0.04–2.3) */
        return {
          k,
          label,
          head: !!hd,
          mean,
          sd,
          lo: Math.max(0, Math.floor(mean - z * sd + 1e-9)),
          hi: Math.max(1, Math.ceil(mean + z * sd - 1e-9)),
          cvSim: Math.sqrt(cv2)
        };
      });
    const hd = rows.find(r => r.head && r.mean != null) || null,
      planSw = EM > 0 ? Math.sqrt(Math.max(0, EM2 / (EM * EM) - 1)) : 0;
    let grade = null;
    if (fit.perf) {
      const vm = P ? P.varMult : 1,
        vmR = (e.eventChoice && e.eventChoice.varMult) || 1,
        pm = ((e.composure103 || 50) - 50) * 0.06 + ((e.momentum103 || 50) - 50) * 0.045,
        mu = 50 + (fit.perf.mu + (P ? P.ratingShift : 0) + 2.4 * dOvr + pm - 50) * vmR,
        sd = vmR * Math.sqrt(fit.perf.sd * fit.perf.sd + 12 * vm * vm);
      grade = { mean: clamp99(mu, 1, 100), sd, lo: clamp99(mu - z * sd, 1, 100), hi: clamp99(mu + z * sd, 1, 100) };
    }
    return {
      n: fit.n,
      N: TU("projGamesV146", 12),
      ready: fit.n >= TU("projMinV146", 3),
      usage: uk,
      rows,
      head: hd
        ? {
            k: hd.k,
            label: hd.label,
            pct: hd.mean > 0 ? Math.round((100 * hd.sd) / hd.mean) : null,
            sim: Math.round(100 * hd.cvSim),
            plan: Math.round(100 * planSw)
          }
        : null,
      planSwing: Math.round(100 * planSw),
      grade,
      plan: P,
      mult: { V: sr, t, Afoc, seedFx, EM, EM2 }
    };
  }
  window.__V146 = {
    project: projectV146,
    facts: planFactsV146,
    sample: projSampleV146,
    sampleN: projSampleNV146,
    start: projStartV146,
    stop: projStopV146,
    samples: () => projV146.samples,
    fit: () => projFitV146(state.player),
    vol: volV146,
    STATS: PROJ_STATS_V146,
    form: formRollV146,
    seed: aiSeedV146,
    onSample: fn => {
      projV146.listeners = projV146.listeners.filter(x => x !== fn);
      projV146.listeners.push(fn);
    },
    reset: () => {
      projV146.key = null;
      projV146.samples = [];
    }
  };
  /* ===== v90 THE ROLLS HAPPEN IN THE BACKGROUND =====
   * Story arcs queue a stage every few games (constantly at the UFF), and each stage wants a
   * choice and a dice roll. On the sim-the-rest path the player is not watching, so the choice
   * is made by a rule and the roll is the game's own roll (zn): the arc advances exactly as if
   * the card had been clicked. The rule: TU("autoStoryStyle",0) — 0 takes the safest option
   * (highest base chance), 1 the boldest (lowest), anything between interpolates. Every roll
   * is written on the week (w.autoRollsV90) and summarised in the toast, so nothing is hidden,
   * only unattended. Quick play, where the player is present, still shows the card. */
  function pickStoryChoiceV90(stage, style) {
    const c = ((stage && stage.choices) || []).slice();
    if (!c.length) return null;
    c.sort((a, b) => (b.baseChance || 0) - (a.baseChance || 0));
    const k = Math.round(clamp99(style == null ? TU("autoStoryStyle", 0) : style, 0, 1) * (c.length - 1));
    return c[k];
  }
  function autoStoryV90(e, w) {
    const out = [];
    if (!e) return out;
    ensureStoryState(e);
    let guard = 0;
    while (e.storyDecisionQueueV11.length && guard++ < 12) {
      const st = e.storyDecisionQueueV11[0],
        pick = pickStoryChoiceV90(st);
      if (!pick) break;
      const r = resolveStoryChoice(e, state, pick.id, state.specializationV11);
      if (!r) break;
      const rec = {
        arc: st.arcTitle || "",
        stage: st.title || "",
        choice: pick.label || pick.id,
        success: !!r.success,
        week: w ? w.week : null
      };
      out.push(rec);
      if (w) (w.autoRollsV90 = w.autoRollsV90 || []).push(rec);
    }
    return out;
  }
  /* UFF life events (v12) queue every third week and stop the sim the same way. They carry no
   * dice: the answer is the event's first choice, which is the resolver's own fallback. */
  function autoLifeV90(e, w) {
    const out = [];
    if (!e) return out;
    let guard = 0;
    for (;;) {
      const a = e.lifeV12,
        s = a && a.eventQueue && a.eventQueue[0];
      if (!s || guard++ > 12) break;
      const c = s.choices && s.choices[0];
      if (!c) break;
      const r = resolveLifeEvent(e, c.id);
      if (!r) break;
      const rec = {
        kind: "life",
        arc: s.title || s.id || "",
        stage: "",
        choice: (r.choice && r.choice.label) || c.label || c.id,
        success: null,
        week: w ? w.week : null
      };
      out.push(rec);
      if (w) (w.autoRollsV90 = w.autoRollsV90 || []).push(rec);
    }
    return out;
  }
  window.__V90 = {
    pick: pickStoryChoiceV90,
    drain: autoStoryV90,
    drainLife: autoLifeV90,
    last: [],
    queueLife: (e, week) => {
      /* test hook: wi() fires on a seeded 28% roll, this queues the first eligible template outright */ if (
        typeof ensureFinanceState != "function"
      )
        return null;
      e.level = Math.max(e.level || 0, 7);
      const a = ensureFinanceState(e);
      if (!a) return null;
      const w = week || 3,
        i = LIFE_EVENTS.find(l => w >= l.minWeek);
      if (!i) return null;
      const r = { ...i, uid: `${i.id}-${e.totalSeasons || 0}-${w}` };
      return (a.eventQueue.push(r), r);
    },
    queueStage: (e, idx) => {
      const t = STORY_ARCS[idx || 0];
      if (!t) return null;
      ensureStoryState(e);
      e.activeStoryArcV11 = e.activeStoryArcV11 || { id: t.id, stage: 0, weeksSinceStage: 0, choices: [] };
      return $n(e, t.id, 0);
    }
  };
  Object.assign(window, {
    chooseOriginV11: chooseOriginV11,
    chooseSpecializationV11: chooseSpecializationV11,
    chooseGamePlanV11: chooseGamePlanV11,
    resolveMomentV11: resolveMomentV11,
    resolveStoryChoiceV11: resolveStoryChoiceV11,
    showNflOffersV11: showNflOffersV11,
    acceptNflOfferV11: acceptNflOfferV11,
    declineNflOffersV11: declineNflOffersV11,
    showPositionChangeV11: showPositionChangeV11,
    changePositionV11: changePositionV11,
    buyLegacyUnlockV11: buyLegacyUnlockV11,
    prepareWeek103: prepareWeek103,
    playWeek: playWeek,
    pickPos: pickPos,
    finishWeekGame: finishWeekGame,
    finishSeasonGames: finishSeasonGames,
    simRemainingWeeks: simRemainingWeeks,
    startSeasonGames: startSeasonGames,
    render: render
  });
  window.__GRIDIRON_LEGACY_VERSION__ = "11.0-career-loop";
  window.__GRIDIRON_FEATURES__ = {
    sequentialWeeks: !0,
    storyArcs: !0,
    namedRivals: !0,
    highLeverageMoments: !0,
    prestigeSpecialization: !0,
    partialVictories: !0,
    scouting: !0,
    conditionManagement: !0,
    nflSurvival: !0,
    runIdentity: !0
  };
  state?.player && ensureV11(state.player);
  safeBootV140(function () {
    render();
  }, "draw the restored screen");
  let lifeTab = "overview";
  function ensureV12() {
    (ensureAccountV12(state),
      state.player && (ensureV11(state.player), state.player.level >= 7 && ensureFinanceState(state.player)));
  }
  const cd = newPlayer;
  newPlayer = function () {
    const e = cd();
    return (ensureAccountV12(state), applyPendingVow(state, e), e);
  };
  function dd(e) {
    return String(e.nflStateV11?.status || "camp")
      .replaceAll("-", " ")
      .replace(/\b\w/g, a => a.toUpperCase());
  }
  function financeCard(e, t = !1) {
    const a = ensureFinanceState(e);
    if (!a) return "";
    const s = retirementReadiness(e),
      n = lifestyleEffects(e),
      i = e.nflStateV11?.contract || {};
    return `<div class="card nfl-life-summary-v12">
    <div class="impact-head"><div><div class="impact-kicker">UFF LIFE · MONEY COMPOUNDS EVERY WEEK</div><div class="h2" style="margin:2px 0 0">${formatMoney(s.netWorth)} Net Worth</div></div><div class="life-ready-v12 ${s.ready ? "ready" : ""}">${Math.round(s.ratio * 100)}%<small>RETIREMENT</small></div></div>
    <div class="life-money-grid-v12"><div><b>${formatMoney(a.cash)}</b><small>Cash</small></div><div><b>${formatMoney(liquidInvestments(e))}</b><small>Invested</small></div><div><b style="color:${a.debt > 0 ? "var(--blood)" : "var(--chalk)"}">${formatMoney(a.debt || 0)}</b><small>Debt</small></div><div><b>${formatMoney(i.salary || 0)}</b><small>Salary</small></div><div><b>${formatMoney(n.weeklyCost * 52)}</b><small>Annual life</small></div><div><b>${escHtml(s.plan.name)}</b><small>Exit plan</small></div></div>
    ${t ? "" : `<div class="retirement-track-v12"><i style="width:${clamp99(s.ratio * 100, 0, 100)}%"></i></div><div class="small">Retirement assets ${formatMoney(s.retirementAssets)} · safe investment income ${formatMoney(s.annualIncome)}/yr · family security ${Math.round(a.familySecurity)} · fulfillment ${Math.round(a.fulfillment)}</div>`}
    <button class="btn secondary" style="margin-top:10px" onclick="showLifeV12()">💼 Manage Money, Home & Family Goals</button>
  </div>`;
  }
  function ud(e) {
    const t = [...(e.weekResults || [])].reverse().find(s => s.played && s.feedbackV12) || e.lastWeekFeedbackV12;
    if (!t) return "";
    const a = t.feedbackV12;
    return `<div class="card feedback-v12"><div class="eyebrow">WHY THIS WEEK HAPPENED</div><div class="h2" style="margin:2px 0 8px">${t.perf} Rating · ${t.won ? "Win" : "Loss"} vs ${escHtml(t.opp)}</div><div class="feedback-lines-v12">${a.reasons.map(s => `<div class="${s.tone || ""}"><span>${s.value}</span><b>${escHtml(s.label)}</b></div>`).join("")}</div><div class="feedback-future-v12">${a.future.map(s => `<span>${escHtml(s)}</span>`).join("")}</div></div>`;
  }
  function fd(e) {
    const t = regretScenario(e.level, e);
    return `<div class="card regret-card-v12"><div class="regret-scene-v12"><span>💭</span><div><div class="eyebrow">ONE LAST THOUGHT</div><div class="regret-dialogue-v12">${escHtml(t.dialogue)}</div></div></div><div class="small" style="margin-top:8px">Choose the promise that carries into the next career, then the Prestige menu opens immediately.</div><div class="vow-grid-v12">${t.vows.map(a => `<button onclick="chooseRegretVowV12('${a.id}')"><span>${a.icon}</span><b>${escHtml(a.label)}</b><small>${escHtml(a.description)}</small></button>`).join("")}</div></div>`;
  }
  function chooseRegretVowV12(e) {
    const t = state.player;
    !t ||
      !regretScenario(t.level, t).vows.some(s => s.id === e) ||
      ((state.pendingVowV12 = e),
      (state.afterCareerV12 = !0),
      saveGame(),
      goView("shop"),
      setTimeout(() => bigMoment("PROMISE MADE", "Spend Prestige, then run the career back", "gold"), 180));
  }
  const md = shopBack;
  shopBack = function () {
    if (state.afterCareerV12) {
      ((state.afterCareerV12 = !1), (state.player = null), saveGame(), goView("menu"));
      return;
    }
    md();
  };
  const pd = screenGameOver;
  screenGameOver = function () {
    pd();
    const e = state.player;
    if (!e) return;
    const t = byId("screen")?.querySelector(".banner");
    if (e.voluntaryRetirementV12) {
      const a = retirementReadiness(e),
        s = ensureFinanceState(e);
      t &&
        ((t.className = "banner nfl"),
        (t.querySelector(".big-emoji").textContent = "🌅"),
        (t.querySelector(".bt").textContent = "Retired on Your Terms"),
        (t.querySelector(".bs").innerHTML =
          `${escHtml(e.name)} walks away at age ${e.age} with <b>${formatMoney(a.netWorth)}</b> in net worth, ${Object.keys(s.completedGoals || {}).length} life goals completed, and a family plan that lasts beyond football.`));
      const n = byId("screen").querySelector(".regret-card-v12");
      (n && n.remove(),
        t?.insertAdjacentHTML("afterend", retirementCard(e)),
        (byId("dock").innerHTML =
          `<button class="btn" onclick="S.afterCareerV12=true;go('shop')">🔁 RUN IT BACK — Prestige & Begin the Next Legacy</button><div style="height:8px"></div><button class="btn secondary" onclick="prestigeReset()">Run It Back Now</button>`));
    } else
      (t?.insertAdjacentHTML("afterend", fd(e)),
        e.level >= 7 && t?.insertAdjacentHTML("afterend", retirementCard(e)),
        (byId("dock").innerHTML =
          `<div class="small center" style="margin-bottom:8px">Choose what you would change. You will be taken to Prestige immediately.</div><button class="btn" onclick="chooseRegretVowV12('smarter')">🧠 Train Smarter & Open Prestige</button><div style="height:8px"></div><button class="btn secondary" onclick="chooseRegretVowV12('health')">🩺 Protect My Body & Open Prestige</button>`));
  };
  function retirementCard(e) {
    const t = ensureFinanceState(e);
    if (!t) return "";
    const a = retirementReadiness(e);
    return `<div class="card finance-legacy-v12"><div class="eyebrow">LIFE AFTER THE FINAL SNAP</div><div class="h2" style="margin:2px 0">${formatMoney(a.netWorth)} left for the future</div><div class="life-money-grid-v12"><div><b>${formatMoney(t.careerGross)}</b><small>Career gross</small></div><div><b>${formatMoney(t.investmentGains)}</b><small>Market gains</small></div><div><b>${Object.keys(t.completedGoals || {}).length}</b><small>Life goals</small></div><div><b>${Math.round(t.familySecurity)}</b><small>Family security</small></div></div></div>`;
  }
  function setLifeTabV12(e) {
    ((lifeTab = e), screenLife());
  }
  function showLifeV12() {
    ((state.view = "life"), saveGame(), render());
  }
  function lifeChoiceBtn(e, t, a) {
    return `<button class="life-choice-v12 ${a === t.id ? "active" : ""}" onclick="chooseLifestyleV12('${e}','${t.id}')"><span>${t.icon}</span><div><b>${escHtml(t.name)}</b><small>${escHtml(t.description)}</small></div><em>${t.weekly ? formatMoney(t.weekly) + "/wk" : "FREE"}</em></button>`;
  }
  function screenLife() {
    const e = state.player;
    if (!e || e.level < 7) {
      goView("hub");
      return;
    }
    ensureV12();
    const t = ensureFinanceState(e),
      a = retirementReadiness(e),
      s = e.nflStateV11 || {},
      n = s.contract || {},
      i = lifestyleEffects(e),
      r = houseById(t.houseId);
    syncCounters();
    const l = document.querySelector(".topbar");
    l && (l.style.display = "");
    let d = "";
    (lifeTab === "overview" &&
      (d = `
    <div class="card life-contract-v12"><div class="impact-head"><div><div class="impact-kicker">${escHtml(dd(e).toUpperCase())} · CURRENT CONTRACT</div><div class="h2" style="margin:2px 0 0">${formatMoney(n.salary || 0)} / season</div></div><div class="contract-years-v12">${n.yearsRemaining || 0}<small>YEARS LEFT</small></div></div><div class="life-money-grid-v12"><div><b>${Math.round((n.guaranteed || 0) * 100)}%</b><small>Guaranteed</small></div><div><b>${formatMoney(n.signingBonus || 0)}</b><small>Signing bonus</small></div><div><b>${formatMoney(i.weeklyCost)}</b><small>Weekly lifestyle</small></div><div><b>${Math.round(s.security || 0)}</b><small>Roster security</small></div></div></div>
    <div class="card retirement-v12"><div class="impact-head"><div><div class="impact-kicker">RETIREMENT PLAN · ${escHtml(a.plan.name.toUpperCase())}</div><div class="h2" style="margin:2px 0 0">${a.ready ? "You Can Walk Away" : "Still Building the Exit"}</div></div><div class="life-ready-v12 ${a.ready ? "ready" : ""}">${Math.round(a.ratio * 100)}%<small>FUNDED</small></div></div><div class="retirement-track-v12"><i style="width:${clamp99(a.ratio * 100, 0, 100)}%"></i></div><div class="small">Target ${formatMoney(a.target)} · retirement assets ${formatMoney(a.retirementAssets)} · total net worth ${formatMoney(a.netWorth)} · sustainable income ${formatMoney(a.annualIncome)}/yr</div><div class="life-goal-pills-v12"><span>${a.goals}/${a.totalGoals} goals</span><span>Family ${Math.round(t.familySecurity)}</span><span>Fulfillment ${Math.round(t.fulfillment)}</span><span class="${t.debt > 0 ? "danger" : ""}">Debt ${formatMoney(t.debt || 0)}</span></div><div class="retirement-plan-grid-v12">${RETIREMENT_PLANS.map(c => `<button class="${t.retirementPlanId === c.id ? "active" : ""}" onclick="setRetirementPlanV12('${c.id}')"><span>${c.icon}</span><b>${escHtml(c.name)}</b><small>${formatMoney(c.target)} target</small></button>`).join("")}</div>${canRetire(e) ? `<button class="btn ${a.ready ? "" : "secondary"}" style="margin-top:10px" onclick="retireV12()">🌅 Retire From Football</button>` : ""}</div>
    <div class="card"><div class="eyebrow">RECENT MONEY MOVES</div><div class="money-history-v12">${
      t.history.length
        ? t.history
            .slice(0, 8)
            .map(
              c =>
                `<div><span>${escHtml(c.label)}</span><b class="${c.amount >= 0 ? "pos" : "neg"}">${c.amount >= 0 ? "+" : ""}${formatMoney(c.amount)}</b></div>`
            )
            .join("")
        : '<div class="small">Your first game check will appear here.</div>'
    }</div></div>`),
      lifeTab === "lifestyle" &&
        (d = `<div class="card"><div class="eyebrow">FOOD · RECOVERY · LUXURY</div><div class="h2" style="margin:2px 0">Spend Money to Shape the Player</div><div class="small">Better support can improve performance and recovery, but every expense reduces the money available for family and retirement.</div></div><div class="h2">Meals</div>${LIFESTYLES.meals.map(c => lifeChoiceBtn("meals", c, t.lifestyle.meals)).join("")}<div class="h2">Recovery Team</div>${LIFESTYLES.recovery.map(c => lifeChoiceBtn("recovery", c, t.lifestyle.recovery)).join("")}<div class="h2">Off-Field Lifestyle</div>${LIFESTYLES.luxury.map(c => lifeChoiceBtn("luxury", c, t.lifestyle.luxury)).join("")}<div class="card tight center"><b style="font-family:Oswald;color:var(--gold)">${formatMoney(i.weeklyCost)} per week</b><div class="small">Recovery +${i.recovery} · performance +${Math.round(i.perf)} · injury risk ${Math.round(i.injuryRisk * 100)}%</div></div>`),
      lifeTab === "home" &&
        (d = `<div class="card"><div class="eyebrow">CURRENT HOME</div><div class="h2" style="margin:2px 0">${r.icon} ${escHtml(r.name)}</div><div class="small">${escHtml(r.description)} · value ${formatMoney(t.houseValue)}</div></div>${HOUSES.map(c => `<div class="house-card-v12 ${t.houseId === c.id ? "owned" : ""}"><span>${c.icon}</span><div><b>${escHtml(c.name)}</b><small>${escHtml(c.description)}</small><em>${c.price ? formatMoney(c.price) : "CURRENT RENTAL"} · ${formatMoney(c.weekly)}/wk upkeep</em></div><button ${t.houseId === c.id ? "disabled" : ""} onclick="buyHouseV12('${c.id}')">${t.houseId === c.id ? "OWNED" : "BUY"}</button></div>`).join("")}`),
      lifeTab === "invest" &&
        (d = `<div class="card"><div class="eyebrow">COMPOUNDING PORTFOLIO</div><div class="h2" style="margin:2px 0">${formatMoney(liquidInvestments(e))} Invested</div><div class="small">Returns and losses are applied after every UFF week. The game protects an emergency reserve of ${formatMoney(emergencyReserve(e))}; debt costs 16% annually until repaid.</div>${t.debt > 0 ? `<div class="nem-banner" style="margin-top:10px">⚠️ Outstanding debt: <b>${formatMoney(t.debt)}</b>. Half of cash above the reserve automatically repays it.</div>` : ""}</div>${PORTFOLIOS.map(
          c => {
            const u = t.investments[c.id] || 0;
            return `<div class="portfolio-card-v12"><span>${c.icon}</span><div><b>${escHtml(c.name)}</b><small>${escHtml(c.description)}</small><em>${formatMoney(u)} · ${Math.round(c.annualReturn * 100)}% expected annual return</em></div><div class="portfolio-actions-v12"><button onclick="investV12('${c.id}',.10)">+10%</button><button onclick="investV12('${c.id}',.25)">+25%</button><button ${u < 1e3 ? "disabled" : ""} onclick="withdrawV12('${c.id}',.25)">SELL</button></div></div>`;
          }
        ).join("")}`),
      lifeTab === "goals" &&
        (d = `<div class="card"><div class="eyebrow">WHAT THE MONEY IS FOR</div><div class="h2" style="margin:2px 0">Lifelong Family Goals</div><div class="small">Football ends. These objectives determine whether the career changed one life or an entire family.</div></div>${LIFE_GOALS.map(
          c => {
            const u = !!t.completedGoals[c.id],
              h = t.contributions[c.id] || 0,
              p = u ? 100 : clamp99((h / c.target) * 100, 0, 100);
            return `<div class="goal-card-v12 ${u ? "complete" : ""}"><span>${u ? "✓" : c.icon}</span><div><b>${escHtml(c.name)}</b><small>${escHtml(c.description)}</small><div class="goal-track-v12"><i style="width:${p}%"></i></div><em>${u ? "COMPLETED" : `${formatMoney(h)} / ${formatMoney(c.target)}`}</em></div>${!u && !c.house && !c.netWorth && !c.portfolio ? `<button onclick="fundGoalV12('${c.id}',.15)">FUND</button>` : ""}</div>`;
          }
        ).join("")}`),
      (byId("screen").innerHTML =
        `<div class="eyebrow">UFF LIFE SIMULATION · AGE ${e.age}</div><div class="h1">Life Beyond the Field</div><div class="life-hero-v12"><div><span>CASH</span><b>${formatMoney(t.cash)}</b></div><div><span>NET WORTH</span><b>${formatMoney(a.netWorth)}</b></div></div><div class="life-tabs-v12">${[
          ["overview", "Overview"],
          ["lifestyle", "Lifestyle"],
          ["home", "Homes"],
          ["invest", "Invest"],
          ["goals", "Goals"]
        ]
          .map(
            ([c, u]) => `<button class="${lifeTab === c ? "active" : ""}" onclick="setLifeTabV12('${c}')">${u}</button>`
          )
          .join("")}</div>${d}`),
      (byId("dock").innerHTML =
        `<button class="btn secondary" onclick="go(S.player.weekResults?'season':'hub')">Back to Football</button>`),
      $o());
  }
  function chooseLifestyleV12(e, t) {
    const a = state.player;
    chooseLifestyle(a, e, t) && (saveGame(), screenLife(), showToast("Lifestyle updated."));
  }
  function setRetirementPlanV12(e) {
    const t = state.player;
    setRetirementPlan(t, e) && (saveGame(), screenLife(), showToast("Retirement target updated."));
  }
  function buyHouseV12(e) {
    const t = buyHouse(state.player, e);
    if (!t.ok) {
      showToast(t.reason || "Purchase failed.");
      return;
    }
    (saveGame(), screenLife(), bigMoment("NEW HOME", t.house.name, "good"));
  }
  function investV12(e, t) {
    const a = ensureFinanceState(state.player),
      s = Math.max(1e3, Math.round(a.cash * t)),
      n = invest(state.player, e, s);
    if (!n.ok) {
      showToast(n.reason || "Investment failed.");
      return;
    }
    (saveGame(), screenLife(), showToast(`Invested ${formatMoney(n.amount)}`));
  }
  function withdrawV12(e, t) {
    const a = ensureFinanceState(state.player),
      s = Math.round((a.investments[e] || 0) * t),
      n = withdraw(state.player, e, s);
    if (!n.ok) {
      showToast(n.reason || "Sale failed.");
      return;
    }
    (saveGame(), screenLife(), showToast(`Moved ${formatMoney(n.amount)} to cash`));
  }
  function fundGoalV12(e, t) {
    const a = ensureFinanceState(state.player),
      s = contributeGoal(state.player, e, Math.round(a.cash * t));
    if (!s.ok) {
      showToast(s.reason || "Contribution failed.");
      return;
    }
    (saveGame(),
      screenLife(),
      s.completed
        ? bigMoment("LIFE GOAL COMPLETE", lifeGoalById(e).name, "good")
        : showToast(`Funded ${formatMoney(s.amount)}`));
  }
  function retireV12() {
    const e = state.player;
    if (!canRetire(e)) {
      showToast("You are not ready to retire yet.");
      return;
    }
    return askV150("Retire from football now? This ends the career and locks in your financial legacy.", {
      title: "Retire",
      ok: "Retire",
      danger: !0
    }).then(ok => ok && state.player === e && !e._settled && ((e.voluntaryRetirementV12 = !0), retirePlayer(e), saveGame(), endCareer(), !0));
  }
  function xd(e) {
    const t = e.lifeV12?.eventQueue?.[0];
    return t
      ? `<div class="decision-overlay life-event-overlay-v12"><div class="decision-panel"><div class="decision-kicker">${t.icon} UFF LIFE DECISION</div><div class="decision-title">${escHtml(t.title)}</div><div class="decision-copy">${escHtml(t.copy)} Your decision affects money, family, recovery, and the career beyond football.</div>${t.choices.map(a => `<button class="decision-choice" onclick="resolveLifeEventV12('${a.id}')"><b>${escHtml(a.label)}</b><small>${escHtml(a.description)}</small></button>`).join("")}</div></div>`
      : "";
  }
  function $o() {
    const e = state.player,
      t = e?.lifeV12?.eventQueue?.[0],
      a = document.querySelector(".life-event-overlay-v12");
    (t && !a && document.body.insertAdjacentHTML("beforeend", xd(e)), !t && a && a.remove());
  }
  function resolveLifeEventV12(e) {
    const t = resolveLifeEvent(state.player, e);
    if (!t) return;
    (document.querySelector(".life-event-overlay-v12")?.remove(), saveGame(), render());
    const a = t.goals || [];
    bigMoment(a.length ? "LIFE GOAL COMPLETE" : "LIFE DECISION MADE", t.choice.label, a.length ? "good" : "gold");
  }
  const Rd = processWeek95;
  processWeek95 = function (e, t) {
    const a = !!e && !e._financeProcessedV12;
    if ((Rd(e, t), !e || !a)) return;
    if (t.level >= 7) {
      const c = processNflFinancialWeek(t, e);
      ((e.financeV12 = c),
        maybeQueueLifeEvent(t, e),
        t.nflStateV11 && (t.nflStateV11.careerEarnings = t.lifeV12?.careerGross || 0));
    }
    const s = e.matchupV11 || {},
      n = e.conditionResultV11 || {},
      i = e.rivalResultV11 || {},
      r = [];
    (s.perf &&
      r.push({
        value: `${s.perf >= 0 ? "+" : ""}${Math.round(s.perf)}`,
        label: s.correct ? "Correct scouting counter" : "Matchup plan",
        tone: s.perf >= 0 ? "good" : "bad"
      }),
      e.lifeEffectsV12?.perf &&
        r.push({ value: `+${Math.round(e.lifeEffectsV12.perf)}`, label: "UFF lifestyle support", tone: "good" }));
    const l = Math.round(e.healthAtKickoffV11?.fatigue || 0);
    if (
      (l >= 45 &&
        r.push({
          value: `-${Math.round(Math.max(0, l - 45) * 0.16)}`,
          label: `Fatigue at kickoff (${l})`,
          tone: "bad"
        }),
      e.momentResolutionsV11?.length)
    ) {
      const c = e.momentResolutionsV11.reduce((u, h) => u + (h.perfDelta || 0), 0);
      r.push({ value: `${c >= 0 ? "+" : ""}${c}`, label: "High-leverage decisions", tone: c >= 0 ? "good" : "bad" });
    }
    r.length || r.push({ value: "±0", label: "No major external modifier", tone: "" });
    const d = [
      `Snap share ${Math.round((t.snapShare || 0) * 100)}%`,
      `Coach trust ${Math.round(t.coachTrust || 0)}`,
      `Fatigue ${n.fatigue ?? Math.round(t.conditionV11?.fatigue || 0)}`
    ];
    (e.financeV12 && d.push(`Game check ${e.financeV12.net >= 0 ? "+" : ""}${formatMoney(e.financeV12.net)}`),
      i.swing != null && d.push(`Rival swing ${i.swing >= 0 ? "+" : ""}${i.swing}%`),
      (e.feedbackV12 = { reasons: r, future: d }),
      (t.lastWeekFeedbackV12 = { perf: e.perf, won: e.won, opp: e.opp, feedbackV12: e.feedbackV12, played: !0 }),
      (e._financeProcessedV12 = !0),
      saveGame());
  };
  const Pd = startWeek;
  startWeek = function (e) {
    if (state.player?.lifeV12?.eventQueue?.length) {
      (render(), showToast("Resolve the UFF life decision first."));
      return;
    }
    Pd(!!e);
  };
  simRemainingWeeks = function () {
    const e = state.player;
    if (!e?.weekResults) return;
    const t = e.weekResults.filter(a => !a.playoff && !a.played);
    let n = 0,
      rolls = [];
    for (const a of t) {
      if (e.offersV146B || e.cutOutV146B) break;
      /* v146 B: a cut stops the sim — he signs somewhere, or it is over */ rolls.push(
        ...autoStoryV90(e, a),
        ...autoLifeV90(e, a)
      );
      /* v90: story stages and life events are answered here, not on screen */ if (!silentWeekV85(e, a)) break;
      /* v85: same silent chain as quick play, week after week */ n++;
      processWeek95(a, e);
    }
    rolls.push(...autoStoryV90(e, null), ...autoLifeV90(e, null));
    window.__V90.last = rolls;
    (saveGame(),
      goView("season"),
      n &&
        showToast(
          `⏭ Simmed ${n} week${n === 1 ? "" : "s"}${rolls.length ? ` · ${rolls.length} decision${rolls.length === 1 ? "" : "s"} made in the background (${rolls.filter(r => r.success).length} rolled up, ${rolls.filter(r => r.success === !1).length} down)` : ""} — every week on the plan you last chose, every game played by the engine`
        ));
  };
  gamePlanOverlayV11 = function (e) {
    const t = state.player;
    ensureV11(t);
    const a = t.weekResults ? t.weekResults.findIndex(n => !n.played) : -1,
      s = a >= 0 ? t.weekResults[a] : null;
    return s
      ? (s.opponentV11 ||
          (s.opponentV11 = createOpponentProfile(
            s.opp,
            t.level,
            s.week || a + 1,
            t.seasonSeed,
            s.playoff ? s.roundIdx : void 0
          )),
        scoutOpponent(s.opponentV11, t, state.specializationV11),
        `<div class="gameplan-overlay"><div class="gameplan-panel impact-plan v11-plan-panel"><div class="plan-hero"><div class="decision-kicker">WEEK ${s.week || a + 1} · NOTHING HAS BEEN ROLLED YET</div><div class="decision-title">Scout. Prepare. Then Play.</div><div class="decision-copy">Projected fatigue and injury risk include age, health, and the selected workload.</div></div>${scoutCard(s, t)}<div class="plan-deck">${GAME_PLANS.map(
          n => {
            const i = yn(n),
              r = planColor(n.id),
              l = s.opponentV11.scouted?.recommendedPlan === n.id,
              d = recoveryProjection(t, n.id);
            return `<button class="impact-plan-choice ${l ? "recommended-v11" : ""}" style="--planColor:${r}" onclick="chooseGamePlanV11('${n.id}',${e ? "true" : "false"})"><div class="plan-icon">${n.icon}</div><div><b>${n.name}${l ? " · SCOUT PICK" : ""}</b><small>${n.desc}</small><div class="gameplan-meta">${n.tags.map(c => `<span>${c}</span>`).join("")}<span class="projection-v12 ${d.label.toLowerCase()}">Fatigue ${d.fatigueAfter} · ${Math.round(d.injuryRisk * 100)}% injury</span></div></div><div class="plan-bars"><div class="mini-rating"><label><span>UPSIDE</span><span>${i[0]}</span></label><div class="mbar"><i style="width:${i[0]}%"></i></div></div><div class="mini-rating"><label><span>CONTROL</span><span>${i[1]}</span></label><div class="mbar"><i style="width:${i[1]}%"></i></div></div><div class="mini-rating"><label><span>RISK</span><span>${i[2]}</span></label><div class="mbar"><i style="width:${i[2]}%"></i></div></div></div></button>`;
          }
        ).join("")}<button class="btn ghost" onclick="closeGamePlan103()">Back</button></div></div></div>`)
      : "";
  };
  gamePlanOverlay = gamePlanOverlayV11;
  hn = function (e) {
    const t = e.seasonStats || {};
    if (t.grade)
      return String(typeof t.grade == "object" ? t.grade.grade || t.grade.letter || "B" : t.grade).toUpperCase();
    const a = t.games || t.sampleGames || [],
      s = t.avg || t.average || (a.length ? a.reduce((h, p) => h + (p.perf || 0), 0) / a.length : 50),
      n = LEVELS[e.level],
      i = playerOvr(e),
      r =
        (e.weekResults || []).filter(h => h.opponentV11).reduce((h, p) => h + p.opponentV11.rating, 0) /
          Math.max(1, (e.weekResults || []).filter(h => h.opponentV11).length) || 58 + e.level * 2,
      l = Object.values(state.tree || {}).reduce((h, p) => h + p, 0),
      d = 30 + effectivePrestige(state.prestige) * 0.65 + Math.sqrt(l) * 0.75,
      c = [28, 35, 43, 52, 62, 72, 78, 84, 90][e.level] || 50,
      u = clamp99(Math.max(c, 45 + (i - n.need) * 0.85, d, e.performanceExpectationV12 || 0), 26, 95);
    return expectedGrade(s, u, e.snapShare || 0.5, r).grade;
  };
  function Cd(e) {
    return e.level >= 7 ? financeCard(e, !1) : "";
  }
  const Ld = nflSurvivalCard;
  nflSurvivalCard = function (e) {
    return Ld(e) + Cd(e);
  };
  function Ed() {
    const e = state.player,
      t = byId("screen");
    if (!(!e || !t)) {
      if ((state.view === "season" || state.view === "result") && !t.querySelector(".feedback-v12")) {
        const a = ud(e);
        a && t.insertAdjacentHTML("beforeend", a);
      }
      (e.level >= 7 &&
        (state.view === "hub" || state.view === "season" || state.view === "result") &&
        !t.querySelector(".nfl-life-summary-v12") &&
        t.insertAdjacentHTML("beforeend", financeCard(e, !1)),
        $o());
    }
  }
  const Ad = render;
  render = function () {
    if ((ensureV12(), state.view === "life")) {
      screenLife();
      return;
    }
    (Ad(), Ed());
  }; /* ===== v146 E THE SHELL'S HOOK — every draw goes through here, so every NEW view (and the boot's restore, and v140's deferred retry) opens at the top =====
   * `shellPreV146` puts the shell class on <html> BEFORE the screen draws (the broadcast and the main menu are drawn without it, so they measure their own frame);
   * `shellPostV146` hands the view to the v146 E block at the end of the file, which resets every scroller it owns when the view changed. Hoisted, per v140. */
  var shellViewV146 = null,
    shellBootV146 = Date.now();
  function shellPreV146() {
    try {
      const v = state.view,
        off = { live: 1, menu: 1, highscore: 1, daily: 1, leaderboard: 1 };
      document.documentElement.classList.toggle("shell-v146", !!v && !off[v]);
    } catch (e) {}
  }
  function shellTopV146() {
    try {
      [document.scrollingElement, document.getElementById("app"), document.getElementById("screen")].forEach(el => {
        el && (el.scrollTop = 0);
      });
    } catch (e) {}
  }
  function shellPostV146() {
    try {
      const v = state.view,
        ch = v !== shellViewV146 || Date.now() - shellBootV146 < 4e3;
      shellViewV146 = v;
      window.__SHELL_V146 ? window.__SHELL_V146.after(v, ch) : ch && shellTopV146();
    } catch (e) {}
  }
  const _qV146 = render;
  render = function () {
    shellPreV146();
    try {
      _qV146();
    } finally {
      shellPostV146();
    }
  };
  Object.assign(window, {
    chooseRegretVowV12: chooseRegretVowV12,
    setLifeTabV12: setLifeTabV12,
    showLifeV12: showLifeV12,
    chooseLifestyleV12: chooseLifestyleV12,
    setRetirementPlanV12: setRetirementPlanV12,
    buyHouseV12: buyHouseV12,
    investV12: investV12,
    withdrawV12: withdrawV12,
    fundGoalV12: fundGoalV12,
    retireV12: retireV12,
    resolveLifeEventV12: resolveLifeEventV12,
    shopBack: shopBack,
    simRemainingWeeks: simRemainingWeeks,
    render: render
  });
  window.__GRIDIRON_LEGACY_VERSION__ = "12.1-career-life-polish";
  window.__GRIDIRON_FEATURES__ = {
    ...(window.__GRIDIRON_FEATURES__ || {}),
    cutReflection: !0,
    balancePolish: !0,
    nflLife: !0,
    weeklySalary: !0,
    investing: !0,
    housing: !0,
    lifeGoals: !0,
    retirementPlanning: !0
  };
  safeBootV140(function () {
    ensureV12();
    render();
  }, "settle the save and draw it"); /* ===== v137 THE VAULT IS WHERE THE POINTS LIVE =====
   * The Prestige Vault (public/rib-vault*.js) is a self-contained screen that knows nothing
   * about this file. This is the whole of the game's side of it: four globals that hand the
   * vault the tree's own truth — `ot` the node table, `At` the price of the NEXT level, `M`
   * the level, `Xa` the gate — plus `Yl` as `window.buy`, which stays THE ONE authoritative
   * purchase. The vault reserves, animates, and then calls `buy` exactly once; it never
   * touches o.pp, o.tree or the save itself, and `window.__prestigeNodesV137().price` is the
   * same `At` the shop row prints, so the two can never disagree.
   * The shop's BUY now routes through `vaultBuy`, which walks the player into the vault with
   * that upgrade selected; if the vault has not loaded, or the node is locked, maxed or
   * unaffordable, it falls straight through to `Yl` and the game behaves exactly as before.
   * `window.__V137` is the hook. vaultcheck.mjs is the gate. */
  /* ===== v146 B TWO STRIKES IN THE UFF, AND YOU PICK THE TEAM =====
   * Before: the UFF had two cut systems and neither ended anything. The v10 season-end roll (the one
   * `longLeash` / `survivor` / `guaranteedDeal` tune) set `nflCutPending`, and the v11 season wrapper
   * cleared it on the very next line; the v11 weekly evaluation (`Qn`) dropped a man to "waivers" and
   * left three offers behind a button on the result card. A UFF career ran until age or a decline.
   *
   * Now a cut is a STRIKE, counted per season (`cutsV146B.k` is level:season, so a new season starts
   * clean). A strike comes from the weekly evaluation hitting waivers, from the season-end roll, or
   * from the v11 season-end waiver (one strike a season-end, never two). `cutsAllowedV146B()` —
   * TU("dflCutsAllowedV146B", 2) plus the `secondChance` node's `cutLives` — is the strike that ENDS
   * it: short of it, he is released and picks one of three clubs that want him as a BACKUP; on it,
   * `cutOutV146B` is set and the career goes to the existing cut screen (`ms`). Signing is a choice of
   * three at every door: entering the UFF (`na`), after a cut, and in free agency (the v11 `Jn`
   * expiry). An offer's club quality `q` is added to the roster factor in `Wr` and `teamPairV76` (the
   * watched and the simmed game), its role maps onto the v11 status the weekly evaluation already
   * moves (a backup who plays well earns rotation, then the start, back), and `roleShareV146B` is the
   * multiplier `trustShareV120` applies — so the snap share on the card is the share the pregame,
   * the engine and the silent week all use. `window.__V146B`; scripts/v146Bcheck.mjs. ===== */
  const clV146B = (v, a, b) => Math.max(a, Math.min(b, v));
  const ROLE_V146B = {
    starter: { label: "STARTER", status: "starter", sec: 72, depth: 1 },
    rotation: { label: "ROTATION", status: "rotation", sec: 58, depth: 2 },
    backup: { label: "BACKUP", status: "active-backup", sec: 40, depth: 3 }
  };
  const TIER_V146B = [
    { k: "contender", label: "CONTENDER", q: () => TU("clubQContenderV146B", 0.16) },
    { k: "middle", label: "MID-TABLE", q: () => 0 },
    { k: "rebuild", label: "REBUILDING", q: () => -TU("clubQRebuildV146B", 0.16) }
  ];
  function seasonKeyV146B(e) {
    return ((e && e.level) || 0) + ":" + ((e && e.totalSeasons) || 0);
  }
  function cutsAllowedV146B() {
    let g = 0;
    try {
      g = treeFx("cutLives");
    } catch (_) {}
    return Math.max(1, Math.round(TU("dflCutsAllowedV146B", 2) + g));
  }
  function strikesV146B(e) {
    const C = e && e.cutsV146B;
    return C && C.k === seasonKeyV146B(e) ? C.n : 0;
  }
  function clubQV146B(e) {
    return e && e.level >= 7 && e.clubV146B && !isNaN(+e.clubV146B.q) ? +e.clubV146B.q : 0;
  }
  /* the snaps a role is worth, off the v11 status the weekly evaluation keeps current */
  function roleShareV146B(pl) {
    if (!pl || !(pl.level >= 7) || !pl.clubV146B) return 1;
    const st = (pl.nflStateV11 && pl.nflStateV11.status) || "",
      r = pl.clubV146B.role;
    const k =
      st === "franchise" || st === "starter"
        ? "starter"
        : st === "rotation"
          ? "rotation"
          : st === "active-backup"
            ? "backup"
            : st === "practice-squad" || st === "waivers"
              ? "ps"
              : r || "starter";
    return k === "starter"
      ? 1
      : k === "rotation"
        ? TU("roleShareRotationV146B", 0.8)
        : k === "backup"
          ? TU("roleShareBackupV146B", 0.55)
          : TU("roleSharePsV146B", 0.4);
  }
  function clubRulesV146B() {
    const n = cutsAllowedV146B();
    return `Get cut <b>${n} times in one UFF season</b> and your career is over. ${n > 2 ? `Each of the first ${n - 1} cuts costs` : "The first cut costs"} you your team: you sign with another club as a <b>backup</b>, on fewer snaps. The count resets every season, and a backup who plays well earns his role back.`;
  }
  function strikeLineV146B(e) {
    if (!e || e.level < 7) return "";
    const n = strikesV146B(e),
      a = cutsAllowedV146B();
    return `<div class="small strike-line-v146b" style="margin-top:8px">✂️ Cuts this season: <b style="color:${n ? "var(--blood)" : "var(--good)"}">${n} of ${a}</b> — cut number ${a} ends the career.${e.clubV146B ? ` · ${escHtml(e.clubV146B.name)}, signed as ${escHtml(ROLE_V146B[e.clubV146B.role] ? ROLE_V146B[e.clubV146B.role].label : "")}` : ""}</div>`;
  }
  function leftV146B(e) {
    const l = cutsAllowedV146B() - strikesV146B(e);
    return l <= 1
      ? "One more cut this season ends the career."
      : `Get cut ${l} more times this season and the career is over.`;
  }
  /* three clubs, one from each tier: the strong side has the least room for him, the weak side
   * starts him. After a cut every door is a backup's, and the trust he walks in with is lower. */
  function offersForV146B(e, kind) {
    const cur = typeof teamName == "function" ? teamName(e) : "",
      pool = DFL_V123.map((n, i) => i).filter(i => DFL_V123[i] !== cur);
    for (let i = pool.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [pool[i], pool[j]] = [pool[j], pool[i]];
    }
    let seed = 55;
    try {
      seed = __seasonSeed(e);
    } catch (_) {}
    const bump =
        kind === "cut" ? 0 : seed >= TU("clubStarSeedV146B", 72) ? 1 : seed < TU("clubFringeSeedV146B", 42) ? -1 : 0,
      R = ["backup", "rotation", "starter"],
      ovr = playerOvr(e);
    return TIER_V146B.map((T, k) => {
      const i = pool[k],
        q = Math.round((T.q() + (Math.random() - 0.5) * 0.05) * 1000) / 1000,
        role = kind === "cut" ? "backup" : R[clV146B(k + bump, 0, 2)],
        RR = ROLE_V146B[role],
        trust = Math.round(
          clV146B(
            kind === "cut" ? TU("cutTrustV146B", 18) + k * 5 : TU("entryTrustV146B", 38) + k * 8 + bump * 6,
            5,
            90
          )
        ),
        security = Math.round(RR.sec + (Math.random() - 0.5) * 6),
        share = trustShareV120({
          level: 7,
          coachTrust: trust,
          clubV146B: { role },
          nflStateV11: { status: RR.status }
        }),
        rating = teamPairV76(e, { clubQ: q }).us,
        salary = Math.round(
          nflSalaryForStatus(RR.status, ovr, security) * (kind === "cut" ? TU("cutPayV146B", 0.7) : 1) * (1 + q)
        ),
        years = kind === "cut" ? 1 : role === "starter" ? 3 : 2;
      return {
        i,
        name: DFL_V123[i],
        town: TOWNS[(i * 7 + 3) % TOWNS.length],
        mascot: MASCOTS[(i * 11 + 5) % MASCOTS.length],
        tier: T.k,
        tierLabel: T.label,
        q,
        role,
        status: RR.status,
        security,
        trust,
        share: Math.round(share * 100) / 100,
        rating,
        salary,
        years,
        schemeFit: Math.round(40 + Math.random() * 40),
        guaranteed: role === "starter" ? 0.52 : role === "rotation" ? 0.31 : 0.16,
        depth: (e.pos || "") + RR.depth
      };
    });
  }
  function openOffersV146B(e, kind, extra) {
    e.offersV146B = Object.assign(
      { kind, list: offersForV146B(e, kind), sel: null, at: seasonKeyV146B(e) },
      extra || {}
    );
    const s = e.nflStateV11;
    s && (s.offers = []);
    return e.offersV146B;
  }
  /* one strike. Returns {end} — the caller does not need to know which door comes next */
  function cutV146B(e, why, key) {
    if (!e || !(e.level >= 7) || e.cutOutV146B) return null;
    if (e.offersV146B && e.offersV146B.kind === "cut") return null; // released and not yet re-signed: one release, one strike
    key = key || seasonKeyV146B(e);
    const C = e.cutsV146B && e.cutsV146B.k === key ? e.cutsV146B : (e.cutsV146B = { k: key, n: 0, log: [] });
    const from = typeof teamName == "function" ? teamName(e) : "";
    C.n++;
    C.log.push({ why, club: from, week: (e.weekResults || []).filter(w => w && w.played).length });
    const allowed = cutsAllowedV146B();
    window.__V146B && window.__V146B.log.push({ why, n: C.n, allowed, key });
    if (C.n >= allowed) {
      e.cutOutV146B = { why, n: C.n, allowed, key };
      e.offersV146B = null;
      e.nflCutPending = !0;
      const s = e.nflStateV11;
      s && (s.offers = []);
      return { end: !0, n: C.n, allowed };
    }
    openOffersV146B(e, "cut", { n: C.n, allowed, why, from });
    return { end: !1, n: C.n, allowed };
  }
  function pickClubV146B(i) {
    const e = state && state.player,
      O = e && e.offersV146B;
    if (!O || !O.list[i]) return;
    O.sel = i;
    document.querySelectorAll(".club-card-v146b").forEach((el, k) => el.classList.toggle("on", k === i));
    const b = document.getElementById("clubSignV146B");
    if (b) {
      b.disabled = !1;
      b.textContent = "✍️ Sign with the " + O.list[i].mascot + " ›";
    }
  }
  function signClubV146B(i) {
    const e = state && state.player,
      O = e && e.offersV146B;
    if (!O) return null;
    const c = O.list[i == null ? O.sel : i];
    if (!c) return null;
    const s = typeof ensureNflState == "function" ? ensureNflState(e, playerOvr(e)) : null;
    if (s) {
      s.status = c.status;
      s.security = c.security;
      s.schemeFit = c.schemeFit;
      s.offers = [];
      s.contract = {
        yearsRemaining: c.years,
        salary: c.salary,
        signingBonus: Math.round(c.salary * (c.role === "starter" ? 0.28 : c.role === "rotation" ? 0.16 : 0.08)),
        guaranteed: c.guaranteed,
        expectedRole: c.status
      };
    }
    e.nflCutPending = !1;
    e.coachTrust = c.trust;
    e.teamIdentity = e.teamIdentity || newTeamIdentity();
    e.teamIdentity.dfl = c.name;
    e.clubV146B = {
      i: c.i,
      name: c.name,
      town: c.town,
      mascot: c.mascot,
      tier: c.tier,
      q: c.q,
      role: c.role,
      rating: c.rating,
      share: c.share,
      from: O.kind,
      signed: seasonKeyV146B(e)
    };
    e.teamOvr = c.rating;
    e.teamRosterSeasonV158 = null; // the roster he walks into is this club's, rebuilt at its rating
    (e.clubHistoryV146B = e.clubHistoryV146B || []).push({
      name: c.name,
      role: c.role,
      kind: O.kind,
      key: seasonKeyV146B(e)
    });
    const back = O.back;
    e.offersV146B = null;
    saveGame();
    try {
      pushStory(
        "✍️",
        `Signed with the ${c.name}`,
        `${ROLE_V146B[c.role].label.toLowerCase()} · ${Math.round(c.share * 100)}% of the snaps · team ${c.rating} OVR`
      );
    } catch (_) {}
    goView(back && ["hub", "season", "training"].indexOf(back) >= 0 ? back : "hub");
    try {
      showToast(`✍️ Signed with the ${c.name} · ${Math.round(c.share * 100)}% of the snaps`);
    } catch (_) {}
    return c;
  }
  /* the club's look replaces the Team Creator's while he plays for it */
  function clubCustomV146B(c) {
    try {
      const e = state && state.player,
        k = e && e.level >= 7 && e.clubV146B;
      if (!k) return c;
      return Object.assign({}, c, { schoolName: k.town, teamName: k.mascot, userSetV22: !1, byLevel: {} });
    } catch (_) {
      return c;
    }
  }
  function clubV146B() {
    const e = state && state.player,
      O = e && e.offersV146B;
    if (!O || !O.list || !O.list.length) {
      state.view = "hub";
      return screenHub();
    }
    const E = window.TEAM_LOGOS_V44,
      cut = O.kind === "cut",
      fa = O.kind === "fa",
      allowed = cutsAllowedV146B(),
      n = strikesV146B(e);
    const kick = cut
        ? `RELEASED · STRIKE ${O.n || n} OF ${allowed}`
        : fa
          ? "UFF · FREE AGENCY"
          : "UFF · YOUR FIRST CONTRACT",
      title = cut ? "Three clubs want a backup" : fa ? "Your contract is up" : "Three clubs made an offer",
      lead = cut
        ? `${O.from ? "The " + escHtml(O.from) + " let you go." : "You were let go."} ${leftV146B(e)}`
        : fa
          ? "Pick where the next deal is signed."
          : "A strong club has the least room for you; a weak one starts you.";
    const card = (c, k) => {
      const li = E ? E.forName(c.name) : 0,
        pal = E ? E.pal(li) : ["#333", "#999"],
        on = O.sel === k;
      return `<button class="club-card-v146b${on ? " on" : ""}" style="--cp:${pal[0]};--cs:${pal[1]}" onclick="pickClubV146B(${k})">
      <div class="cc-top"><i class="emblem-v44 cc-crest" style="${E ? E.css(li) : ""}"></i><div class="cc-name"><b>${escHtml(c.name)}</b><small>${c.tierLabel} · ${c.years} yr · ${fmtMoneyShort(c.salary)}</small></div><div class="cc-role cc-${c.role}">${ROLE_V146B[c.role].label}</div></div>
      <div class="cc-grid"><div><b>${c.rating}</b><small>TEAM OVR</small></div><div><b>${Math.round(c.share * 100)}%</b><small>SNAP SHARE</small></div><div><b>${escHtml(c.depth)}</b><small>DEPTH</small></div><div><b>${c.trust}</b><small>COACH TRUST</small></div></div></button>`;
    };
    document.getElementById("screen").innerHTML =
      `<div class="club-v146b"><div class="eyebrow" style="color:${cut ? "var(--blood)" : "var(--gold)"}">${kick}</div><div class="h2 cc-title">${title}</div><div class="small cc-lead">${lead}</div>
    ${O.list.map(card).join("")}<div class="cc-rules">✂️ ${clubRulesV146B()}${n || cut ? ` <b>This season: ${n} of ${allowed}.</b>` : ""}</div></div>`;
    document.getElementById("dock").innerHTML =
      `<button class="btn" id="clubSignV146B" ${O.sel == null ? "disabled" : ""} onclick="signClubV146B()">${O.sel == null ? "Tap a club to choose" : "✍️ Sign with the " + escHtml(O.list[O.sel].mascot) + " ›"}</button>`;
  }
  (function () {
    try {
      const st = document.createElement("style");
      st.textContent = `
.club-v146b{display:flex;flex-direction:column;gap:7px;padding-bottom:4px}
.club-v146b .cc-title{margin:0}.club-v146b .cc-lead{margin:-2px 0 2px;line-height:1.3}
.club-card-v146b{display:block;width:100%;text-align:left;padding:9px 10px;border-radius:12px;border:1px solid rgba(255,255,255,.12);border-left:5px solid var(--cp);background:linear-gradient(180deg,rgba(255,255,255,.05),rgba(0,0,0,.18));color:inherit;font:inherit;cursor:pointer}
.club-card-v146b.on{border-color:var(--gold);border-left-color:var(--cp);box-shadow:0 0 0 2px var(--gold) inset}
.club-card-v146b .cc-top{display:flex;align-items:center;gap:9px}
.club-card-v146b .cc-crest{width:40px;height:40px;flex:0 0 40px;display:block;border-radius:8px;background-color:var(--cp)}
.club-card-v146b .cc-name{flex:1;min-width:0}.club-card-v146b .cc-name b{display:block;font-family:'Oswald',sans-serif;font-size:16px;line-height:1.1;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.club-card-v146b .cc-name small{font-size:10.5px;opacity:.72;letter-spacing:.03em}
.club-card-v146b .cc-role{font-family:'Oswald',sans-serif;font-size:11px;letter-spacing:.06em;padding:3px 7px;border-radius:999px;background:rgba(255,255,255,.1)}
.club-card-v146b .cc-starter{background:rgba(87,224,122,.2);color:#8ff0ad}.club-card-v146b .cc-rotation{background:rgba(240,187,69,.2);color:#f5cf77}.club-card-v146b .cc-backup{background:rgba(224,98,98,.2);color:#f0a0a0}
.club-card-v146b .cc-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:4px;margin-top:7px;text-align:center}
.club-card-v146b .cc-grid b{display:block;font-family:'Oswald',sans-serif;font-size:17px;line-height:1.1}.club-card-v146b .cc-grid small{font-size:9px;opacity:.65;letter-spacing:.05em}
.club-v146b .cc-rules{font-size:11px;line-height:1.35;opacity:.85;padding:7px 9px;border-radius:10px;background:rgba(0,0,0,.22)}`;
      document.head.appendChild(st);
    } catch (_) {}
  })();
  /* the weekly evaluation: waivers is a cut */
  const Qn0V146B = evaluateNflWeek;
  evaluateNflWeek = function (e, t, a) {
    const r = Qn0V146B.apply(this, arguments);
    try {
      if (r && r.status === "waivers" && TU("v146B", 1)) {
        r.cutV146B = cutV146B(e, "week");
        const s = e.nflStateV11;
        s && (s.offers = []);
      }
    } catch (_) {}
    return r;
  };
  /* the season end: the v10 roll (live again) or the v11 waiver is one strike; an expired deal is free agency */
  const tt0V146B = simSeason;
  simSeason = function (e) {
    const key = seasonKeyV146B(e),
      lvl = e && e.level,
      t = tt0V146B.apply(this, arguments);
    try {
      if (e && lvl >= 7 && t && TU("v146B", 1) && !e.cutOutV146B) {
        const sc = t.nflCareerV11,
          rolled = !!(TU("seasonCutRollV146B", 1) && t.nflCutRollV146B);
        if (rolled || (sc && sc.cut)) t.cutV146B = cutV146B(e, rolled ? "season" : "waivers", key);
        else if (sc && sc.freeAgent && !e.offersV146B) openOffersV146B(e, "fa");
        e.clubV146B && (e.teamOvr = teamPairV76(e, {}).us);
      }
    } catch (_) {}
    return t;
  };
  /* the door into the UFF */
  const na0V146B = advanceLevel;
  advanceLevel = function () {
    const e = state && state.player,
      was = e ? e.level : 99,
      r = na0V146B.apply(this, arguments);
    try {
      const p = state && state.player;
      if (p && was < 7 && p.level >= 7 && !p.clubV146B && !p.offersV146B && TU("v146B", 1)) {
        openOffersV146B(p, "entry");
        saveGame();
        state.view === "hub" && goView("club");
      }
    } catch (_) {}
    return r;
  };
  window.advance = advanceLevel;
  /* the screens between weeks wait for the signature; a career that is over goes to its screen */
  const q0V146B = render;
  render = function () {
    try {
      const e = state && state.player,
        v = state && state.view,
        soft = ["hub", "season", "training"];
      if (e && e.cutOutV146B && !e._settled && (soft.indexOf(v) >= 0 || v === "club")) state.view = "gameover";
      else if (e && e.offersV146B && soft.indexOf(v) >= 0) {
        e.offersV146B.back = v;
        state.view = "club";
      }
    } catch (_) {}
    const r = q0V146B.apply(this, arguments);
    try {
      const e = state && state.player;
      if (state.view === "result" && e && e.offersV146B && e.offersV146B.kind === "cut") {
        const sc = document.getElementById("screen");
        sc &&
          !sc.querySelector(".banner-v146b") &&
          sc.insertAdjacentHTML(
            "afterbegin",
            `<div class="banner fail banner-v146b"><div class="big-emoji">✂️</div><div class="bt">Released · Strike ${e.offersV146B.n} of ${e.offersV146B.allowed}</div><div class="bs">Three clubs want you as a backup. You pick one before the offseason. ${leftV146B(e)}</div></div>`
          );
      }
    } catch (_) {}
    return r;
  };
  window.pickClubV146B = pickClubV146B;
  window.signClubV146B = signClubV146B;
  window.__V146B = {
    log: [],
    allowed: () => cutsAllowedV146B(),
    strikes: e => strikesV146B(e || state.player),
    rules: () => clubRulesV146B(),
    roleShare: e => roleShareV146B(e || state.player),
    offers: (e, k) => openOffersV146B(e || state.player, k || "entry"),
    sign: i => signClubV146B(i),
    pick: i => pickClubV146B(i),
    cut: (why, e) => cutV146B(e || state.player, why || "test"),
    evaluate: (perf, e) => {
      e = e || state.player;
      return evaluateNflWeek(e, { perf: perf == null ? 10 : perf, injured: !1 }, playerOvr(e));
    },
    clubQ: e => clubQV146B(e || state.player),
    ROLE: ROLE_V146B
  };
  /* ===== v147 A THE CAREER RUNS TO THE END, AND ENDS WHEN YOU SAY =====
   * Three reports from a phone, a QB in the Interstellar League, and one cause under each.
   *
   * ENDING A CAREER. The only voluntary exit above college is the life screen's RETIRE FROM FOOTBALL
   * (`Md`), and it could not be reached: the way in (`showLifeV12`, "Manage Money, Home & Family
   * Goals") is appended by `Ed()` AFTER the v75 sectioner has split the hub, so it lands in the STORY
   * tab inside a folded v97 accordion; and on the life screen the v146 E shell's `fit()` squeezes the
   * retirement card (`#screen > .card`) into an inner scroller, which clips the button off the bottom
   * of the card — tapping around the screen finds nothing. And `qs()` hid the button entirely under age
   * 30 unless the exit plan was funded. Now: at level 7+ `qs` lets him walk away any time
   * (TU("retireAnyTimeV147",1)), the life screen carries the retire button in its DOCK (never
   * clipped), the hub dock has a "🌅 LIFE & RETIRE" chip, and the v146 B club screen has "🌅 RETIRE
   * INSTEAD" — every one of them goes through `Md` (the confirm, `voluntaryRetirementV12`, `Si`, `fn`).
   *
   * THE SIM. `silentWeekV85` refuses to play a week while `nflStateV11.offers` holds anything ("an
   * offer on the table needs an answer first"), and v146 B moved signing to `offersV146B` — but a save
   * from before v146 B, or a season end where `cutV146B` declined a second strike, still carries the
   * v11 list. Sim Remaining then played NOTHING and said nothing, and PLAY LIVE opened the dead v11
   * offers overlay. `migrateOffersV147` turns a stale list into the v146 B three-club screen (free
   * agency, no strike) wherever it is found. And at level 7+ Sim Remaining is `simSeasonV147`: the
   * regular season, the playoffs round by round (`dn`), and the report card (`finishSeasonGames`) in
   * one tap (TU("simSeasonEndV147",1) / TU("simFinishV147",1)). A real cut still stops it — the club
   * screen says the sim is paused and after how many games, and signing picks the sim back up
   * (TU("simResumeV147",1)).
   *
   * STORY DECISIONS. From the UFF up (TU("storyProLevelV147",7), switch TU("storyProV147",1)) nothing
   * stops the season to ask: the season event (`Za` via the `event` view) is answered by
   * `pickEventV147` — the option with the best safe return, never a locked one and never one that
   * spends PP; Rivalry Week is booked for game week and decided there off screen (`rivalResolveV136`
   * in `ca()`; the pregame page is off — `rivalPageV136`); UFF life events are answered the moment
   * `wi` queues them (`pickLifeV147`: the best family / fulfillment / recovery return he can afford
   * out of TU("lifeSpendShareV147",.35) of his cash); a queued midseason crossroads is dropped
   * (`v135MountD`); and a leftover v11 story stage is rolled the v90 way. Every answer is written on
   * the player (`autoDecisionsV147`) and toasted. Lower levels are unchanged. `window.__V147A`;
   * scripts/v147Acheck.mjs. ===== */
  function proV147(e) {
    return !!(e && (e.level || 0) >= TU("storyProLevelV147", 7) && TU("storyProV147", 1));
  }
  function noteV147(e, kind, title, choice) {
    const r = {
      kind,
      title: String(title || ""),
      choice: String(choice || ""),
      season: (e && e.totalSeasons) || 0,
      week: e && e.weekResults ? e.weekResults.filter(w => w && w.played).length : 0
    };
    try {
      if (e) {
        (e.autoDecisionsV147 = e.autoDecisionsV147 || []).push(r);
        e.autoDecisionsV147.length > 30 && e.autoDecisionsV147.splice(0, e.autoDecisionsV147.length - 30);
      }
    } catch (_) {}
    try {
      window.__V147A.log.push(r);
    } catch (_) {}
    return r;
  }
  /* the season event: the best safe return among the options he can actually take */
  function eventScoreV147(e, c) {
    const X = (c && c.eff) || {};
    if (X.ppCost) return null;
    let lk = null;
    try {
      lk = rivalLockV128(X, e);
    } catch (_) {}
    if (lk && !lk.ok) return null;
    const A = e.attrs || {};
    if (X.awareReqBase && (A.awareness || 0) < eventReq(X.awareReqBase, e.level)) return null;
    if (X.gritReqBase && (A.grit || 0) < eventReq(X.gritReqBase, e.level)) return null;
    const fg = X.focusGainBase ? Object.values(X.focusGainBase).reduce((s, v) => s + (+v || 0), 0) / 3 : 0,
      gain = (X.awareGainBase || 0) + (X.gritGainBase || 0) + (X.disciplineGainBase || 0) + fg;
    return (
      (X.perf || 0) +
      (X.growth || 0) * 40 +
      gain * 1.5 -
      (X.inj || 0) * 60 -
      (X.bench || 0) * 40 -
      (X.restWeeks || 0) * 3 +
      (X.healFatigue || 0) * 0.1 -
      ((X.varMult || 1) - 1) * 4
    );
  }
  function pickEventV147(e, ev) {
    let bi = -1,
      bs = -1e9;
    ((ev && ev.choices) || []).forEach((c, i) => {
      const s = eventScoreV147(e, c);
      if (s != null && s > bs) {
        bs = s;
        bi = i;
      }
    });
    return bi;
  }
  function autoEventV147(e) {
    const ev = SEASON_EVENTS.find(z => z.id === e.pendingEvent);
    if (!ev) return !1;
    if (ev.id === "bigGame" && TU("rivalSpinOnWeekV136", 1)) {
      noteV147(e, "event", ev.title, "the approach is decided on game week");
      rivalDeferV136();
      return state.view !== "event";
    }
    const i = pickEventV147(e, ev);
    if (i < 0) {
      noteV147(e, "event", ev.title, "passed");
      e.pendingEvent = null;
      goView("sim");
      return !0;
    }
    noteV147(e, "event", ev.title, ev.choices[i].label);
    chooseEvent(i);
    try {
      showToast(`📋 ${ev.title} — handled for you: ${ev.choices[i].label}`);
    } catch (_) {}
    return state.view !== "event";
  }
  /* UFF life: the best return he can afford */
  function pickLifeV147(e, ev) {
    const a = ensureFinanceState(e),
      cash = a ? a.cash || 0 : 0,
      cs = (ev && ev.choices) || [];
    if (!cs.length) return null;
    const cost = c => Math.max(0, -(c.cash || 0)) + (c.amount || 0),
      room = cash * TU("lifeSpendShareV147", 0.35),
      sc = c =>
        (c.family || 0) +
        (c.fulfillment || 0) +
        (c.trust || 0) * 2 +
        (c.recovery || 0) * 1.5 -
        (c.fatigue || 0) * 1.5 -
        (c.mental || 0) * 0.5 -
        (c.media || 0) * 0.3 +
        Math.max(0, c.cash || 0) / 5e4;
    const ok = cs.filter(c => cost(c) <= room),
      pool = ok.length
        ? ok
        : cs
            .slice()
            .sort((a2, b2) => cost(a2) - cost(b2))
            .slice(0, 1);
    return pool.slice().sort((a2, b2) => sc(b2) - sc(a2))[0] || cs[0];
  }
  function autoLifeV147(e, w) {
    const out = [];
    if (!e) return out;
    let g = 0;
    for (;;) {
      const a = e.lifeV12,
        s = a && a.eventQueue && a.eventQueue[0];
      if (!s || g++ > 12) break;
      const c = pickLifeV147(e, s);
      if (!c) break;
      const r = resolveLifeEvent(e, c.id);
      if (!r) break;
      const lab = (r.choice && r.choice.label) || c.label || c.id,
        rec = {
          kind: "life",
          arc: s.title || s.id || "",
          stage: "",
          choice: lab,
          success: null,
          week: w ? w.week : null
        };
      out.push(rec);
      if (w) (w.autoRollsV90 = w.autoRollsV90 || []).push(rec);
      noteV147(e, "life", s.title || s.id, lab);
    }
    return out;
  }
  /* a v11 offer list the v146 B era never answers: it becomes the three-club screen */
  function migrateOffersV147(e) {
    try {
      const s = e && e.nflStateV11;
      if (
        !e ||
        !(e.level >= 7) ||
        e._settled ||
        !TU("v146B", 1) ||
        !TU("offersMigrateV147", 1) ||
        e.offersV146B ||
        e.cutOutV146B ||
        !s ||
        !s.offers ||
        !s.offers.length
      )
        return !1;
      openOffersV146B(e, "fa");
      noteV147(e, "offers", "old roster offers", "moved to the club screen");
      window.__V147A.migrated++;
      saveGame();
      return !0;
    } catch (_) {
      return !1;
    }
  }
  /* Sim Remaining at 7+: the regular season, the playoffs, and the report card */
  function simSeasonV147(e) {
    migrateOffersV147(e);
    let n = 0,
      po = 0,
      why = null,
      g = 0;
    const rolls = [];
    for (; g++ < 60;) {
      if (e.cutOutV146B) {
        why = "out";
        break;
      }
      if (e.offersV146B) {
        why = "club";
        break;
      }
      try {
        ensurePlayoffs(e);
      } catch (_) {}
      const w = (e.weekResults || []).find(z => !z.played);
      if (!w) break;
      rolls.push(...autoStoryV90(e, w), ...autoLifeV147(e, w));
      if (!silentWeekV85(e, w)) {
        if (e.offersV146B || e.cutOutV146B) continue;
        why = "stuck";
        break;
      }
      n++;
      w.playoff && po++;
      processWeek95(w, e);
    }
    try {
      ensurePlayoffs(e);
    } catch (_) {}
    rolls.push(...autoStoryV90(e, null), ...autoLifeV147(e, null));
    try {
      window.__V90.last = rolls;
    } catch (_) {}
    const left = (e.weekResults || []).filter(z => !z.played).length,
      err = (window.__V85 && window.__V85.lastSilentError) || null;
    window.__V147A.lastSim = { n, po, why, left, err: why === "stuck" ? err : null };
    saveGame();
    const say = `⏭ Simmed ${n} game${n === 1 ? "" : "s"}${po ? ` (${po} in the playoffs)` : ""}${rolls.length ? ` · ${rolls.length} decision${rolls.length === 1 ? "" : "s"} made for you` : ""}`;
    if (why === "club" && e.offersV146B) {
      e.offersV146B.simV147 = { n, left };
      goView("season");
      try {
        showToast(
          `✂️ Released after ${n} simmed game${n === 1 ? "" : "s"} — pick a club and the rest of the season sims on`
        );
      } catch (_) {}
      return;
    }
    if (!why && !left && (!e.playoffState || e.playoffState.done) && TU("simFinishV147", 1)) {
      try {
        showToast(say + " — the season is done");
      } catch (_) {}
      (window.finishSeasonGames || finishSeasonGames)();
      return;
    }
    goView("season");
    try {
      showToast(
        why === "stuck" ? `⏸ The sim stopped after ${n} game${n === 1 ? "" : "s"} — play the next week to see why` : say
      );
    } catch (_) {}
  }
  /* walking away: the life screen's own exit, reachable */
  function retireNowV147() {
    const e = state && state.player;
    if (!e || e._settled) return;
    if (!canRetire(e)) {
      showToast("You are not ready to retire yet.");
      return;
    }
    const had = e.offersV146B;
    return Promise.resolve(retireV12()).then(done => {
      if (done && state.view === "gameover" && had) e.offersV146B = null;
    });
  }
  function postV147() {
    const e = state && state.player,
      d = byId("dock"),
      v = state && state.view;
    if (!e || !d || e._settled || !(e.level >= 7)) return;
    if (v === "season" && TU("simSeasonEndV147", 1) && e.weekResults && e.weekResults.some(w => !w.played)) {
      const lbl = "⏭ Sim the Rest of the Season";
      let b = d.querySelector('[onclick^="simRemainingWeeks"]');
      if (!b) {
        const qp = [...d.querySelectorAll("button")].find(z =>
          /playWeek\(false\)/.test(z.getAttribute("onclick") || "")
        );
        qp &&
          qp.insertAdjacentHTML("afterend", `<button class="btn ghost" onclick="simRemainingWeeks()">${lbl}</button>`);
      } else if (b.textContent !== lbl) b.textContent = lbl;
    }
    if (v === "hub" && TU("retireAnyTimeV147", 1) && !d.querySelector(".retire-chip-v147"))
      d.insertAdjacentHTML(
        "beforeend",
        `<button class="btn ghost retire-chip-v147" onclick="showLifeV12()">🌅 Life & Retire</button>`
      );
    if (v === "club") {
      const O = e.offersV146B,
        sc = byId("screen"),
        box = sc && sc.querySelector(".club-v146b");
      if (O && O.simV147 && box && !box.querySelector(".sim-note-v147"))
        box.insertAdjacentHTML(
          "afterbegin",
          `<div class="small sim-note-v147" style="color:var(--gold)">⏸ The sim is paused after ${O.simV147.n} game${O.simV147.n === 1 ? "" : "s"} (${O.simV147.left} left). Sign, and the rest of the season sims on by itself.</div>`
        );
      if (TU("retireAnyTimeV147", 1) && !d.querySelector(".retire-chip-v147"))
        d.insertAdjacentHTML(
          "beforeend",
          `<button class="btn ghost retire-chip-v147" onclick="retireNowV147()">🌅 Retire Instead</button>`
        );
    }
  }
  const qs0V147 = canRetire;
  canRetire = function (e) {
    return !!(qs0V147(e) || (e && e.level >= 7 && TU("retireAnyTimeV147", 1) && ensureFinanceState(e)));
  };
  const St0V147 = screenLife;
  screenLife = function () {
    const r = St0V147.apply(this, arguments);
    try {
      const e = state && state.player,
        d = byId("dock");
      if (state.view === "life" && e && !e._settled && d && canRetire(e) && !d.querySelector(".retire-chip-v147"))
        d.insertAdjacentHTML(
          "beforeend",
          `<button class="btn ghost retire-chip-v147" onclick="retireNowV147()">🌅 Retire From Football</button>`
        );
    } catch (_) {}
    return r;
  };
  const wi0V147 = maybeQueueLifeEvent;
  maybeQueueLifeEvent = function (e, t) {
    const r = wi0V147.apply(this, arguments);
    try {
      if (r && proV147(e)) {
        const out = autoLifeV147(e, t);
        out.length &&
          !window.__silentSimV85 &&
          showToast(`🤲 ${r.title || "Life"} — handled for you: ${out[0].choice}`);
      }
    } catch (_) {}
    return r;
  };
  const wt0V147 = simRemainingWeeks;
  simRemainingWeeks = function () {
    const e = state && state.player;
    if (e && e.weekResults && e.level >= 7 && TU("simSeasonEndV147", 1)) return simSeasonV147(e);
    return wt0V147.apply(this, arguments);
  };
  window.simRemainingWeeks = simRemainingWeeks;
  const sign0V147 = signClubV146B;
  signClubV146B = function (i) {
    const e = state && state.player,
      O = e && e.offersV146B,
      sim = O && O.simV147,
      r = sign0V147.apply(this, arguments);
    try {
      const p = state && state.player;
      if (r && sim && TU("simResumeV147", 1) && p && p.weekResults && p.weekResults.some(w => !w.played)) {
        window.__V147A.resumed++;
        setTimeout(
          () => {
            try {
              state.player === p && !p.offersV146B && simRemainingWeeks();
            } catch (_) {}
          },
          TU("simResumeMsV147", 450)
        );
      }
    } catch (_) {}
    return r;
  };
  window.signClubV146B = signClubV146B;
  let busyV147 = 0;
  const q0V147 = render;
  render = function () {
    try {
      const e = state && state.player;
      if (e && !e._settled && e.level >= 7) {
        migrateOffersV147(e);
        if (proV147(e)) {
          (e.storyDecisionQueueV11 || []).length && autoStoryV90(e, null);
          e.lifeV12 && e.lifeV12.eventQueue && e.lifeV12.eventQueue.length && autoLifeV147(e, null);
          if (state.view === "event" && e.pendingEvent && !busyV147) {
            busyV147 = 1;
            let done = !1;
            try {
              done = autoEventV147(e);
            } finally {
              busyV147 = 0;
            }
            if (done) return;
          }
        }
      }
    } catch (_) {}
    const r = q0V147.apply(this, arguments);
    try {
      postV147();
    } catch (_) {}
    return r;
  };
  window.retireNowV147 = retireNowV147;
  window.__V147A = {
    log: [],
    migrated: 0,
    resumed: 0,
    lastSim: null,
    pro: proV147,
    note: noteV147,
    pickEvent: pickEventV147,
    eventScore: eventScoreV147,
    autoEvent: autoEventV147,
    pickLife: pickLifeV147,
    autoLife: autoLifeV147,
    migrate: migrateOffersV147,
    sim: () => simSeasonV147(state.player),
    retire: retireNowV147,
    canRetire: e => canRetire(e || state.player)
  };
  /* a save that stopped on a story week or on an old offer list is drawn again through the wrapper */
  safeBootV140(function () {
    const e = state && state.player;
    if (
      e &&
      !e._settled &&
      e.level >= 7 &&
      ((state.view === "event" && proV147(e)) ||
        (e.nflStateV11 && e.nflStateV11.offers && e.nflStateV11.offers.length && !e.offersV146B))
    )
      render();
  }, "v147 A: settle a story week or an old offer list");
  window.__prestigeNodesV137 = () => ({
    node: k => TREE_NODES[k],
    level: k => nodeLvl(k),
    price: k => (TREE_NODES[k] ? nodeCost(TREE_NODES[k]) : 0),
    open: k => (TREE_NODES[k] ? !!nodeUnlocked(TREE_NODES[k]) : !1),
    keys: () => Object.keys(TREE_NODES)
  });
  window.__prestigeRefreshV137 = () => {
    try {
      state.view === "shop" && screenPrestige();
    } catch (_) {}
    try {
      syncCounters();
    } catch (_) {}
  };
  window.__ribToast = m => {
    try {
      showToast(m);
    } catch (_) {}
  };
  /* v140: DECLARATIONS, not assignments. The boot restores the saved view and RENDERS it while this
   * block is still running its own top level — so a screen drawn at boot can only call what is
   * HOISTED. `ms` and `no` (the two career-end screens) call `vaultPayBtnV137` in their markup, and
   * it was a `window.x = …` assignment 600 lines below them: a save sitting on either screen threw
   * ReferenceError at boot, which aborted the REST of this block — `window.__GRIDIRON_AUDIT__`, the
   * patch layer, everything after it — and left the game dead on the splash, permanently, because
   * the save kept restoring that same view. Function declarations hoist; these cannot come too late
   * again. (The `window.` aliases stay: the buttons call them by name through `onclick`.) */
  function vaultBuy(key) {
    const n = TREE_NODES[key];
    if (!n) return;
    if (
      !window.__RIB_VAULT_BRIDGE ||
      !window.__RIB_VAULT ||
      nodeLvl(key) >= n.max ||
      !nodeUnlocked(n) ||
      state.pp < nodeCost(n)
    )
      return buyNode(key);
    window.__RIB_VAULT_BRIDGE.open({ key: key });
  }
  function openVaultV137(key) {
    if (!window.__RIB_VAULT_BRIDGE) {
      showToast("The vault is still opening…");
      return;
    }
    window.__RIB_VAULT_BRIDGE.open(key ? { key: key } : {});
  }
  function vaultPayBtnV137(e) {
    return window.__RIB_VAULT_BRIDGE && e && (e._vaultPayV137 || 0) > 0
      ? `<button class="btn secondary" onclick="vaultPayoutV137()">\u{1F3E6} Watch ${fmtInt(e._vaultPayV137)} PP land in the Vault</button><div style="height:8px"></div>`
      : "";
  }
  function vaultPayoutV137() {
    const e = state.player;
    if (!window.__RIB_VAULT_BRIDGE || !e || !(e._vaultPayV137 > 0)) return;
    window.__RIB_VAULT_BRIDGE.payout(e._vaultPayV137);
  }
  window.vaultBuy = vaultBuy;
  window.openVaultV137 = openVaultV137;
  window.vaultPayBtnV137 = vaultPayBtnV137;
  window.vaultPayoutV137 = vaultPayoutV137;
  window.__V137 = {
    nodes: () => window.__prestigeNodesV137(),
    pp: () => state.pp,
    banked: () => bankedV136(),
    tree: () => state.tree,
    buy: k => buyNode(k)
  };
  /* v146 C: Hand-Picked -- trust at birth, added after the coachStart sum (whose ai() soft limit would eat it) */ const kt146C =
    newPlayer;
  newPlayer = function () {
    const e = kt146C.apply(this, arguments);
    return (e && (e.coachTrust = clamp99((e.coachTrust || 0) + trustPlusV146(), 0, 100)), e);
  };
  window.__GRIDIRON_AUDIT__ = {
    getState: () => state,
    setState: e => {
      state = e;
    },
    completeChallengesV134: e => completeChallenges(e),
    materializeInjuryV134: (e, w) => materializeInjuryV18(e, w),
    freshState: freshState,
    newPlayer: newPlayer,
    suggestPositions: suggestPositions,
    genRivals: genRivals,
    playerOVR: playerOvr,
    devRating: devRating,
    recommendTraining: recommendTraining,
    simSeason: simSeason,
    startSeasonGames: startSeasonGames,
    buildSeasonSchedule: buildSeasonSchedule,
    ensurePlayoffs: ensurePlayoffs,
    resolveSequentialWeekV11: resolveWeekV11,
    processWeek95: processWeek95,
    advanceChance: advanceChance,
    maxSeasonsAllowed: maxSeasons,
    minSeasonsRequired: minSeasons,
    advance: advanceLevel,
    grantMilestone: grantMilestone,
    prestigeStarReward: prestigeStarReward,
    screenGameOver: screenGameOver,
    screenWin: screenWin,
    LEVELS: LEVELS,
    POSITIONS: POSITIONS,
    ATTRS: ATTR_KEYS,
    TRAINING: PROGRAMS,
    EVENTS: SEASON_EVENTS,
    TREE: TREE,
    TREE_NODES: TREE_NODES,
    nodeUnlocked: nodeUnlocked,
    nodeCost: nodeCost,
    nodeLvl: nodeLvl,
    chooseTier: chooseTier,
    choosePath: choosePath,
    V11: V11_MODULE,
    V12: V12_MODULE,
    ensureV11: ensureV11,
    ensureV12: ensureV12,
    rollSeasonMod: rollSeasonMod,
    genContracts: genContracts,
    depthChartCoreV11: depthChartCore,
    playoffRoundNames: playoffRoundNames,
    treeFx: treeFx,
    pathVal: pathVal,
    tierPPMult: tierPPMult,
    hasTrait: hasTrait,
    gearFx: gearFx,
    hofWings: hofWings,
    posMasteryCount: posMasteryCount,
    eraMult: eraMult,
    chaosEarnedMult: chaosEarnedMult
  };

  /* ===== GRIDIRON PATCH LAYER — runs inside the legacy scope ===== */
  /* Has lexical access to: o (state), sa (branches), ot (node registry), M (node lvl), yt (plans) */
  (function () {
    try {
      // ---------- 1. FATE ROLL DEFINITIONS ----------
      var FATE_PLANS = {
        recovery: { odds: 0.88, win: 4, lose: 0, label: "SAFE BET" },
        disciplined: { odds: 0.78, win: 6, lose: -2, label: "STEADY" },
        team: { odds: 0.7, win: 8, lose: -3, label: "SOLID" },
        feature: { odds: 0.55, win: 13, lose: -5, label: "BOLD" },
        explosive: { odds: 0.35, win: 22, lose: -8, label: "LONGSHOT" }
      };
      window.__FATE_PLANS = FATE_PLANS; // for tests
      function fateOdds(base) {
        return Math.min(0.97, base + nodeLvl("fateOdds") * 0.04);
      }
      window.__fateOdds = function (b) {
        return fateOdds(b);
      };

      // ---------- 2. NEW FATE-BRANCH PRESTIGE NODES ----------
      var newNodes = [
        {
          key: "fateOdds",
          icon: "🎲",
          name: "Weighted Coin",
          max: 5,
          cost: 3,
          mult: 1.5,
          desc: "+4% game-plan roll odds per level. Applies to every plan."
        },
        {
          key: "fateReroll",
          icon: "🎰",
          name: "Loaded Dice",
          max: 1,
          cost: 8,
          mult: 1,
          desc: "Roll every game plan TWICE and keep the better result.",
          req: { node: "fateOdds", lvl: 2 }
        },
        {
          key: "fateHedge",
          icon: "🛡️",
          name: "House Money",
          max: 1,
          cost: 6,
          mult: 1,
          desc: "A declined fate roll still grants 30% of the stat buff instead of nothing."
        },
        {
          key: "fateDestiny",
          icon: "✨",
          name: "Scripted Destiny",
          max: 1,
          cost: 12,
          mult: 1,
          desc: "Once per season, a failed roll is rewritten into a success.",
          req: { node: "fateReroll", lvl: 1 }
        },
        {
          key: "callRun",
          icon: "🏃",
          name: "Ground & Pound",
          max: 3,
          cost: 4,
          mult: 1.6,
          desc: "Your coaches call 7% more RUN plays per level. AI effect: the play-caller's pass/run dice shift toward the ground game in every game you play."
        },
        {
          key: "callPass",
          icon: "🎯",
          name: "Air Raid",
          max: 3,
          cost: 4,
          mult: 1.6,
          desc: "Your coaches call 7% more PASS plays per level. AI effect: the play-caller's pass/run dice shift toward the air in every game you play."
        },
        {
          key: "fieldGeneral",
          icon: "🧠",
          name: "Field General",
          max: 4,
          cost: 5,
          mult: 1.7,
          desc: "The staff listens to you. +18% per level to how much of your PREGAME game-plan suggestion the coach actually adopts — so scouting the opponent's weak unit and calling for more runs (or more air) really bends the play-mix."
        }
      ];
      if (TREE && TREE.fate && Array.isArray(TREE.fate.nodes)) {
        newNodes.forEach(function (n) {
          if (!TREE_NODES[n.key]) {
            TREE.fate.nodes.push(n);
            TREE_NODES[n.key] = Object.assign({}, n, { branch: "fate" });
          }
        });
      }

      // ---------- 3. WRAP chooseGamePlanV11 WITH THE FATE ROLL ----------
      function toast(msg) {
        var t = document.getElementById("toast");
        if (!t) return;
        t.textContent = msg;
        t.classList.add("show");
        clearTimeout(toast._t);
        toast._t = setTimeout(function () {
          t.classList.remove("show");
        }, 2600);
      }
      function banner(title, sub, good) {
        var b = document.getElementById("momentBanner");
        if (!b) return toast(title);
        b.innerHTML = title + "<small>" + sub + "</small>";
        b.style.borderColor = good ? "var(--gold)" : "rgba(224,72,79,.8)";
        b.classList.remove("go");
        void b.offsetWidth;
        b.classList.add("go");
        var f = document.getElementById("cinemaFlash");
        if (good && f) {
          f.classList.remove("go");
          void f.offsetWidth;
          f.classList.add("go");
        }
      }
      // fate now buffs a temporary ATTRIBUTE for the game (or declines) instead of a flat rating swing.
      // the target attribute is derived deterministically so the plan card and the actual roll agree.
      function fateWeekIdx() {
        var p = state && state.player;
        if (!p || !p.weekResults) return 0;
        var i = p.weekResults.findIndex(function (w) {
          return !w.played;
        });
        return i < 0 ? 0 : i;
      }
      function fateAttrFor(planId) {
        var p = state && state.player,
          def = FATE_PLANS[planId];
        var seed = seededRng((p && p.seasonSeed) || 1, fateWeekIdx(), planId, "fate-attr-v13");
        var pool = (p && POSITIONS[p.pos] && Object.keys(POSITIONS[p.pos].w)) || ATTR_KEYS;
        var attr = pool[Math.floor(seed() * pool.length)] || "speed";
        var amount = Math.max(2, Math.round((def ? def.win : 6) * 0.9));
        return {
          attr: attr,
          amount: amount,
          name: (ATTR_INFO[attr] && ATTR_INFO[attr].name) || attr,
          icon: (ATTR_INFO[attr] && ATTR_INFO[attr].icon) || "🎲"
        };
      }
      window.__fateAttrFor = fateAttrFor; // for tests
      function runFateRoll(planId) {
        var def = FATE_PLANS[planId];
        if (!def) return null;
        var pity = state && state._fateMiss >= 2 ? 0.12 : 0; // two straight misses tilt the table
        var odds = Math.min(0.97, fateOdds(def.odds) + pity);
        var rolls = nodeLvl("fateReroll") >= 1 || nodeLvl("oracle") >= 1 ? 2 : 1; // v134 Apex: the Oracle rolls the game plan twice as well
        var success = false;
        for (var i = 0; i < rolls; i++) {
          if (Math.random() < odds) success = true;
        }
        var destiny = false;
        if (!success && nodeLvl("fateDestiny") >= 1 && !(state && state._destinyUsed)) {
          success = true;
          destiny = true;
          if (state) state._destinyUsed = true;
        }
        var out = fateAttrFor(planId);
        // HIT: full temporary buff. DECLINED (miss): nothing, unless House Money grants a partial buff.
        var amount = success ? out.amount : nodeLvl("fateHedge") >= 1 ? Math.max(1, Math.round(out.amount * 0.3)) : 0;
        if (state) {
          state._fateMiss = success ? 0 : (state._fateMiss || 0) + 1;
          state._fateLast = amount > 0 ? "+" + amount + " " + out.name : "DECLINED";
        }
        return {
          success: success,
          destiny: destiny,
          odds: odds,
          attr: out.attr,
          amount: amount,
          name: out.name,
          icon: out.icon,
          applied: amount > 0,
          rolls: rolls,
          plan: planId,
          pity: pity
        };
      }
      window.__runFateRoll = runFateRoll; // for tests

      if (typeof window.chooseGamePlanV11 === "function") {
        var origChoose = window.chooseGamePlanV11;
        window.chooseGamePlanV11 = function (planId, arg2) {
          var res = null,
            applied = null,
            p = state && state.player;
          try {
            if (state) state._fateBoost = 0;
            // reset Scripted Destiny at the start of each season
            if (state && state.player && (state.player.week || 0) <= 1) state._destinyUsed = false;
            res = runFateRoll(planId);
            // apply the temporary attribute buff BEFORE the game is generated
            if (res && res.applied && p && p.attrs && p.attrs[res.attr] != null) {
              applied = { attr: res.attr, before: p.attrs[res.attr] };
              p.attrs[res.attr] = clamp99(p.attrs[res.attr] + res.amount, 1, attrCap());
            }
          } catch (e) {
            console.warn("[fate] roll error", e);
          }
          var out = origChoose.apply(this, arguments); // game is generated here, using the temporary buff
          try {
            // revert the buff — it only counted for this one game — then re-save the clean attrs
            if (applied && p && p.attrs) {
              p.attrs[applied.attr] = applied.before;
              if (typeof saveGame === "function") {
                try {
                  saveGame();
                } catch (e2) {}
              }
            }
            if (state) state._fateBoost = 0;
            // fate result popup removed by request: no box appears before the play (the buff still applies silently)
          } catch (e) {}
          return out;
        };
      }

      // ---------- 4. INJECT ODDS + EXACT EFFECTS INTO PLAN CARDS ----------
      function decoratePlans(root) {
        var btns = (root || document).querySelectorAll(".impact-plan-choice, .gameplan-choice");
        btns.forEach(function (btn) {
          if (btn.__fateDecorated) return;
          var oc = btn.getAttribute("onclick") || "";
          var m = oc.match(/chooseGamePlan\w*\([\x22'](\w+)[\x22']/);
          if (!m || !FATE_PLANS[m[1]]) return;
          btn.__fateDecorated = true;
          var def = FATE_PLANS[m[1]],
            odds = fateOdds(def.odds),
            pct = Math.round(odds * 100);
          var plan =
            typeof GAME_PLANS !== "undefined" && GAME_PLANS.find
              ? GAME_PLANS.find(function (p) {
                  return p.id === m[1];
                })
              : null;
          var fx = [];
          if (plan) {
            if (plan.perf) fx.push((plan.perf > 0 ? "+" : "") + plan.perf + " base rating");
            if (plan.snap) fx.push((plan.snap > 0 ? "+" : "") + Math.round(plan.snap * 100) + "% snaps");
            if (plan.varMult && plan.varMult !== 1) fx.push("×" + plan.varMult + " variance");
            if (plan.inj) fx.push((plan.inj > 0 ? "+" : "") + Math.round(plan.inj * 100) + "% injury");
            if (plan.trust) fx.push((plan.trust > 0 ? "+" : "") + plan.trust + " trust");
          }
          var oc2 = fateAttrFor(m[1]);
          var winTxt = "HIT → " + oc2.icon + " +" + oc2.amount + " " + oc2.name + " this game";
          var failTxt =
            nodeLvl("fateHedge") >= 1
              ? "MISS → partial +" + Math.max(1, Math.round(oc2.amount * 0.3)) + " " + oc2.name + " (House Money)"
              : "MISS → Fate declines · no change";
          var perks = [];
          if (nodeLvl("fateOdds")) perks.push("🎲+" + nodeLvl("fateOdds") * 4 + "%");
          if (nodeLvl("fateReroll")) perks.push("🎰 roll ×2");
          if (nodeLvl("fateDestiny") && !(state && state._destinyUsed)) perks.push("✨ destiny ready");
          if (state && state._fateMiss >= 2) perks.push("🔥 pity +12%");
          if (state && state._fateLast) perks.push("LAST: " + state._fateLast);
          var host = btn.querySelector(".gameplan-meta") || btn;
          var div = document.createElement("div");
          div.className = "fate-odds-row";
          div.innerHTML =
            '<span class="fo-pct" style="color:' +
            (pct >= 70 ? "#57e07a" : pct >= 50 ? "#f0bb45" : "#ff7b62") +
            '">🎲 ' +
            pct +
            "% · " +
            def.label +
            "</span>" +
            '<span class="fo-win">' +
            winTxt +
            "</span>" +
            '<span class="fo-lose">' +
            failTxt +
            "</span>" +
            (fx.length ? '<span class="fo-fx">Guaranteed: ' + fx.join(" · ") + "</span>" : "") +
            (perks.length ? '<span class="fo-perk">' + perks.join(" · ") + "</span>" : "");
          host.parentNode.insertBefore(div, host.nextSibling);
        });
      }
      new MutationObserver(function (muts) {
        for (var i = 0; i < muts.length; i++)
          if (muts[i].addedNodes.length) {
            decoratePlans(document);
            break;
          }
      }).observe(document.body, { childList: true, subtree: true });
      window.__decoratePlans = decoratePlans; // for tests

      // ---------- 5. TIGHT END BLOCKING STATS + OVR REBALANCE ----------
      try {
        // live-sim tiles: REC / TD / PANCAKES
        var origJi = liveStatCols;
        liveStatCols = function (pos) {
          if (pos === "TE")
            return [
              ["rec", "REC YDS"],
              ["td", "TD"],
              ["pancake", "PANCAKES"]
            ];
          return origJi(pos);
        };
        // box-score synthesis carries pancakes through for TE
        var origQi = qi;
        qi = function (e, t, a) {
          var r = origQi(e, t, a);
          if (e === "TE") r.pancake = (t && t.pancake) || 0;
          return r;
        };
        // full box score: add the blocking line for TE
        var origCl = liveBoxLine;
        liveBoxLine = function (e, t) {
          var rows = origCl(e, t);
          if (e === "TE") {
            rows.push(["PANCAKES", (t && t.pancake) || 0]);
            if (t && t.sackAllowed) rows.push(["SACKS ALL", t.sackAllowed]);
          }
          return rows;
        };
        // season stat lines: TE now logs pancakes across a season
        if (
          POS_STATS &&
          POS_STATS.TE &&
          POS_STATS.TE.stats &&
          !POS_STATS.TE.stats.some(function (s) {
            return s.key === "pancakes";
          })
        ) {
          POS_STATS.TE.stats.push({ key: "pancakes", name: "Pancakes", per: [2, 2.5, 3, 3.5, 4, 4.5, 5, 4] });
        }
        // OVR rebalance: blocking matters more, receiving slightly less — net weight unchanged
        if (POSITIONS && POSITIONS.TE && POSITIONS.TE.w) {
          var w = POSITIONS.TE.w;
          w.blocking = 0.21;
          w.strength = 0.16;
          w.catching = 0.13;
          w.speed = 0.07;
          w.jumping = 0.06;
        }
        window.__qcTE = {
          w: POSITIONS.TE.w,
          ji: liveStatCols("TE"),
          seasonKeys: POS_STATS.TE.stats.map(function (s) {
            return s.key;
          })
        };
        // buy haptics
        if (typeof window.buy === "function") {
          var origBuy = window.buy;
          window.buy = function () {
            try {
              navigator.vibrate && navigator.vibrate(12);
            } catch (e) {}
            return origBuy.apply(this, arguments);
          };
        }
      } catch (e) {
        console.warn("[TE patch]", e);
      }
    } catch (e) {
      console.warn("[gridiron patch layer]", e);
    }
  })();

  /* ===== POST-GAME STATS + SEASON TOTALS + NATIONAL RANK (v13) ===== */
  (function () {
    try {
      if (typeof finishWeekGame !== "function") return;
      var MAXKEYS = { longest: 1 }; // some box-score keys are a season best, not a sum
      // v17: expose the box-score grader to the (top-level) AI season engine so every
      // simmed game — watched or quick — grades its emergent stat line the same way.
      try {
        window.__gradeGame = gradeGame;
        window.__pgSeasonAvg = pgSeasonAvg;
      } catch (e) {}

      function pgSeasonAvg(p) {
        var w = (p.weekResults || []).filter(function (x) {
          return x.played && x.perf != null;
        });
        if (!w.length) return p.perf || 50;
        return (
          w.reduce(function (s, x) {
            return s + x.perf;
          }, 0) / w.length
        );
      }
      function pgAccumulate(p, gameStat) {
        if (p._pgSeed !== p.seasonSeed) {
          p._pgSeed = p.seasonSeed;
          p._pgSeasonBox = {};
          p._pgGames = 0;
          p._pgRank = null;
        }
        var box = p._pgSeasonBox || (p._pgSeasonBox = {});
        Object.keys(gameStat || {}).forEach(function (k) {
          var v = Number(gameStat[k]) || 0;
          if (MAXKEYS[k]) box[k] = Math.max(box[k] || 0, v);
          else box[k] = (box[k] || 0) + v;
        });
        p._pgGames = (p._pgGames || 0) + 1;
        return box;
      }
      function pgRankLine(p) {
        var avg = pgSeasonAvg(p);
        var r = typeof nationalRank === "function" ? nationalRank(p, avg) : { rank: 0, of: 0 };
        var prev = p._pgRank;
        var delta = prev ? prev - r.rank : 0; // positive = climbed the board
        p._pgRank = r.rank;
        var arrow = !prev
          ? "NEW"
          : delta > 0
            ? "\u25B2 " + delta.toLocaleString()
            : delta < 0
              ? "\u25BC " + Math.abs(delta).toLocaleString()
              : "\u2014 even";
        var col = !prev ? "var(--gold)" : delta > 0 ? "var(--good)" : delta < 0 ? "#e08a8a" : "var(--chalk-dim)";
        return { rank: r.rank, of: r.of, arrow: arrow, col: col };
      }
      function pgGrid(pos, statObj, gameObj) {
        var rows = typeof liveBoxLine === "function" ? liveBoxLine(pos, statObj) : [];
        var gmap = null;
        if (gameObj) {
          gmap = {};
          (liveBoxLine(pos, gameObj) || []).forEach(function (r) {
            gmap[r[0]] = r[1];
          });
        }
        var skip = /AVG|C\/ATT|LONG/i; // derived / best-of rows get no "+n" delta
        return rows
          .map(function (r) {
            var label = r[0],
              val = r[1],
              plus = "";
            if (gmap && !skip.test(label)) {
              var gv = parseFloat(gmap[label]);
              if (!isNaN(gv) && gv > 0)
                plus =
                  '<span style="color:var(--good);font-size:11px;margin-left:3px;font-family:Oswald;font-weight:700">+' +
                  Math.round(gv) +
                  "</span>";
            }
            return (
              '<div class="fb-item"><span class="fb-l">' +
              label +
              '</span><span class="fb-v">' +
              val +
              plus +
              "</span></div>"
            );
          })
          .join("");
      }
      // ===== Position-aware game grade (0-100) with a transparent breakdown =====
      // The visible box score (o._liveGame.stat) is graded directly, so the number
      // always maps to what happened on the field. Efficiency (yards per attempt,
      // per carry, per target) and splash plays (TD/INT/sack/FF) dominate; raw
      // volume matters little. A big play on a handful of snaps grades high; heavy
      // usage with nothing to show for it grades low. Every rubric is per-position.
      function ggLetter(s) {
        return s >= 93
          ? "A+"
          : s >= 86
            ? "A"
            : s >= 79
              ? "A-"
              : s >= 72
                ? "B+"
                : s >= 64
                  ? "B"
                  : s >= 56
                    ? "C+"
                    : s >= 48
                      ? "C"
                      : s >= 39
                        ? "D"
                        : "F";
      }
      function ggColor(s) {
        return s >= 72 ? "var(--good)" : s >= 56 ? "var(--gold)" : s >= 42 ? "#c9a13b" : "var(--blood)";
      }
      function ggSummary(s) {
        return s >= 90
          ? "Dominant performance"
          : s >= 79
            ? "Excellent game"
            : s >= 64
              ? "Strong outing"
              : s >= 52
                ? "Solid contribution"
                : s >= 42
                  ? "Quiet day"
                  : "Rough game";
      }
      function gradeGame(pos, st, snaps) {
        st = st || {};
        snaps = Number(snaps) || 0;
        var N = function (k) {
          return Number(st[k]) || 0;
        };
        var lines = [],
          base = 50,
          bigPlay = false;
        var add = function (label, detail, pts) {
          pts = Math.round(pts || 0);
          lines.push({ l: label, d: detail, p: pts });
        };
        var cap = function (v, hi) {
          return Math.min(hi, v);
        };
        if (pos === "QB") {
          var att = N("att"),
            cmp = N("comp"),
            yds = N("pass"),
            td = N("td"),
            ints = N("int"),
            rush = N("rush"),
            lng = N("longest");
          var ypa = att > 0 ? yds / att : 0;
          var e =
            ypa >= 9
              ? [24, "elite"]
              : ypa >= 7.5
                ? [15, "strong"]
                : ypa >= 6
                  ? [6, "steady"]
                  : att > 0
                    ? ypa >= 4.5
                      ? [-2, "below average"]
                      : [-13, "poor"]
                    : [0, "no attempts"];
          add("Yards / attempt", (att > 0 ? Math.round(ypa) : 0) + " yd · " + e[1], e[0]);
          if (td) {
            add("Passing TDs", td + " thrown", cap(td * 12, 34));
            bigPlay = true;
          }
          if (ints) add("Interceptions", ints + " thrown", -ints * 15);
          if (att >= 5) {
            var cp = cmp / att;
            var c = cp >= 0.7 ? [8, "sharp"] : cp >= 0.6 ? [4, "accurate"] : cp >= 0.45 ? [0, ""] : [-7, "erratic"];
            add("Completion %", Math.round(cp * 100) + "%" + (c[1] ? " · " + c[1] : ""), c[0]);
          }
          if (rush >= 8) add("Scramble yards", rush + " yd on the ground", cap(Math.round(rush / 6), 8));
          if (lng >= 30) add("Long strike", lng + " yd completion", 5);
        } else if (pos === "RB") {
          var car = N("carries"),
            ry = N("rush"),
            td = N("td"),
            fum = N("fum"),
            rec = N("rec"),
            lng = N("longest");
          var ypc = car > 0 ? ry / car : 0;
          var e =
            ypc >= 6
              ? [26, "elite"]
              : ypc >= 5
                ? [16, "explosive"]
                : ypc >= 4.3
                  ? [8, "efficient"]
                  : car > 0
                    ? ypc >= 3.5
                      ? [1, "grinding"]
                      : [-10, "bottled up"]
                    : [0, "no carries"];
          add("Yards / carry", (car > 0 ? Math.round(ypc) : 0) + " yd · " + e[1], e[0]);
          if (ry) add("Rushing volume", ry + " yd on " + car + " car", cap(Math.round(ry / 12), 12));
          if (td) {
            add("Touchdowns", td + " scored", cap(td * 14, 32));
            bigPlay = true;
          }
          if (lng >= 20) {
            add("Breakaway run", lng + " yd", 7);
            bigPlay = true;
          }
          if (rec >= 10) add("Receiving yards", rec + " yd catching", cap(Math.round(rec / 10), 9));
          if (fum) add("Fumbles lost", fum + " turnover" + (fum > 1 ? "s" : ""), -fum * 16);
        } else if (pos === "WR" || pos === "TE") {
          var tgt = N("tgt"),
            rc = N("rec_c"),
            ry = N("rec"),
            td = N("td"),
            lng = N("longest");
          var ypt = tgt > 0 ? ry / tgt : 0;
          var e =
            ypt >= 12
              ? [23, "elite"]
              : ypt >= 9
                ? [14, "dangerous"]
                : ypt >= 6
                  ? [6, "productive"]
                  : tgt > 0
                    ? [-6, "contained"]
                    : [0, "not targeted"];
          add("Yards / target", (tgt > 0 ? Math.round(ypt) : 0) + " yd · " + e[1], e[0]);
          if (tgt >= 2) {
            var cr = rc / tgt;
            var c = cr >= 0.75 ? [8, "sure hands"] : cr >= 0.55 ? [3, ""] : [-7, "drops"];
            add("Catch rate", rc + "/" + tgt + (c[1] ? " · " + c[1] : ""), c[0]);
          }
          if (ry) add("Receiving yards", ry + " yd on " + rc + " rec", cap(Math.round(ry / 12), 12));
          if (td) {
            add("Touchdowns", td + " scored", cap(td * 14, 32));
            bigPlay = true;
          }
          if (lng >= 25) {
            add("Explosive catch", lng + " yd", 6);
            bigPlay = true;
          }
        } else if (pos === "OL") {
          base = 52;
          var pan = N("pancake"),
            sa = N("sackAllowed");
          if (pan) add("Pancake blocks", pan + " defender" + (pan > 1 ? "s" : "") + " flattened", cap(pan * 7, 28));
          if (sa) add("Sacks allowed", sa + " surrendered", -sa * 12);
          else add("Pass protection", "clean sheet · 0 sacks allowed", 15);
        } else if (pos === "DL") {
          var tkl = N("tackle"),
            sk = N("sack"),
            tfl = N("tfl"),
            qh = N("qbhit"),
            ff = N("ff");
          if (sk) {
            add("Sacks", sk + " on the QB", cap(sk * 18, 40));
            bigPlay = true;
          }
          if (tfl) add("Tackles for loss", tfl + " behind the line", cap(tfl * 10, 24));
          if (tkl) add("Tackles", tkl + " stop" + (tkl > 1 ? "s" : ""), cap(tkl * 3, 18));
          if (qh) add("QB hits", qh + " pressure" + (qh > 1 ? "s" : ""), cap(qh * 5, 15));
          if (ff) {
            add("Forced fumbles", ff + " pried loose", ff * 16);
            bigPlay = true;
          }
          if (!sk && !tfl && !tkl && !qh && !ff) add("Quiet front", "no disruptive plays", -10);
        } else if (pos === "LB") {
          var tkl = N("tackle"),
            sk = N("sack"),
            tfl = N("tfl"),
            ints = N("int"),
            ff = N("ff");
          if (tkl) add("Tackles", tkl + " stop" + (tkl > 1 ? "s" : ""), cap(tkl * 4, 28));
          if (sk) {
            add("Sacks", sk + " on the QB", cap(sk * 14, 28));
            bigPlay = true;
          }
          if (tfl) add("Tackles for loss", tfl + " behind the line", cap(tfl * 9, 22));
          if (ints) {
            add("Interceptions", ints + " pick" + (ints > 1 ? "s" : ""), ints * 26);
            bigPlay = true;
          }
          if (ff) {
            add("Forced fumbles", ff + " pried loose", ff * 16);
            bigPlay = true;
          }
          if (!tkl && !sk && !tfl && !ints && !ff) add("Quiet game", "no impact plays", -10);
        } else if (pos === "CB") {
          var tkl = N("tackle"),
            pd = N("pd"),
            ints = N("int"),
            tgt = N("tgt");
          var allowed = Math.max(0, tgt - pd - ints);
          if (ints) {
            add("Interceptions", ints + " pick" + (ints > 1 ? "s" : ""), ints * 30);
            bigPlay = true;
          }
          if (pd) add("Pass breakups", pd + " contested away", cap(pd * 14, 28));
          if (tgt >= 3 && allowed === 0 && pd + ints > 0)
            add("Lockdown coverage", "0 catches on " + tgt + " targets", 12);
          if (allowed)
            add(
              "Coverage given up",
              allowed + " catch" + (allowed > 1 ? "es" : "") + " allowed",
              -cap(allowed * 7, 26)
            );
          if (tkl) add("Tackles", tkl + " stop" + (tkl > 1 ? "s" : ""), cap(tkl * 3, 12));
          if (!ints && !pd && !allowed && !tkl) add("Not tested", "few plays your way", -4);
        } else if (pos === "S") {
          var tkl = N("tackle"),
            pd = N("pd"),
            ints = N("int"),
            tfl = N("tfl");
          if (ints) {
            add("Interceptions", ints + " pick" + (ints > 1 ? "s" : ""), ints * 30);
            bigPlay = true;
          }
          if (pd) add("Pass breakups", pd + " contested away", cap(pd * 13, 26));
          if (tfl) add("Tackles for loss", tfl + " behind the line", cap(tfl * 10, 20));
          if (tkl) add("Tackles", tkl + " stop" + (tkl > 1 ? "s" : ""), cap(Math.round(tkl * 3.5), 18));
          if (!ints && !pd && !tfl && !tkl) add("Quiet game", "no impact plays", -8);
        } else {
          var gtkl = N("tackle"),
            gtd = N("td");
          if (gtkl) add("Tackles", gtkl + " stop" + (gtkl > 1 ? "s" : ""), cap(gtkl * 4, 24));
          if (gtd) {
            add("Touchdowns", gtd + " scored", gtd * 14);
            bigPlay = true;
          }
        }
        // v17 pick sixes — the highest-value defensive play there is
        if (["LB", "CB", "S", "DL"].indexOf(pos) >= 0 && N("pick6")) {
          add("Pick sixes", N("pick6") + " returned to the house", N("pick6") * 32);
          bigPlay = true;
        }
        var sum0 = lines.reduce(function (a, x) {
          return a + x.p;
        }, 0);
        if (snaps > 0) {
          if (snaps <= 14 && bigPlay && sum0 > 0) add("Snap efficiency", "made it count on " + snaps + " snaps", 8);
          else if (snaps >= 26 && sum0 < 8) add("Limited impact", "quiet across " + snaps + " snaps", -6);
        }
        var sum = lines.reduce(function (a, x) {
          return a + x.p;
        }, 0);
        var score = Math.max(2, Math.min(100, Math.round(base + sum)));
        return {
          score: score,
          grade: ggLetter(score),
          color: ggColor(score),
          summary: ggSummary(score),
          lines: lines,
          snaps: snaps,
          base: base
        };
      }
      function gradeCardHtml(pos, grade) {
        // Rows start hidden (opacity 0) but still occupy space, so the card's height is
        // final from the outset — the reveal animation never reflows or shifts Continue.
        var rows = grade.lines
          .map(function (L, i) {
            var c = L.p > 0 ? "var(--good)" : L.p < 0 ? "#e08a8a" : "var(--chalk-dim)";
            var v = L.p > 0 ? "+" + L.p : L.p < 0 ? String(L.p) : "—";
            return (
              '<div class="pg-row" data-p="' +
              L.p +
              '" style="opacity:0;transform:translateY(5px);display:flex;align-items:baseline;gap:8px;padding:2.5px 0;border-bottom:1px solid rgba(255,255,255,.05)">' +
              '<span style="font-family:Barlow Condensed;font-size:13.5px;color:var(--chalk);min-width:104px">' +
              L.l +
              "</span>" +
              '<span style="font-family:Barlow Condensed;font-size:11.5px;color:var(--chalk-dim);flex:1">' +
              L.d +
              "</span>" +
              '<span class="pg-val" style="font-family:Oswald;font-weight:700;font-size:14px;color:' +
              c +
              ';transform-origin:right center">' +
              v +
              "</span>" +
              "</div>"
            );
          })
          .join("");
        var snapLine = grade.snaps > 0 ? " · " + grade.snaps + " snaps" : "";
        // Initialise on the baseline; animateGradeCard() tallies each line into it.
        var b = grade.base;
        return (
          '<div id="pgGradeLabel" class="stat-sec-label" style="margin-top:11px;color:' +
          ggColor(b) +
          ';transition:color .3s ease">GAME GRADE · ' +
          pos +
          "</div>" +
          '<div style="display:flex;align-items:center;gap:12px;padding:7px 11px;border-radius:11px;background:#00000038;border:1px solid rgba(255,255,255,.08);margin-bottom:6px">' +
          '<div style="font-family:Oswald;font-weight:700;font-size:36px;line-height:.8;transition:color .3s ease"><span id="pgScore" style="color:' +
          ggColor(b) +
          ';display:inline-block;transform-origin:left center">' +
          b +
          "</span>" +
          '<span style="font-size:13px;color:var(--chalk-dim);font-weight:600">/100</span></div>' +
          '<div style="min-width:0">' +
          '<div style="font-family:Oswald;font-weight:700;font-size:18px;line-height:1.1"><span id="pgLetter" style="color:' +
          ggColor(b) +
          ';transition:color .3s ease">' +
          ggLetter(b) +
          "</span>" +
          ' <span id="pgSummary" style="font-size:12.5px;color:var(--chalk-dim);font-weight:600;letter-spacing:.3px">' +
          ggSummary(b) +
          snapLine +
          "</span></div>" +
          "</div>" +
          "</div>" +
          '<div id="pgBreakdown" style="margin-bottom:2px">' +
          rows +
          "</div>"
        );
      }
      function pgApplyScore(val, snapLine) {
        var s = Math.max(2, Math.min(100, Math.round(val))),
          col = ggColor(s);
        var scoreEl = document.getElementById("pgScore"),
          letterEl = document.getElementById("pgLetter"),
          sumEl = document.getElementById("pgSummary"),
          labelEl = document.getElementById("pgGradeLabel");
        if (scoreEl) {
          scoreEl.textContent = s;
          scoreEl.style.color = col;
        }
        if (letterEl) {
          letterEl.textContent = ggLetter(s);
          letterEl.style.color = col;
        }
        if (sumEl) {
          sumEl.textContent = ggSummary(s) + (snapLine || "");
        }
        if (labelEl) {
          labelEl.style.color = col;
        }
      }
      // Reveal each scoring factor in turn and count the big number up (or down) to the
      // final grade — "line by line, tally up, watch the number move."
      function animateGradeCard(grade) {
        var overlay = document.getElementById("pgOverlayV13");
        if (!overlay) return;
        var rows = [].slice.call(overlay.querySelectorAll(".pg-row"));
        var snapLine = grade.snaps > 0 ? " · " + grade.snaps + " snaps" : "";
        var reduce =
          (typeof REDUCED_MOTION !== "undefined" && REDUCED_MOTION) ||
          (window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches);
        if (reduce || !rows.length) {
          pgApplyScore(grade.score, snapLine);
          rows.forEach(function (r) {
            r.style.opacity = 1;
            r.style.transform = "none";
          });
          return;
        }
        pgApplyScore(grade.base, snapLine);
        var i = 0,
          running = grade.base;
        function pulseScore() {
          var el = document.getElementById("pgScore");
          if (!el) return;
          el.style.transition = "transform .12s ease";
          el.style.transform = "scale(1.16)";
          setTimeout(function () {
            if (el) el.style.transform = "scale(1)";
          }, 120);
        }
        function step() {
          if (i >= rows.length) {
            pgApplyScore(grade.score, snapLine);
            pulseScore();
            return;
          }
          var row = rows[i],
            p = Number(row.getAttribute("data-p")) || 0;
          row.style.transition = "opacity .26s ease, transform .26s ease";
          row.style.opacity = 1;
          row.style.transform = "none";
          var val = row.querySelector(".pg-val");
          if (val) {
            val.style.transition = "transform .16s ease";
            val.style.transform = "scale(1.4)";
            setTimeout(function () {
              if (val) val.style.transform = "scale(1)";
            }, 150);
          }
          var from = running,
            to = running + p,
            t0 = null,
            dur = 360;
          function tick(ts) {
            if (t0 === null) t0 = ts;
            var k = Math.min(1, (ts - t0) / dur),
              e = 1 - Math.pow(1 - k, 2);
            pgApplyScore(from + (to - from) * e, snapLine);
            if (k < 1) requestAnimationFrame(tick);
            else {
              running = to;
              pulseScore();
              i++;
              setTimeout(step, 190);
            }
          }
          requestAnimationFrame(tick);
        }
        setTimeout(step, 300);
      }
      function showPostGame(cont) {
        var p = state && state.player,
          g = state && state._liveGame;
        if (!p || !p.pos || !g || !g.stat) return false;
        var wr = p.weekResults && p.currentWeek != null ? p.weekResults[p.currentWeek] : null;
        var counted = !!(wr && wr._pgCounted);
        // Grade THIS game from its box score and REPLACE the position-blind perf so the
        // season average, national rank, and schedule rating all use the explainable number.
        var snaps = Number(wr && wr.snaps) || Number(g.snaps) || 0;
        var grade = gradeGame(p.pos, g.stat, snaps);
        if (wr) {
          wr.perf = grade.score;
          wr._gameGrade = grade.grade;
        }
        g.perf = grade.score;
        var seasonBox = counted ? p._pgSeasonBox || {} : pgAccumulate(p, g.stat);
        var rank = counted && p._pgRankCache ? p._pgRankCache : pgRankLine(p);
        if (wr) {
          wr._pgCounted = true;
          p._pgRankCache = rank;
        }
        var us = g.usScore != null ? g.usScore : 0,
          them = g.themScore != null ? g.themScore : 0;
        // How many games this season line covers, and the record. THIS game is not on
        // weekResults yet — the week is finalised only after the card is dismissed — so
        // its result comes off the live scoreboard, or the card shows a win as 0-1.
        var played = 0,
          won = 0;
        try {
          (p.weekResults || []).forEach(function (w) {
            if (w && w.played && !w.playoff) {
              played++;
              if (w.won) won++;
            }
          });
          if (!wr || !wr.played) {
            played++;
            if (us > them) won++;
          }
        } catch (e) {}
        var seasonThru = played
          ? ' <small style="color:var(--chalk-dim);letter-spacing:0">through ' +
            played +
            " game" +
            (played > 1 ? "s" : "") +
            " \u00B7 " +
            won +
            "\u2013" +
            (played - won) +
            "</small>"
          : "";
        var opp = String(state._oppName || (wr && wr.opp) || "OPPONENT");
        var wk = p.currentWeek != null ? p.currentWeek + 1 : p._pgGames || 1;
        var res = us > them ? "\u2705 WIN" : us < them ? "\u274C LOSS" : "\u2022 TIE";
        // v98: the coach's read on the game. The swing He() will apply once the card is
        // dismissed (a win pays 3 plus the grade above 58; a loss costs 3 less the grade below
        // it), previewed from the same numbers, or quoted back if it has already been applied.
        var ct = null;
        try {
          var xp = hypeState(),
            now = Math.round(xp.coach || 0),
            sw =
              wr && wr._pulse95 && wr.coachDelta98 != null
                ? wr.coachDelta98
                : Math.max(0, Math.min(100, now + window.__coachSwingV98(grade.score, us > them))) - now;
          ct = { now: now, delta: sw, after: Math.max(0, Math.min(100, now + sw)) };
          // the card is a promise: book the number it shows, so the week is finalised against
          // it even though the sim re-derives the game's rating after Continue
          if (wr && !wr._pulse95) wr.coachPreview98 = sw;
        } catch (e) {
          ct = null;
        }
        var ctCol = !ct ? "" : ct.delta > 0 ? "var(--good)" : ct.delta < 0 ? "#e08a8a" : "var(--chalk-dim)";
        var coachRow = !ct
          ? ""
          : '<div id="pgCoachV98" style="display:flex;align-items:center;gap:8px;margin:0 0 4px;padding:8px 11px;border-radius:11px;background:#00000040;border:1px solid rgba(255,255,255,.07);font-family:Oswald">' +
            '<span style="font-size:10px;letter-spacing:1.5px;color:var(--chalk-dim)">COACH TRUST</span>' +
            '<b style="font-size:20px;color:' +
            ctCol +
            '">' +
            (ct.delta > 0 ? "+" : "") +
            ct.delta +
            "</b>" +
            '<small style="color:var(--chalk-dim)">' +
            ct.now +
            " \u2192 " +
            ct.after +
            "</small>" +
            '<em style="margin-left:auto;font-style:normal;font-size:11px;letter-spacing:.6px;color:' +
            ctCol +
            '">' +
            (ct.delta >= 5
              ? "HE LOVED IT"
              : ct.delta > 0
                ? "EARNED IT"
                : ct.delta < -4
                  ? "NOT HAPPY"
                  : ct.delta < 0
                    ? "A STEP BACK"
                    : "NO CHANGE") +
            "</em>" +
            "</div>";
        var html =
          '<div class="decision-overlay" id="pgOverlayV13">' +
          '<div class="decision-panel" style="border-color:var(--cyan)">' +
          '<div class="decision-kicker">WEEK ' +
          wk +
          " FINAL \u00B7 " +
          opp +
          "</div>" +
          '<div class="decision-title">' +
          res +
          " \u00B7 " +
          us +
          " \u2013 " +
          them +
          "</div>" +
          '<div style="display:flex;align-items:center;gap:8px;margin:8px 0 4px;padding:9px 11px;border-radius:11px;background:#00000040;border:1px solid rgba(255,255,255,.07);font-family:Oswald">' +
          '<span style="font-size:10px;letter-spacing:1.5px;color:var(--chalk-dim)">NATIONAL RANK</span>' +
          '<b style="font-size:20px;color:var(--gold)">#' +
          rank.rank.toLocaleString() +
          "</b>" +
          '<small style="color:var(--chalk-dim)">of ' +
          rank.of.toLocaleString() +
          "</small>" +
          '<em style="margin-left:auto;font-style:normal;font-weight:700;color:' +
          rank.col +
          '">' +
          rank.arrow +
          "</em>" +
          "</div>" +
          coachRow +
          // v53: the SEASON line leads. What a player wants after a whistle is where
          // the year now stands — the single-game box is the detail underneath it.
          // The green deltas still show what this game contributed, so leading with
          // the season total loses nothing.
          '<div class="stat-sec-label">SEASON TOTALS' +
          seasonThru +
          ' <small style="color:var(--good);letter-spacing:0">+ from this game</small></div>' +
          '<div class="fullbox">' +
          pgGrid(p.pos, seasonBox, g.stat) +
          "</div>" +
          gradeCardHtml(p.pos, grade) +
          '<div class="stat-sec-label" style="margin-top:12px">THIS GAME</div>' +
          '<div class="fullbox">' +
          pgGrid(p.pos, g.stat, null) +
          "</div>" +
          '<button class="btn" style="margin-top:14px" onclick="window.__pgContinueV13&&window.__pgContinueV13()">Continue \u203A</button>' +
          "</div></div>";
        var old = document.getElementById("pgOverlayV13");
        if (old) old.remove();
        document.body.insertAdjacentHTML("beforeend", html);
        requestAnimationFrame(function () {
          try {
            animateGradeCard(grade);
          } catch (e) {
            pgApplyScore(grade.score, grade.snaps > 0 ? " · " + grade.snaps + " snaps" : "");
          }
        });
        window.__pgContinueV13 = function () {
          var el = document.getElementById("pgOverlayV13");
          if (el) el.remove();
          try {
            cont();
          } catch (e) {
            console.warn("[postgame continue]", e);
          }
        };
        return true;
      }
      var origEa = finishWeekGame;
      finishWeekGame = function () {
        var self = this,
          args = arguments,
          shown = false;
        try {
          shown = showPostGame(function () {
            origEa.apply(self, args);
          });
        } catch (e) {
          console.warn("[postgame]", e);
          shown = false;
        }
        if (!shown) return origEa.apply(self, args);
      };
      window.finishWeekGame = finishWeekGame;
      window.__GRIDIRON_POSTGAME_V13__ = true;
    } catch (e) {
      console.warn("[postgame patch]", e);
    }
  })();

  /* ===== v14: live-flash suppression · prestige recall · attribute AI notes ===== */
  (function () {
    try {
      // 1) NOTHING flashes before or between live plays — from ANY system.
      function liveNow() {
        try {
          return state && state.view === "live";
        } catch (e) {
          return false;
        }
      }
      ["momentBanner", "cinemaFlash"].forEach(function (id) {
        var el = document.getElementById(id);
        if (!el) return;
        new MutationObserver(function () {
          if (el.classList.contains("go") && liveNow()) el.classList.remove("go");
        }).observe(el, { attributes: true, attributeFilter: ["class"] });
      });
      if (typeof window.__flushDeferred === "function") {
        var origFlush = window.__flushDeferred;
        window.__flushDeferred = function () {
          if (liveNow()) return;
          return origFlush.apply(this, arguments);
        };
      }
      if (typeof bigMoment === "function") {
        // big-moment takeovers also stay out of live games
        var origYe = bigMoment;
        bigMoment = function () {
          if (liveNow()) return;
          return origYe.apply(this, arguments);
        };
        window.bigMoment = bigMoment;
      }

      // 2) every attribute card explains what the skill does to the live-sim AI
      var AI_NOTES = {
        speed: "sets your marker's top speed in the live sim",
        acceleration: "how fast your marker reaches top speed after starting or cutting",
        quickness: "snappier starts and stops for your marker",
        agility: "sharp cuts and jukes scrub less of your speed",
        strength: "you fight through contact with less slowdown",
        stamina: "keeps the stat engine strong late in games",
        awareness: "the play resolver reads routes and leads receivers better",
        vision: "the resolver finds cleaner running lanes",
        tackling: "defenders wrap up instead of whiffing in the resolver",
        blocking:
          "your line holds the pocket longer, raises your pancake odds at OL, and when you're a free blocker you find and seal defenders along the runner's path faster and longer",
        coverage: "the resolver blankets routes when you play defense",
        catching: "the resolver wins more contested catches",
        jumping: "high-points the ball in the resolver"
      };
      /* v142: the ⓘ card now says this properly — in full, for all seventeen stats rather than
       * thirteen, and off the code that reads them. Appending it to `desc` pushed the row's one
       * line past its width, so every note was read as "...sets your marker's top speed i…" and the
       * four stats with no entry looked like they did nothing. Kept behind a dial. */
      if (TU("aiNoteV142", 0) && typeof ATTR_INFO === "object" && ATTR_INFO)
        Object.keys(AI_NOTES).forEach(function (k) {
          if (ATTR_INFO[k] && typeof ATTR_INFO[k].desc === "string" && ATTR_INFO[k].desc.indexOf("AI:") < 0)
            ATTR_INFO[k].desc += " · AI: " + AI_NOTES[k];
        });

      // 3) recall (refund) button on every purchased prestige node
      window.__recallPrestigeV14 = function (key, ev) {
        try {
          ev && ev.stopPropagation && ev.stopPropagation();
        } catch (e) {}
        var n = TREE_NODES[key];
        if (!n) return;
        var lvl = nodeLvl(key);
        if (lvl <= 0) {
          showToast("Nothing to recall.");
          return;
        }
        for (var k2 in TREE_NODES) {
          var d = TREE_NODES[k2];
          if (d && d.req && d.req.node === key && nodeLvl(k2) > 0 && lvl - 1 < d.req.lvl) {
            showToast("Recall " + d.name + " first — it requires this node.");
            return;
          }
        }
        state.tree[key] = lvl - 1;
        var refund = nodeCost(n); // At prices the next level = the one just removed
        state.pp = (state.pp || 0) + refund;
        saveGame();
        render();
        showToast("↩ Recalled " + n.name + " · +" + refund + " PP");
      };
      function decorateRecall() {
        document.querySelectorAll(".shop-item").forEach(function (item) {
          if (item.__recallV14) return;
          var txt = item.textContent || "",
            key = null;
          for (var k3 in TREE_NODES) {
            if (TREE_NODES[k3] && TREE_NODES[k3].name && txt.indexOf(TREE_NODES[k3].name) >= 0) {
              key = k3;
              break;
            }
          }
          if (!key) return;
          item.__recallV14 = true;
          if (nodeLvl(key) <= 0) return;
          var b = document.createElement("button");
          b.textContent = "↩ RECALL";
          b.style.cssText =
            "margin-top:6px;padding:4px 9px;border-radius:8px;border:1px solid rgba(240,187,69,.45);background:#00000055;color:#f0bb45;font-family:Oswald,sans-serif;font-size:10px;letter-spacing:1px;cursor:pointer";
          b.onclick = function (ev) {
            window.__recallPrestigeV14(key, ev);
          };
          (item.querySelector(".si") || item).appendChild(b);
        });
      }
      new MutationObserver(function (muts) {
        for (var i2 = 0; i2 < muts.length; i2++)
          if (muts[i2].addedNodes.length) {
            decorateRecall();
            break;
          }
      }).observe(document.body, { childList: true, subtree: true });
      decorateRecall();
      window.__GRIDIRON_V14__ = true;
    } catch (e) {
      console.warn("[v14 patch]", e);
    }
  })();

  /* ===== v15.3: team creator, 100 logos, animation hardening, richer turf ===== */
  (function () {
    try {
      const CUSTOM_KEY = "gridironTeamCustomV153";
      const palettes = (window.TEAM_PALETTES || []).slice(); // v44: all palettes, incl. the emblem-matched additions
      const EMB = window.TEAM_LOGOS_V44 || null,
        NLOGO = EMB ? EMB.db.length : 90;
      function safeText(v, max) {
        return cleanNameV150(v, max || 24); /* v150 A: it only stripped <>, so a quote closed the input's value="…" */
      }
      function logoDef(i) {
        i = (((Number(i) || 0) % NLOGO) + NLOGO) % NLOGO;
        return { id: i, shape: "emblem", glyph: (EMB ? EMB.name(i) : "?").slice(0, 1), name: EMB ? EMB.name(i) : "" };
      }
      /* v97 THE PALETTE'S NAME — the naming convention: "Primary & Secondary", each colour named
       * by its hue and depth (Navy & Gold, Crimson & Silver, Forest & White), so a palette is
       * something you can say out loud instead of "number 23". */
      function palNameV97(p) {
        const nm = h => {
          const n = parseInt(String(h).replace("#", ""), 16),
            r = (n >> 16) & 255,
            g = (n >> 8) & 255,
            b = n & 255,
            mx = Math.max(r, g, b),
            mn = Math.min(r, g, b),
            L = (mx + mn) / 2 / 255,
            sat = mx ? (mx - mn) / mx : 0;
          let hue = 0;
          if (mx !== mn) {
            if (mx === r) hue = (60 * ((g - b) / (mx - mn)) + 360) % 360;
            else if (mx === g) hue = 60 * ((b - r) / (mx - mn)) + 120;
            else hue = 60 * ((r - g) / (mx - mn)) + 240;
          }
          if (sat < 0.14)
            return L > 0.86 ? "White" : L > 0.6 ? "Silver" : L > 0.32 ? "Grey" : L > 0.12 ? "Charcoal" : "Black";
          const dk = L < 0.28,
            lt = L > 0.68;
          if (hue < 14 || hue >= 345) return dk ? "Maroon" : lt ? "Rose" : "Crimson";
          if (hue < 40) return dk ? "Bronze" : lt ? "Peach" : "Orange";
          if (hue < 66) return dk ? "Bronze" : lt ? "Cream" : "Gold";
          if (hue < 95) return dk ? "Olive" : lt ? "Lime" : "Chartreuse";
          if (hue < 160) return dk ? "Forest" : lt ? "Mint" : "Green";
          if (hue < 195) return dk ? "Teal" : lt ? "Aqua" : "Teal";
          if (hue < 250) return dk ? "Navy" : lt ? "Sky" : "Royal";
          if (hue < 290) return dk ? "Indigo" : lt ? "Lavender" : "Purple";
          return dk ? "Plum" : lt ? "Pink" : "Magenta";
        };
        return nm(p[0]) + " & " + nm(p[1]);
      }
      window.palNameV97 = palNameV97;
      function getCustom() {
        let c = { schoolName: "Westfield State", teamName: "Storm", palette: 0, logo: 0, byLevel: {} };
        try {
          Object.assign(c, JSON.parse(localStorage.getItem(CUSTOM_KEY) || "{}"));
        } catch (e) {}
        c.palette = Math.max(0, Math.min(palettes.length - 1 || 39, Number(c.palette) || 0));
        c.logo = Math.max(0, Math.min(NLOGO - 1, Number(c.logo) || 0));
        c.byLevel = c.byLevel && typeof c.byLevel === "object" ? c.byLevel : {};
        c.schoolName = safeText(c.schoolName, 24) || "Westfield State"; /* v150 A: what storage hands back is cleaned too */
        c.teamName = safeText(c.teamName, 18) || "Storm";
        return c;
      }
      function saveCustom(c) {
        localStorage.setItem(CUSTOM_KEY, JSON.stringify(c));
        window.__GRIDIRON_TEAM_CUSTOM__ = c;
        applyTeamCustom();
      }
      function logoHTML(i) {
        if (EMB) return `<span class="team-logo-v44 emblem-v44" title="${EMB.name(i)}" style="${EMB.css(i)}"></span>`;
        const s = 38;
        const p = palettes[effIdV18(getCustom()).palette] || palettes[0];
        return `<span class="team-logo-v153 circle" style="--p:${p[0]};--s:${p[1]};width:${s}px;height:${s}px"><b>${logoDef(i).glyph}</b></span>`;
      }
      /* v44.1: live identity preview — the card re-renders on every palette pick, emblem pick,
       * and team-name keystroke, so the palette selection always shows what you're getting. */
      function idPrevHTML(pi, li) {
        const p = palettes[pi] || palettes[0],
          nm = EMB ? EMB.name(li) : "Logo " + (li + 1);
        return `<div class="id-jersey-v44" style="background:${p[0]};box-shadow:inset 0 0 0 5px ${p[1]}">${EMB ? `<i class="emblem-v44" style="width:42px;height:42px;${EMB.css(li)}"></i>` : ""}</div><div class="id-meta-v44"><b>${nm}</b><span><i style="background:${p[0]}"></i><i style="background:${p[1]}"></i> Palette ${pi + 1}</span></div>`;
      }
      function refreshIdPrevV44() {
        const el = document.getElementById("creatorPreviewV44");
        if (!el) return;
        const c = getCustom();
        el.innerHTML = idPrevHTML(window.__tempPaletteV153 ?? c.palette, window.__tempLogoV153 ?? c.logo);
      }
      window.teamNameLiveV44 = function (v) {
        if (!EMB || window.__logoHandPickedV44) return;
        const m = EMB.forName(String(v || "").trim());
        if (m != null) window.pickLogoV153(m, true);
      };
      /* v18: every NEW TEAM gets a new identity. Each career level (Pee Wee → Middle
       * School → JV → Varsity → College → UFF...) deterministically rotates the uniform
       * palette and logo, unless the player saved a custom look at that level. */
      function lvlV18() {
        try {
          const st = window.__getGridironState && window.__getGridironState();
          return (st && st.player && st.player.level) || 0;
        } catch (e) {
          return 0;
        }
      }
      /* v44: identity resolution. A saved per-level or explicit Team Creator pick always wins;
       * otherwise the logo AUTO-MATCHES the team name (Wolves -> the wolf emblem) and the
       * palette AUTO-MATCHES the logo, so colors track the crest by default. Teams whose name
       * matches nothing fall back to the old per-level rotation over the emblem set. */
      function effIdV18(c) {
        const lv = lvlV18(),
          ov = c.byLevel && c.byLevel[lv],
          P = palettes.length || 40;
        const nm = EMB ? EMB.forName(c.teamName || c.schoolName) : null;
        const logo =
          ov && ov.logo != null ? ov.logo : c.userSetV22 ? c.logo : nm != null ? nm : (c.logo + lv * 13) % NLOGO;
        const palette =
          ov && ov.palette != null
            ? ov.palette
            : c.userSetV22
              ? c.palette
              : EMB
                ? EMB.palIdx(logo)
                : (c.palette + lv * 7) % P;
        return { palette, logo, lv };
      }
      function applyTeamCustom() {
        const c =
            clubCustomV146B(
              getCustom()
            ) /* v146 B: the UFF club he signed with wears its own name, crest and colours */,
          eff = effIdV18(c),
          p = palettes[eff.palette] || palettes[0];
        window.__GRIDIRON_TEAM_CUSTOM__ = Object.assign({}, c, { palette: eff.palette, logo: eff.logo, col: p });
        const _tnV25 = c.teamName.toUpperCase();
        document.querySelectorAll(".sb-side.us .team").forEach(el => {
          if (el.textContent !== _tnV25) el.textContent = _tnV25;
        });
        document.querySelectorAll(".logo-sub").forEach(el => {
          if (state && state.player && el.textContent !== c.schoolName) el.textContent = c.schoolName;
        });
        try {
          if (window.__gridironScene && RIB.ready && window.__lastPalV25 !== eff.palette) {
            window.__lastPalV25 = eff.palette;
            ribRegisterTeam(window.__gridironScene, "off", p[0], p[1]);
          }
        } catch (e) {}
      }
      function creatorHTML() {
        const c = getCustom(),
          mp = EMB ? EMB.palIdx(c.logo) : -1;
        return `<div class="team-modal-v153" id="teamModalV153"><div class="team-panel-v153"><button class="team-close-v153" onclick="closeTeamCreatorV153()">×</button><div class="eyebrow">TEAM CREATOR</div><div class="h1">Build Your School</div><div class="team-form-v153"><label>School name<input id="schoolNameV153" maxlength="24" value="${escHtml(safeText(c.schoolName, 24))}"></label><label>Team name<input id="teamNameV153" maxlength="18" value="${escHtml(safeText(c.teamName, 18))}" oninput="teamNameLiveV44(this.value)"></label></div><div id="nameIdeasV139">${nameIdeasHTMLV139()}</div><div class="creator-preview-v44" id="creatorPreviewV44">${idPrevHTML(c.palette, c.logo)}</div><div class="h2">${palettes.length} Uniform Palettes <small class="pal-hint-v44">✓ marks the palette matched to your emblem</small></div><div class="palette-grid-v153">${palettes.map((p, i) => `<button class="palette-v153 ${i === c.palette ? "on" : ""} ${i === mp ? "match" : ""}" data-i="${i}" title="${palNameV97(p)}" onclick="pickPaletteV153(${i})"><i style="background:${p[0]}"></i><i style="background:${p[1]}"></i><small>${i + 1}</small><em class="pal-name-v97">${palNameV97(p)}</em></button>`).join("")}</div><div class="h2">${NLOGO} Team Emblems <small class="pal-hint-v44">picking one selects its matching palette</small></div><div class="logo-grid-v153">${Array.from({ length: NLOGO }, (_, i) => `<button class="logo-pick-v153 ${i === c.logo ? "on" : ""}" data-i="${i}" title="${EMB ? EMB.name(i) : i + 1}" onclick="pickLogoV153(${i})">${logoHTML(i)}</button>`).join("")}</div><div class="btn-row mt"><button class="btn secondary" onclick="previewUniformV153()">Open Uniform Preview</button><button class="btn" onclick="saveTeamCreatorV153()">Save Team</button></div></div></div>`;
      }
      window.openTeamCreatorV153 = function () {
        window.__tempLogoV153 = null;
        window.__tempPaletteV153 = null;
        window.__logoHandPickedV44 = false;
        document.getElementById("teamModalV153")?.remove();
        document.body.insertAdjacentHTML("beforeend", creatorHTML());
      };
      window.closeTeamCreatorV153 = function () {
        document.getElementById("teamModalV153")?.remove();
      };
      /* ===== v139 FIVE NAMES, ON THE HOUSE =====
       * The team creator handed you two empty boxes and a wall of palettes. The game already owns 120
       * invented towns, 88 mascots, the college suffixes and the fifty UFF clubs (v123) — so it offers
       * five whole names, level-shaped, and they are re-rolled every time you pick a palette, which is
       * exactly when you are looking at the thing they have to sit beside. */
      function nameIdeasV139(n) {
        const N = window.__NAMES_V123;
        if (!N) return [];
        const lvl = (state && state.player && state.player.level) | 0,
          T = N.towns(),
          M = N.mascots(),
          C = N.colleges(),
          D = N.dfl(),
          out = [],
          seen = {};
        for (let g = 0; g < 60 && out.length < (n || 5); g++) {
          const t = T[(Math.random() * T.length) | 0],
            m = M[(Math.random() * M.length) | 0];
          /* a UFF club is already "Town Mascot" — the mascot IS its own last word, so it does not get a second one */
          const club = lvl >= 7 ? D[(Math.random() * D.length) | 0] : "";
          const school = club || (lvl >= 5 ? t + " " + C[(Math.random() * C.length) | 0] : t),
            team = club ? club.split(" ").pop() : m;
          if (seen[school] || seen[team]) continue;
          seen[school] = seen[team] = 1;
          out.push({ school, team });
        } /* five distinct names, five distinct mascots */
        return out;
      }
      function nameIdeasHTMLV139() {
        const ideas = nameIdeasV139(5);
        window.__NAME_IDEAS_V139 = ideas;
        if (!ideas.length) return "";
        return `<div class="name-ideas-v139"><div class="ni-head-v139">TAP A NAME TO USE IT</div><div class="ni-row-v139">${ideas.map((d, i) => `<button type="button" class="ni-chip-v139" onclick="pickNameIdeaV139(${i})"><b>${escHtml(d.school)}</b><small>${escHtml(d.team)}</small></button>`).join("")}</div></div>`;
      }
      function reIdeasV139() {
        const h = document.getElementById("nameIdeasV139");
        if (h) h.innerHTML = nameIdeasHTMLV139();
      }
      window.pickNameIdeaV139 = function (i) {
        const d = (window.__NAME_IDEAS_V139 || [])[i];
        if (!d) return;
        const a = document.getElementById("schoolNameV153"),
          b = document.getElementById("teamNameV153");
        if (a) a.value = d.school;
        if (b) {
          b.value = d.team;
          try {
            teamNameLiveV44(d.team);
          } catch (_) {}
        }
        try {
          refreshIdPrevV44();
        } catch (_) {}
      };
      window.__NAME_IDEAS_FN_V139 = nameIdeasV139;
      window.pickPaletteV153 = function (i) {
        document.querySelectorAll(".palette-v153").forEach(x => x.classList.toggle("on", Number(x.dataset.i) === i));
        window.__tempPaletteV153 = i;
        refreshIdPrevV44();
        reIdeasV139();
      }; /* v139: a new palette, five new names beside it */
      window.pickLogoV153 = function (i, auto) {
        if (!auto) window.__logoHandPickedV44 = true;
        document.querySelectorAll(".logo-pick-v153").forEach(x => x.classList.toggle("on", Number(x.dataset.i) === i));
        window.__tempLogoV153 = i;
        // v44: an emblem pick highlights AND selects its color-matched palette (still overridable by hand)
        if (EMB) {
          const pi = EMB.palIdx(i);
          window.__tempPaletteV153 = pi;
          let mEl = null;
          document.querySelectorAll(".palette-v153").forEach(x => {
            const m = Number(x.dataset.i) === pi;
            x.classList.toggle("on", m);
            x.classList.toggle("match", m);
            if (m) mEl = x;
          });
          try {
            !auto && mEl && mEl.scrollIntoView({ block: "nearest" });
          } catch (e) {}
        }
        refreshIdPrevV44();
      };
      window.saveTeamCreatorV153 = function () {
        const old = getCustom(),
          c = {
            schoolName: safeText(document.getElementById("schoolNameV153")?.value, 24) || old.schoolName,
            teamName: safeText(document.getElementById("teamNameV153")?.value, 18) || old.teamName,
            palette: window.__tempPaletteV153 ?? old.palette,
            logo: window.__tempLogoV153 ?? old.logo,
            byLevel: old.byLevel || {},
            userSetV22: true
          };
        c.byLevel[lvlV18()] = { palette: c.palette, logo: c.logo };
        saveCustom(c);
        closeTeamCreatorV153();
        try {
          saveGame();
          render();
        } catch (e) {}
        showToast("Team customization saved.");
      };
      // v22: every NEW career gets a FRESH random team look (unless the player has
      // explicitly customized it in the Team Creator, which sets userSetV22).
      window.__randomizeTeamLookV22 = function (seed) {
        try {
          const c = getCustom();
          if (c.userSetV22) return;
          const r = n => {
            let x = ((seed >>> 0) * 2654435761 + n * 40503) >>> 0;
            return x / 4294967296;
          };
          c.logo = EMB ? EMB.forName(c.teamName || c.schoolName) : Math.floor(r(2) * NLOGO);
          if (c.logo == null) c.logo = Math.floor(r(2) * NLOGO);
          c.palette = EMB ? EMB.palIdx(c.logo) : Math.floor(r(1) * (palettes.length || 40));
          c.byLevel = {};
          saveCustom(c);
        } catch (e) {}
      };
      window.previewUniformV153 = function () {
        const old = getCustom(),
          c = {
            schoolName: safeText(document.getElementById("schoolNameV153")?.value, 24) || old.schoolName,
            teamName: safeText(document.getElementById("teamNameV153")?.value, 18) || old.teamName,
            palette: window.__tempPaletteV153 ?? old.palette,
            logo: window.__tempLogoV153 ?? old.logo
          },
          p = palettes[c.palette],
          d = logoDef(c.logo);
        const _mark = EMB
          ? `<i style="position:absolute;left:50%;top:52px;transform:translateX(-50%);width:80px;height:80px;filter:drop-shadow(0 3px 5px #0009);${EMB.cssFull(c.logo)}"></i>`
          : "";
        const _html =
          `<!doctype html><title>${escHtml(c.schoolName)} Uniform</title><style>body{margin:0;background:#0b1119;color:white;font-family:Arial;display:grid;place-items:center;min-height:100vh}.card{width:330px;padding:28px;border-radius:24px;background:linear-gradient(160deg,#182434,#080c12);box-shadow:0 20px 60px #000}.jersey{position:relative;margin:20px auto;width:220px;height:270px;background:${p[0]};clip-path:polygon(20% 0,38% 8%,62% 8%,80% 0,100% 24%,84% 39%,76% 31%,76% 100%,24% 100%,24% 31%,16% 39%,0 24%);box-shadow:inset 0 0 0 8px ${p[1]}}${EMB ? "" : `.jersey:before{content:'${d.glyph}';position:absolute;left:50%;top:66px;transform:translateX(-50%);width:72px;height:72px;border-radius:18px;display:grid;place-items:center;background:${p[1]};color:${p[0]};font-size:42px;font-weight:900}`}.jersey:after{content:'00';position:absolute;left:50%;top:145px;transform:translateX(-50%);font-size:66px;font-weight:900;color:${p[1]};text-shadow:3px 3px 0 #0006}h1{text-align:center;margin:0}p{text-align:center;color:#aab4c2}</style><div class=card><h1>${escHtml(c.schoolName)}</h1><p>${escHtml(c.teamName)} · ${EMB ? EMB.name(c.logo) : "Logo " + (c.logo + 1)} · Palette ${c.palette + 1}</p><div class=jersey>${_mark}</div></div>`; /* v150 A: an in-app frame (ribDialog), not a pop-up a WebView blocks or leaves the app for */
        const _paint = w => {
          if (!w) return;
          w.document.open();
          w.document.write(_html);
          w.document.close();
        };
        if (window.ribDialog && window.ribDialog.frame) return window.ribDialog.frame("Uniform preview").then(_paint);
        const w = window.open("", "gridironUniformPreview", "width=430,height=720");
        if (!w) return showToast("Pop-up blocked. Allow pop-ups for the preview.");
        _paint(w);
      };
      function addCreatorButton() {
        if (document.getElementById("teamCreatorBtnV153")) return;
        const top = document.querySelector(".topbar");
        if (top) {
          const b = document.createElement("button");
          b.id = "teamCreatorBtnV153";
          b.className = "team-creator-btn-v153";
          b.textContent = "🎨";
          b.title = "Team Creator";
          b.onclick = openTeamCreatorV153;
          top.appendChild(b);
        }
      }
      new MutationObserver(() => {
        addCreatorButton();
        applyTeamCustom();
      }).observe(document.body, { childList: true, subtree: true });
      addCreatorButton();
      applyTeamCustom();
      // New-school hook: invite customization once per level/school.
      let lastLevel = null;
      setInterval(() => {
        try {
          const lv = state?.player?.level;
          if (lv == null) return;
          if (lastLevel == null) lastLevel = lv;
          else if (lv !== lastLevel) {
            lastLevel = lv;
            if (!(lv >= 7 && state && state.player && (state.player.offersV146B || state.player.clubV146B)))
              setTimeout(openTeamCreatorV153, 250);
          } /* v146 B: a UFF club is signed, not designed */
        } catch (e) {}
      }, 900);
      // Animation safeguards: clamp huge frame deltas, remove stale tweens, and provide a deterministic QC runner.
      const oldUpdate = LIFESTYLES.prototype.update;
      LIFESTYLES.prototype.update = function (time, delta) {
        delta = Math.max(0, Math.min(50, Number(delta) || 16.67));
        try {
          return oldUpdate.call(this, time, delta);
        } catch (err) {
          console.warn("[LiveField recovered]", err);
          try {
            this.softStop();
            this.complete();
          } catch (e) {}
        }
      };
      const oldAnimate = LIFESTYLES.prototype.animatePlay;
      LIFESTYLES.prototype.animatePlay = function (et, rt) {
        try {
          if (!et || typeof et !== "object") throw new Error("Invalid play payload");
          if (this.play && !this.play.done) this.softStop();
          return oldAnimate.call(this, et, rt);
        } catch (err) {
          console.warn("[animatePlay recovered]", err);
          try {
            this.complete();
          } catch (e) {}
        }
      };
      window.__GRIDIRON_SIMULATE_V153 = function (iterations) {
        const out = {
          iterations: iterations || 250,
          errors: [],
          checks: { palettes: palettes.length, logos: NLOGO, metaFrames: Object.keys(RIB_META).length }
        };
        for (let i = 0; i < out.iterations; i++) {
          try {
            const p = palettes[i % 40];
            if (!p || p.length !== 2) throw new Error("bad palette " + i);
            const d = logoDef(i % NLOGO);
            if (!d.shape || !d.glyph) throw new Error("bad logo " + i);
            const h = (i % 31) * 3.2,
              arc = Math.min(26, h * 0.34),
              sx = 0.43,
              sy = 0.25;
            if (!Number.isFinite(arc + sx + sy) || arc < 0 || arc > 26) throw new Error("ball physics " + i);
          } catch (e) {
            out.errors.push(String(e.message || e));
          }
        }
        out.ok = !out.errors.length;
        return out;
      };
      window.__GRIDIRON_FEATURES__ = {
        ...(window.__GRIDIRON_FEATURES__ || {}),
        teamCreator: true,
        palettes40: true,
        logos100: true,
        teamEmblemsV44: true,
        uniformPreview: true,
        animationHardened: true,
        characterFallback: true,
        sceneTextureReregister: true,
        bulletFootball: true,
        multicolorGrass: true,
        defensivePushback: true,
        forwardProgress: true
      };
    } catch (e) {
      console.warn("[v15.3 patch]", e);
    }
  })();

  /* ===== v113 THE BOARD IS A GRID, AND THE CHOICE IS TWO TAPS =====
   * The offseason board listed twelve programs as full-width cards, each one carrying its
   * scene, its focus chips, its per-stat projection and its cost verdict: choosing a season's
   * work meant scrolling two thousand pixels and holding four numbers in your head, and the
   * tap that finally showed you what a program DOES was the same tap that committed the
   * season to it. There was no way to look without choosing.
   *
   * The board is the twelve scenes on one screen now — four across, three down — and the tap
   * is split in two:
   *   - a tile PREVIEWS. The sheet under the grid redraws with every attribute's bar at the
   *     value it holds today and the season this program would add on top of it: the stats the
   *     program actually pushes are marked in light blue and pulse slowly (2.6s, held still
   *     under prefers-reduced-motion), the ones it charges are marked in red. Nothing is
   *     written to the player — projectSeasonGainsV85 is asked for the program, not told it.
   *   - the dock's CONFIRM TRAINING commits, through the unchanged chooseTraining (Ir), so the
   *     event roll and the season behind it are exactly what they were.
   * The suggested program is previewed on arrival, so the sheet is never empty and the shape of
   * the screen explains itself before the first tap.
   *
   * Kept on purpose, because the dev checks read them: the tiles are still `.train-card`
   * elements whose onclick names the program key in the first quoted token — capcheck and
   * skillartcheck both pull it out with a regex that stops at the first digit, which is why the
   * handlers are `previewTraining`/`confirmTraining` and not `...V113` — they still carry the
   * v64 scene art, and the panel still carries the v67 cap badges on `.train-chip`s and the
   * one-line program verdict on `.train-meta`. ===== */
  const TP_SEG_MIN_V113 = 2.4; // a sub-point season still has to be visible on the bar
  let tpSelV113 = null; // the PREVIEWED program — never the player's, until confirmed
  function tpRiskV113(n) {
    return n.injury >= 1.4
      ? ["HIGH RISK", "var(--blood)"]
      : n.injury >= 1.1
        ? ["SOME RISK", "var(--gold)"]
        : n.injury < 0.9
          ? ["SAFE", "var(--good)"]
          : ["STEADY", "var(--good)"];
  }
  // the projected season, as the board has always phrased it: a range across a group of stats
  function tpRangeV113(pj, ks) {
    const vs = ks.map(k => pj[k] || 0);
    if (!vs.length) return "";
    const lo = Math.round(Math.min(...vs)),
      hi = Math.round(Math.max(...vs));
    const sg = v => (v >= 0 ? "+" : "\u2212") + Math.abs(v); // v124: a program that TRADES can project a negative, and "+-1" is not a number
    return lo === hi ? sg(hi) : sg(lo) + " to " + sg(hi);
  }
  // one attribute: where it stands, and the light-blue length this program would add to it
  function tpRowV113(e, k, g, pri, gmax) {
    // the bar is drawn against the SOFT CAP, not the absolute wall: at 12 of an eventual 250
    // every stat read as a sliver, and the number that actually governs this season is the cap
    // the next point starts costing more at. A stat already past it stretches the scale to fit
    // and keeps a tick where the cap sits, so "over the cap" stays visible instead of pinned.
    const cur = wholeNum(e.attrs[k] || 0),
      abs = attrCap(),
      cap = clamp99(drSoftCap(e, k), 1, abs);
    const scale = Math.min(abs, Math.max(cap, cur + Math.max(0, g))),
      pct = clamp99((cur / scale) * 100, 0, 100),
      gn = Math.round(g);
    const up = g > 0 ? Math.min(100 - pct, Math.max(TP_SEG_MIN_V113, (g / scale) * 100)) : 0;
    const dn = g < 0 ? Math.min(pct, Math.max(TP_SEG_MIN_V113, (-g / scale) * 100)) : 0;
    const capPct = scale > cap + 0.5 ? clamp99((cap / scale) * 100, 0, 100) : 0;
    const tier = attrTierV133(e, k, g, gmax); // v133: the colour of the season it would add
    return `<div class="tp-row-v113${pri ? " pri" : ""}${g < 0 ? " neg" : ""} tier-${tier}" data-tier="${tier}">
      <div class="tp-rt-v113"><span class="tp-rn-v113">${ATTR_INFO[k].icon} ${ATTR_INFO[k].name}${statInfoBtnV142(k)}</span><span class="tp-rv-v113">${cur}${gn !== 0 ? `<em>→ ${wholeNum(cur + g)}</em>` : ""}<span class="tp-capn-v113">/ ${wholeNum(cap)} cap</span></span></div>
      <div class="track ${ovrGradeClass(cur)} tp-bar-v113"><i style="width:${pct}%"></i>${up ? `<b class="tp-up-v113${pri ? " flash" : ""} tier-${tier}" style="left:${pct}%;width:${up}%"></b>` : ""}${dn ? `<b class="tp-dn-v113" style="left:${pct - dn}%;width:${dn}%"></b>` : ""}${capPct ? `<b class="tp-capt-v113" style="left:${capPct}%"></b>` : ""}</div>
    </div>`;
  }
  // the sheet under the grid: what a season of ONE program does to all seventeen stats
  function tpPanelV113(e, key, sug) {
    const n = PROGRAMS[key],
      pj = projectSeasonGainsV85(e, key, !0),
      foc = n.focus && n.focus.length ? n.focus : null;
    const pri = k => (foc ? foc.indexOf(k) >= 0 : !0),
      [risk, rc] = tpRiskV113(n),
      cp = capPlanV67(e, foc);
    const others = ATTR_KEYS.filter(k => !(foc && foc.indexOf(k) >= 0) && !(n.cost && n.cost[k]));
    const head = foc
      ? `FOCUS STATS ${tpRangeV113(pj, foc)} · OTHERS ${tpRangeV113(pj, others)} THIS SEASON`
      : `EVERY STAT ${tpRangeV113(pj, ATTR_KEYS)} THIS SEASON`;
    const gv = k => {
      const v = Math.round(pj[k] || 0);
      return v >= 0 ? "+" + v : String(v);
    };
    const chips = foc
      ? foc
          .map(
            k =>
              `<span class="train-chip">${ATTR_INFO[k].icon} ${ATTR_INFO[k].name} <b class="v85g" style="color:var(--good);margin:0 6px 0 3px">${gv(k)}</b>${capBadgeV67(e, k)}</span>`
          )
          .join("")
      : `<span class="train-chip">⚖️ All stats evenly</span>` +
        Object.keys(POSITIONS[e.pos].w)
          .sort((a, b) => POSITIONS[e.pos].w[b] - POSITIONS[e.pos].w[a])
          .slice(0, 4)
          .map(k => `<span class="train-chip">${ATTR_INFO[k].icon} ${ATTR_INFO[k].name}${capBadgeV67(e, k)}</span>`)
          .join("");
    const costs = n.cost
      ? Object.entries(n.cost)
          .map(([k, m]) => `<span class="train-chip neg">▼ ${ATTR_INFO[k].name} ${m}</span>`)
          .join("")
      : "";
    /* v124: the trade in one sentence, and the roll with its real odds and both outcomes */
    const trade = n.cost
      ? `<div class="threshold-note" style="margin-top:8px;color:#ff9b93;border-color:rgba(255,155,147,.45)">\u2696\ufe0f <b>THIS PROGRAM TRADES</b> \u00b7 it takes ${Object.entries(
          n.cost
        )
          .map(([k, m]) => `<b>${m} ${ATTR_INFO[k].name}</b>`)
          .join(
            " and "
          )} off you at the end of the season to pay for the growth above. That is a real number on the sheet, not a risk.</div>`
      : "";
    const fd = PLAN_FATE_V124[key],
      fate = fd
        ? `<div class="threshold-note" style="margin-top:6px;color:#c9b8ff;border-color:rgba(201,184,255,.45)">\ud83c\udfb2 <b>THIS ONE IS A ROLL \u00b7 ${Math.round(planFateOddsV124(key) * 100)}% IT LANDS</b><br><span style="color:#9fe6b0">Lands:</span> the trade above is <b>waived entirely</b> and every priority stat grows <b>\u00d7${fd.hi}</b>. <span style="color:#ff9b93">Misses:</span> the trade <b>doubles</b> (${
            Object.entries(n.cost || {})
              .map(([k, m]) => `${m * 2} ${ATTR_INFO[k].name}`)
              .join(", ") || "nothing to double"
          }) and priority growth is cut to <b>\u00d7${fd.lo}</b>.<br><span style="color:var(--chalk-dim)">The bars above show the AVERAGE of those two, so they are honest about a season you cannot predict. Weighted Coin raises the odds, Loaded Dice rolls twice and keeps the better, House Money pays part of a miss, Scripted Destiny rewrites one failure a season.</span></div>`
        : "";
    // priority stats first — they are the answer to "what would this improve" — then the rest
    // of the sheet in its usual order, so the eye can compare two programs row for row
    const gmax = Math.max(0.01, ...ATTR_KEYS.map(k => pj[k] || 0)),
      TR = { green: 0, blue: 1, red: 2, none: 3, cost: 4 },
      byTier = ks =>
        ks
          .slice()
          .sort(
            (a, b) => (TR[attrTierV133(e, a, pj[a] || 0, gmax)] || 0) - (TR[attrTierV133(e, b, pj[b] || 0, gmax)] || 0)
          ); // v133: green first, then blue, then red
    const rest = byTier(ATTR_KEYS.filter(k => !pri(k))),
      lead = byTier(ATTR_KEYS.filter(pri));
    const rows = g => g.map(k => tpRowV113(e, k, pj[k] || 0, pri(k), gmax)).join("");
    return `<div class="tp-panel-v113">
      <div class="train-head">
        ${window.RIB_SKILL_ICO ? window.RIB_SKILL_ICO("tp_" + key, n.icon, 54) : `<span style="font-size:24px">${n.icon}</span>`}
        <div style="flex:1;min-width:0">
          <div class="pn">${n.name} ${key === sug ? '<span class="fit-tag fit-natural">SUGGESTED</span>' : ""}${(() => {
            try {
              const gb = boardGradesV133(e, sug).by[key];
              return gb
                ? `<span class="tp-tierpill-v133" data-tier="${gb.tier}" style="color:${TIER_V133[gb.tier].col};border-color:${TIER_V133[gb.tier].col}">${TIER_V133[gb.tier].name}</span>`
                : "";
            } catch (_) {
              return "";
            }
          })()}</div>
          <div class="train-meta"><span style="color:var(--good)">${head}</span> · <span style="color:${rc}">${risk}</span>${n.bonusPoints ? ` · <span style="color:var(--gold)">+${n.bonusPoints} pts</span>` : ""}${cp ? ` · <span style="color:${cp.col}">${cp.label}</span>` : ""}</div>
        </div>
      </div>
      <div class="pd" style="margin-top:7px">${n.desc}</div>
      <div class="train-chips">${chips}${costs}</div>
      ${trade}${fate}
      <div class="tp-legend-v113"><span><b class="tp-key-v113 tier-green flash"></b> GREEN · A KEY STAT WITH ROOM</span><span><b class="tp-key-v113 tier-blue"></b> BLUE · A SOLID GAIN</span><span><b class="tp-key-v113 tier-red"></b> RED · CAPPED, OR MINOR</span>${costs ? '<span><b class="tp-key-v113 dn"></b> WHAT IT COSTS</span>' : ""}</div>
      <div class="tp-rows-v113">
        <div class="tp-grouphead-v113">${foc ? "PRIORITY GROWTH" : "EVERY STAT — NOTHING IS PRIORITISED"}</div>
        ${rows(lead)}
        ${rest.length ? `<div class="tp-grouphead-v113 dim">EVERYTHING ELSE</div>${rows(rest)}` : ""}
      </div>
    </div>`;
  }
  /* ===== v133 THE COACH GRADES THE BOARD =====
   * The board suggested ONE program off the single furthest-behind stat (v124) and told you nothing about
   * the other eleven. Every program is graded now, from what a season of it would actually do to THIS
   * sheet: `attrTierV133` grades one attribute under one program — GREEN when it is a stat the position is
   * graded on (or a body stat that is breaking down) with room under its soft cap and a real gain, BLUE
   * when the gain is real but the stat is secondary or near its cap, RED when it is at its cap (the next
   * point costs double), secondary, or barely moves — and `trainGradeV133` sums a program's focus stats by
   * the position's own weights into a value (a program that TRADES a key stat is dragged down). Relative
   * to the best on the board: GREEN pushes several key stats with room (or is the coach's pick), BLUE is a
   * solid gain, RED is minor or mostly capped; Balanced is BLUE by nature. `Hi` (the pick) is the best
   * graded value now, not the single top stat — except a body that is breaking down still sends him to
   * Conditioning, as v124 decided. The tiles wear the tier as a colour and a pill, the pick wears a
   * ribbon and a pulse, the sheet's bars are shaded per attribute (green / blue / red) and sorted by it,
   * and the coach's note says why in one line, with the runner-up. `window.__V133`; `gradecheck.mjs`. */
  const TIER_V133 = {
    green: { name: "KEY", col: "#7fe0a0" },
    blue: { name: "SOLID", col: "#7ec8ff" },
    red: { name: "MINOR", col: "#ff8a80" }
  };
  // the stats a position is graded on: its four heaviest weights — plus a body stat that is actually breaking down
  function keyStatsV133(e) {
    const W = POSITIONS[e.pos].w,
      top = Object.keys(W)
        .sort((a, b) => (W[b] || 0) - (W[a] || 0))
        .slice(0, 4);
    for (const k of ["injuryResist", "stamina"])
      if ((e.attrs[k] || 0) < 14 + e.level * 3 && top.indexOf(k) < 0) top.push(k);
    return top;
  }
  // one attribute under one program. Gains are fractions of a point at Pee Wee and several in the UFF, so a
  // gain is read RELATIVE to the best gain on the sheet (gmax), never against a fixed number of points
  function attrTierV133(e, k, g, gmax) {
    const cur = e.attrs[k] || 0,
      cap = clamp99(drSoftCap(e, k), 1, attrCap()),
      room = cap - cur,
      key = keyStatsV133(e).indexOf(k) >= 0,
      rel = g / Math.max(0.01, gmax || Math.max(1, g));
    if (g < 0) return "cost";
    if (g <= 0) return "none";
    if (room <= 0 || rel < 0.3) return "red";
    if (key && rel >= 0.55) return "green";
    return key || rel >= 0.55 ? "blue" : "red";
  }
  function trainGradeV133(e, key) {
    const n = PROGRAMS[key],
      pj = projectSeasonGainsV85(e, key, !0),
      foc = n.focus && n.focus.length ? n.focus : ATTR_KEYS.slice(),
      W = POSITIONS[e.pos].w,
      top = Math.max(1, ...Object.values(W)),
      gmax = Math.max(0.01, ...ATTR_KEYS.map(k => pj[k] || 0)),
      KEY = keyStatsV133(e);
    let value = 0;
    const keyHits = [],
      blue = [],
      red = [],
      capped = [];
    for (const k of foc) {
      const g = pj[k] || 0,
        t = attrTierV133(e, k, g, gmax),
        want = Math.max((W[k] || 0) / top, KEY.indexOf(k) >= 0 ? 0.8 : 0),
        cur = e.attrs[k] || 0,
        cap = clamp99(drSoftCap(e, k), 1, attrCap());
      if (t === "green") keyHits.push(k);
      else if (t === "blue") blue.push(k);
      else if (t === "red") red.push(k);
      if (cap - cur <= 0) capped.push(k);
      if (n.focus) value += Math.max(0, g) * (0.35 + want) * (cap - cur > 0 ? 1 : 0.25);
    }
    let costKey = 0;
    if (n.cost) for (const k in n.cost) if ((W[k] || 0) / top >= 0.8) costKey++;
    value -= costKey * 1.2;
    return {
      key,
      name: n.name,
      value: +value.toFixed(2),
      keyHits,
      blue,
      red,
      capped,
      focus: foc,
      costKey,
      balanced: !n.focus
    };
  }
  function boardGradesV133(e, sug) {
    const all = Object.keys(PROGRAMS).map(k => trainGradeV133(e, k)),
      best = Math.max(0.01, ...all.map(g => g.value));
    for (const g of all) {
      const capShare = g.capped.length / Math.max(1, g.focus.length),
        P = e.pos;
      g.rel = +(g.value / best).toFixed(2);
      const rel = g.rel; // the rounded number is the one the tier reads, so the board and its check agree
      g.tier =
        g.key === sug
          ? "green"
          : g.balanced
            ? "blue"
            : g.keyHits.length >= 2 && rel >= 0.5 && capShare < 0.5
              ? "green"
              : rel >= 0.3 && capShare < 0.5 && (g.keyHits.length || g.blue.length >= 2)
                ? "blue"
                : "red";
      const nm = ks => ks.map(k => ATTR_INFO[k].name).join(", ");
      g.why = g.balanced
        ? "Even growth on every stat. Never wrong, never the fastest road to the stats a " + P + " is graded on."
        : g.tier === "green"
          ? g.keyHits.length
            ? `Pushes ${g.keyHits.length} key stat${g.keyHits.length === 1 ? "" : "s"} for a ${P} — ${nm(g.keyHits)} — with room under the cap${g.keyHits.length === 1 ? "" : "s"}.`
            : `The coach's pick — ${nm(g.focus.slice(0, 3))} is where the sheet is furthest behind.`
          : g.tier === "blue"
            ? `A real gain — ${nm([...g.keyHits, ...g.blue].slice(0, 3)) || "across the sheet"} — but ${g.keyHits.length ? "only one stat a " + P + " is graded on" : "nothing a " + P + " is graded on"}${g.capped.length ? ", and " + nm(g.capped) + " already at cap" : ""}.`
            : g.capped.length >= Math.ceil(g.focus.length / 2)
              ? `Mostly capped: ${nm(g.capped)} ${g.capped.length === 1 ? "is" : "are"} at the soft cap, so every point here costs double.`
              : g.costKey
                ? `Trades away a stat a ${P} is graded on. The growth does not pay for the loss.`
                : `Minor for a ${P}: ${nm(g.focus.slice(0, 3))} ${g.focus.length === 1 ? "is" : "are"} secondary at your position.`;
    }
    all.sort((a, b) => b.value - a.value);
    return {
      all,
      by: Object.fromEntries(all.map(g => [g.key, g])),
      sug,
      best,
      runner: all.find(g => g.key !== sug && !g.balanced) || null
    };
  }
  // the pick: the best graded season for THIS sheet — the body's own rule (v124) still sends a breaking-down man to Conditioning
  const HiV124 = recommendTraining;
  recommendTraining = function (e) {
    try {
      const top = trainScoreV124(e)[0];
      if (top && top.key === "injuryResist") return "conditioning";
      const all = Object.keys(PROGRAMS)
        .filter(k => PROGRAMS[k].focus)
        .map(k => trainGradeV133(e, k))
        .sort((a, b) => b.value - a.value);
      if (all.length && all[0].value > 0) return all[0].key;
    } catch (_) {}
    return HiV124(e);
  };
  window.__V133 = {
    grade: (e, k) => trainGradeV133(e || (state && state.player), k),
    board: e => {
      const p = e || (state && state.player);
      return boardGradesV133(p, recommendTraining(p));
    },
    attr: (e, k, g, gm) => attrTierV133(e || (state && state.player), k, g, gm),
    keys: e => keyStatsV133(e || (state && state.player)),
    tiers: TIER_V133,
    stage: typeof growStageV133 == "function" ? growStageV133 : null
  };
  screenTraining = function () {
    const e = state.player,
      t = LEVELS[e.level],
      sug = recommendTraining(e),
      key = tpSelV113 && PROGRAMS[tpSelV113] ? tpSelV113 : sug,
      GR = boardGradesV133(e, sug);
    tpSelV113 = key;
    byId("screen").innerHTML = `
    <div class="eyebrow">${t.name} · Offseason</div>
    <div class="h1">Choose Your Training</div>
    <div class="sub">This is where careers are built. Tap a program to see the season it would give you — the bars below are your stats today, and the colour on each bar is what it adds: <b style="color:#7fe0a0">green</b> for a key stat with room, <b style="color:#7ec8ff">blue</b> for a solid gain, <b style="color:#ff8a80">red</b> for a stat that is capped or barely moves. Nothing is locked until you confirm.<br><span style="color:var(--chalk-dim)">Each stat shows what your next <b>+1</b> costs in skill points: <b style="color:#7fe0a0">1 pt</b> below its soft cap, then <b style="color:#f0bb45">2</b>, <b style="color:#ff9a5a">3</b>, <b style="color:#ff8a80">4+</b> above it. Priority growth into a capped stat is growth you cannot afford to keep.</span></div>
    ${(() => {
      const w = trainWhyV124(e),
        g = GR.by[sug];
      return `<div class="threshold-note tp-note-v133" style="margin-bottom:8px">\ud83d\udcdd <b>COACH SUGGESTS ${PROGRAMS[sug].name.toUpperCase()}</b> \u00b7 ${g ? g.why : ""}${w ? ` <span class="tp-need-v133">Top need: ${w.line.replace(/^Train /, "")}</span>` : ""}${GR.runner ? ` <span class="tp-runner-v133">Runner-up: <b>${GR.runner.name}</b>.</span>` : ""}</div>
      <div class="tp-tierkey-v133"><span><b style="background:${TIER_V133.green.col}"></b>GREEN · SEVERAL KEY STATS, ROOM TO GROW</span><span><b style="background:${TIER_V133.blue.col}"></b>BLUE · A SOLID, MODERATE GAIN</span><span><b style="background:${TIER_V133.red.col}"></b>RED · MINOR, OR CAPPED OUT</span></div>`;
    })()}
    <div class="tp-grid-v113">
      ${Object.entries(PROGRAMS)
        .map(([s, n]) => {
          const [risk, rc] = tpRiskV113(n),
            g = GR.by[s],
            T = TIER_V133[g.tier];
          return `<div class="pos-card train-card tp-tile-v113 tier-${g.tier}${s === key ? " sel" : ""}${s === sug ? " best" : ""}" onclick="previewTraining('${s}')" title="${g.why.replace(/"/g, "&quot;")}">
          ${s === sug ? '<i class="tp-pick-v133">COACH\'S PICK</i>' : ""}
          ${window.RIB_SKILL_ICO ? window.RIB_SKILL_ICO("tp_" + s, n.icon, 46) : `<span style="font-size:24px">${n.icon}</span>`}
          <div class="tp-tname-v113">${n.name}</div>
          <div class="tp-trisk-v113" style="color:${rc}">${PLAN_FATE_V124[s] ? `\ud83c\udfb2 ${Math.round(planFateOddsV124(s) * 100)}%` : risk}</div>
          <div class="tp-tier-v133" data-tier="${g.tier}" style="color:${T.col}">${g.tier === "green" && g.keyHits.length ? `${T.name} \u00d7${g.keyHits.length}` : g.tier === "red" && g.capped.length >= Math.ceil(g.focus.length / 2) ? "CAPPED" : T.name}</div>
        </div>`;
        })
        .join("")}
    </div>
    ${tpPanelV113(e, key, sug)}
  `;
    byId("dock").innerHTML =
      `<button class="btn" onclick="confirmTraining()">CONFIRM TRAINING · ${PROGRAMS[key].name}</button>`;
  };
  // the preview costs the player nothing; only the dock's button reaches chooseTraining
  window.previewTraining = function (k) {
    if (!PROGRAMS[k]) return;
    tpSelV113 = k;
    render();
    const el = document.querySelector(".tp-panel-v113");
    el && el.scrollIntoView({ behavior: "smooth", block: "nearest" });
  };
  window.confirmTraining = function () {
    const k = tpSelV113 && PROGRAMS[tpSelV113] ? tpSelV113 : recommendTraining(state.player);
    tpSelV113 = null;
    chooseTraining(k);
  };
  // a fresh trip to the board starts on the suggestion again, not on last season's preview
  const tpStartV113 = startSeason;
  startSeason = function () {
    tpSelV113 = null;
    return tpStartV113.apply(this, arguments);
  };
  window.startSeason = startSeason;
  window.__V124 = {
    fate: PLAN_FATE_V124,
    odds: planFateOddsV124,
    expect: planFateExpectV124,
    roll: (e, k) =>
      rollPlanFateV124(
        e || (state && state.player),
        k || (state && state.player && state.player.training) || "balanced"
      ),
    season: (e, k) =>
      planFateSeasonV124(
        e || (state && state.player),
        k || (state && state.player && state.player.training) || "balanced"
      ),
    say: planFateSayV124,
    score: e => trainScoreV124(e || (state && state.player)),
    why: e => trainWhyV124(e || (state && state.player)),
    rec: e => recommendTraining(e || (state && state.player)),
    trade: (e, k) => planTradeV124(e || (state && state.player), k),
    progFor: PROG_FOR_V124
  };
  window.__V113 = {
    get sel() {
      return tpSelV113;
    },
    set sel(v) {
      tpSelV113 = v;
    },
    preview: k => window.previewTraining(k),
    panel: tpPanelV113,
    row: tpRowV113
  };
})();
