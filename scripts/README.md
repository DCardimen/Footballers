# Dev scripts

Headless Playwright helpers. All of them drive the running dev server —
start `npm run dev` first, then `node scripts/<name>.mjs` from the repo root.
Chromium is launched from `/opt/pw-browsers/chromium` (pre-installed; no
`playwright install` needed). Every check prints JSON plus a `page errors`
line — treat any page error as a failure even if the numbers look right.

`movementcheck.mjs` and `equaltalentcheck.mjs` are the exceptions. The movement
check loads the pure choreography/FieldSim block directly in Node and can compare
an older revision with `node scripts/movementcheck.mjs --git-ref=<commit>`.
The equal-talent check launches Chromium but injects the real engine directly,
so it does not need a dev server; set `CHROME_PATH` when Chromium is elsewhere.

## Checks (assert game behavior)

| Script | What it verifies |
|---|---|
| `creditcheck.mjs` | **Tackle-credit truth.** Sims 60 games as an LB, wraps `__FieldSim.run/pass` to record who the sim actually named tackler/assist, and asserts the credited tackle stat never exceeds sim-truth + sacks. Exits non-zero on violation. |
| `statcreditcheck.mjs` | Box score may only credit plays the user's roster actor actually made or was in the pile for (all stats, several positions). |
| `tacklecheck.mjs` | Solo/gang tackle split (~70/30 target) and whiff / truck / stiff-arm / stagger / big-hit rates across ~3,000 run plays. |
| `jukecheck.mjs` | Stat gaps drive evasion: one-on-one juke probability across superstar/scrub matchups. |
| `simcheck.mjs` | Batch-runs the emergent game engine (`window.__simGameV2`) — score, pace, and yardage distributions. |
| `blowoutcheck.mjs` | v76 — the margin curve. Samples BOTH score paths (the live engine `__simGameV2` and the quick generator `ia()`) across prestige/opponent/level/seed, bins by the observed scoreboard-OVR gap, and asserts the measured margin tracks ~0.7 points per OVR over the +2..+14 bands, that scoring stays recognisable, that five-score blowouts are rare, and that a +10 edge can still produce a statement win. Scores measured BAND MEANS rather than a fitted line — the relationship is convex, so one line across the whole range is dragged up by the tail and misreports the ordinary band. Past +18 the curve is reported but not gated. `BLOW_N`, `BLOW_SLOPE`, `BLOW_CURVE_TOL` and `BLOW_TUNE='{"key":val}'` override the sample size, the target and the tunables, so retuning happens against the gating harness rather than a separate one. |
| `equaltalentcheck.mjs` | Loads the real full-game engine with exact mirrored rosters and asserts fair wins, realistic scoring, YPC, completion/YPA, interceptions, sacks, and blowout frequency. Runs without a dev server. |
| `realismprobe.mjs` | The v30 realism dashboard over 60 full games: yards-per-carry + run-distance histogram, sacks/scrambles, punts vs FG attempts, penalty counts BY TYPE (holding/DPI/face-mask are flag-on-the-play), and average scores. Run after any tuning that touches the run game, kicking decisions, or penalties. |
| `movementcheck.mjs` | Ten seeded 120-play batches (1,200 plays total): exact sideline spots, no out-of-field frames, first-frame offensive TD/pick-six crossings, route-break reactions, directional cuts/bad angles, low/mid/elite acceleration curves, and run/pass/YAC stability. Pure Node; no server required. |
| `renderpathcheck.mjs` | Fraction of plays rendering from the FieldSim agent log vs falling back to the legacy choreographer (healthy: ~87–90%). Run when sim changes don't show on screen. |
| `growthcheck.mjs` | v42 Growth Decisions entertainment probe: Monte-Carlos the outcome generator (mix, magnitudes, durations, permanents, story variety) via `window.__GROWTH_V42.simulate`. |
| `injurycheck.mjs` | Serious injuries force DNP weeks, heal on schedule, clear the worn flag. |
| `menu-integration-check.mjs` | v89 main menu on the real `index.html`: every picture decoded, the drawn wordmark / swash / six legacy icons actually in use (and the wall slogan coming from the photograph, not from the DOM), the no-career card, a fresh career's name / OVR / season dots / latest row from the v89 feed (a sat-out week must say DID NOT PLAY rather than carry a score), team tints in picture pixels with the emblem on the helmet, every tile / nav link / card routes to its view and the same menu returns (compared on a stable signature, since the milestone board is live), the footer reachable on a phone, no page errors or failed requests. `MENU_INTEGRATION_URL` overrides the target. |
| `emblemcheck.mjs` | v44 team emblems, full surface audit: every generated nickname resolves to the right emblem (Wolves → wolf), every emblem's matched palette exists, the sheet is baked (data URL — no server file), creator tiles/preview stay sized and contained at 520px AND 320px, palette picks visibly update the live preview, emblem picks select+✓ their palette, pregame chips are 44px inside their tiles, scoreboard chips 28–40px inside the bar, and the midfield crest composites at ~44% field width. |
| `readcheck.mjs` | v81 ball awareness + v82 systems, pure Node (no server). v82: twists are run and resolved, a spy only shadows a mobile QB, protection calls and chips happen, the shell rotates and presses and can fool the QB, the pocket moves (step-ups, rollouts, a taken sack under 5%), the catch point is contested (swats), bounces are rare, effort and pile pushes show, the back presses a closed hole, punts/kickoffs/field goals resolve on the field with football fair-catch, return and block rates and no frame off the field. v81: every defender diagnoses the play on his own clock (reads spread, linebackers before corners, awareness orders them), draws and play action produce bites that discipline resists and a draw is read later than a straight run, blocks resolve at the point of attack (push/drive/lost, pancakes rare, a dominant line wins more), the designed hole opens and the line finds second-level work, and the run curve stays a football curve with play action paying out. Exits non-zero on any failure. |
| `reactioncheck.mjs` | v56 reaction time: the route-break curve separates every stat level (the old hard clamp made stat 10 and 50 differ by 17ms on a sharp break — now 89ms), the reaction blend leans on quickness while recognition leans on awareness, reaction is position-aware (CB 1.14 → DT 0.86), and first-step latency **actually fires** in the sim (`reactMs` was dead code). Also asserts the **scoreboard** stays in a football range — the first version of the latency passed every other assertion while games finished 251-249. Exits non-zero on any failure. |
| `routecheck.mjs` | v55 route tree: drives the real FieldSim, records every assigned route and the path actually walked in the sim's own coordinates, and checks they match. Asserts the tree carries 45 shapes × 3 releases × 3 depth tiers (405 combinations, ~40× the previous 10), that every name a concept can call is a real shape (`cross` used to fall through to a straight line), that the variety reaches the field, that receivers hit **every waypoint, in sequence**, and that they keep working after the route instead of parking on the last point. Exits non-zero on any failure. |
| `availabilitycheck.mjs` | v54 availability: the injury model scales with the opponent gap (25 above ≈ 40% safer, outmatched ≈ 40% riskier), the condition swing is exactly +5% fresh / −10% worn, the season distribution lands on the design target (~1.3 games missed, 1–2 the common case, season-enders under 1%), and a **real played season** actually loses games — DNP, no stats. Exits non-zero on any failure. |
| `postgamecheck.mjs` | v53 post-game card: plays two LIVE weeks (the quick sim bypasses the card entirely) and asserts SEASON TOTALS leads both the game grade and the single-game box, that the season line states how many games it covers and the record, that **this** game is already in that record (the week is only finalised after the card is dismissed, so its result comes off the live scoreboard), and that a second game reads "through 2 games" — counted once, not twice. Exits non-zero on any failure. |
| `rankcheck.mjs` | v52 national rank vs the leaders board: asserts both describe the same world — every level ranks against the population on the level table (`A[].slots`), the positional pool is exactly that split 9 ways, the rank card tracks the leaders board within a small factor at every level, producing more never ranks you worse, and a level with no stat line still ranks sanely off the rating alone. Screenshots the leaders screen. Exits non-zero on any failure. |
| `refcheck.mjs` | v45 referee crew + v49 ref art: drives into a live game and asserts 7 officials take the field (referee in the backfield), that they move during a play, and that a foul spot pulls the nearest official into a flag-throw. Then the v49 pass — every `spr_ref_*` texture comes from the officials' own 64px sheet (no 48px zebra recolor survives), no pose is missing, feet land on the players' foot line (cell y=51), the touchdown / whistle / flag sequences each reach a real texture, and a score plus a dead ball land on two different officials. Writes `_ref_{idle,signal,flag,whistle}.png` crops plus full-field frames. Exits non-zero on any failure. |
| `sidelinecheck.mjs` | v78 team area: the sideline sheet decodes and every packed cell reaches a texture; the apron is populated in CATEGORIES (coaches, trainers, backups in the right kit, a bench row, hydration, medical, equipment racks, storage, coaching tech, the field's own markers); the three lanes run OUTWARD in order and all stay in front of the stand; nothing but the pylons stands on the playing surface; every sprite draws above the stands and below the players; the layout is **seeded**, so the next snap does not reshuffle the bench, while the chain crew follows the ball and its box shows the down actually being played. Then the v79 layer: every sprite casts a contact shadow and carries a lighting tint (the far end measurably dimmer than the near end); benches and racks on the two banks FACE THE FIELD (mirrored per bank, asserted by scaleX SIGN per bank — low-vv is always screen-left because crowdProject carries no VDIR mirror, and "the two banks differ" was also true when both faced away); the benches are occupied, every sitter rides his own seat (same field depth, in front of the backrest) and EVERYONE watches the field — sitters all in profile, standing backups mostly so, and every profile faces the touchline on both banks; a `td` through `sideReact` spikes the excitement and it decays back; the staff kit recolors to the team primary while warm skin/khaki pixels stay untouched; the painted apron is measured on the warp canvas itself (`sidePaintHalf` really lands on the art's own painted touchline; kit-row shade darker than open apron), and NO sprite box crosses that painted line — the worst overhang across every sprite is measured in screen px, with the pylons alone allowed on the line; and the weather hook is driven end to end (rain → ponchos and no towels, snow → extra heaters and no fans). Then the fallback: a blocked sheet leaves the bare apron v57 shipped, with players still on the field and no page errors. Exits non-zero on any failure. |
| `declarecheck.mjs` | v77: a failed declare is the end of the career. Walks a real career to a declare, forces the miss, and asserts it routes to a career-end screen with one way out (no Determination bonus, no path back), that the screen summarises career totals AND the best season, and that the seasonLogV77 archive holds one row per finished season with rates averaged rather than summed. Also asserts the one-shot stakes are stated **before** the roll. Exits non-zero on any failure. |
| `crowdcheck.mjs` | v57 crowd stands: the sheet decodes into sections on **both** sidelines, the stands **recede and converge** (a far section is shorter than a near one and sits closer to the centre line), and — measured on the **drawn pixels**, not the bounding box, since a diagonal band's box necessarily overhangs the field — no crowd pixel touches the playing surface or the LOS/first-down **line extension**. It also asserts a stand is built behind the **end zone being attacked** (at a single depth, inside the headroom `NSTOP` reserves, and wholly beyond the end line), that the stand carries real **architecture** (stairways running up through it, a concourse darkening every tier, and `crowdArch=0` genuinely removing both — so the test is measuring the structure and not the art), that a **team area** is reserved between the sideline and the stand (room for benches/coaches), that the crowd draws above the LOS/first-down lines but below the players' ground FX, and that **no turf shows between stacked decks** (measured as full-width row ink — the art's own stairwell silhouette leaves plenty of legitimately empty columns). Then the behaviour: `crowdCheer` raises heat and the cheer layer's alpha with it, the roar arrives as a **wave** (near sections first, loudest nearest the play) and decays, the cheer layer **moves** while the bleachers never do, a `td` event reaches the crowd through `fireEvent` while an unremarkable one does not, the three tiers really differ (by **people-share** — raw alpha mass is all bleacher structure and sits within noise) and the level picks one, and a **blocked sheet** degrades to plain grass with no page errors. Exits non-zero on any failure. |
| `wheelcheck.mjs` | v50 decision wheel, all three claims: the wheel's **wedge arcs are the personality weights** (same seed, two characters, the shares move and stay unequal); the **fit roll is its own system** (band odds total 100%, a no-opinion character lands a near-even three-way split, high jive is heavily positive, low jive heavily negative, and over 400 rolled careers the green rate tracks jive); and **tapping runs the rest at 5×** (rate reads 5 mid-roll, and the tapped roll finishes ~4× sooner than the untouched one). Also samples the live canvas ring (classified by hue, which uniform shading preserves) to prove every drawn arc matches the weight that asked for it, and **drives a real week** to prove the wheel is on the PREGAME decision — shortlisted readably, scout pick kept, tap-to-speed working, and the match still starting afterwards. Writes `_wheel.png` + `_wheel_landed.png`. Exits non-zero on any failure. |
| `skillartcheck.mjs` | v65 — the training art on the **offseason board**, the other screen a season's training is chosen on (`wheelcheck` owns the wheel). Walks a real career to "Choose Your Training" and asserts all twelve programs show ART rather than the emoji they replaced, that each shows its OWN scene (no picture twice on a board that lists every program), that the icon rules are a document-level sheet rather than the wheel overlay's, and that switching `__SKILL_V64.off` on puts every row back on its emoji. Also reads the ATLAS both screens share and asserts **every cell's outer 5% ring is empty** — the gutter is what stops a CSS background, which addresses these cells by percentage at the device's DPR, sampling the neighbouring scene into the edge of every tile. Exits non-zero on any failure. |
| `capcheck.mjs` | v67 — the price of a point is stated where the choice is made. Walks a career, parks the Weight Room's focus stats past their soft cap, and asserts every program on the offseason board prices the stats it would push, carries a one-line cost verdict, and that **every badge quotes what `drCost` would actually charge** (a decorative badge that drifts from the real charge is worse than none). Then the upgrade screen: every row states its cap without a hover, both sides of the cap appear, and the readout follows the points as they are spent. |
| `teamqualcheck.mjs` | v68 — a maxed team-quality tree is an edge, not the game. Drives the REAL score generator over 6,000 games with every team-quality node maxed, at the old dial and at `TU("teamQualK")`, and asserts the new setting is ~10x weaker in points per game, that it still does something (a nerf, not a deletion), and that the win rate moves by an edge rather than a landslide. |
| `bobcheck.mjs` | v70 — the you-marker is a crystal over the head, not an aura on the turf. Asserts none of the v18 ground FX survive, that the plain foot ring does, that the plumbob floats above the sprite and draws over the players (so a pile cannot bury it), that it actually TURNS (its recorded silhouette half-width swings frame to frame), and that a gassed player gets a different crystal. Saves a zoomed crop. |
| `endzonecheck.mjs` | v72 — the painted field and the simulated field are one field. Measures the turf art's goal-line rows **independently** (so it cannot just agree with whatever the game measured), asserts the game found the same rows off the clean art, that the sim's goal lines map exactly onto the painted ones, and — end to end, on the warped canvas — that the painted far goal line lands where the projection puts the sim's. Also proves the old full-height map really was yards out. |
| `bodycheck.mjs` | v73 — the injury section answers the question it exists for. Asserts the net figure moves with the body in the right direction and NET POSITIVE / NEUTRAL / NET NEGATIVE read correctly, that the swing is quoted off the multiplier the game actually plays with, that the risk rises with fatigue like the model it reads, that one more point of durability measurably lowers it (the skill-point hook is real), and that a game the body took a bite out of says so afterwards on both the card and the schedule row. |
| `v85check.mjs` | v85 — ratings past 99 (a prestiged NFL side clears 99 on `Wr` and `teamPairV76`), the wheel in the background (quick play and sim-the-rest book every week through `ca()` with a `wheelV85` record and nothing on screen), the body on the sheet (effective values, the cut on the track, the injury-risk badge quoting `injChanceV54`, the pregame list agreeing), the season projection (positive, plan-sensitive, hollow bars with `▹+N`), and `bookLiveGameV85` keeping the watched game. |
| `scrollcheck.mjs` | v75 — how far each screen overflows a phone. Walks a real career, visits every screen the loop reaches, finds the element that actually scrolls (`<html>` carries `overflow:hidden` here, so the document never is) and reports the overflow **in screens**. Fails when any screen needs more than `SCROLL_LIMIT` (1.35) screens of scroll. `SCROLL_W`/`SCROLL_H` set the viewport. |
| `hscheck.mjs` | v46 Score Attack, end-to-end through the real UI: menu button navigates to the mode, a run plays and survives/ends correctly, the Steady + Glory paths resolve without JS errors, and the best score persists to the save object and to storage. Exits non-zero on any failure. |
| `hsprobe.mjs` | v46 Score Attack **scoring calibration**: runs the ship scoring formula (`hsScore`, kept identical to the copy baked into `index.html`) over many resolved games and prints per-position + overall percentile bands, so the survival-threshold ramp stays tuned to real engine output. |
| `lbcheck.mjs` | v47 Leaderboards, end-to-end on the mock backend: a Score Attack run auto-submits, the score appears on the Global board (sorted, tops the seeds), Weekly + Per-position filters work, the board screen + tabs render, and the player name persists. Exits non-zero on any failure. |
| `dailycheck.mjs` | v48 Daily Challenge: proves the **browser engine == the canonical `daily-engine.mjs`** over a 160-case parity matrix (no drift), plus determinism, the full 5-round play flow, one-attempt-per-day lock, daily-board submission, and that a tampered score is rejected. |
| `replay.mjs` | **Server-replay anti-cheat**, runnable in Node: recomputes a daily score from `(daySeed, choices)` via `daily-engine.mjs` and accepts only exact matches — the same logic the `verify-daily` Edge Function runs. `node scripts/replay.mjs` self-tests; `node scripts/replay.mjs <seed> <choices> <claimed>` verifies one submission. |

## Utilities (look at the game)

| Script | What it does |
|---|---|
| `shot.mjs` | Boot headless and screenshot: `node scripts/shot.mjs [out.png] [url]` (also `npm run shot`). |
| `walk.mjs` | Click through screens by button text: `node scripts/walk.mjs "START NEW CAREER" "ARCH" …`, screenshotting each step. |
| `hsshot.mjs` | Screenshot the v46 Score Attack screens (intro / round / result / over) for visual QA; seeds a save with the first-run tutorial dismissed. |
| `genicons.mjs` | Rasterize `public/icon.svg` into the manifest / iOS / store PNG icon sizes. Re-run after replacing the placeholder icon art. |
| `lbshot.mjs` | Screenshot the v47 Leaderboard screen (Global + Per-position) for visual QA. |
| `explore.mjs` | Dump visible buttons/text of the current screen — find the exact labels `walk.mjs`/checks should click. |
| `readshot.mjs` | v81 visual QA: walks a real career into a live game (linebacker by default, `READ_POS=CB` etc.), captures the **Phaser framebuffer** every few hundred ms (`scripts/_read_<n>.png`, plus `_read_q.png` the first frame with "?" markers on screen, `_read_block.png` a sim-rendered play mid-trench, and `_read_<kick>.png` the first punt / kickoff / field goal seen; `READ_DEBUG=1` logs each frame's play state), counts the "?" markers actually drawn and reports the current play's event mix and page errors. |
| `v86check.mjs` | v86 — the seven no-new-art animations, watched on a live game for ~90s (`READ_POS`, `V86_MS`, `V86_SHOTS=1` writes `scripts/_v86_post*.png` / `_v86_pre*.png` from the game's own framebuffer): post-play phases run and the 22 men measurably close on the ball, the cadence fires and the front sways, a dropback backpedals, tackles are classed by geometry, the target looks back, the turf wears. |
| `v87check.mjs` | v87 — sims games at CB, LB and QB with the sim wrapped for truth: coverage share by alignment (a corner covers 5–42% of targets, a linebacker <20%), pass break-ups ≤ the sim's swats by you, zero throws to a target behind the QB, opportunity scrambles only into an open lane (and none with `scrOppBase` at 0), safeties flip possession when they occur; then a live segment for the huddle phase, the ring around the QB and the goalposts (`V87_GAMES`, `V87_LIVE_MS`, `V87_SHOTS=1`). |
| `v88check.mjs` | v88 — the promotion curve on its own (#1 near-certain at every level, the last man inside the advancing share a coin flip, well outside single digits, monotonic) and the integration: a combine player who led the country reads ≥90% on the hub declare button, the button's number is the roll's number, the card names the rank, a mid-pack player is not handed the call. |
| `gamerunprobe.mjs` | v81 balance probe: wraps `__FieldSim.run/.pass` under real `__simGameV2` games and prints the sim-level run histogram by concept, pass stats split play-action vs straight dropback, and per-game rush/pass/score averages. `GAMES=30`, `TUNE='{"readBaseMs":480}'` sweeps dials live, `GAME_URL` points it at another build (serve `git show <ref>:index.html` on a second port to compare against a base commit). |
| `live.mjs` | Boot into a live game and capture frames of the broadcast view. |
| `scroll.mjs` | Phone-viewport scroll-through screenshots (layout/overflow checks). |
| `crop.mjs` | High-DPI cropped screenshots of a screen region. |
| `menushot.mjs` | v89 menu QA: screenshots the LIVE main menu (top and bottom) at a set of sizes; `CAREER=1` starts a career and plays three silent weeks first — `SIZES=390x844,900x1100 OUT=/tmp/menu CAREER=1 node scripts/menushot.mjs`. |
| `menu-preview-shot.mjs` | Screenshot + layout metrics of the standalone `menu-preview.html` (`?menuPreview=1` sample data); fails on page errors, a closed asset gate, or a tile count other than six. |
| `kitshot.mjs` | Renders the menu kit in a saturated palette (crimson jersey, gold helmet and pants) so the team tint and its silhouette masks are judged on more than the default slate — `OUT=/tmp/kit.png node scripts/kitshot.mjs`. |
| `build-menu-art.mjs` | Cuts every shipped `public/menu/*.webp` from the originals in `art/menu/`: badges cropped to their coin, icons to their alpha, the swash de-fringed, photographs downscaled. Run it after adding art; never hand-edit `public/menu/`. |
| `bake-menu-into-index.mjs` | Rewrites the `RIB_DIRECT_MENU_*` marker blocks in `index.html` to reference the versioned menu CSS/JS in `public/`. |
| `assemble-pages.mjs` | Builds the exact GitHub Pages site into `_site/` (root index + `public/` + generated menu sheets) and verifies the baked menu references. |
| `crowdshot.mjs` | Screenshot the v57 stands on the **real Phaser framebuffer** (via `renderer.snapshot` — a DOM screenshot of the canvas region grabs whatever HTML overlay sits on top) with the crowd idle, mid-roar and removed, so the three can be compared. `node scripts/crowdshot.mjs [tier]`; `CROWD_TUNE='{"crowdGap":34}'` sweeps dials live and `CROWD_SUFFIX` names the output. |
| `spritekit/pack_crowd.mjs` | Rebuilds `public/rib_crowd_v57.png` + `art/crowd_v57.cellmap.json` from `art/source/crowd stands pixel art.png` (six full-width strips: 3 density tiers x idle/cheer, on a flat magenta matte). Keyed by exact colour, but the key **kills RGB with alpha** (a keyed pixel keeps its magenta and the downscale blends it back as a purple rim) and the dark-magenta back railing is desaturated in place rather than eroded away. Cells are one size, **bottom-aligned**, so a tier's poses overlay pixel for pixel and the crossfade cannot make the stand sink; each entry also carries the **deck pitch** (the seating block's height) the renderer stacks by. Packs at half scale — the renderer maps this art at a horizontal scale of `k`, so it is downsampled ~3x at draw size and the master's full resolution would only cost base64. `CROWD_SCALE` overrides. `CROWD_DEBUG_OUT=path.png` writes a labelled contact sheet. Run `spritekit/bake_crowd.mjs` afterwards. |
| `spritekit/bake_crowd.mjs` | Inlines `public/rib_crowd_v57.png` into `index.html` as `window.__RIB_CROWD_V57` and refreshes the inline `RIB_META_CROWD` cellmap. Idempotent — re-run after every `pack_crowd.mjs` change. |
| `spritekit/pack_sideline.mjs` | Rebuilds `public/rib_side_v78.png` + `art/side_v78.cellmap.json` from the five `art/source/sideline *.png` masters (staff, equipment, gear, medical, officials) — 83 cells: ten coaches, ten trainers, benches and chairs, hydration, medical, equipment racks, storage, coaching tech, the chain crew's markers and the loose kit a touchline collects. The masters are free ARRANGEMENTS on a black matte, not grids (a full-width bench sits above a row of six trunks), so the row/column band detection the other packers use merges half a sheet into one cell; items are found by **connected component** on dilated ink and read back in reading order instead. Keying is by **reachability** — a flood fill inward from the border — not by threshold: the matte is black and so are a black trunk's own shadows, and a threshold test punches holes through the middle of the dark items. Every selection in `PICK` is by INDEX, so `SHEETS` records the component count each master must yield and the run **fails** rather than packing the wrong objects if the segmentation drifts; `SIDE_DEBUG=1` (with optional `SIDE_DEBUG_DIR`) writes a labelled contact sheet per master, which is how the indices were read off. One global `SIDE_SCALE` (0.34) keeps the whole sideline in proportion with itself — the masters are drawn at one scale, a figure ~270px and a bench ~370px. Run `spritekit/bake_sideline.mjs` afterwards. |
| `spritekit/bake_sideline.mjs` | Inlines `public/rib_side_v78.png` into `index.html` as `window.__RIB_SIDE_V78` and refreshes the inline `RIB_META_SIDE` rect cellmap. Idempotent — re-run after every `pack_sideline.mjs` change. |
| `spritekit/pack_skills.mjs` | Rebuilds `public/rib_skill_v64.png` + `art/skill_v64.cellmap.json` from the three uploaded `art/file_*.png` sheets — each a 2x2 grid of isometric training scenes on a flat navy ground, twelve in all, one per training theme. The scenes do not fill their quadrants and are not centred in them, so each is **tight-cropped to its own ink** rather than to its grid cell (a grid crop leaves every icon a different visual weight for no reason but where the artist put it), then fitted into a uniform cell by its longest side. The ground is keyed on the full-size crop **before** the downscale — scaling then keying resamples the navy into every edge as a halo. `SKILL_CELL` sets the packed cell size (144 default; the icons draw at ~44 CSS px, so this is DPR-3 headroom and no more, because the sheet is baked into `index.html` as base64). The `ORDER` array at the bottom is the theme-to-tile mapping — **the one thing to edit** if art for a theme changes. **v65**: each quadrant's ink is labelled into connected components and the ones that both touch a quadrant border and are small next to the main scene are dropped — scenes overhang the 2x2 split, so a neighbour's grass was landing in the crop. And the per-cell gutter is 9% of the cell, not 4%: a CSS background addresses these cells by percentage at the device's DPR and samples a little past the boundary, so a thin gutter shows as a sliver of the next scene down every tile. Run `spritekit/bake_skills.mjs` afterwards. |
| `spritekit/bake_skills.mjs` | Inlines `public/rib_skill_v64.png` into `index.html` as `window.__RIB_SKILL_V64` and refreshes the inline `RIB_META_SKILL` cellmap. Idempotent — re-run after every `pack_skills.mjs` change. |
| `spritekit/pack_wheel.mjs` | Rebuilds `public/rib_wheel_v50.png` + `art/wheel_v50.cellmap.json` from the three `art/source/wheel *.png` / `outcome icons` masters (rim, hub, four pointer-deflection frames, twelve theme icons in `THEMES` order, three outcome seals, two stat arrows). The masters sit on a flat magenta matte, so the key is an exact colour test plus a two-pass fringe erode — no flood-fill needed. Unlike the player/ref atlases this one is **not** a uniform cell grid (a 256px rim beside a 56px icon would waste the sheet), so the cellmap is rect-based and shelf-packed. Run `spritekit/bake_wheel.mjs` afterwards. |
| `spritekit/bake_wheel.mjs` | Inlines `public/rib_wheel_v50.png` into `index.html` as `window.__RIB_WHEEL_V50` and refreshes the inline `RIB_META_WHEEL` rect cellmap. Idempotent — re-run after every `pack_wheel.mjs` change. |
| `spritekit/pack_refs.mjs` | Rebuilds `public/rib_refs_v49.png` + `art/refs_v49.cellmap.json` from `art/source/referee crew pixel art.png` (8×8 officials, keyed off its white checkerboard matte by border flood-fill, packed into 8×8 cells of 64px). One global scale for every frame, feet on a shared foot line, centred on the FOOT centroid so an extended arm never slides the body. Side/diagonal rows are mirrored so unflipped art faces LEFT, the convention `faceMarker` relies on. `REF_DEBUG_OUT=path.png` also writes a labelled contact sheet. Run `spritekit/bake_refs.mjs` afterwards. |
| `spritekit/bake_refs.mjs` | Inlines `public/rib_refs_v49.png` into `index.html` as `window.__RIB_REFS_V49` and refreshes the inline `RIB_META_REF` cellmap. Idempotent — re-run after every `pack_refs.mjs` change. |
| `spritekit/pack_logos.mjs` | Rebuilds `public/rib_logos_v44.png` from the three `art/football-logo-sheet-*.png` masters (90 emblems, 10×9 grid of 128px cells, index order = sheet order) AND re-bakes the sheet into `index.html` (`window.__RIB_LOGOS_V44` data URL — how deployed builds ship it). Re-run only if the master sheets change. |

## Writing a new check

Copy the shape of `tacklecheck.mjs` / `creditcheck.mjs`:

1. `addInitScript` kills the tutorial/onboard overlay.
2. Click through career creation by button text (see `explore.mjs` for labels).
3. In `page.evaluate`, drive the exposed hooks — `window.__simGameV2(perf, pos)`
   for full games, `window.__FieldSim` (wrap `.run`/`.pass` to observe every
   resolved play), `window.RIB_TUNE` to pin tunables.
4. Print a JSON summary, report `page errors`, exit non-zero on failure.
